---
id: 260525-i2i
type: quick
status: ready
phase: quick/260525-i2i
files_modified:
  - src/components/ListingMetaTable.tsx
autonomous: true
must_haves:
  truths:
    - "On PropertyDetailsScreen, the property area (e.g., 120 m²) renders exactly once — in the dedicated 🛏 | 🚿 | 📐 specs row below the ID/Available pill."
  artifacts:
    - path: "src/components/ListingMetaTable.tsx"
      provides: "Extras grid without the duplicate areaSqm Text"
      contains: "no \"m²\" literal in this file"
  key_links:
    - from: "src/components/ListingMetaTable.tsx extrasGrid block"
      to: "src/screens/PropertyDetailsScreen.tsx specs row (line 1047+)"
      via: "single source of truth — only the specs row renders m²"
      pattern: "areaSqm rendered in PropertyDetailsScreen, never in ListingMetaTable"
---

<objective>
On `PropertyDetailsScreen`, the property area renders twice today: once inside `ListingMetaTable.tsx` extrasGrid (lines 177–181, plain `{areaSqm} m²`), and again in the dedicated 🛏 | 🚿 | 📐 specs row at `PropertyDetailsScreen.tsx:1047–1076`. User flagged the duplicate during 260525-ggp QA — the version inside the ID/Available pill area looks unintentional. Remove the `ListingMetaTable.tsx` render so the dedicated specs row (icon + value + unit) is the single source of truth.

Purpose: ship a clean detail-screen layout for the active TestFlight/Play Console build.
Output: `ListingMetaTable.tsx` extrasGrid no longer renders `{areaSqm} m²`. Zero other changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/components/ListingMetaTable.tsx
@src/screens/PropertyDetailsScreen.tsx

<interfaces>
Existing structure in `src/components/ListingMetaTable.tsx`:
- Line 70: `const areaSqm = property?.basics?.areaSqm;` — destructure (KEEP, still used by `hasExtras` on line 81)
- Line 81: `areaSqm != null ||` inside `hasExtras` boolean (KEEP)
- Lines 177–181: the duplicate JSX block to DELETE:
  ```
  {areaSqm != null && (
    <Text style={[styles.extraText, { color: colors.text, fontSize: valueSize }]} numberOfLines={1}>
      {areaSqm} m²
    </Text>
  )}
  ```
- All other extras-grid rows (rooms 163–167, bathroom 168–176, condition 182–186, furnished 187–191, minTerm 192–196, deposit 197–201, prepaymentMonths 202–206, negotiable 207–211) MUST remain unchanged.

Canonical m² render lives at `src/screens/PropertyDetailsScreen.tsx:1070–1074` (📐 icon + `property.basics?.areaSqm ?? '-'` + `m²` label). DO NOT touch.

PropertyCard does not pass `property` to `ListingMetaTable`, so `hasExtras` is false there and the extras grid never renders — confirmed zero visible effect on the listings page.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate areaSqm render from ListingMetaTable extras grid</name>
  <files>src/components/ListingMetaTable.tsx</files>
  <action>Delete lines 177–181 of `src/components/ListingMetaTable.tsx` — the entire `{areaSqm != null && (<Text …>{areaSqm} m²</Text>)}` JSX block inside the `extrasGrid` `<View>`. Keep the `areaSqm` destructure on line 70 and its reference inside `hasExtras` on line 81 (both still used to keep the grid's gating semantics correct). Do NOT modify any other extras-grid rows (rooms, bathroom, condition, furnished, minTerm, deposit, prepaymentMonths, negotiable). Do NOT modify `PropertyDetailsScreen.tsx`. Do NOT change styles, imports, comments outside the deleted lines, or any other file.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tail -20 &amp;&amp; test -z "$(grep -n 'm²' src/components/ListingMetaTable.tsx)" &amp;&amp; echo OK</automated>
  </verify>
  <done>`src/components/ListingMetaTable.tsx` contains no `m²` literal. `npx tsc --noEmit` reports zero errors. `src/screens/PropertyDetailsScreen.tsx` is byte-identical to its pre-task state. Manual sanity: on PropertyDetailsScreen, the area renders exactly once (in the 📐 specs row); PropertyCard listings page is visually unchanged.</done>
</task>

</tasks>

<verification>
- `grep -n "m²" src/components/ListingMetaTable.tsx` returns no matches.
- `grep -n "m²" src/screens/PropertyDetailsScreen.tsx` still matches the canonical specs row (line ~1073).
- `npx tsc --noEmit` is clean.
- `git diff --stat` shows exactly one file changed (`src/components/ListingMetaTable.tsx`) with a small deletion-only diff.
</verification>

<success_criteria>
PropertyDetailsScreen renders the property area exactly once, in the dedicated 🛏 | 🚿 | 📐 specs row. The duplicate inside the ID/Available pill area is gone. Everything else (PropertyCard, other extras-grid rows, KBD-02 keyboard surface, role gating, i18n strings) is untouched.
</success_criteria>

<output>
After completion, create `.planning/quick/260525-i2i-remove-duplicate-m-rendering-from-listin/260525-i2i-SUMMARY.md`.
</output>
