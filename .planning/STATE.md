---
gsd_state_version: 1.0
milestone: v6.0
milestone_name: Filter Variants + Profile Reskin
status: milestone_complete
last_updated: "2026-06-03T18:55:00.000Z"
last_activity: 2026-06-03 -- Quick task 260603-eus shipped (Cascading filter Reset button per handoff; 67/67 filter tests green; pending on-device QA)
progress:
  total_phases: 9
  completed_phases: 9
  total_plans: 29
  completed_plans: 27
  percent: 100
---

# STATE: JayTap

## Project Reference

See: `.planning/PROJECT.md` (updated 2026-05-31 — M6 added as current milestone)

**Core value:** Prospective renters and buyers can reliably browse, filter, and inquire about Bishkek (and KG/KZ/UZ expansion markets) properties on a phone without UI blockers (keyboard covering inputs, navigation getting stuck, forms requesting wrong fields for the property type).

**Current focus:** Phase 16 — profile-reskin-user-grouped-rows-admin-mod-tile-dashboard

## Current Position

Phase: 16
Plan: Not started
Status: Milestone complete
Next: Phase 16 (Profile Reskin) — depends only on Phase 12 (complete since 2026-05-31), unblocked. Run `/gsd-plan-phase 16` to begin.
Last activity: 2026-06-01 — Quick tasks 260601-dqh + 260601-elb QA passed (Guided dual-action footer + header Reset)

## M6 Phase Map (Phases 12–16) — CURRENT MILESTONE

| # | Phase | Requirements | Status |
|---|-------|--------------|--------|
| 12 | Whole-App Palette Migration (Dark + Light) + Visual-Regression Sweep | PAL-01..03 (3) | ✅ Complete 2026-05-31 — foundation phase shipped |
| 13 | Shared Filter Data Model + AsyncStorage Persistence | DATA-01..03 (3) | Planned 2026-05-31 — 2 plans wave-1 parallel; ready for `/gsd-execute-phase 13` |
| 14 | Filter UI Variants (Guided Steps + Cascading Reveal) + HomeScreen Variant Dispatch | FILT-01..03 (3) | ✅ Complete 2026-05-31 |
| 15 | Account Settings Restructure + Filter-Style Picker | SET-01..03 (3) | ✅ Complete 2026-06-01 — Plan 01 (c404111+20f6dd8+998790a) + Plan 02 (2a2f4cb+bf27e16+6584c2d); UAT 7/7 pass; verifier passed |
| 16 | Profile Reskin (User Grouped-Rows + Admin/Mod Tile Dashboard) | PROF-01..03 (3) | Not started — depends on Phase 12 only; unblocked since 2026-05-31 |

Full per-phase goals + success criteria in `.planning/ROADMAP.md § Phase Details`. Per-requirement mapping in `.planning/REQUIREMENTS.md § Traceability (M6)`.

**M6 deferred (Phase B — out of M6 v1 scope):**

- FILT-04 Master–Detail variant
- FILT-05 Sentence Builder variant
- SET-04 Un-gate Master-Detail + Sentence options in the picker

**M6 release:** Out of scope for this roadmap. Will ride M4 Phase 10's hardening cycle OR get a separate release phase added later (user decision).

## M4 Phase Map (Phases 6–10) — CONCURRENT IN-FLIGHT

| # | Phase | Requirements | Status |
|---|-------|--------------|--------|
| 6 | Schema Extension (Backend Mongoose + RN Type Stub + Body-Strip Validator) | SCHEMA-01..04 (4) | ✅ Complete 2026-05-25 |
| 7 | Stepper Component + ContextualListingFlow Integration | FORM-01..05 (5) | ✅ Complete 2026-05-25 |
| 8 | Display Surfaces (PropertyCard + HospitalityCard + PropertyDetailsScreen) | DISP-01..05 (5) | ✅ Complete 2026-05-26 |
| 9 | i18n Audit + Sentinel (Property-Type / Category / Deal-Type Display Strings) | I18N-01..07 (7) | Not started |
| 10 | Hardening + Manual Physical-Device QA + Release v4.0.0 | REL-01..06 (6) | Not started |

## M5 Phase Map (Phase 11+) — CONCURRENT IN-FLIGHT

| # | Phase | Requirements | Status |
|---|-------|--------------|--------|
| — | Property Details Redesign (M5 Phase 1) | n/a — merged outside GSD 2026-05-26 | ✅ Merged outside GSD |
| 11 | Listing Address Geocode (Forward + Reverse) | TBD | ✅ Complete 2026-05-27 |

## Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260603-eus | Reset button for the **Cascading** filter, per new handoff `reset_button_for_cascading_filter.zip` (`design_handoff_filter_redesign/README.md` "The Reset change" + `filters-cascade-live.jsx CascadePanelInner`). Mirrors the Guided-steps header Reset already shipped in 260601-elb. **Shipped (`CascadingFilter.tsx`):** new top row of the inline panel — `FILTERS` section label (left) + Reset pill (right), above the Deal toggle. Reset = `RotateCcw` icon (size 15) + "Reset" label, pill `height 32` / `paddingHorizontal 12` / `borderRadius 999` / transparent bg / no border / `14px·600` / `gap 6`; enabled → `colors.filterAccent` (project periwinkle, NOT the prototype's coral — README says map design tokens onto ours), disabled → `colors.textTertiary` + `opacity 0.5`. `isFilterDefault = transactionType==='rent' && selectedCategory==='Residential' && types.length===0` gates disabled; `handleReset` → `setTransactionType('rent')` → `setSelectedCategory('Residential')` → `setTypes([])` (**Pitfall 4** preserved: category before types; no `setStep` — cascading has no stepper; reset is a pure live-state clear). No HomeScreen change (CascadingFilter already receives all setters). **i18n:** reused `filters.reset` (Reset/Сбросить) + added `filters.cascading.filtersHeader` = FILTERS/ФИЛЬТРЫ (matches existing cascading uppercase-header convention; EN/RU parity). **Tests (+3 in `CascadingFilter.test.tsx`):** Reset disabled-at-default, enabled-after-change, and tap clears with Pitfall-4 invocationCallOrder. Gates: `tsc` 0 errors in touched files (baseline 17 preserved); `jest src/components/filters` **67/67** across 11 suites (was 64); `scripts/check-i18n-parity.sh` PASS. Out of scope: Guided reset (mirrored not changed), Master-detail/Sentence-builder variants (M6 Phase B deferred), the `.zip` was NOT committed. Single atomic commit. Pending USER on-device QA (FILTERS+Reset row; disabled at default; tap clears to Rent/Residential/no-types + live results update; RU "Сбросить"; light+dark; RotateCcw renders). | 2026-06-03 | 2f28b52 | [260603-eus-add-reset-button-to-cascading-filter-per](./quick/260603-eus-add-reset-button-to-cascading-filter-per/) |
| 260603-emq | Filter UI missing RU translations. Diagnosis corrected the initial framing: **categories** (Residential/Commercial/Hospitality) already translate on the category cards/tabs via `t(CATEGORY_KEY_MAP[cat])` — the real gaps were (a) the **Guided sheet footer `Breadcrumb`** rendered `deal` + **`category`** + `types` ALL raw English (this is where the user saw "Residential"/"Commercial" untranslated, since the cards translate but the footer didn't), and (b) **property TYPE labels** (Apartment/Office/Retail…) rendered raw at the Guided type cards (`GuidedFilterSheet.tsx:327`), Cascading type chips (`CascadingFilter.tsx:224`), and the Home results summary line (`HomeScreen.tsx:654` via `joinTypes`). All needed keys (`propertyType.*`, `category.*`, `filters.deal.*`) already existed in both locales with parity — never called at these sites. **Fix:** `primitives/Breadcrumb.tsx` gained `useLanguage` + a module-level `CATEGORY_KEY` map and now translates deal (`filters.deal.rent|buy`), category (`category.*`), and each type (`propertyType.${lower}`) — prop contract unchanged; type card + type chip now call `t('propertyType.<lower>')`; `joinTypes.ts` gained an optional 4th arg `opts?:{translate?;connective?}` (defaults identity + `'or'` → backward compatible, existing joinTypes tests untouched) and HomeScreen passes `translate` + `connective:t('filters.or')` so the RU summary reads "квартира или дом". One new i18n key `filters.or` = `or`/`или` added to en.ts+ru.ts (only addition; parity preserved). **Tests:** `Breadcrumb.test.tsx` added a `useLanguage` mock mapping keys back to the English words the existing assertions expect (wrong key → falls through → fails, proving wiring); `CascadingFilter.test.tsx` extended its `t` mock so `propertyType.*`→capitalized word, keeping `findChipByLabel('Apartment')` valid (category-tab key matching unchanged). Gates: `tsc --noEmit` 0 errors in touched files (baseline 17 elsewhere preserved); `jest src/components/filters` 64/64 across 11 suites; `jest HomeScreen-filter` 7/7; `scripts/check-i18n-parity.sh` PASS (identical key sets). Out of scope: PropertyCard/PropertyDetailsScreen type rendering (not "inside the filters"). Single atomic commit. Pending USER on-device QA (RU Guided footer "Аренда › Жилая › Квартира"; RU type cards/chips; RU summary "N домов · Аренда · Жилая · квартира или дом"; EN unchanged; light+dark). | 2026-06-03 | ae334e5 | [260603-emq-translate-property-type-and-breadcrumb-l](./quick/260603-emq-translate-property-type-and-breadcrumb-l/) |
| 260603-ecf | HomeScreen header title — stack country/city on separate rows. Follow-up to 260603-dww: the single-line `numberOfLines={1}` title ellipsized long RU names awkwardly ("Кыргызстан • Бишк…"), which the user found hard to read; they wanted country and city on separate rows (and liked the 260603-dww toggle shrink, which stays). **Fix (`src/screens/HomeScreen.tsx` only):** for the selected-city case, replaced the single Text with a stacked `locationTextBlock` (`alignItems:center`) — line 1 country via new `locationCountry` (13pt / weight 400 / `colors.textSecondary` / Georgia-serif / `numberOfLines={1}`), line 2 city + ` ⌄` via new `locationCity` (16pt / weight 600 / `colors.text` / Georgia-serif / `numberOfLines={1}`); dropped the `•` bullet (redundant once stacked). The "All cities" default (no `selectedCity`) stays a single line via existing `locationTitle`. Subtle small-country-over-bold-city hierarchy chosen for readability (the complaint was specifically the ellipsis). Fits without ellipsis because 260603-dww freed ~50pt of title width (right cluster 206→156pt); per-row `numberOfLines={1}` is a guard, not the normal path. Toggle sizes / `rightIcons.gap` / avatar / dropdown Modal untouched. Gates: `tsc --noEmit` 0 errors in HomeScreen (baseline 17 elsewhere preserved), no i18n keys / deps / theme tokens. Single atomic commit. Pending USER on-device QA (RU two-row no-cutoff; EN two-row; "All cities" single row; Казахстан/Алматы + Узбекистан/Ташкент; light+dark; Android serif; dropdown opens; hierarchy feels right). | 2026-06-03 | 0c6edc6 | [260603-ecf-stack-country-and-city-on-separate-rows-](./quick/260603-ecf-stack-country-and-city-on-separate-rows-/) |
| 260603-dww | HomeScreen header — RU country title cut off / wrapped to 3 lines ("Кыргызста" / "н • Бишкек" / stray "˅" = the trailing ⌄). **Root cause (flex math):** `topBar` row = avatar (~40pt) + `locationWrapper` (`flex:1`) + `rightIcons` (intrinsic, no flex). Right cluster = `ThemeToggleSwitch` width 100 + `LanguageToggleSwitch` width 90 + `rightIcons.gap` 16 = **206pt ≈ 52%** of a ~393pt row, starving the `flex:1` title; `locationTitle` had no `numberOfLines`, so the long RU string wrapped. **Fix (shrink toggles + guard title):** `ThemeToggleSwitch` width 100→78 / height 44→40 / radius 24→20 / knob 38→32 (r19→16) / `translateX` outputRange [3,58]→[3,43] (=78−32−3) / text pad 10→7. `LanguageToggleSwitch` width 90→68 / height 44→40 / radius 24→20 / knob 38→32 / `translateX` [3,49]→[3,33] (=68−32−3) / text pad 12→8 / flag fontSize 20→18. `HomeScreen` `rightIcons.gap` 16→10 + `numberOfLines={1}` on the title Text (worst case = single-line tail-ellipsis, never a 3-line wrap). New right cluster 78+10+68 = **156pt ≈ 40%** → title regains ~50pt and "Кыргызстан • Бишкек ⌄" fits one line. Slide endpoints recomputed so the knob lands flush-right in both toggled states (theme 43+32=75=78−3; lang 33+32=65=68−3 — no overshoot/gap); knob vertical centering preserved (absolute knob + container `alignItems:center` static position: 32-in-40 → 4px inset, same pattern as old 38-in-44). Memory `m6-language-pill-stays-in-header` honored — language toggle resized in place, NOT moved to Account Settings. Gates: `tsc --noEmit` 0 errors in the 3 touched files (17 pre-existing errors elsewhere untouched), no new deps / i18n keys / theme tokens. Single atomic commit. Pending USER on-device QA (RU single-line; longest names e.g. "Узбекистан • Ташкент"; toggles tap+animate flush; EN sanity; light+dark; Android serif). | 2026-06-03 | 93434df | [260603-dww-shrink-theme-and-language-toggles-so-ru-](./quick/260603-dww-shrink-theme-and-language-toggles-so-ru-/) |
| 260603-d9y | Favorites tab didn't refresh on reopen — a listing favorited from Home (or anywhere) never appeared on the Favorites page until app reload, most visibly via the bottom-nav heart tab. **Root cause (corrected mid-investigation — NOT the originally-theorized backend POST→GET race):** `FavoritesScreen` is kept-alive by `App.tsx`, not unmounted — mount guard `{(tabEverMounted.favorites \|\| showFavorites) && ...}` holds it in the tree after first open, and `mainStackScreenStyle(visible)` (`App.tsx:786`) hides it with `display:'none'` rather than tearing it down, so React never re-runs mount effects. Its only fetch trigger was `useEffect(() => loadFavorites(), [user])` (`FavoritesScreen.tsx:46`) → runs on first mount + auth change only → re-opening the tab flips `display` back to `flex` but never re-fetches → stale list. No `react-navigation` in this custom state machine, so no `useFocusEffect` was available. **Fix (refetch-on-visible, the manual focus-refetch equivalent):** added optional `isVisible?: boolean` prop to `FavoritesScreenProps` + new `useEffect(() => { if (isVisible) loadFavorites(); }, [isVisible])` (kept the `[user]` effect for first-mount/auth; matched the file's existing `loadFavorites` exhaustive-deps opt-out, suppressed inline); App.tsx passes `isVisible={showFavorites}` (`App.tsx:817`) — `showFavorites` already the single source of truth for "Favorites is the active main-stack screen", so both Profile→Favorites and bottom-nav→Favorites entry paths fire the refetch. No spinner flash on revisit (`loadFavorites()` never sets `loading=true`; list updates in place). Gates: `tsc --noEmit` 0 errors in the two changed files (17 pre-existing errors in unrelated files untouched), eslint 0 new problems (the lone `exhaustive-deps` on the `[user]` effect confirmed pre-existing via stash-lint of the original), no new deps / i18n keys / theme tokens / backend change. Single atomic commit. Pending USER on-device QA (favorite-from-Home → bottom-nav Favorites shows immediately; via Profile too; unfavorite-in-place still works; first-ever open still loads; iOS+Android × light/dark). | 2026-06-03 | 6a13661 | [260603-d9y-fix-favorites-not-refreshing-when-reopen](./quick/260603-d9y-fix-favorites-not-refreshing-when-reopen/) |
| 260531-x3z | Scoped color swap inside the Phase 14 filter UI variants — active deal cards, category cards, type cards in Guided Steps + active category-tab underline + active type chips in Cascading Reveal now render periwinkle blue (`#6f7bff`) instead of brand pink (`#ff5a6f`). Added a parallel `filterAccent / filterAccentSoft / filterAccentLine` triple to `MODE_INDEPENDENT_PALETTE` in `src/theme/colors.ts` (mirrors the existing accent triple — light/dark parity FALSIFIABLE by construction per Phase 12 D-04), then swapped every `colors.accent*` reference inside the two filter components: 10 in `GuidedFilterSheet.tsx` + 2 in `CascadingFilter.tsx`. Every other accent surface (HomeScreen filter button, AccountSettings English toggle, ProfileScreen Create Listing row, Send, RoleBadge, banner accents) stays on the brand pink. Gates green: i18n parity PASS, KBD-02 = 0, filter jest suites 14/14, 0 NET new tsc errors, no hardcoded hex introduced. Three atomic commits (`8ff1808` + `c444526` + `c806c35`). Pending USER on-device QA. | 2026-06-01 | c806c35 | [260531-x3z-filter-variants-accent-swap](./quick/260531-x3z-filter-variants-accent-swap/) |
| 260511-cog | Edit-on-behalf field-only cutover (M3 post-ship hotfix) — PUT /api/moderation/listings/:id stops force-flipping status to 'live', MEDIA_REQUIRED gate consolidated to /approve only. Backend 275/275 green; cross-repo (backend 84016d4 + frontend 01ed067). | 2026-05-11 | 9182a63 | [260511-cog-edit-on-behalf-field-only-cutover-m3-pos](./quick/260511-cog-edit-on-behalf-field-only-cutover-m3-pos/) |
| 260514-rk1 | Photo-required approval gate parity — Moderation Queue row-tap unified to PropertyDetailsScreen so the existing NeedsMediaBanner + disabled Approve fires for zero-photo listings on every entry path. On-device repro surfaced JSX z-order bug (queue overlay stacked above details, hiding the gate); resolved via keep-alive display:'none' under selectedProperty / isContextualListingFlowOpen / isMediaCurationOpen — queue scroll position + filter chips + list cache preserved through the side-trip. Routing fix c2bc982; UX hotfixes 6dd58ef → 2901deb. ModerationQueueScreen suite 15/15 green. | 2026-05-15 | 2901deb | [260514-rk1-apply-photo-required-approval-gate-to-mo](./quick/260514-rk1-apply-photo-required-approval-gate-to-mo/) |
| 260515-djv | Zoomable full-screen property photos — installed react-native-gesture-handler@2.31.2 + @likashefqet/react-native-image-zoom@4.3.0, mounted GestureHandlerRootView at the app root, wrapped each full-screen photo in ImageZoom (pinch / double-tap / pan 1x–5x) with scrollEnabled={!isPhotoZoomed} coordinating swipe-paging vs zoom-pan. Also added a missing onScroll index-sync handler to the full-screen FlatList (pagination indicator never updated on swipe) + Jest mocks for the new native dep. Hospitality listings inherit via shared PropertyDetailsScreen. NOTE: original react-native-awesome-gallery pick swapped — every version pins reanimated@^3.2.0, incompatible with the project's 4.3.1. Code merged (23fe2c8 + 7d976d1). On-device QA (Task 3 blocking checkpoint) passed on iPhone 15 Pro Max + Moto G XT2513V — user-approved 2026-05-15. | 2026-05-15 | e5840df | [260515-djv-user-cannot-zoom-in-to-the-images](./quick/260515-djv-user-cannot-zoom-in-to-the-images/) |
| 260515-iqi | Email verification at sign-up (soft banner) + Firebase deliverability runbook. Reported as one issue ("no email verification; password-recovery email lands in spam"); triaged WITH the user into two deliverables. **A (code):** AuthService `:sendOobCode` VERIFY_EMAIL + `:lookup` REST helpers; AuthContext sends the verification email best-effort after sign-up (a send failure never blocks sign-up) and exposes `emailVerified` + `resendVerificationEmail` + `recheckEmailVerified` (stale-token recovery via `refreshIdToken` then re-lookup, so the banner never sticks); new soft dismissible `EmailVerifyBanner` mounted beside RoleRefreshBanner; 5 EN/RU `auth.verifyEmail.*` keys. No hard gate — existing unverified accounts keep working. **B (docs):** the spam-folder half is a Firebase Console/DNS config issue, NOT a code defect (`sendPasswordResetEmail` already calls the correct endpoint) → committed runbook `scripts/firebase-email-deliverability.md` (template customization → custom domain → custom SMTP + SPF/DKIM/DMARC; covers both the reset and verification emails). No new deps (REST-only, no Firebase SDK). 14/14 new tests + i18n parity green; zero new tsc errors (3 pre-existing `App.tsx` `Property.tours` errors untouched). On-device QA: round 1 surfaced banner-behind-status-bar → safe-area inset (5a600c9 + ae44170 sibling). Round 2 surfaced banner not auto-clearing after verification (cooldown / no-persistence / login-seeding — 8437558) + RU content overflow (column-layout wrap — 5bbc1b8). Re-verified + user-approved 2026-05-15. | 2026-05-15 | 0712bca | [260515-iqi-add-email-verification-at-sign-up-soft-b](./quick/260515-iqi-add-email-verification-at-sign-up-soft-b/) |
| 260525-eva | Fix Photos tile in 2x2 action grid under 3D Virtual Tour card on `PropertyDetailsScreen` — was misusing `media.photos[0]` and pushing a regular hero JPG into the `Tour3DScreen` WebView. Surfaced schema gap: M3 nested `Property` type intentionally has NO tour-photos URL field (legacy `panoramicPhotosUrl` removed at M3 Phase 2 D-20); adding one is milestone-shaped. Disposition: added thin reader `src/utils/getTourPhotosUrl.ts` returning `undefined` today + 4-case co-located test pinning current behavior; wired the Photos tile IIFE through `getTourPhotosUrl(property)` so the tile lands in the same disabled-state path as the Instagram tile when its source field is absent (opacity 0.6, `colors.textSecondary`/`textTertiary`, `onPress=undefined`, `disabled=true`). Single-point cutover for future milestone — only the reader changes when the schema decision lands. No new i18n keys, no theme-token changes, no Property-type schema change. 2 atomic commits (d34c821 reader, f8bac08 tile gate) merged to main as 381ff93. On-device QA passed 2026-05-25 (b2f976e). | 2026-05-25 | 381ff93 | [260525-eva-fix-photos-button-under-3d-virtual-tour-](./quick/260525-eva-fix-photos-button-under-3d-virtual-tour-/) |
| 260525-i2i | Remove duplicate `m²` rendering from `ListingMetaTable.tsx` extrasGrid (lines 177-181). Property area was being rendered twice on `PropertyDetailsScreen` — once awkwardly inside the ID/Available pill area, once in the canonical Beds \| Baths \| m² specs row below. Flagged by user during QA on 260525-ggp. 5-line deletion, 1 commit (15f1010) merged as 5a728a4. `areaSqm` destructure + `hasExtras` reference preserved (still load-bearing for grid gating). Invariants: `m²` count in ListingMetaTable went 1→0; PropertyDetailsScreen specs row m² untouched (still 1); KBD-02 grep gate still 0; tsc 0 net new errors. One process note: first executor Edit hit the main repo path instead of the worktree (CC cwd-drift); recovered cleanly via `git checkout` before re-applying via explicit worktree path. Same QA round also surfaced two schema/UX gaps NOT addressed by this fix — `basics.rooms` is total-rooms not bedroom-count, and `basics.bathroom` is captured only for office/commercial — both seeded as M4 backlog candidates (commit 6462e2b in ROADMAP.md); see memory `central-asia-rooms-vs-bedrooms-convention.md` for the Central Asia cultural context the M4 planner will need. Pending USER on-device QA for the m² placement (non-blocking). | 2026-05-25 | 5a728a4 | [260525-i2i-remove-duplicate-m-rendering-from-listin](./quick/260525-i2i-remove-duplicate-m-rendering-from-listin/) |
| 260525-ggp | Two-layer bedroom/bathroom card-render fix. **L1 read path:** `PropertyCard.tsx:283` + `PropertyDetailsScreen.tsx:1051` both read only `basics?.rooms`, missing the canonical M3 Phase 1 D-07 routing where hotel/hostel listings write room count to `basics.hotelRooms`; transplanted the existing `HospitalityCard.tsx:210` fallback pattern (`basics?.hotelRooms ?? basics?.rooms ?? '-'`) to both surfaces. Bathroom enum-map untouched on both — by M3 design, bathroom is captured only for office/commercial, so `-` for residential/hospitality bathroom is the correct render. **L2 routing:** Pascal-vs-lowercase mismatch in `src/utils/propertyCategory.ts` — `PROPERTY_TYPE_TO_CATEGORY` map keys are Pascal but M3 `Property.ts:29` + `Step1DealAndPropertyType.tsx` write the enum lowercase, so the lookup always missed and ALL listings safe-defaulted to 'Residential'. Hotel listings never routed to `HospitalityCard` pre-fix. Disposition: normalize input case at function entry (Pascal-ify before map read); kept Pascal map keys + exported PROPERTY_TYPES arrays untouched because `HomeScreen.tsx:514` renders them as raw English display labels without i18n wrap (flipping to lowercase would visibly regress the chip labels — flagged as M4 i18n gap in plan Out of Scope). 3 atomic commits (824a6a9 L1 + bd263ea L2-test + e4c0dc4 L2-source) merged to main as 1f609d5. Verification: 24/24 tests green across 3 suites; 0 net new tsc errors; KBD-02 grep gate still 0. **Q4 heads-up for QA:** pre-fix every listing was misclassified as Residential → `HospitalityCard` render paths were effectively dead code for production data → this fix exposes them for the first time, so any latent HospitalityCard layout/data bug will surface during QA (not a revert reason; expect broader QA surface). Pending USER on-device QA (non-blocking). | 2026-05-25 | 1f609d5 | [260525-ggp-fix-bedroom-and-bathroom-counts-not-rend](./quick/260525-ggp-fix-bedroom-and-bathroom-counts-not-rend/) |
| 260525-fjw | Fix iOS keyboard covering chat input on `ChatThreadScreen` + `ChatComposeScreen`. Both screens had `renderHeader()` rendered as a sibling ABOVE the screen-level `KeyboardAvoidingView` (from `react-native-keyboard-controller`) — header outside the KAV's coordinate system → library KAV's keyboard-top-to-its-own-bottom padding math under-compensated → composer rode under keyboard. Orchestrator's first-guess fix (`keyboardVerticalOffset={headerHeight}`) is PROJECT-BANNED by M1 Phase 2 §6 SC3.a / KBD-02 grep gate (must remain 0 in `src/` — held across 3 milestones); `behavior="padding"` is load-bearing per M1 hotfix `47a52b7` (disproved A8 — library KAV no-ops without explicit behavior prop). Project-canonical fix: hoist the KAV outward to be the direct child of `SafeAreaView`, wrapping `renderHeader()` + body, so header sits INSIDE the KAV's coordinate system. Initial hoist: a0dc245 (ChatThreadScreen) + fd588eb (ChatComposeScreen), merged as 656a37d. **Round 1 on-device QA caught a static-layout regression** — the original `ChatComposeScreen` `styles.keyboardView` was `{flex:1, justifyContent:'flex-end'}`, load-bearing when the KAV wrapped only `inputRow`; after the hoist that justification bottom-aligned ALL THREE children (header + previewCard + inputRow), leaving the chrome crammed above the keyboard. Fix-forward inline: 8175715 drops `justifyContent:'flex-end'` and inserts `<View style={{flex:1}}/>` spacer between previewCard and inputRow; 412df18 adds `style={styles.messagesContainer}` (`{flex:1}`) to the ChatThread FlatList (companion latent bug — lost implicit flex after becoming a Fragment-sibling of inputRow). Both KBD-02 + `behavior="padding"` invariants preserved through both rounds. Round 2 user QA pending. | 2026-05-25 | 412df18 | [260525-fjw-fix-ios-keyboard-covering-chat-input-on-](./quick/260525-fjw-fix-ios-keyboard-covering-chat-input-on-/) |
| 260525-x8l | Fix admin/mod "Edit on behalf" backdoor on `<ContextualListingFlow>` — Step 6 "Save and approve" submit was active even with zero photos, letting moderators publish photo-less listings (bypassing the photo-required gate that fires on `PropertyDetailsScreen` + backend `/approve` MEDIA_REQUIRED). Single-file fix on the orchestrator (`src/components/ContextualListingFlow/index.tsx`): `photoCount` derivation (`Infinity` for create/edit-owner so the gate is naturally noop'd for those modes; real count from `props.initialListing?.media?.photos?.length` for edit-mod), `isFinalSubmitBlocked` predicate (Step 6 + edit-mod + photoCount===0), `handleNext` defense-in-depth early-return (mitigates Android long-press race where the `disabled` prop could be momentarily bypassed), `disabled` prop + opacity 0.5 on the submit button, and an inline warning row reusing `commonStyles.modContext*` keys + existing `moderation.needsMediaBanner.{title,body}` i18n keys (zero new strings — EN+RU parity preserved by reuse, not re-derivation). Mirrors 260514-rk1 "single trust boundary on backend, client UI is guidance only" pattern — backend `/approve` MEDIA_REQUIRED gate from 260511-cog untouched. Tests: 8/8 in `integration.test.tsx` (5 new Cases A-E covering gate fires / gate skipped-photos-present / gate skipped-wrong-mode / gate skipped-create / gate scoped-to-Step-6); 137/137 across all `ContextualListingFlow/` suites; i18n parity GREEN; KBD-02 grep gate still 0; tsc 0 new errors in fix scope. Single code commit d990ad7, merged as 625d36e. Process note: same cwd-drift pattern as 260525-i2i recurred (executor's first Edits landed in main repo path instead of worktree; recovered via patch capture → `git apply` to worktree before commit). Pending USER on-device QA (4-cell: iPhone × Moto × EN × RU). | 2026-05-26 | 625d36e | [260525-x8l-fix-edit-on-behalf-stepper-photo-gate](./quick/260525-x8l-fix-edit-on-behalf-stepper-photo-gate/) |
| 260526-ebl | Redesign home-screen `PropertyCard.tsx` per new UX mockup. Visual refactor on a single component + 3 new i18n keys (EN+RU). **Locked decisions:** third stat cell = `basics.areaSqm` (hidden when missing/zero); 3D-tour pill = conditional, only when `media.tourUrl` is set (planner caught legacy-field reference in brief — `is3DTourAvailable`/`tours[]` removed at M3 Phase 2 D-20, pivoted to canonical M3 predicate); status badge = deal-type-aware live indicator (`property.statusBadge.live.sale` / `.rent`, falls back to existing `<StatusPill>` for non-live statuses). **Visual deltas:** dark sale/rent pill w/ red leading dot (was white pill), dark translucent share + heart buttons (was white-bg), floating semi-transparent price + stats overlay pinned to bottom of hero photo with icon-only bed/bath/area cells (each conditional on value present; bed cell still hidden on office/commercial per existing residential rule), restructured below-image region: row 1 = `#<listingId>` + live status badge + conditional `3D ТУР`/`3D Tour` pill, row 2 = serif Georgia title, row 3 = `MapPin` icon + `<district> · <city>` in `textSecondary`. Owner-actions row (Edit/Archive/Unarchive/Delete via `showEditButton`) preserved verbatim, lifted under the location row. Two atomic commits — `024ab4b` (i18n keys) + `9aabc2d` (component refactor) — merged as `c08720f`. Post-merge polish: `e56ef15` swapped the unclear Maximize2 area-icon for explicit `m²` suffix + made `MapPin` use `colors.accent` for pop; `f6fe016` added a `cardPressBlockedRef` ref-flag to swallow the outer card's onPress for 500ms after Share.share() settles (fixes "tap outside share sheet → navigates to details" bug — native iOS modal interrupts RN responder chain, so e.stopPropagation alone wasn't enough). Gates: EN+RU each got 3 new keys (parity script GREEN), legacy-field grep in `PropertyCard.tsx` = 0, KBD-02 `keyboardVerticalOffset` grep in `src/` = 0, tsc 0 new errors in change scope. Pushed to `origin/main` 2026-05-26 along with separate fixes `3a2adb6` (handleOpenTours regression: M3 D-20 cutover missed App.tsx — was still reading `property.tours[]` array, pivoted to `media.tourUrl` and bypasses now-dead TourSelectionScreen for single-URL shape) + `c4df2e9` (Android versionCode 36→37, versionName 3.0.5→3.0.6 release prep). | 2026-05-26 | c08720f | [260526-ebl-redesign-home-card](./quick/260526-ebl-redesign-home-card/) |
| 260530-sud | Country/city picker on HomeScreen — replaces the hardcoded "Bishkek • All / microdistricts" dropdown with a dynamic country-grouped city list fed by `fetchCities()` (GET /locations/cities). Default is "All cities"; selected cities render as `{Country} • {City}` via existing `country.KG/KZ/UZ` keys + the new `home.allCities` key. Filter contract changed from substring-match across 4 fields (district / city / address / description) to exact case-insensitive slug equality on `p.location.city` — microdistrict zero-result trap is resolved by removing the chips (Phase 12 / 260527-0cg already stopped capturing district on new listings, so the chips matched effectively nothing). Freetext search bar still hits `location.district` so legacy listings stay findable. Dead i18n keys `home.bishkekAll` + `home.district` removed (grep confirmed no consumers). One atomic commit `ac2c44b`. Verification: 7/7 jest tests for the new `matchesCityAndSearch` predicate (null=All / exact / case-insensitive / negative-Almaty / freetext-address / legacy-district / sanity); tsc clean on changed files (pre-existing baseline errors in `ChatScreen` / `DeleteListingModal` / `TourSelectionScreen` / `ThemeContext` / `StepperInput.test` preserved unchanged). Out-of-scope follow-ups surfaced: 3 hardcoded `BISHKEK_COORDINATES` map centers (`PropertyMap.tsx:16-18`, `Step2Location.tsx:54`, `PropertyDetailsScreen.tsx:766`), 2 Bishkek-only timezone strings (`profile.bishkekTime`, `schedule.timesBishkek`), 1 Bishkek placeholder (`step5.titlePlaceholder`) — seed candidates for M4/M5 backlog. | 2026-05-31 | ac2c44b | [260530-sud-country-and-city-picker-on-homescreen-dr](./quick/260530-sud-country-and-city-picker-on-homescreen-dr/) |
| 260601-elb | Guided filter sheet — header **Reset** button (Task 6 of `GSD-HANDOFF-filter-reset.md`; Tasks 1–5 shipped in 260601-dqh). Root cause: no reset existed — X just closed with selections intact, and Deal/Category always hold a value, so there was no way back to the broadest state. Fix: header row now reads `Filters … [Reset] [X]` with a quiet text Pressable left of the close X. `isFilterDefault = transactionType === 'rent' && selectedCategory === 'Residential' && types.length === 0` — when true the button is `disabled`, `accessibilityState.disabled=true`, dimmed (`colors.textTertiary`, opacity 0.5); when false it's accent (`colors.filterAccent`). `handleReset` sets `setTransactionType('rent')` → `setSelectedCategory('Residential')` → `setTypes([])` → `setStep(0)` in that exact order — **Pitfall 4 holds** (`setSelectedCategory` before `setTypes`). Reset is NOT an apply — it does NOT call `onClose()`; the sheet stays open at step 0 so the user can keep adjusting. Decision: resets to the *broadest default* (Rent · Residential), not a blank/no-deal state — Deal & Category are never null in the data model; a truly empty state would be a separate, larger change and is out of scope. **i18n:** 1 new key `filters.reset` in EN+RU (`Reset` / `Сбросить`) — `TranslationKeys` auto-extends via `keyof typeof en`, no type edit. **No new deps**, **no new StyleSheet entries** (inline style for the Reset Text so `colors.*` stays in scope — mirrors the `continueBtn` shadowColor pattern from 260601-dqh). Single atomic commit `8379d98` merged as `5e63402`. Gates: i18n parity GREEN, `tsc --noEmit` 0 errors on touched files, jest `src/components/filters` 64/64 across 11 suites (GuidedFilterSheet 13/13 — 12 existing + 1 new asserting disabled-at-default / enabled-after-change / `invocationCallOrder` Pitfall 4 / `setStep(0)` called), KBD-02 grep gate still 0. Invariants: D-04 / D-14 / Pitfall 4 / `isFilterDefault` three-slot conjunction (no weakening) / Stepper / `reached()` / `onStepPress` / `localOpen` / `Animated.parallel` / `animationType="none"` / CascadingFilter — ALL untouched. Two minor in-summary observations (verification-script ergonomics): plan's awk gate uses GNU `\s` so bleeds past `handleReset` on macOS BSD awk — re-verified with Python regex, cat-before-types verdict still PASS; defensive `filterAccent` triple added to test's `useTheme` mock as cosmetic future-proofing (no assertions depend on it). Pending USER on-device QA (8-step matrix from SUMMARY: header layout, Reset enable/disable transitions, reset-stays-open behavior, RU `Сбросить` no-truncate sanity, cascading-style unaffected). | 2026-06-01 | 5e63402 | [260601-elb-guided-sheet-header-reset-button-task-6-](./quick/260601-elb-guided-sheet-header-reset-button-task-6-/) |
| 260601-dqh | Guided filter sheet — "Guide-first" dual-action footer so users can stop at any step. Root cause: `GuidedFilterSheet.tsx` rendered a single footer action and deal/category cards auto-advanced via `setStep(1)`/`setStep(2)` inside their `onPress`, so once you started, your only forward path was finish-or-cancel; users who picked `Rent → Residential` and wanted results there were trapped. Fix: every non-final step now renders **two** actions — a primary `Continue · Add a {category\|type}` that advances + a secondary `Show N homes` that closes (`onPress={onClose}`, D-04 holds). Step 2 (Type) collapses back to a single full-width primary Show. Deal/Category cards stop auto-advancing — Continue is the only forward path (multi-select on Type still toggles as before). **ShowButton primitive** gains `variant?: 'primary' \| 'secondary'` (default primary); secondary = `colors.surface2` bg + 1px border + `colors.text` label + no shadow; primary keeps accent fill but swaps the old `#000 / r26 / y12` halo (was leaking a coral glow under the indigo accent) for an accent-tied `colors.filterAccent / 0.28 / r14 / y6 / elevation 4`. D-14 always-enabled holds for both variants. **Pitfall 4 ordering** (`setSelectedCategory(cat)` before `setTypes([])` in `renderCategoryCard.onPress`) preserved verbatim, comment retained. **i18n:** 2 new keys (`filters.continue.addCategory`, `filters.continue.addType`) added to EN+RU with U+00B7 middle-dot — `Continue · Add a category`/`Continue · Add a type` / `Далее · Категория`/`Далее · Тип`; `filters.showHomes.one/many` reused verbatim. **No new deps** — `ChevronRight` reused from existing `lucide-react-native` import block. Three atomic commits — `7694f1a` (ShowButton variant + shadow) + `5d4be7e` (footer + no auto-advance) + `81756fb` (i18n + tests) — merged as `3a2b0c4`. Gates: i18n parity GREEN, `tsc --noEmit` 0 net new errors on touched files, jest `src/components/filters` 63/63 across 11 suites (ShowButton 6/6, GuidedFilterSheet 12/12), KBD-02 grep gate still 0, only the 6 in-scope files touched. Stepper / `reached()` / `onStepPress` / `localOpen` / `Animated.parallel` / `animationType="none"` UNTOUCHED; CascadingFilter UNTOUCHED. Three minor in-summary deviations documented (overly-strict `accessibilityState` grep softened with comment-stripped verification; `findShowButtons` test helper for `Pressable.testID` triple-match; step-state assertions via observable DOM rather than internal `setStep` mock — matches existing file idiom). Pending USER on-device QA (7-step handoff checklist: iPhone + Android × light/dark × EN/RU, plus RU middle-dot sanity check). | 2026-06-01 | 3a2b0c4 | [260601-dqh-guided-sheet-let-people-stop-at-any-step](./quick/260601-dqh-guided-sheet-let-people-stop-at-any-step/) |
| 260601-1b8 | Cascading filter now scrolls away. Moved `<CascadingFilter>` from `HomeScreen`'s static `renderHeaderContent` (above the FlatList) into the results FlatList's `ListHeaderComponent` via a memoized `renderListHeader` (`useCallback` — load-bearing so the header subtree doesn't remount per render), so the panel scrolls naturally with the list and stops claiming half the screen permanently. Added `listRef = useRef<FlatList<any>>(null)` + `ref={listRef}` on the results list (location-dropdown FlatList at L383 untouched); `toggleFiltersExpanded` now calls `listRef.current?.scrollToOffset({offset:0, animated:true})` on the expand transition so the panel is in view when re-opened from a scrolled-down position (load-bearing per handoff Task 3 — without it the panel mounts off-screen above the fold). Upgraded the pinned result-count line into a tappable `TouchableOpacity` summary breadcrumb: `{count} {t('home.homes')}` row + `Deal · Category · joinTypes(selectedCategory, types, true)` row joined with the middle-dot separator (same as the existing location title), both `numberOfLines={1}`, `onPress={toggleFiltersExpanded}` (re-entry affordance besides the filter icon; reuses Task 3's scroll-to-top). `joinTypes` reused from `src/components/filters/primitives/joinTypes.ts`; `accessibilityLabel` composes count + breadcrumb for VoiceOver/TalkBack. Zero new i18n keys, zero new color tokens, zero new deps. **Invariants held:** `keyboardVerticalOffset` count in `src/` = 0 (KBD-02), `LayoutAnimation.configureNext(...)` preserved as the first line of `toggleFiltersExpanded`, Guided variant (`<GuidedFilterSheet>`) and its Modal pattern untouched, `src/components/filters/CascadingFilter.tsx` source UNTOUCHED (only mount location moves), Hospitality gate (`selectedCategory !== 'Hospitality'`) on `<HospitalitySection>` preserved (now inside `renderListHeader`), 0 net new tsc errors (baseline 21 preserved), i18n parity GREEN, `<CascadingFilter>` mount count in `HomeScreen.tsx` = 1 (inside renderListHeader). 4 atomic commits on worktree branch (`6602830` add listRef + `abe5a50` move mount + `3b21733` scroll-to-top + `85a4675` summary breadcrumb), merged to main as `badc8d7`. QA passed 2026-06-01 (8-step handoff matrix verified — scroll-away, scroll-to-top on expand, summary-row tap re-entry, collapse, live update, Guided unaffected, pull-to-refresh — iPhone + Android × light/dark × EN/RU). | 2026-06-01 | badc8d7 | [260601-1b8-cascading-scroll-away](./quick/260601-1b8-cascading-scroll-away/) |
| 260526-foc | Restore the "Available from" date picker to the listing-creation flow — the M3 (`v3.0`) `ContextualListingFlow` rewrite silently dropped it (`Property.availableDate?: string` still on the type and rendering via `ListingMetaTable` on `PropertyDetailsScreen`, but no input surface captured it anymore, so new listings could never have one — only M1-era data persisted it). **Locked decisions:** placement = Step 6 (Deal Conditions, alongside negotiable/deposit/terms — universal across sale + rent_long + rent_daily); requiredness = OPTIONAL with blank = "available now" (matches existing `ListingMetaTable` "Now"-within-30-days display contract — no new submit gate, no new step-advance blocker); date constraints = today ≤ x ≤ today+2y (sanity cap; out-of-range values silently coerced rather than erroring). **Storage contract:** `FormBag.terms.availableDate?: string` (nested under Step 6 in the form bag for UX adjacency) ↔ `Property.availableDate?: string` (TOP-LEVEL on the Property — preserves existing M3 schema, no backend change). Adapter flattens nested→top on submit and rehydrates top→nested on edit. Empty/undefined → key omitted from payload entirely (backend never sees empty string). **Library:** `@react-native-community/datetimepicker@^8.6.0` (already in package.json since M1, but UNUSED in src/ — no new dep, native modal respects system dark mode automatically). **Process note:** planner explicitly flagged subagent CWD-drift recurrence pattern (memory `subagent-cwd-drift-recurring`); executor prompt prepended `cd "$(git rev-parse --show-toplevel)" &&` to every Bash invocation as defense — landed all 3 commits on the correct worktree branch first try. Three atomic commits — `aa46203` (types + adapters + validator + 11 unit tests A-K) + `f4fb0f1` (4 EN+RU i18n keys in `contextualListing.step6.*` namespace) + `6e06f1a` (Step 6 UI section + native picker integration) — merged as `bd9f964`. Gates: 158/158 ContextualListingFlow tests passing (+21 net new), i18n parity GREEN, tsc 17 errors (baseline preserved, 0 new), KBD-02 grep gate still 0. Manual on-device QA required (24-cell matrix: iOS+Android × sale+rent_long+rent_daily × light+dark × EN+RU × set+clear). | 2026-05-26 | bd9f964 | [260526-foc-bring-back-availabledate-calendar-in-lis](./quick/260526-foc-bring-back-availabledate-calendar-in-lis/) |

## Shipped State (M3 v3.0.x)

- **iOS:** `3.0.0` build 29 in App Store Connect TestFlight Internal Testing (processing)
- **Android:** `3.0.1` versionCode 32 in Google Play Console Internal Testing (processing — Android marketing-version drift from iOS due to Play Console rejecting versionCode 31; M1 D-02 pattern firing twice now per memory `release-android-versioncode-trust-play-console.md`)
- **Backend:** Railway live at SHA `5bf23fe`; 273/273 backend tests passing across 7 suites; `firebase-admin` confirmed absent (3rd consecutive milestone)
- **Git tag:** `v3.0`
- **Atlas:** MongoDB password rotation carries forward from M2 HF-02 (`mongo_revoked_at: 2026-04-29T00:00:00Z`)
- **AWS IAM:** PARTIAL — `jaytap-prod-s3` IAM user used exclusively by JayTap runtime; OLD cross-project shared user retains JayTap-bucket policy (cannot delete; other project depends on it). Re-open condition unchanged.

## Deferred Items (M4 carry-forward)

7 items carried forward from M3 close (see `.planning/milestones/v3.0-ROADMAP.md § Known Gaps at Close` for full detail):

| # | Item | Source | Disposition |
|---|------|--------|-------------|
| 1 | Race-cell test rig (coordinated-curl OR Bluetooth-trigger-pair sync) | M2 close (already deferred once); M3 Plan 05-06 audit | M4 candidate (timing-fragile; opt-in based on M4 surface) |
| 2 | Android `clean bundleRelease` reanimated prefab gotcha — release runbook doc | M2 close; M3 Plan 05-07 close (workaround applied again) | M4 candidate (codify `gradlew :react-native-reanimated:assembleRelease :app:bundleRelease` in `scripts/release-android.md` or `bundleReleaseSafe` gradle wrapper) |
| 3 | AWS IAM cross-project residual | M2 Phase 6 REL-05 | Re-open when other project unblocks scoping the OLD shared IAM user away from JayTap bucket ARN |
| 4 | CARRY-01 banner-latency live device walk | M3 Phase 4 Plan 04-04 verification | Opt-in if regressions surface; automatable parts covered via RTL smoke |
| 5 | CARRY-02 live Atlas uid-match device walk | M3 Phase 4 Plan 04-02 verification | Opt-in if regressions surface; supertest proves invariant in 273/273 |
| 6 | `JayTap-orphan-data-cleanup` (drop pre-patch orphan collections) | M3 Plan 05-01 pre-flight surfaced | M4 backlog (non-blocking; data-hygiene) |
| 7 | `ATLAS-CRED-ROTATION` (defense-in-depth password rotation) | M3 Plan 05-01 pre-flight surfaced | M4 backlog (non-blocking; security-posture cycling; last rotation 2026-04-29 per M2 HF-02) |

## Candidate M4 Scope (Future Requirements)

From `.planning/milestones/v3.0-REQUIREMENTS.md § "Future Requirements (M4+ — deferred)"`:

- KZT + UZS currency support — KG+KZ+UZ market expansion (Almaty / Tashkent serviceAreas)
- Multi-language localization beyond EN+RU (KK + UZ if KZ/UZ markets warrant it)
- 2GIS native map bridge — multi-week effort; plan drafted in `2GIS_BRIDGE_PLAN.md`
- Automated/AI moderation (image/text classifiers)
- Full audit log UI (`moderationLog` + `roleChangeLog` data already captured in M2)
- Push notifications / email notifications for moderation events
- Bulk moderation actions (multi-select approve/reject)
- Real-estate document verification (Avito-style ownership proof — multi-month compliance subproject)

## Conventions Carried Forward (CLAUDE.md + memory)

- **No Firebase SDK** in either repo (RN client OR backend) — REPO RULE per memory `no-firebase-sdk.md`. Backend uses `jose` for JWKS verification.
- **No `react-navigation` migration** — custom `App.tsx` state machine stays.
- **No backend changes in M6** — client-only milestone.
- **MongoDB `userType` is the role authority** — Firebase ID token proves uid only.
- **EN+RU bilingual parity** required for every new UI string (CI gate `scripts/check-i18n-parity.sh`).
- **Manual physical-device QA** on iPhone 15 Pro Max + Moto G XT2513V × EN/RU × light/dark (M6 explicitly requires both modes due to palette migration).
- **Sentinel chain in backend `npm test`:** `actoruid` → `landlord-uid` → `media-stripped` → `i18n-parity` → `create-listing-screen-removed` → jest. **M4 adds RN-client sentinel `check-no-raw-property-type-strings.sh` chained into RN-client jest pre-test step (I18N-06).**
- **M1 D-02 pattern (Android versionCode rejection):** pre-archive Wave-0 query Play Console + TestFlight for highest-accepted version-code per track. Fired twice (M1 + M3); M4 must also re-query at submission time.
- **M1 KBD-02 grep gate:** `keyboardVerticalOffset` count in `src/` must remain 0 (held across 3 milestones).
- **M6 language pill stays in HomeScreen header** — handoff's "after" SearchHeader proposal explicitly rejected (memory `m6-language-pill-stays-in-header.md`).
- **M6 filter variants are the value** — ≥2 variants in v1; collapsing to one rejected (memory `m6-filter-variants-are-the-point.md`).
- **Geographic scope:** KG launch market (Bishkek). KZ + UZ expansion remains M4+ — KZT + UZS currencies deferred.
- **Backend repo location:** `/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services` (same maintainer as RN client). Backend Node ≥22.12 (`nvm use 24` before backend npm/node ops; jose@6 ESM-only).

---

*STATE.md updated 2026-05-31 — M6 v6.0 "Filter Variants + Profile Reskin" roadmap landed via `/gsd-roadmap`. 5 phases (Phases 12–16) covering 15 v1 requirements; 100% coverage validated. Phase 12 (palette migration foundation) ready for `/gsd-plan-phase 12`. M6 starts as a parallel third in-flight milestone alongside M4 (Phases 9–10 open) and M5 (Phase 11 closed 2026-05-27). Prior STATE: M4 roadmap landed 2026-05-25; Phase 6 closed 2026-05-25; Phase 7 closed 2026-05-25; Phase 8 closed 2026-05-26. Prior in-flight session content + accreted execution history archived implicitly in git history (see `git log --oneline` for granular activity).*
