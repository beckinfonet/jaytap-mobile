---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified: []
autonomous: false
requirements: [MOD-02]
must_haves:
  truths:
    - "Production Mongo `--dry-run` output reviewed by operator and matches expected legacy counts"
    - "Live `npm run migrate:listings-m2` run against production MONGO_URI completes without error"
    - "`--verify` subcommand exits 0 — `db.properties.countDocuments({status:{$nin:['pending','live','rejected','archived']}}) === 0` post-condition holds on production data"
    - "Verify output saved as evidence (commit message OR SUMMARY.md) for downstream Plan 03 deploy gate"
    - "Plan 03 (backend code deploy) is BLOCKED until this plan's --verify exits 0"
  artifacts:
    - path: ".planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md"
      provides: "Migration evidence — dry-run + live + verify outputs (commit-logged)"
      contains: "ACCEPTANCE MET (LISTING-LIFECYCLE-M2)"
  key_links:
    - from: "production Mongo"
      to: "src/scripts/migrate-listings-m2.js"
      via: "MONGO_URI environment variable"
      pattern: "npm run migrate:listings-m2"
---

<objective>
**[BLOCKING WAVE]** Run the Wave-0-built `migrate-listings-m2.js` against production MongoDB. This wave is the load-bearing data-reconciliation gate before Plan 03's schema cutover deploys.

Per CONTEXT.md "Derived deploy order" (D-01..D-04 and D-18):
1. Pre-deploy: `npm run migrate:listings-m2 -- --dry-run` — operator reviews output.
2. Pre-deploy: live `npm run migrate:listings-m2` — writes `{status:'draft'} → {status:'pending'}` and `{status:{$exists:false}} → {status:'live'}` per D-04.
3. Pre-deploy: `npm run migrate:listings-m2 -- --verify` — exits 0 (acceptance gate).

Until step 3 returns 0, Plan 03 (backend code deploy with schema enum cutover) is BLOCKED — see 02-RESEARCH.md Pitfall 2: deploying the new enum without the backfill causes any owner editing a legacy 'draft' listing to hit Mongoose ValidationError 500.

This plan is `autonomous: false` because it is fundamentally an operator-supervised production data write. The `checkpoint:human-verify` task in this plan stops Claude before the live write, presents the dry-run output to the operator, and resumes only on explicit "approved" signal.

Purpose: Reconcile legacy listings (M1 'draft' rows + status-missing legacy rows) with the new 4-state enum BEFORE Plan 03's schema validation locks in. Closes 02-VALIDATION.md MOD-02 row.

Output: SUMMARY.md with verbatim console output of all three migration invocations (dry-run, live, verify). The SUMMARY itself becomes the evidence file Plan 03 reads to confirm the gate is open.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-CONTEXT.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-RESEARCH.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-01-SUMMARY.md
@backend-services/JayTap-services/src/scripts/migrate-listings-m2.js

<interfaces>
<!-- Migration script invocation contract from Plan 01 -->

Three subcommands the script supports:

1. `--dry-run` — prints filters + match counts. NO writes.
   Expected output shape:
   ```
   === DRY RUN ===
     draft -> pending: <N1> records
     (no status) -> live: <N2> records

   Post-migration: <N1+N2> records still at non-canonical status (this is hypothetical; would be 0 after the live run).
   (dry-run — would meet acceptance after running without --dry-run)
   ```

2. live (no flag) — performs writes.
   Expected output shape:
   ```
   === MIGRATION ===
     draft -> pending: <N1> records
       updated: <M1>
     (no status) -> live: <N2> records
       updated: <M2>

   Post-migration: 0 records still at non-canonical status.
   ACCEPTANCE MET (LISTING-LIFECYCLE-M2): db.properties.countDocuments({status:{$nin:["pending","live","rejected","archived"]}}) === 0
   ```

3. `--verify` — read-only post-condition check.
   Expected output:
   ```
   Records with non-canonical status: 0
   VERIFY: PASS — LISTING-LIFECYCLE-M2 acceptance met
   ```
   Exit code 0 on PASS, 1 on FAIL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm production MONGO_URI is set + run --dry-run</name>
  <files></files>
  <read_first>
    - backend-services/JayTap-services/src/scripts/migrate-listings-m2.js (the script Plan 01 produced)
    - backend-services/JayTap-services/.env.example (the env-var template — the runtime expects MONGO_URI to be exported in the operator's shell, NOT read from `.env` in production-targeted runs since `.env` is git-ignored per HF-02)
    - .planning/phases/02-listing-lifecycle-status-field-absorption/02-CONTEXT.md (D-03 — manual run, not Railway-hooked)
  </read_first>
  <action>
    Step 1 — Confirm production MONGO_URI is exported in the operator's shell. The agent runs:
    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && nvm use 24 && node -e "console.log('MONGO_URI present:', !!process.env.MONGO_URI, 'starts with:', (process.env.MONGO_URI || '').slice(0, 25))"
    ```
    If MONGO_URI is missing, ABORT this plan with a clear error: "MONGO_URI not set. Operator must export MONGO_URI=mongodb+srv://... before resuming this wave. See HF-02 secret-rotation runbook in 01-SUMMARY for credential location." Then return to the orchestrator with `## CHECKPOINT REACHED`.

    Step 2 — Run the dry-run:
    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && nvm use 24 && npm run migrate:listings-m2 -- --dry-run 2>&1 | tee /tmp/jaytap-migrate-listings-m2-dryrun.log
    ```
    Capture the full stdout+stderr to the log file. Print the log contents inline so the operator (and the next checkpoint task) can see them.

    Step 3 — Sanity-check the dry-run output. The expected line patterns are:
    - `=== DRY RUN ===`
    - `  draft -> pending: <number> records`
    - `  (no status) -> live: <number> records`
    - `(dry-run — would meet acceptance after running without --dry-run)`
    If any of these lines is missing, ABORT and return to the orchestrator with `## PLANNING INCONCLUSIVE` — the script may have hit a Mongo connection error or a runtime error. Surface the error verbatim.

    Do NOT proceed to live writes in this task. The next task (checkpoint:human-verify) gates that.
  </action>
  <verify>
    <automated>test -f /tmp/jaytap-migrate-listings-m2-dryrun.log &amp;&amp; grep -F "=== DRY RUN ===" /tmp/jaytap-migrate-listings-m2-dryrun.log &amp;&amp; grep -F "draft -> pending:" /tmp/jaytap-migrate-listings-m2-dryrun.log &amp;&amp; grep -F "(no status) -> live:" /tmp/jaytap-migrate-listings-m2-dryrun.log &amp;&amp; echo "DRY-RUN OK"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/jaytap-migrate-listings-m2-dryrun.log` exists.
    - The log contains the line `=== DRY RUN ===`.
    - The log contains a line matching `draft -> pending: \d+ records`.
    - The log contains a line matching `\(no status\) -> live: \d+ records`.
    - The log does NOT contain `MongoServerSelectionError` (would indicate connection failure).
    - The log does NOT contain `MIGRATION INCOMPLETE` (would indicate a write attempt — dry-run must NOT write).
    - The agent has surfaced the dry-run match counts inline in the chat for operator review at the next checkpoint.
  </acceptance_criteria>
  <done>
    Dry-run completed successfully. Match counts captured to `/tmp/jaytap-migrate-listings-m2-dryrun.log` AND printed inline. Operator can now review the counts at Task 2's checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — operator reviews dry-run + approves live migration</name>
  <what-built>
    Plan 01 created `migrate-listings-m2.js`. Task 1 of this plan ran `--dry-run` against production Mongo. The dry-run output is captured at `/tmp/jaytap-migrate-listings-m2-dryrun.log` and printed in the prior chat turn.

    The dry-run reports two match counts:
    - `draft -> pending: <N1> records` — legacy M1 'draft' rows that will become 'pending' after live run.
    - `(no status) -> live: <N2> records` — legacy status-missing rows that will become 'live' after live run.

    No writes have occurred yet. The next step is the live `npm run migrate:listings-m2` (without flags), which executes both updateMany operations against production MongoDB. After the live run, `--verify` confirms `db.properties.countDocuments({status:{$nin:['pending','live','rejected','archived']}}) === 0`.
  </what-built>
  <how-to-verify>
    1. Inspect the dry-run match counts (printed in the prior turn AND in `/tmp/jaytap-migrate-listings-m2-dryrun.log`):
       - Are the `draft -> pending` and `(no status) -> live` counts plausible given the M1 production dataset size? (M1 shipped 2026-04-28; the dataset is recent.)
       - Are the counts consistent with the operator's mental model of how many M1-era listings exist?
    2. If counts look anomalously HIGH (e.g., thousands when M1 had dozens) — type "abort" and investigate. The script may be misfiltering.
    3. If counts look anomalously LOW (e.g., 0 / 0) — that may be fine if the production database has been pre-cleaned, OR a sign that MONGO_URI points at staging instead of production. Verify the connection target.
    4. If counts look right — type "approved" to proceed with the live run. The next task will execute `npm run migrate:listings-m2` (without flags) and write the migration.

    **Exact resume command shape after approval:** the agent will run
    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && nvm use 24 && npm run migrate:listings-m2 2>&1 | tee /tmp/jaytap-migrate-listings-m2-live.log
    ```
    immediately upon resume, then run `--verify` as a separate command in the same task. Both outputs are written to log files and committed to the SUMMARY.

    **Reverting:** Phase 1 D-08 precedent — there is no rollback. The migration is one-shot. If you approve and the live run produces unexpected results, the recovery path is a manual mongo `updateMany` rolling the affected `_id`s back to their original `status` values from the live-run log (which lists `updated: <count>` per operation but NOT the per-record _ids — accept this risk; the script's idempotency means a re-run with corrected filters is also safe).
  </how-to-verify>
  <action>This task is a checkpoint:human-verify gate. The agent presents the verification material from `<what-built>` and `<how-to-verify>` to the operator and waits for the resume-signal before proceeding. No code changes occur in this task. See `<how-to-verify>` for the operator workflow.</action>
  <resume-signal>Type "approved" to proceed with live migration, "abort" to halt and investigate, or describe a concern (e.g., "the dry-run hit 50000 'draft' rows, that's wrong, abort")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute live migration + verify</name>
  <files></files>
  <read_first>
    - /tmp/jaytap-migrate-listings-m2-dryrun.log (the dry-run output the operator just approved)
    - .planning/phases/02-listing-lifecycle-status-field-absorption/02-CONTEXT.md (D-04 — the two operations the live run will execute)
  </read_first>
  <action>
    Resumed after operator approval at Task 2. Execute live migration:

    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && nvm use 24 && npm run migrate:listings-m2 2>&1 | tee /tmp/jaytap-migrate-listings-m2-live.log
    ```

    Print the log contents inline. Verify the log contains:
    - `=== MIGRATION ===` (NOT `=== DRY RUN ===`)
    - `updated: <number>` lines for both operations (the actual modifiedCount values from `updateMany`)
    - `Post-migration: 0 records still at non-canonical status.`
    - `ACCEPTANCE MET (LISTING-LIFECYCLE-M2):` line

    If `Post-migration: 0` is NOT in the log OR the script exited non-zero, ABORT and return to the orchestrator with `## PLANNING INCONCLUSIVE`. Surface the full log inline.

    Then run the verify subcommand as the final acceptance gate:

    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && nvm use 24 && npm run migrate:listings-m2 -- --verify 2>&1 | tee /tmp/jaytap-migrate-listings-m2-verify.log; echo "VERIFY EXIT CODE: $?"
    ```

    The verify run MUST output:
    - `Records with non-canonical status: 0`
    - `VERIFY: PASS — LISTING-LIFECYCLE-M2 acceptance met`
    - `VERIFY EXIT CODE: 0`

    If the exit code is non-zero or the PASS line is missing, ABORT.

    Then write a SUMMARY.md at `.planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md` containing the FULL verbatim outputs of all three runs (dry-run, live, verify) inside fenced code blocks. The SUMMARY is the evidence file that Plan 03 references when checking "is the migration gate open?"

    Finally, commit the SUMMARY to git (do NOT include the /tmp/ log files — they're transient):
    ```bash
    git add .planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md
    git commit -m "$(cat <<'EOF'
    docs(02): wave-1 production migration verify=PASS

    Migration migrate-listings-m2.js executed against production Mongo per D-03/D-04:
    - draft -> pending: <N1> records modified
    - (no status) -> live: <N2> records modified
    - --verify: 0 non-canonical status records (acceptance gate met)

    Plan 03 (backend code deploy) gate now open per CONTEXT.md derived deploy order.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```
    Replace `<N1>` and `<N2>` with the actual modifiedCount values from the live log.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md &amp;&amp; grep -F "ACCEPTANCE MET (LISTING-LIFECYCLE-M2)" .planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md &amp;&amp; grep -F "VERIFY: PASS" .planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md &amp;&amp; echo "MIGRATION GATE OPEN"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/jaytap-migrate-listings-m2-live.log` exists and contains the literal line `ACCEPTANCE MET (LISTING-LIFECYCLE-M2)`.
    - `/tmp/jaytap-migrate-listings-m2-verify.log` exists and contains the literal line `VERIFY: PASS — LISTING-LIFECYCLE-M2 acceptance met`.
    - The verify command's exit code was 0 (printed inline as `VERIFY EXIT CODE: 0`).
    - `.planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md` exists.
    - `grep -F "VERIFY: PASS" .planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md` returns 1 line.
    - `grep -F "ACCEPTANCE MET (LISTING-LIFECYCLE-M2)" .planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md` returns 1 line.
    - `git log -1 --format=%s .planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md` returns a commit message containing `02` and `migration` and `verify=PASS`.
  </acceptance_criteria>
  <done>
    Production migration ran cleanly. `--verify` exits 0. SUMMARY.md committed as evidence. Plan 03's deploy gate is now open per CONTEXT.md derived deploy order step 3.

    Maps to 02-VALIDATION.md row: MOD-02 (legacy null absorbed by D-04 backfill — verified empirically against production data).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Wave 1 — production data write)

| Boundary | Description |
|----------|-------------|
| Operator shell → production MongoDB | The migration script's `connectDB()` reads `MONGO_URI` from the operator's shell (NOT from a committed `.env` per HF-02). The connection itself is the trust boundary; mis-pointing MONGO_URI at staging would corrupt staging data without the operator noticing |
| migrate-listings-m2.js → production properties collection | `updateMany` writes touch the production data layer directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V5-Tampering-03 | Tampering | Live updateMany against wrong collection/database | mitigate | Operator-supervised checkpoint:human-verify gate before live run; dry-run output reviewed for sane counts; MONGO_URI pre-flight check at Task 1 prints the URI prefix so the operator can verify "atlas" vs "staging" vs "localhost" before approval. |
| T-V5-Tampering-04 | Tampering | Idempotency under re-run | accept | Filters `{status:'draft'}` and `{status:{$exists:false}}` are mutually exclusive and target only legacy rows; re-running the migration after Plan 03 deploys would match 0 rows because the schema enum no longer accepts 'draft' (writes would still succeed but with modifiedCount: 0). Phase 1 D-08 precedent. |
| T-Repudiation-01 | Repudiation | No per-record audit trail | accept | The migration touches at most a few hundred legacy rows (M1 just shipped); the live log's `updated: <count>` is sufficient evidence. Per-record audit logging would require Phase 4-style moderationLog scaffolding which is out of Phase 2 scope per D-08. |

**No new HTTP routes** at this wave — purely a data migration. The Phase 2 route-level threats (D-05/D-06/D-12 information disclosure, D-22 tampering) ship in Plan 03; this plan's role is to clear the data-layer path so those routes don't break on legacy 'draft' enum-rejected rows.
</threat_model>

<verification>
- All 3 tasks complete per `<acceptance_criteria>`.
- `grep -F "VERIFY: PASS" .planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md` returns 1 line.
- The git commit referencing this plan exists and the SUMMARY.md is checked in.
- 02-VALIDATION.md MOD-02 row upgrades from `❌ Wave 0` to `✅ green` (production data verified — Plan 03 will tighten the schema enum to lock the new state in).
- Plan 03's `depends_on: [02]` is now satisfiable.
</verification>

<success_criteria>
1. `/tmp/jaytap-migrate-listings-m2-dryrun.log` and `/tmp/jaytap-migrate-listings-m2-live.log` and `/tmp/jaytap-migrate-listings-m2-verify.log` all exist and contain the expected output patterns.
2. The verify command exited 0.
3. `02-02-SUMMARY.md` is committed with the full verbatim outputs of all three runs and contains both `ACCEPTANCE MET (LISTING-LIFECYCLE-M2)` and `VERIFY: PASS` lines.
4. The operator's checkpoint:human-verify approval is recorded in the plan's chat history (not a separate file — the resume signal IS the approval).
5. Plan 03 is now unblocked.
</success_criteria>

<output>
After completion: `.planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md` (committed) containing:
- Verbatim dry-run output (fenced code block).
- Operator approval timestamp + signal text.
- Verbatim live-run output (fenced code block).
- Verbatim verify output (fenced code block) including exit code.
- "Plan 03 deploy gate: OPEN" closing line.
</output>
