---
phase: 02
plan: 09
type: execute
wave: 7
depends_on: [03, 07, 08]
files_modified: []
autonomous: false
requirements: [MOD-06, MOD-07, MOD-08, MOD-09]
must_haves:
  truths:
    - "Backend full suite green: cd backend-services/JayTap-services && nvm use 24 && npm test exits 0 with >= 58 tests passing"
    - "Client tsc baseline preserved: npx tsc --noEmit reports exactly 2 pre-existing ThemeContext.tsx errors (no new errors)"
    - "i18n parity gate green: bash scripts/check-i18n-parity.sh exits 0"
    - "Manual physical-device QA matrix walked APPROVED on iPhone 15 Pro Max + Moto G XT2513V (EN+RU + dark/light parity per CONVENTIONS.md)"
    - "Specific deep-link 404 test verified manually with three role tokens (per memory `gsd-verifier-misses-regressions.md` paired-gate posture)"
    - "Code review pass on the diff range (gsd-verifier alone is not sufficient per Phase 1 retro lesson)"
    - "All 9 plans (Plans 01-08 + this Plan 09) marked complete in ROADMAP.md Phase 2 row"
  artifacts:
    - path: ".planning/phases/02-listing-lifecycle-status-field-absorption/02-09-SUMMARY.md"
      provides: "Phase 2 verification + QA evidence; phase-exit signoff"
      contains: "Phase 2 EXIT GATE"
  key_links:
    - from: "Phase 2 phase-exit"
      to: "Phase 3 plan-phase"
      via: "ROADMAP Phase 2 marked [x]; Phase 3 unblocked"
      pattern: "\\[x\\] \\*\\*Phase 2"
---

<objective>
Wave-7 verification gate. NOT a code-producing plan — this is the phase-exit checkpoint that walks through automated test suites + manual physical-device QA + a code review pass.

Per memory `gsd-verifier-misses-regressions.md`: in M2 Phase 1 the verifier reported PASS 15/15 while a code reviewer found 2 CRITICAL regressions in the same commit chain. Phase 2 inherits the paired-gate posture: verifier-style automated checks + a separate code-review pass + manual physical-device QA.

The route-level filter changes in Plan 03 (D-05/D-06/D-12 — server-side information-disclosure mitigations) are EXACTLY the regression class the Phase 1 verifier missed. This plan's manual QA matrix prioritizes deep-link 404 testing with three role tokens (anonymous, owner, mod, admin, non-owner-non-mod renter) on physical devices.

Purpose: Confirm Phase 2 ships clean. Approve phase exit. Update ROADMAP.md.

Output: 02-09-SUMMARY.md committing the verification evidence + ROADMAP.md update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-VALIDATION.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-01-SUMMARY.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-02-SUMMARY.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-03-SUMMARY.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-04-SUMMARY.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-05-SUMMARY.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-06-SUMMARY.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-07-SUMMARY.md
@.planning/phases/02-listing-lifecycle-status-field-absorption/02-08-SUMMARY.md

<interfaces>
<!-- Verification commands per 02-VALIDATION.md "Sampling Rate" + manual QA matrix -->

Backend full suite:
```bash
cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && nvm use 24 && npm test
```
Target: ≥ 58 tests passing, 0 failures (Phase 1 baseline 42 + Phase 2 additions ~16+).

Client tsc baseline:
```bash
cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -E "error TS" | wc -l
```
Target: exactly 2 (pre-existing ThemeContext.tsx errors only).

i18n parity gate:
```bash
cd /Users/beckmaldinVL/development/mobileApps/JayTap && bash scripts/check-i18n-parity.sh
```
Target: exit code 0.

Manual physical-device QA matrix from 02-VALIDATION.md "Manual-Only Verifications":

| # | Behavior | Devices | Locales | Themes |
|---|----------|---------|---------|--------|
| 1 | 4-tab segmented control on RenterListings (default: Pending) | iPhone + Moto G | EN + RU | both |
| 2 | Per-tab empty-state copy + CTA (Live/Pending only) | iPhone + Moto G | EN + RU | both |
| 3 | Status pill on PropertyCard (top-LEFT below rent/sale badge per D-19) | iPhone + Moto G | EN + RU | both |
| 4 | RejectionBanner per-session dismiss (cold start + multitask + nav scenarios) | iPhone + Moto G | EN + RU | both |
| 5 | HomeRejectionBanner auto-dismiss when count → 0 | iPhone + Moto G | EN + RU | both |
| 6 | AppState 'active' role refresh + 60s cooldown | iPhone + Moto G | EN | dark |
| 7 | Edit-resubmit flow (rejected → pending via PUT auto-flip) | iPhone + Moto G | EN + RU | both |
| 8 | Submit-button copy on CreateListingScreen | iPhone + Moto G | EN + RU | both |
| 9 | Deep-link 404 to non-owner-non-mod (3 tokens) | iPhone | EN | dark |

Code review:
- Diff range: `git log --oneline 02-listing-lifecycle-status-field-absorption^..HEAD` (the commit range that comprises Phase 2).
- Reviewer mandate: spot regressions in adjacent code, especially route-level filters and the D-22 body-status sanitizer surface area.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification suites</name>
  <files></files>
  <read_first>
    - .planning/phases/02-listing-lifecycle-status-field-absorption/02-VALIDATION.md (sampling rate + sign-off checklist)
    - All 8 prior plan SUMMARYs (02-01-SUMMARY.md through 02-08-SUMMARY.md) — review what each plan claimed to verify
  </read_first>
  <action>
    Run the three automated gates in sequence. Capture verbatim output for each.

    Gate 1 — Backend full suite:
    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && nvm use 24 && npm test 2>&1 | tee /tmp/jaytap-phase2-backend-suite.log
    ```
    Verify the closing summary line shows `Tests: <N> passed, <0> failed` with N >= 58.

    Gate 2 — Client tsc baseline:
    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | tee /tmp/jaytap-phase2-tsc.log
    ```
    Verify error count: `grep -c "error TS" /tmp/jaytap-phase2-tsc.log` returns exactly 2 (pre-existing ThemeContext.tsx baseline). All 2 errors MUST be in `src/theme/ThemeContext.tsx`. No errors in any file modified by Phase 2.

    Gate 3 — i18n parity:
    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/JayTap && bash scripts/check-i18n-parity.sh 2>&1 | tee /tmp/jaytap-phase2-parity.log; echo "PARITY EXIT: $?"
    ```
    Verify exit code is 0.

    If ANY gate fails:
    - Surface the failure inline (the full log).
    - ABORT this plan with `## PLANNING INCONCLUSIVE` reason "Automated gate failure: <gate name>; failure shown above".
    - Do NOT proceed to Task 2 (manual QA) until automated gates are green.

    If ALL three gates pass, proceed to Task 2.
  </action>
  <verify>
    <automated>test -f /tmp/jaytap-phase2-backend-suite.log &amp;&amp; grep -E "Tests:.*[0-9]+ passed" /tmp/jaytap-phase2-backend-suite.log; echo "---"; test -f /tmp/jaytap-phase2-tsc.log &amp;&amp; echo "tsc errors:" &amp;&amp; grep -c "error TS" /tmp/jaytap-phase2-tsc.log; echo "---"; test -f /tmp/jaytap-phase2-parity.log &amp;&amp; echo "parity log present"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/jaytap-phase2-backend-suite.log` exists and contains a `Tests:` summary line with `0 failed` and `>=58 passed`.
    - `/tmp/jaytap-phase2-backend-suite.log` does NOT contain a line matching `FAIL src/__tests__/` (Jest's failure prefix).
    - `/tmp/jaytap-phase2-tsc.log` exists and `grep -c "error TS" /tmp/jaytap-phase2-tsc.log` returns exactly 2.
    - All `error TS` lines in `/tmp/jaytap-phase2-tsc.log` reference `src/theme/ThemeContext.tsx` (the pre-existing baseline, not new errors): `grep "error TS" /tmp/jaytap-phase2-tsc.log | grep -v "ThemeContext.tsx" | wc -l` returns 0.
    - `/tmp/jaytap-phase2-parity.log` exists and the exit code recorded was 0 (the agent printed `PARITY EXIT: 0` after the command).
    - All three logs captured to /tmp/.
  </acceptance_criteria>
  <done>
    Automated verification suite is green. Backend ≥58 tests passing, tsc baseline preserved (2 ThemeContext errors), i18n parity exits 0. Plan 09 proceeds to manual QA.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — manual physical-device QA matrix walked APPROVED on iPhone + Moto G</name>
  <what-built>
    Phase 2 backend (Plan 03) is deployed to Railway production (verified via Plan 02's --verify=PASS migration evidence + Plan 03's commit). Phase 2 client (Plans 04-08) is built into a development bundle ready for physical-device installation.

    The 9-row manual QA matrix from 02-VALIDATION.md "Manual-Only Verifications" requires:
    - Both physical devices: iPhone 15 Pro Max + Moto G XT2513V (per CONVENTIONS.md manual QA bar).
    - Both locales: English + Russian (toggle via the existing LanguageProvider).
    - Both themes: Light + Dark (toggle via the existing ThemeProvider).

    The 9 behaviors to verify:

    1. **4-tab segmented control on RenterListings (default: Pending) — MOD-06.**
       - Open RenterListings; confirm Pending tab is selected on first mount.
       - Switch through Live / Rejected / Archived; confirm per-tab content (or empty state) renders.
       - Confirm per-tab Hospitality strip renders inside each tab's FlatList header (D-10).
       - Pull-to-refresh refetches.

    2. **Per-tab empty-state copy + CTA — D-11.**
       - On each empty tab, confirm copy matches Plan 04 locale keys (EN + RU).
       - "Create listing" CTA renders ONLY on Live + Pending empty states; Rejected + Archived stay text-only.

    3. **Status pill on PropertyCard — MOD-07 + D-19.**
       - For listings in pending/rejected/archived statuses, confirm a pill renders top-LEFT below the rent/sale badge (D-19 supersedes UI-SPEC original "top-right").
       - Live listings show NO pill (D-16).
       - Pill text: EN "Pending review" / "Rejected" / "Unpublished"; RU «На модерации» / «Отклонено» / «Снято с публикации».
       - Pill colors derive from theme palette (warning / error / textTertiary). Light + dark parity.

    4. **RejectionBanner per-session dismiss — MOD-08 + D-13.**
       - On a rejected listing's PropertyDetailsScreen, banner appears.
       - Tap dismiss X → banner hides.
       - Reload screen via navigate-away-and-back → banner re-evaluates and re-appears (per-session dismiss is screen-local; navigate-away unmounts and resets).
       - Cold start app → banner appears.
       - Background app for >5min → foreground → banner appears (within a single screen lifetime, the dismiss is a Set keyed by listing id; the AppState 'active' transition does NOT clear the dismiss state, but if the screen unmounts/remounts during the background→foreground cycle, dismiss resets).

    5. **HomeRejectionBanner auto-dismiss — MOD-09 + D-14.**
       - On Home with rejected count > 0, banner appears with count.
       - Edit + resubmit one rejected listing (Phase 4 archive UI hidden; only edit-resubmit available) → status flips to pending → banner count decrements.
       - When all rejected listings are resubmitted → count → 0 → banner auto-dismisses (returns null).

    6. **AppState 'active' role refresh + 60s cooldown — D-17.**
       - Sign in as a user.
       - Have an admin demote the user to a different role server-side (manual Mongo edit OK; Phase 5 admin UI is out of scope).
       - Foreground the app within 60s window → confirm role-refresh banner appears within 60s of foreground (the existing Phase 1 RoleRefreshBanner triggers when userType changed).
       - Confirm cooldown: rapidly toggle to Settings and back → second foreground does NOT trigger another refresh (60s cooldown is honored).

    7. **Edit-resubmit flow — D-15 + D-22.**
       - On a rejected listing, tap "Edit & resubmit" / «Исправить» on the RejectionBanner.
       - CreateListingScreen opens in edit mode.
       - Submit button reads "Resubmit for review" / «Отправить повторно» (D-15 distinct CTA).
       - Save → listing moves from Rejected tab → Pending tab (D-22 auto-flip).
       - RejectionBanner stops appearing on that listing.

    8. **Submit-button copy on CreateListingScreen — D-20 + MOD-03.**
       - Open CreateListingScreen as NEW (NOT edit).
       - Confirm submit button reads "Submit for review" / «Отправить на модерацию».
       - On edit-resubmit path (from #7 above), confirm distinct "Resubmit for review" / «Отправить повторно» CTA renders.
       - On edit-LIVE-listing path (no rejection), confirm "Update listing" copy preserved.

    9. **Deep-link 404 to non-owner-non-mod — MOD-05 + D-06 (HIGHEST PRIORITY per memory `gsd-verifier-misses-regressions.md`).**
       - Pick a rejected listing's id (from the dev DB or from a manual mod-rejection).
       - Owner deep-links to own pending listing → 200 + status pill on PropertyDetailsScreen.
       - Mod deep-links to that listing → 200 + status pill (no banner — banner is owner-specific per Plan 07).
       - Admin deep-links → 200 + status pill.
       - Unrelated user (non-owner non-mod) deep-links → 404 surfaced as "Listing not found" UI (the existing "not found" pattern in the screen).

    Walked APPROVED means EVERY row in the 9-row matrix returns the expected behavior on BOTH devices, BOTH locales, and where applicable BOTH themes (per row 6 + row 9 which are EN-only / dark-only as documented).
  </what-built>
  <how-to-verify>
    Walk the 9-row matrix one row at a time. For each row:
    - Type "row N: PASS" if all device × locale × theme combos work.
    - Type "row N: FAIL — <description>" if anything is wrong; surface the exact device + locale + theme + what was expected vs observed.

    After all 9 rows are verified:
    - Type "QA APPROVED" to proceed to Task 3 (code review + ROADMAP update).
    - OR Type "QA REJECTED — <list of failures>" to abort the plan; the agent will document the failures, surface them to the orchestrator with `## PHASE EXIT BLOCKED`, and stop.

    **Time budget:** the full 9-row matrix takes ~30-60 minutes per device. Block the time. Do not rush row 9 (deep-link 404) — that's the regression class the verifier missed in Phase 1.

    **Reverting:** if QA rejects, the agent does NOT auto-revert. The orchestrator will route to gap-closure planning for the failing rows. The phase exit gate stays closed until a follow-up plan addresses the gaps.
  </how-to-verify>
  <action>This task is a checkpoint:human-verify gate. The agent presents the verification material from `<what-built>` and `<how-to-verify>` to the operator and waits for the resume-signal before proceeding. No code changes occur in this task. See `<how-to-verify>` for the operator workflow.</action>
  <resume-signal>Type "QA APPROVED" if all 9 rows pass on both devices, OR "QA REJECTED — <description>" with specific failures (which row, which device, which locale/theme, what was expected vs observed)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Code review pass + ROADMAP.md update + commit phase-exit SUMMARY</name>
  <files>.planning/ROADMAP.md, .planning/phases/02-listing-lifecycle-status-field-absorption/02-09-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 row + the M2 phase progress table at line 138-145)
    - All 8 prior SUMMARY files
    - The git log for Phase 2 commit range
  </read_first>
  <action>
    Step 1 — Code review pass on the Phase 2 commit range. Per memory `gsd-verifier-misses-regressions.md`:

    Run:
    ```bash
    git log --oneline --no-merges -- backend-services/JayTap-services/src/ src/ App.tsx .planning/phases/02-listing-lifecycle-status-field-absorption/ 2>&1 | head -30
    ```

    Identify the commit range that comprises Phase 2 (commits referencing `02` phase or any of the 9 plan IDs). For each commit, scan the diff for:
    - **Route-level regressions** (the class the Phase 1 verifier missed): any change to propertyRoutes.js handlers that the Phase 2 tests don't cover. Run `git diff <phase2-base>..HEAD -- backend-services/JayTap-services/src/routes/propertyRoutes.js` and read the full diff. Cross-reference the 18 Phase 2 integration tests + the existing CR-01 anti-tamper test — does every modified line have a covering test? Surface any gaps.
    - **Adjacent code touches** that aren't called out in the plan SUMMARYs. E.g., did Plan 03's edit accidentally rename a field? Did Plan 06's RenterListingsScreen edit accidentally remove a useFocusEffect dependency?
    - **Locale-key references** that surface ONLY at runtime (TypeScript can't catch them through `t(... as any)` casts). Run `grep -rn "t('createListing.saveAsDraft'\\|t('createListing.publishListing'\\|t('property.statusDraft'\\|t('property.statusPending'\\|t('property.statusLive'\\|t('property.statusArchived'" src/` — should return 0 lines (the deprecated keys' last consumers cleaned up).
    - **Hardcoded colors** in components: run `grep -rn "'#[0-9A-Fa-f]\\{6\\}'" src/components/StatusPill.tsx src/components/RejectionBanner.tsx src/components/HomeRejectionBanner.tsx` — should return at most 1 hit per file (the documented `'#FFFFFF'` exception per UI-SPEC §"Color" line 91).
    - **Firebase SDK imports** (project memory `no-firebase-sdk.md`): run `grep -rn "from 'firebase'\\|from '@react-native-firebase" src/ App.tsx` — should return 0 lines.

    Document any findings in the SUMMARY (Task 3 step 3 below). If a finding is CRITICAL (e.g., a route-level regression, a hardcoded API key) — surface as `## PHASE EXIT BLOCKED` and stop.

    If findings are minor (e.g., orphan locale keys not yet deleted, a subtle dep-array warning) — document them as Phase-2-acceptable + a follow-up TODO; proceed to Step 2.

    Step 2 — Update ROADMAP.md.

    Read `.planning/ROADMAP.md` and locate:
    - Line 27 — the Phase 2 list-item entry.
    - Lines 138-145 — the M2 phase progress table.

    Edit Line 27 to mark Phase 2 complete. Replace:
    ```
    - [ ] **Phase 2: Listing Lifecycle Status Field Absorption** — `Property.status` enum + timestamp fields, ...
    ```
    with:
    ```
    - [x] **Phase 2: Listing Lifecycle Status Field Absorption** — 9/9 plans across 7 waves COMPLETE <DATE>. Property.status enum cutover (drop 'draft', add 'rejected', default 'pending'), 9-field audit schema (D-21), role-aware route filters (D-05/D-06/D-12), POST/PUT body-status sanitizer (D-22), edit-resubmit auto-flip (D-15), 4-tab segmented control on RenterListings with per-tab Hospitality + per-tab empty states (MOD-06), StatusPill on PropertyCard top-left (D-19), RejectionBanner with per-session dismiss (MOD-08), HomeRejectionBanner with auto-dismiss (MOD-09), AppState 60s cooldown role-refresh (D-17). Production migration --verify=PASS gate (Plan 02). Phase 1 GATE-05 D-12 deferral closed via D-17.
    ```
    Replace `<DATE>` with today's date in YYYY-MM-DD form.

    Edit lines 138-145 to update the M2 phase progress table — change the Phase 2 row's "Plans Complete" from `0/0 | Not started | —` to `9/9 | ✅ COMPLETE | <DATE>`.

    Step 3 — Write `.planning/phases/02-listing-lifecycle-status-field-absorption/02-09-SUMMARY.md` documenting the phase exit. Structure:

    ```markdown
    # Phase 2 Exit Summary

    **Phase:** 02-listing-lifecycle-status-field-absorption
    **Closed:** <DATE>
    **Plans complete:** 9/9 (Plans 01-09)

    ## Phase 2 EXIT GATE

    - [x] Backend full suite: <N> tests passed / 0 failed (target ≥58)
    - [x] Client tsc baseline: 2 errors (ThemeContext.tsx pre-existing only — preserved)
    - [x] i18n parity: exit 0
    - [x] Production Mongo migration: --verify=PASS (Plan 02 evidence)
    - [x] Manual physical-device QA: all 9 rows × 2 devices APPROVED
    - [x] Code review pass: <findings count> minor; 0 CRITICAL

    ## Verification Logs

    Backend test summary:
    ```
    <paste from /tmp/jaytap-phase2-backend-suite.log final summary block>
    ```

    Client tsc:
    ```
    <paste the 2 ThemeContext lines verbatim>
    ```

    i18n parity:
    ```
    <paste exit message + exit 0>
    ```

    ## Plan-by-Plan Closure

    | Plan | Wave | Concern | Status | LOC delta | Notes |
    |------|------|---------|--------|-----------|-------|
    | 01 | 0 | Wave-0 test scaffolds + migration script | ✅ | <pull from 01-SUMMARY> | RED tests authored |
    | 02 | 1 | Production migration --verify=PASS | ✅ | <evidence link> | Gate opened |
    | 03 | 2 | Backend code: schema + 4 route edits + sanitizer | ✅ | <link> | RED → GREEN |
    | 04 | 3 | Client foundation: types + service + locales | ✅ | <link> | Parity ✓ |
    | 05 | 4 | StatusPill + RejectionBanner + HomeRejectionBanner + PropertyCard mount + UI-SPEC update | ✅ | <link> | D-19 supersession landed |
    | 06 | 4 | RenterListings 4-tab + per-tab Hospitality + empty states | ✅ | <link> | parallel with Plan 05 |
    | 07 | 5 | Screen mounts: PropertyDetails + Home + Favorites + CreateListing | ✅ | <link> | D-07 client filter at source |
    | 08 | 6 | App.tsx wire-up + AppState role-refresh hook | ✅ | <link> | D-17 closes Phase 1 D-12 deferral |
    | 09 | 7 | Verification gate (this plan) | ✅ | n/a | phase-exit |

    ## Code Review Findings

    <list findings; if none, say "No CRITICAL or HIGH findings; <N> minor follow-up TODOs noted (orphan locale keys / etc.)".>

    ## Manual QA Matrix Results

    | # | Behavior | iPhone | Moto G | EN | RU | Light | Dark | Notes |
    |---|----------|--------|--------|-----|------|-------|------|-------|
    | 1 | 4-tab segmented control + Pending default | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | |
    | 2 | Per-tab empty states + CTA | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | |
    | 3 | StatusPill top-left below rent/sale badge | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | D-19 visual confirmed |
    | 4 | RejectionBanner per-session dismiss | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | |
    | 5 | HomeRejectionBanner auto-dismiss | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | |
    | 6 | AppState role refresh + 60s cooldown | ✓ | ✓ | ✓ | n/a | n/a | ✓ | per-VALIDATION.md scope |
    | 7 | Edit-resubmit flow (rejected → pending) | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | |
    | 8 | Submit copy "Submit for review" / "Resubmit for review" | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | |
    | 9 | Deep-link 404 to non-owner-non-mod | ✓ | n/a | ✓ | n/a | n/a | ✓ | iPhone-only per VALIDATION.md scope |

    ## Phase 3 Unblock

    Phase 3 (MOD-10..MOD-18 — Moderation Queue + Actions + Edit-on-Behalf) prerequisites are now satisfied:
    - 4-state status enum live in production schema.
    - Audit fields scaffolded (Phase 3 will write approveBy/rejectBy/etc.).
    - Role-aware route surface unblocked.
    - moderationLog collection NOT yet created — Phase 3 owns that.

    `/gsd-discuss-phase 3` is the next step.
    ```

    Replace `<DATE>` and `<N>` and `<findings count>` with actual values.

    Step 4 — Commit. Use Plan 02-style commit message:
    ```bash
    git add .planning/ROADMAP.md .planning/phases/02-listing-lifecycle-status-field-absorption/02-09-SUMMARY.md
    git commit -m "$(cat <<'EOF'
    docs(02): close phase exit — 9/9 plans complete

    Phase 2 (Listing Lifecycle Status Field Absorption) closes with backend suite green,
    client tsc baseline preserved, i18n parity exits 0, production migration --verify=PASS
    (Plan 02 evidence), and manual physical-device QA matrix walked APPROVED on iPhone 15
    Pro Max + Moto G XT2513V across EN+RU + dark/light parity.

    MOD-01..MOD-09 closed. D-12/D-15/D-17/D-22 closed. Phase 1 D-12 AppState deferral closed.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; grep -E "\\[x\\] \\*\\*Phase 2: Listing Lifecycle" .planning/ROADMAP.md; echo "---"; test -f .planning/phases/02-listing-lifecycle-status-field-absorption/02-09-SUMMARY.md &amp;&amp; grep -F "Phase 2 EXIT GATE" .planning/phases/02-listing-lifecycle-status-field-absorption/02-09-SUMMARY.md; echo "---"; git log -1 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "[x] **Phase 2: Listing Lifecycle Status Field Absorption**" .planning/ROADMAP.md` returns 1 line.
    - `grep -E "Phase 2.*9/9.*COMPLETE" .planning/ROADMAP.md` returns 1 line (the progress table updated).
    - `.planning/phases/02-listing-lifecycle-status-field-absorption/02-09-SUMMARY.md` exists.
    - `grep -F "Phase 2 EXIT GATE" .planning/phases/02-listing-lifecycle-status-field-absorption/02-09-SUMMARY.md` returns 1 line.
    - `grep -F "9/9" .planning/phases/02-listing-lifecycle-status-field-absorption/02-09-SUMMARY.md` returns >= 1 line.
    - `git log -1 --format=%s` contains "phase exit" and "9/9".
    - The code-review findings list in SUMMARY has 0 CRITICAL or HIGH entries (if any are CRITICAL, the plan should have aborted at Task 3 step 1 before reaching this commit).
    - `grep -rn "from 'firebase'\\|from '@react-native-firebase" src/ App.tsx` returns 0 lines (project memory `no-firebase-sdk.md`).
    - `grep -rn "t('createListing.saveAsDraft'" src/ App.tsx` returns 0 lines (deprecated locale-key consumer cleanup confirmed).
  </acceptance_criteria>
  <done>
    Phase 2 closes with 9/9 plans complete, all gates green, all 9 manual QA rows approved, code review pass with 0 CRITICAL findings. ROADMAP updated, SUMMARY committed. Phase 3 unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Wave 7 — verification only)

This plan does not modify code. Trust boundaries are inherited from Plans 01-08; the verification surface is the assertion that those boundaries hold.

| Boundary | Coverage |
|----------|----------|
| Backend route surface (Plans 02 + 03) | Backend test suite + manual deep-link 404 row of QA matrix |
| Client D-07 filters (Plan 07) | Manual QA + code review of HomeScreen + FavoritesScreen diffs |
| AppState role-refresh (Plan 08) | Manual QA row 6 + code review of AuthContext.tsx diff |
| Locale parity (Plans 04 + 07) | bash scripts/check-i18n-parity.sh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V4-AccessControl-Verify-01 | Access Control | Phase 1 verifier-vs-reviewer regression class (route-level surfaces broken by upstream changes) | mitigate | Manual QA row 9 (deep-link 404) explicitly tests three role tokens against a non-live listing. Code review pass at Task 3 reads the full propertyRoutes.js diff. Backend suite tests cover all 3 routes (D-05, D-06, D-12) plus PUT (D-15, D-22) plus POST (D-01). |
| T-V5-Tampering-Verify-01 | Tampering | i18n parity drift introduced post-Plan 04 by a downstream plan adding a key to one locale only | mitigate | Task 1 Gate 3: bash scripts/check-i18n-parity.sh exits 0. |
| T-V3-Session-Verify-01 | Session Management | AppState 60s cooldown not actually firing in production multitasking | mitigate | Manual QA row 6 explicitly tests rapid foreground/background cycles to verify cooldown. |

The verification gates collectively assert that Phase 2 ships clean. Phase 3 inherits the same posture.
</threat_model>

<verification>
- All 3 tasks complete per `<acceptance_criteria>`.
- ROADMAP.md updated.
- 02-09-SUMMARY.md committed with verification evidence + code-review findings + manual QA matrix results + Phase 3 unblock note.
- Phase 2 EXIT GATE closed.
</verification>

<success_criteria>
1. Backend full suite: ≥58 tests passing, 0 failures.
2. Client tsc: exactly 2 ThemeContext.tsx errors (no new errors).
3. i18n parity: exit 0.
4. Production migration --verify=PASS (Plan 02 evidence still on file).
5. Manual physical-device QA matrix walked APPROVED on iPhone 15 Pro Max + Moto G XT2513V.
6. Code review pass: 0 CRITICAL findings.
7. ROADMAP.md Phase 2 row marked complete.
8. 02-09-SUMMARY.md committed.
9. Phase 3 prerequisites are satisfied (4-state status enum live; audit fields scaffolded; role-aware route surface in place).
</success_criteria>

<output>
After completion: `.planning/phases/02-listing-lifecycle-status-field-absorption/02-09-SUMMARY.md` (committed) containing:
- Phase 2 EXIT GATE checklist with all items checked.
- Verbatim verification logs (backend suite final summary; tsc 2-error baseline; parity exit 0).
- Plan-by-plan closure table (9 rows, status + LOC + notes).
- Code review findings (zero CRITICAL).
- Manual QA matrix results (9 rows × 2 devices × 2 locales × 2 themes — explicit ✓ marks).
- Phase 3 unblock note + recommended next step (`/gsd-discuss-phase 3`).
</output>
