---
phase: 03
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - ../backend-services/JayTap-services/src/routes/moderationRoutes.js
autonomous: true
requirements: [MOD-10, MOD-11, MOD-12, MOD-13, MOD-15, MOD-16, MOD-17]
tags: [phase-03, backend, moderation, endpoints, race-safe]

must_haves:
  truths:
    - "GET /api/moderation/queue returns {items, totalCount} with FIFO sort by submittedAt ASC, role-gated to moderator+"
    - "POST /api/moderation/properties/:id/approve flips status pending->live atomically via findOneAndUpdate({_id, status: 'pending'}); returns 409 with code: 'ALREADY_MODERATED' on race"
    - "POST /api/moderation/properties/:id/reject validates reasonCode against VALID_REJECT_CODES; flips status pending->rejected atomically; persists rejectionReasonCode + rejectionReasonNote + rejectedAt + rejectedByUid; returns 409 on race"
    - "Every approve/reject writes a ModerationLog row with actorUid sourced from req.firebaseUid (NEVER req.body or req.headers); audit insert is follow-up (orphan-tolerant) with structured logging on failure"
    - "GET /api/properties/:id payload to non-moderator users does NOT include rejectedByUid (moderator-identity-privacy MOD-13)"
  artifacts:
    - path: "../backend-services/JayTap-services/src/routes/moderationRoutes.js"
      provides: "3 endpoint handlers: GET /queue, POST /properties/:id/approve, POST /properties/:id/reject -- all race-safe"
      contains: "findOneAndUpdate"
  key_links:
    - from: "../backend-services/JayTap-services/src/routes/moderationRoutes.js"
      to: "../backend-services/JayTap-services/src/models/Property.js"
      via: "Property.findOneAndUpdate({_id, status: 'pending'}, ...)"
      pattern: "findOneAndUpdate\\(\\{ _id"
    - from: "../backend-services/JayTap-services/src/routes/moderationRoutes.js"
      to: "../backend-services/JayTap-services/src/models/ModerationLog.js"
      via: "await ModerationLog.create({actorUid: req.firebaseUid, ...})"
      pattern: "actorUid: req\\.firebaseUid"
---

<objective>
Land the 3 race-safe public moderator endpoints in `moderationRoutes.js` (the skeleton from Plan 01): `GET /queue` (FIFO list with totalCount piggyback per D-03), `POST /properties/:id/approve` (atomic status flip pending->live), `POST /properties/:id/reject` (atomic flip pending->rejected with reasonCode+reasonNote persistence). Every state-mutating endpoint MUST use atomic `findOneAndUpdate({_id, status: 'pending'}, ...)` per PATTERNS §B (the Phase 4.5 load-then-check anti-pattern is explicitly NOT copied -- see auto-memory `gsd-verifier-misses-regressions.md`). Audit-log inserts use the `req.firebaseUid` token-derived actorUid (per PATTERNS §D + auto-memory `phase45-landlord-application-uid-mismatch-bug.md`) and run as orphan-tolerant follow-up writes per D-10.

Purpose: This plan ships the highest-risk Phase 3 surface. The race-condition logic is exactly the regression class the GSD verifier missed in Phase 1 -- the supertest race-condition test in Plan 05 is the gating safety net.

Output: `moderationRoutes.js` grows from skeleton (~25 LOC after Plan 01) to ~150-180 LOC with 3 handlers attached. Backend full suite still passes (67/67 baseline; Plan 05 adds the new race-condition tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Backend Node version (mandatory):** `nvm use 24` before any `npm`/`node`/`npx` command in `JayTap-services`. Auto-memory `backend-node-version.md`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-CONTEXT.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-RESEARCH.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-VALIDATION.md

<interfaces>
From Plan 01 -- `../backend-services/JayTap-services/src/routes/moderationRoutes.js` skeleton:
- Imports: express, Property, ModerationLog, verifyFirebaseToken, requireMinRole
- `router.use(verifyFirebaseToken, requireMinRole('moderator'))` already mounted at the top
- `VALID_REJECT_CODES = ['incomplete-info','prohibited-content','out-of-service-area','other']` already declared
- Export shape: `module.exports = { router, VALID_REJECT_CODES }`
- HANDLERS attach between the const declaration and the module.exports

From `../backend-services/JayTap-services/src/models/Property.js` (Phase 2 audit fields -- already exist; Plan 03 WRITES them):
- `status: { enum: ['pending','live','rejected','archived'], default: 'pending' }`
- `submittedAt: Date` (used for FIFO sort in GET /queue)
- `approvedAt: Date`, `approvedByUid: String` (Plan 03 sets on approve)
- `rejectedAt: Date`, `rejectedByUid: String` (Plan 03 sets on reject)
- `rejectionReasonCode: String`, `rejectionReasonNote: String` (Plan 03 sets on reject)

From `../backend-services/JayTap-services/src/middleware/verifyFirebaseToken.js`:
- `req.firebaseUid` is set on success (the JWKS-verified token sub claim) -- THIS is the only trustworthy source of actor identity. NEVER read uid from `req.body` or `req.headers['x-firebase-uid']`.

From `../backend-services/JayTap-services/src/routes/propertyRoutes.js` (Phase 2):
- The role-aware GET `/api/properties/:id` payload should return `status` + audit fields to mods/admins (per Phase 2 D-06).
- The owner-facing payload returned to a non-mod-non-owner role MUST NOT include `rejectedByUid`. Verify by code-read first; if Phase 2 already strips correctly, no change needed in this plan.
</interfaces>

<canonical_response_shapes>
Success -- GET /queue:
```
{ "items": [ ... pending listings sorted by submittedAt ASC ... ], "totalCount": 7 }
```

Success -- POST /:id/approve:
```
{ "_id": "...", "status": "live", "approvedAt": "...", "approvedByUid": "<modUid>", ... }
```

Success -- POST /:id/reject:
```
{ "_id": "...", "status": "rejected", "rejectedAt": "...", "rejectedByUid": "<modUid>", "rejectionReasonCode": "incomplete-info", "rejectionReasonNote": "..." }
```

Race conflict -- 409 (locked verbatim per REQUIREMENTS MOD-15):
```
{ "code": "ALREADY_MODERATED", "message": "This listing was already reviewed by another moderator." }
```

Bad request -- 400 (invalid reasonCode):
```
{ "message": "reasonCode must be one of incomplete-info, prohibited-content, out-of-service-area, other" }
```

Forbidden -- 403 (plain user, handled automatically by requireMinRole):
```
{ "code": "insufficient-role", "message": "Insufficient permissions" }
```
</canonical_response_shapes>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 03-03-01: Implement GET /queue + POST /:id/approve with race-safe atomic findOneAndUpdate</name>
  <read_first>
    - `../backend-services/JayTap-services/src/routes/moderationRoutes.js` (CURRENT skeleton from Plan 01 -- read in full before editing)
    - `../backend-services/JayTap-services/src/routes/landlordApplicationRoutes.js` lines 100-180 (analog admin queue handler -- copy structure; NOTE the load-then-check race anti-pattern at lines 238-246 is exactly what Phase 3 MUST NOT copy)
    - `../backend-services/JayTap-services/src/models/Property.js` lines 1-80 (verify status enum, submittedAt, approvedAt, approvedByUid fields)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-RESEARCH.md` lines 622-665 (verbatim approve handler skeleton)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md` sections B (Atomic state transitions), D (actorUid provenance), G (Audit-log follow-up insert) -- these three patterns MUST be applied verbatim
    - Auto-memory `gsd-verifier-misses-regressions.md` (Phase 1 verifier missed race-condition logic; the atomic pattern is non-negotiable)
    - Auto-memory `phase45-landlord-application-uid-mismatch-bug.md` (actorUid MUST come from req.firebaseUid)
  </read_first>
  <files>../backend-services/JayTap-services/src/routes/moderationRoutes.js</files>
  <action>
Attach 2 handlers to the existing `router` between the `VALID_REJECT_CODES` const and the `module.exports = { router, VALID_REJECT_CODES }` line.

Handler 1 -- `GET /queue` -- D-03 piggyback `{items, totalCount}` to avoid a 5th endpoint. FIFO sort by `submittedAt` ASC per REQUIREMENTS.md MOD-10 verbatim. Pure read, no race surface, no audit row needed:

```
router.get('/queue', async (req, res) => {
  try {
    const items = await Property.find({ status: 'pending' }).sort({ submittedAt: 1 });
    return res.json({ items, totalCount: items.length });
  } catch (err) {
    console.error('Error fetching moderation queue:', err);
    return res.status(500).json({ message: err.message });
  }
});
```

Handler 2 -- `POST /properties/:id/approve` -- atomic state transition (PATTERNS §B); audit follow-up (§G); actorUid from req.firebaseUid (§D). Copy verbatim from RESEARCH.md lines 636-665:

```
router.post('/properties/:id/approve', async (req, res) => {
  try {
    const now = new Date();
    const result = await Property.findOneAndUpdate(
      { _id: req.params.id, status: 'pending' },
      { $set: { status: 'live', approvedAt: now, approvedByUid: req.firebaseUid } },
      { new: true }
    );
    if (!result) {
      return res.status(409).json({
        code: 'ALREADY_MODERATED',
        message: 'This listing was already reviewed by another moderator.',
      });
    }
    try {
      await ModerationLog.create({
        actorUid: req.firebaseUid,
        action: 'approve',
        targetType: 'property',
        targetId: result._id,
        before: { status: 'pending' },
        after:  { status: 'live', approvedAt: now, approvedByUid: req.firebaseUid },
        at: now,
      });
    } catch (auditErr) {
      console.error(JSON.stringify({
        evt: 'moderation_audit_orphan',
        action: 'approve',
        targetId: String(result._id),
        actorUid: req.firebaseUid,
        err: auditErr.message,
        ts: new Date().toISOString(),
      }));
    }
    return res.json(result);
  } catch (err) {
    console.error('Error approving listing:', err);
    return res.status(500).json({ message: err.message });
  }
});
```

CRITICAL invariants to preserve (acceptance criteria below grep these):
- The `findOneAndUpdate` filter MUST include `status: 'pending'` -- without it the race window is wide open.
- `approvedByUid: req.firebaseUid` -- NEVER `req.body.approvedByUid` (auto-memory `phase45...uid-mismatch-bug.md`).
- The audit-insert is wrapped in its OWN `try/catch` so an audit failure does NOT roll back the state flip.
- The `evt: 'moderation_audit_orphan'` structured log line is preserved verbatim -- Plan 05's race test may assert this string format for the orphan-detection observability path.
- The 409 message string is the EXACT REQUIREMENTS MOD-15 verbatim copy: `"This listing was already reviewed by another moderator."` (no rewording, no translation -- locale translation happens client-side per PATTERNS §F).

Do NOT modify `module.exports` or the existing `VALID_REJECT_CODES` / `router.use` lines from Plan 01 -- those are load-bearing for downstream plans.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services &amp;&amp; nvm use 24 &amp;&amp; node -e "const {router} = require('./src/routes/moderationRoutes'); const routes = router.stack.filter(l =&gt; l.route).map(l =&gt; Object.keys(l.route.methods)[0].toUpperCase() + ' ' + l.route.path); console.log(JSON.stringify(routes));" &amp;&amp; npm test 2&gt;&amp;1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "router.get('/queue'" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1
    - `grep -c "router.post('/properties/:id/approve'" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1
    - `grep -F "Property.findOneAndUpdate(" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns at least 1 hit
    - `grep -F "{ _id: req.params.id, status: 'pending' }" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit (the atomic filter -- race-safety invariant)
    - `grep -F "approvedByUid: req.firebaseUid" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit (token-derived; not body)
    - `grep -F "actorUid: req.firebaseUid" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns at least 1 hit (audit row actorUid)
    - `grep -c "actorUid: req.body" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 0 (anti-pattern check)
    - `grep -c "actorUid: req.headers" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 0 (anti-pattern check)
    - `grep -F "code: 'ALREADY_MODERATED'" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit
    - `grep -F "This listing was already reviewed by another moderator." ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit (REQUIREMENTS MOD-15 verbatim)
    - `grep -F "evt: 'moderation_audit_orphan'" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit (structured log for orphan detection)
    - `grep -F "sort({ submittedAt: 1 })" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit (FIFO order)
    - The verify command's first JS output prints a JSON array containing both `"GET /queue"` and `"POST /properties/:id/approve"`
    - `npm test` exits 0 with the existing 67/67 backend test count preserved
  </acceptance_criteria>
  <done>GET /queue returns FIFO pending listings with `{items, totalCount}` shape; POST approve uses atomic `findOneAndUpdate` with the `status: 'pending'` filter, returns 409 with locked MOD-15 toast string on race loss, writes ModerationLog row with `actorUid: req.firebaseUid` in a follow-up try/catch (orphan-tolerant). Backend boot + existing test suite still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-03-02: Implement POST /:id/reject with reasonCode validation + reasonNote persistence + verify owner-payload privacy</name>
  <read_first>
    - `../backend-services/JayTap-services/src/routes/moderationRoutes.js` (post-task-01 state with the queue + approve handlers)
    - `../backend-services/JayTap-services/src/routes/landlordApplicationRoutes.js` lines 240-300 (analog admin reject -- copy reasonCode validation, swap audit-log fields)
    - `../backend-services/JayTap-services/src/routes/propertyRoutes.js` lines 1-200 (the GET `/api/properties/:id` handler -- verify it strips `rejectedByUid` from the owner-facing payload OR add a strip step in this task per MOD-13; if Phase 2 already does it correctly, document so in the SUMMARY)
    - `../backend-services/JayTap-services/src/models/Property.js` (verify rejectedAt, rejectedByUid, rejectionReasonCode, rejectionReasonNote fields are on the schema -- they were added in Phase 2 D-21)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-RESEARCH.md` lines 668-709 (verbatim reject handler skeleton)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md` sections B, D, G again
    - `.planning/REQUIREMENTS.md` lines 55-63 for MOD-12 (4 reasonCode enum) + MOD-13 (moderator identity NEVER exposed to owner) + MOD-15 (409 toast verbatim)
  </read_first>
  <files>../backend-services/JayTap-services/src/routes/moderationRoutes.js</files>
  <action>
Attach the third handler to the same router. Place it immediately after the approve handler from Task 01.

`POST /properties/:id/reject` -- validate `reasonCode` against the `VALID_REJECT_CODES` const (Plan 01 already declared it); use the same atomic `findOneAndUpdate` race-safe pattern; persist `rejectionReasonCode`, `rejectionReasonNote`, `rejectedAt`, `rejectedByUid`; write audit-log row with `reasonCode` + `reasonNote` populated:

```
router.post('/properties/:id/reject', async (req, res) => {
  try {
    const { reasonCode, reasonNote } = req.body;

    // Validate reasonCode against the locked 4-value enum (REQUIREMENTS MOD-12).
    if (!reasonCode || !VALID_REJECT_CODES.includes(reasonCode)) {
      return res.status(400).json({
        message: `reasonCode must be one of ${VALID_REJECT_CODES.join(', ')}`,
      });
    }

    const now = new Date();
    const trimmedNote = (typeof reasonNote === 'string' && reasonNote.trim()) ? reasonNote.trim() : null;

    // Atomic state flip -- same race-safety pattern as approve.
    const result = await Property.findOneAndUpdate(
      { _id: req.params.id, status: 'pending' },
      { $set: {
          status: 'rejected',
          rejectedAt: now,
          rejectedByUid: req.firebaseUid,
          rejectionReasonCode: reasonCode,
          rejectionReasonNote: trimmedNote,
        } },
      { new: true }
    );

    if (!result) {
      return res.status(409).json({
        code: 'ALREADY_MODERATED',
        message: 'This listing was already reviewed by another moderator.',
      });
    }

    // Audit log -- follow-up insert with reasonCode + reasonNote populated per MOD-16 schema.
    try {
      await ModerationLog.create({
        actorUid: req.firebaseUid,
        action: 'reject',
        targetType: 'property',
        targetId: result._id,
        before: { status: 'pending' },
        after:  { status: 'rejected', rejectionReasonCode: reasonCode },
        reasonCode,
        reasonNote: trimmedNote,
        at: now,
      });
    } catch (auditErr) {
      console.error(JSON.stringify({
        evt: 'moderation_audit_orphan',
        action: 'reject',
        targetId: String(result._id),
        actorUid: req.firebaseUid,
        err: auditErr.message,
        ts: new Date().toISOString(),
      }));
    }

    return res.json(result);
  } catch (err) {
    console.error('Error rejecting listing:', err);
    return res.status(500).json({ message: err.message });
  }
});
```

**MOD-13 verification step (separate but mandatory):** Read `../backend-services/JayTap-services/src/routes/propertyRoutes.js` GET `/:id` handler. Verify the returned payload to a non-mod-non-owner role does NOT include `rejectedByUid`. Three possible states:

1. **Phase 2 already strips it correctly** (e.g., via `.select('-rejectedByUid')` or a manual delete in the handler when `req.user.userType === 'user'` AND `property.ownerUid !== req.firebaseUid`). Document this in the SUMMARY; no code change in this task.

2. **Phase 2 does NOT strip it** -- add a defensive strip in the GET handler:
```
// MOD-13: moderator identity NEVER exposed to owner-facing payload.
const ownerFacing = !req.user || (req.user.userType !== 'admin' && req.user.userType !== 'moderator');
if (ownerFacing) {
  delete responsePayload.rejectedByUid;
  delete responsePayload.approvedByUid;
}
```

3. **The schema already excludes via `select: false`** at the Mongoose model level -- no code change needed.

Whichever state Phase 2 left it in, document in the SUMMARY with the actual code citation. The MOD-13 invariant MUST be observable: an `<RejectionBanner>` component on a renter's device MUST NOT receive any moderator-identifying field through the `GET /api/properties/:id` payload.

If a code change is needed, this task ships it; otherwise just document that no change was needed.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services &amp;&amp; nvm use 24 &amp;&amp; node -e "const {router} = require('./src/routes/moderationRoutes'); const routes = router.stack.filter(l =&gt; l.route).map(l =&gt; Object.keys(l.route.methods)[0].toUpperCase() + ' ' + l.route.path); console.log(JSON.stringify(routes));" &amp;&amp; npm test 2&gt;&amp;1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "router.post('/properties/:id/reject'" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1
    - `grep -F "VALID_REJECT_CODES.includes(reasonCode)" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit (enum validation)
    - `grep -F "reasonCode must be one of" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit (400 message)
    - `grep -F "rejectedByUid: req.firebaseUid" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit
    - `grep -F "rejectionReasonCode: reasonCode" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit
    - `grep -F "rejectionReasonNote: trimmedNote" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 1 hit
    - `grep -c "actorUid: req.firebaseUid" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 2 (one per audit insert; approve + reject)
    - `grep -c "actorUid: req.body" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 0 (anti-pattern absent)
    - `grep -c "evt: 'moderation_audit_orphan'" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 2 (one per orphan log; approve + reject)
    - `grep -c "code: 'ALREADY_MODERATED'" ../backend-services/JayTap-services/src/routes/moderationRoutes.js` returns 2 (one per race-loser branch)
    - The verify command's JSON array contains all three: `"GET /queue"`, `"POST /properties/:id/approve"`, `"POST /properties/:id/reject"`
    - `npm test` exits 0 with the existing 67/67 backend test count preserved
    - SUMMARY.md documents the MOD-13 owner-payload privacy verification (one of: "Phase 2 already strips correctly at line X" / "Added defensive strip at line Y" / "Schema-level select:false confirmed")
  </acceptance_criteria>
  <done>POST /:id/reject validates reasonCode against the 4-value enum, persists rejection metadata atomically with the same race-safe pattern, writes audit-log row with reasonCode+reasonNote. The MOD-13 owner-payload privacy invariant is verified (and a defensive strip added if Phase 2 left a hole). Backend test suite still 67/67.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Bearer token verified by verifyFirebaseToken; req.firebaseUid set on success |
| Two concurrent moderators -> one Property document | Atomic findOneAndUpdate with status filter is the lock primitive |
| Reject endpoint -> ModerationLog | actorUid + reasonCode flow into audit; client cannot influence actorUid |
| GET /api/properties/:id -> owner client | rejectedByUid MUST NOT cross this boundary (MOD-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering / Race | Two mods approve same listing concurrently -> double action | mitigate | Atomic `findOneAndUpdate({_id, status: 'pending'}, ...)`. Loser receives null -> 409 ALREADY_MODERATED. PATTERNS §B is the source-of-truth pattern; Phase 4.5 load-then-check is explicitly NOT copied. Plan 05's supertest race test (Promise.all([approveA, approveB]) -> exactly one 200, one 409) is the regression net. |
| T-03-03-02 | Spoofing | actorUid set from req.body.actorUid in audit row | mitigate | Every `ModerationLog.create({...})` site uses `actorUid: req.firebaseUid` literally. Acceptance criteria grep `actorUid: req.body` returns 0 hits. Plan 05's anti-spoofing supertest asserts that a request body with `actorUid: 'attacker-uid'` does NOT influence the persisted audit row. Auto-memory `phase45-landlord-application-uid-mismatch-bug.md` is the prior-incident class. |
| T-03-03-03 | Elevation of Privilege | Plain user calls /api/moderation/* | mitigate | `requireMinRole('moderator')` from Plan 01 already mounts on the router. Plan 05's role-gating supertest asserts user-role token -> 403 with `code: 'insufficient-role'` on every endpoint (3 tests, one per endpoint). |
| T-03-03-04 | Information Disclosure | rejectedByUid leaked in owner-facing payload (MOD-13 violation) | mitigate | Task 02 verifies (and patches if needed) the GET /api/properties/:id handler so non-mod-non-owner roles do NOT receive `rejectedByUid` or `approvedByUid` in the payload. SUMMARY documents the actual code state (already-stripped / defensive-add / schema-level-exclusion). |
| T-03-03-05 | Tampering | reasonCode set to arbitrary string (e.g., HTML injection) | mitigate | `VALID_REJECT_CODES.includes(reasonCode)` at the top of the reject handler returns 400 for any value outside the 4-enum. Frontend chip selector also constrains; backend is authoritative. |
| T-03-03-06 | Repudiation | Audit row never written even though state flipped | accept | Audit insert is in a follow-up try/catch (D-10 orphan-tolerant). Orphan rate is logged via `evt: 'moderation_audit_orphan'` for ops visibility. Mongo Atlas tier supports transactions if planner ever wants to upgrade; M2 chooses the lighter pattern matching Phase 4.5 precedent. |
| T-03-03-07 | Information Disclosure | reasonNote contains free-text that could include PII / abuse | accept | reasonNote is mod-authored (only mods can call /reject); the free-text is intended to be visible to the owner via the RejectionBanner. The mod is the principal here, not the threat. M3+ could add server-side sanitization if needed. |
</threat_model>

<verification>
After both tasks complete, run from `/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services`:

```
nvm use 24
npm test
```

Expected: 67/67 tests passing (the Phase 2 baseline). Plan 05 will add the new supertest race-condition cases that increase this count to ~75-80.

Spot-check the routes are registered:

```
node -e "const {router} = require('./src/routes/moderationRoutes'); console.log(router.stack.filter(l => l.route).map(l => Object.keys(l.route.methods)[0].toUpperCase() + ' ' + l.route.path));"
```

Expected output includes all three: `GET /queue`, `POST /properties/:id/approve`, `POST /properties/:id/reject`.

Smoke-test the role gate manually (this is informal -- Plan 05 ships the formal supertest version):

```
curl -i -X POST http://localhost:3000/api/moderation/properties/507f1f77bcf86cd799439011/approve
```

Expected: `401 Unauthorized` (no Bearer token). Set a valid moderator Bearer and the same call returns either 409 ALREADY_MODERATED (if the listing is not pending) or 200 with the updated property.
</verification>

<success_criteria>
- `moderationRoutes.js` exposes 3 endpoints: GET /queue, POST /properties/:id/approve, POST /properties/:id/reject
- Every state-mutating endpoint uses `findOneAndUpdate({_id, status: 'pending'}, ...)` with the status filter inside the query (race-safe)
- Every audit insert uses `actorUid: req.firebaseUid` literally
- Zero anti-pattern strings in the file: no `actorUid: req.body`, no `actorUid: req.headers`, no `findById` followed by `findByIdAndUpdate` race pattern
- The 409 message string matches REQUIREMENTS.md MOD-15 verbatim
- The 4-value `VALID_REJECT_CODES` enum is enforced on the reject endpoint
- The MOD-13 owner-payload privacy invariant is verified or patched (SUMMARY documents the actual state)
- Backend `npm test` still 67/67 green
</success_criteria>

<output>
After completion, create `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-03-SUMMARY.md` documenting:
- Final LOC of moderationRoutes.js after handler attachment
- Verbatim citation of the 3 race-safe `findOneAndUpdate` filter strings
- Verbatim citation of the 2 `actorUid: req.firebaseUid` lines (proof of token-derived provenance)
- The MOD-13 owner-payload privacy state (already-stripped at line X / defensive-add at line Y / schema-level select:false)
- Backend test count delta (must be 67/67 -> 67/67 in this plan; Plan 05 raises it)
- Confirmation that PATTERNS §B / §D / §G all applied
- Any deviations and rationale
</output>
