---
phase: 03
plan: 04
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - src/components/RejectListingModal.tsx
  - src/screens/ModerationQueueScreen.tsx
  - src/screens/ProfileScreen.tsx
autonomous: true
requirements: [MOD-10, MOD-11, MOD-12, MOD-15, MOD-18]
tags: [phase-03, client, moderation, queue-screen, reject-modal, profile-entry]

must_haves:
  truths:
    - "src/components/RejectListingModal.tsx exists as a reusable component (chip row + reasonNote input + Reject Listing submit), accepts props: visible, onClose, onSubmit({reasonCode, reasonNote}), submitting"
    - "src/screens/ModerationQueueScreen.tsx exists, fetches GET /api/moderation/queue, renders FIFO PropertyCard rows, Approve/Reject row actions wired with 409 toast handling"
    - "ProfileScreen entry-point row gates on canViewModerationQueue; renders Inbox icon + label + pending-count badge (when count > 0) + ChevronRight; tap fires onReviewModerationQueue prop"
    - "Profile pending count fetched on mount + AppState 'active' (independent useEffect with its own 60s cooldown ref)"
    - "client tsc baseline preserved at 2 (the known ThemeContext errors); no NEW errors introduced"
    - "i18n parity gate exits 0; new keys consumed via t() in all three new/modified files"
  artifacts:
    - path: "src/components/RejectListingModal.tsx"
      provides: "Reusable reject modal: 4 chips (single-select) + multiline note input + Cancel + Reject Listing buttons"
      min_lines: 150
    - path: "src/screens/ModerationQueueScreen.tsx"
      provides: "Overlay queue screen: FIFO PropertyCard list + per-row inline actions + pull-to-refresh + AppState refetch + 409 handling"
      min_lines: 250
    - path: "src/screens/ProfileScreen.tsx"
      provides: "Profile menu row gated by canViewModerationQueue with pending-count badge"
      contains: "canViewModerationQueue"
  key_links:
    - from: "src/screens/ModerationQueueScreen.tsx"
      to: "src/services/PropertyService.ts"
      via: "PropertyService.getModerationQueue() / approveListing() / rejectListing()"
      pattern: "PropertyService\\.(getModerationQueue|approveListing|rejectListing)"
    - from: "src/screens/ModerationQueueScreen.tsx"
      to: "src/components/RejectListingModal.tsx"
      via: "<RejectListingModal visible={...} onClose={...} onSubmit={...} />"
      pattern: "<RejectListingModal"
    - from: "src/screens/ProfileScreen.tsx"
      to: "src/services/PropertyService.ts"
      via: "PropertyService.getModerationQueueCount()"
      pattern: "getModerationQueueCount"
    - from: "src/screens/ProfileScreen.tsx"
      to: "src/hooks/useRole.ts"
      via: "can('viewModerationQueue')"
      pattern: "viewModerationQueue"
---

<objective>
Land the three RN-client UI components needed for the moderation queue surface in one focused agent session: the reusable `<RejectListingModal>` (extracted from Phase 4.5's inline modal), the new `<ModerationQueueScreen>` overlay (FIFO PropertyCard list + per-row Approve/Reject actions + pull-to-refresh + AppState refetch + 409 handling), and the `ProfileScreen` entry-point row with pending-count badge gated by `<Gated action="viewModerationQueue">`.

Purpose: Phase 4.5's `LandlordApplicationQueueScreen` is the verbatim sibling-pattern source per PATTERNS.md NEW—`src/screens/ModerationQueueScreen.tsx` and CONTEXT.md D-01..D-04. This plan ships standalone screens that App.tsx Plan 06 wires into the navigation state machine; the RejectListingModal is also consumed by PropertyDetailsScreen action footer (Plan 06).

Output: 1 NEW component + 1 NEW screen + 1 modified screen. App.tsx is NOT touched here (Plan 06 owns wiring). PropertyDetailsScreen is NOT touched (Plan 06 owns the action footer). i18n parity preserved; tsc baseline preserved at 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-CONTEXT.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-RESEARCH.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-UI-SPEC.md

<interfaces>
From Plan 02 — locale keys (already in en.ts + ru.ts):
- moderation.queue.entryPoint, moderation.queue.title, moderation.queue.empty
- moderation.action.approve, moderation.action.reject, moderation.action.editOnBehalf
- moderation.approve.confirmTitle, moderation.approve.confirmMessage
- moderation.reject.modalTitle, moderation.reject.notePlaceholder, moderation.reject.submit
- moderation.reject.reason.{incomplete-info, prohibited-content, out-of-service-area, other}
- moderation.race.toast

From Plan 02 — `src/services/PropertyService.ts` (already added in Wave 1):
- `PropertyService.getModerationQueue(): Promise<{items: Property[]; totalCount: number}>`
- `PropertyService.getModerationQueueCount(): Promise<number>`
- `PropertyService.approveListing(propertyId: string): Promise<Property>` (throws 409 with response.status===409 + response.data.code === 'ALREADY_MODERATED' on race)
- `PropertyService.rejectListing(propertyId: string, reasonCode: '...'|..., reasonNote?: string): Promise<Property>`
- `PropertyService.editAsModerator(...)` (used by Plan 06, not this plan)

From Plan 02 — `src/hooks/useRole.ts`:
- `useRole().can('viewModerationQueue')` returns true for moderator|admin
- `useRole().can('approveListings')` returns true for moderator|admin (already present from Phase 1)

From Phase 4.5 sibling-pattern source:
- `src/screens/LandlordApplicationQueueScreen.tsx` (379 LOC) — the verbatim structural template; copy imports, loader pattern, RefreshControl, FlatList, Approve handler with Alert.alert confirm, reject modal layout (extract to RejectListingModal)
- `src/screens/ProfileScreen.tsx` lines 210-219 — the verbatim entry-point row pattern (`canReviewLandlordApplications && onReviewLandlordApplications && (...)`); Phase 3 Moderation Queue row mirrors structurally + adds pending-count badge

From Phase 2 — `src/components/PropertyCard.tsx`:
- D-01: reuse as-is for queue rows (StatusPill from Phase 2 D-19 already labels "На модерации")
- Standard PropertyCard props: property, onFavorite, onPress, themeStyles, etc.

From `src/context/AuthContext.tsx`:
- AppState 'active' subscription with 60s cooldown is already in place at lines 28-37 + 231-252
- Per RESEARCH §"5. AppState 'active' refresh coordination" + PATTERNS.md §E: Phase 3 adds SEPARATE per-screen subscriptions (each with own cooldown ref) because they perform DIFFERENT work (refreshRole vs queue refetch vs count refetch)
- Do NOT extend AuthContext to fan-out queue concerns — couples auth context to feature concerns
</interfaces>

<rejection_modal_contract>
Per `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-UI-SPEC.md` §"Interaction Contracts > <RejectListingModal>":

**Props:**
```
interface RejectListingModalProps {
  visible: boolean;
  onClose: () => void;
  onSubmit: (params: { reasonCode: 'incomplete-info'|'prohibited-content'|'out-of-service-area'|'other'; reasonNote?: string }) => Promise<void>;
  submitting?: boolean;
}
```

**Layout (per UI-SPEC §Spacing/Color/Typography):**
- Modal: animationType="fade" transparent + KeyboardAvoidingView (matches Phase 4.5)
- Card: borderRadius:14, padding:20, backgroundColor: colors.surface (20px is in Spacing Exceptions block — brownfield reuse)
- Title row: 18/600 colors.text "Reject this listing" / «Отклонить объявление»
- Chip row: 4 chips horizontal flex-wrap gap:8; per-chip paddingHorizontal:12 paddingVertical:8 borderRadius:16 borderWidth:1
  - Unselected: inputBackground bg + border + text label
  - Selected: colors.primary bg + colors.primary border + white label (light) / #121212 label (dark)
  - Single-select; default selected on open: 'incomplete-info' (Phase 4.5 precedent)
- Note input: multiline TextInput maxLength=500, borderRadius:10, paddingHorizontal:12, paddingVertical:10, borderWidth:1, minHeight:96
  - Placeholder: t('moderation.reject.notePlaceholder')
- Buttons row: Cancel (left, neutral surface+border) + Reject Listing (right, error bg + white label)
  - Submit ALWAYS enabled when a chip is selected (default 'incomplete-info' on open is pre-selected, so submit is enabled from the start)
  - Submit shows ActivityIndicator color="#FFF" + disabled=true while submitting

**On submit:** call `onSubmit({reasonCode, reasonNote: note.trim() || undefined})` — strip empty note before sending (matches PropertyService.rejectListing's expected shape).

**Locale keys consumed:**
- t('moderation.reject.modalTitle')
- t(`moderation.reject.reason.${code}`) — for each chip label (loops over the 4 codes)
- t('moderation.reject.notePlaceholder')
- t('moderation.reject.submit') — submit button reads "Reject Listing" / «Отклонить объявление»
- t('common.cancel')
</rejection_modal_contract>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 03-04-01: Create RejectListingModal reusable component</name>
  <read_first>
    - `src/screens/LandlordApplicationQueueScreen.tsx` lines 207-275 (the inline reject Modal — extract this layout into the new component) AND lines 324-352 (the modal StyleSheet — copy verbatim, except modalTitle weight 700→600)
    - `src/locales/en.ts` and `ru.ts` (verify the 19 new moderation.* keys land per Plan 02; specifically the `moderation.reject.*` and `moderation.reject.reason.*` keys)
    - `src/theme/ThemeContext.tsx` (the useTheme() shape — confirm colors.surface, colors.primary, colors.text, colors.error, colors.border, colors.inputBackground, colors.textSecondary tokens exist)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-UI-SPEC.md` §"Interaction Contracts > <RejectListingModal>" + §"Spacing Scale > Modal geometry" + §"Color > Selected-chip color note" + §"Typography"
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md` §NEW—`src/components/RejectListingModal.tsx`
  </read_first>
  <files>src/components/RejectListingModal.tsx</files>
  <action>
Create `src/components/RejectListingModal.tsx` as a reusable RN component. Extract the layout from Phase 4.5's inline modal (LandlordApplicationQueueScreen lines 207-275 + StyleSheet 324-352) and rename to use Phase 3 locale keys.

Component shape (TypeScript + RN):

```
import React, { useState, useEffect } from 'react';
import {
  View, Text, StyleSheet, TouchableOpacity, TextInput, Modal,
  KeyboardAvoidingView, Platform, ScrollView, ActivityIndicator,
} from 'react-native';
import { useTheme } from '../theme/ThemeContext';
import { useLanguage } from '../context/LanguageContext';

export type RejectReasonCode = 'incomplete-info' | 'prohibited-content' | 'out-of-service-area' | 'other';

const REJECT_CODES: RejectReasonCode[] = ['incomplete-info', 'prohibited-content', 'out-of-service-area', 'other'];

interface RejectListingModalProps {
  visible: boolean;
  onClose: () => void;
  onSubmit: (params: { reasonCode: RejectReasonCode; reasonNote?: string }) => Promise<void>;
  submitting?: boolean;
}

const RejectListingModal: React.FC<RejectListingModalProps> = ({ visible, onClose, onSubmit, submitting = false }) => {
  const { colors, isDark } = useTheme();
  const { t } = useLanguage();
  const [rejectCode, setRejectCode] = useState<RejectReasonCode>('incomplete-info');
  const [rejectNote, setRejectNote] = useState('');

  // Reset to default on open (per Phase 4.5 precedent — pre-selects 'incomplete-info' chip)
  useEffect(() => {
    if (visible) {
      setRejectCode('incomplete-info');
      setRejectNote('');
    }
  }, [visible]);

  const handleSubmit = async () => {
    const trimmed = rejectNote.trim();
    await onSubmit({ reasonCode: rejectCode, reasonNote: trimmed || undefined });
  };

  return (
    <Modal visible={visible} transparent animationType="fade" onRequestClose={onClose}>
      <KeyboardAvoidingView
        style={styles.overlay}
        behavior={Platform.OS === 'ios' ? 'padding' : undefined}
      >
        <ScrollView
          style={styles.scrollWrap}
          contentContainerStyle={styles.scrollContent}
          keyboardShouldPersistTaps="handled"
          showsVerticalScrollIndicator={false}
        >
          <View style={[styles.card, { backgroundColor: colors.surface }]}>
            <Text style={[styles.title, { color: colors.text }]}>
              {t('moderation.reject.modalTitle')}
            </Text>

            <View style={styles.codesGroup}>
              {REJECT_CODES.map((code) => {
                const selected = rejectCode === code;
                const selectedTextColor = isDark ? '#121212' : '#FFFFFF';
                return (
                  <TouchableOpacity
                    key={code}
                    onPress={() => setRejectCode(code)}
                    style={[
                      styles.codeChip,
                      {
                        backgroundColor: selected ? colors.primary : colors.inputBackground,
                        borderColor: selected ? colors.primary : colors.border,
                      },
                    ]}
                    activeOpacity={0.7}
                  >
                    <Text style={[styles.codeChipText, { color: selected ? selectedTextColor : colors.text }]}>
                      {t(`moderation.reject.reason.${code}` as any)}
                    </Text>
                  </TouchableOpacity>
                );
              })}
            </View>

            <TextInput
              style={[styles.input, { backgroundColor: colors.inputBackground, color: colors.text, borderColor: colors.border }]}
              placeholder={t('moderation.reject.notePlaceholder')}
              placeholderTextColor={colors.textSecondary}
              multiline
              numberOfLines={3}
              maxLength={500}
              value={rejectNote}
              onChangeText={setRejectNote}
            />

            <View style={styles.buttonRow}>
              <TouchableOpacity
                style={[styles.button, { backgroundColor: colors.inputBackground }]}
                onPress={onClose}
                disabled={submitting}
                activeOpacity={0.7}
              >
                <Text style={[styles.buttonText, { color: colors.text }]}>{t('common.cancel')}</Text>
              </TouchableOpacity>
              <TouchableOpacity
                style={[styles.button, { backgroundColor: colors.error }]}
                onPress={handleSubmit}
                disabled={submitting}
                activeOpacity={0.7}
              >
                {submitting
                  ? <ActivityIndicator color="#FFF" size="small" />
                  : <Text style={[styles.buttonText, { color: '#FFF' }]}>{t('moderation.reject.submit')}</Text>}
              </TouchableOpacity>
            </View>
          </View>
        </ScrollView>
      </KeyboardAvoidingView>
    </Modal>
  );
};

const styles = StyleSheet.create({
  overlay: { flex: 1, backgroundColor: 'rgba(0,0,0,0.5)' },
  scrollWrap: { flex: 1 },
  scrollContent: { flexGrow: 1, justifyContent: 'center', padding: 20 },
  card: { borderRadius: 14, padding: 20 },
  title: { fontSize: 18, fontWeight: '600', lineHeight: 24, marginBottom: 16 },     // UI-SPEC: 600 (Phase 3 diverges from 4.5's 700)
  codesGroup: { flexDirection: 'row', flexWrap: 'wrap', gap: 8, marginBottom: 12 },
  codeChip: { paddingHorizontal: 12, paddingVertical: 8, borderRadius: 16, borderWidth: 1 },
  codeChipText: { fontSize: 13, fontWeight: '400', lineHeight: 18 },
  input: {
    borderRadius: 10, paddingHorizontal: 12, paddingVertical: 12, borderWidth: 1,
    minHeight: 96, textAlignVertical: 'top', fontSize: 13, marginBottom: 16,
  },
  buttonRow: { flexDirection: 'row', gap: 8 },
  button: { flex: 1, paddingVertical: 12, borderRadius: 10, alignItems: 'center', justifyContent: 'center' },
  buttonText: { fontSize: 14, fontWeight: '600', lineHeight: 20 },
});

export default RejectListingModal;
```

Key points:
- Default-selected chip is `'incomplete-info'` (Phase 4.5 precedent at line 94 — pre-selected so submit is always enabled).
- `useEffect` resets state on `visible` flip — prevents stale state across modal opens.
- `submitting` prop is provided by the parent (queue or detail screen); during submit, both buttons disable and the Reject Listing button shows ActivityIndicator.
- 12px and 20px values are in UI-SPEC's Exceptions block (brownfield reuse) — do NOT change to 8/16/24.
- Title weight is 600 (Phase 3 divergence from Phase 4.5's 700) per UI-SPEC §"Typography" 2-weight cap.
- Submit button label: `t('moderation.reject.submit')` resolves to "Reject Listing" / «Отклонить объявление» — verb-noun per UI-SPEC.
- The 4 chip labels resolve via `t('moderation.reject.reason.${code}')` lookup — owner-locale invariant satisfied via Plan 02's RejectionBanner patch.
- Component is fully presentational — no service calls, no navigation logic. Parent owns the HTTP call (queue screen calls PropertyService.rejectListing in onSubmit; PropertyDetailsScreen does the same in Plan 06).
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10 ; ls -la src/components/RejectListingModal.tsx ; wc -l src/components/RejectListingModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/RejectListingModal.tsx` exists
    - `wc -l src/components/RejectListingModal.tsx` returns at least 150 (component is non-trivial)
    - `grep -F "type RejectReasonCode = 'incomplete-info' | 'prohibited-content' | 'out-of-service-area' | 'other'" src/components/RejectListingModal.tsx` returns 1 hit
    - `grep -F "REJECT_CODES.map((code) =>" src/components/RejectListingModal.tsx` returns 1 hit (chip row map)
    - `grep -F "t('moderation.reject.modalTitle')" src/components/RejectListingModal.tsx` returns 1 hit
    - `grep -F "t('moderation.reject.notePlaceholder')" src/components/RejectListingModal.tsx` returns 1 hit
    - `grep -F "t('moderation.reject.submit')" src/components/RejectListingModal.tsx` returns 1 hit
    - `grep -F "t(`moderation.reject.reason.${code}`" src/components/RejectListingModal.tsx` returns 1 hit (chip label)
    - `grep -F "maxLength={500}" src/components/RejectListingModal.tsx` returns 1 hit
    - `grep -F "useState<RejectReasonCode>('incomplete-info')" src/components/RejectListingModal.tsx` returns 1 hit (default-select behavior)
    - `grep -F "fontWeight: '600'" src/components/RejectListingModal.tsx` returns at least 2 hits (title + button)
    - `grep -F "fontWeight: '700'" src/components/RejectListingModal.tsx` returns 0 (NO 700 weight per UI-SPEC 2-weight cap)
    - `grep -F "export default RejectListingModal" src/components/RejectListingModal.tsx` returns 1 hit
    - `npx tsc --noEmit` reports the same 2 baseline errors only (no new errors from this component)
  </acceptance_criteria>
  <done>RejectListingModal exists as a self-contained reusable component matching the UI-SPEC contract: 4-chip single-select (default 'incomplete-info'), multiline note input maxLength=500, Cancel + Reject Listing buttons, submit awaits parent's onSubmit promise. Phase 4.5 modal styling is reused except for the 700→600 weight divergence per UI-SPEC §Typography. tsc baseline preserved at 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-04-02: Create ModerationQueueScreen overlay with FIFO list + pull-to-refresh + 409 handling</name>
  <read_first>
    - `src/screens/LandlordApplicationQueueScreen.tsx` (FULL file — 379 LOC; the verbatim structural template per PATTERNS.md NEW—`src/screens/ModerationQueueScreen.tsx`)
    - `src/components/PropertyCard.tsx` (verify the existing props shape — Phase 3 reuses PropertyCard as-is per CONTEXT.md D-01)
    - `src/components/RejectListingModal.tsx` (just created in Task 01)
    - `src/services/PropertyService.ts` (Plan 02 added the 5 new moderation methods)
    - `src/context/AuthContext.tsx` lines 28-37 + 231-252 (the AppState 'active' pattern with 60s cooldown — Phase 3 adds a SIBLING per-screen subscription with own cooldown ref per PATTERNS §E)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-UI-SPEC.md` §"Interaction Contracts > <ModerationQueueScreen>" + §"Visual Focal Points" + §"Spacing Scale > ModerationQueueScreen header geometry"
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md` §NEW—`src/screens/ModerationQueueScreen.tsx` AND §C "409 race-condition UX (client side)" AND §E "AppState 'active' refresh coordination"
  </read_first>
  <files>src/screens/ModerationQueueScreen.tsx</files>
  <action>
Create `src/screens/ModerationQueueScreen.tsx` mirroring the structure of `LandlordApplicationQueueScreen.tsx`. Differences from the analog:
1. Service is `PropertyService` (not LandlordApplicationService); methods are `getModerationQueue` / `approveListing` / `rejectListing`
2. Row content is `<PropertyCard>` (existing component, reused per D-01) — NOT inline card-jsx
3. Empty state copy: `t('moderation.queue.empty')` ("No pending listings.")
4. Header title: `t('moderation.queue.title')` ("Moderation Queue")
5. Reject modal is the new `<RejectListingModal>` (extracted reusable), wired via `rejectTarget` state + `submitting` flag
6. Header title weight is 600 not 700 (UI-SPEC divergence)
7. Approve/Reject row actions inline below each PropertyCard — recommended Layout (b) per UI-SPEC; planner uses inline 3-button row mirroring Phase 4.5 actionsRow style (Approve Listing green / Reject Listing red / Edit on behalf neutral)
8. AppState 'active' refetch with own per-screen 60s cooldown ref (PATTERNS §E)
9. 409 race-condition UX (PATTERNS §C): on `e?.response?.status === 409`, close any open modal, fire `Alert.alert('', t('moderation.race.toast'))`, then refetch the queue

Component skeleton:

```
import React, { useState, useEffect, useCallback, useRef } from 'react';
import {
  View, Text, StyleSheet, TouchableOpacity, FlatList, RefreshControl,
  ActivityIndicator, Alert, AppState, AppStateStatus,
} from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { ChevronLeft, Check, X, Edit3 } from 'lucide-react-native';
import { useTheme } from '../theme/ThemeContext';
import { useLanguage } from '../context/LanguageContext';
import PropertyService from '../services/PropertyService';
import PropertyCard from '../components/PropertyCard';
import RejectListingModal from '../components/RejectListingModal';
import type { Property } from '../types/Property';

interface ModerationQueueScreenProps {
  onBack: () => void;
  onOpenPropertyDetails: (property: Property) => void;
  onEditOnBehalf: (property: Property) => void;
}

const REFRESH_COOLDOWN_MS = 60_000;

const ModerationQueueScreen: React.FC<ModerationQueueScreenProps> = ({ onBack, onOpenPropertyDetails, onEditOnBehalf }) => {
  const { colors } = useTheme();
  const { t } = useLanguage();

  const [items, setItems] = useState<Property[]>([]);
  const [loading, setLoading] = useState(true);
  const [refreshing, setRefreshing] = useState(false);
  const [actingId, setActingId] = useState<string | null>(null);
  const [rejectTarget, setRejectTarget] = useState<Property | null>(null);
  const [submittingReject, setSubmittingReject] = useState(false);

  // Per-screen AppState cooldown ref (PATTERNS §E — separate from AuthContext's refreshRole cooldown)
  const lastRefreshAt = useRef<number | null>(null);

  const load = useCallback(async () => {
    try {
      const { items: queueItems } = await PropertyService.getModerationQueue();
      setItems(queueItems);
    } catch (err: any) {
      console.error('Failed to load moderation queue:', err);
      Alert.alert(t('common.error'), err?.response?.data?.message || err?.message || '');
    } finally {
      setLoading(false);
      setRefreshing(false);
    }
  }, [t]);

  useEffect(() => { load(); }, [load]);

  // AppState 'active' refetch — separate subscription from AuthContext per PATTERNS §E
  useEffect(() => {
    const onChange = (nextState: AppStateStatus) => {
      if (nextState !== 'active') return;
      const now = Date.now();
      if (lastRefreshAt.current && now - lastRefreshAt.current < REFRESH_COOLDOWN_MS) return;
      lastRefreshAt.current = now;
      load();
    };
    const sub = AppState.addEventListener('change', onChange);
    return () => sub.remove();
  }, [load]);

  const handleRaceConflict = async () => {
    setRejectTarget(null);
    Alert.alert('', t('moderation.race.toast'));
    await load();
  };

  const handleApprove = (property: Property) => {
    Alert.alert(
      t('moderation.approve.confirmTitle'),
      t('moderation.approve.confirmMessage'),
      [
        { text: t('common.cancel'), style: 'cancel' },
        {
          text: t('moderation.action.approve'),
          onPress: async () => {
            setActingId(property.id);
            try {
              await PropertyService.approveListing(property.id);
              setItems(prev => prev.filter(p => p.id !== property.id));
            } catch (err: any) {
              if (err?.response?.status === 409) {
                await handleRaceConflict();
                return;
              }
              Alert.alert(t('common.error'), err?.response?.data?.message || err?.message || '');
            } finally {
              setActingId(null);
            }
          },
        },
      ]
    );
  };

  const handleRejectSubmit = async (params: { reasonCode: any; reasonNote?: string }) => {
    if (!rejectTarget) return;
    setSubmittingReject(true);
    try {
      await PropertyService.rejectListing(rejectTarget.id, params.reasonCode, params.reasonNote);
      setItems(prev => prev.filter(p => p.id !== rejectTarget.id));
      setRejectTarget(null);
    } catch (err: any) {
      if (err?.response?.status === 409) {
        await handleRaceConflict();
        return;
      }
      Alert.alert(t('common.error'), err?.response?.data?.message || err?.message || '');
    } finally {
      setSubmittingReject(false);
    }
  };

  const renderItem = ({ item }: { item: Property }) => (
    <View style={styles.row}>
      <PropertyCard
        property={item}
        onPress={() => onOpenPropertyDetails(item)}
        // Reuse existing PropertyCard props as-is — read PropertyCard.tsx for the exact shape
      />
      <View style={styles.actionsRow}>
        <TouchableOpacity
          style={[styles.actionBtn, { backgroundColor: colors.success }]}
          onPress={() => handleApprove(item)}
          disabled={actingId === item.id}
          activeOpacity={0.7}
        >
          {actingId === item.id
            ? <ActivityIndicator color="#FFF" size="small" />
            : (<><Check size={18} color="#FFF" /><Text style={styles.actionBtnText}>{t('moderation.action.approve')}</Text></>)}
        </TouchableOpacity>
        <TouchableOpacity
          style={[styles.actionBtn, { backgroundColor: colors.error }]}
          onPress={() => setRejectTarget(item)}
          disabled={actingId === item.id}
          activeOpacity={0.7}
        >
          <X size={18} color="#FFF" />
          <Text style={styles.actionBtnText}>{t('moderation.action.reject')}</Text>
        </TouchableOpacity>
        <TouchableOpacity
          style={[styles.actionBtn, { backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border }]}
          onPress={() => onEditOnBehalf(item)}
          disabled={actingId === item.id}
          activeOpacity={0.7}
        >
          <Edit3 size={18} color={colors.text} />
          <Text style={[styles.actionBtnText, { color: colors.text }]}>{t('moderation.action.editOnBehalf')}</Text>
        </TouchableOpacity>
      </View>
    </View>
  );

  return (
    <SafeAreaView edges={['top']} style={[styles.container, { backgroundColor: colors.background }]}>
      <View style={[styles.header, { borderBottomColor: colors.border }]}>
        <TouchableOpacity onPress={onBack} style={styles.iconButton} activeOpacity={0.7}>
          <ChevronLeft size={24} color={colors.text} />
        </TouchableOpacity>
        <Text style={[styles.headerTitle, { color: colors.text }]}>{t('moderation.queue.title')}</Text>
        <View style={{ width: 40 }} />
      </View>

      {loading ? (
        <View style={styles.loadingContainer}>
          <ActivityIndicator size="large" color={colors.primary} />
        </View>
      ) : (
        <FlatList
          data={items}
          keyExtractor={(item) => item.id}
          renderItem={renderItem}
          contentContainerStyle={styles.listContent}
          refreshControl={
            <RefreshControl
              refreshing={refreshing}
              onRefresh={() => { setRefreshing(true); load(); }}
              tintColor={colors.primary}
              colors={[colors.primary]}
            />
          }
          ListEmptyComponent={
            <View style={styles.emptyContainer}>
              <Text style={[styles.emptyText, { color: colors.textSecondary }]}>
                {t('moderation.queue.empty')}
              </Text>
            </View>
          }
        />
      )}

      <RejectListingModal
        visible={!!rejectTarget}
        onClose={() => setRejectTarget(null)}
        onSubmit={handleRejectSubmit}
        submitting={submittingReject}
      />
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1 },
  header: {
    flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between',
    paddingHorizontal: 12, paddingVertical: 10, borderBottomWidth: 1,
  },
  iconButton: { padding: 8 },
  headerTitle: { fontSize: 18, fontWeight: '600', lineHeight: 24 },     // UI-SPEC: 600 not 700
  loadingContainer: { flex: 1, justifyContent: 'center', alignItems: 'center' },
  listContent: { padding: 16, paddingBottom: 32 },
  row: { marginBottom: 16 },
  actionsRow: { flexDirection: 'row', gap: 8, marginTop: 8 },
  actionBtn: {
    flex: 1, flexDirection: 'row', alignItems: 'center', justifyContent: 'center',
    paddingVertical: 12, borderRadius: 10, gap: 6,
  },
  actionBtnText: { color: '#FFF', fontSize: 14, fontWeight: '600', lineHeight: 20 },
  emptyContainer: { paddingVertical: 60, alignItems: 'center' },
  emptyText: { fontSize: 14, fontWeight: '600', lineHeight: 20 },       // UI-SPEC: 14/600 (revised from 15/600)
});

export default ModerationQueueScreen;
```

Key points:
- 3 callback props (`onBack`, `onOpenPropertyDetails`, `onEditOnBehalf`) — App.tsx Plan 06 wires them.
- AppState refetch uses a per-screen `useRef` cooldown (NOT module-scope; the screen unmounts when overlay closes).
- 409 handling closes any open modal first then fires the Alert.alert toast (PATTERNS §C).
- After successful approve/reject the row is filtered out of `items` immediately (per PATTERNS §"Pitfall 7: Pessimistic UI" — only after the await resolves successfully, not before).
- The PropertyCard is rendered with `onPress` that calls `onOpenPropertyDetails` — reuses Phase 2 D-19 StatusPill which already labels "На модерации".
- The reject modal extraction means the screen doesn't reimplement the 4-chip layout; just wires the visibility flag.
- The action button labels use the verb-noun keys from Plan 02 ("Approve Listing" / "Reject Listing" / "Edit on behalf").
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -15 ; wc -l src/screens/ModerationQueueScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/screens/ModerationQueueScreen.tsx` exists
    - `wc -l src/screens/ModerationQueueScreen.tsx` returns at least 250
    - `grep -F "import PropertyService from '../services/PropertyService'" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "import PropertyCard from '../components/PropertyCard'" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "import RejectListingModal from '../components/RejectListingModal'" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "PropertyService.getModerationQueue()" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "PropertyService.approveListing(" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "PropertyService.rejectListing(" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "err?.response?.status === 409" src/screens/ModerationQueueScreen.tsx` returns at least 2 hits (approve + reject paths)
    - `grep -F "t('moderation.race.toast')" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "t('moderation.approve.confirmTitle')" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "t('moderation.queue.empty')" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "AppState.addEventListener('change'" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "REFRESH_COOLDOWN_MS = 60_000" src/screens/ModerationQueueScreen.tsx` returns 1 hit
    - `grep -F "fontWeight: '600'" src/screens/ModerationQueueScreen.tsx` returns at least 2 hits
    - `grep -F "fontWeight: '700'" src/screens/ModerationQueueScreen.tsx` returns 0
    - `grep -F "moderatorContext" src/screens/ModerationQueueScreen.tsx` returns 0 (this screen does NOT touch CreateListingScreen wiring; Plan 06 owns that)
    - `npx tsc --noEmit` reports the same 2 baseline errors only (no new errors)
  </acceptance_criteria>
  <done>ModerationQueueScreen exists with FIFO PropertyCard list + per-row 3-button action footer + pull-to-refresh + AppState refetch + 409 conflict handling. The screen takes 3 callback props that App.tsx Plan 06 wires; it's standalone-renderable. tsc baseline preserved at 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-04-03: Add ProfileScreen entry-point row with pending-count badge</name>
  <read_first>
    - `src/screens/ProfileScreen.tsx` (FULL file — 368 LOC; specifically lines 210-219 the existing `canReviewLandlordApplications && onReviewLandlordApplications && (...)` row block + lines 1-50 for imports/props interface)
    - `src/hooks/useRole.ts` (Plan 02 added 'viewModerationQueue' to the Action union)
    - `src/services/PropertyService.ts` (Plan 02 added `getModerationQueueCount()`)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-UI-SPEC.md` §"Interaction Contracts > Profile entry-point" + §"Spacing Scale > Profile entry-point geometry"
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-RESEARCH.md` §"Code Examples > Client: Profile entry-point row" (lines 977-1015)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md` §MODIFIED—`src/screens/ProfileScreen.tsx`
  </read_first>
  <files>src/screens/ProfileScreen.tsx</files>
  <action>
Add the new entry-point to ProfileScreen mirroring the existing `canReviewLandlordApplications && onReviewLandlordApplications` block. Three additive changes:

**Change 1: Props interface** — Add 2 new optional props (placement: with the existing optional handlers around lines 13-22):

```
interface ProfileScreenProps {
  // ... existing props ...
  onReviewLandlordApplications?: () => void;
  // Phase 3 additions:
  onReviewModerationQueue?: () => void;
  pendingModerationCount?: number;
}
```

**Change 2: Capability flag + count fetch** — In the component body alongside the existing `canReviewLandlordApplications` line:

```
const canViewModerationQueue = can('viewModerationQueue');
const [internalPendingCount, setInternalPendingCount] = useState<number>(0);

// Use prop if parent passes it; otherwise self-fetch
const displayCount = (typeof pendingModerationCount === 'number') ? pendingModerationCount : internalPendingCount;

// Self-fetch if no prop provided (parent App.tsx Plan 06 may pass it from a hook)
useEffect(() => {
  if (!canViewModerationQueue) return;
  if (typeof pendingModerationCount === 'number') return; // parent owns the count
  let cancelled = false;
  (async () => {
    try {
      const count = await PropertyService.getModerationQueueCount();
      if (!cancelled) setInternalPendingCount(count);
    } catch { /* non-fatal — badge stays at 0 */ }
  })();
  return () => { cancelled = true; };
}, [canViewModerationQueue, pendingModerationCount]);

// AppState 'active' refresh — own per-screen cooldown ref (PATTERNS §E)
const lastCountFetchAt = useRef<number | null>(null);
useEffect(() => {
  if (!canViewModerationQueue || typeof pendingModerationCount === 'number') return;
  const onChange = (nextState: AppStateStatus) => {
    if (nextState !== 'active') return;
    const now = Date.now();
    if (lastCountFetchAt.current && now - lastCountFetchAt.current < 60_000) return;
    lastCountFetchAt.current = now;
    PropertyService.getModerationQueueCount().then(setInternalPendingCount).catch(() => {});
  };
  const sub = AppState.addEventListener('change', onChange);
  return () => sub.remove();
}, [canViewModerationQueue, pendingModerationCount]);
```

(Add `useState`, `useEffect`, `useRef` to existing react imports; add `AppState`, `AppStateStatus` to react-native imports; add `PropertyService` import.)

**Change 3: Entry-point JSX row** — Place AFTER the existing `canReviewLandlordApplications && onReviewLandlordApplications && (...)` block (~lines 210-219). Same shape + added pending-count badge:

```
{canViewModerationQueue && onReviewModerationQueue && (
  <>
    <View style={[styles.menuDivider, { backgroundColor: themeStyles.border }]} />
    <TouchableOpacity style={styles.menuRow} onPress={onReviewModerationQueue} activeOpacity={0.7}>
      <Inbox size={22} color={themeStyles.accent} strokeWidth={1.5} />
      <Text style={[styles.menuText, { color: themeStyles.text, flex: 1 }]}>
        {t('moderation.queue.entryPoint')}
      </Text>
      {displayCount > 0 && (
        <View style={[styles.pendingBadge, { backgroundColor: themeStyles.accent }]}>
          <Text style={styles.pendingBadgeText}>{displayCount}</Text>
        </View>
      )}
      <ChevronRight size={20} color={themeStyles.textSecondary} />
    </TouchableOpacity>
  </>
)}
```

Add the 2 new badge styles to the existing StyleSheet (or sibling-style block):

```
pendingBadge: {
  borderRadius: 10, paddingHorizontal: 6, paddingVertical: 2,
  minWidth: 20, alignItems: 'center', justifyContent: 'center', marginRight: 8,
},
pendingBadgeText: { color: '#FFFFFF', fontSize: 11, fontWeight: '600', lineHeight: 14 },
```

(Per UI-SPEC §"Profile entry-point geometry": badge `borderRadius: 10, paddingHorizontal: 6, paddingVertical: 2, minWidth: 20`; typography `11/600/14` matching Phase 2 StatusPill.)

Key constraints:
- The badge is hidden when `displayCount === 0` (or undefined). The row stays visible because the moderator may want to verify the queue is empty.
- The badge color is `themeStyles.accent` per UI-SPEC §"Color > Accent reserved for the following surfaces ONLY" — the 10% rule explicitly lists this surface as one of two acceptable accent uses (the other is the selected-chip in RejectListingModal).
- The fetch is guarded by `canViewModerationQueue` — plain users never trigger the network call (server would 403 anyway, but client-side hygiene avoids noise).
- `AppState.addEventListener` is used per-screen (NOT extending AuthContext) per PATTERNS §E.
- If the parent (App.tsx Plan 06) decides to own the count via a hook, `pendingModerationCount` prop takes precedence and the self-fetch effect skips. This keeps the screen flexible for either approach.
- Existing entry-points (landlord applications, account settings, etc.) MUST remain unchanged — additive only.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -15 ; bash scripts/check-i18n-parity.sh; echo "PARITY=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "onReviewModerationQueue?: () => void" src/screens/ProfileScreen.tsx` returns 1 hit
    - `grep -F "pendingModerationCount?: number" src/screens/ProfileScreen.tsx` returns 1 hit
    - `grep -F "const canViewModerationQueue = can('viewModerationQueue')" src/screens/ProfileScreen.tsx` returns 1 hit
    - `grep -F "PropertyService.getModerationQueueCount()" src/screens/ProfileScreen.tsx` returns at least 1 hit
    - `grep -F "t('moderation.queue.entryPoint')" src/screens/ProfileScreen.tsx` returns 1 hit
    - `grep -F "AppState.addEventListener('change'" src/screens/ProfileScreen.tsx` returns 1 hit (the new sibling subscription)
    - `grep -F "canReviewLandlordApplications" src/screens/ProfileScreen.tsx` returns at least 1 hit (Phase 4.5 row preserved)
    - `grep -F "pendingBadge" src/screens/ProfileScreen.tsx` returns at least 2 hits (style + JSX)
    - `grep -F "fontSize: 11, fontWeight: '600', lineHeight: 14" src/screens/ProfileScreen.tsx` returns 1 hit (badge typography per UI-SPEC)
    - `npx tsc --noEmit` reports the SAME 2 baseline errors only (no new errors from this task)
    - `bash scripts/check-i18n-parity.sh` exits 0 (PARITY=0)
  </acceptance_criteria>
  <done>ProfileScreen has a new menu row gated by `canViewModerationQueue`, displays the pending-count badge using the accent token (per UI-SPEC 10% rule), self-fetches the count on mount + AppState 'active' (with 60s cooldown ref) UNLESS the parent passes `pendingModerationCount` prop. Existing Phase 4.5 landlord-application row is unchanged. tsc baseline preserved at 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User UI -> PropertyService | Service-layer canFromUser belt-and-suspenders gate (Plan 02) |
| RejectListingModal onSubmit -> parent screen | Modal is presentational only; parent owns the HTTP call (queue or detail screen) |
| ProfileScreen mount -> /api/moderation/queue (count) | Background fetch only when canViewModerationQueue |
| AppState 'active' subscriptions | Multiple listeners coexist; each per-screen subscription has own cooldown ref (no shared registry) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Elevation of Privilege | Plain user sees Profile moderation entry-point | mitigate | Entry-point row is wrapped in `canViewModerationQueue && onReviewModerationQueue && (...)`. canFromUser returns false for role==='user'/guest (Plan 02 verified). Backend Plan 03 `requireMinRole('moderator')` is the security boundary; client gating is UX. |
| T-03-04-02 | Information Disclosure | Pending count fetched and displayed to plain user | mitigate | The `useEffect` count fetch is guarded by `if (!canViewModerationQueue) return;`. Even if a developer wires the count without the gate, the backend returns 403 and the catch block silently keeps badge at 0. |
| T-03-04-03 | Tampering | RejectListingModal submits arbitrary reasonCode | mitigate | The modal's `onSubmit` callback is invoked only when a chip is selected from the locked 4-value enum (`REJECT_CODES` typed array). The TypeScript union type prevents any other value at compile time. Backend Plan 03 `VALID_REJECT_CODES.includes(...)` is the authoritative check. |
| T-03-04-04 | Tampering | reasonNote contains 600+ chars (overflow) | mitigate | TextInput `maxLength={500}` enforces the cap. Backend Plan 03 does not enforce a server-side length cap explicitly but Mongo string fields accept up to 16MB; UI-side enforcement is sufficient for M2 mod-team scale. |
| T-03-04-05 | Race / 409 | Two devices both Approve -> one sees 200, other sees 409 | mitigate | The 409 catch in handleApprove + handleRejectSubmit closes the modal first, fires Alert.alert with verbatim MOD-15 toast copy, then refetches. Per PATTERNS §C. |
| T-03-04-06 | Information Disclosure | Moderator's identity surfaces in PropertyCard | accept | PropertyCard is the existing Phase 2 component; it does NOT receive moderator identity. The owner-facing payload from /api/properties/:id (Plan 03 verifies) does not include rejectedByUid. |
</threat_model>

<verification>
After all 3 tasks complete, run from `/Users/beckmaldinVL/development/mobileApps/JayTap`:

```
npx tsc --noEmit 2>&1 | tail -10
bash scripts/check-i18n-parity.sh; echo "PARITY=$?"
```

Expected:
- tsc reports only the 2 baseline ThemeContext errors
- PARITY=0

The screens are not yet wired into App.tsx (Plan 06's job). Manual smoke tests deferred to Plan 06's wire-up.
</verification>

<success_criteria>
- `src/components/RejectListingModal.tsx` is a self-contained reusable component with the 4-chip layout, default-selected 'incomplete-info', maxLength=500 note input, and Cancel + Reject Listing buttons
- `src/screens/ModerationQueueScreen.tsx` fetches via PropertyService.getModerationQueue, renders FIFO PropertyCard rows with inline Approve/Reject/Edit-on-behalf actions, handles 409 conflict per PATTERNS §C, refetches on AppState 'active' with own 60s cooldown
- `src/screens/ProfileScreen.tsx` has a new row gated by canViewModerationQueue with pending-count badge styled per UI-SPEC (accent bg, 11/600/14 typography)
- All three files use Plan 02's `moderation.*` locale keys and Plan 02's PropertyService methods
- tsc baseline preserved at 2 errors
- i18n parity gate exits 0
- Phase 4.5 entry-point row in ProfileScreen is unchanged (additive only)
- App.tsx is NOT touched in this plan (Plan 06 owns wiring)
- PropertyDetailsScreen is NOT touched in this plan (Plan 06 owns the action footer)
</success_criteria>

<output>
After completion, create `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-04-SUMMARY.md` documenting:
- Final LOC of the 3 files (RejectListingModal, ModerationQueueScreen, ProfileScreen delta)
- Locale keys consumed (vs Plan 02 declared)
- The decision on count fetch ownership (self-fetch when prop absent vs prop-driven when parent owns)
- The AppState pattern used (per-screen useRef cooldown vs module-scope) and rationale
- tsc baseline stat (must remain 2)
- Confirmation that App.tsx and PropertyDetailsScreen are NOT touched (Plan 06 territory)
- Any deviations and rationale
</output>
