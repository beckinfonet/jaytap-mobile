---
phase: 06-hardening-manual-physical-device-qa-release
plan: 06
type: execute
wave: 3
depends_on: [06-01, 06-04, 06-05]
files_modified:
  - .planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md
autonomous: false
requirements: [REL-03]
requirements_addressed: [REL-03]
tags: [release, manual-qa, physical-device, iphone-15-pro-max, moto-g-xt2513v, two-device-race, role-transitions, archive-flows, owner-rejection-banner-persistence, hotfix-bundle-empirical, en-ru-parity, dark-light-parity, cross-cutting]

must_haves:
  truths:
    - "06-QA-MATRIX.md exists with one row per coverage cell defined below; every row has PASS / FAIL / N-A populated by the time the checkpoint resumes"
    - "iPhone 15 Pro Max walks every cell that's not iPhone-N/A (iOS Hospitality cell etc., per memory `nav-overlay-hides-bottom-nav.md`)"
    - "Moto G XT2513V walks every cell that's not Moto-N/A — closing the deferred Moto G rows from Phase 3 (03-06-SUMMARY) + Phase 4 (04-07-SUMMARY) + Phase 5 (05-05-SUMMARY)"
    - "Two-device moderation race-condition cells (Matrix 2 cells 2.4 / 2.5 / 2.6) each record their observed outcome (1 × 200 + 1 × 409) AND the ModerationLog row count = 1 evidence (queried post-race via mongosh / Atlas Data API / user-relayed integer / temporary admin debug endpoint). NO race cell is marked PASS via supertest-fallback rationale without explicit user sign-off recorded in 06-06-SUMMARY.md"
    - "If a race cell cannot be observed after 3 retries, the executor escalates per T-06-33: either coordinate via a tool/script that forces an observable race, or formally defer race-coverage to a follow-up phase with user-recorded written approval — never silently auto-accept"
    - "All four hotfix-bundle fixes (HF-01 Hospitality fields persist; HF-02 backend env rotated + secrets dead; HF-03 firebaseUid mismatch 403; HF-04 socket handshake JWKS) are empirically verified end-to-end on at least one device per fix"
    - "Owner-side rejection banner persists across sessions: walked by signing out + back in on iOS; banner reappears for any rejected listing; tapping navigates to OwnerListings → Rejected tab"
    - "Role transition coverage walks user → moderator → admin → user-via-demotion (full ROLE-11 roleRevokedAt invariant chain) on at least one device, end-to-end, with the demoted account's next request returning 403 within 60s"
    - "EN+RU parity walked: at least one Russian-language session per device covering CreateListing / OwnerListings tabs / Moderation Queue / Role Management / Archive flow"
    - "Dark/light parity walked: dark theme session per device covers PropertyDetailsScreen RejectionBanner + Moderation footer + Role Management screen"
    - "Phase 3+4 paired-gates prerequisite (per 06-STORE-HISTORY.md `phase_3_4_paired_gates_prerequisite`) is `clear` BEFORE the QA matrix walk starts; the SUMMARY records the user's confirmation that `/gsd-verify-work 3+4` and `/gsd-code-review 3+4` ran clean"
    - "Walk disposition is APPROVED (zero blocking FAILs) before the user resume signal `approved` is given"
  artifacts:
    - path: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md"
      provides: "REL-03 — manual physical-device QA matrix; binding evidence for cross-cutting M2 surface coverage; gate artifact for Plan 07 release submission"
      contains: "iPhone 15 Pro Max"
      contains_also: "Moto G XT2513V"
  key_links:
    - from: "06-QA-MATRIX.md :: walk_disposition: APPROVED"
      to: "Plan 06-07 :: archive build + store submission"
      via: "Plan 07 only proceeds if walk_disposition is APPROVED"
      pattern: "walk_disposition: APPROVED"
    - from: "Plan 06-05 commit SHA"
      to: "06-QA-MATRIX.md :: walked_against_sha"
      via: "QA matrix walks the bumped binary, not pre-bump"
      pattern: "walked_against_sha:"
    - from: "06-BACKEND-DEPLOY.md :: railway_deploy_sha"
      to: "06-QA-MATRIX.md :: backend_walked_against_sha"
      via: "QA matrix walks against the live backend deploy from Plan 04"
      pattern: "backend_walked_against_sha:"
---

<objective>
Wave 3 of Phase 6 — the cross-cutting manual physical-device QA matrix walk. This is the gate that proves M2's full surface (Phases 1–5 deliverables) works end-to-end on real iOS + Android devices before the v2.0.0 binary uploads to the stores.

Per CLAUDE.md, the M2 testing bar is manual physical-device QA on iPhone 15 Pro Max + Moto G XT2513V. M1's testing bar was the same. M2 Phases 3, 4, 5 deferred Moto G coverage to Phase 6 REL-03 (per `03-06-SUMMARY.md`, `04-07-SUMMARY.md`, `05-05-SUMMARY.md`) — this plan absorbs those deferred rows AND adds M2-specific cross-cutting cells:

1. **Role transition coverage (cross-phase)** — user → moderator → admin → user-via-demotion. Walks ROLE-01..ROLE-11 + ADMIN-02. Confirms ROLE-11 `roleRevokedAt` invariant: demoting a moderator while their app is open causes their next protected request to 403 within 60s.

2. **Moderation Queue actions with two-device race-condition coverage (REL-03 explicit)** — Device A and Device B both signed in as moderators, both open Moderation Queue, both attempt to act on the same listing simultaneously. Verifies MOD-15 (one winner gets 200, loser gets 409 ALREADY_MODERATED) end-to-end against the live backend with verifyFirebaseToken middleware in the loop.

3. **Archive flow coverage** — owner self-archive (ARCH-01); mod/admin archive with reasonCode (ARCH-02); restore-to-pending re-moderation (ARCH-04 + D-13 conditional gate where owner CANNOT restore mod-archived listing); admin hard-delete on archived listing (ARCH-05).

4. **Owner-side rejection banner persistence** — sign in as owner with a rejected listing; banner appears on Home; tap → OwnerListings → Rejected tab; sign out; sign back in; banner STILL appears (persistence, not just session-state).

5. **Hotfix bundle empirical verification (4 fixes)** — HF-01 Hospitality field persistence; HF-02 backend secrets dead (cross-device login still works under new credentials); HF-03 firebaseUid mismatch returns 403 (curl test); HF-04 socket handshake JWKS (chat thread receives messages only for the verified user's room).

6. **EN+RU parity** — Russian-language session per device covering at least the M2-new screens (Moderation Queue, Role Management, RejectionBanner with «Модератор JayTap» / «На модерации» strings).

7. **Dark/light parity** — dark-theme session covers M2-new surfaces (PropertyDetailsScreen mod footer; ArchiveListingModal; RoleManagementScreen self-row badge).

This plan is `autonomous: false` — the executor (Claude) builds the matrix scaffold and the user (the human) walks the cells with the v2.0.0 binary in hand. The user resumes execution with the `approved` signal once the walk completes.

PREREQUISITE: Per `06-STORE-HISTORY.md` `phase_3_4_paired_gates_prerequisite`, Phase 3 and Phase 4's `/gsd-verify-work` + `/gsd-code-review` gates MUST clear BEFORE this plan starts. The orchestrator's additional_context flagged that those gates haven't run yet. The QA matrix DOES NOT replace the verifier+reviewer gates; per memory `gsd-verifier-misses-regressions.md`, paired-gates discipline is mandatory. The Task 0 below is a hard prerequisite check.

Output: `06-QA-MATRIX.md` walked APPROVED; user resume signal `approved`; phase ready for Plan 07 submission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-06-SUMMARY.md
@.planning/phases/04-archive-lifecycle-owner-mod-admin/04-07-SUMMARY.md
@.planning/phases/05-admin-role-management-ui/05-05-SUMMARY.md
@.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-13-SUMMARY.md
@.planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md
@.planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md
@.planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-QA-MATRIX.md

<interfaces>
<!-- Devices + accounts (per CLAUDE.md M1 testing bar inheritance into M2; same hardware): -->

Devices:
- iPhone 15 Pro Max / iOS 26.x (Hermes + Fabric, Release config)
- Moto G XT2513V / Android 16 (Fabric, Release config)

Accounts (the user has these provisioned per Phase 5 Plan 05 SUMMARY):
- Admin: `beckprograms@gmail.com` (per memory + ROLE-07 baseline)
- Moderator: <to be promoted from a test user via the v2.0.0 admin RoleManagementScreen at walk time>
- Owner with rejected listing: <to be created via Phase 3 reject-flow during the walk>
- Plain user: <test account>

Build identity at walk time:
- Git SHA: <Plan 05 commit SHA — recorded in 06-QA-MATRIX.md frontmatter `walked_against_sha`>
- iOS build: `2.0.0 build <next_ios_build_number from 06-STORE-HISTORY.md>` (e.g., `2.0.0 build 27`)
- Android build: `2.0.0 versionCode <next_android_version_code>` (e.g., `2.0.0 versionCode 30`)
- Backend deploy SHA: <Plan 04 deploy SHA — recorded in 06-QA-MATRIX.md frontmatter `backend_walked_against_sha`>

Per memory `nav-overlay-hides-bottom-nav.md`: overlays (PropertyDetails / CreateListing / RenterListings / Tour3D / ModerationQueue / RoleManagement / LandlordApplicationQueue) hide the bottom tab bar by design. Cells that would require seeing the tab bar WHILE an overlay is still open are intentionally `— N/A`. DO NOT mark FAIL.

Per memory `last-admin-lockout-semantics.md`: ADMIN-03 last-admin lockout only fires on Promise.all race or count==1 self-demote (latter blocked first by SELF_MUTATION 403). Don't write a manual UAT row expecting the lockout outside a concurrent race.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: PREREQUISITE GATE — confirm Phase 3+4 paired-gates clear before scaffolding the matrix</name>
  <files>.planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md</files>
  <read_first>
    - .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md (read `phase_3_4_paired_gates_prerequisite` block — confirm `status: clear` at execution time)
    - .planning/STATE.md (sample for any recent /gsd-verify-work 3 / /gsd-code-review 3 / /gsd-verify-work 4 / /gsd-code-review 4 entries — these would be in the "Phase pipeline" section)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/gsd-verifier-misses-regressions.md (re-anchor the paired-gates discipline rule)
  </read_first>
  <action>
    Read `06-STORE-HISTORY.md`'s `phase_3_4_paired_gates_prerequisite.status` field.

    - If `status: clear` AND STATE.md / SUMMARY history confirms `/gsd-verify-work 3` + `/gsd-code-review 3` + `/gsd-verify-work 4` + `/gsd-code-review 4` ran clean — proceed to Task 1.
    - If `status: outstanding` OR the user has not confirmed paired-gates ran since Plan 06-01 was authored — HALT. Do NOT scaffold the QA matrix or schedule the walk. Surface to the user: "Phase 3+4 paired-gates discipline is a hard prerequisite per `gsd-verifier-misses-regressions.md`. Please run `/gsd-verify-work 3` + `/gsd-code-review 3` and `/gsd-verify-work 4` + `/gsd-code-review 4` first; resolve any defects; then resume Plan 06-06."

    The executor MUST surface this prerequisite check verbatim in the chat (visible to the user). The user's explicit "paired-gates clear" reply is the resume signal.
  </action>
  <verify>
    <automated>grep -A 2 'phase_3_4_paired_gates_prerequisite:' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md | grep -E '^\s*status:\s*clear'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 2 'phase_3_4_paired_gates_prerequisite:' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md | grep -E '^\s*status:\s*clear'` exits 0 — the match is context-anchored to the `phase_3_4_paired_gates_prerequisite:` YAML block (avoids matching unrelated `status: clear` lines elsewhere in the file). If the block says `outstanding`, the executor must HALT and surface to the user.
    - The user's explicit confirmation that paired-gates ran clean is recorded in this plan's SUMMARY (`paired_gates_clear: true` with timestamps for the four `/gsd-verify-work` + `/gsd-code-review` runs).
  </acceptance_criteria>
  <done>
    Either Phase 3+4 paired-gates are confirmed clear (proceed) OR the plan is HALTED and the user is informed. No QA matrix walk happens until paired-gates clear.
  </done>
</task>

<task type="auto">
  <name>Task 1: Scaffold 06-QA-MATRIX.md with all coverage cells (executor authors; user walks)</name>
  <files>.planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md</files>
  <read_first>
    - .planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-QA-MATRIX.md (full file — copy the multi-matrix shape: frontmatter, Cell Markers section, Test Environment table, Matrix 1..N tables with iOS / Android columns + evidence column, sign-off block)
    - .planning/phases/03-moderation-queue-actions-edit-on-behalf/03-06-SUMMARY.md lines 80-95 (sample the deferred Moto G items the matrix must absorb)
    - .planning/phases/04-archive-lifecycle-owner-mod-admin/04-07-SUMMARY.md lines 200-240 (sample Phase 4's QA matrix structure — the 11-row matrix is referenced; this plan extends it to dual-device)
    - .planning/phases/05-admin-role-management-ui/05-05-SUMMARY.md lines 70-95 (sample Phase 5's HAPPY_PATH_VERIFIED_EDGES_PENDING table — the edges go in Plan 06's matrix)
    - .planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-13-SUMMARY.md (re-anchor HF-01..HF-04 hotfix-bundle deliverables — Matrix 5 below tests these empirically)
  </read_first>
  <action>
    Author `06-QA-MATRIX.md` with the multi-matrix structure inherited from M1's `08-QA-MATRIX.md` template. Use the `Write` tool.

    **Section 1 — Frontmatter:**

    ```yaml
    ---
    phase: 06-hardening-manual-physical-device-qa-release
    plan: 06
    type: verification-matrix
    status: scaffolded
    created: <ISO 8601>
    walked: pending
    walked_against_sha: <Plan 06-05 commit SHA from `git rev-parse HEAD`>
    backend_walked_against_sha: <Plan 06-04 06-BACKEND-DEPLOY.md railway_deploy_sha>
    walk_disposition: pending
    devices:
      ios:
        model: iPhone 15 Pro Max
        os: iOS 26.x
        build: Fabric / Release
        binary: 2.0.0 build <next_ios_build_number from 06-STORE-HISTORY.md>
      android:
        model: Moto G XT2513V
        os: Android 16
        build: Fabric / Release
        binary: 2.0.0 versionCode <next_android_version_code from 06-STORE-HISTORY.md>
    accounts:
      admin: beckprograms@gmail.com
      moderator: "<promoted at walk time via Phase 5 RoleManagementScreen>"
      owner_with_rejected: "<created at walk time via Phase 3 reject-flow>"
      plain_user: "<test account>"
    requirements_addressed: [REL-03]
    testing_bar: M2 manual physical-device QA per CLAUDE.md
    walk_scope: cross-cutting M2 (Phases 1–5) on dual-device + 4 hotfix-bundle empirical verifications + EN+RU parity + dark/light parity
    paired_gates_prerequisite_clear: <true | false from 06-STORE-HISTORY.md + user confirmation>
    matrix_count: 7
    ---
    ```

    **Section 2 — Cell Markers + Test Environment + Memory note:**

    Inherit the structure from M1's `08-QA-MATRIX.md` lines 48-67 (cell markers ✅ PASS · ❌ FAIL · — N/A · ☐ Pending; Test Environment table; the `nav-overlay-hides-bottom-nav.md` memory note).

    **Section 3 — Matrix 1: Role Transitions (8 cells: 4 transitions × 2 devices)**

    Cells:
    | Cell | Transition | Device | Steps |
    |------|------------|--------|-------|
    | 1.1  | user → moderator (via admin promote) | iOS | Sign in as admin → RoleManagementScreen → search target user → RoleChangeModal → moderator chip → confirm; signed-in target user's app refreshes role within 60s; Moderation Queue row appears in Profile |
    | 1.2  | user → moderator | Android | Same |
    | 1.3  | moderator → admin (via admin promote) | iOS | Same flow as 1.1 with admin chip |
    | 1.4  | moderator → admin | Android | Same |
    | 1.5  | admin → user (via demotion — ROLE-11 invariant) | iOS | Sign in as different admin → RoleChangeModal → user chip; demoted user's NEXT protected request returns 403 (token rejected because iat < roleRevokedAt); RoleRefreshBanner shows; tap → re-login required |
    | 1.6  | admin → user (ROLE-11) | Android | Same |
    | 1.7  | concurrent demote race (both admins demote same target) | iOS + Android | Two devices both signed in as DIFFERENT admins; both attempt to demote target user simultaneously (race); per memory `last-admin-lockout-semantics.md`, last-admin-lockout only fires on count == 1-self-demote OR Promise.all race. Document outcome (one 200, one 409 OR both 200 with target user demoted). |

    **Section 4 — Matrix 2: Moderation Queue Actions with Two-Device Race (10 cells)**

    Per REL-03 explicit requirement: race-condition coverage on two devices simultaneously.

    Cells (the three TWO-DEVICE RACE cells — 2.4, 2.5, 2.6 — MUST include an `Audit row count` column populated from the live MongoDB `moderationlogs` collection after the race resolves; see Audit-Row-Count Sub-Step below):

    | Cell | Action | Device-A | Device-B | Expected | Audit row count |
    |------|--------|----------|----------|----------|-----------------|
    | 2.1 | Approve listing X | iOS (mod) | (idle) | Status flips to live; PropertyCard StatusPill removes pending; ModerationLog row 1 | n/a (single-action cell) |
    | 2.2 | Reject listing Y with reason chip | Android (mod) | (idle) | Status flips to rejected; reasonCode/reasonNote persist; owner sees RejectionBanner | n/a (single-action cell) |
    | 2.3 | Edit-on-behalf listing Z | iOS (mod) | (idle) | CreateListingScreen opens with moderatorContext banner + RejectionBanner suppressed; save dispatches editAsModerator; status flips to live; ownerUid preserved (defensive strip) | n/a (single-action cell) |
    | 2.4 | TWO-DEVICE RACE — Device A approves listing X / Device B rejects listing X SIMULTANEOUSLY | iOS (mod) | Android (mod) | Exactly ONE winner: approve OR reject persists. Loser sees 409 ALREADY_MODERATED toast (EN or RU per locale). The winning action's audit row is the persisted state. Both devices' queue refetches show X removed from queue. | MUST = 1 (count from `db.moderationlogs.countDocuments({targetId: "<X>"})`) |
    | 2.5 | TWO-DEVICE RACE — Device A approves / Device B edits-on-behalf SIMULTANEOUSLY | iOS (mod) | Android (mod) | Same race semantics: one winner; loser 409 | MUST = 1 (same query against listing's targetId) |
    | 2.6 | TWO-DEVICE RACE — Device A rejects / Device B archives (mod-archive) SIMULTANEOUSLY | iOS (mod) | Android (mod) | Same race semantics | MUST = 1 (same query against listing's targetId) |
    | 2.7 | Approve flow native Alert.alert confirm | iOS | — | Approve button → confirm prompt → 200 → toast | n/a |
    | 2.8 | Approve flow native Alert.alert confirm | Android | — | Same | n/a |
    | 2.9 | Reject flow chip selection (4 chips) | iOS | — | All 4 reasonCodes selectable; default `incomplete-info`; chip-deselection clears pendingReason | n/a |
    | 2.10 | Reject flow chip selection (4 chips) | Android | — | Same | n/a |

    **Audit-Row-Count Sub-Step (race cells 2.4 / 2.5 / 2.6, MANDATORY):** After the race resolves, the walker (or executor, if MongoDB query access is available from the QA station) runs the equivalent of `db.moderationlogs.countDocuments({targetId: "<X>"})` against the LIVE backend's MongoDB and records the integer in the matrix's `Audit row count` column. Three acceptable access paths:

    - Path A — `mongosh` from the QA station shell with the live MONGO_URI: `mongosh "<MONGO_URI>" --eval 'db.moderationlogs.countDocuments({targetId: ObjectId("<listingId>")})'`
    - Path B — Atlas Data API from a separate terminal: `curl -X POST "<atlas-data-api-endpoint>/action/aggregate" -H "Content-Type: application/json" -H "api-key: <atlas-data-api-key>" -d '{"dataSource":"<cluster>","database":"<db>","collection":"moderationlogs","pipeline":[{"$match":{"targetId":{"$oid":"<listingId>"}}},{"$count":"n"}]}'`
    - Path C — User runs the count from a separate terminal (Atlas web UI's Mongo Shell) and pastes the integer into the matrix.

    If MongoDB query access is not available at all from the QA station OR via the user's environment, the executor MUST surface this BEFORE the race walk starts and request the user expose a temporary admin-only debug endpoint (e.g., `GET /api/admin/moderation-logs/count?targetId=<X>` gated by `requireAuth + requireAdmin`) to read the count via curl. In all paths, the integer goes into the cell's `Audit row count` column AND `audit_row_count = 1` is the binding acceptance criterion for that cell.

    **Section 5 — Matrix 3: Archive Flows (12 cells absorbing Phase 4's 11-row matrix + dual-device)**

    Phase 4 walked these on iPhone only (per 04-07-SUMMARY); Phase 6 closes Moto G coverage:

    Cells:
    | Cell | Scenario | iOS | Android |
    |------|----------|-----|---------|
    | 3.1 | Owner archives own live listing → Pending tab | walked in Phase 4 (re-walk for v2.0.0 binary parity) | NEW — closes deferred Moto G |
    | 3.2 | Owner restores self-archived listing → Pending tab + re-moderation copy | re-walk | NEW |
    | 3.3 | Owner CANNOT restore mod-archived listing (D-13 conditional gate) | re-walk | NEW |
    | 3.4 | Mod archives any live listing via PropertyDetailsScreen → ArchiveListingModal 4-chip + note → status archived | re-walk | NEW |
    | 3.5 | Mod restores any archived listing → status pending | re-walk | NEW |
    | 3.6 | Admin sees Hard Delete on archived → DeleteListingModal → permanentlyDeletedToast → screen pops | re-walk | NEW |
    | 3.7 | Plain user sees no Archive/Restore/Hard-Delete affordances | re-walk | NEW |
    | 3.8 | Archived listings hidden from Home / Favorites public lanes (regression preserved) | re-walk | NEW |
    | 3.9 | Owner sees own archived in Archived tab (self + mod-archived both visible to owner) | re-walk | NEW |
    | 3.10 | EN+RU locale parity Rows 1+2+4 walked in Russian | re-walk | NEW |
    | 3.11 | Dark theme parity confirmed for ArchiveListingModal + RejectionBanner | re-walk | NEW |
    | 3.12 | ARCH-04 re-moderation copy: "back to the moderation queue for review before becoming public again" / «очередь модерации для проверки» | re-walk | NEW |

    **Section 6 — Matrix 4: Owner-Side Rejection Banner Persistence (4 cells)**

    Cells:
    | Cell | Scenario | iOS | Android |
    |------|----------|-----|---------|
    | 4.1 | Owner with rejected listing sees Home rejection banner; tap navigates to OwnerListings → Rejected tab | ✅ | ✅ |
    | 4.2 | Sign out → sign back in → banner STILL appears (persistence, not session-state) | ✅ | ✅ |
    | 4.3 | Edit and resubmit a rejected listing → status → pending → banner auto-dismisses | ✅ | ✅ |
    | 4.4 | Locale switch EN ↔ RU mid-session → banner copy updates with current locale (D-09 client-side i18n adaptation) | ✅ | ✅ |

    **Section 7 — Matrix 5: Hotfix Bundle Empirical Verification (4 fixes × 2 devices = 8 cells, with HF-03 as a cross-device curl test)**

    HF-01 — Hospitality field persistence:
    | Cell | Scenario | iOS | Android |
    |------|----------|-----|---------|
    | 5.1 | Create Hospitality (Hotel) listing with rooms=10, maxGuests=20, all 12 amenities → save → re-fetch via PropertyDetails → all values persist | ✅ | ✅ |

    HF-02 — Backend secrets dead:
    | Cell | Scenario | iOS | Android |
    |------|----------|-----|---------|
    | 5.2 | Sign in with current Mongo password (post-rotation per Plan 04 Section 3) → /api/auth/me returns 200; cross-reference with `06-BACKEND-DEPLOY.md` Section 3 timestamp | ✅ | ✅ |

    HF-03 — firebaseUid mismatch 403 (curl, executor-driven):
    | Cell | Scenario | curl test |
    |------|----------|-----------|
    | 5.3 | `curl -X POST <BACKEND>/api/auth/users -H "Authorization: Bearer <attacker-token>" -d '{"firebaseUid": "<victim-uid>", "email": "v@v.com"}'` returns 403 | executor runs from local shell |
    | 5.4 | Same with no Authorization header → returns 401 | executor runs |

    HF-04 — Socket handshake JWKS:
    | Cell | Scenario | iOS | Android |
    |------|----------|-----|---------|
    | 5.5 | Sign in as User A → ChatThreadScreen with User B; exchange messages; messages flow correctly | ✅ | ✅ |
    | 5.6 | Attacker socket attempt (curl + websocat scripted) tries to join `user:<victim-uid>` with attacker token → connection drops or refuses room subscription | executor runs |

    **Section 8 — Matrix 6: EN+RU Parity (per-screen Russian-language session)**

    Cells:
    | Cell | Screen | iOS RU | Android RU |
    |------|--------|--------|------------|
    | 6.1 | LoginScreen / SignupScreen | ✅ | ✅ |
    | 6.2 | RoleManagementScreen + RoleChangeModal (3-chip; 23 admin.roles.* keys) | ✅ | ✅ |
    | 6.3 | ModerationQueueScreen + RejectListingModal (4-chip + note) | ✅ | ✅ |
    | 6.4 | OwnerListings 4-tab segmented control (owner.listings.tab.{live,pending,rejected,archived}) | ✅ | ✅ |
    | 6.5 | RejectionBanner with reasonCode lookup in RU | ✅ | ✅ |
    | 6.6 | ArchiveListingModal (4-chip + note) | ✅ | ✅ |
    | 6.7 | DeleteListingModal | ✅ | ✅ |
    | 6.8 | Home rejection banner singular + plural | ✅ | ✅ |

    **Section 9 — Matrix 7: Dark/Light Parity (4 cells, M2-new surfaces only)**

    Cells:
    | Cell | Surface | iOS dark | Android dark |
    |------|---------|----------|--------------|
    | 7.1 | PropertyDetailsScreen mod-action footer (Approve / Reject / Edit-on-behalf / Archive / Restore / Hard-Delete buttons + StatusPill) | ✅ | ✅ |
    | 7.2 | RoleManagementScreen self-row "(you)" badge + 50-row footer hint | ✅ | ✅ |
    | 7.3 | RejectionBanner colors (warning palette) | ✅ | ✅ |
    | 7.4 | ArchiveListingModal + DeleteListingModal contrast | ✅ | ✅ |

    **Section 10 — Sign-off block:**

    ```markdown
    ## Sign-off

    | Field | Value |
    |-------|-------|
    | Walker | <user> |
    | Walk start | <ISO 8601> |
    | Walk end | <ISO 8601> |
    | Total cells walked | <count> |
    | PASS | <count> |
    | FAIL | <count, MUST be 0 for APPROVED disposition> |
    | N/A | <count, with brief rationale per N/A cell> |
    | Disposition | <APPROVED \| BLOCKED-BY-FAIL> |

    Bug-fix loop (per M1 D-08): If any cell FAILs, the executor opens a sub-task to fix the regression in-phase, re-walks the affected cell on both devices, then resumes. APPROVED disposition requires zero blocking FAILs.
    ```

    Use the `Write` tool. After scaffold is committed, the user walks the cells and updates the matrix in place. The Plan 06 close-out commit lands the walked matrix.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md && grep -c 'iPhone 15 Pro Max' .planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md && grep -c 'Moto G XT2513V' .planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md && grep -c '## Matrix' .planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md && grep -c 'TWO-DEVICE RACE' .planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-QA-MATRIX.md` exists with 7 matrices (Role Transitions / Moderation Race / Archive / Rejection Banner Persistence / Hotfix Bundle / EN+RU / Dark+Light)
    - `grep -c 'iPhone 15 Pro Max' <file>` returns ≥ 1
    - `grep -c 'Moto G XT2513V' <file>` returns ≥ 1
    - `grep -c 'TWO-DEVICE RACE' <file>` returns ≥ 3 (Matrix 2 cells 2.4, 2.5, 2.6 — REL-03's explicit two-device race-condition coverage)
    - `grep -c 'Audit row count' <file>` returns ≥ 1 (Matrix 2 race-cell schema includes the audit-row-count column per WARNING #3 fix)
    - `grep -c 'audit_row_count = 1' <file>` returns ≥ 1 (binding acceptance criterion encoded in the matrix scaffold)
    - `grep -c 'moderationlogs.countDocuments' <file>` returns ≥ 1 (the audit-query mechanism is documented in the Audit-Row-Count Sub-Step)
    - `grep -E '^\| ([0-9]+\.[0-9]+) ' <file> | wc -l` returns ≥ 50 (total cell row count across all matrices; rough lower bound based on 8 + 10 + 24 + 8 + 10 + 16 + 8 = 84 cells; allow some N/A consolidation)
    - `grep -c 'walk_disposition: pending' <file>` returns 1 in scaffold form (flips to APPROVED at walk close)
    - `grep -c 'paired_gates_prerequisite_clear:' <file>` returns 1 (flag must be true)
    - frontmatter `walked_against_sha:` is populated with Plan 05's commit SHA
    - frontmatter `backend_walked_against_sha:` is populated with Plan 04's deploy SHA
  </acceptance_criteria>
  <done>
    `06-QA-MATRIX.md` is committed in scaffolded form. The user receives a walk-through-this-matrix instruction in the next checkpoint task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walk the QA matrix on iPhone 15 Pro Max + Moto G XT2513V (REL-03)</name>
  <what-built>
    Plan 06-05 produced a v2.0.0 binary; Plan 06-04 deployed the matching backend. Plan 06-06 Task 1 scaffolded the QA matrix in `06-QA-MATRIX.md`. This checkpoint pauses execution while the user walks every cell on both physical devices and flips the cell markers.
  </what-built>
  <how-to-verify>
    1. **Build + install v2.0.0:**
       - iOS: open `ios/JayTap.xcworkspace` in Xcode 26.4+, select iPhone 15 Pro Max (physical device, USB-connected or wireless paired), Product → Run (Release scheme). Confirm app launches with build identity `2.0.0 (<next_ios_build_number>)` shown in About / debug surface.
       - Android: connect Moto G XT2513V via USB; `cd android && ./gradlew assembleRelease && adb install -r app/build/outputs/apk/release/app-release.apk` (or use `npx react-native run-android --variant=release`). Confirm app launches with `2.0.0` versionName + correct versionCode in About / debug surface.

    2. **Walk every matrix in `06-QA-MATRIX.md`** — each cell is 1 row in 1 of the 7 matrices. For each cell:
       - Perform the listed steps on the listed device.
       - Mark cell with ✅ PASS or ❌ FAIL or — N/A (with rationale).
       - For FAIL cells, attach evidence (screenshot path under `.planning/phases/06-hardening-manual-physical-device-qa-release/videos/` if needed; the videos dir is created on demand).
       - For TWO-DEVICE RACE cells (Matrix 2 cells 2.4 / 2.5 / 2.6), coordinate with the second device — both signed in, both navigated to the queue, both tap their action button as close to simultaneously as possible. Capture the timestamps of the 200 + 409 responses. **AFTER each race cell resolves**, run the Audit-Row-Count Sub-Step from Matrix 2 (`db.moderationlogs.countDocuments({targetId: "<listingId>"})` via `mongosh` / Atlas Data API / user-relayed integer / temporary admin debug endpoint) and record the resulting integer in the cell's `Audit row count` column. The cell PASSes ONLY if the observed outcome was 1×200 + 1×409 AND `audit_row_count = 1`. If `audit_row_count` is anything other than 1, the cell FAILs (regression of MOD-15 atomic findOneAndUpdate guard) — bug-fix loop applies.
       - **Race-cell escalation (T-06-33 mitigation)**: If the two devices cannot coordinate the race window after **3 retries** (e.g., network latency makes both 200 OR both 409 OR one device fails to fire its request), the cell does NOT auto-accept and is NOT marked PASS via supertest fallback. The executor surfaces to the user with two options: (a) use a coordinated tool/script (e.g., a small `node` snippet or `curl &` parallel invocation that fires both POSTs against the live backend within a tight window with two valid moderator tokens) to force an observable race; (b) explicitly defer race-coverage to a follow-up phase (e.g., Phase 6.5 inserted via `/gsd-insert-phase`) with the user's written approval recorded verbatim in `06-06-SUMMARY.md`. Only an observed-race outcome with `audit_row_count = 1` OR a user-approved formal deferral closes the cell.
       - For HF-03 + HF-04 cells (5.3 / 5.4 / 5.6), the executor runs the curl/websocat tests from the local shell — those don't need a device walk; their PASS/FAIL goes in the matrix as the curl exit code + response body.

    3. **Bug-fix loop (per M1 D-08):** If any cell FAILs, surface to the executor — the executor opens a sub-task to fix the regression in-phase, commits the fix atomically, then asks the walker to re-walk the affected cell on both devices.

    4. **Sign-off:**
       - Fill the Sign-off block at the bottom of `06-QA-MATRIX.md`: walker name (`beckprograms@gmail.com` per Plan 5 SUMMARY precedent), walk start + end timestamps, PASS / FAIL / N-A counts, disposition.
       - Disposition is APPROVED iff FAIL count = 0 (post-fix-loop). If any blocking FAIL remains, disposition is BLOCKED-BY-FAIL and the plan does NOT close.

    5. **Update frontmatter** `walk_disposition: pending` → `walk_disposition: APPROVED` AND `walked: pending` → `walked: <ISO 8601 walk-end timestamp>`.

    Resume by replying with `approved` once the matrix is walked and the disposition is APPROVED. If a FAIL is unresolvable in-phase (e.g., requires a Phase 6.5 backlog), the user explicitly downgrades the disposition and surfaces the carry-forward.
  </how-to-verify>
  <resume-signal>
    Reply with `approved` (after matrix walk + sign-off lands APPROVED), OR describe the blocking FAILs that need fix-loop iteration.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 05 binary → walked binary | The matrix walks the v2.0.0 archive built from Plan 05's commit SHA. Walking a stale binary (e.g., v1.0.4 still installed) would invalidate the entire matrix; the build-identity assertion in Step 1 of the checkpoint guards this. |
| Plan 04 backend → walked backend | The matrix walks against the live Railway deploy from Plan 04. A backend rollback between Plan 04 and Plan 06 would mean the QA results don't apply to the production state Plan 07 will submit against. |
| Two-device race → 1-winner invariant | The MOD-15 race-condition cells (2.4 / 2.5 / 2.6) require the LIVE backend's atomic `findOneAndUpdate` to produce exactly one 200 + one 409. A race that produces two 200s would be a regression of MOD-15. |
| Owner rejection banner persistence → cross-session | Cells 4.2 verifies persistence across sign-out + sign-in. Tampering with AsyncStorage (e.g., banner state cached in client-only storage that doesn't survive logout) would defeat persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-27 | Race-condition false-pass | Matrix 2 two-device cells | mitigate | Cells 2.4 / 2.5 / 2.6 require BOTH a 200 + 409 outcome AND ModerationLog audit row count = 1 (post-walk verification — the executor or user runs `db.moderationlogs.countDocuments({targetId: '<X>'})` and confirms 1). If both devices report 200, the test FAILED — that's a regression of MOD-15's atomic findOneAndUpdate guard, and the bug-fix loop must close it before APPROVED. |
| T-06-28 | Build-identity drift | Walker walks stale binary | mitigate | Step 1 of the checkpoint requires the device's About screen to show `2.0.0 (<next_ios_build_number>)` / `2.0.0 versionName + correct versionCode`. The executor cross-references this against `06-STORE-HISTORY.md` derived targets. If the device shows v1.0.4, the walker re-installs the v2.0.0 archive before walking. |
| T-06-29 | Spoofing | HF-03 / HF-04 attacker token paths skipped | mitigate | Cells 5.3 / 5.4 / 5.6 are executor-driven curl/websocat tests, not human-walked. The executor MUST run them and record the actual exit code + response. They're not bypassed by the human walk. |
| T-06-30 | Repudiation | Walk evidence missing | mitigate | The matrix file is committed to git after walk completion. PASS / FAIL marks + sign-off block are auditable. For FAIL cells, evidence (screenshots / video clips) lives under `.planning/phases/06-hardening-manual-physical-device-qa-release/videos/` per M1 precedent. |
| T-06-31 | Information Disclosure | Test accounts' real emails / Mongo uids in committed matrix | accept | The matrix mentions `beckprograms@gmail.com` (the user's own email — already in committed git history per M1 precedent) and refers to "<test account>" placeholders for non-admin walks. No real PII for end users is committed. |
| T-06-32 | Tampering | walk_disposition flipped to APPROVED without walking | mitigate | The user is the walker; the executor cannot self-approve. The resume signal `approved` is the user's binding sign-off. The frontmatter `walk_disposition: pending` flips ONLY by the user's edit. Plan 07 (downstream) reads this disposition as a binding gate. |
| T-06-33 | DoS | Two-device race coordination fails (one device delayed) | mitigate | Retry up to 3× per cell to coordinate the simultaneous taps. After 3 retries with no observable race, the cell does NOT auto-accept and is NOT marked PASS via supertest fallback. The executor escalates to the user with two options: (a) use a coordinated tool/script (e.g., a small `node` snippet or `curl &` parallel invocation that fires both POSTs against the live backend within a tight window with two valid moderator tokens) to force an observable race; (b) explicitly defer race-coverage to a follow-up phase (e.g., Phase 6.5 inserted via `/gsd-insert-phase`) with the user's written approval recorded verbatim in `06-06-SUMMARY.md`. The supertest case at Plan 03-05 is the backend-level invariant but does NOT, on its own, satisfy REL-03's binding two-device race requirement; only an observed-race cell or a user-approved formal deferral closes the cell. |

Block-on severity: high.
- T-06-27 (HIGH) — blocks if mitigation absent (race regression silently shipped)
- T-06-28 (HIGH) — blocks if mitigation absent (false-positive QA on stale binary)
- T-06-32 (HIGH) — blocks if mitigation absent (rubber-stamp approval defeats the gate)
</threat_model>

<verification>
- `06-QA-MATRIX.md` exists with 7 matrices, all cells walked
- `walk_disposition: APPROVED` lands in frontmatter
- Zero blocking FAILs
- Both devices walked (matrix has both `iPhone 15 Pro Max` and `Moto G XT2513V` PASS columns populated)
- Three two-device race cells produce 1-winner outcomes
- Four hotfix-bundle fixes (HF-01..HF-04) empirically verified
- EN+RU parity walked on at least one device per matrix-6 cell
- Dark/light parity walked
- Phase 3+4 paired-gates prerequisite confirmed clear in SUMMARY
</verification>

<success_criteria>
Plan 07 (Wave 4 — store submission) reads `walk_disposition: APPROVED` from `06-QA-MATRIX.md` and proceeds with archive build + store upload. The matrix is the binding evidence that v2.0.0 ships with M2's full surface working end-to-end on both target devices.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-manual-physical-device-qa-release/06-06-SUMMARY.md` with: walker, walk start + end, PASS/FAIL/N-A counts per matrix, disposition, paired-gates prerequisite confirmation, any FAIL → fix-loop iteration evidence, the v2.0.0 binary's git SHA + backend deploy SHA, and the close-out commit SHA.
</output>
