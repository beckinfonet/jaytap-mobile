---
type: milestone-archive
milestone: v2.0
name: M2 "Roles & Moderation"
status: shipped
shipped_date: 2026-05-05
phases: 6
plans_executed: 47
phase_45_inserted: true
phase_45_name: Landlord Application Workflow (inserted 2026-04-30, completed 2026-04-30)
shipped_ios_identifier: 2.0.0 build 27
shipped_android_identifier: 2.0.0 versionCode 30
xcode_toolchain: Xcode 26.4 (Build 17E192)
ios_submission_state: testflight_internal_testing
android_submission_state: play_console_internal_testing
archive_path_phases: .planning/milestones/v2.0-phases/
archived_at: 2026-05-05
known_deferred_at_close: 2 (audit-open) + 13 stale checkboxes (REQUIREMENTS.md drift; see Known Gaps)
---

# M2 "Roles & Moderation" — v2.0 (Archived)

**Shipped:** 2026-05-05 to App Store Connect (TestFlight Internal Testing, build 27) and Google Play Console (Internal Testing, versionCode 30). Production rollout is post-phase-exit (M1 D-12 inheritance — TestFlight Internal + Play Console Internal Testing visibility = shipped for milestone-close purposes).

This is the archived ROADMAP for the M2 milestone. Active roadmap is at `.planning/ROADMAP.md`. See `.planning/milestones/v2.0-REQUIREMENTS.md` for archived requirements.

---

## Goal

Replace M1's hardcoded admin email gate with a real three-role permission system, give moderators + admins a workflow to review listings before they go public, let owners + mods/admins archive listings, and close four security/data-integrity bugs surfaced during backend review — without adding any Firebase SDK to either repo.

## Hard Rules Carried Forward

- No Firebase SDK in the RN client repo (REPO RULE per memory `no-firebase-sdk.md`). Backend uses `jose` for JWKS verification, NOT `firebase-admin`.
- No Firebase custom claims as role source — MongoDB `userType` is authoritative.
- No `react-navigation` — custom `App.tsx` state machine stays.
- EN+RU bilingual parity for every new UI string (CI gate enforces).
- Manual physical-device QA on iPhone 15 Pro Max + Moto G XT2513V.

---

## Phase Summary (M2)

| # | Phase | Plans | Closed | Requirements |
|---|-------|-------|--------|--------------|
| 1 | Backend Role Foundation + Auth Migration + Hotfix Bundle | 13/13 | 2026-04-30 | HF-01..HF-04, ROLE-01..ROLE-11 |
| 2 | Listing Lifecycle Status Field Absorption | 9/9 | 2026-05-01 | MOD-01..MOD-09 |
| 3 | Moderation Queue + Actions + Edit-on-Behalf | 6/6 | 2026-05-02 | MOD-10..MOD-18 |
| 4 | Archive Lifecycle (Owner + Mod/Admin) | 7/7 | 2026-05-03 | ARCH-01..ARCH-05 |
| 4.5 | Landlord Application Workflow (inserted) | — | 2026-04-30 | (out-of-roadmap insertion; landlord application gate before listing-creation) |
| 5 | Admin Role Management UI | 5/5 | 2026-05-03 | ADMIN-01..ADMIN-07 |
| 6 | Hardening + Manual Physical-Device QA + Release | 7/7 | 2026-05-05 | REL-01..REL-06 |

**Totals:** 6 phases (+ Phase 4.5 inserted), 47 plans executed, 51 v1 requirements scoped, 38 strictly checked + 5 partial + 8 stale-bookkeeping at close.

---

## Phase 1: Backend Role Foundation + Auth Migration + Hotfix Bundle (13/13 plans)

**Goal:** Replace M1's hardcoded admin-email allowlist with a server-resolved three-role system backed by MongoDB as the role authority, migrate all 5 backend services to JWKS-verified Bearer tokens, and close four security/data-integrity bugs — without adding any Firebase SDK to either repo.

**Depends on:** Nothing (foundation phase; closes M1 GATE-05 D-22 Path B).

**Requirements:** HF-01, HF-02, HF-03, HF-04, ROLE-01..ROLE-11.

**Closed 2026-04-30** with 13 plans across 9 waves: shared `firebase.js` JWKS singleton via `jose.createRemoteJWKSet`; `verifyFirebaseToken` middleware (TDD 9-case golden token matrix + `requireMinRole`); 5-service auth migration (auth, property, favorite, chat, appointment) collapsing 5 inline auth blocks + deleting `chatAuthMiddleware.js`; HF-03 firebaseUid-spoof close on `POST /api/auth/users` (explicit field allowlist); HF-04 `io.use()` socket handshake with verified-only room join + D-04 dual-accept legacy path observable-only; production Mongo migration (`migrate-roles-m2.js` flipping `renter|owner|agent` → canonical 3-role enum); `User.userType` enum cutover (default `'user'`); RN client `Auth.ts` + `apiClient.ts` (Bearer + 401/403 single-flight + `_retry` guard) + `AuthService.refreshIdToken` Firebase REST refresh-token flow + `AuthContext.refreshRole()` + `RoleRefreshBanner` (sticky, optimistic-dismiss, useEffect rebase fixing W2 stale-closure regression) + 5-service Bearer migration on the client + `useRole.ts:64-65` allowlist branch + `src/constants/adminAllowlist.ts` deletion as the LAST step (Pitfall 5 ordering). M1 GATE-05 D-22 Path B accepted-risk row CLOSED. Backend test suite: 0 → 67 passing (Phase 1 baseline).

---

## Phase 2: Listing Lifecycle Status Field Absorption (9/9 plans)

**Goal:** Replace the "post = instantly live" model with a 4-state listing lifecycle (`pending` / `live` / `rejected` / `archived`) — surfaced to renters as filtering, to owners as a tab grouping with rejection messaging, and to mods/admins as deep-link visibility — without yet adding the Moderation Queue UI.

**Depends on:** Phase 1.

**Requirements:** MOD-01..MOD-09.

**Closed 2026-05-01** with 9 plans across 7 waves: `Property.status` Mongoose enum cutover dropping `'draft'` (Plan 02-02 production migration absorbed all draft rows to pending) and adding `'rejected'` + `'archived'`; default flips to `'pending'`; 9-field audit schema (`submittedAt` / `approvedAt` / `approvedByUid` / `rejectedAt` / `rejectedByUid` / `rejectionReasonCode` / `rejectionReasonNote` / `archivedAt` / `archivedByUid`); role-aware route filters via D-05/D-06/D-12; POST/PUT body-status sanitizer (D-22) + edit-resubmit auto-flip (D-15 `rejected` → `pending`); 4-tab segmented control on RenterListingsScreen with per-tab Hospitality + per-tab empty states; `<StatusPill>` mounted top-LEFT of PropertyCard (D-19); `<RejectionBanner>` per-session dismiss; `<HomeRejectionBanner>` with auto-dismiss + tap-to-rejected-tab nav; AppState `'active'` role-refresh hook with 60s cooldown (D-17 closes Phase 1 D-12 deferral). Manual physical-device QA matrix Rows 1–3 walked PASS on iPhone 15 Pro Max; Rows 4–9 deferred to Phase 3 (require artificial Mongo state-setup that Phase 3's moderation queue exercises naturally).

---

## Phase 3: Moderation Queue + Actions + Edit-on-Behalf (6/6 plans)

**Goal:** Give moderators and admins a dedicated overlay screen to review pending listings, with approve / reject / edit-on-behalf actions, canned bilingual rejection reasons, and race-condition-safe handling for two-mods-on-the-same-listing — reusing the M1-decomposed `CreateListingScreen` for edit-on-behalf rather than building a second editor.

**Depends on:** Phase 2.

**Requirements:** MOD-10..MOD-18.

**Closed 2026-05-02** with 6 plans across 4 waves: `/api/moderation/*` 4-handler backend (GET /queue + POST approve + POST reject + PUT /listings/:id) all race-safe via atomic `findOneAndUpdate` with status-filter lock primitive (loser → 409 ALREADY_MODERATED + verbatim MOD-15 toast); `ModerationLog` audit model + `serviceAreas` config (KG/KZ/UZ-shaped); 19 EN+RU `moderation.*` locale keys (18 actively consumed); `useRole.viewModerationQueue` action + 5 PropertyService methods with `canFromUser` belt-and-suspenders guards; reusable `<RejectListingModal>` (4-chip + note); `ModerationQueueScreen` overlay (FIFO + 3-button action row + AppState refresh + 409 race UX); ProfileScreen entry-point + pending-count badge; `PropertyDetailsScreen` mod-action footer + `<RejectListingModal>` sibling; `CreateListingScreen` `moderatorContext` prop + warning-stripe banner + `editAsModerator` dispatcher branch; `App.tsx` full wireup (`OVERLAY_FLAGS` + back-handler + `<PropertyDetailsHost>` extraction for LOC mitigation); 18-case backend supertest harness covering MOD-15 race (Promise.all) + MOD-16 audit + actorUid anti-spoofing per memory `gsd-verifier-misses-regressions.md`; manual physical-device smoke walked APPROVED on iPhone 15 Pro Max (9/9 PASS). Backend test suite: 67 → 85 passing.

---

## Phase 4: Archive Lifecycle (Owner + Mod/Admin) (7/7 plans)

**Goal:** Let owners archive their own listings without a reason, let mods/admins archive any listing with a structured reason, and route restoration through `pending` (preventing post-rejection bypass) — while making hard-delete admin-only.

**Depends on:** Phase 2 (status field) + Phase 3 (endpoint + audit-log patterns).

**Requirements:** ARCH-01..ARCH-05.

**Closed 2026-05-03** with 7 plans across 4 waves: `<ArchiveListingModal>` forked from `<RejectListingModal>` (4-line t() diff + Pitfall 7 dark-mode contrast preserved + 8 new EN+RU strings under `moderation.archive.*`); `archiveOwnListing` / `archiveAnyListing` / `restoreListing` / `hardDeleteListing` PropertyService methods (single role-aware `restoreListing` per PATTERNS §10); `useRole.ts` `Action` union extended with `archiveOwnListing` / `archiveAnyListing` / `hardDeleteListing` (3-tier defense: client `<Gated>` predicate + service-layer `canFromUser` + backend `requireMinRole`); backend race-safe atomic `findOneAndUpdate` archive + unarchive routes (D-13 two-condition gate `{_id, ownerUid, archivedByUid, status:'archived'}` blocks owner from restoring mod-archived listings — closes the post-rejection-bypass attack); D-15 selective field clears (preserve approve/reject metadata by omission); D-18 full pre-delete snapshot in ModerationLog audit row for hard-delete; `requireMinRole('admin')` middleware swap on DELETE /:id + Pitfall 2 inline ownership block deletion; PropertyDetailsScreen mod-action footer (Archive amber + Restore emerald + Hard Delete red); RenterListingsScreen Pitfall 6 atomic strip + archive rewire + canSelfRestore gate; 5 D-12 EN+RU locale value rewrites for re-moderation framing. Manual physical-device matrix walked APPROVED on iPhone 15 Pro Max (11/11 PASS). Backend test suite: 85 → 106 passing (20 new Phase 4 tests, 0 RED).

---

## Phase 4.5: Landlord Application Workflow (INSERTED)

**Inserted:** 2026-04-30 between Phases 4 and 5 to close a UX gap surfaced during Phase 4 device QA — the existing role-gating allowed any authenticated user to reach `CreateListingScreen` without an explicit "I want to list properties" capability check.

**Closed:** 2026-04-30.

**Delivered:** `User.canListProperties` capability flag (additive); `LandlordApplication` + `LandlordApplicationAuditLog` models; 5 endpoints (submit / get-mine / withdraw / admin-queue / admin-decide); `LandlordApplicationScreen` form (phone, ID photo, intent, optional note); `LandlordApplicationStatusBanner` on Profile (none / submitted / approved / rejected / withdrawn states); `LandlordApplicationQueueScreen` admin overlay (FIFO queue, approve / reject with 4-code canned reasons); `useRole.manageListings` gate tightened (admin/moderator implicit; user requires `canListProperties`); `reviewLandlordApplications` action added (admin-only); pre-flight gate on Profile/BottomNav `add` entry-points routes ungated users to the application form; `requireListingCapability` middleware on `POST /properties`; one-time `migrate-landlord-capability.js` auto-grants existing self-listers; ~50 EN+RU strings.

**Known open bug at close (M3 carry-forward):** Submitted apps land in Mongo with a `uid` that doesn't match the submitting Firebase uid. Diagnostic uids saved. See memory `phase45-landlord-application-uid-mismatch-bug.md`.

---

## Phase 5: Admin Role Management UI (5/5 plans)

**Goal:** Give admins an in-app screen to search users by email and change any user's `userType` between `user` / `moderator` / `admin`, with server-side guardrails (last-admin lockout, self-mutation prevention) and an append-only audit trail.

**Depends on:** Phase 1.

**Requirements:** ADMIN-01..ADMIN-07.

**Closed 2026-05-03** with 5 plans: `RoleChangeLog` Mongoose model (append-only audit) + `adminRoutes.js` mounted under `/api/admin` with router-level `verifyFirebaseToken + requireMinRole('admin')`; `GET /api/admin/users?query=<email>&limit=50` + `PATCH /api/admin/users/:uid/role` endpoints; server guardrails: ADMIN-04 self-mutation prevention (Step 1 — 403 SELF_MUTATION when actor uid matches target) + ADMIN-03 race-safe last-admin lockout (Step 6 preflight + Step 8 post-write Promise.all rollback); `roleRevokedAt = now` bumped on **demotion only** (Pitfall 3 — promotion does NOT force re-login); 17/17 backend supertest cases GREEN covering happy path + race rollback + actorUid anti-spoofing + envelope codes (SELF_MUTATION 403, LAST_ADMIN_LOCKOUT 409, NOT_FOUND 404). RN client: `useRole` Action union extended with `manageRoles` (admin-only); `UserService.searchUsers` + `setUserRole` (debounced 300ms); `<RoleChangeModal>` forked from `<RejectListingModal>` (3 chips with current-role de-emphasis + Pitfall 7 chip-deselection clears pendingRole); `<RoleManagementScreen>` overlay with self-row "(you)" badge non-tappable + 50-row footer hint + 23 `admin.roles.*` keys × EN+RU; ProfileScreen entry-point `<Gated action="manageRoles">`; App.tsx OVERLAY_FLAGS extension. **Paired gates clear:** UAT 11 PASS + 1 skipped-with-reason (Test 11 LAST_ADMIN_LOCKOUT reclassified pass-as-spec — fires only on Promise.all race per memory `last-admin-lockout-semantics.md`); REVIEW all 4 warnings fixed; SECURITY 27/27 threats closed; VALIDATION `nyquist_compliant: true`.

---

## Phase 6: Hardening + Manual Physical-Device QA + Release (7/7 plans)

**Goal:** Cross-cutting validation of every M2 surface (role transitions, moderation actions with race-condition coverage, archive flows, hotfix bundle, EN+RU + dark/light parity) on physical iOS + Android devices, followed by atomic v2.0.0 version bump and dual-store submission — applying the M1 D-02 lesson (query Play Console + TestFlight history BEFORE setting versionCode/build-number baseline).

**Depends on:** Phases 1–5.

**Requirements:** REL-01..REL-06.

**Closed 2026-05-05** with 7 plans across 4 waves:

- **Wave 1 (query-first artifacts):** `06-STORE-HISTORY.md` derives iOS 27 + Android 30 from `max(local, store)+1` per M1 D-02 lesson (RETROSPECTIVE.md Key Lesson #1); `06-INHERITANCE-AUDIT.md` 7/7 INHERIT dispositions with `package.json` byte-for-byte parity vs v1.0.4 baseline; `06-RELEASE-NOTES.md` EN 403 / RU 409 chars under Play Console's 500-char-per-locale binding limit; `06-BACKEND-DEPLOY.md` backend live at SHA `2fb5639` with all 7 Railway env vars present + Mongo password rotated post-2026-04-29 + AWS IAM PARTIAL documented (cross-project-shared OLD IAM user retains JayTap-bucket policy access — re-open condition logged) + `firebase-admin` confirmed absent from backend `package.json`.
- **Wave 2 (atomic version bump):** Commit `aa6f659` D-14 retro-fix restored Phase-3 role-grep CI gate via 5 source edits (ProfileScreen / HomeScreen / LandlordApplicationStatusBanner / RoleRefreshBanner / useRole.test.ts); commit `95b13c1` is the atomic v2.0.0 bump (`package.json` + iOS pbxproj `MARKETING_VERSION = 2.0.0` ×2 + iOS pbxproj `CURRENT_PROJECT_VERSION = 27` ×2 + Android `build.gradle` `versionName "2.0.0"` + Android `build.gradle` `versionCode 30`).
- **Wave 3 (manual physical-device QA matrix):** Walked APPROVED on iPhone 15 Pro Max + Moto G XT2513V — 7 matrices, 82 cells: 74 PASS / 3 PARTIAL / 5 DEFERRED-USER-APPROVED / 0 FAIL. Race-condition cells (1.7 + 2.4 + 2.5 + 2.6) deferred per T-06-33 option (b) with explicit user sign-off — backend MOD-15 + ROLE-11 atomic invariants covered by Phase 3 supertest suite + Phase 1 verifyFirebaseToken middleware tests. Cells 1.5/1.6 PARTIAL: backend ROLE-11 token-rejection verified; frontend mid-action 403 recovery UX (popup loading-state hangs) deferred to M3 backlog. Cell 5.6 (HF-04 attacker socket via websocat) and cell 5.3 (firebaseUid mismatch) DEFERRED-USER-APPROVED — backend invariant covered by tests; physical-device curl rig is M3 backlog. All four hotfix-bundle items (HF-01..HF-04) empirically verified end-to-end.
- **Wave 4 (dual-store submission):** Archived `95b13c1` in Xcode 26.4 (build 17E192) → uploaded to ASC TestFlight Internal Testing (build 27); built `app-release.aab` via `gradlew :react-native-reanimated:assembleRelease :app:bundleRelease` (per memory `android-reanimated-clean-prefab-gotcha.md` — `clean bundleRelease` wipes reanimated's release prefab) → uploaded to Play Console Internal Testing (versionCode 30); EN+RU release notes pasted in both stores (en-US + ru-RU localizations on ASC; en-US + ru-RU on Play Console release notes). M1 D-13 inheritance descope honored: 7/7 privacy + store-listing items INHERITED from v1.0.4. Per M1 D-12: TestFlight Internal + Play Console Internal Testing visibility = phase-exit; production rollout is post-phase-exit.

---

## Known Gaps at Close

Recorded for transparency; do not block close per user acknowledgement on 2026-05-05.

### Audit-open gaps (2)

- Phase 03 `03-HUMAN-UAT.md` — status `partial` (0 pending scenarios); Phase 03 behaviors re-validated end-to-end by Phase 6 QA matrix.
- Phase 03 `03-VERIFICATION.md` — status `human_needed`; same.

### REQUIREMENTS.md checkbox drift (13)

13 v1 REQ-IDs were not flipped `[x]` in REQUIREMENTS.md before close. Phase artifacts and the Phase 6 QA matrix walk validate end-to-end behavior; the missing flips are bookkeeping, not material gaps. The genuine M3 carry-forward inside this set is **ROLE-11 frontend mid-action 403 recovery UX**.

| REQ-ID | Bookkeeping vs material |
|--------|-------------------------|
| HF-01 (Hospitality field persistence) | Phase 1 hotfix shipped; checkbox not flipped |
| HF-02 (.env rotation, AWS IAM) | DONE per memory `aws-iam-jaytap-prod-s3.md` + REL-05; checkbox not flipped |
| ROLE-02 (Mongo migration script) | DONE — Phase 1 Plan 08 ran 2026-04-30 |
| ROLE-03 (JWKS middleware) | DONE — ROLE-04 (`[x]`) literally consumes it |
| ROLE-11 (roleRevokedAt invariant) | Backend DONE; **frontend popup-hang 403 recovery is M3 carry-forward** per memory `phase06-m3-carry-forward.md` |
| MOD-02 (legacy `status:missing` fallback) | DONE — Phase 2 D-07 client filter `(p.status ?? 'live') === 'live'` |
| MOD-03 (`[~]`) (POST defaults to 'pending' + submit-for-review copy) | Plan 02-09 walked; checkbox left at partial |
| MOD-04 (`GET /api/properties` filters to live for non-mod) | Phase 2 territory; assumed shipped |
| MOD-05 (`GET /:id` 404 for non-live deep-link) | Phase 2 territory; assumed shipped |
| MOD-06 (`[~]`) (4-tab segmented control) | Plan 02-06 + 02-09 shipped + walked PASS |
| MOD-07 (`[~]`) (StatusPill) | Plan 02-05 D-19 shipped |
| MOD-08 (`[~]`) (PropertyDetailsScreen RejectionBanner) | Plan 02-07 shipped |
| MOD-09 (`[~]`) (HomeRejectionBanner) | Plan 02-07 shipped |

### Genuine M3 carry-forwards

Per memory `phase06-m3-carry-forward.md`:

1. ROLE-11 mid-action 403 recovery UX (frontend popup-hang on demote-mid-action).
2. Race-cell test rig (cells 1.7 + 2.4–2.6 + 5.6 DEFERRED-USER-APPROVED + cell 5.3 mismatch path).
3. Android `clean bundleRelease` reanimated prefab gotcha — document in `scripts/release-android.md`.
4. AWS IAM cross-project residual — re-open when other project unblocks scoping the OLD shared IAM user's policy away from the JayTap bucket ARN.
5. Phase 4.5 landlord application uid-mismatch bug.

---

## Out of Scope (M2)

Same exclusions list as REQUIREMENTS.md `## Out of Scope` section — re-read there for the canonical list.

---

## Stats

- **Phases:** 6 (+ Phase 4.5 inserted)
- **Plans:** 47
- **Commits in M2 active phase:** 203 (`bc5e028` 2026-04-29 → `6448960` 2026-05-05)
- **Feat commits:** 54
- **Diff:** 207 files changed, 61,531 insertions, 2,324 deletions
- **TS/TSX LOC in src + App.tsx:** 22,800 (post-M2)
- **Timeline:** 7 days (2026-04-29 milestone start → 2026-05-05 ship)
- **Backend test suite growth:** 0 → 106 passing across 6 suites (Phase 1 baseline 67 → Phase 4 close 106)
- **Toolchain:** Xcode 26.4 (Build 17E192); RN 0.84.0 New Architecture (Fabric + Hermes both platforms); backend Node ≥22.12 (jose@6 ESM-only)

---

*Archived: 2026-05-05 by `/gsd-complete-milestone`. M1 GATE-05 D-22 Path B accepted-risk row CLOSED via this milestone's Phase 1 ROLE-03 + ROLE-04. Phase 6 4.5 (Landlord Application) was inserted out-of-roadmap on 2026-04-30 and is captured in this archive for traceability.*
