---
phase: 02-universal-keyboard-handling
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/screens/AccountSettingsScreen.tsx
  - src/screens/CreateListingScreen.tsx
autonomous: true
requirements: [KBD-01]
requirements_addressed: [KBD-01]
threat_model_complete: true
tags: [react-native, keyboard, kasv, account-settings, create-listing, scrollview-swap]

must_haves:
  truths:
    - "AccountSettingsScreen's <ScrollView> at line 150 is replaced by <KeyboardAwareScrollView> from react-native-keyboard-controller"
    - "ScrollView is removed from AccountSettingsScreen's react-native import line (no other usage in the file per PATTERNS verified-grep)"
    - "CreateListingScreen's admin-verify branch <ScrollView> at line 455 is replaced by <KeyboardAwareScrollView> per CONTEXT D-07"
    - "CreateListingScreen's main listing form <ScrollView> at line 497 is replaced by <KeyboardAwareScrollView> per CONTEXT D-07"
    - "ScrollView is removed from CreateListingScreen's react-native import line (only the two top-level uses are being converted; FlatList stays per PATTERNS)"
    - "All 3 KASV sites pass through their existing styles.scrollContent (CreateListing) or scrollContent (AccountSettings) verbatim — no style changes"
    - "All 3 KASV sites add keyboardShouldPersistTaps='handled' and bottomOffset={20} per RESEARCH §3.3 + §3.4"
    - "Internal nested structure of CreateListingScreen is NOT touched per CONTEXT D-07 (FlatList for image picker stays; nested sections stay)"
    - "Phase 4/5 ripples are accepted (D-08) — KASV wrappers may be reshaped during taxonomy decomposition; that's expected, not throwaway"
  artifacts:
    - path: "src/screens/AccountSettingsScreen.tsx"
      provides: "Imports KeyboardAwareScrollView; replaces line-150 ScrollView with KASV; removes ScrollView from react-native import"
      contains: "KeyboardAwareScrollView"
    - path: "src/screens/CreateListingScreen.tsx"
      provides: "Imports KeyboardAwareScrollView; replaces TWO top-level ScrollViews (admin-verify branch line 455 + main form line 497) with KASV; removes ScrollView from react-native import"
      contains: "KeyboardAwareScrollView"
  key_links:
    - from: "AccountSettingsScreen + CreateListingScreen (both branches)"
      to: "react-native-keyboard-controller package + KeyboardProvider context (Plan 01 + Plan 02)"
      via: "import { KeyboardAwareScrollView } from 'react-native-keyboard-controller';"
      pattern: "from 'react-native-keyboard-controller'"
    - from: "Existing styles.scrollView + styles.scrollContent (CreateListingScreen lines 990-996)"
      to: "Replaced KASV elements"
      via: "Same prop names accepted by KASV; verbatim pass-through"
      pattern: "contentContainerStyle={styles.scrollContent}"
---

<objective>
Replace the `ScrollView` element on three input-bearing sites with `KeyboardAwareScrollView`
from `react-native-keyboard-controller`:

1. **AccountSettingsScreen** (line 150 — single top-level ScrollView wrapping `renderInfoRow()` x4)
2. **CreateListingScreen admin-verify branch** (line 455 — Switch rows + submit button; no inputs today, but converts per D-07 and future-proofs Phase 3 admin text fields)
3. **CreateListingScreen main listing form** (line 497 — 19 TextInputs across nested sections)

Purpose: KBD-01 for D-12 Tier 1 rows 5-6 (CreateListing both branches) + Tier 2 row 9
(AccountSettings). Per CONTEXT D-07 + RESEARCH §3.3 + §3.4, these are the cleanest swaps in
the phase: one element rename + one import swap per site. Internal structure of CreateListing
is NOT touched per D-07 (Phase 4/5 will decompose it; ripples are accepted per D-08).

Output: Two files modified, two atomic commits (one per file so AccountSettings can be
verified independently of CreateListing in Plan 06's matrix walk). Three KASV sites total.

This plan is `autonomous: true`. Per-site physical-device verification happens in Plan 06.
All grep gates here are mechanical and self-checking.

Critical: Wave 2 entry gate — Task 1 of this plan re-verifies App.tsx has KeyboardProvider
wired (same gate as Plan 03 Task 1). Plans 03/04/05 are mutually independent (no shared
file edits) and can run in parallel under config.json `parallelization: true`. They share
no files, so execution order between them is irrelevant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-universal-keyboard-handling/02-CONTEXT.md
@.planning/phases/02-universal-keyboard-handling/02-RESEARCH.md
@.planning/phases/02-universal-keyboard-handling/02-PATTERNS.md
@.planning/phases/02-universal-keyboard-handling/02-VALIDATION.md
@.planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
@src/screens/AccountSettingsScreen.tsx
@src/screens/CreateListingScreen.tsx

<interfaces>
<!-- VERBATIM BEFORE state, copied from PATTERNS.md -->

ACCOUNTSETTINGSSCREEN — current ScrollView opening (PATTERNS line 605, src line 150):

  <ScrollView contentContainerStyle={styles.scrollContent} showsVerticalScrollIndicator={false}>
    {/* renderInfoRow x 4 (each conditionally renders a TextInput when isEditing) */}
  </ScrollView>

(closes at line 305)

ACCOUNTSETTINGSSCREEN — current import block (PATTERNS lines 612-628, src lines 1-15):

  import React, { useState, useEffect, useRef } from 'react';
  import {
      View,
      Text,
      StyleSheet,
      TouchableOpacity,
      Alert,
      TextInput,
      ScrollView,
      Switch,
      ActivityIndicator,
      Animated,
      LayoutChangeEvent,
  } from 'react-native';
  import { SafeAreaView } from 'react-native-safe-area-context';

PATTERNS confirms (line 630): ScrollView has no other use in the file (`grep -n "ScrollView"
AccountSettingsScreen.tsx` shows 3 matches: import line 9, opening tag 150, closing tag 305 —
safe to remove from the import).

CREATELISTINGSCREEN — Branch A admin-verify ScrollView (PATTERNS lines 502-525, src lines 455-477):

  <ScrollView style={styles.scrollView} contentContainerStyle={styles.scrollContent}>
    <Text style={[styles.hint, { color: colors.textSecondary, marginBottom: 16 }]}>
      {propertyToEdit?.title ? propertyToEdit.title : ''}
    </Text>
    <Text style={[styles.sectionTitle, { color: colors.text, marginBottom: 8 }]}>{t('verification.adminSectionTitle')}</Text>
    <Text style={[styles.hint, { color: colors.textSecondary, marginBottom: 20 }]}>{t('verification.adminSectionHint')}</Text>
    {verificationSwitchRow(t('verification.ownershipDocuments'), verifyOwnership, setVerifyOwnership)}
    {verificationSwitchRow(t('verification.ownerIdentity'), verifyOwnerId, setVerifyOwnerId)}
    {verificationSwitchRow(t('verification.stateIssued'), verifyStateDocs, setVerifyStateDocs)}
    <TouchableOpacity ...>
      {/* save button */}
    </TouchableOpacity>
  </ScrollView>

(no TextInputs in this branch today; converts per D-07 + future-proofs Phase 3)

CREATELISTINGSCREEN — Branch B main form ScrollView (PATTERNS lines 531-538, src lines 497-501):

  <ScrollView
    style={styles.scrollView}
    contentContainerStyle={styles.scrollContent}
    showsVerticalScrollIndicator={false}
  >
    {/* Transaction Type section + 19 TextInputs across nested sections — closes at line 956 */}
  </ScrollView>

CREATELISTINGSCREEN — current import block (PATTERNS lines 555-575, src lines 1-25):

  import React, { useState, useEffect } from 'react';
  import {
    View,
    Text,
    StyleSheet,
    ScrollView,
    TextInput,
    TouchableOpacity,
    StatusBar,
    Alert,
    ActivityIndicator,
    Switch,
    Platform,
    Image,
    FlatList,
    Dimensions,
  } from 'react-native';
  import DateTimePicker from '@react-native-community/datetimepicker';
  import { SafeAreaView } from 'react-native-safe-area-context';

PATTERNS confirms (lines 577-594): only TWO ScrollView uses in this file (lines 455 and 497).
After conversion both are gone. FlatList STAYS — it's used internally per the import.

CREATELISTINGSCREEN — styles.scrollView + styles.scrollContent (PATTERNS lines 543-549, src lines 990-996):

  scrollView: {
    flex: 1,
  },
  scrollContent: {
    padding: 20,
    paddingBottom: 40,
  },

These styles are PASSED THROUGH verbatim to KASV (KASV accepts the same `style` and
`contentContainerStyle` props per RESEARCH §3.3 + library API).

PATTERNS Pattern 1 — KASV import + props (apply to all 3 sites):

  import { KeyboardAwareScrollView } from 'react-native-keyboard-controller';

  // For CreateListing — pass through existing scrollContent style + scrollView style:
  <KeyboardAwareScrollView
    style={styles.scrollView}
    contentContainerStyle={styles.scrollContent}
    keyboardShouldPersistTaps="handled"
    bottomOffset={20}
    showsVerticalScrollIndicator={false}   // where original ScrollView had it (Branch B only)
  >

NESTED-SCROLLER GREP (RESEARCH §3.4 caveat — re-verify before merging CreateListing):

  grep -n "ScrollView\|FlatList" src/screens/CreateListingScreen.tsx

PATTERNS line 583-589 expects exactly 6 matches (import ScrollView + import FlatList + 2x
opening + 2x closing). If MORE than 6 OR a NESTED ScrollView appears, STOP — KASV nested
inside ScrollView is the banned mix per D-04 + RESEARCH §8 L7.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [Wave 2 entry gate] Verify Plan 02 (KeyboardProvider wiring) landed</name>
  <read_first>
    - .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md (must exist; must say "Wave 2 is unblocked")
    - App.tsx (verify KeyboardProvider import + JSX wrapper present)
  </read_first>
  <files></files>
  <action>
    Same gate as Plan 03 Task 1 — Wave 2 plans share the same precondition. No file edits.

      [ -f .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md ]
      grep -c "Wave 2 is unblocked" .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md  # MUST return ≥ 1
      grep -c "import { KeyboardProvider }" App.tsx                                                   # MUST return 1
      grep -c "<KeyboardProvider>" App.tsx                                                            # MUST return 1
      grep -B1 "<KeyboardProvider>" App.tsx | grep -c "<SafeAreaProvider>"                            # MUST return 1
      grep -A1 "<KeyboardProvider>" App.tsx | grep -c "<ThemeProvider>"                               # MUST return 1

    If any check fails, ABORT and surface to user. No commit in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md && grep -c "Wave 2 is unblocked" .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md && grep -c "<KeyboardProvider>" App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md` exits 0
    - `grep -c "Wave 2 is unblocked" .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md` returns ≥ 1
    - `grep -c "import { KeyboardProvider }" App.tsx` returns 1
    - `grep -c "<KeyboardProvider>" App.tsx` returns 1
    - `grep -B1 "<KeyboardProvider>" App.tsx | grep -c "<SafeAreaProvider>"` returns 1
    - `grep -A1 "<KeyboardProvider>" App.tsx | grep -c "<ThemeProvider>"` returns 1
    - No git commit in this task
  </acceptance_criteria>
  <done>Wave 2 entry gate verified — Plan cleared to proceed to Tasks 2-3.</done>
</task>

<task type="auto">
  <name>Task 2: Swap AccountSettingsScreen ScrollView → KeyboardAwareScrollView (KBD-01, RESEARCH §3.3)</name>
  <read_first>
    - src/screens/AccountSettingsScreen.tsx (verify imports at lines 1-15 and ScrollView opening at line 150 still match the verbatim BEFORE state in <interfaces>; abort if drift)
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md §AccountSettingsScreen (lines 599-630) for the verbatim BEFORE excerpt + the "ScrollView has no other use" PATTERNS-verified note
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md Pattern 1 (KASV import + props)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §3.3 (cleanest swap — element rename + import swap)
  </read_first>
  <files>src/screens/AccountSettingsScreen.tsx</files>
  <action>
    Pre-check:

      grep -c "KeyboardAwareScrollView" src/screens/AccountSettingsScreen.tsx                # MUST return 0
      grep -c "from 'react-native-keyboard-controller'" src/screens/AccountSettingsScreen.tsx # MUST return 0
      grep -nE "(^|[^a-zA-Z])ScrollView([^a-zA-Z]|$)" src/screens/AccountSettingsScreen.tsx
      # Expect 3 matches: import line 9, opening tag line 150, closing tag line 305 (per PATTERNS verified-grep)

    If KASV is already imported, STOP — task already ran.
    If standalone-ScrollView grep returns NOT-3, surface drift to user.

    Edit A — Remove `ScrollView,` from the react-native named-imports block (lines 2-14).
    The import block currently has 12 entries. After this edit it has 11. Find this line:

          ScrollView,

    DELETE that line entirely (preserving the surrounding 4-space indent of other entries).
    The new block should have:

      import {
          View,
          Text,
          StyleSheet,
          TouchableOpacity,
          Alert,
          TextInput,
          Switch,
          ActivityIndicator,
          Animated,
          LayoutChangeEvent,
      } from 'react-native';

    Edit B — Add KASV import in the third-party RN modules group, immediately AFTER the
    existing `import { SafeAreaView } from 'react-native-safe-area-context';` line:

      import { KeyboardAwareScrollView } from 'react-native-keyboard-controller';

    The two lines after the edit MUST read:

      import { SafeAreaView } from 'react-native-safe-area-context';
      import { KeyboardAwareScrollView } from 'react-native-keyboard-controller';

    Edit C — At line 150, replace the opening `<ScrollView ...>` tag with KASV. Per RESEARCH
    §3.3 add the two library-idiomatic props (`keyboardShouldPersistTaps="handled"` +
    `bottomOffset={20}`).

    Before (verbatim):

      <ScrollView contentContainerStyle={styles.scrollContent} showsVerticalScrollIndicator={false}>

    After (verbatim — multi-line for readability since prop count grew from 2 to 4):

      <KeyboardAwareScrollView
        contentContainerStyle={styles.scrollContent}
        keyboardShouldPersistTaps="handled"
        bottomOffset={20}
        showsVerticalScrollIndicator={false}
      >

    Edit D — At line 305, replace the closing `</ScrollView>` tag with `</KeyboardAwareScrollView>`.

    Do NOT touch:
    - Any of the renderInfoRow body / TextInput / Switch logic
    - styles or theme tokens
    - i18n strings

    Verify file consistency:

      grep -nE "(^|[^a-zA-Z])ScrollView([^a-zA-Z]|$)" src/screens/AccountSettingsScreen.tsx
      # MUST return 0 lines — no STANDALONE ScrollView identifier remains

      grep -c "KeyboardAwareScrollView" src/screens/AccountSettingsScreen.tsx
      # MUST return 3 (import + opening + closing — substring matches OK)

    Run TypeScript check:

      npx tsc --noEmit

    Commit atomically:

      git add src/screens/AccountSettingsScreen.tsx
      git commit -m "feat(02): swap AccountSettingsScreen ScrollView → KeyboardAwareScrollView (KBD-01, D-04)"

    Commit body: notes the cleanest swap in the phase (4 edits: remove ScrollView from RN
    import, add KASV import, rename element, rename closing tag); notes Plan 06 D-12 Tier 2
    row 9 verifies on device.
  </action>
  <verify>
    <automated>grep -c "from 'react-native-keyboard-controller'" src/screens/AccountSettingsScreen.tsx && grep -c "<KeyboardAwareScrollView" src/screens/AccountSettingsScreen.tsx && grep -c "</KeyboardAwareScrollView>" src/screens/AccountSettingsScreen.tsx && grep -cE "(^|[^a-zA-Z])ScrollView([^a-zA-Z]|$)" src/screens/AccountSettingsScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from 'react-native-keyboard-controller'" src/screens/AccountSettingsScreen.tsx` returns 1
    - `grep -c "import { KeyboardAwareScrollView }" src/screens/AccountSettingsScreen.tsx` returns 1
    - `grep -c "<KeyboardAwareScrollView" src/screens/AccountSettingsScreen.tsx` returns 1
    - `grep -c "</KeyboardAwareScrollView>" src/screens/AccountSettingsScreen.tsx` returns 1
    - `grep -c "keyboardShouldPersistTaps=\"handled\"" src/screens/AccountSettingsScreen.tsx` returns 1
    - `grep -c "bottomOffset={20}" src/screens/AccountSettingsScreen.tsx` returns 1
    - `grep -c "contentContainerStyle={styles.scrollContent}" src/screens/AccountSettingsScreen.tsx` returns 1 (existing prop preserved verbatim)
    - `grep -c "showsVerticalScrollIndicator={false}" src/screens/AccountSettingsScreen.tsx` returns 1 (existing prop preserved verbatim)
    - `grep -cE "(^|[^a-zA-Z])ScrollView([^a-zA-Z]|$)" src/screens/AccountSettingsScreen.tsx` returns 0 (no standalone ScrollView identifier remains)
    - `grep -c "keyboardVerticalOffset" src/screens/AccountSettingsScreen.tsx` returns 0
    - `npx tsc --noEmit` exits 0 (or no NEW errors vs pre-task baseline)
    - `git diff HEAD~1 --name-only` returns ONLY `src/screens/AccountSettingsScreen.tsx`
    - `git log --oneline -1` shows a commit message containing "AccountSettings", "KeyboardAware", "KBD-01"
  </acceptance_criteria>
  <done>AccountSettingsScreen's single ScrollView is fully swapped to KASV. ScrollView removed from react-native imports. Atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Swap CreateListingScreen BOTH top-level ScrollViews → KeyboardAwareScrollView (KBD-01, D-07, RESEARCH §3.4)</name>
  <read_first>
    - src/screens/CreateListingScreen.tsx (verify imports at lines 1-25, Branch A at lines 455-477, Branch B at lines 497-501, styles at lines 990-996 still match the verbatim BEFORE state in <interfaces>; abort if drift)
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md §CreateListingScreen (lines 496-594) for the verbatim BEFORE excerpts + the "no nested ScrollView" PATTERNS-verified note
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md Pattern 1 (KASV import + props)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §3.4 (D-07 — both branches; internal structure NOT touched)
    - .planning/phases/02-universal-keyboard-handling/02-CONTEXT.md D-07 + D-08 (both ScrollViews convert; Phase 4/5 ripples accepted)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §8 L7 (KASV nested in ScrollView is banned mix — pre-grep guard)
  </read_first>
  <files>src/screens/CreateListingScreen.tsx</files>
  <action>
    Pre-check (CRITICAL — verifies no nested ScrollView added since 2026-04-22):

      grep -c "KeyboardAwareScrollView" src/screens/CreateListingScreen.tsx                # MUST return 0
      grep -c "from 'react-native-keyboard-controller'" src/screens/CreateListingScreen.tsx # MUST return 0
      grep -nE "(^|[^a-zA-Z])ScrollView([^a-zA-Z]|$)" src/screens/CreateListingScreen.tsx
      # Expect EXACTLY 5 matches:
      #   line ~6:   ScrollView,         (named import)
      #   line ~455: <ScrollView         (Branch A opening)
      #   line ~477: </ScrollView>       (Branch A closing)
      #   line ~497: <ScrollView         (Branch B opening)
      #   line ~956: </ScrollView>       (Branch B closing)
      # If MORE than 5 OR a nested ScrollView appears anywhere, STOP and surface to user.
      # KASV nested inside ScrollView is the banned mix per D-04 + RESEARCH §8 L7.

      grep -nE "(^|[^a-zA-Z])FlatList([^a-zA-Z]|$)" src/screens/CreateListingScreen.tsx
      # Expect ≥ 1 match (import line + internal use(s) for image picker rows).
      # FlatList stays — it is NOT being converted.

    If pre-check shows drift, ABORT and surface to user.

    Edit A — Remove `ScrollView,` from the react-native named-imports block (line 6).
    Find this line in the named imports list:

        ScrollView,

    DELETE entirely. New block should look like (Note: existing 2-space indent per
    CreateListingScreen's style — different from AccountSettings which uses 4-space):

      import {
        View,
        Text,
        StyleSheet,
        TextInput,
        TouchableOpacity,
        StatusBar,
        Alert,
        ActivityIndicator,
        Switch,
        Platform,
        Image,
        FlatList,
        Dimensions,
      } from 'react-native';

    FlatList STAYS — used internally per PATTERNS line 590.

    Edit B — Add KASV import in the third-party RN modules group, immediately AFTER the
    existing `import { SafeAreaView } from 'react-native-safe-area-context';` line:

      import { KeyboardAwareScrollView } from 'react-native-keyboard-controller';

    Edit C — Branch A admin-verify (line 455). Replace `<ScrollView ...>` opening tag with KASV:

    Before (verbatim):

      <ScrollView style={styles.scrollView} contentContainerStyle={styles.scrollContent}>

    After (verbatim — add `keyboardShouldPersistTaps` + `bottomOffset` per RESEARCH §3.4):

      <KeyboardAwareScrollView
        style={styles.scrollView}
        contentContainerStyle={styles.scrollContent}
        keyboardShouldPersistTaps="handled"
        bottomOffset={20}
      >

    Note: Branch A has no inputs today (Switch rows + button only), so KASV degrades to an
    inert ScrollView when no field is focused (per RESEARCH §3.4). The conversion is required
    by D-07 and future-proofs Phase 3 (which may add admin-only text fields here).

    Edit D — Branch A closing tag at line 477. Replace `</ScrollView>` with
    `</KeyboardAwareScrollView>`.

    Edit E — Branch B main listing form (line 497). Replace opening `<ScrollView ...>`:

    Before (verbatim):

      <ScrollView
        style={styles.scrollView}
        contentContainerStyle={styles.scrollContent}
        showsVerticalScrollIndicator={false}
      >

    After (verbatim — same 3 props passed through; add `keyboardShouldPersistTaps` +
    `bottomOffset`):

      <KeyboardAwareScrollView
        style={styles.scrollView}
        contentContainerStyle={styles.scrollContent}
        showsVerticalScrollIndicator={false}
        keyboardShouldPersistTaps="handled"
        bottomOffset={20}
      >

    Edit F — Branch B closing tag at line 956. Replace `</ScrollView>` with
    `</KeyboardAwareScrollView>`.

    Do NOT touch (per CONTEXT D-07 + D-08 — internal structure stays):
    - Any of the 19 TextInputs across nested sections
    - The internal FlatList (image picker rows)
    - Any nested View / sectioned layout
    - The 3x verificationSwitchRow calls
    - The save/submit button
    - styles, theme tokens, i18n strings
    - DateTimePicker integration

    Verify file consistency after all edits:

      grep -cE "(^|[^a-zA-Z])ScrollView([^a-zA-Z]|$)" src/screens/CreateListingScreen.tsx
      # MUST return 0 — no standalone ScrollView identifier remains anywhere

      grep -c "KeyboardAwareScrollView" src/screens/CreateListingScreen.tsx
      # MUST return 5 (import + 2x opening + 2x closing; substring matches OK)

      grep -cE "(^|[^a-zA-Z])FlatList([^a-zA-Z]|$)" src/screens/CreateListingScreen.tsx
      # MUST return ≥ 1 (import + internal use(s) preserved)

    Run TypeScript check:

      npx tsc --noEmit

    Commit atomically:

      git add src/screens/CreateListingScreen.tsx
      git commit -m "feat(02): swap CreateListingScreen BOTH top-level ScrollViews → KeyboardAwareScrollView (KBD-01, D-07)"

    Commit body: notes BOTH Branch A (admin-verify line 455) and Branch B (main form line 497)
    are converted per CONTEXT D-07; notes internal structure (FlatList, nested sections, 19
    TextInputs) NOT touched per D-07; notes Phase 4/5 ripples accepted per D-08 — wrappers
    may be reshaped during taxonomy decomposition; notes Plan 06 D-12 Tier 1 rows 5+6 verify
    on device.
  </action>
  <verify>
    <automated>grep -c "from 'react-native-keyboard-controller'" src/screens/CreateListingScreen.tsx && grep -c "<KeyboardAwareScrollView" src/screens/CreateListingScreen.tsx && grep -c "</KeyboardAwareScrollView>" src/screens/CreateListingScreen.tsx && grep -cE "(^|[^a-zA-Z])ScrollView([^a-zA-Z]|$)" src/screens/CreateListingScreen.tsx && grep -cE "(^|[^a-zA-Z])FlatList([^a-zA-Z]|$)" src/screens/CreateListingScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from 'react-native-keyboard-controller'" src/screens/CreateListingScreen.tsx` returns 1
    - `grep -c "import { KeyboardAwareScrollView }" src/screens/CreateListingScreen.tsx` returns 1
    - `grep -c "<KeyboardAwareScrollView" src/screens/CreateListingScreen.tsx` returns 2 (Branch A + Branch B opening tags)
    - `grep -c "</KeyboardAwareScrollView>" src/screens/CreateListingScreen.tsx` returns 2 (Branch A + Branch B closing tags)
    - `grep -c "keyboardShouldPersistTaps=\"handled\"" src/screens/CreateListingScreen.tsx` returns 2 (one per KASV)
    - `grep -c "bottomOffset={20}" src/screens/CreateListingScreen.tsx` returns 2 (one per KASV)
    - `grep -c "contentContainerStyle={styles.scrollContent}" src/screens/CreateListingScreen.tsx` returns 2 (existing prop preserved on both)
    - `grep -c "style={styles.scrollView}" src/screens/CreateListingScreen.tsx` returns 2 (existing prop preserved on both)
    - `grep -c "showsVerticalScrollIndicator={false}" src/screens/CreateListingScreen.tsx` returns ≥ 1 (Branch B preserved this prop)
    - `grep -cE "(^|[^a-zA-Z])ScrollView([^a-zA-Z]|$)" src/screens/CreateListingScreen.tsx` returns 0 (no standalone ScrollView identifier)
    - `grep -cE "(^|[^a-zA-Z])FlatList([^a-zA-Z]|$)" src/screens/CreateListingScreen.tsx` returns ≥ 1 (FlatList still imported + used internally)
    - `grep -c "keyboardVerticalOffset" src/screens/CreateListingScreen.tsx` returns 0
    - `grep -c "verificationSwitchRow" src/screens/CreateListingScreen.tsx` returns ≥ 3 (Branch A's 3 Switch rows untouched)
    - `npx tsc --noEmit` exits 0 (or no NEW errors)
    - `git diff HEAD~1 --name-only` returns ONLY `src/screens/CreateListingScreen.tsx`
    - `git log --oneline -1` shows a commit message containing "CreateListing", "KeyboardAware", "KBD-01", "D-07"
  </acceptance_criteria>
  <done>CreateListingScreen both top-level ScrollViews swapped to KASV per D-07. ScrollView removed from react-native imports. FlatList preserved. Internal structure untouched. Atomic commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan replaces existing ScrollView elements with KASV
on three sites within two existing screens. No new auth surface, no new HTTP, no new storage,
no PII handling. The replacement component is from a trusted package supply-chain-vetted in
Plan 01.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | T (Tampering) | Nested ScrollView accidentally introduced inside KASV (banned mix per D-04 + RESEARCH §8 L7) | mitigate | Task 3 pre-check greps for ALL ScrollView occurrences in CreateListingScreen.tsx and aborts if more than 5 (the canonical count per PATTERNS). Phase exit gate (Plan 06) re-greps. KASV-inside-ScrollView causes gesture conflicts and would be detected during physical-device testing in Plan 06. |
| T-02-04-02 | T (Tampering) | Magic-number keyboardVerticalOffset accidentally introduced | mitigate | Tasks 2+3 each include `grep -c "keyboardVerticalOffset" returns 0` acceptance criterion. Phase exit gate re-greps `src/` recursively. PATTERNS Pattern 1 explicitly notes `bottomOffset={20}` is NOT a magic number. |
| T-02-04-03 | T (Tampering) | FlatList accidentally removed from CreateListingScreen during ScrollView import cleanup | mitigate | Task 3 acceptance criterion explicitly verifies FlatList is still imported and used (`grep -cE "(^\|[^a-zA-Z])FlatList([^a-zA-Z]\|$)" returns ≥ 1`). FlatList is internal to image picker rows per PATTERNS — orthogonal to keyboard work. |
| T-02-04-04 | I (Information Disclosure) | KASV wrapping forms with PII fields (AccountSettings: email, name, phone; CreateListing: address, contact info) | accept | KASV is a presentation-layer scroll container; it does not access TextInput value, secureTextEntry, or any other prop content. The same PII flowed through ScrollView before this plan; trust posture is unchanged. |

No S/R/D/E applicable. Block on `high` severity per project default; zero high-severity threats raised.
Phase 2 is presentational wiring — `<security_threat_model>` from the downstream consumer brief
noted "no new threat surface" with the nested-ScrollView pre-check being the only meaningful
mechanical safeguard.
</threat_model>

<verification>
- `grep -c "from 'react-native-keyboard-controller'" src/screens/AccountSettingsScreen.tsx src/screens/CreateListingScreen.tsx` totals 2 (one per file)
- `grep -c "<KeyboardAwareScrollView" src/screens/AccountSettingsScreen.tsx` returns 1; `grep -c "<KeyboardAwareScrollView" src/screens/CreateListingScreen.tsx` returns 2
- `grep -cE "(^|[^a-zA-Z])ScrollView([^a-zA-Z]|$)" src/screens/AccountSettingsScreen.tsx src/screens/CreateListingScreen.tsx` totals 0 (no standalone ScrollView remains)
- `grep -cE "(^|[^a-zA-Z])FlatList([^a-zA-Z]|$)" src/screens/CreateListingScreen.tsx` returns ≥ 1 (FlatList preserved)
- `grep -rn "keyboardVerticalOffset" src/screens/AccountSettingsScreen.tsx src/screens/CreateListingScreen.tsx` returns 0 (no anti-pattern)
- `git diff HEAD~2 -- src/locales/en.json src/locales/ru.json` returns empty (no copy changes — Phase 2 introduces zero new strings)
- `npx tsc --noEmit` exits 0 after both commits
- 2 atomic commits in `git log --oneline | head -2` (one per file)
- 02-VALIDATION.md row "Wave 2 settings + listing" PASSES
</verification>

<success_criteria>
- KBD-01 partial ✓ (2 of 8 Tier 1 screens covered for CreateListing branches A+B + Tier 2 row 9
  AccountSettings) — combined with Plan 03 (4 auth screens) and Plan 05 (2 chat screens) the
  full Tier 1+2 set is wired
- AccountSettingsScreen has 1 KASV (line 150 area)
- CreateListingScreen has 2 KASVs (Branch A admin-verify + Branch B main form per D-07)
- ScrollView import dropped from both files; FlatList preserved in CreateListing
- Internal nested structure of CreateListing untouched per D-07 (Phase 4/5 ripples accepted per D-08)
- 2 atomic commits — each per-file change can be bisected independently if Plan 06 device
  verification surfaces a per-screen regression
- Plan 06 (Wave 3 verification matrix) Tier 1 rows 5-6 + Tier 2 row 9 ready to test on device
</success_criteria>

<output>
After completion, create `.planning/phases/02-universal-keyboard-handling/02-04-SUMMARY.md` documenting:
- 2 commit SHAs (one per file)
- Confirmation: ScrollView import dropped from both files (mechanical grep)
- Confirmation: FlatList preserved in CreateListingScreen (mechanical grep)
- Confirmation: en.json + ru.json zero-diff (verified by `git diff HEAD~2 -- src/locales/`)
- Verbatim diff snippet for AccountSettingsScreen showing the BEFORE/AFTER swap
- Verbatim diff snippet for CreateListingScreen Branch B showing the BEFORE/AFTER swap (Branch A is structurally identical to B's diff)
- Note: The 19 TextInputs in CreateListingScreen Branch B are sampled in Plan 06 D-12 Tier 1 row 6 — top, middle, and bottom of form
- Note: Branch A has no inputs today; KASV degrades to inert ScrollView; future-proofs Phase 3
- Explicit statement: "Wave 2 (settings + listing slice) complete — Plan 06 Tier 1 rows 5-6 and Tier 2 row 9 ready for device matrix walk."
</output>
</content>
</invoke>