---
phase: 02-universal-keyboard-handling
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - App.tsx
autonomous: false
requirements: [KBD-02, KBD-04]
requirements_addressed: [KBD-02, KBD-04]
threat_model_complete: true
tags: [react-native, keyboard, provider-wiring, root-render-tree]

must_haves:
  truths:
    - "App.tsx imports KeyboardProvider from 'react-native-keyboard-controller' (added to import group 2 — third-party RN modules)"
    - "App.tsx provider tree is exactly: SafeAreaProvider → KeyboardProvider → ThemeProvider → LanguageProvider → AuthProvider → AppContent (KeyboardProvider slotted between SafeAreaProvider and ThemeProvider per CONTEXT D-03)"
    - "The existing provider order from CLAUDE.md Key Conventions (ThemeProvider → LanguageProvider → AuthProvider) is preserved verbatim — only KeyboardProvider is newly wrapping above them"
    - "App boots on both physical devices (iPhone 15 Pro Max / iOS 26.4 + Moto G XT2513V / Android 16) with no visible regression — provider is a context-only wrapper, no render output of its own"
    - "No red box at startup; no 'KeyboardProvider' Metro warnings; no 'Reanimated is not configured properly' runtime error"
  artifacts:
    - path: "App.tsx"
      provides: "Imports KeyboardProvider from react-native-keyboard-controller and wraps SafeAreaProvider's only child with <KeyboardProvider>...</KeyboardProvider>"
      contains: "KeyboardProvider"
  key_links:
    - from: "App.tsx import block (line ~3)"
      to: "react-native-keyboard-controller package installed in Plan 01 Task 3"
      via: "ES module import — third-party RN modules group, adjacent to react-native-safe-area-context"
      pattern: "from 'react-native-keyboard-controller'"
    - from: "App.tsx App() function (line ~948)"
      to: "All sub-providers (ThemeProvider/LanguageProvider/AuthProvider) and AppContent"
      via: "<KeyboardProvider> wraps the entire sub-tree so library-provided KASV/KAV components mount under a live KeyboardProvider context"
      pattern: "<KeyboardProvider>"
---

<objective>
Wire the just-installed `react-native-keyboard-controller` library into the App.tsx render tree by inserting `<KeyboardProvider>` between `SafeAreaProvider` and `ThemeProvider` per CONTEXT D-03. This is the SINGLE root-level wiring change that satisfies KBD-02's "universal, not per-screen patching" bar — every per-screen wrapper in Wave 2 (Plans 03/04/05) depends on a live KeyboardProvider context.

Purpose: KBD-02 (single root-level solution) + KBD-04 (Fabric / New Architecture verified — boot smoke confirms the provider mounts cleanly on both platforms with the new native modules linked).

Output: One file modified (`App.tsx`), one new import line, one new JSX wrapper pair. Per RESEARCH §4 Wave 1, this is a ~3-5 line diff. Plus a physical-device boot smoke check confirming no regression.

Critical: This plan is `autonomous: false`. Per the downstream consumer contract, Wave 1 gates on Wave 0's `[BLOCKING]` install verification grep set returning all-pass before any JSX edit lands — Task 1 of this plan is the gate-check (and aborts if Wave 0 didn't actually pass). Then Task 2 makes the JSX edit, and Task 3 is a human-gated boot smoke check on both physical devices before Wave 2 may begin.

Why misplacement is load-bearing: PITFALLS §Pitfall 2 + PATTERNS Pattern 3 specify that putting KeyboardProvider in the wrong slot (e.g., inside ThemeProvider rather than outside it) makes EVERY downstream library component (KASV / KAV in Wave 2) silently no-op. There is no compile error, no runtime warning — just zero keyboard avoidance behavior. The exact 4-line region between `<SafeAreaProvider>` and `<ThemeProvider>` is the only correct slot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-universal-keyboard-handling/02-CONTEXT.md
@.planning/phases/02-universal-keyboard-handling/02-RESEARCH.md
@.planning/phases/02-universal-keyboard-handling/02-PATTERNS.md
@.planning/phases/02-universal-keyboard-handling/02-VALIDATION.md
@.planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md
@CLAUDE.md
@App.tsx

<interfaces>
<!-- Verbatim BEFORE state of App.tsx, copied from PATTERNS.md §App.tsx (lines 168-196) -->
<!-- The executor should NOT need to re-read PATTERNS.md to look this up. -->

VERBATIM CURRENT App() function (App.tsx:948-960 per PATTERNS):

  function App() {
    return (
      <SafeAreaProvider>
        <ThemeProvider>
          <LanguageProvider>
            <AuthProvider>
              <AppContent />
            </AuthProvider>
          </LanguageProvider>
        </ThemeProvider>
      </SafeAreaProvider>
    );
  }

VERBATIM CURRENT import block (relevant lines, App.tsx:1-3, 23-25 per PATTERNS):

  import React, { useState, useEffect, useCallback, useRef } from 'react';
  import { View, ActivityIndicator, Linking, Alert, BackHandler, Platform } from 'react-native';
  import { SafeAreaProvider } from 'react-native-safe-area-context';
  // ... screen imports lines 4-22 ...
  import { ThemeProvider, useTheme } from './src/theme/ThemeContext';
  import { AuthProvider, useAuth } from './src/context/AuthContext';
  import { LanguageProvider, useLanguage } from './src/context/LanguageContext';

VERBATIM TARGET App() function (PATTERNS Pattern 3, lines 204-219 — copy verbatim):

  function App() {
    return (
      <SafeAreaProvider>
        <KeyboardProvider>
          <ThemeProvider>
            <LanguageProvider>
              <AuthProvider>
                <AppContent />
              </AuthProvider>
            </LanguageProvider>
          </ThemeProvider>
        </KeyboardProvider>
      </SafeAreaProvider>
    );
  }

VERBATIM TARGET import addition (PATTERNS Pattern 3, lines 224-227 — insert immediately
after the existing react-native-safe-area-context import line):

  import { SafeAreaProvider } from 'react-native-safe-area-context';
  import { KeyboardProvider } from 'react-native-keyboard-controller';

CONVENTIONS.md import-organization rule (group 2 — third-party RN modules): the new
KeyboardProvider import goes alongside SafeAreaProvider, NOT in any internal-imports group.

§4 Wave 1 entry gate (must hold before Task 2 may run — Task 1 verifies):
  - grep -c "react-native-reanimated" package.json returns 1
  - grep -c "react-native-worklets" package.json returns 1
  - grep -c "react-native-keyboard-controller" package.json returns 1
  - grep -c "react-native-worklets/plugin" babel.config.js returns 1
  - 02-01-SUMMARY.md exists and records both physical-device builds passing in Wave 0 Task 4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING gate] Verify Wave 0 install precondition before any App.tsx edit</name>
  <read_first>
    - .planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md (must exist; must record both physical-device builds passing per Plan 01 Task 4 acceptance criteria)
    - package.json (verify three dependencies are present)
    - babel.config.js (verify worklets/plugin is the last entry)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §2.5 (the 8-item Wave 0 entry-gate checklist)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §4 Wave 1 gating language
  </read_first>
  <files></files>
  <action>
    No file edits in this task. This task ONLY verifies that Plan 01 (Wave 0) actually passed
    its [BLOCKING] gate before this plan touches App.tsx. If any check below fails, ABORT this
    plan and surface to the user — Wave 1 cannot proceed without a fully-installed library tree.

    Run the following grep checks (each must hold):

      # 1. Three packages present in package.json
      [ $(grep -c "react-native-reanimated" package.json) -eq 1 ]
      [ $(grep -c "react-native-worklets" package.json) -eq 1 ]
      [ $(grep -c "react-native-keyboard-controller" package.json) -eq 1 ]

      # 2. Babel plugin is the v4 name (NOT v3) and is the LAST plugins-array entry
      [ $(grep -c "react-native-worklets/plugin" babel.config.js) -eq 1 ]
      [ $(grep -c "react-native-reanimated/plugin" babel.config.js) -eq 0 ]
      [ $(tail -n 5 babel.config.js | grep -c "react-native-worklets/plugin") -eq 1 ]

      # 3. iOS Podfile.lock has the new native pods registered
      [ $(grep -c "react-native-keyboard-controller\|RNKeyboardController" ios/Podfile.lock) -ge 1 ]
      [ $(grep -c "react-native-reanimated\|RNReanimated" ios/Podfile.lock) -ge 1 ]

      # 4. 02-01-SUMMARY.md exists with the Wave 0 sign-off
      [ -f .planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md ]
      grep -c "Wave 1 is unblocked" .planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md  # MUST be ≥ 1

    If any check fails, do NOT proceed to Task 2. Surface the failing check to the user with
    one of these recovery paths:

    - If grep checks 1-3 fail: Wave 0 commits did not actually land. Resume Plan 01 from the
      failing task (likely Task 1, 2, or 3).
    - If 02-01-SUMMARY.md is missing or doesn't say "Wave 1 is unblocked": Plan 01 Task 4
      (the human-verify checkpoint) was skipped or failed. Re-run Plan 01 Task 4 — both
      physical-device builds MUST be confirmed passing before this plan continues.

    Do NOT commit anything in this task. This is a pure verification gate.
  </action>
  <verify>
    <automated>grep -c "react-native-reanimated" package.json && grep -c "react-native-worklets" package.json && grep -c "react-native-keyboard-controller" package.json && grep -c "react-native-worklets/plugin" babel.config.js && test -f .planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md && grep -c "Wave 1 is unblocked" .planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "react-native-reanimated" package.json` returns 1
    - `grep -c "react-native-worklets" package.json` returns 1
    - `grep -c "react-native-keyboard-controller" package.json` returns 1
    - `tail -n 5 babel.config.js | grep -c "react-native-worklets/plugin"` returns 1
    - `grep -c "react-native-reanimated/plugin" babel.config.js` returns 0 (legacy v3 name absent)
    - `grep -c "react-native-keyboard-controller\|RNKeyboardController" ios/Podfile.lock` returns ≥ 1
    - `grep -c "react-native-reanimated\|RNReanimated" ios/Podfile.lock` returns ≥ 1
    - `test -f .planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md` exits 0
    - `grep -c "Wave 1 is unblocked" .planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md` returns ≥ 1
    - No git commit in this task (verification only)
    - If ANY check fails: this plan is ABORTED and a remediation note is written for the user
  </acceptance_criteria>
  <done>All 9 Wave 0 gate checks pass. Plan is cleared to proceed to Task 2 (App.tsx edit).</done>
</task>

<task type="auto">
  <name>Task 2: Insert <KeyboardProvider> between SafeAreaProvider and ThemeProvider in App.tsx</name>
  <read_first>
    - App.tsx (verify lines 1-3, 23-25 import block matches the verbatim BEFORE state in <interfaces>; verify lines 948-960 App() function matches the verbatim BEFORE state)
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md §App.tsx (the load-bearing comment about silent no-op if misplaced)
    - .planning/phases/02-universal-keyboard-handling/02-CONTEXT.md D-03 (placement fixed)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §4 Wave 1 (~3-5 line diff)
    - CLAUDE.md "Key Conventions" (provider order ThemeProvider → LanguageProvider → AuthProvider — preserved verbatim)
    - .planning/codebase/CONVENTIONS.md "Import Organization" (group 2 — third-party RN modules — for the new import line)
  </read_first>
  <files>App.tsx</files>
  <action>
    Pre-check: re-grep App.tsx to confirm the BEFORE state has not drifted since 2026-04-22:

      grep -n "<SafeAreaProvider>" App.tsx                        # expect line ~950
      grep -n "<ThemeProvider>" App.tsx                            # expect line ~951 (one line below SafeAreaProvider)
      grep -n "from 'react-native-safe-area-context'" App.tsx     # expect line ~3
      grep -c "<KeyboardProvider>" App.tsx                         # MUST return 0 (precondition: not yet wired)

    If `<KeyboardProvider>` is already present (returns ≥ 1), STOP — this plan has already
    been applied; do not double-wrap. If `<SafeAreaProvider>` and `<ThemeProvider>` lines
    are no longer adjacent, surface drift to the user before proceeding.

    Edit A — Add import line. Insert this exact line IMMEDIATELY AFTER the existing
    `import { SafeAreaProvider } from 'react-native-safe-area-context';` line (currently
    around App.tsx:3, in the third-party RN modules group per CONVENTIONS.md import order):

      import { KeyboardProvider } from 'react-native-keyboard-controller';

    The two lines after the edit MUST read:

      import { SafeAreaProvider } from 'react-native-safe-area-context';
      import { KeyboardProvider } from 'react-native-keyboard-controller';

    Edit B — Insert <KeyboardProvider> wrapper in the App() function (currently at App.tsx:948-960).

    Before (verbatim — must match exactly):

      function App() {
        return (
          <SafeAreaProvider>
            <ThemeProvider>
              <LanguageProvider>
                <AuthProvider>
                  <AppContent />
                </AuthProvider>
              </LanguageProvider>
            </ThemeProvider>
          </SafeAreaProvider>
        );
      }

    After (verbatim — KeyboardProvider slotted EXACTLY between SafeAreaProvider and ThemeProvider,
    indentation matches existing 2-space convention):

      function App() {
        return (
          <SafeAreaProvider>
            <KeyboardProvider>
              <ThemeProvider>
                <LanguageProvider>
                  <AuthProvider>
                    <AppContent />
                  </AuthProvider>
                </LanguageProvider>
              </ThemeProvider>
            </KeyboardProvider>
          </SafeAreaProvider>
        );
      }

    LOAD-BEARING SPECIFICS (PATTERNS Pattern 3 + PITFALLS §Pitfall 2):

    1. KeyboardProvider goes BETWEEN SafeAreaProvider and ThemeProvider — NOT inside
       ThemeProvider. Misplacement = silent no-op of every downstream library component
       (KASV in Plans 03/04, KAV in Plan 05). No compile error, no warning, just zero
       keyboard avoidance.
    2. The four existing providers (ThemeProvider → LanguageProvider → AuthProvider →
       AppContent) MUST stay in the exact order shown above — locked by CLAUDE.md
       "Key Conventions" + CONTEXT D-03. Do not reorder them under any circumstance.
    3. KeyboardProvider takes NO props in M1. Default behavior is correct — `enabled`,
       `statusBarTranslucent`, etc. are intentionally omitted (CONTEXT D-03 + RESEARCH §3).

    Do NOT touch:
    - Any state declarations (App.tsx:32-76)
    - The OVERLAY_FLAGS array (Phase 1's deliverable; sits in AppContent body, not App())
    - Any other render-tree code
    - Phase 1's BackHandler effect at App.tsx:184-297 (D-Discretion: confirm no conflict
      during Plan 06 chat verification — not this plan)

    After both edits: run TypeScript check to confirm no type errors:

      npx tsc --noEmit

    Commit atomically with message:

      git add App.tsx
      git commit -m "feat(02): wire KeyboardProvider between SafeAreaProvider and ThemeProvider (KBD-02, D-03)"

    Commit body: notes the exact slot per D-03 (between SafeAreaProvider and ThemeProvider);
    notes the existing provider order is preserved verbatim; notes that misplacement would
    cause silent no-op of downstream library components per PITFALLS §Pitfall 2; references
    PATTERNS Pattern 3 as the verbatim target shape.
  </action>
  <verify>
    <automated>grep -c "from 'react-native-keyboard-controller'" App.tsx && grep -c "<KeyboardProvider>" App.tsx && grep -c "</KeyboardProvider>" App.tsx && grep -B1 "<KeyboardProvider>" App.tsx | grep -c "<SafeAreaProvider>" && grep -A1 "<KeyboardProvider>" App.tsx | grep -c "<ThemeProvider>"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from 'react-native-keyboard-controller'" App.tsx` returns exactly 1 (the new import line)
    - `grep -c "import { KeyboardProvider }" App.tsx` returns exactly 1
    - `grep -c "<KeyboardProvider>" App.tsx` returns exactly 1 (opening tag, JSX usage)
    - `grep -c "</KeyboardProvider>" App.tsx` returns exactly 1 (closing tag)
    - `grep -nE "KeyboardProvider" App.tsx` returns exactly 3 lines total (import + opening tag + closing tag)
    - `grep -B1 "<KeyboardProvider>" App.tsx | grep -c "<SafeAreaProvider>"` returns 1 (KeyboardProvider sits IMMEDIATELY below SafeAreaProvider)
    - `grep -A1 "<KeyboardProvider>" App.tsx | grep -c "<ThemeProvider>"` returns 1 (KeyboardProvider opens IMMEDIATELY above ThemeProvider)
    - `grep -B1 "</KeyboardProvider>" App.tsx | grep -c "</ThemeProvider>"` returns 1 (KeyboardProvider closes immediately after ThemeProvider closes)
    - The existing provider order is intact: `grep -nE "<(Theme|Language|Auth)Provider>" App.tsx` shows ThemeProvider line < LanguageProvider line < AuthProvider line
    - Zero new red-text TypeScript errors: `npx tsc --noEmit` exits 0 (or returns the same number of pre-existing errors as before this task — tracked in 02-01-SUMMARY.md if any)
    - `git diff HEAD~1 --name-only` shows ONLY `App.tsx` (single-file atomic commit)
    - `git log --oneline -1` shows a commit message containing "KeyboardProvider", "KBD-02", and "D-03"
    - Satisfies §6 SC2.b: `grep -n "KeyboardProvider" App.tsx` returns ≥ 2 matches (target was met: import + JSX = 3 lines)
  </acceptance_criteria>
  <done>App.tsx imports KeyboardProvider and slots it between SafeAreaProvider and ThemeProvider per D-03 / PATTERNS Pattern 3. TypeScript clean. Atomic commit landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Boot smoke check on both physical devices — confirm no regression before Wave 2</name>
  <what-built>
    - App.tsx imports KeyboardProvider and wraps the entire sub-tree below SafeAreaProvider
    - Provider tree shape: SafeAreaProvider → KeyboardProvider → ThemeProvider → LanguageProvider → AuthProvider → AppContent
    - Library is now LIVE — every TextInput in the app is now mounted under a KeyboardProvider context
    - No per-screen wrappers added yet (Wave 2 plans do that)

    Per RESEARCH §4 Wave 1 go/no-go gate, the success criterion for this wave is "no visible
    change" — KeyboardProvider is a context-only wrapper with no render output of its own.
    Wave 2 (per-screen KASV/KAV adoption in Plans 03/04/05) MUST NOT begin until both physical
    devices boot the app cleanly with the new provider in place.
  </what-built>
  <how-to-verify>
    Human, please run BOTH physical-device builds and report results before approving Wave 2 entry.

    1. **iOS build on iPhone 15 Pro Max / iOS 26.4:**

       ```
       npx react-native run-ios --device "iPhone 15 Pro Max"
       ```

       Expected:
       - Build completes (xcodebuild exits 0; this is a JS-only change since native modules
         were already linked in Wave 0, so iOS rebuild should be fast)
       - App launches on the device
       - Home screen renders identically to pre-Wave-1 state
       - Bottom navigation taps still work (Phase 1 fix preserved — quick sanity)
       - NO red box at startup
       - Metro logs show ZERO occurrences of:
         * "Reanimated is not configured properly"
         * "KeyboardProvider" warning of any kind
         * Any new React/RN warning that wasn't present pre-Wave-1

       Smoke test: open the Login modal (Profile → sign in if not authed). Tap the email field.
       Keyboard should pop. The field SHOULD STILL BE COVERED right now — that's expected
       because no KASV wrapper is on LoginScreen yet (Plan 03 adds that). The point of this
       smoke test is just "does the app boot with KeyboardProvider in the tree without
       crashing or warning?"

    2. **Android build on Moto G XT2513V / Android 16:**

       ```
       npx react-native run-android
       ```

       Expected: same as iOS — clean boot, no regression, no Reanimated runtime errors.

    3. **Visual regression check (BOTH platforms):**

       Per RESEARCH §4 Wave 1 success criterion: app should render IDENTICALLY to pre-Wave-1
       state. KeyboardProvider is a context-only wrapper. If you see any visible difference
       (status bar color shift, safe-area-inset change, layout shift), do NOT approve —
       surface what changed and we'll diagnose. Most likely cause of visible change would be
       KeyboardProvider being in the wrong slot (e.g., outside SafeAreaProvider).

    4. **Capture artifact metadata** in your approval message:

       - iOS device confirmed booted: iPhone 15 Pro Max / iOS 26.4 ✓ / ✗
       - Android device confirmed booted: Moto G XT2513V / Android 16 ✓ / ✗
       - Metro logs clean of Reanimated/KeyboardProvider errors: ✓ / ✗
       - Visual regression check passed: ✓ / ✗
       - App.tsx commit SHA from Task 2 (will be used as Wave 1 sign-off in 02-02-SUMMARY.md)

    If any check fails, the most likely failures + recoveries:

    - Red box "KeyboardProvider is not a function" → import is wrong; verify Task 2 added
      `from 'react-native-keyboard-controller'` (NOT some other package)
    - Red box "Cannot find module 'react-native-keyboard-controller'" → Wave 0 install didn't
      complete on this device's build (cache mismatch). Run `npx react-native start --reset-cache`
      and rebuild.
    - Layout regression (status bar / safe area) → KeyboardProvider may have been placed in
      wrong slot. Re-verify Task 2 acceptance criteria grep checks.
    - "Reanimated is not configured properly" runtime error → Wave 0's babel.config.js + Metro
      cache reset didn't take effect. Run `rm -rf $TMPDIR/metro-* $TMPDIR/haste-map-*` and
      rebuild.
  </how-to-verify>
  <acceptance_criteria>
    - Human reports: iOS boot SUCCEEDED on iPhone 15 Pro Max / iOS 26.4 — no red box, Metro clean
    - Human reports: Android boot SUCCEEDED on Moto G XT2513V / Android 16 — no red box, logcat clean
    - Human reports: visual regression check PASSED (no visible change vs pre-Wave-1 state)
    - Human reports: Login modal email field still gets covered by keyboard (expected — no KASV
      yet) but the keyboard pops without crashing the app
    - Resume signal received with explicit "Wave 1 verified — proceed to Wave 2" or itemized
      failure
    - Wave 1 go/no-go gate (RESEARCH §4) all items PASS:
      * App launches on both devices ✓
      * No new red-box, no Metro warnings about KeyboardProvider ✓
      * Existing screens render unchanged ✓
    - Plans 03 / 04 / 05 (Wave 2 — parallelizable per-screen adoption) are unblocked
  </acceptance_criteria>
  <resume-signal>
    Type one of:
    - "Wave 1 verified — proceed to Wave 2" + iOS/Android boot confirmation + Task 2 commit SHA
    - "Wave 1 FAILED on {iOS|Android}" + paste the failing build output / red-box / Metro log
    - "Wave 1 visual regression detected" + describe what changed
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. KeyboardProvider is a context-only React component
imported from react-native-keyboard-controller (already a trusted package after Wave 0's
supply-chain disposition in Plan 01). This plan adds a single wrapper and a single import
to App.tsx — no new auth surface, no new HTTP, no new storage, no PII handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | T (Tampering) | KeyboardProvider misplacement (silent no-op of all downstream library components) | mitigate | Task 2 acceptance criteria include 4 explicit grep checks confirming KeyboardProvider sits IMMEDIATELY between SafeAreaProvider and ThemeProvider — not inside any other provider. Misplacement risk is the highest-priority encoding error per PITFALLS §Pitfall 2 / PATTERNS Pattern 3, and it is detected by the grep gate (not just by visual inspection). |
| T-02-02-02 | I (Information Disclosure) | KeyboardProvider context (keyboard frame events) | accept | KeyboardProvider broadcasts keyboard frame events (height, animation duration) to descendant components via React context. No PII / token / user content is in the event payload. Library-internal use only. |

No S/R/D/E applicable — this plan adds no auth surface, no repudiation-relevant state, no DoS
surface, no elevation path. Block on `high` severity per project default; zero high-severity
threats raised. Phase 2 is presentational wiring — `<security_threat_model>` from the
downstream consumer brief noted this plan is "no new threat surface" with the misplacement
risk being the only meaningful concern (and it is mitigated mechanically by Task 2's grep gate).
</threat_model>

<verification>
- `grep -c "import { KeyboardProvider }" App.tsx` returns 1 (KBD-02 — root-level wiring)
- `grep -c "<KeyboardProvider>" App.tsx` returns 1
- `grep -c "</KeyboardProvider>" App.tsx` returns 1
- `grep -B1 "<KeyboardProvider>" App.tsx | grep "<SafeAreaProvider>"` returns 1 (D-03 placement)
- `grep -A1 "<KeyboardProvider>" App.tsx | grep "<ThemeProvider>"` returns 1 (D-03 placement)
- `npx tsc --noEmit` exits 0 (or no NEW errors vs pre-task baseline)
- Human-verified: iOS boot on iPhone 15 Pro Max / iOS 26.4 succeeds with no regression
- Human-verified: Android boot on Moto G XT2513V / Android 16 succeeds with no regression
- Human-verified: no visible change vs pre-Wave-1 state (Wave 1 success criterion per RESEARCH §4)
- One atomic commit in `git log --oneline App.tsx | head -1` referencing KBD-02 / D-03
</verification>

<success_criteria>
- KBD-02 ✓ — single root-level wiring established (KeyboardProvider in App.tsx, not per-screen)
- KBD-04 ✓ — boot smoke confirms Fabric build runs cleanly on both physical devices with the
  new context-only wrapper installed
- §6 SC2.b ✓ — `grep -n "KeyboardProvider" App.tsx` returns ≥ 2 lines (import + JSX usage)
- D-03 placement honored — grep proves SafeAreaProvider → KeyboardProvider → ThemeProvider
- 02-VALIDATION.md row "Wave 1 provider" gate PASSES
- Plans 03 / 04 / 05 (Wave 2 — parallelizable screen conversions) are unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md` documenting:
- App.tsx commit SHA from Task 2
- Verbatim before/after diff of the App() function (5 lines added: 1 import + opening + closing + 2 indent shifts on existing providers)
- Boot smoke results: iOS device + version, Android device + version, both PASSED
- Confirmation that no visual regression detected
- Confirmation that no Metro/logcat warnings introduced
- Explicit statement: "Wave 2 is unblocked — Plans 03 / 04 / 05 (parallelizable per-screen KASV/KAV adoption) may proceed."
</output>
</content>
</invoke>