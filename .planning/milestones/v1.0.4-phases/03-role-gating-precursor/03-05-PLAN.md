---
phase: 03-role-gating-precursor
plan: 05
plan_id: 03-05
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/screens/CreateListingScreen.tsx
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
requirements_addressed:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
tags:
  - call-site-migration
  - ui-gating
user_setup: []

must_haves:
  truths:
    - "No `userType === 'admin'` comparison remains in CreateListingScreen.tsx (D-14 invariant #1 locally)"
    - "No direct `user?.backendProfile?.userType` read remains in CreateListingScreen.tsx (D-14 invariant #2 locally)"
    - "Matterport tours section (lines ~781-830) is hidden from non-admin users"
    - "Panoramic photo URL TextInput (lines ~843-852) is hidden from non-admins but surrounding Links section (videoUrl, instagramUrl) stays visible"
    - "Verification-switches section (line ~933) is hidden from non-admins"
    - "Existing edit-mode `tours` and `panoramicPhotosUrl` values flow through the submit payload unchanged for non-admins (D-09 preserve-on-save invariant)"
    - "Payload branch at line ~396 governs `platformVerifications` only and is gated by `can('editVerifications')` (not `can('editMatterportUrl')` — per Q1 + RESEARCH §2.3)"
  artifacts:
    - path: "src/screens/CreateListingScreen.tsx"
      provides: "useRole consumer with 4 isAdmin sites migrated + 3 Gated wraps added"
      contains: "import { useRole } from '../hooks/useRole';"
  key_links:
    - from: "src/screens/CreateListingScreen.tsx"
      to: "src/hooks/useRole.ts"
      via: "useRole() destructure at top of component"
      pattern: "const \\{ can.*\\} = useRole\\(\\);"
    - from: "src/screens/CreateListingScreen.tsx"
      to: "src/components/Gated.tsx"
      via: "import Gated + 3 JSX wrappers"
      pattern: "<Gated action="
---

<objective>
Migrate `CreateListingScreen.tsx` to the Phase 3 gating surface. This plan handles the biggest of the three screen migrations: 4 `isAdmin` sites -> `can()` / `<Gated>` plus 2 NEW `<Gated>` wraps around Matterport tours and panoramic URL input.

Six specific changes per D-11 / RESEARCH §3.7:

1. Line ~110 `isAdmin` declaration -> `const { can } = useRole();` (drop `isAdmin` — no residual non-gating uses remain).
2. Line ~319 `!isAdmin` in fetchExistingVerifications -> `!can('editVerifications')`.
3. Line ~396 payload branch `isAdmin ? {...} : {}` -> `can('editVerifications') ? {...} : {}` (Q1 resolution: action is `editVerifications`, NOT `editMatterportUrl`).
4. Line ~933 `{isAdmin && (...)}` verification switches -> `<Gated action="editVerifications">...</Gated>`.
5. Lines ~781-830 Matterport tours section -> wrap outer `<View style={styles.section}>` in `<Gated action="editMatterportUrl">`.
6. Lines ~843-852 panoramic `TextInput` -> wrap ONLY the `<TextInput>` in `<Gated action="editPanoramicUrl">` so videoUrl/instagramUrl inputs stay visible to all users.

Purpose: CreateListingScreen is where the D-08 behavior change lands — non-admin listers lose Matterport + panoramic URL inputs. This is intentional ("curated 3D tours are the platform differentiator"). The D-09 preserve-on-save invariant is structurally already met by existing lines 185/202/390/394 — this plan must NOT disturb those lines.

Output: One modified file. All previous Phase 3 tests remain GREEN. `grep -c "isAdmin\|userType === 'admin'"` in this file returns 0 after all tasks complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-role-gating-precursor/03-CONTEXT.md
@.planning/phases/03-role-gating-precursor/03-RESEARCH.md
@.planning/phases/03-role-gating-precursor/03-PATTERNS.md
@.planning/phases/03-role-gating-precursor/03-02-SUMMARY.md
@src/screens/CreateListingScreen.tsx
@src/hooks/useRole.ts
@src/components/Gated.tsx

<interfaces>
Current CreateListingScreen line 110 (isAdmin declaration):
```
  const isAdmin = user?.backendProfile?.userType === 'admin';
```

Current line 319 (fetchExistingVerifications guard):
```
      if (!propertyToEdit?.id || !isAdmin) return;
```

Current lines 394-405 (submit payload with isAdmin branch — governs `platformVerifications` only):
```
        tours: tours.length > 0 ? tours : undefined, // Include Matterport tours
        existingImages: existingImageUrls, // Send existing image URLs for merging
        ...(isAdmin
          ? {
              platformVerifications: {
                ownershipDocuments: verifyOwnership,
                ownerIdentityVerified: verifyOwnerId,
                stateIssuedDocumentsVerified: verifyStateDocs,
              },
            }
          : {}),
      };
```

Current lines 781-830 (Matterport tours section — wrap the outer View):
```
        {/* Matterport 3D Tours */}
        <View style={styles.section}>
          <Text ...>{t('createListing.tours3d')}</Text>
          <Text ...>{t('createListing.matterportHint')}</Text>
          <TextInput ... placeholder={t('createListing.tourTitle')} ... />
          <TextInput ... placeholder={t('createListing.matterportUrlExample')} ... />
          <TouchableOpacity ... onPress={addTour}> ... </TouchableOpacity>
          {tours.length > 0 && (<View style={styles.toursList}>{tours.map(...)}</View>)}
        </View>
```

Current lines 832-860 (Links section — wrap ONLY the panoramic TextInput, NOT this View):
```
        {/* Links */}
        <View style={styles.section}>
          <Text ...>{t('createListing.links')}</Text>
          <TextInput ... placeholder={t('createListing.videoUrl')} ... />
          <TextInput
            style={[styles.input, ...]}
            placeholder={t('createListing.panoramicUrl')}
            placeholderTextColor={colors.textSecondary}
            value={panoramicPhotosUrl}
            onChangeText={setPanoramicPhotosUrl}
            keyboardType="url"
          />
          <TextInput ... placeholder={t('createListing.instagramUrl')} ... />
          <Text ...>{t('createListing.instagramHint')}</Text>
        </View>
```

Current line 933 (isAdmin-gated verification switches section):
```
        {isAdmin && (
          <View style={styles.section}>
            <Text ...>{t('verification.adminSectionTitle')}</Text>
            <Text ...>{t('verification.adminSectionHint')}</Text>
            <View ...>
              {verificationSwitchRow(t('verification.ownershipDocuments'), verifyOwnership, setVerifyOwnership)}
              {verificationSwitchRow(t('verification.ownerIdentity'), verifyOwnerId, setVerifyOwnerId)}
              {verificationSwitchRow(t('verification.stateIssued'), verifyStateDocs, setVerifyStateDocs)}
            </View>
          </View>
        )}
```

Preserve-on-save invariant (D-09) currently holds via these existing lines that MUST NOT be moved/wrapped/nested:

- Line 185: `setPanoramicPhotosUrl((propertyToEdit as any).panoramicPhotosUrl || '');` — hydrates state from edit-mode listing regardless of admin.
- Lines 202-210: `setTours(propertyToEdit.tours.map(...))` — same pattern for tours.
- Line 390: `panoramicPhotosUrl: panoramicPhotosUrl.trim(),` — payload spreads state unconditionally.
- Line 394: `tours: tours.length > 0 ? tours : undefined,` — same for tours.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add imports + replace isAdmin declaration with useRole destructure + migrate line 319 guard + migrate line 396 payload branch</name>
  <files>src/screens/CreateListingScreen.tsx</files>
  <read_first>
    - src/screens/CreateListingScreen.tsx lines 1-30 (existing import block — identify where to add new imports matching CONVENTIONS.md Import Organization)
    - src/screens/CreateListingScreen.tsx lines 100-115 (isAdmin declaration context)
    - src/screens/CreateListingScreen.tsx lines 315-325 (line 319 guard context)
    - src/screens/CreateListingScreen.tsx lines 388-410 (line 396 payload branch context)
    - src/screens/CreateListingScreen.tsx lines 180-215 (D-09 preserve-on-save anchors at lines 185, 202-210 — confirm they are untouched)
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §3.7 rows 1, 2, 3 + §2.3 (Q1 resolution: action is `editVerifications`, NOT `editMatterportUrl`)
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md D-10, D-09, D-11, D-14
    - src/hooks/useRole.ts (confirm `useRole` named export)
    - src/components/Gated.tsx (confirm `Gated` named export)
  </read_first>
  <behavior>
    - File imports `useRole` and `Gated` (two new import lines).
    - Line ~110 `isAdmin` declaration REPLACED with `const { can } = useRole();`.
    - Line ~319 `!isAdmin` REPLACED with `!can('editVerifications')`.
    - Line ~396 `...(isAdmin` REPLACED with `...(can('editVerifications')` (Q1: action is `editVerifications`, not `editMatterportUrl`).
    - Lines 390, 394 UNCHANGED (D-09 preserve-on-save).
    - After this task: `grep -c "isAdmin" src/screens/CreateListingScreen.tsx` returns exactly `1` (only line 933 remains — Task 2 handles it).
    - After this task: `grep -c "userType === 'admin'" src/screens/CreateListingScreen.tsx` returns `0`.
  </behavior>
  <action>
PRE-CHECK (Q6 machine-verifiable hardening). Run this grep BEFORE making any edits:

```
grep -n "isAdmin" src/screens/CreateListingScreen.tsx
```

Expected output: EXACTLY 4 matches at lines 110, 319, 396, 933. If any OTHER match exists (5+ matches, or matches at different line numbers), STOP and surface to the human operator — a new `isAdmin` use has been introduced since research was captured and the §10 Q6 resolution may no longer apply. Do NOT proceed with the migration until the discrepancy is triaged.

Acceptance: `[ "$(grep -c 'isAdmin' src/screens/CreateListingScreen.tsx)" = "4" ]` exits 0 before Task 1 EDIT 1 runs.

Four sequential edits in `src/screens/CreateListingScreen.tsx`.

EDIT 1 — Add two imports. Find the existing line `import { useAuth } from '../context/AuthContext';`. Immediately AFTER that line, insert two new lines:

```
import { useRole } from '../hooks/useRole';
import { Gated } from '../components/Gated';
```

The three lines together are:

```
import { useAuth } from '../context/AuthContext';
import { useRole } from '../hooks/useRole';
import { Gated } from '../components/Gated';
```

EDIT 2 — Replace line ~110. Current:

```
  const isAdmin = user?.backendProfile?.userType === 'admin';
```

Replace with:

```
  const { can } = useRole();
```

Do NOT keep `isAdmin` in the destructure — after Tasks 1 and 2 complete, no non-gating use remains.

EDIT 3 — Replace line ~319. Current:

```
      if (!propertyToEdit?.id || !isAdmin) return;
```

Replace with:

```
      if (!propertyToEdit?.id || !can('editVerifications')) return;
```

EDIT 4 — Replace line ~396 (first line of the spread-branch only). Current:

```
        ...(isAdmin
          ? {
              platformVerifications: {
```

Change `...(isAdmin` to `...(can('editVerifications')`. After the change:

```
        ...(can('editVerifications')
          ? {
              platformVerifications: {
```

The inner block `platformVerifications: {...}` and the closing `: {})` stay identical. Do NOT touch lines 390 (`panoramicPhotosUrl: ...`) or 394 (`tours: ...`) — they remain unconditional (D-09 preserve-on-save).

Q1 RESOLUTION — critical: The action at line 396 is `editVerifications`, NOT `editMatterportUrl`. This branch governs the `platformVerifications` subtree only. Do NOT move `tours`/`panoramicPhotosUrl` payload fields into this branch — D-09 requires them to flow unconditionally.

Grep validation after all four edits: `grep -c "isAdmin" src/screens/CreateListingScreen.tsx` MUST return exactly `1` (only line 933). If it returns anything else, STOP — an unknown 5th reference means the research grep was stale.
  </action>
  <verify>
    <automated>grep -Fq "import { useRole } from '../hooks/useRole';" src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq "import { Gated } from '../components/Gated';" src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq "const { can } = useRole();" src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq "!can('editVerifications')) return;" src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq "...(can('editVerifications')" src/screens/CreateListingScreen.tsx &amp;&amp; [ "$(grep -c isAdmin src/screens/CreateListingScreen.tsx)" = "1" ] &amp;&amp; [ "$(grep -c 'userType === .admin.' src/screens/CreateListingScreen.tsx)" = "0" ] &amp;&amp; grep -Fq "panoramicPhotosUrl: panoramicPhotosUrl.trim()," src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq "tours: tours.length > 0 ? tours : undefined," src/screens/CreateListingScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Pre-check (asserted before EDIT 1 begins): `[ "$(grep -c 'isAdmin' src/screens/CreateListingScreen.tsx)" = "4" ]` exits 0 — exactly 4 `isAdmin` matches at lines 110, 319, 396, 933. If pre-check fails, Task 1 is aborted.
    - `grep -Fc "import { useRole } from '../hooks/useRole';" src/screens/CreateListingScreen.tsx` returns `1`.
    - `grep -Fc "import { Gated } from '../components/Gated';" src/screens/CreateListingScreen.tsx` returns `1`.
    - `grep -Fc "const { can } = useRole();" src/screens/CreateListingScreen.tsx` returns `1`.
    - `grep -c "const isAdmin = " src/screens/CreateListingScreen.tsx` returns `0`.
    - `grep -c "userType === 'admin'" src/screens/CreateListingScreen.tsx` returns `0` (local D-14 invariant #1).
    - `grep -c "backendProfile.userType" src/screens/CreateListingScreen.tsx` returns `0` (local D-14 invariant #2).
    - `grep -Fc "!can('editVerifications')) return;" src/screens/CreateListingScreen.tsx` returns `1` (line 319 migrated).
    - `grep -Fc "...(can('editVerifications')" src/screens/CreateListingScreen.tsx` returns `1` (line 396 migrated — Q1 resolved).
    - `grep -Fc "...(can('editMatterportUrl')" src/screens/CreateListingScreen.tsx` returns `0` (Q1 anti-check — do NOT use the wrong action here).
    - `grep -c "isAdmin" src/screens/CreateListingScreen.tsx` returns exactly `1` (only line 933 remains; Task 2 of this plan finishes the job).
    - D-09 preserve-on-save anchors intact: `grep -Fc "panoramicPhotosUrl: panoramicPhotosUrl.trim()," src/screens/CreateListingScreen.tsx` returns `1`; `grep -Fc "tours: tours.length > 0 ? tours : undefined," src/screens/CreateListingScreen.tsx` returns `1`; `grep -Fc "setPanoramicPhotosUrl((propertyToEdit as any).panoramicPhotosUrl || '');" src/screens/CreateListingScreen.tsx` returns `1`.
  </acceptance_criteria>
  <done>
    Two imports added. Line 110 isAdmin declaration replaced. Lines 319 and 396 migrated to `can('editVerifications')`. D-09 preserve-on-save anchors untouched. One residual `isAdmin` match remains at line 933 (Task 2 handles it). Local invariants D-14 #1, #2 hold.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wrap verification switches section (line 933) in Gated action="editVerifications"</name>
  <files>src/screens/CreateListingScreen.tsx</files>
  <read_first>
    - src/screens/CreateListingScreen.tsx lines 925-945 (verification switches section context — post-Task-1 state should still have the `{isAdmin && (...)` wrapper at line 933)
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §3.7 row 4 + §3.6 "Verification switches section" block
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md D-11 site 4 (`{isAdmin && (...verification switches...)}` -> `<Gated action="editVerifications">...</Gated>`)
  </read_first>
  <behavior>
    - The `{isAdmin && (...)}` pattern at line 933 is REPLACED with `<Gated action="editVerifications">` wrapping the same `<View style={styles.section}>...</View>` children.
    - After this task: `grep -c "isAdmin" src/screens/CreateListingScreen.tsx` returns `0`.
    - Non-admin user: the verification switches section does NOT render.
    - Admin user (via allowlist or userType): the verification switches section renders identically to before.
  </behavior>
  <action>
Find the exact block at line ~933 in `src/screens/CreateListingScreen.tsx`:

```
        {isAdmin && (
          <View style={styles.section}>
            <Text style={[styles.sectionTitle, { color: colors.text }]}>{t('verification.adminSectionTitle')}</Text>
            <Text style={[styles.hint, { color: colors.textSecondary, marginBottom: 12 }]}>{t('verification.adminSectionHint')}</Text>
            <View style={{ backgroundColor: colors.inputBackground, borderColor: colors.border, borderWidth: 1, borderRadius: 16, paddingHorizontal: 16, paddingVertical: 8 }}>
              {verificationSwitchRow(t('verification.ownershipDocuments'), verifyOwnership, setVerifyOwnership)}
              {verificationSwitchRow(t('verification.ownerIdentity'), verifyOwnerId, setVerifyOwnerId)}
              {verificationSwitchRow(t('verification.stateIssued'), verifyStateDocs, setVerifyStateDocs)}
            </View>
          </View>
        )}
```

Replace the outer `{isAdmin && (...)}` wrapper with a `<Gated action="editVerifications">...</Gated>` element. Preserve the inner `<View style={styles.section}>...</View>` and all children verbatim. After the change:

```
        <Gated action="editVerifications">
          <View style={styles.section}>
            <Text style={[styles.sectionTitle, { color: colors.text }]}>{t('verification.adminSectionTitle')}</Text>
            <Text style={[styles.hint, { color: colors.textSecondary, marginBottom: 12 }]}>{t('verification.adminSectionHint')}</Text>
            <View style={{ backgroundColor: colors.inputBackground, borderColor: colors.border, borderWidth: 1, borderRadius: 16, paddingHorizontal: 16, paddingVertical: 8 }}>
              {verificationSwitchRow(t('verification.ownershipDocuments'), verifyOwnership, setVerifyOwnership)}
              {verificationSwitchRow(t('verification.ownerIdentity'), verifyOwnerId, setVerifyOwnerId)}
              {verificationSwitchRow(t('verification.stateIssued'), verifyStateDocs, setVerifyStateDocs)}
            </View>
          </View>
        </Gated>
```

Key changes: `{isAdmin && (` -> `<Gated action="editVerifications">`; `)}` at the end -> `</Gated>`. The inner `<View style={styles.section}>...</View>` block is unchanged.
  </action>
  <verify>
    <automated>[ "$(grep -c isAdmin src/screens/CreateListingScreen.tsx)" = "0" ] &amp;&amp; grep -Fq '<Gated action="editVerifications">' src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq '</Gated>' src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq "verificationSwitchRow(t('verification.ownershipDocuments'), verifyOwnership, setVerifyOwnership)" src/screens/CreateListingScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isAdmin" src/screens/CreateListingScreen.tsx` returns `0` (D-14 invariant #1 locally complete for this file).
    - `grep -Fc '<Gated action="editVerifications">' src/screens/CreateListingScreen.tsx` returns exactly `1` after this task (Task 3 adds a second `<Gated>` but with different action labels).
    - `grep -Fc "verificationSwitchRow(t('verification.ownershipDocuments'), verifyOwnership, setVerifyOwnership)" src/screens/CreateListingScreen.tsx` returns `1` (verification switch children preserved verbatim).
    - `grep -Fc "{t('verification.adminSectionTitle')}" src/screens/CreateListingScreen.tsx` returns `1` (section title preserved).
    - `grep -Fc "{isAdmin && (" src/screens/CreateListingScreen.tsx` returns `0` (no residual conditional wrapper).
  </acceptance_criteria>
  <done>
    `{isAdmin && (...)}` wrapper at line ~933 replaced by `<Gated action="editVerifications">...</Gated>`. All verification switch children preserved. Zero `isAdmin` references remain in the file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wrap Matterport tours section (lines ~781-830) and panoramic TextInput only (lines ~843-852) in Gated</name>
  <files>src/screens/CreateListingScreen.tsx</files>
  <read_first>
    - src/screens/CreateListingScreen.tsx lines 775-830 (Matterport tours section context — wrap the outer View)
    - src/screens/CreateListingScreen.tsx lines 832-860 (Links section context — wrap ONLY the panoramic TextInput; videoUrl + instagramUrl inputs MUST remain visible to all users)
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §2.2 (load-bearing nuance: panoramic TextInput sits INSIDE a Links section with videoUrl + instagramUrl; only the panoramic input is gated, NOT the whole section)
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §3.6 (authoritative wrapper placement)
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md D-08 (hide entirely for non-admins), D-09 (preserve existing URLs verbatim on save)
  </read_first>
  <behavior>
    - Matterport tours section (the `<View style={styles.section}>` at line ~781) is now wrapped in `<Gated action="editMatterportUrl">`.
    - Panoramic URL `TextInput` at lines ~843-852 is wrapped in `<Gated action="editPanoramicUrl">` — ONLY the `<TextInput>` itself, NOT the surrounding `<View style={styles.section}>` (otherwise videoUrl + instagramUrl inputs would be hidden too — RESEARCH §2.2 explicit anti-pattern).
    - Non-admin users see: no Matterport tours section; no panoramic input; videoUrl + instagramUrl inputs still visible.
    - Admin users: all three inputs + Matterport section render identically to pre-Phase-3 behavior.
    - D-09 preserve-on-save still holds: existing `tours`/`panoramicPhotosUrl` hydration (lines 185, 202-210) and payload spread (lines 390, 394) are untouched.
  </behavior>
  <action>
Two targeted JSX wraps.

WRAP A — Matterport tours section. Find the section starting at line ~781 (after the preceding `</View>` that closes the block before it — look for the comment `{/* Matterport 3D Tours */}`). Current:

```
        {/* Matterport 3D Tours */}
        <View style={styles.section}>
          <Text style={[styles.sectionTitle, { color: colors.text }]}>{t('createListing.tours3d')}</Text>
          ... (rest of Matterport section — tourTitle TextInput, tourUrl TextInput, Add Tour TouchableOpacity, tours list)
        </View>
```

Wrap the whole `<View style={styles.section}>...</View>` in `<Gated action="editMatterportUrl">...</Gated>`. After the change:

```
        {/* Matterport 3D Tours — admin only (D-08) */}
        <Gated action="editMatterportUrl">
          <View style={styles.section}>
            <Text style={[styles.sectionTitle, { color: colors.text }]}>{t('createListing.tours3d')}</Text>
            ... (inner children unchanged)
          </View>
        </Gated>
```

The `{/* Matterport 3D Tours */}` comment is updated to `{/* Matterport 3D Tours — admin only (D-08) */}` so future maintainers see the intent. All inner JSX (title Text, hint Text, tourTitle TextInput, tourUrl TextInput, Add Tour TouchableOpacity, tours list `{tours.length > 0 && ...}`) remains verbatim.

WRAP B — Panoramic TextInput ONLY. Find the Links section at line ~832. The section has THREE `TextInput` children: videoUrl, panoramicPhotosUrl, instagramUrl (plus the hint `Text` at the end). Current:

```
        {/* Links */}
        <View style={styles.section}>
          <Text style={[styles.sectionTitle, { color: colors.text }]}>{t('createListing.links')}</Text>
          <TextInput
            style={[styles.input, { backgroundColor: colors.inputBackground, color: colors.text, borderColor: colors.border }]}
            placeholder={t('createListing.videoUrl')}
            placeholderTextColor={colors.textSecondary}
            value={videoUrl}
            onChangeText={setVideoUrl}
            keyboardType="url"
          />
          <TextInput
            style={[styles.input, { backgroundColor: colors.inputBackground, color: colors.text, borderColor: colors.border }]}
            placeholder={t('createListing.panoramicUrl')}
            placeholderTextColor={colors.textSecondary}
            value={panoramicPhotosUrl}
            onChangeText={setPanoramicPhotosUrl}
            keyboardType="url"
          />
          <TextInput
            style={[styles.input, { backgroundColor: colors.inputBackground, color: colors.text, borderColor: colors.border }]}
            placeholder={t('createListing.instagramUrl')}
            placeholderTextColor={colors.textSecondary}
            value={instagramUrl}
            onChangeText={setInstagramUrl}
            keyboardType="url"
          />
          <Text style={[styles.hint, { color: colors.textSecondary }]}>{t('createListing.instagramHint')}</Text>
        </View>
```

Wrap the SECOND `<TextInput>` (the one with `placeholder={t('createListing.panoramicUrl')}` and `value={panoramicPhotosUrl}`) in `<Gated action="editPanoramicUrl">...</Gated>`. Keep the videoUrl and instagramUrl TextInputs as SIBLINGS of the `<Gated>`, NOT children. After the change, the section body reads:

```
        {/* Links */}
        <View style={styles.section}>
          <Text style={[styles.sectionTitle, { color: colors.text }]}>{t('createListing.links')}</Text>
          <TextInput
            style={[styles.input, { backgroundColor: colors.inputBackground, color: colors.text, borderColor: colors.border }]}
            placeholder={t('createListing.videoUrl')}
            placeholderTextColor={colors.textSecondary}
            value={videoUrl}
            onChangeText={setVideoUrl}
            keyboardType="url"
          />
          {/* Panoramic URL — admin only (D-08) */}
          <Gated action="editPanoramicUrl">
            <TextInput
              style={[styles.input, { backgroundColor: colors.inputBackground, color: colors.text, borderColor: colors.border }]}
              placeholder={t('createListing.panoramicUrl')}
              placeholderTextColor={colors.textSecondary}
              value={panoramicPhotosUrl}
              onChangeText={setPanoramicPhotosUrl}
              keyboardType="url"
            />
          </Gated>
          <TextInput
            style={[styles.input, { backgroundColor: colors.inputBackground, color: colors.text, borderColor: colors.border }]}
            placeholder={t('createListing.instagramUrl')}
            placeholderTextColor={colors.textSecondary}
            value={instagramUrl}
            onChangeText={setInstagramUrl}
            keyboardType="url"
          />
          <Text style={[styles.hint, { color: colors.textSecondary }]}>{t('createListing.instagramHint')}</Text>
        </View>
```

Anti-pattern explicitly forbidden (RESEARCH §2.2): Do NOT wrap the outer `<View style={styles.section}>` at line 833 in `<Gated>` — that would hide the videoUrl and instagramUrl inputs too, which is wrong. Only the panoramic `<TextInput>` element gets wrapped.

Do NOT modify the hydration call at line 185 (`setPanoramicPhotosUrl(...)`) or the payload spread at line 390 (`panoramicPhotosUrl: panoramicPhotosUrl.trim(),`) — D-09 preserve-on-save requires both to remain unconditional so existing panoramic URLs on edited listings survive a save by a non-admin.
  </action>
  <verify>
    <automated>grep -Fq '<Gated action="editMatterportUrl">' src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq '<Gated action="editPanoramicUrl">' src/screens/CreateListingScreen.tsx &amp;&amp; [ "$(grep -Fc '<Gated action=' src/screens/CreateListingScreen.tsx)" = "3" ] &amp;&amp; [ "$(grep -Fc '</Gated>' src/screens/CreateListingScreen.tsx)" = "3" ] &amp;&amp; grep -Fq "placeholder={t('createListing.videoUrl')}" src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq "placeholder={t('createListing.instagramUrl')}" src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq "admin only (D-08)" src/screens/CreateListingScreen.tsx &amp;&amp; grep -Fq "setPanoramicPhotosUrl((propertyToEdit as any).panoramicPhotosUrl || '');" src/screens/CreateListingScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc '<Gated action="editMatterportUrl">' src/screens/CreateListingScreen.tsx` returns exactly `1`.
    - `grep -Fc '<Gated action="editPanoramicUrl">' src/screens/CreateListingScreen.tsx` returns exactly `1`.
    - `grep -Fc '<Gated action="editVerifications">' src/screens/CreateListingScreen.tsx` returns exactly `1` (from Task 2 — unchanged).
    - `grep -Fc '<Gated action=' src/screens/CreateListingScreen.tsx` returns exactly `3` (three total `<Gated>` wrappers across the file — D-11 sites 4, 5, 6).
    - `grep -Fc '</Gated>' src/screens/CreateListingScreen.tsx` returns exactly `3` (matched close tags).
    - Anti-pattern check: `grep -Fq "placeholder={t('createListing.videoUrl')}" src/screens/CreateListingScreen.tsx` succeeds — videoUrl input still present (not hidden by an over-wide Gated wrap).
    - Anti-pattern check: `grep -Fq "placeholder={t('createListing.instagramUrl')}" src/screens/CreateListingScreen.tsx` succeeds — instagramUrl input still present.
    - Anti-pattern check: No `<Gated action="editPanoramicUrl">` nests the entire Links section. Verify: `awk '/<Gated action=\"editPanoramicUrl\">/,/<\/Gated>/' src/screens/CreateListingScreen.tsx | grep -Fc "placeholder={t('createListing.videoUrl')}"` returns `0` (videoUrl NOT inside the panoramic Gated wrap). Same for instagramUrl: `awk '/<Gated action=\"editPanoramicUrl\">/,/<\/Gated>/' src/screens/CreateListingScreen.tsx | grep -Fc "placeholder={t('createListing.instagramUrl')}"` returns `0`.
    - D-09 preserve-on-save anchors untouched:
      * `grep -Fc "setPanoramicPhotosUrl((propertyToEdit as any).panoramicPhotosUrl || '');" src/screens/CreateListingScreen.tsx` returns `1`.
      * `grep -Fc "panoramicPhotosUrl: panoramicPhotosUrl.trim()," src/screens/CreateListingScreen.tsx` returns `1`.
      * `grep -Fc "tours: tours.length > 0 ? tours : undefined," src/screens/CreateListingScreen.tsx` returns `1`.
    - Comment present documenting the D-08 intent: `grep -Fc "admin only (D-08)" src/screens/CreateListingScreen.tsx` returns `2` (one above Matterport section, one above panoramic input).
    - TypeScript compiles: `npx tsc --noEmit 2>&1 | grep -E "CreateListingScreen\.tsx.*error TS" | wc -l` returns `0`.
    - Full-file grep invariant post-plan: `grep -c "isAdmin\|userType === 'admin'" src/screens/CreateListingScreen.tsx` returns `0`.
    - Full test suite regression: `npx jest --runInBand 2>&1 | tail -5` shows zero `failed`.
  </acceptance_criteria>
  <done>
    Three `<Gated>` wrappers present in `CreateListingScreen.tsx` (editVerifications from Task 2, editMatterportUrl from this task, editPanoramicUrl from this task). videoUrl and instagramUrl inputs remain siblings — visible to all users. Matterport section + panoramic input visible only to admins. D-09 preserve-on-save anchors untouched. Zero `isAdmin` or `userType === 'admin'` references in this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Non-admin lister -> Listing owner's persisted admin-only fields | When a non-admin edits a listing that already has `tours[]` or `panoramicPhotosUrl` set by an admin (or the system), the non-admin can STILL save the listing. The UI gate hides the inputs, but the payload MUST spread the existing values unchanged. If the payload accidentally zeroes them out, the admin-curated content is destroyed silently. This is the D-09 preserve-on-save invariant. |
| Non-admin lister -> New admin-only fields on create | A non-admin creating a new listing CANNOT inject `tours` / `panoramicPhotosUrl` values (the input UI is gated). They CAN still inject via a tampered JS bundle; the Railway backend is the real boundary (Plan 07 D-22 coordination). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 (T1 seed — HIGH) | Elevation of Privilege | Non-admin crafts a create/update PATCH with `tours`/`matterportUrl`/`panoramicPhotosUrl` set | mitigate (partial) | Client UI gate via `<Gated>` + existing UI (no input fields shown). Service-layer guard at Plan 04 covers `patchPlatformVerifications` (verification switches) but NOT `createProperty`/`updateProperty` which carry tours/panoramicPhotosUrl (D-15 scope). Real boundary is the Railway backend (Plan 07 coordination; if unconfirmed, D-22 accepted-risk row in PROJECT.md Key Decisions). |
| T-03-05-02 | Tampering | D-09 violation — an executor wraps the payload `tours`/`panoramicPhotosUrl` fields inside the admin branch, blanking them on non-admin save | mitigate | Explicit anti-pattern warning in action text + acceptance_criteria grep that confirms lines 390 and 394 remain unchanged. Also: D-14 invariant #1 grep (no `userType === 'admin'` in file) forces executor through `can(action)` checks, which the payload branch already uses. |
| T-03-05-03 | Information Disclosure | Over-wide `<Gated>` wrap accidentally hides videoUrl/instagramUrl from all users (RESEARCH §2.2 anti-pattern) | mitigate | Acceptance_criteria include anti-pattern greps confirming videoUrl and instagramUrl are NOT inside the `editPanoramicUrl` Gated wrapper. The `awk` range check between `<Gated action="editPanoramicUrl">` and `</Gated>` must not contain videoUrl or instagramUrl placeholder strings. |
| T-03-05-04 (T4 seed — LOW) | Regression | Future maintainer reintroduces `userType === 'admin'` inline check in this file | mitigate | D-14 invariant #1 grep is enforced at phase exit (Plan 07) as a CI gate. Any new inline check in this file on next PR will fail CI. |
</threat_model>

<verification>
- `grep -c "isAdmin" src/screens/CreateListingScreen.tsx` returns `0`.
- `grep -c "userType === 'admin'" src/screens/CreateListingScreen.tsx` returns `0`.
- `grep -c "backendProfile.userType" src/screens/CreateListingScreen.tsx` returns `0`.
- Three `<Gated>` wrappers present with the expected actions: `editVerifications`, `editMatterportUrl`, `editPanoramicUrl`.
- `npx tsc --noEmit` reports zero errors in this file.
- All Phase-3 tests still GREEN (`npx jest --runInBand` clean).
- D-09 preserve-on-save anchor lines 185, 390, 394 untouched (three grep checks confirm).
- videoUrl and instagramUrl TextInputs are siblings of the `editPanoramicUrl` Gated, NOT children.
</verification>

<success_criteria>
- File local invariant D-14 #1, #2 hold (zero `userType === 'admin'` and zero `backendProfile.userType` references in the file).
- All 6 call sites from D-11 migrated:
  1. Line 110 declaration swapped.
  2. Line 319 guard migrated.
  3. Line 396 payload branch gated by `can('editVerifications')` (Q1 resolved).
  4. Line 933 verification switches wrapped in `<Gated action="editVerifications">`.
  5. Matterport section wrapped in `<Gated action="editMatterportUrl">`.
  6. Panoramic `TextInput` wrapped in `<Gated action="editPanoramicUrl">` (with videoUrl + instagramUrl as siblings, NOT wrapped).
- D-08 behavior change active: non-admin users do not see Matterport / panoramic / verification UI.
- D-09 preserve-on-save invariant intact: hydration and payload spread lines unchanged.
- TypeScript clean. Tests clean.
</success_criteria>

<output>
After completion, create `.planning/phases/03-role-gating-precursor/03-05-SUMMARY.md` documenting:
- The 6 migrated sites with before/after line numbers (line numbers may drift by a few lines due to insertions — capture the post-migration numbers).
- Three `<Gated>` wrappers with their actions and what they wrap.
- Confirmation of D-09 preserve-on-save anchors (quote the three unchanged lines: 185, 390, 394 by content).
- `grep -c "isAdmin" src/screens/CreateListingScreen.tsx` = 0 confirmation.
- TypeScript + Jest clean confirmation (paste `npx tsc --noEmit && npx jest --runInBand 2>&1 | tail -5`).
- Anti-pattern confirmation: awk-range check showing videoUrl and instagramUrl are NOT inside the panoramic Gated block.
</output>
