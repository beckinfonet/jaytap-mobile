---
phase: 03-role-gating-precursor
plan: 07
plan_id: 03-07
type: execute
wave: 4
depends_on:
  - 03-04
  - 03-05
  - 03-06
files_modified:
  - scripts/check-role-grep.sh
  - .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md
  - .planning/phases/03-role-gating-precursor/03-VERIFICATION.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - GATE-03
  - GATE-05
requirements_addressed:
  - GATE-03
  - GATE-05
tags:
  - phase-exit
  - ci-gate
  - backend-coordination
user_setup:
  - service: railway-backend
    why: "Confirm server-side admin enforcement for GATE-05 per D-21/D-22"
    dashboard_config:
      - task: "Contact the Railway backend owner and ask whether PATCH /properties/:id/verifications and PUT /properties/:id (with tours/matterportUrl/panoramicPhotosUrl payloads) enforce admin server-side"
        location: "Railway team Slack / email (out-of-band)"

must_haves:
  truths:
    - "A runnable script `scripts/check-role-grep.sh` exists that enforces all 4 D-14 grep invariants; exits 0 on pass, non-zero on fail"
    - "The phase-exit gate command (`./scripts/check-role-grep.sh`) passes green across the entire `src/` tree"
    - "A `03-BACKEND-COORDINATION.md` artifact exists recording the Railway ask + response (or 'no response')"
    - "GATE-05 outcome is recorded in `03-VERIFICATION.md` as either 'confirmed' (path A) or 'accepted risk' (path B)"
    - "If path B taken: PROJECT.md Key Decisions has a new row documenting the accepted-risk disposition per D-22"
  artifacts:
    - path: "scripts/check-role-grep.sh"
      provides: "CI-runnable 4-part grep invariant from D-14"
      contains: "userType === 'admin'"
    - path: ".planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md"
      provides: "Backend coordination ticket artifact per RESEARCH §5.3"
      contains: "Status:"
    - path: ".planning/phases/03-role-gating-precursor/03-VERIFICATION.md"
      provides: "Phase 3 exit-verification record including GATE-05 outcome"
      contains: "GATE-05 outcome:"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row for D-22 accepted-risk fallback (conditional — only if path B)"
      contains: "Role Gating Precursor"
  key_links:
    - from: "scripts/check-role-grep.sh"
      to: "src/"
      via: "4 grep commands against the entire src/ tree"
      pattern: "grep -rn"
    - from: ".planning/phases/03-role-gating-precursor/03-VERIFICATION.md"
      to: ".planning/PROJECT.md"
      via: "cross-reference: if path B, point to the PROJECT.md row"
      pattern: "Key Decisions"
---

<objective>
Wave 4 phase-exit work. Three concerns:

1. Lock the D-14 4-part grep invariant as a runnable CI gate (`scripts/check-role-grep.sh`). This script enforces that no regressions can reintroduce `userType === 'admin'` or unauthorized `backendProfile.userType` / `adminAllowlist` reads into the codebase post-Phase-3.

2. Execute the backend coordination artifact per D-21 / RESEARCH §5 — create `03-BACKEND-COORDINATION.md` as the ticket-tracking file, record the ask to the Railway backend owner, and capture the response (or the absence of one).

3. Close out GATE-05 via one of the two D-22 exit paths: (A) confirmed enforcement recorded in `03-VERIFICATION.md`, or (B) accepted-risk row added to `.planning/PROJECT.md` Key Decisions + VERIFICATION.md.

Purpose: The 4-part grep invariant is the regression bar for the whole migration — every future PR touching `src/` will run it and be blocked if anyone reintroduces `userType === 'admin'` inline checks. Backend coordination closes GATE-05 without blocking the ship per D-23.

Output: One new script + three new/modified planning artifacts. Phase 3 is ready for `/gsd-verify-work` after this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-role-gating-precursor/03-CONTEXT.md
@.planning/phases/03-role-gating-precursor/03-RESEARCH.md
@.planning/phases/03-role-gating-precursor/03-VALIDATION.md
@.planning/phases/03-role-gating-precursor/03-04-SUMMARY.md
@.planning/phases/03-role-gating-precursor/03-05-SUMMARY.md
@.planning/phases/03-role-gating-precursor/03-06-SUMMARY.md

<interfaces>
D-14 four-part grep invariant (from CONTEXT.md):

1. `grep -rn 'userType === .admin.' src/` returns zero matches.
2. `grep -rn 'backendProfile.userType' src/` returns ONLY `useRole.ts` hook internals.
3. `grep -rn 'ADMIN_EMAIL\|adminAllowlist' src/` returns ONLY `useRole.ts` + `adminAllowlist.ts` (no other call-site imports).
4. `grep -rn 'isAllowlistedAdmin' src/` returns ONLY `adminAllowlist.ts` definition + `useRole.ts` consumer.

Relevant D-13 exception: `ProfileScreen.tsx` still reads `profile.userType` at line 70 via `setUserType(profile.userType || '')` — that is a DISPLAY-ONLY read, not a gating comparison. Pattern `backendProfile.userType` would NOT match this (ProfileScreen reads `profile.userType`, not `backendProfile.userType`) — invariant #2 holds.

Backend coordination ask (from RESEARCH §5.1 — paste-ready):

> Hi — for JayTap v1.0.4 we're adding client-side gating on admin-only fields. Can you confirm whether the Railway backend **independently verifies** that the authenticated user (`x-firebase-uid` header) is an admin server-side before accepting these requests? Without server enforcement, the client gate is cosmetic only.
>
> Two specific endpoints:
> 1. `PATCH /properties/:id/verifications` — sets `platformVerifications.{ownershipDocuments, ownerIdentityVerified, stateIssuedDocumentsVerified}`. Current code: `src/services/PropertyService.ts:190-213`. Sends `x-firebase-uid` + `firebaseUid` in body.
> 2. `PUT /properties/:id` — any write that includes `tours`, `matterportUrl` (deprecated field), or `panoramicPhotosUrl` in the payload. Current code: `src/services/PropertyService.ts:117-187`.
>
> Specifically: if a user whose backend `userType !== 'admin'` sends one of these requests, does the backend reject (what status code?), or accept?
>
> Context: this is M1 precursor work. M2 will ship Firebase custom claims + server-side token verification (`firebase-admin` SDK) — but for M1 we'd like to know whether we're shipping a cosmetic gate or a real-enough one.

D-22 two exit paths:

- Path A (confirmed): Backend team answers "yes, enforced." Record the verbatim response in 03-VERIFICATION.md. No PROJECT.md change.
- Path B (unconfirmed at M1 ship): Add a row to PROJECT.md Key Decisions: "Accepted risk: client-only gating on admin URL / verification edits in v1.0.4. Backend enforcement not confirmed by M1 ship; rolls into existing CONCERNS.md 'Firebase uid used as sole auth' pre-existing risk. M2 ROLE-04 ships server-side Firebase token verification." Also record in 03-VERIFICATION.md.

Existing `scripts/` directory status: check via `ls scripts 2>/dev/null` — if the directory does not exist, create it as part of Task 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/check-role-grep.sh enforcing D-14 4-part grep invariant</name>
  <files>scripts/check-role-grep.sh</files>
  <read_first>
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md D-14 (the four invariants, verbatim)
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §4 Wave-boundary gates
    - .planning/phases/03-role-gating-precursor/03-VALIDATION.md §Per-Task Verification Map row "4-part grep invariant (D-14)"
  </read_first>
  <behavior>
    - Script is runnable as `./scripts/check-role-grep.sh` (executable bit set; Bash shebang).
    - Exits 0 when all 4 invariants pass.
    - Exits non-zero with a human-readable error identifying which invariant failed and the violating lines.
    - Runs in under 2 seconds on a typical repo-sized `src/` tree.
    - Output includes a green "OK" line per passing invariant and a banner on overall result.
  </behavior>
  <action>
Create the `scripts/` directory if it does not exist:

```
mkdir -p scripts
```

Create `scripts/check-role-grep.sh` with the following exact content. This script implements D-14's four grep invariants as a composite CI gate.

```bash
#!/usr/bin/env bash
# check-role-grep.sh — Phase 3 (Role Gating Precursor) D-14 invariant enforcer.
#
# Runs four grep invariants across src/ and fails non-zero if any regressed.
# Designed to run in CI or pre-commit. No external deps beyond bash + grep.
#
# Expected clean output: four "OK" lines + a banner. Exit 0.
# Any failure prints the violating grep output and the invariant number, exit 1.

set -u
cd "$(dirname "$0")/.."

fail=0
violations=""

echo "== D-14 Phase-3 role-gating grep invariant =="

# Invariant 1: no inline `userType === 'admin'` in src/
hits1=$(grep -rn "userType === 'admin'" src/ 2>/dev/null || true)
if [ -n "$hits1" ]; then
  echo "FAIL #1: userType === 'admin' found in src/"
  echo "$hits1"
  fail=1
  violations="$violations #1"
else
  echo "OK   #1: no inline userType === 'admin' comparisons"
fi

# Invariant 2: `backendProfile.userType` only inside useRole.ts
hits2=$(grep -rn "backendProfile\.userType" src/ 2>/dev/null | grep -v '^src/hooks/useRole\.ts:' || true)
if [ -n "$hits2" ]; then
  echo "FAIL #2: backendProfile.userType found outside src/hooks/useRole.ts"
  echo "$hits2"
  fail=1
  violations="$violations #2"
else
  echo "OK   #2: backendProfile.userType read only inside useRole.ts"
fi

# Invariant 3: ADMIN_EMAIL / adminAllowlist identifiers only in useRole.ts + adminAllowlist.ts
hits3=$(grep -rn -E "ADMIN_EMAIL|adminAllowlist" src/ 2>/dev/null | grep -v -E '^src/(constants/adminAllowlist\.ts:|hooks/useRole\.ts:)' || true)
if [ -n "$hits3" ]; then
  echo "FAIL #3: ADMIN_EMAIL or adminAllowlist referenced outside the two allowed files"
  echo "$hits3"
  fail=1
  violations="$violations #3"
else
  echo "OK   #3: allowlist identifiers confined to useRole.ts + adminAllowlist.ts"
fi

# Invariant 4: isAllowlistedAdmin only in adminAllowlist.ts + useRole.ts
hits4=$(grep -rn "isAllowlistedAdmin" src/ 2>/dev/null | grep -v -E '^src/(constants/adminAllowlist\.ts:|hooks/useRole\.ts:)' || true)
if [ -n "$hits4" ]; then
  echo "FAIL #4: isAllowlistedAdmin referenced outside the two allowed files"
  echo "$hits4"
  fail=1
  violations="$violations #4"
else
  echo "OK   #4: isAllowlistedAdmin symbol confined to its definition + hook consumer"
fi

echo "==========================================="
if [ $fail -eq 0 ]; then
  echo "PASS: all 4 D-14 grep invariants hold"
  exit 0
else
  echo "FAIL: D-14 violations in invariants:$violations"
  exit 1
fi
```

Make it executable:

```
chmod +x scripts/check-role-grep.sh
```

The script is self-contained (no dependencies beyond bash + grep). It is intentionally verbose so a failing CI run immediately shows which invariant failed and the violating lines.
  </action>
  <verify>
    <automated>test -x scripts/check-role-grep.sh &amp;&amp; head -1 scripts/check-role-grep.sh | grep -q "^#!/usr/bin/env bash$" &amp;&amp; bash scripts/check-role-grep.sh &amp;&amp; bash scripts/check-role-grep.sh | grep -q "PASS: all 4 D-14 grep invariants hold"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/check-role-grep.sh` exits 0.
    - `test -x scripts/check-role-grep.sh` exits 0 (executable bit set).
    - `head -1 scripts/check-role-grep.sh` returns `#!/usr/bin/env bash`.
    - `./scripts/check-role-grep.sh` exits 0 (all 4 invariants pass at this point in the phase).
    - Output includes exactly four lines matching `^OK   #[1-4]:` (one per invariant).
    - Output includes the banner `PASS: all 4 D-14 grep invariants hold`.
    - Failure-mode coverage: the script's FAIL-path logic is covered by the inline invariants (each grep block prints `FAIL #N:` with violating lines on non-empty match). No smoke-test fixture is planted in `src/` — the main gate (`./scripts/check-role-grep.sh exits 0 on current tree`) together with the inline FAIL-path code blocks is sufficient. Rationale: planting a violation file inside `src/` risks leaving the tree dirty if the run aborts mid-test; the script does not accept a path argument so a safer out-of-tree fixture would require a script signature change that is out of scope for Phase 3.
    - Runtime: `time bash scripts/check-role-grep.sh` reports real time under 2 seconds.
  </acceptance_criteria>
  <done>
    `scripts/check-role-grep.sh` exists, is executable, implements all 4 D-14 invariants, exits 0 on the current clean state of the repo, and produces the expected banner output.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 03-BACKEND-COORDINATION.md artifact + record the outreach</name>
  <files>.planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md</files>
  <read_first>
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §5.1 (paste-ready outreach text), §5.3 (artifact template)
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md D-21 (parallel coordination task), D-22 (two exit paths), D-23 (phase does NOT block on response)
    - src/services/PropertyService.ts lines 117-213 (the two endpoints referenced in the outreach)
  </read_first>
  <behavior>
    - New file exists at `.planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` using the RESEARCH §5.3 template.
    - Status field populated based on whether the executor (Claude) has access to send the outreach message or if it is logged as a to-do for the human.
    - Per-endpoint status section lists both endpoints.
    - File does NOT block the phase — D-23 says coordination runs parallel. If no response, Task 3 picks up Path B.
  </behavior>
  <action>
Create `.planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` with this content:

```markdown
# Phase 3 — Backend Enforcement Coordination

**Opened:** 2026-04-23 (Phase 3 Wave 4)
**Contact:** Railway backend owner (out-of-band — project owner to route)
**Status:** OPEN

## Context

Phase 3 (Role Gating Precursor) ships client-side gating on admin-only fields in v1.0.4. Per GATE-05 + CONTEXT D-21, we must either CONFIRM or ACCEPT-AS-KNOWN-RISK that the Railway backend independently enforces admin-only writes. Without server enforcement, the client gate is cosmetic only (PITFALLS §Pitfall 5, CONCERNS.md "Firebase uid used as sole auth").

Per D-23 the phase implementation does NOT block on this response — the coordination runs in parallel. Two exit paths per D-22:

- **Path A (confirmed):** Railway confirms enforcement. Record verbatim response here and close GATE-05 as "confirmed" in `03-VERIFICATION.md`. No PROJECT.md change.
- **Path B (unconfirmed at M1 ship):** Add an accepted-risk row to `.planning/PROJECT.md` Key Decisions. Record the same here and in `03-VERIFICATION.md`.

## Question sent

> Hi — for JayTap v1.0.4 we're adding client-side gating on admin-only fields. Can you confirm whether the Railway backend **independently verifies** that the authenticated user (`x-firebase-uid` header) is an admin server-side before accepting these requests? Without server enforcement, the client gate is cosmetic only.
>
> Two specific endpoints:
> 1. `PATCH /properties/:id/verifications` — sets `platformVerifications.{ownershipDocuments, ownerIdentityVerified, stateIssuedDocumentsVerified}`. Current code: `src/services/PropertyService.ts:190-213`. Sends `x-firebase-uid` + `firebaseUid` in body.
> 2. `PUT /properties/:id` — any write that includes `tours`, `matterportUrl` (deprecated field), or `panoramicPhotosUrl` in the payload. Current code: `src/services/PropertyService.ts:117-187`.
>
> Specifically: if a user whose backend `userType !== 'admin'` sends one of these requests, does the backend reject (what status code?), or accept?
>
> Context: this is M1 precursor work. M2 will ship Firebase custom claims + server-side token verification (`firebase-admin` SDK) — but for M1 we'd like to know whether we're shipping a cosmetic gate or a real-enough one.

**Channel:** TBD — project owner to route via Railway team Slack / email.
**Sent on:** PENDING (to be filled when outreach lands).

## Response received

*(To be filled — paste verbatim response + timestamp + respondent name/handle. If no response by M1 submission day, note "no response at ship" and take Path B.)*

## Outcome

- [ ] **Path A — confirmed:** Record verbatim confirmation below + mirror into `03-VERIFICATION.md`.
- [ ] **Path B — accepted risk at ship:** Add row to `.planning/PROJECT.md` Key Decisions; mirror into `03-VERIFICATION.md`.

## Per-endpoint status

- [ ] `PATCH /properties/:id/verifications` — status: [confirmed / accepted risk] — [rationale / link to response]
- [ ] `PUT /properties/:id` (tours + panoramicPhotosUrl + matterportUrl fields) — status: [confirmed / accepted risk] — [rationale / link to response]

## Notes

Per D-23: Phase 3 implementation (hook + migrations + service guard + tests) ships regardless of the response on this coordination. What this file governs is whether PROJECT.md gets a new Key Decisions row (Path B) or not (Path A). The phase's code is already complete at the time this coordination artifact opens.
```

The file is a LIVING artifact — Task 3 updates it AND `03-VERIFICATION.md` based on which path Claude / the project owner can confirm at the time of plan execution. If Claude is running this plan autonomously and cannot reach the Railway team, the executor takes Path B in Task 3 (the phase must ship per D-23; unconfirmed-at-ship is the explicit fallback).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md &amp;&amp; grep -q "Status:" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md &amp;&amp; grep -q "PATCH /properties/:id/verifications" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md &amp;&amp; grep -q "Path A" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md &amp;&amp; grep -q "Path B" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` exits 0.
    - `grep -c "Status:" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` returns a number `>= 1`.
    - `grep -c "PATCH /properties/:id/verifications" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` returns `1`.
    - `grep -c "PUT /properties/:id" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` returns `1`.
    - `grep -c "Path A" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` returns a number `>= 1`.
    - `grep -c "Path B" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` returns a number `>= 1`.
    - `grep -c "D-22" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` returns a number `>= 1`.
    - `grep -c "D-23" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` returns a number `>= 1`.
  </acceptance_criteria>
  <done>
    Coordination artifact exists with ask, response-slot, two-path outcome checklist, and per-endpoint status list. Status is OPEN (to be closed by Task 3).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Close GATE-05 via path A (confirmed) or path B (accepted risk) + write 03-VERIFICATION.md + phase-exit gate run</name>
  <files>.planning/phases/03-role-gating-precursor/03-VERIFICATION.md, .planning/PROJECT.md, .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md</files>
  <read_first>
    - .planning/PROJECT.md (REQUIRED — inspect Key Decisions format BEFORE editing. Run `grep -qE '^\| Decision' .planning/PROJECT.md && grep -q 'Key Decisions' .planning/PROJECT.md` to detect table-vs-bullet layout deterministically; see STEP 2B selector below.)
    - .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md (output of Task 2 — may or may not have a Railway response at execution time)
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md D-22 (two-path spec)
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §5.2 (two-path mechanics)
    - Output of `./scripts/check-role-grep.sh` (from Task 1 — must pass)
  </read_first>
  <behavior>
    - The executor inspects `03-BACKEND-COORDINATION.md` for an actual Railway response. If none exists (expected in autonomous execution), take Path B per D-22 / D-23.
    - Path A: Update `03-BACKEND-COORDINATION.md` to `Status: CONFIRMED`, populate the Response + Outcome + Per-endpoint fields with verbatim quotes. Write `03-VERIFICATION.md` recording "GATE-05 outcome: confirmed." No PROJECT.md change.
    - Path B: Update `03-BACKEND-COORDINATION.md` to `Status: UNCONFIRMED-AT-SHIP`, mark Path B box. Add a row to `.planning/PROJECT.md` Key Decisions. Write `03-VERIFICATION.md` recording "GATE-05 outcome: accepted risk."
    - Regardless of path: `03-VERIFICATION.md` includes the output of `./scripts/check-role-grep.sh` confirming all 4 D-14 invariants green. Also includes a brief summary of the Jest test count (all Phase 3 tests green).
    - `./scripts/check-role-grep.sh` is run once more at the very end and its PASS output is embedded in `03-VERIFICATION.md`.
  </behavior>
  <action>
Two sub-steps.

STEP 1 — Determine the path. Read `03-BACKEND-COORDINATION.md`. If the "Response received" section has a verbatim Railway confirmation (not the placeholder "(To be filled ...)" text), take Path A. Otherwise, take Path B. In autonomous execution this will almost always be Path B because no out-of-band channel has been opened.

STEP 2A — If Path A (confirmed):

1. Edit `03-BACKEND-COORDINATION.md`: change `**Status:** OPEN` to `**Status:** CONFIRMED`. Fill in the Response received section with the verbatim quote + date + respondent. Check the "Path A" box in the Outcome section. Update the Per-endpoint status list with `confirmed`.

2. Create `.planning/phases/03-role-gating-precursor/03-VERIFICATION.md` with this structure:

```markdown
# Phase 3 — Verification Record

**Verified:** 2026-04-23
**Requirements:** GATE-01, GATE-02, GATE-03, GATE-04, GATE-05

## GATE-01..GATE-04 — implementation sign-off

- `src/hooks/useRole.ts`, `src/components/Gated.tsx`, `src/constants/adminAllowlist.ts` exist.
- 4 `isAdmin` sites in `CreateListingScreen.tsx` migrated; 3 `<Gated>` wraps added (D-11 sites 1-6).
- `PropertyDetailsScreen.tsx:178` + `401` migrated (D-11 sites 7-8).
- `ProfileScreen.tsx:33` migrated to `can('manageListings')` per D-12 (site 9); display-only userType state at line 70 preserved per D-13.
- `PropertyService.patchPlatformVerifications` guarded by `canFromUser(userData, 'editVerifications')`; throws `PermissionDeniedError('E_PERMISSION_DENIED')`.
- EN + RU locale files have `errors.permissionDenied` key (D-18).

## D-14 grep invariant output

Captured from `./scripts/check-role-grep.sh`:

```
<paste the exact script output here>
```

Status: PASS — all 4 invariants green.

## Unit tests

`npx jest --runInBand` output:

```
<paste the final 5-10 lines>
```

All Phase 3 tests GREEN.

## GATE-05 outcome: CONFIRMED

Railway backend team confirmed server-side admin enforcement on:
- `PATCH /properties/:id/verifications` — [paste quote]
- `PUT /properties/:id` (tours + panoramicPhotosUrl + matterportUrl) — [paste quote]

Response received: [date] — [name / handle].

No PROJECT.md change required per D-22 Path A.

## Sign-off

Phase 3 exit bar met. Ready for `/gsd-verify-work`.
```

STEP 2B — If Path B (unconfirmed at M1 ship):

1. Edit `03-BACKEND-COORDINATION.md`: change `**Status:** OPEN` to `**Status:** UNCONFIRMED-AT-SHIP`. In the Response received section, write: `**No response at ship** — coordination channel not closed in M1 timeline. Accepted-risk fallback taken per D-22 Path B.` Check the Path B box in the Outcome section. Update Per-endpoint list: both entries marked `accepted risk — rolls into PROJECT.md Key Decisions`.

2. Edit `.planning/PROJECT.md`. DETERMINISTIC FORMAT SELECTOR:

   Run: `grep -qE '^\| Decision' .planning/PROJECT.md && grep -q 'Key Decisions' .planning/PROJECT.md`

   - **IF exit code 0** (a Markdown table with a "Decision" column exists under a "Key Decisions" section), append THIS EXACT table row at the end of the Key Decisions table:

     ```markdown
     | Backend enforcement of admin endpoints (PATCH /properties/:id/verifications, /tours) | M1 1.0.4 | Phase 3 | Accepted as known risk — not confirmed by Railway team by release cut. Release proceeds per D-22. |
     ```

   - **ELSE** (Key Decisions section exists but is not a Decision-columned table — bulleted list or other prose format), append THIS EXACT bullet under the `## Key Decisions` section heading:

     ```markdown
     - **Backend enforcement (Phase 3, M1 1.0.4):** Accepted as known risk — not confirmed by Railway team by release cut. Release proceeds per D-22.
     ```

   Preserve all existing formatting, table columns, and bullet indentation. Do NOT reorder or delete any existing rows/bullets. Do NOT create a new Key Decisions section — if none exists, STOP and surface to the human operator.

3. Create `.planning/phases/03-role-gating-precursor/03-VERIFICATION.md` with the same structure as Step 2A but replace the GATE-05 section with:

```markdown
## GATE-05 outcome: ACCEPTED RISK

No response from Railway backend team by M1 ship deadline. Accepted-risk path taken per D-22 Path B.

Rationale: This risk is NOT new in v1.0.4. CONCERNS.md "Firebase uid used as sole auth" (pre-existing) is the independent, pre-Phase-3 source of this exposure. Phase 3's client-side gating reduces the blast radius for in-app mistakes (typos, forgotten gates) but does not close the server-side enforcement gap. M2 ROLE-04 closes the gap via `firebase-admin` SDK and signed ID token verification.

Disposition: Accepted risk, documented in `.planning/PROJECT.md` Key Decisions (see new row "Role Gating Precursor (v1.0.4)").

Backend coordination artifact: `03-BACKEND-COORDINATION.md` (Status: UNCONFIRMED-AT-SHIP).
```

STEP 3 — Run `./scripts/check-role-grep.sh` one final time. Capture the output and paste it into the D-14 grep invariant output section of `03-VERIFICATION.md`. Must exit 0. If it does not, STOP and raise — something regressed between Plans 04-06 and now.

STEP 4 — Run the full Jest suite `npx jest --runInBand` and paste the final summary (last 5-10 lines) into the Unit tests section of `03-VERIFICATION.md`. All tests must pass.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-role-gating-precursor/03-VERIFICATION.md &amp;&amp; grep -q "GATE-05 outcome:" .planning/phases/03-role-gating-precursor/03-VERIFICATION.md &amp;&amp; grep -q "D-14 grep invariant" .planning/phases/03-role-gating-precursor/03-VERIFICATION.md &amp;&amp; bash scripts/check-role-grep.sh &amp;&amp; grep -E "Status: (CONFIRMED|UNCONFIRMED-AT-SHIP)" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/03-role-gating-precursor/03-VERIFICATION.md` exits 0.
    - `grep -c "GATE-05 outcome:" .planning/phases/03-role-gating-precursor/03-VERIFICATION.md` returns a number `>= 1`.
    - `03-VERIFICATION.md` contains either `GATE-05 outcome: CONFIRMED` OR `GATE-05 outcome: ACCEPTED RISK` (exactly one of the two).
    - `grep -c "D-14 grep invariant" .planning/phases/03-role-gating-precursor/03-VERIFICATION.md` returns a number `>= 1`.
    - `03-VERIFICATION.md` contains the PASS banner from the script: `grep -q "PASS: all 4 D-14 grep invariants hold" .planning/phases/03-role-gating-precursor/03-VERIFICATION.md` succeeds.
    - `03-VERIFICATION.md` contains a Jest summary line (typically `Tests:`, `Suites:`, or `Test Suites:`): `grep -E "^(Tests|Test Suites):" .planning/phases/03-role-gating-precursor/03-VERIFICATION.md | wc -l` returns a number `>= 1`.
    - `grep -E "Status: (CONFIRMED|UNCONFIRMED-AT-SHIP)" .planning/phases/03-role-gating-precursor/03-BACKEND-COORDINATION.md` succeeds (status was moved off OPEN).
    - If Path B taken: `grep -qE "Backend enforcement|Role Gating Precursor" .planning/PROJECT.md` AND `grep -q "D-22" .planning/PROJECT.md` AND `grep -qE "not confirmed by Railway team by release cut|not confirmed by M1 ship" .planning/PROJECT.md`. The exact match string depends on which snippet was written (table row contains "not confirmed by Railway team by release cut"; bullet contains "not confirmed by Railway team by release cut" as well — both snippets use that phrase verbatim so one grep covers both formats).
    - If Path A taken: PROJECT.md is unchanged — run `git diff --stat .planning/PROJECT.md 2>/dev/null | grep -q "1 file changed" && echo "modified" || echo "unchanged"`; either is acceptable per which path was taken.
    - Final `./scripts/check-role-grep.sh` exit status: 0 (captured in VERIFICATION.md).
    - Final `npx jest --runInBand` exit status: 0 (all Phase 3 tests + pre-existing App.test.tsx green).
  </acceptance_criteria>
  <done>
    GATE-05 closed via one of the two D-22 paths. `03-VERIFICATION.md` captures the 4-part grep invariant PASS + Jest summary + GATE-05 outcome. `03-BACKEND-COORDINATION.md` status updated. If Path B: PROJECT.md has the new Key Decisions row. Phase 3 is ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo source tree -> Future PRs | The 4-part grep invariant is the regression bar. Without it, a future maintainer can reintroduce inline `userType === 'admin'` checks, silently eroding Phase 3's migration discipline. |
| M1 ship boundary -> M2 | D-22 Path B accepts a known risk. Without the PROJECT.md row, that risk is undocumented — a future reader cannot trace the M1 posture or know it's queued for M2 ROLE-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 (T4 seed — LOW) | Regression | Future PR reintroduces `userType === 'admin'` inline | mitigate | `scripts/check-role-grep.sh` is the CI-runnable gate. Task 1 creates it. If wired into a pre-commit hook or CI step (planner scope — see Plan 07 Task 1), it blocks regressions. Note: wiring into `.github/workflows/` or `husky` is out of scope for Phase 3 — the script exists and is runnable; next infra phase can wire it. |
| T-03-07-02 (T1 seed — HIGH, part 2) | Elevation of Privilege | Backend unconfirmed at ship; attacker crafts direct HTTP request | accept (documented) | D-22 Path B: PROJECT.md Key Decisions row records the accepted risk. CONCERNS.md already documents the `Firebase uid used as sole auth` pre-existing issue — Phase 3 does NOT regress it, only surfaces it. M2 ROLE-04 closes. |
| T-03-07-03 | Repudiation | No audit trail of GATE-05 outcome | mitigate | `03-VERIFICATION.md` records the outcome. `03-BACKEND-COORDINATION.md` records the ask + response (or absence). Both files land in the phase directory + commit history. |
</threat_model>

<verification>
- `scripts/check-role-grep.sh` exists, executable, exits 0 on current tree.
- `03-BACKEND-COORDINATION.md` exists with Status != OPEN.
- `03-VERIFICATION.md` exists with both D-14 grep invariant output (PASS) and GATE-05 outcome line.
- If Path B: `.planning/PROJECT.md` has a new row / bullet in Key Decisions referencing the accepted risk.
- Full Jest suite passes.
</verification>

<success_criteria>
- D-14 4-part grep invariant enforceable as a runnable command (`./scripts/check-role-grep.sh`).
- GATE-05 outcome recorded (confirmed or accepted risk).
- All Phase 3 artifacts co-located under `.planning/phases/03-role-gating-precursor/`:
  * `03-CONTEXT.md` (pre-existing)
  * `03-DISCUSSION-LOG.md` (pre-existing)
  * `03-RESEARCH.md` (pre-existing)
  * `03-PATTERNS.md` (pre-existing)
  * `03-VALIDATION.md` (pre-existing)
  * `03-01-PLAN.md` ... `03-07-PLAN.md` (this plan set)
  * `03-01-SUMMARY.md` ... `03-07-SUMMARY.md` (one per plan)
  * `03-BACKEND-COORDINATION.md` (new — Task 2)
  * `03-VERIFICATION.md` (new — Task 3)
- Phase 3 ready for `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-role-gating-precursor/03-07-SUMMARY.md` documenting:
- Path taken (A or B) with the inline reason (e.g., "Path B: no Railway response in autonomous execution").
- Final `./scripts/check-role-grep.sh` PASS output.
- Final `npx jest --runInBand` pass count.
- Link to `03-VERIFICATION.md` and `03-BACKEND-COORDINATION.md`.
- If Path B: quote the new PROJECT.md row / bullet added.
- Confirmation that Phase 3 meets exit criteria: all 5 ROADMAP success criteria verifiably met.
</output>
