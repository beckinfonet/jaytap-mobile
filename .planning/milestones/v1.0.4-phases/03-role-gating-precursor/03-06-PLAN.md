---
phase: 03-role-gating-precursor
plan: 06
plan_id: 03-06
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/screens/PropertyDetailsScreen.tsx
  - src/screens/ProfileScreen.tsx
autonomous: true
requirements:
  - GATE-01
  - GATE-03
  - GATE-04
requirements_addressed:
  - GATE-01
  - GATE-03
  - GATE-04
tags:
  - call-site-migration
  - ui-gating
user_setup: []

must_haves:
  truths:
    - "No `userType === 'admin'` comparison remains in PropertyDetailsScreen.tsx (D-14 invariant #1 locally)"
    - "No `userType === 'admin'` comparison remains in ProfileScreen.tsx (D-14 invariant #1 locally)"
    - "Admin-verify-documents icon on PropertyDetailsScreen is wrapped in <Gated action=\"editVerifications\"> with the inner onAdminVerifyDocuments prop nil-check preserved"
    - "ProfileScreen.canManageListings is derived from can('manageListings') — admin OR renter userType per D-12"
    - "ProfileScreen display-only userType state (line 70 setUserType) is NOT migrated (D-13 — display, not gating)"
  artifacts:
    - path: "src/screens/PropertyDetailsScreen.tsx"
      provides: "useRole consumer; admin-verify icon wrapped in <Gated>"
      contains: "import { useRole } from '../hooks/useRole';"
    - path: "src/screens/ProfileScreen.tsx"
      provides: "useRole consumer; canManageListings = can('manageListings')"
      contains: "import { useRole } from '../hooks/useRole';"
  key_links:
    - from: "src/screens/PropertyDetailsScreen.tsx"
      to: "src/hooks/useRole.ts"
      via: "useRole() destructure"
      pattern: "useRole\\(\\)"
    - from: "src/screens/ProfileScreen.tsx"
      to: "src/hooks/useRole.ts"
      via: "useRole() destructure at top of component"
      pattern: "useRole\\(\\)"
    - from: "src/screens/PropertyDetailsScreen.tsx"
      to: "src/components/Gated.tsx"
      via: "Gated wrapping admin-verify icon button"
      pattern: "<Gated action=\"editVerifications\">"
---

<objective>
Migrate PropertyDetailsScreen.tsx and ProfileScreen.tsx to the Phase 3 gating surface. This plan runs in Wave 3 in parallel with Plan 05 (Plan 05 touches only CreateListingScreen.tsx; Plan 06 touches only these two files — zero file overlap).

Three call-site migrations per D-11 / RESEARCH §3.7:

1. `PropertyDetailsScreen.tsx:178` `const isAdmin = user?.backendProfile?.userType === 'admin';` → `const { can } = useRole();` (drop isAdmin — only one non-gating use at line 401 which this plan also migrates).
2. `PropertyDetailsScreen.tsx:401` `{isAdmin && onAdminVerifyDocuments && (...)}` → `<Gated action="editVerifications">{onAdminVerifyDocuments && (...)}</Gated>`. Preserve the inner `onAdminVerifyDocuments &&` nil-check (that is a prop-presence check, not a gate — D-11).
3. `ProfileScreen.tsx:33` `const canManageListings = userType === 'renter' || userType === 'admin';` → `const { can } = useRole(); const canManageListings = can('manageListings');` (D-12: `can('manageListings')` encapsulates admin OR renter-userType).

D-13 NOT-migrated (explicit): `ProfileScreen.tsx:70` `setUserType(profile.userType || '')` — display state, kept as-is. The local `userType` state variable also stays (still used for display copy/conditionals that aren't gating).

Purpose: Same-wave parallel with Plan 05 maximizes throughput. Each file is a self-contained 1-2-line migration. Grep invariant after this plan: `grep -c "userType === 'admin'" src/screens/PropertyDetailsScreen.tsx src/screens/ProfileScreen.tsx` returns 0.

Output: Two modified files. All Phase 3 tests remain GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-role-gating-precursor/03-CONTEXT.md
@.planning/phases/03-role-gating-precursor/03-RESEARCH.md
@.planning/phases/03-role-gating-precursor/03-PATTERNS.md
@.planning/phases/03-role-gating-precursor/03-02-SUMMARY.md
@src/screens/PropertyDetailsScreen.tsx
@src/screens/ProfileScreen.tsx
@src/hooks/useRole.ts
@src/components/Gated.tsx

<interfaces>
PropertyDetailsScreen.tsx line 178 (isAdmin declaration; context: inside the main component function, after `const { user } = useAuth()`):
```
  const isAdmin = user?.backendProfile?.userType === 'admin';
```

PropertyDetailsScreen.tsx lines ~395-409 (admin-verify icon button — surrounded by the header-right-actions View):
```
      <View style={styles.header}>
        <TouchableOpacity onPress={onBack} style={[styles.iconButton, { backgroundColor: colors.surface }]}>
          <Text style={[styles.iconText, { color: colors.text }]}>←</Text>
        </TouchableOpacity>
        <Text style={[styles.headerTitle, { color: colors.text }]}>{t('property.details')}</Text>
        <View style={styles.headerRightActions}>
          {isAdmin && onAdminVerifyDocuments && (
            <TouchableOpacity
              style={[styles.iconButton, { backgroundColor: colors.surface }]}
              onPress={() => onAdminVerifyDocuments(property)}
              accessibilityLabel={t('verification.screenTitle')}
            >
              <ShieldCheck size={22} color={colors.accent} />
            </TouchableOpacity>
          )}
          ...
```

Note: There is a SECOND `ShieldCheck` usage at PropertyDetailsScreen.tsx:669 but it is NOT gated by isAdmin (grep confirms only line 401 has the isAdmin conditional). Do NOT touch line 669.

ProfileScreen.tsx lines 20-33 (component setup):
```
function ProfileScreenComponent({ onBack, onCreateListing, onViewListings, onViewFavorites, onViewAppointments, onViewAccountSettings }: ProfileScreenProps) {
    const { user, logout } = useAuth();
    const { t } = useLanguage();
    const { isDark } = useTheme();

    const [isRenterApplicant, setIsRenterApplicant] = useState(false);
    const [userType, setUserType] = useState<string>('');
    const [loading, setLoading] = useState(false);
    const [loggingOut, setLoggingOut] = useState(false);
    const [blockSize, setBlockSize] = useState<'30min' | '60min'>('30min');
    const profileDataLoadedRef = useRef(false);

    /** Same listing tools as renters; admins keep access after userType change */
    const canManageListings = userType === 'renter' || userType === 'admin';
```

ProfileScreen.tsx line 70 (display state — NOT migrated per D-13):
```
                    setUserType(profile.userType || '');
```

ProfileScreen.tsx line 168 (consumer of canManageListings — do not touch):
```
                    {canManageListings && (
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate PropertyDetailsScreen.tsx — replace isAdmin declaration + wrap admin-verify icon in Gated</name>
  <files>src/screens/PropertyDetailsScreen.tsx</files>
  <read_first>
    - src/screens/PropertyDetailsScreen.tsx lines 1-30 (existing import block — locate useAuth import)
    - src/screens/PropertyDetailsScreen.tsx lines 170-185 (isAdmin declaration context at line 178)
    - src/screens/PropertyDetailsScreen.tsx lines 395-415 (admin-verify icon button block at line 401)
    - src/screens/PropertyDetailsScreen.tsx lines 665-675 (verify the second ShieldCheck usage at line 669 is NOT isAdmin-gated — should not require any change)
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §3.7 rows 7 and 8 (migration mechanics)
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md D-11 sites 5 and 6
    - src/hooks/useRole.ts, src/components/Gated.tsx (confirm named exports)
  </read_first>
  <behavior>
    - File imports `useRole` from `../hooks/useRole` and `Gated` from `../components/Gated`.
    - Line 178 `const isAdmin = ...` replaced by `const { can } = useRole();`.
    - Line 401 `{isAdmin && onAdminVerifyDocuments && (...)}` replaced by `<Gated action="editVerifications">{onAdminVerifyDocuments && (...)}</Gated>` — preserving the inner `onAdminVerifyDocuments &&` nil-check.
    - After task: `grep -c "isAdmin" src/screens/PropertyDetailsScreen.tsx` returns `0`.
    - After task: `grep -c "userType === 'admin'" src/screens/PropertyDetailsScreen.tsx` returns `0`.
    - The unrelated `ShieldCheck` at line 669 is UNCHANGED.
  </behavior>
  <action>
Three sequential edits in `src/screens/PropertyDetailsScreen.tsx`.

EDIT 1 — Add two imports. Find the existing `useAuth` import (`import { useAuth } from '../context/AuthContext';`). Immediately AFTER that line, add:

```
import { useRole } from '../hooks/useRole';
import { Gated } from '../components/Gated';
```

EDIT 2 — Replace line 178. Current:

```
  const isAdmin = user?.backendProfile?.userType === 'admin';
```

Replace with:

```
  const { can } = useRole();
```

Drop `isAdmin` entirely — only one remaining reference at line 401, which Edit 3 migrates.

EDIT 3 — Line ~401. Current (inside the header-right-actions View):

```
          {isAdmin && onAdminVerifyDocuments && (
            <TouchableOpacity
              style={[styles.iconButton, { backgroundColor: colors.surface }]}
              onPress={() => onAdminVerifyDocuments(property)}
              accessibilityLabel={t('verification.screenTitle')}
            >
              <ShieldCheck size={22} color={colors.accent} />
            </TouchableOpacity>
          )}
```

Replace with:

```
          <Gated action="editVerifications">
            {onAdminVerifyDocuments && (
              <TouchableOpacity
                style={[styles.iconButton, { backgroundColor: colors.surface }]}
                onPress={() => onAdminVerifyDocuments(property)}
                accessibilityLabel={t('verification.screenTitle')}
              >
                <ShieldCheck size={22} color={colors.accent} />
              </TouchableOpacity>
            )}
          </Gated>
```

Key mechanics: the outer `{isAdmin &&` becomes `<Gated action="editVerifications">`. The inner `onAdminVerifyDocuments &&` stays because it is a PROP-PRESENCE check (not a gating check) — if the parent didn't pass the callback, there is no action to gate. D-11 explicitly preserves this shape.

Do NOT touch the second `ShieldCheck` usage at line 669 — grep confirms it is NOT isAdmin-gated (it is inside a different UI path).
  </action>
  <verify>
    <automated>grep -Fq "import { useRole } from '../hooks/useRole';" src/screens/PropertyDetailsScreen.tsx &amp;&amp; grep -Fq "import { Gated } from '../components/Gated';" src/screens/PropertyDetailsScreen.tsx &amp;&amp; grep -Fq "const { can } = useRole();" src/screens/PropertyDetailsScreen.tsx &amp;&amp; grep -Fq '<Gated action="editVerifications">' src/screens/PropertyDetailsScreen.tsx &amp;&amp; grep -Fq "{onAdminVerifyDocuments &amp;&amp; (" src/screens/PropertyDetailsScreen.tsx &amp;&amp; [ "$(grep -c isAdmin src/screens/PropertyDetailsScreen.tsx)" = "0" ] &amp;&amp; [ "$(grep -c 'userType === .admin.' src/screens/PropertyDetailsScreen.tsx)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "import { useRole } from '../hooks/useRole';" src/screens/PropertyDetailsScreen.tsx` returns `1`.
    - `grep -Fc "import { Gated } from '../components/Gated';" src/screens/PropertyDetailsScreen.tsx` returns `1`.
    - `grep -Fc "const { can } = useRole();" src/screens/PropertyDetailsScreen.tsx` returns `1`.
    - `grep -c "isAdmin" src/screens/PropertyDetailsScreen.tsx` returns `0` (no residual references).
    - `grep -c "userType === 'admin'" src/screens/PropertyDetailsScreen.tsx` returns `0` (D-14 invariant #1 holds locally).
    - `grep -c "backendProfile.userType" src/screens/PropertyDetailsScreen.tsx` returns `0` (D-14 invariant #2 holds locally).
    - `grep -Fc '<Gated action="editVerifications">' src/screens/PropertyDetailsScreen.tsx` returns exactly `1` (one wrapper only — the admin-verify icon).
    - Inner nil-check preserved (D-11): `grep -Fc "{onAdminVerifyDocuments && (" src/screens/PropertyDetailsScreen.tsx` returns `1`.
    - Both `ShieldCheck` usages remain in the file: `grep -Fc "<ShieldCheck " src/screens/PropertyDetailsScreen.tsx` returns `2` (line 407 inside the Gated, line 669 untouched).
    - TypeScript clean in this file: `npx tsc --noEmit 2>&1 | grep -E "PropertyDetailsScreen\\.tsx.*error TS" | wc -l` returns `0`.
  </acceptance_criteria>
  <done>
    Two imports added; `isAdmin` declaration replaced; admin-verify icon wrapped in `<Gated action="editVerifications">` with inner nil-check preserved. Zero `isAdmin` or `userType === 'admin'` references. Second ShieldCheck (line 669) unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate ProfileScreen.tsx — replace canManageListings derivation with can('manageListings')</name>
  <files>src/screens/ProfileScreen.tsx</files>
  <read_first>
    - src/screens/ProfileScreen.tsx lines 1-15 (existing import block — locate useAuth import)
    - src/screens/ProfileScreen.tsx lines 20-35 (component setup + canManageListings derivation at line 33)
    - src/screens/ProfileScreen.tsx lines 60-75 (confirm setUserType at line 70 is display-state — NOT migrated per D-13)
    - src/screens/ProfileScreen.tsx lines 165-175 (canManageListings consumer at line 168 — unchanged)
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §3.7 row 9 (migration specifics), row 10 (D-13 — NOT migrated)
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md D-11 site 7, D-12 (manageListings action: admin OR renter userType), D-13 (display-only userType stays)
  </read_first>
  <behavior>
    - File imports `useRole` from `../hooks/useRole`.
    - New `const { can } = useRole();` declaration inserted before line 33 (ideally immediately after the existing `useAuth`/`useLanguage`/`useTheme` destructures at lines 21-23, so all hook calls are grouped at the top of the component body).
    - Line 33 `canManageListings` derivation REPLACED with `const canManageListings = can('manageListings');`.
    - Comment at line 32 `/** Same listing tools as renters; admins keep access after userType change */` kept or updated to reference `can('manageListings')` (planner's choice — either keep the original or update).
    - Line 70 `setUserType(profile.userType || '')` UNCHANGED — D-13 display-only state stays.
    - `userType` local state at line 26 UNCHANGED — still used for display copy and conditionals elsewhere.
    - After task: `grep -c "userType === 'admin'" src/screens/ProfileScreen.tsx` returns `0`.
  </behavior>
  <action>
Three sequential edits in `src/screens/ProfileScreen.tsx`.

EDIT 1 — Add one import. Find the existing `useAuth` import (`import { useAuth } from '../context/AuthContext';`). Immediately AFTER that line, add:

```
import { useRole } from '../hooks/useRole';
```

Do NOT import `Gated` — ProfileScreen uses only `can()`, no JSX gating in this file.

EDIT 2 — Add `useRole()` destructure after existing hook calls. Find the existing destructure block at lines 21-23:

```
    const { user, logout } = useAuth();
    const { t } = useLanguage();
    const { isDark } = useTheme();
```

Immediately AFTER line 23 (`const { isDark } = useTheme();`), insert a new line at the same 4-space indent:

```
    const { can } = useRole();
```

After the insertion, the block reads:

```
    const { user, logout } = useAuth();
    const { t } = useLanguage();
    const { isDark } = useTheme();
    const { can } = useRole();
```

EDIT 3 — Replace line 33 canManageListings derivation. Current:

```
    /** Same listing tools as renters; admins keep access after userType change */
    const canManageListings = userType === 'renter' || userType === 'admin';
```

Replace with:

```
    /** Same listing tools as renters; admins keep access. Encapsulated in can('manageListings') per D-12. */
    const canManageListings = can('manageListings');
```

Do NOT delete the `userType` state variable at line 26 — it is still consumed by display code elsewhere in the file (D-13). Do NOT change line 70 `setUserType(profile.userType || '')` — display state, kept by D-13. The `canManageListings` consumer at line 168 `{canManageListings && (` is unchanged (same variable name, same boolean semantic).

Why this works for D-12: `can('manageListings')` is defined in useRole.ts as `role === 'admin' || user?.backendProfile?.userType === 'renter'`. That encapsulates the pre-migration logic (admin OR renter userType) inside the hook, so the UI layer no longer reads `userType` directly for GATING (D-14 invariant #2). The UI still reads `userType` for display copy — that stays on purpose (D-13).
  </action>
  <verify>
    <automated>grep -Fq "import { useRole } from '../hooks/useRole';" src/screens/ProfileScreen.tsx &amp;&amp; grep -Fq "const { can } = useRole();" src/screens/ProfileScreen.tsx &amp;&amp; grep -Fq "const canManageListings = can('manageListings');" src/screens/ProfileScreen.tsx &amp;&amp; [ "$(grep -c 'userType === .admin.' src/screens/ProfileScreen.tsx)" = "0" ] &amp;&amp; grep -Fq "setUserType(profile.userType || '');" src/screens/ProfileScreen.tsx &amp;&amp; grep -Fq "useState&lt;string&gt;('')" src/screens/ProfileScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "import { useRole } from '../hooks/useRole';" src/screens/ProfileScreen.tsx` returns `1`.
    - `grep -Fc "const { can } = useRole();" src/screens/ProfileScreen.tsx` returns `1`.
    - `grep -Fc "const canManageListings = can('manageListings');" src/screens/ProfileScreen.tsx` returns `1`.
    - `grep -c "userType === 'admin'" src/screens/ProfileScreen.tsx` returns `0` (D-14 invariant #1 holds locally).
    - `grep -c "userType === 'renter'" src/screens/ProfileScreen.tsx` returns `0` (the gating check moved inside the hook; D-14 invariant #2 reinforced).
    - D-13 anchors intact:
      * `grep -Fc "setUserType(profile.userType || '');" src/screens/ProfileScreen.tsx` returns `1` (display state kept).
      * `grep -Fc "const [userType, setUserType] = useState<string>('')" src/screens/ProfileScreen.tsx` returns `1` (state declaration kept — the display variable).
      * `grep -Fc "{canManageListings && (" src/screens/ProfileScreen.tsx` returns `1` (consumer at line 168 kept unchanged).
    - No import of `Gated` (this file does not need it): `grep -Fc "import { Gated }" src/screens/ProfileScreen.tsx` returns `0`.
    - TypeScript clean: `npx tsc --noEmit 2>&1 | grep -E "ProfileScreen\\.tsx.*error TS" | wc -l` returns `0`.
  </acceptance_criteria>
  <done>
    useRole imported; `const { can } = useRole();` added to component-body hook cluster; `canManageListings` derivation now uses `can('manageListings')`. D-13 display state for `userType` preserved (state declaration + setUserType call + any display-copy consumers unchanged). Zero `userType === 'admin'` references.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI -> Client UI (intra-screen) | PropertyDetailsScreen's admin-verify icon and ProfileScreen's manage-listings entry are both UX-layer gates. Neither is a security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Elevation of Privilege | Non-admin sees the admin-verify icon and taps it (reaches `onAdminVerifyDocuments(property)` -> eventually `PropertyService.patchPlatformVerifications`) | mitigate | Plan 04's service-layer guard throws `PermissionDeniedError` if the service is reached. UI gate via `<Gated>` hides the icon entirely (D-08 "hide entirely" posture). Defense-in-depth. |
| T-03-06-02 | Tampering | Executor drops the `onAdminVerifyDocuments &&` inner nil-check during the migration | mitigate | D-11 explicitly preserves the nil-check. Acceptance_criteria includes `grep -Fc "{onAdminVerifyDocuments && (" src/screens/PropertyDetailsScreen.tsx` returns `1`. If the executor removes the nil-check, admin users with no callback prop would see a tap-target that does nothing — regression. |
| T-03-06-03 | Tampering | Executor migrates ProfileScreen.tsx line 70 setUserType (D-13 forbids this) | mitigate | D-13 is explicit in both CONTEXT and acceptance_criteria. Grep check: `grep -Fc "setUserType(profile.userType || '');" src/screens/ProfileScreen.tsx` returns `1`. Also: userType state declaration at line 26 must survive (another grep check). |
| T-03-06-04 | Repudiation | can('manageListings') returns true for renter userType. M2 plan is to replace with custom claims; if the M2 plan never lands and the renter userType can be self-assigned by a compromised backend, non-admins get manage-listings access. | accept (M1) | D-12 semantics encapsulate the existing behavior (admin OR renter). The pre-migration code had the same trust chain — not a Phase 3 regression. M2 ROLE-01..04 closes it. |
</threat_model>

<verification>
- PropertyDetailsScreen.tsx: zero `isAdmin`, zero `userType === 'admin'`, zero `backendProfile.userType` references; one `<Gated action="editVerifications">` wrapper; both ShieldCheck usages present (401 inside Gated, 669 untouched).
- ProfileScreen.tsx: zero `userType === 'admin'`, zero `userType === 'renter'` references; `can('manageListings')` is the new derivation; D-13 display-only userType state (line 26 declaration + line 70 setter) intact; `canManageListings` consumer at line 168 unchanged.
- TypeScript clean for both files.
- All Phase 3 tests GREEN.
</verification>

<success_criteria>
- D-14 invariants #1 and #2 hold locally in both modified files.
- D-11 call sites #5, #6, #7 migrated (per RESEARCH §3.7 table rows 7, 8, 9).
- D-12 `manageListings` action is the single source of truth for the "admin OR renter can manage listings" rule.
- D-13 display-only userType state preserved in ProfileScreen (state declaration + setter + non-gating consumers).
- Inner nil-check `onAdminVerifyDocuments &&` preserved inside `<Gated>` per D-11.
- TypeScript + Jest clean.
</success_criteria>

<output>
After completion, create `.planning/phases/03-role-gating-precursor/03-06-SUMMARY.md` documenting:
- PropertyDetailsScreen.tsx post-state: zero isAdmin refs, `<Gated action="editVerifications">` wrapper location (final line number), inner nil-check preserved.
- ProfileScreen.tsx post-state: useRole destructure inserted after useTheme (final line number), canManageListings derivation replaced, userType display state preserved at line 26 + 70.
- Confirmation that `ShieldCheck` at PropertyDetailsScreen.tsx:669 was NOT touched.
- Any line-number drift caused by import insertions (capture the post-migration line numbers).
- `npx tsc --noEmit && npx jest --runInBand 2>&1 | tail -5` output (both clean).
</output>
