---
phase: 01-nav-reliability
plan: 02
type: execute
wave: 0.5
depends_on: [01]
files_modified:
  - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md
  - .planning/phases/01-nav-reliability/videos/
autonomous: false
requirements: [NAV-03]
requirements_addressed: [NAV-03, NAV-01, NAV-02]
tags: [react-native, navigation, baseline-qa, validation]

must_haves:
  truths:
    - "Human has run the full 45-cell matrix on iOS on the current HEAD (pre-fix) and recorded every cell PASS/FAIL/N/A/FLAKY"
    - "Human has run the full 45-cell matrix on Android on the current HEAD (pre-fix) and recorded every cell PASS/FAIL/N/A/FLAKY"
    - "Every FAIL cell has an accompanying video under videos/{platform}-S{n}-T{m}.mp4"
    - "All 6 high-risk transitions HR-1..HR-6 have video evidence on both iOS and Android (except HR-6-iOS which is architecturally N/A)"
    - "Baseline section of matrix is committed to git at a SHA that predates any App.tsx touch-handling edit — this git-log ordering IS NAV-03"
  artifacts:
    - path: ".planning/phases/01-nav-reliability/01-REPRO-MATRIX.md"
      provides: "Filled Baseline (pre-fix) section — every cell has a PASS/FAIL/N/A/FLAKY marker, commit SHA recorded in header"
      contains: "commit [a-f0-9]{7,}"
    - path: ".planning/phases/01-nav-reliability/videos/"
      provides: "Video evidence for every Baseline FAIL cell + all HR-1..HR-6 transitions (minimum ~12 videos if baseline reproduces the bug; fewer only if no FAIL found)"
  key_links:
    - from: "matrix commit SHA"
      to: "git log App.tsx"
      via: "git log ordering — matrix commit MUST predate any App.tsx touch-handling change"
      pattern: "baseline commit SHA is ancestor of first App.tsx edit in Plan 03"
---

<objective>
Human runs the full 45-cell reproduction matrix on the current HEAD commit (BEFORE any App.tsx touch-handling edits land) on both physical iOS and physical Android, records every cell's result in 01-REPRO-MATRIX.md "Baseline (pre-fix)" section, captures video evidence for every FAIL and every HR-1..HR-6 transition, and commits the filled baseline. This commit is NAV-03's satisfying artifact — the git log ordering (baseline before fix) is load-bearing, not a formality.

Purpose: Establish the measurable pre-fix state. "0% repro" post-fix is only meaningful against this recorded baseline. Per RESEARCH §3.8 protocol: step 1–3 of the pre-fix baseline recording protocol happen here.

Output: Filled Baseline (pre-fix) section + FAIL-cell videos + HR videos + git commit that predates Plan 03's first App.tsx edit.

Branch outcome per RESEARCH §9 A6 / §10 Q2: if baseline produces zero FAILs, Phase 1 scope collapses to the pointerEvents prophylactic alone and D-04 "pause, reassess" triggers. Plan 03 still runs the belt-and-suspenders, but Plan 04's gate evaluates against "did we introduce any regression," not "did we reduce FAILs to zero."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-nav-reliability/01-CONTEXT.md
@.planning/phases/01-nav-reliability/01-RESEARCH.md
@.planning/phases/01-nav-reliability/01-VALIDATION.md
@.planning/phases/01-nav-reliability/01-REPRO-MATRIX.md

<interfaces>
<!-- RESEARCH §2.1 canonical trap sequence (the minimum cell execution protocol): -->
<!-- 1. Cold-start app, land on Home -->
<!-- 2. Navigate to starting state Sn (see matrix "How to reach" column) -->
<!-- 3. Tap target tab Tm in BottomNavigator -->
<!-- 4. Within 1 s, observe: visual content switch (PASS) or no switch / ripple-only / ignored (FAIL) -->
<!-- 5. Record cell + notes -->

<!-- RESEARCH §3.7 evidence format: -->
<!-- ✅ PASS | ❌ FAIL | — N/A | ? FLAKY -->
<!-- FAIL cells require videos/{platform}-S{n}-T{m}.mp4 -->
<!-- HR-1..HR-6 require videos regardless of pass/fail -->
<!-- FLAKY cells require 3 recordings -->

<!-- RESEARCH §3.8 protocol steps 1-3: -->
<!-- 1. Build release build of iOS and Android from HEAD (pre-Wave-1) -->
<!-- 2. Run full 45-cell matrix each platform, record every FAIL as video -->
<!-- 3. Commit as "Baseline (pre-fix)" with HEAD SHA -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human runs Baseline matrix on iOS physical device</name>
  <what-built>
    Matrix skeleton from Plan 01 is in place. App is on current HEAD (no App.tsx edits from this phase have landed). Human now executes the baseline measurement on physical iOS device.
  </what-built>
  <how-to-verify>
    **This is manual-device-qa per CONTEXT D-03. Simulator does not satisfy exit criteria.**

    On your iOS device ({device/ver from matrix header}):

    1. Build a release (or dev) build from current HEAD and install on the device. Confirm you are on HEAD — no uncommitted App.tsx edits: `git status` should show `01-REPRO-MATRIX.md` and `videos/.gitkeep` as the only tracked changes from Phase 1.

    2. Execute the canonical trap sequence from RESEARCH §2.1 on device as a warm-up (S1 Home → S6 PropertyDetails → back → tap Profile).

    3. Run all 45 cells of the iOS baseline table in 01-REPRO-MATRIX.md:
       - For each (Sn, Tm) cell that is NOT marked `— N/A`:
         - Navigate to Sn per the "How to reach" column
         - Tap Tm in the bottom navigator
         - Within 1 s: observe PASS predicate (visual switch + responsive) or FAIL predicate (no switch / ripple-only / ignored / new tab shows but old overlay still captures taps)
         - Edit the matrix iOS table: replace the `⬜` marker with `✅ PASS`, `❌ FAIL`, or `? FLAKY`
         - For every FAIL: start screen recording BEFORE repeating the trap sequence, save as `.planning/phases/01-nav-reliability/videos/ios-S{n}-T{m}.mp4`
       - For FLAKY cells: record 3 separate attempts per RESEARCH §3.7, note the ratio in the cell comment (e.g., `? FLAKY 1/3`)

    4. For each HR transition (HR-1 S6→T1..T5, HR-2 S7→T1..T5, HR-3 S8→T1..T5, HR-4 S9→back→T1..T5, HR-5 nested S6→Tour3D→back→S6→back→T1..T5): regardless of pass/fail, screen-record and save as `videos/ios-S{n}-T{m}.mp4` or `videos/ios-HR{x}-T{m}.mp4`. Update the HR lines in the matrix with the video path and PASS/FAIL.

    5. At the bottom of the iOS baseline section, add a one-line summary: `iOS baseline summary: {count} PASS / {count} FAIL / {count} N/A / {count} FLAKY — run on {date}`.

    **Special case per RESEARCH §9 A6 / §10 Q2:** if you complete the full 45 cells and observe ZERO FAIL cells, stop here and signal in chat: "Baseline produced zero FAILs on iOS — D-04 reassess trigger." Per RESEARCH §3.8 step this means the bug is either not iOS-reproducible or not deterministic; Plan 03 will still run the pointerEvents prophylactic but Plan 04's gate changes meaning. Do NOT skip Android — still run Task 2.
  </how-to-verify>
  <read_first>
    - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md (the iOS table to fill)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §3.2 (starting states + "How to reach")
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §3.5 (HR transitions)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §3.6 (PASS/FAIL predicates)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §3.7 (evidence format + video paths)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §9 A6 (zero-FAIL-baseline branch)
  </read_first>
  <acceptance_criteria>
    - iOS baseline table in `01-REPRO-MATRIX.md` has zero `⬜` markers remaining — every cell is PASS/FAIL/N/A/FLAKY
    - `ls .planning/phases/01-nav-reliability/videos/ios-*.mp4 2>/dev/null | wc -l` returns ≥ 5 if any FAIL exists (one video per FAIL + HR videos) OR ≥ 5 if only HR videos exist
    - HR-1 through HR-5 iOS lines in the matrix each have a `videos/ios-...mp4` path filled (not `TBD`)
    - iOS baseline summary line present at bottom of iOS section with counts
    - `grep "iOS baseline summary" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` finds the line
  </acceptance_criteria>
  <resume-signal>Type "iOS baseline complete: {X} PASS / {Y} FAIL / {Z} N/A / {W} FLAKY, {N} videos captured" or "iOS baseline: zero-FAIL branch (D-04 reassess)" or describe blockers.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human runs Baseline matrix on Android physical device</name>
  <what-built>
    iOS baseline complete. Now repeat the 45-cell matrix on physical Android device, including HR-6 (Android hardware back is iOS-N/A).
  </what-built>
  <how-to-verify>
    Same protocol as Task 1, but on your Android device ({device/ver from matrix header}):

    1. Build and install release build from HEAD. Confirm no App.tsx edits: `git status`.

    2. Run all 45 Android cells — edit the Android table in `01-REPRO-MATRIX.md`. FAIL videos go to `videos/android-S{n}-T{m}.mp4`.

    3. Run HR-1 through HR-5 with Android video saves.

    4. Run HR-6 (unique to Android): from each starting state, press the Android hardware/gesture back once, then immediately tap a bottom-nav tab. Record whether the tab responds. Save video as `videos/android-HR6-T{m}.mp4`.

    5. Add Android baseline summary line.

    Same zero-FAIL-branch rule as Task 1: if zero FAILs on Android, signal "Android baseline produced zero FAILs — D-04 reassess."
  </how-to-verify>
  <read_first>
    - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md (Android table after Task 1's iOS fill)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §3.5 HR-6 "Any state immediately after Android hardware back"
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.2 C2 repro trigger (for context on Android-specific timing)
  </read_first>
  <acceptance_criteria>
    - Android baseline table has zero `⬜` remaining
    - `ls .planning/phases/01-nav-reliability/videos/android-*.mp4 2>/dev/null | wc -l` returns ≥ 6 (HR-1..HR-6 all present on Android; more if FAILs add videos)
    - HR-1 through HR-6 Android lines have filled video paths
    - Android baseline summary line present with counts
    - `grep "Android baseline summary" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` finds the line
  </acceptance_criteria>
  <resume-signal>Type "Android baseline complete: {X} PASS / {Y} FAIL / {Z} N/A / {W} FLAKY, {N} videos captured" or "Android baseline: zero-FAIL branch (D-04 reassess)" or describe blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit the Baseline matrix (this commit IS NAV-03)</name>
  <read_first>
    - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md (final filled state after human Tasks 1 + 2)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §3.8 steps 3 (commit as "Baseline (pre-fix)" with HEAD SHA)
    - .planning/phases/01-nav-reliability/01-VALIDATION.md (NAV-03 artifact-check: git log must show baseline commit predates any App.tsx touch-handling edit)
  </read_first>
  <files>.planning/phases/01-nav-reliability/01-REPRO-MATRIX.md, .planning/phases/01-nav-reliability/videos/</files>
  <action>
    Before committing: verify `git diff --name-only HEAD` contains only `.planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` and new `.planning/phases/01-nav-reliability/videos/*.mp4` files — NO `App.tsx` changes should be staged or unstaged. If any `App.tsx` change is present, STOP and alert the human — NAV-03 ordering is about to be violated.

    Read the current commit SHA: `git rev-parse --short HEAD`. Fill this into the matrix header, replacing `commit TBD (SHA of Wave 0.5 baseline commit)` with `commit <actual-sha>` on the `**Baseline captured:**` line. Also fill the `TBD` date with today's date.

    Stage and commit:
    ```
    git add .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md
    git add .planning/phases/01-nav-reliability/videos/
    git commit -m "docs(01): record pre-fix baseline reproduction matrix (NAV-03)

    Full 45-cell × 2-platform matrix executed on {iOS device/ver}, {Android device/ver}.
    Baseline captured BEFORE any App.tsx touch-handling edits land — this commit is
    NAV-03's satisfying artifact.

    iOS: {X} PASS / {Y} FAIL / {Z} N/A / {W} FLAKY
    Android: {X} PASS / {Y} FAIL / {Z} N/A / {W} FLAKY
    Video evidence: {N} files in videos/

    Per RESEARCH §3.8: this commit's SHA is the baseline against which Post-Wave-1
    results are measured."
    ```

    After the commit, re-read the HEAD SHA — this SHA now appears twice in the history if any summary is written later (in the matrix header as the recorded baseline, and as the actual commit SHA). That's intentional.
  </action>
  <verify>
    <automated>git log --oneline -1 .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md | grep -q "baseline" && git diff HEAD -- App.tsx | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1 -- .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` shows a commit with message containing "baseline" (case-insensitive) and the SHA is recorded in the matrix header
    - `git diff HEAD -- App.tsx | wc -l` returns `0` (no App.tsx changes landed in or before this commit from this phase — NAV-03 "before the fix" ordering preserved)
    - `git log --oneline App.tsx | head -5` shows the most recent App.tsx commit is older than the baseline commit OR there are no App.tsx commits after phase 01 began (verify by comparing commit dates)
    - `grep "commit [a-f0-9]\{7,\}" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` returns one line (the header SHA is filled, not `TBD`)
    - `grep "TBD" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md | wc -l` — any remaining TBDs are acceptable only in "Post-Wave-1" / "Post-Wave-N" sections (which are filled later), not in the Baseline section or header
    - Videos present: `ls .planning/phases/01-nav-reliability/videos/ | wc -l` returns ≥ 12 (6 HR × 2 platforms minimum, assuming HR-6-iOS remains N/A) — if fewer, fail with explanation (e.g., baseline produced zero FAILs AND some HR transitions weren't recordable)
  </acceptance_criteria>
  <done>
    Baseline committed. NAV-03 is satisfied: the baseline matrix is in git, with a commit SHA that predates any App.tsx touch-handling edit. Plan 03 may now run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan records observations and commits markdown + video files. No code execution, no network I/O beyond running the already-built app binary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | I (Information Disclosure) | videos/*.mp4 screen recordings | mitigate | Matrix transitions exercise Home / Favorites / Chat / Profile / PropertyDetails / CreateListing / RenterListings / Tour3D — none of which are auth screens per CONTEXT D-06. HR-6 (Android hardware back) may traverse auth paths — human MUST review any video involving Login / Signup / ForgotPassword / ResetPassword screens for email/password visibility before commit. If visible, re-record with test-only dummy credentials OR crop the recording. |
| T-01-02-02 | I | Railway test listing data (address / price / owner contact) in videos | accept | Test listings are non-production per CLAUDE.md "Clean-slate data. No production listings exist." Risk is LOW — content is synthetic. |

No S/T/R/D/E applicable — no code changes, no new network surface, no new auth paths, no new input handling.
</threat_model>

<verification>
- iOS + Android baseline tables both fully filled (zero `⬜`)
- Video directory contains ≥ 12 files (6 HR × 2 platforms minimum)
- Baseline commit exists with SHA recorded in matrix header
- `git log App.tsx` shows no App.tsx edits in or before the baseline commit from this phase
</verification>

<success_criteria>
- NAV-03 artifact-check: `git log --oneline -- .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` shows a "Baseline (pre-fix)" commit
- Commit SHA is ancestor of any future App.tsx touch-handling edit (verified via git ordering)
- Baseline cells are marked (either producing FAILs — expected — or zero FAILs triggering the D-04 reassess branch)
- Video evidence captured for all HR transitions and all FAIL cells
</success_criteria>

<output>
After completion, create `.planning/phases/01-nav-reliability/01-02-SUMMARY.md`:
- Baseline commit SHA
- iOS summary: `{X} PASS / {Y} FAIL / {Z} N/A / {W} FLAKY`
- Android summary: same
- Video count
- Branch: "normal" (FAILs exist) or "zero-FAIL" (D-04 reassess — note the reassess outcome if applicable)
- Explicit: `git log -- App.tsx | head -1` result confirming no App.tsx edits from phase 01 exist yet
</output>
