---
phase: 01-nav-reliability
plan: 06
type: execute
wave: 4
depends_on: [04]
files_modified:
  - App.tsx
  - src/components/BottomNavigator.tsx
  - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md
  - .planning/phases/01-nav-reliability/videos/
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03]
requirements_addressed: [NAV-01, NAV-02, NAV-03]
tags: [react-native, navigation, cleanup, finalize, phase-gate]

must_haves:
  truths:
    - "All diagnostic console.log calls tagged [NAV], [BACK], [C3], [C4] are stripped from App.tsx and src/components/BottomNavigator.tsx per CONTEXT D-02"
    - "All temporary diagnostic borders (borderWidth + borderColor magenta/cyan/yellow) from any C3 diagnostic that ran are removed"
    - "A final Post-Wave-N full 45×2 matrix run on both platforms shows 0% repro (or explicit zero-baseline-prophylactic note per Plan 02 A6 branch)"
    - "videos/ directory has ≥ 12 final-state videos (HR-1..HR-6 × 2 platforms) for the Post-Wave-N sign-off"
    - "The 5 ROADMAP Success Criteria for Phase 1 are all satisfied and each has a matrix entry referencing its evidence per RESEARCH §6 goal-backward mapping"
    - "The 8 items from RESEARCH §8 'What counts as validation passing' are all verifiable via the commands in the acceptance criteria"
    - "CONVENTIONS.md Keep-alive-and-overlay section survived the strip (was not accidentally touched); OVERLAY_FLAGS array still present with 5 conditions"
  artifacts:
    - path: "App.tsx"
      provides: "Final state — pointerEvents + OVERLAY_FLAGS retained; all [NAV]/[BACK]/[C3]/[C4] logs removed; all C3 diagnostic borders removed; BackHandler useEffect in whatever state C2 escalation left it (original or ref-pattern)"
      contains: "OVERLAY_FLAGS"
    - path: "src/components/BottomNavigator.tsx"
      provides: "Final state — [NAV] tab-tap-received log removed; any [C4] onPressIn/onPressOut/onPress logs removed; original tab-touchable behavior otherwise intact"
    - path: ".planning/phases/01-nav-reliability/01-REPRO-MATRIX.md"
      provides: "Final Post-Wave-N section showing 0% repro on all 90 cells + HR videos + Phase 1 sign-off block mapping SC1-SC5 to matrix evidence + human sign-off line"
      contains: "Phase 1 sign-off"
    - path: ".planning/phases/01-nav-reliability/videos/"
      provides: "Final-state videos (wave{N}-{platform}-HR{x}-T{m}.mp4) + all prior-wave baseline / wave1 / diagnostic videos retained for audit trail"
  key_links:
    - from: "strip grep"
      to: "zero-match acceptance"
      via: "grep -n '\\[NAV\\]\\|\\[BACK\\]\\|\\[C3\\]\\|\\[C4\\]' App.tsx src/components/BottomNavigator.tsx returns zero after Task 1"
      pattern: "NAV\\]|BACK\\]|C3\\]|C4\\]"
    - from: "Final matrix Post-Wave-N all-PASS"
      to: "NAV-01 + NAV-02 + SC1 + SC3 satisfaction"
      via: "RESEARCH §6 evidence mapping"
      pattern: "All cells PASS"
---

<objective>
Wave 4 — strip + finalize. Remove all diagnostic instrumentation introduced in Plans 03 and (if they ran) 05, run the final 45×2 matrix pass to prove 0% repro, and sign off Phase 1.

Purpose: Close out NAV-01 + NAV-02 + NAV-03 with a clean codebase. Per CONTEXT D-02, diagnostic logs are temporary and MUST NOT ship. Per RESEARCH §6 goal-backward mapping, each of the 5 Phase 1 Success Criteria gets an explicit evidence line in the matrix sign-off block. Per RESEARCH §8, the 8 "what counts as validation passing" items all become verifiable commands in this plan's acceptance.

Runs after: Plan 04 Decision A or D (skips Plan 05) OR Plan 05 final decision PROCEED (reached via any candidate's successful fix). Does NOT run if Plan 04 Decision C (pause-and-reassess) or Plan 05 final decision was D-04 pause — those outcomes block the phase gate pending human re-scoping.

Output: 2 strip commits (one for code diagnostic logs, one for C3 borders if they existed), one final device-matrix commit, and one sign-off commit. Phase 1 exits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-nav-reliability/01-CONTEXT.md
@.planning/phases/01-nav-reliability/01-RESEARCH.md
@.planning/phases/01-nav-reliability/01-VALIDATION.md
@.planning/phases/01-nav-reliability/01-REPRO-MATRIX.md
@.planning/phases/01-nav-reliability/01-04-SUMMARY.md
@App.tsx
@src/components/BottomNavigator.tsx

<interfaces>
Strip scope (from CONTEXT D-02 + RESEARCH §8 item 4):

Primary grep: `grep -n "\[NAV\]\|\[BACK\]\|\[C3\]\|\[C4\]" App.tsx src/components/BottomNavigator.tsx` — MUST return zero matches after Task 1.

Log sources by plan-of-origin:
- Plan 03 Task 3 installed 3 [NAV] logs in App.tsx + 1 [NAV] log in BottomNavigator.tsx (ALWAYS present when Plan 06 runs)
- Plan 05 Task 1 installed 3 [BACK] logs in App.tsx (present IF Plan 05 Task 1 ran)
- Plan 05 Task 5 installed 3+ [C3] logs + 3 diagnostic borders in App.tsx (present IF Plan 05 Task 5 ran)
- Plan 05 Task 9 installed 3 [C4] logs in BottomNavigator.tsx (present IF Plan 05 Task 9 ran)

Border scope (from RESEARCH §2.3): if any `// TEMP C3 diagnostic` comment exists in App.tsx, its accompanying `borderWidth: 3` + `borderColor: 'magenta'|'cyan'|'yellow'` fields must also be removed.

MUST preserve (NOT touch during strip):
- `OVERLAY_FLAGS` array (adjacent to state declarations, from Plan 03 Task 2)
- `hideMainStackUnderOverlay = OVERLAY_FLAGS.some(Boolean);` derivation
- `pointerEvents: visible ? 'auto' : 'none'` in mainStackScreenStyle
- Overlay-wrapper pointerEvents (RenterListings, CreateListing, PropertyDetails — from Plan 03 Task 1)
- C2 ref-pattern refactor if Plan 05 Task 3 landed (backStateRef + two useEffects)
- C3 auth-modal pointerEvents if Plan 05 Task 7 landed
- CONVENTIONS.md Keep-alive-and-overlay section (from Plan 03 Task 4)
- Any code the diagnostic logs were sitting adjacent to

RESEARCH §6 Goal-Backward SC→Evidence mapping (the sign-off block in the matrix must reference every SC):
- SC1 (0% repro on 45×2 matrix): matrix Post-Wave-N all cells PASS
- SC2 (baseline recorded before fix): git log SHA of Plan 02 baseline commit, pre-dates all App.tsx touch-handling edits
- SC3 (overlay return releases touch capture): HR-1..HR-5 videos PASS on both platforms
- SC4 (belt-and-suspenders pattern + OVERLAY_FLAGS + CONVENTIONS entry): grep -n "pointerEvents: visible" + grep -n "OVERLAY_FLAGS" + grep -n "^## Keep-alive and overlay screens" all return matches
- SC5 (Android hardware back unchanged if no C2, OR 12-point checklist pass if C2 landed): matrix notes which branch applies

RESEARCH §8 Validation-passing items (all 8 must be evidenced):
1. Baseline section complete with ≥1 FAIL OR documented-not-reproducible note
2. Post-Wave-N section complete with all 90 cells PASS, zero FAIL, zero FLAKY
3. videos/ contains ≥ 12 video files (6 HR × 2 platforms) for final sign-off
4. All diagnostic tags stripped (composite grep returns zero)
5. CONVENTIONS.md has new Keep-alive-and-overlay subsection
6. App.tsx mainStackScreenStyle has pointerEvents
7. App.tsx overlay wrap sites show style-based pointerEvents
8. App.tsx has OVERLAY_FLAGS array matching D-10 shape
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Route gate — run ONLY if reached via Plan 04 Decision A/D or Plan 05 PROCEED</name>
  <what-built>
    Matrix contains a decision block from Plan 04 and (if Plan 05 ran) additional Post-Wave-N Decision blocks from Plan 05 tasks.
  </what-built>
  <read_first>
    - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md (most recent Go/No-Go Decision block)
    - .planning/phases/01-nav-reliability/01-04-SUMMARY.md
    - .planning/phases/01-nav-reliability/01-05-SUMMARY.md (if Plan 05 ran)
  </read_first>
  <decision>
    Check the most recent "Next plan to run:" line in the matrix.

    - If "Plan 06" → PROCEED with Task 1.
    - If "Plan 05 Task X" → STOP. Plan 05 was supposed to run first. Re-route.
    - If "HOLD — D-04 reassess required" or "HOLD — reassess required" → STOP. Phase is paused pending human re-scoping.
    - If ambiguous or missing → STOP. Signal for manual routing.
  </decision>
  <options>
    <option id="proceed-finalize">
      <name>Proceed to Task 1 (strip diagnostics)</name>
      <pros>Matrix signaled PROCEED; phase ready to close.</pros>
      <cons>None.</cons>
    </option>
    <option id="stop-escalation-pending">
      <name>Stop — Plan 05 escalation pending</name>
      <pros>Prevents a clean strip while diagnostics are still active.</pros>
      <cons>None.</cons>
    </option>
    <option id="stop-reassess">
      <name>Stop — D-04 pause active</name>
      <pros>Honors the D-04 rule.</pros>
      <cons>Phase does not ship until re-scoped.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Route decision recorded in chat or as a comment at top of matrix
    - If stop, no code changes land in this plan run — exit cleanly
    - If proceed, Task 1 begins
  </acceptance_criteria>
  <resume-signal>Select: option-proceed-finalize or option-stop-escalation-pending or option-stop-reassess</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Strip all diagnostic logs and any C3 borders from App.tsx + BottomNavigator.tsx</name>
  <read_first>
    - App.tsx (identify every [NAV], [BACK], [C3], [C4] log line and any `// TEMP C3 diagnostic` borders)
    - src/components/BottomNavigator.tsx (identify [NAV] and any [C4] logs)
    - .planning/phases/01-nav-reliability/01-CONTEXT.md D-02 (strip mandate — diagnostic logs MUST NOT ship)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §8 item 4 (composite grep must return zero)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.3 (C3 border shapes — magenta/cyan/yellow `borderColor`, `borderWidth: 3`, `// TEMP C3 diagnostic` comments)
  </read_first>
  <files>App.tsx, src/components/BottomNavigator.tsx</files>
  <action>
    Step 1 — enumerate strip scope. Run: `grep -n "\[NAV\]\|\[BACK\]\|\[C3\]\|\[C4\]\|TEMP C3 diagnostic" App.tsx src/components/BottomNavigator.tsx`. Collect the full list of lines that must be removed.

    Step 2 — for each log line, remove:
    - The `console.log(...)` statement itself
    - Any immediate `const __backEffectCount = ...` declaration that was added as a pair to a [BACK] log (Plan 05 Task 1 added one)
    - Any `onStartShouldSetResponder` wrapper added only for the [C3] log (Plan 05 Task 5 added these). If the handler existed previously for another reason, leave it but remove the [C3] console.log from its body.

    Step 3 — for each C3 diagnostic border, remove:
    - The `borderWidth: 3` field that was added with a `// TEMP C3 diagnostic` comment
    - The `borderColor: 'magenta' | 'cyan' | 'yellow'` field with the matching TEMP comment
    - The `// TEMP C3 diagnostic` comment lines themselves

    Step 4 — verify the MUST-PRESERVE items are still intact:
    - `OVERLAY_FLAGS` array (should still be adjacent to state declarations)
    - `hideMainStackUnderOverlay = OVERLAY_FLAGS.some(Boolean);` derivation
    - `pointerEvents: visible ? 'auto' : 'none'` in mainStackScreenStyle
    - Overlay-wrapper pointerEvents (RenterListings, CreateListing, PropertyDetails)
    - C2 ref-pattern code (`backStateRef`, register-once effect with empty deps) IF Plan 05 Task 3 landed
    - C3 auth-modal pointerEvents IF Plan 05 Task 7 landed
    - BottomNavigator.tsx original onPress logic (without [NAV] / [C4] console.logs)

    Step 5 — TypeScript check. Then confirm the strip via the composite grep — it MUST return zero matches: `grep -n "\[NAV\]\|\[BACK\]\|\[C3\]\|\[C4\]\|TEMP C3 diagnostic" App.tsx src/components/BottomNavigator.tsx`.

    Step 6 — Commit atomically with message: `chore(01): strip diagnostic instrumentation (D-02, RESEARCH §8 item 4)`. Body: removes all `[NAV] / [BACK] / [C3] / [C4]` logs introduced in Waves 1/3; removes any C3 diagnostic borders; preserves OVERLAY_FLAGS, pointerEvents helpers, C2 ref-pattern (if landed), C3 fix (if landed), and CONVENTIONS.md entry; composite grep returns zero.
  </action>
  <verify>
    <automated>grep -c "NAV\]\|BACK\]\|C3\]\|C4\]" App.tsx src/components/BottomNavigator.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\[NAV\]\|\[BACK\]\|\[C3\]\|\[C4\]" App.tsx src/components/BottomNavigator.tsx` returns zero matches (satisfies RESEARCH §8 item 4 and CONTEXT D-02)
    - `grep -n "TEMP C3 diagnostic" App.tsx` returns zero matches
    - `grep -n "borderColor: 'magenta'\|borderColor: 'cyan'\|borderColor: 'yellow'" App.tsx` returns zero matches (C3 diagnostic borders removed)
    - `grep -n "OVERLAY_FLAGS" App.tsx` returns ≥ 2 matches (declaration + derivation) — MUST-PRESERVE item intact
    - `grep -n "pointerEvents: visible ? 'auto' : 'none'" App.tsx` returns one match (mainStackScreenStyle) — MUST-PRESERVE item intact
    - `grep -n "pointerEvents: isRenterListingsOpen" App.tsx` returns one match — MUST-PRESERVE item intact
    - `grep -c "pointerEvents: 'auto'" App.tsx` returns ≥ 2 — MUST-PRESERVE items intact (CreateListing + PropertyDetails)
    - `grep -n "^## Keep-alive and overlay screens$" .planning/codebase/CONVENTIONS.md` returns one match — MUST-PRESERVE item intact (CONVENTIONS.md not touched in this task)
    - If Plan 05 Task 3 landed: `grep -c "backStateRef" App.tsx` returns ≥ 3 (C2 ref-pattern still present)
    - If Plan 05 Task 7 landed: `grep -c "pointerEvents: (showResetPasswordModal\|pointerEvents: showResetPasswordModal" App.tsx` returns ≥ 1 (C3 auth-modal fix still present)
    - TypeScript compilation succeeds
    - `git log --oneline -1` returns a commit whose message contains "strip" and "diagnostic"
  </acceptance_criteria>
  <done>All diagnostic instrumentation stripped. App.tsx + BottomNavigator.tsx contain only the ship-state fixes + their surrounding code. Committed atomically.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Final 45×2 matrix run on both platforms (strip-state build) + HR video capture</name>
  <what-built>
    Post-strip build. Must show 0% repro on all 90 cells (or the zero-baseline-prophylactic outcome per Plan 02 A6). HR-1..HR-6 videos captured on this final build are the Post-Wave-N sign-off evidence.
  </what-built>
  <how-to-verify>
    1. Build fresh from HEAD (post-strip). Install on iOS + Android.

    2. Run the full 45 iOS cells + 45 Android cells. Record results in a new matrix subsection `## Post-Wave-N (Final — post-strip)` with both tables.

    3. Record HR-1..HR-5 videos on iOS and HR-1..HR-6 on Android (HR-6 iOS remains N/A). Save as `videos/final-ios-HR{x}-T{m}.mp4` and `videos/final-android-HR{x}-T{m}.mp4`. Minimum 12 final videos total (5 iOS HR × 1 tab each + 6 Android HR × 1 tab each = 11 conservative minimum; record one tab per HR; if multiple tabs per HR exercised, cap at 2 per HR).

    4. Add a Final-summary line: `Final Post-Wave-N summary: iOS {X} PASS / {Y} FAIL / {Z} N/A / {W} FLAKY; Android {X} PASS / {Y} FAIL / {Z} N/A / {W} FLAKY — run on {date}`.

    5. Apply the D-04 ship bar: if ANY cell is FAIL or FLAKY, Phase 1 does NOT exit — signal and route back (either revert strip commit or re-open diagnostics; human decides).

    6. Special case for Plan 02's zero-baseline branch: if baseline produced zero FAILs AND final produces zero FAILs, matrix note this explicitly: "Zero-baseline branch: bug not reproduced in baseline or final. pointerEvents + OVERLAY_FLAGS applied prophylactically per RESEARCH §9 A6."
  </how-to-verify>
  <read_first>
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §6 SC1 (0% repro bar)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §8 items 2 + 3 (post-wave-N complete, videos ≥ 12)
    - .planning/phases/01-nav-reliability/01-VALIDATION.md "Manual-Only Verifications"
  </read_first>
  <acceptance_criteria>
    - Matrix contains `## Post-Wave-N (Final — post-strip)` section with filled iOS + Android tables
    - Both tables have zero `⬜` markers remaining
    - Final-summary line present with counts
    - If ANY cell FAIL or FLAKY: matrix explicitly routes "PHASE DOES NOT EXIT — {remediation description}"; human signals accordingly
    - If all PASS (or zero-baseline happy path): `ls .planning/phases/01-nav-reliability/videos/final-*.mp4 2>/dev/null | wc -l` returns ≥ 11
    - Zero-baseline branch, if applicable, explicitly labeled in the matrix
  </acceptance_criteria>
  <resume-signal>Type "Final: all PASS — proceed to sign-off" or "Final: zero-baseline branch — proceed to sign-off" or "Final: {N} FAIL/FLAKY remain — PHASE DOES NOT EXIT".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write Phase 1 sign-off block in matrix (SC1-SC5 evidence table) + commit</name>
  <read_first>
    - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md (all accumulated sections + final Post-Wave-N)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §6 Goal-Backward SC-to-Evidence mapping
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §8 "What counts as validation passing" 8 items
    - .planning/ROADMAP.md Phase 1 5 Success Criteria (SC1-SC5)
  </read_first>
  <files>.planning/phases/01-nav-reliability/01-REPRO-MATRIX.md</files>
  <action>
    SKIP this task if Task 2 signalled "PHASE DOES NOT EXIT" — route back for remediation.

    Otherwise, append a `## Phase 1 Sign-Off` block to the end of `01-REPRO-MATRIX.md`. Content:

```
## Phase 1 Sign-Off

**Sign-off date:** {today's date}
**Final HEAD SHA:** {run `git rev-parse --short HEAD` and paste}
**Signed by (human):** {human name/handle}

### ROADMAP Success Criteria → Evidence

| SC | Criterion | Evidence |
|----|-----------|----------|
| SC1 | 0% repro across 45×2 matrix on iOS + Android physical | Post-Wave-N section: all 90 cells PASS; `Final Post-Wave-N summary` line |
| SC2 | Baseline recorded before fix | Plan 02 commit SHA {fill from 01-02-SUMMARY}; `git log --oneline -- .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` shows baseline commit pre-dates all App.tsx touch-handling edits |
| SC3 | Overlay return releases touch capture (HR-1..HR-5) | HR-1..HR-5 videos in videos/final-*.mp4 show PASS on both platforms |
| SC4 | Belt-and-suspenders inherited by new screens (pointerEvents + CONVENTIONS entry + OVERLAY_FLAGS) | `grep "pointerEvents: visible" App.tsx` matches; `grep "OVERLAY_FLAGS" App.tsx` matches; `grep "## Keep-alive and overlay screens" .planning/codebase/CONVENTIONS.md` matches |
| SC5 | Android hardware back unchanged OR 12-point checklist pass | {If Plan 05 Task 3 landed: "12-point Android back regression checklist in Post-Wave-2 section all PASS"; else: "No C2 refactor landed — Android back behavior unchanged; spot-check cells in Final Post-Wave-N confirm back behavior intact"} |

### RESEARCH §8 Validation-Passing Items

| # | Item | Verification Command | Status |
|---|------|---------------------|--------|
| 1 | Baseline section complete with ≥1 FAIL or doc-not-reproducible | `grep "Baseline (pre-fix)" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` | PASS |
| 2 | Post-Wave-N section complete, all 90 cells PASS, zero FAIL, zero FLAKY | Inspect `## Post-Wave-N (Final — post-strip)` | PASS |
| 3 | videos/ has ≥ 12 videos for Post-Wave-N sign-off | `ls .planning/phases/01-nav-reliability/videos/final-*.mp4 \| wc -l` ≥ 11 | PASS |
| 4 | All diagnostic tags stripped | `grep -n "\[NAV\]\|\[BACK\]\|\[C3\]\|\[C4\]" App.tsx src/components/BottomNavigator.tsx` returns zero | PASS |
| 5 | CONVENTIONS.md has new Keep-alive-and-overlay subsection | `grep -n "^## Keep-alive and overlay screens$" .planning/codebase/CONVENTIONS.md` returns 1 | PASS |
| 6 | App.tsx mainStackScreenStyle has pointerEvents | `grep -n "pointerEvents: visible ? 'auto' : 'none'" App.tsx` returns 1 | PASS |
| 7 | App.tsx overlay wrap sites show style-based pointerEvents | `grep -n "pointerEvents: isRenterListingsOpen" App.tsx` + `grep -c "pointerEvents: 'auto'" App.tsx` ≥ 2 | PASS |
| 8 | App.tsx has OVERLAY_FLAGS array matching D-10 shape | `grep -n "const OVERLAY_FLAGS = \[" App.tsx` returns 1 | PASS |

### NAV Requirement Coverage

- **NAV-01** — satisfied by SC1 (0% repro on 45×2 matrix)
- **NAV-02** — satisfied by SC3 (HR-1..HR-5 releases touch capture) + SC4 (pointerEvents pattern)
- **NAV-03** — satisfied by SC2 (baseline recorded before fix; git log ordering proves pre-dating)
```

    Fill the `{today's date}`, `{Final HEAD SHA}`, `{human name/handle}`, and `{Plan 02 commit SHA}` placeholders. The SC5 conditional expands based on whether Plan 05 Task 3 landed (check `grep -c backStateRef App.tsx` — ≥ 3 means the ref-pattern refactor is present, so the 12-point checklist variant applies).

    Commit with message: `docs(01): Phase 1 sign-off — NAV-01/NAV-02/NAV-03 satisfied`. Body: references SC1-SC5 evidence + RESEARCH §8 8-item validation-passing checklist.
  </action>
  <verify>
    <automated>grep -n "## Phase 1 Sign-Off" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md && grep -c "SC[1-5]" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "## Phase 1 Sign-Off" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` returns one match
    - `grep -c "| SC[1-5] |" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` returns ≥ 5 (SC1-SC5 rows present in evidence table)
    - `grep -c "| [1-8] |" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` includes the 8 RESEARCH §8 items
    - `grep "NAV-01\|NAV-02\|NAV-03" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md | wc -l` returns ≥ 3 (explicit NAV-ID coverage)
    - `grep "Sign-off date:" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` finds a filled date (not `TBD`)
    - `grep "Final HEAD SHA:" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` finds a filled SHA (not `TBD`)
    - All verification commands referenced in the sign-off block actually return their claimed results (spot-check 2 or 3 of them in the executor's output)
    - `git log --oneline -1 -- .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` returns the sign-off commit
  </acceptance_criteria>
  <done>Phase 1 sign-off committed. NAV-01 / NAV-02 / NAV-03 satisfied. Orchestrator may advance to Phase 2 planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan removes diagnostic instrumentation (strictly narrows observability surface) and appends documentation. No code behavior change on the ship-path — the strip reverses the observability changes from Plans 03/05 without touching the fix commits.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | T (Tampering) | Strip task accidentally removes a load-bearing line (e.g., pointerEvents, OVERLAY_FLAGS, C2 ref-pattern) | mitigate | Task 1 acceptance criteria explicitly verify all MUST-PRESERVE items post-strip via grep. TypeScript compilation also catches accidentally-orphaned references. If a grep returns zero where it should return a match, executor STOPS and reverts. |
| T-01-06-02 | I (Information Disclosure) | Final-matrix videos committed to repo | mitigate | Same posture as Plans 02/04: non-auth screens per D-06; human reviews any HR-6 Android video for auth-flow content before commit. |
| T-01-06-03 | I | Sign-off block contains HEAD SHA + human identity | accept | Non-sensitive — git commits already expose author identity and SHAs. No additional disclosure. |

No S/R/D/E applicable. Block on: high severity only per config default. No high-severity threats raised.
</threat_model>

<verification>
- Composite grep `grep -n "\[NAV\]\|\[BACK\]\|\[C3\]\|\[C4\]" App.tsx src/components/BottomNavigator.tsx` returns zero
- `grep -n "OVERLAY_FLAGS" App.tsx`, `grep -n "pointerEvents: visible" App.tsx`, and `grep -n "^## Keep-alive and overlay screens$" .planning/codebase/CONVENTIONS.md` all return their expected matches
- Post-Wave-N matrix section has 90 cells PASS / 0 FAIL / 0 FLAKY (or zero-baseline happy-path noted)
- videos/final-*.mp4 count ≥ 11
- Matrix contains `## Phase 1 Sign-Off` block with SC1-SC5 evidence + RESEARCH §8 8-item table + NAV-ID coverage
- All 3 plan commits (strip, final-matrix, sign-off) present in git log
</verification>

<success_criteria>
- NAV-01 satisfied: matrix Post-Wave-N all cells PASS on both platforms
- NAV-02 satisfied: HR-1..HR-5 videos demonstrate overlay-return releases touch capture
- NAV-03 satisfied: git log ordering proves baseline matrix commit pre-dates all App.tsx touch-handling edits
- CONTEXT D-02 satisfied: all diagnostic logs stripped
- D-04 rule honored throughout — no partial-success ship happened
- RESEARCH §6 SC1-SC5 evidence lines in place
- RESEARCH §8 items 1-8 all verifiable via the acceptance grep commands
- Phase 1 exits; orchestrator may plan Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-nav-reliability/01-06-SUMMARY.md`:
- Strip commit SHA + count of log lines removed (total across both files)
- List of preserved items verified intact (OVERLAY_FLAGS, pointerEvents sites, C2 ref-pattern if present, C3 fix if present, CONVENTIONS entry)
- Final matrix commit SHA
- Sign-off commit SHA
- Which candidates landed during the phase (C1 always; C2 / C3 / C4 conditional)
- Final video count in videos/
- Explicit phase-exit signal: "Phase 1 exits. NAV-01 / NAV-02 / NAV-03 satisfied per matrix sign-off."
</output>
