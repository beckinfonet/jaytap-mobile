---
phase: 08-release-and-store-submission
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md
autonomous: true
requirements: [REL-02]
requirements_addressed: [REL-02]
tags: [release, pre-archive, verify-only, qa-scaffold, version-baseline, xcode]

must_haves:
  truths:
    - "Xcode toolchain on the build machine is at 26.x (REL-04 SDK gate stays cleared per D-04 / PROJECT.md row 129)"
    - "ios/JayTap.xcodeproj/project.pbxproj CURRENT_PROJECT_VERSION = 21 (already at v1.0.4 target — no edit needed per D-02)"
    - "android/app/build.gradle versionCode 25 already at v1.0.4 target (no edit needed per D-02)"
    - "android/app/build.gradle versionName \"1.0.24\" already at v1.0.4 target (no edit needed per D-02)"
    - "08-QA-MATRIX.md exists with the bounded smoke-walk cell groups defined by D-07 (keyboard / nav / listing categories / role gating) and is ready for the Wave-2 walk"
    - "Working tree state at plan exit is documented (current SHA + uncommitted file list) so Plan 08-02 begins from a known baseline"
  artifacts:
    - path: ".planning/phases/08-release-and-store-submission/08-QA-MATRIX.md"
      provides: "Physical-device QA matrix scaffold for the D-07 bounded smoke walk (4 cell groups: keyboard, nav, listing categories, role gating) — ready for the Wave-2 walk to fill"
      min_lines: 120
      contains: "iPhone 15 Pro Max"
  key_links:
    - from: "08-QA-MATRIX.md"
      to: "Phase 1/2/6 matrix templates"
      via: "structural-precedent borrow (Device × Scenario rows, PASS/FAIL columns, evidence link column)"
      pattern: "01-REPRO-MATRIX|02-KEYBOARD-MATRIX|06-QA-MATRIX"
    - from: "Wave 0 verifications"
      to: "Plan 08-02 atomic bump"
      via: "baseline-then-bump ordering — Plan 08-02 must NOT run if any Wave-0 verify-only assertion failed"
      pattern: "wave: 0 -> wave: 1 (depends_on: [08-01])"
---

<objective>
Wave 0 of Phase 8 — pre-archive verification gate plus QA matrix scaffold creation.

Confirms (without editing) that the v1.0.4 baseline state captured in CONTEXT.md D-02/D-04/D-05 is intact on the working machine: Xcode 26.x toolchain, pbxproj `CURRENT_PROJECT_VERSION = 21`, gradle `versionCode 25` + `versionName "1.0.24"`. These are verify-only because they were already advanced from v1.0.3 in earlier work — there is nothing to bump for them. Creates `08-QA-MATRIX.md` from Phase 1/2/6 templates so Wave 2's regression walk has a structured place to record results.

Purpose: A failing Wave-0 assertion is the cheapest possible release-blocker — catches any drift (e.g., someone reset the gradle baseline, Xcode CLI tools point at an older SDK, pbxproj got reverted) before any binary work begins. The QA scaffold is created in Wave 0 (not Wave 2) so Plan 08-03's executor walks against an already-structured document.

Output: 1 new doc (`08-QA-MATRIX.md`), zero source edits, six grep-verified state assertions, one git status snapshot. No commit in this plan unless the QA scaffold itself needs committing — that's the only created file. Per `commit_docs: true` in `.planning/config.json`, the scaffold is committed atomically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-release-and-store-submission/08-CONTEXT.md
@.planning/phases/01-nav-reliability/01-REPRO-MATRIX.md
@.planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md
@.planning/phases/06-hospitality-rendering/06-QA-MATRIX.md

<interfaces>
<!-- The verify-only assertions from D-05 use literal grep patterns. Embedding the exact -->
<!-- state of the source-of-truth files at plan-creation time so the executor knows what -->
<!-- "PASS" looks like and detects drift if any value moved between plan creation and execution. -->

Current state of `package.json` line 3 (verified 2026-04-28):
```
  "version": "1.0.3",
```
Wave-0 expectation: still "1.0.3" (Plan 08-02 bumps to "1.0.4"). Plan 08-01 does NOT touch this file.

Current state of `ios/JayTap.xcodeproj/project.pbxproj` (verified 2026-04-28):
- Line 274: `CURRENT_PROJECT_VERSION = 21;` (Debug)
- Line 283: `MARKETING_VERSION = 1.0.3;` (Debug)
- Line 308: `CURRENT_PROJECT_VERSION = 21;` (Release)
- Line 316: `MARKETING_VERSION = 1.0.3;` (Release)
Wave-0 expectation: CURRENT_PROJECT_VERSION = 21 already at v1.0.4 target (D-02), MARKETING_VERSION = 1.0.3 still pre-bump (Plan 08-02 bumps to 1.0.4). Plan 08-01 does NOT touch this file.

Current state of `android/app/build.gradle` (verified 2026-04-28):
- Line 91: `        versionCode 25`
- Line 92: `        versionName "1.0.24"`
Wave-0 expectation: both already at v1.0.4 targets per D-02. Plan 08-01 does NOT touch this file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run six verify-only state assertions per D-05</name>
  <files>(no edits — read-only assertions)</files>
  <read_first>
    - .planning/phases/08-release-and-store-submission/08-CONTEXT.md (re-read D-02 + D-04 + D-05 to anchor the exact assertion strings)
    - package.json (confirm pre-bump baseline of "1.0.3" still holds)
    - ios/JayTap.xcodeproj/project.pbxproj (confirm CURRENT_PROJECT_VERSION = 21 + MARKETING_VERSION = 1.0.3 baseline)
    - android/app/build.gradle (confirm versionCode 25 + versionName "1.0.24" baseline)
  </read_first>
  <action>
    Run the following six commands from the repo root and record exit codes / output verbatim. The five Wave-0 commands are LITERAL strings from CONTEXT.md D-05 — do not paraphrase.

    Pre-bump baseline assertion (extra — not in D-05; confirms package.json hasn't already been bumped before Plan 08-02 runs):
    1. `grep '"version": "1.0.3"' package.json` — MUST exit 0 (executor halts and pings user if it fails: package.json was bumped out-of-order; investigate before running Plan 08-02).

    iOS pbxproj — CURRENT_PROJECT_VERSION already at target per D-02 / D-05:
    2. `grep 'CURRENT_PROJECT_VERSION = 21' ios/JayTap.xcodeproj/project.pbxproj` — MUST exit 0. Verify-only.

    iOS pbxproj — MARKETING_VERSION still at v1.0.3 baseline (Plan 08-02 bumps in Wave 1; Wave 0 must see pre-bump value):
    3. `grep -c 'MARKETING_VERSION = 1.0.3' ios/JayTap.xcodeproj/project.pbxproj` — MUST return 2 (Debug + Release configs). NOT D-05 spec; this is the pre-bump twin of D-05's post-bump `grep -c 'MARKETING_VERSION = 1.0.4' ... returns 2`.

    Android build.gradle — both values already at target per D-02 / D-05:
    4. `grep 'versionCode 25' android/app/build.gradle` — MUST exit 0. Verify-only.
    5. `grep 'versionName "1.0.24"' android/app/build.gradle` — MUST exit 0. Verify-only.

    Xcode toolchain check per D-04 / D-05 (REL-04 stays cleared):
    6. `xcodebuild -version | grep '^Xcode 26'` — MUST exit 0. One-shot toolchain assertion (NOT a migration task — the SDK gate was already cleared 2026-04-28 per PROJECT.md Key Decisions row 129).

    Record the literal output of `xcodebuild -version` (full multi-line output) for the SUMMARY so the verifier can confirm both Xcode major version + build number. Expected output line 1 starts `Xcode 26.` — fail and HALT if anything else (e.g., `Xcode 15.x`, `Xcode 16.x`) appears; the user must update Command Line Tools before any archive can ship.

    If ALL six commands pass: capture `git rev-parse HEAD` (the SHA the Wave-0 baseline is anchored to) + `git status --short` (confirm working tree state) into the SUMMARY.

    If ANY command fails: HALT the plan, do NOT proceed to Task 2, surface the failed assertion + actual output to the user, and request guidance. Do NOT auto-fix gradle/pbxproj drift in this plan (D-02 says verify-only); fixes go through a separate atomic commit if needed.
  </action>
  <verify>
    <automated>grep '"version": "1.0.3"' package.json && grep 'CURRENT_PROJECT_VERSION = 21' ios/JayTap.xcodeproj/project.pbxproj && [ "$(grep -c 'MARKETING_VERSION = 1.0.3' ios/JayTap.xcodeproj/project.pbxproj)" = "2" ] && grep 'versionCode 25' android/app/build.gradle && grep 'versionName "1.0.24"' android/app/build.gradle && xcodebuild -version | grep '^Xcode 26'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"version": "1.0.3"' package.json` exits 0 (pre-bump baseline holds)
    - `grep 'CURRENT_PROJECT_VERSION = 21' ios/JayTap.xcodeproj/project.pbxproj` exits 0 (D-05)
    - `grep -c 'MARKETING_VERSION = 1.0.3' ios/JayTap.xcodeproj/project.pbxproj` outputs `2` (pre-bump twin of D-05)
    - `grep 'versionCode 25' android/app/build.gradle` exits 0 (D-05)
    - `grep 'versionName "1.0.24"' android/app/build.gradle` exits 0 (D-05)
    - `xcodebuild -version | grep '^Xcode 26'` exits 0 (D-05 / D-04)
    - SUMMARY records `xcodebuild -version` full output (2 lines: `Xcode 26.x` + `Build version <id>`)
    - SUMMARY records `git rev-parse HEAD` (Wave-0 baseline SHA)
    - SUMMARY records `git status --short` (working tree state at Wave-0 entry)
  </acceptance_criteria>
  <done>All six commands exited 0; SUMMARY documents Xcode build, baseline SHA, and working-tree state; no source files were modified.</done>
</task>

<task type="auto">
  <name>Task 2: Create 08-QA-MATRIX.md scaffold (D-07 bounded smoke walk; Phase 1/2/6 template)</name>
  <files>.planning/phases/08-release-and-store-submission/08-QA-MATRIX.md</files>
  <read_first>
    - .planning/phases/08-release-and-store-submission/08-CONTEXT.md (D-06 + D-07 + D-09 — devices, scope, file location)
    - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md (template for nav transitions matrix — S1–S9 × T1–T5 cell shape)
    - .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md (template for keyboard matrix — 22-cell scaffold; D-07 says use as reference, NOT full re-walk)
    - .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md (template for multi-group matrix — frontmatter shape, sign-off section, evidence-link column)
  </read_first>
  <action>
    Create `.planning/phases/08-release-and-store-submission/08-QA-MATRIX.md` per D-09. The scaffold is filled to walked-state by Plan 08-03 (Wave 2); Plan 08-01 produces only the empty cells + structural skeleton.

    Frontmatter (YAML, mirror Phase 6 06-QA-MATRIX.md shape):
    ```yaml
    ---
    phase: 08-release-and-store-submission
    plan: 01
    type: verification-matrix
    status: scaffold
    devices:
      ios:
        model: iPhone 15 Pro Max
        os: iOS 26.x
        build: Fabric / Release
      android:
        model: Moto G XT2513V
        os: Android 16
        build: Fabric / Release
    accounts:
      admin: beckprograms@gmail.com
      non_admin: <test-account>
    requirements_addressed: [REL-05]
    testing_bar: M1 manual physical-device QA per CLAUDE.md
    walk_scope: bounded smoke walk per D-07 (NOT a full re-walk of every Phase 1–6 cell)
    ---
    ```

    Body sections (in order):

    1. **Header** — `# Phase 8: Release Regression QA Matrix (Physical Devices)`. Restate testing bar, devices, accounts (admin allowlist `beckprograms@gmail.com` + at least one non-admin email), and the bounded-smoke-walk principle from D-07 (this is NOT a full Phase 1–6 re-walk). Reference `nav-overlay-hides-bottom-nav.md` memory note: overlays hide the bottom tab bar by design, so cells with overlays open × tab targets render `—` (N/A) intentionally — DO NOT mark FAIL.

    2. **Cell markers** — `✅ PASS · ❌ FAIL · — N/A · ☐ Pending`.

    3. **Test environment** — Two-row table: device | OS | build variant | accounts. Leave a "git SHA at walk start" field blank for Plan 08-03 to fill.

    4. **Matrix 1 — Keyboard handling (D-07 first bullet, ~10 input-bearing screens × 2 devices = ~20 cells).** One row per screen. Columns: Screen | Steps | iOS PASS/FAIL | iOS evidence | Android PASS/FAIL | Android evidence. Screens (LIST EXPLICITLY, do not say "follow Phase 2"):
       - LoginScreen — email + password TextInputs, focus each
       - SignupScreen — email, password, confirm-password
       - ForgotPasswordScreen — email
       - ResetPasswordScreen — code, new-password, confirm
       - ChatScreen (list) — search/filter input if present
       - ChatThreadScreen — message compose input at bottom
       - ChatComposeScreen — recipient + message inputs
       - AccountSettingsScreen — display name, phone, email-display, delete-confirm flow
       - CreateListingScreen (create branch, transactionType=Rent, propertyType=Apartment) — Title, Description, Address, City, District, Price, Currency selector, Bedrooms, Bathrooms, AreaSqm, AvailableDate
       - CreateListingScreen (edit branch — pick any existing listing from RenterListingsScreen) — same fields, with rehydrated values
       - ScheduleViewingScreen — message + date input
       Acceptance per cell: focused TextInput stays visible above the keyboard on Fabric (no per-screen `keyboardVerticalOffset` magic numbers — D-07 explicit). Reference: Phase 2's 22-cell matrix is the universe; these 11 screens × 2 devices = 22 cells, identical scope to Phase 2 but walked once-through (not the full Phase-2 ScrollView-fully-bottom + tap-each-input cell breakdown).

    5. **Matrix 2 — Bottom-nav transitions (D-07 second bullet, spot-walk).** Two sub-tables:
       - **Matrix 2a (main-stack → tab):** rows = main-stack starting states (Home, Favorites, Chat, Profile, Appointments) × columns = target tabs (Home, Favorites, Add/CreateListing, Chat, Profile). 5 × 5 = 25 cells per device; mark `—` for the same-tab diagonal (S=T is a no-op tap, not a transition). Net 20 cells × 2 devices = 40 cells.
       - **Matrix 2b (overlay-close → tab):** rows = overlay starting states (PropertyDetails, CreateListing, RenterListings, Tour3D) × steps = "close the overlay (back-button or close-affordance), then tap each tab in turn". 4 × 5 = 20 cells per device. NOTE: cells where the overlay is still open and you try to tap a tab are intentionally `— N/A` per the `nav-overlay-hides-bottom-nav.md` memory — overlays hide the bottom-nav by design; only post-close transitions are testable.
       Reference: Phase 1's 01-REPRO-MATRIX.md S1–S9 × T1–T5 is the template, but D-07 says "spot-walk" — collapse to the canonical trap sequences from Phase 1's RESEARCH §2.1 (Home → PropertyDetails → back → tap Profile + Home → CreateListing → back → tap Chat) plus the 5×5 main-stack matrix.

    6. **Matrix 3 — Listing categories (D-07 third bullet, 9 cells × 2 devices = 18 cells).** One row per (category × action). Categories: Residential, Commercial, Hospitality. Actions: Create, Edit, View. 3 × 3 = 9 cells per device. Acceptance per cell:
       - Create: form opens with the correct field set per Phase 5 D-13 (Residential → bedrooms/bathrooms/areaSqm; Commercial → areaSqm + sub-type, no bedrooms/bathrooms; Hospitality → rooms/bathrooms/maxGuests/amenities, NO price).
       - Edit: rehydrate initializes into the listing's category with field set intact.
       - View: PropertyDetailsScreen renders correctly per category — Residential/Commercial show price block + specs row; Hospitality omits price block, promotes 3D tour above gallery, shows sticky 3-button contact bar (Phase 6 D-13/D-14/D-15/D-16/D-23 + HI-01 fix).
       Plus: confirm `HospitalitySection` strip renders on HomeScreen + FavoritesScreen + RenterListingsScreen + OwnerListingsScreen and is HIDDEN when the user filter excludes Hostel/Hotel (Phase 6 D-01 first-return guard).

    7. **Matrix 4 — Role gating (D-07 fourth bullet, 4 cells × 2 devices = 8 cells).** Two sub-rows per device, one per account type:
       - **Admin (`beckprograms@gmail.com`):** open CreateListingScreen → confirm Matterport tour URL field is visible + panoramic image URL field is visible → save a no-op edit on an existing admin-touchable listing → confirm save succeeds. PropertyDetailsScreen → confirm verification edit affordance is visible.
       - **Non-admin (any other email):** repeat → confirm Matterport URL field is HIDDEN (D-08 hide-entirely from Phase 3) → confirm panoramic URL field is HIDDEN → confirm verification edit affordance is HIDDEN. Attempting raw HTTP to admin endpoint is OUT OF SCOPE per D-07 (GATE-05 accepted-risk in Phase 3 D-22 Path B; not re-litigated in Phase 8).

    8. **Optional — Matrix 5 (planner discretion per CONTEXT.md `<specifics>`):** "Universal Link smoke" — open `https://www.moveinplatform.com/property/6987ab8b698816d4875ec37a` from Mail/Messages on iOS → JayTap opens at the listing → confirm. 1 cell, iOS only (Android App Links is out-of-scope per D-13 / Deferred). Mark this section as "OPTIONAL — execute if convenient; do NOT block phase exit on it."

    9. **Defect-handling instructions** — cell-level instruction restating D-08: any FAIL is fixed in-phase via atomic commit; affected matrix scenario re-walks on both devices; if a defect is out-of-scope of the smoke walk, file under `999.x` backlog UNLESS user flags release-blocking. State explicitly: defects do NOT auto-bump the build number — the conditional `CURRENT_PROJECT_VERSION = 22` bump is only triggered by TestFlight history collision per D-03 (handled in Plan 08-05).

    10. **Sign-off section** — fields to fill at walk close: `git SHA at walk start`, `git SHA at walk end` (may differ if D-08 fixes commit), defects fixed (commit SHAs + scenario IDs), defects deferred (with `999.x` IDs + rationale), final disposition (`APPROVED — phase exit unblocked` or `BLOCKED — list reason`).

    Total cell count: ~91 cells (22 keyboard + 40 nav-main + 20 nav-overlay-close + 18 listing-categories + 8 role-gating + 1 optional UL). All cells start `☐ Pending` — Plan 08-03 fills them.

    Write via the Write tool (not heredoc). Per `commit_docs: true`, commit the new file atomically: `docs(08-01): create QA matrix scaffold for v1.0.4 release smoke walk`.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md && [ "$(wc -l < .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md)" -ge 120 ] && grep -q 'iPhone 15 Pro Max' .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md && grep -q 'Moto G XT2513V' .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md && grep -q 'beckprograms@gmail.com' .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md && grep -qE 'Matrix 1.*[Kk]eyboard' .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md && grep -qE 'Matrix 2.*[Nn]av' .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md && grep -qE 'Matrix 3.*[Cc]ateg' .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md && grep -qE 'Matrix 4.*[Rr]ole' .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/08-release-and-store-submission/08-QA-MATRIX.md` exists
    - File is ≥ 120 lines
    - File contains literal `iPhone 15 Pro Max` (D-06 device name)
    - File contains literal `Moto G XT2513V` (D-06 device name)
    - File contains literal `beckprograms@gmail.com` (admin allowlist account)
    - File contains a Matrix 1 keyboard section
    - File contains a Matrix 2 nav section
    - File contains a Matrix 3 categories section
    - File contains a Matrix 4 role-gating section
    - File commits as a single atomic commit with message `docs(08-01): create QA matrix scaffold for v1.0.4 release smoke walk` (or equivalent — `08-01` token must appear)
    - Frontmatter `status: scaffold` (not `complete` or `walked` — Plan 08-03 advances state)
    - Zero source files in `src/`, `ios/`, `android/`, or `package.json` are modified by this task
  </acceptance_criteria>
  <done>QA matrix scaffold exists with all four mandatory matrices defined, devices + accounts populated, all cells in `☐ Pending` state, ready for the Plan 08-03 walk; committed atomically with `08-01` in the message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced this plan) | Plan 08-01 introduces no new attack surface — verify-only assertions on the local working tree + creation of an internal documentation file. v1.0.3 declarations inherited per D-13. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Local working tree (pbxproj/gradle/package.json drift between plan creation and execution) | mitigate | Six grep assertions in Task 1 catch any out-of-band edit before Plan 08-02 runs; on any FAIL the plan halts and prompts the user. |
| T-08-02 | (no further threats) | n/a | accept | No new attack surface; v1.0.3 declarations inherited per D-13 (descope record). |
</threat_model>

<verification>
Phase-level (cross-cutting checks for this plan only):

1. `git diff --name-only HEAD~1` after the QA-scaffold commit shows exactly one new path: `.planning/phases/08-release-and-store-submission/08-QA-MATRIX.md` (no source-tree drift).
2. `grep -c 'MARKETING_VERSION = 1.0.3' ios/JayTap.xcodeproj/project.pbxproj` still returns 2 (Plan 08-01 did NOT touch pbxproj).
3. `grep '"version": "1.0.3"' package.json` still exits 0 (Plan 08-01 did NOT touch package.json).
4. `./scripts/check-role-grep.sh` exits 0 (Phase 3 D-14 invariant preserved — Plan 08-01 cannot regress role-gating because it touches no source).
5. `./scripts/check-land-removed.sh` exits 0 (Phase 4 invariant preserved).
6. `./scripts/check-i18n-parity.sh` exits 0 (Phase 4 invariant preserved).
</verification>

<success_criteria>
1. All six D-05 verify-only assertions exited 0 in Task 1 (or the plan halted cleanly with a documented FAIL — no silent skip).
2. `08-QA-MATRIX.md` exists with the 4 mandatory matrices (keyboard, nav, listing categories, role gating) plus optional UL section, all cells in `☐ Pending` state.
3. Frontmatter records both devices, both account types, and `status: scaffold`.
4. Atomic commit landed for the scaffold (per `commit_docs: true`).
5. Working-tree baseline (SHA + `git status --short`) recorded in SUMMARY for Plan 08-02 to anchor against.
6. Phase-3/4 CI gates exit 0 (Plan 08-01 changes nothing they care about; this is a regression-floor check).
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-and-store-submission/08-01-SUMMARY.md` documenting:
- Six verify-only assertion results (literal command + exit code per row)
- `xcodebuild -version` full output (Xcode major + build id)
- Wave-0 baseline SHA
- `git status --short` snapshot
- Path + line count of the QA scaffold
- Cell counts per matrix group (so Plan 08-03's executor can sanity-check the scaffold survived to walk-time)
</output>
