---
phase: 08-release-and-store-submission
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md
autonomous: true
requirements: [REL-06]
requirements_addressed: [REL-06]
tags: [release, release-notes, i18n, en-ru-parity, asc, play-console, submission-artifact]

must_haves:
  truths:
    - "08-RELEASE-NOTES.md exists with bilingual EN + RU release notes per D-10 (PROJECT.md i18n constraint applies to user-facing notes)"
    - "Both EN and RU sections cover the same content in the same order: What's new (Hospitality + tour-first cards) -> Improvements (keyboard + nav + listing form) -> Bug fixes (defects fixed in Plan 08-03 D-08 loop, if any)"
    - "Release notes total length per language fits ASC + Play Console limits (ASC: 4000 chars; Play Console: 500 chars per locale — Play is the binding limit)"
    - "Notes describe what users SEE, not internal implementation language (e.g., \"Improved navigation reliability\" — NOT \"fixed pointerEvents on keep-alive screens\")"
    - "08-03-SUMMARY.md was read so any defect-fix commits from D-08 are reflected in the Bug fixes section (not silently omitted)"
    - "Notes are draft artifacts per D-11 — pasted into ASC + Play Console at submission (Plan 08-05); not preserved as a permanent in-app i18n key"
  artifacts:
    - path: ".planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md"
      provides: "Bilingual EN + RU release notes for ASC + Play Console paste at submission time"
      min_lines: 40
      contains: "## EN"
  key_links:
    - from: "08-03-SUMMARY.md (defects fixed in regression loop)"
      to: "08-RELEASE-NOTES.md Bug fixes section"
      via: "user-facing translation of internal fix-commit subjects"
      pattern: "Bug fixes|Fixes|Исправления"
    - from: "08-RELEASE-NOTES.md"
      to: "App Store Connect + Play Console paste fields (Plan 08-05)"
      via: "submission artifact (D-11) — copied at upload time, not auto-deployed"
      pattern: "(EN|RU)"
---

<objective>
Wave 3 of Phase 8 — author bilingual EN + RU release notes per D-10 / D-11 / REL-06.

Notes are draft artifacts: written to `08-RELEASE-NOTES.md` so the user has one place to review and edit before pasting into App Store Connect (per-locale notes) and Google Play Console (per-language listings) at submission time in Plan 08-05. Per D-11 they are NOT preserved as permanent repo artifacts unless the user explicitly requests one — the file lives in `.planning/phases/08-release-and-store-submission/` as a submission-cycle artifact.

Content order per D-10 (mandatory ordering; same in both languages):
1. **What's new:** Hospitality category (Hostel + Hotel) with tour-first cards, 3D tour discovery on listings, contact-driven workflow.
2. **Improvements:** Universal keyboard handling on all input screens; reliable bottom navigation across all overlays; redesigned listing form with three categories (Residential / Commercial / Hospitality).
3. **Bug fixes:** anything fixed during regression QA in Plan 08-03 D-08 loop; if zero defects surfaced, omit the section in EN AND RU (do not pad with "Various improvements" — D-10 explicit).

Purpose: Release notes are the only user-facing communication for the v1.0.4 ship. Bishkek's user base is bilingual EN+RU; the project's M1 i18n constraint (`Record<TranslationKeys,string>` parity) applies to release notes by extension — RU-only or EN-only release notes ship as a bug per PROJECT.md Constraints. The draft file exists so the user can edit/refine before submission rather than improvising in the ASC web UI.

Output: 1 new doc file, draft EN + RU release notes, no source edits, atomic commit per `commit_docs: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-release-and-store-submission/08-CONTEXT.md
@.planning/phases/08-release-and-store-submission/08-03-SUMMARY.md

<interfaces>
<!-- Input feature inventory for the "What's new" + "Improvements" sections — drawn from -->
<!-- Phase 1–6 deliverables. Same content in both EN and RU per D-10. -->

What's new (Phase 6 deliverables):
- HospitalitySection horizontal strip on Home / Favorites / Renter Listings / Owner Listings (HOSP-01)
- HospitalityCard tour-first variant — 3D tour thumbnail emphasis, no price chip, Hostel/Hotel badge (HOSP-03)
- Tour-first PropertyDetailsScreen for Hostel/Hotel listings — 3D tour above gallery, panoramic media, sticky 3-button contact bar (WhatsApp / Telegram / phone) (HOSP-04)
- 12-amenity taxonomy multi-select on listing form + readable chip list on details (HOSP-05)

Improvements:
- Universal keyboard handling — text inputs no longer hidden behind the keyboard on any screen, iOS or Android (KBD-01..04, Phase 2)
- Reliable bottom navigation — tabs respond from every screen and after closing every overlay (NAV-01..03, Phase 1)
- Redesigned listing form — three categories (Residential / Commercial / Hospitality), category-branched required fields, three-way Hostel/Hotel addition, Land removal (FORM-01..09, Phase 4 + Phase 5)

Bug fixes (read from Plan 08-03 SUMMARY):
- Whatever D-08 surfaced. If 08-03 reported zero defects, OMIT the Bug fixes heading entirely in BOTH languages.

Length budgets:
- ASC release notes: 4000 chars per locale
- Play Console "Recent changes" / release notes: 500 chars per locale (THIS IS THE BINDING LIMIT — write to ~450 chars per language to leave headroom)

RU translation reference (project glossary from `src/locales/ru.ts` / Phase 4–6 translation work):
- "Hospitality" ≈ "Гостиничные" (project uses this in `src/locales/ru.ts` Phase 6)
- "Hostel" → "Хостел" / "Hotel" → "Отель" (Phase 4 i18n keys)
- "3D tour" → "3D-тур"
- "Listing" → "Объявление"
- "Bottom navigation" → "Нижнее меню" / "Навигация"
- "Keyboard" → "Клавиатура"
- "What's new" → "Что нового"
- "Improvements" → "Улучшения"
- "Bug fixes" → "Исправления"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Plan 08-03 SUMMARY + check whether D-08 fix commits exist</name>
  <files>(read-only)</files>
  <read_first>
    - .planning/phases/08-release-and-store-submission/08-03-SUMMARY.md (defects-fixed list — drives the Bug fixes section)
    - .planning/phases/08-release-and-store-submission/08-CONTEXT.md (D-10 + D-11 — re-anchor content order + ASC/Play paste workflow)
    - .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md (Sign-off section — confirms walk disposition and surfaces defect cell IDs for translation)
  </read_first>
  <action>
    Build the input list for Plan 08-04 Task 2 by extracting:

    1. **Defects fixed in-phase via D-08:** read `08-03-SUMMARY.md` "Defects fixed" section. Each entry is a (commit SHA, cell ID, one-line subject). Translate each into a single user-facing bullet, e.g.:
       - Internal: `fix(08-03): tap targets on Hospitality contact bar enlarged for Android Fabric` → user-facing EN: "Improved tap targets on hostel/hotel contact actions on Android."
       - Internal: `fix(08-03): keyboard offset in ChatThread on Android` → user-facing EN: "Fixed an issue where the chat input could be hidden by the keyboard on some Android devices."

       For each user-facing EN bullet, draft the RU equivalent using the `<interfaces>` glossary above. If a defect is too internal (e.g., "fixed grep regex in CI gate"), OMIT — release notes are user-facing only.

    2. **Confirm zero-defect path:** if `08-03-SUMMARY.md` reports 0 defects fixed, the Bug fixes section is OMITTED entirely (heading + bullets). D-10 explicitly forbids padding with "Various improvements" or similar.

    3. **Confirm walk disposition:** read `08-QA-MATRIX.md` Sign-off section. Disposition MUST be `APPROVED`. If it is `BLOCKED` for any reason, HALT this plan and surface to user — release notes for a blocked walk are premature.

    4. **Stage no edits.** Task 1 is fact-gathering for Task 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-release-and-store-submission/08-03-SUMMARY.md && test -f .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md && grep -q 'APPROVED' .planning/phases/08-release-and-store-submission/08-QA-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - `08-03-SUMMARY.md` exists (Plan 08-03 closed)
    - `08-QA-MATRIX.md` Sign-off contains literal `APPROVED` (walk closed, not blocked)
    - Defects-fixed list extracted (zero or more entries) — recorded in plan execution notes for Task 2 to consume
    - For each defect entry, draft user-facing EN + RU bullets prepared for Task 2
  </acceptance_criteria>
  <done>Defects list extracted; walk disposition confirmed APPROVED; Task 2 has its input list.</done>
</task>

<task type="auto">
  <name>Task 2: Create 08-RELEASE-NOTES.md (bilingual EN + RU per D-10 / D-11)</name>
  <files>.planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md</files>
  <read_first>
    - .planning/phases/08-release-and-store-submission/08-CONTEXT.md (D-10 + D-11 — content order, length budgets, paste-only / not-permanent disposition)
    - .planning/phases/08-release-and-store-submission/08-03-SUMMARY.md (defects list from Task 1)
    - src/locales/en.ts (project EN voice — read 30 lines as reference for tone)
    - src/locales/ru.ts (project RU translations of Hospitality/keyboard/nav terms — see `<interfaces>` glossary)
  </read_first>
  <action>
    Create `.planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md` with frontmatter + bilingual EN + RU body. Write via Write tool.

    **Frontmatter:**
    ```yaml
    ---
    phase: 08-release-and-store-submission
    plan: 04
    type: release-notes
    version: 1.0.4
    languages: [en, ru]
    submission_targets: [App Store Connect, Google Play Console]
    binding_length_limit: 500 chars per locale (Play Console — write to ~450 to leave headroom)
    asc_length_limit: 4000 chars per locale
    paste_only: true  # D-11 — NOT preserved as a permanent repo artifact
    walked_against_sha: <SHA from 08-03-SUMMARY 'walk_completed_sha'>
    ---
    ```

    **Body — H1 + intro:**
    ```
    # JayTap v1.0.4 — Release Notes

    Bilingual draft for App Store Connect (per-locale notes) + Google Play Console (per-language "Recent changes" / "Whats new" fields). Paste at submission time in Plan 08-05; do NOT commit changes back from ASC/Play UI into this file (per D-11, this is a paste-only artifact).
    ```

    **Body — `## EN` section:**
    Draft to ~400-450 chars total (Play Console binding limit + headroom). Project tone is plain, factual, user-focused (no marketing fluff, no emoji unless the user later adds them). Same content order as RU.

    Skeleton (executor fills concrete bullets from `<interfaces>` + Task 1 defect list):

    ```markdown
    ## EN

    ### What's new

    - Browse hostels and hotels in a new Hospitality section on Home, Favorites, and your listings
    - Tour-first listing cards put 3D tours and panoramic photos front and center
    - Sticky contact bar on hostel/hotel listings — WhatsApp, Telegram, or phone in one tap

    ### Improvements

    - Keyboard no longer covers text inputs on any screen
    - Bottom navigation responds reliably from every screen and after closing any overlay
    - Redesigned listing form with three categories: Residential, Commercial, and Hospitality

    ### Bug fixes

    - <One bullet per defect from 08-03-SUMMARY, user-facing voice; OMIT this whole section if zero defects>
    ```

    **Body — `## RU` section:**
    Same content, translated. Use the project glossary in `<interfaces>`. Project precedent (Phase 4 + Phase 6) uses formal Russian (вы-form), not informal (ты-form).

    ```markdown
    ## RU

    ### Что нового

    - Просматривайте хостелы и отели в новом разделе «Гостиничные» на главной, в избранном и в ваших объявлениях
    - Карточки объявлений с акцентом на 3D-туры и панорамные фотографии
    - Закреплённая панель контактов на объявлениях хостелов и отелей — WhatsApp, Telegram или телефон в одно касание

    ### Улучшения

    - Клавиатура больше не закрывает поля ввода ни на одном экране
    - Нижнее меню стабильно работает на всех экранах и после закрытия любых наложений
    - Обновлённая форма объявления с тремя категориями: Жильё, Коммерческое и Гостиничные

    ### Исправления

    - <По одному пункту на каждый дефект из 08-03-SUMMARY, в пользовательской формулировке; ОПУСТИТЬ весь раздел при отсутствии исправлений>
    ```

    **Length check:** after drafting, run `wc -c` on the EN body section content (everything between `## EN` and `## RU`, headings included) and on the RU body section content (everything from `## RU` to EOF). Each MUST be ≤ 500 chars to fit Play Console; aim for ≤ 450. ASC's 4000-char limit is comfortably non-binding. If either language is over budget, trim by collapsing bullets — Phase 4 + Phase 6 features are the priority; bug-fix bullets are first to trim if needed.

    **Submission instructions block — append at EOF:**
    ```markdown
    ---

    ## Submission Instructions (for Plan 08-05)

    **App Store Connect:**
    1. App Store → JayTap → Prepare for Submission → "What's New in This Version"
    2. Default language: English (paste `## EN` body)
    3. Add localization: Russian (paste `## RU` body)

    **Google Play Console:**
    1. Production → Releases → Create new release
    2. Release notes: tap "Edit release notes per language"
    3. en-US: paste `## EN` body
    4. ru-RU: paste `## RU` body

    Do NOT include the `### What's new` / `### Improvements` / `### Bug fixes` headings if the destination field renders Markdown literally (Play Console does not render Markdown — strip headings to plain text bullets if needed). ASC does NOT render Markdown either — same rule.
    ```

    **Post-write atomic commit per `commit_docs: true`:**
    Stage exactly one file: `git add .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md`. Commit message: `docs(08-04): draft v1.0.4 release notes (EN + RU per D-10)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md && grep -q '## EN' .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md && grep -q '## RU' .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md && grep -qE 'What.s new|Whats new' .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md && grep -q 'Что нового' .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md && grep -q 'Improvements' .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md && grep -q 'Улучшения' .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md && grep -q '1.0.4' .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md && [ "$(wc -l < .planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md)" -ge 40 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md` exists
    - File ≥ 40 lines
    - File contains literal `## EN` heading
    - File contains literal `## RU` heading
    - File contains `What's new` (or `Whats new`) heading in EN section
    - File contains literal `Что нового` heading in RU section
    - File contains literal `Improvements` heading in EN section
    - File contains literal `Улучшения` heading in RU section
    - File contains `1.0.4` (version reference)
    - EN body section between `## EN` and `## RU` is ≤ 500 chars (Play Console binding limit) — verifier may run `awk '/## EN/,/## RU/' file | wc -c` and confirm output ≤ 500 (or measure the user-facing notes only, excluding the heading)
    - RU body section from `## RU` to before the `## Submission Instructions` block is ≤ 500 chars
    - File contains a "Submission Instructions" block referencing both ASC and Play Console
    - Frontmatter `version: 1.0.4` and `languages: [en, ru]` present
    - Atomic commit lands with `08-04` token in message; only the new file is in the commit (`git diff --name-only HEAD~1` shows exactly 1 path)
    - If `08-03-SUMMARY.md` reported 0 defects, the `### Bug fixes` / `### Исправления` sections are entirely OMITTED from both languages (no empty heading, no "Various improvements" filler)
    - If `08-03-SUMMARY.md` reported N>0 defects, both EN and RU `Bug fixes` / `Исправления` sections contain ≥ 1 bullet each
  </acceptance_criteria>
  <done>EN + RU draft release notes exist, fit Play Console's 500-char-per-locale limit, follow D-10 content order, include submission instructions for Plan 08-05; atomic commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → ASC/Play submission | Release notes are user-facing copy that ships in store listings; trust boundary is the human author. No data flow / no executable code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information Disclosure | Release notes accidentally describe internal-only details (e.g., "fixed pointerEvents on keep-alive screens" — leaks implementation language to App Store reviewers and users) | mitigate | Task 1 step 1 explicitly translates internal commit subjects to user-facing voice; "OMIT — release notes are user-facing only" rule in Task 1. |
| T-08-10 | (no further threats) | n/a | accept | No new attack surface; v1.0.3 declarations inherited per D-13. Release notes are paste-only artifacts (D-11) — no auto-deploy path. |
</threat_model>

<verification>
Phase-level (cross-cutting checks for this plan only):

1. `git diff --name-only HEAD~1` returns exactly `.planning/phases/08-release-and-store-submission/08-RELEASE-NOTES.md`.
2. Both `## EN` and `## RU` headings exist with ordered content matching D-10.
3. Length per language ≤ 500 chars (Play Console binding limit).
4. Submission Instructions block referencing both ASC and Play Console paste workflows.
5. Frontmatter `paste_only: true` flagged per D-11 (executor's reminder this is a draft, not a deploy artifact).
</verification>

<success_criteria>
1. Bilingual draft EN + RU release notes drafted to D-10 order.
2. Each language ≤ 500 chars to fit Play Console.
3. Bug fixes section reflects 08-03 defects exactly (zero entries → section omitted; N entries → N bullets per language).
4. Atomic commit lands with `08-04` in message.
5. Submission Instructions block makes paste workflow unambiguous for Plan 08-05.
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-and-store-submission/08-04-SUMMARY.md` documenting:
- Final EN char count + RU char count (both ≤ 500 confirmed)
- Bug-fix bullet count per language (EN, RU)
- Commit SHA + message
- Path to release notes file
- Note for Plan 08-05: paste destination per store
</output>
