---
type: requirements-archive
milestone: v3.0
name: M3 "Contextual Forms"
status: shipped
shipped_date: 2026-05-11
total_v1_requirements: 38
strictly_checked: 38
partial: 0
stale_bookkeeping: 0
material_carry_forward: none (all 38 v1 requirements code-satisfied; 7 M4 items are net-new backlog, not carry-forward gaps — see v3.0-ROADMAP.md § "Known Gaps at Close")
archived_at: 2026-05-11
---

# M3 "Contextual Forms" — v3.0 Requirements (Archived)

**Archived:** 2026-05-11 at milestone close. All 38 v1 requirements fully delivered (38 strictly `[x]`). No stale `[~]` or `[ ]` bookkeeping drift this milestone — every requirement was flipped to `[x]` with phase reference at milestone close after verifying against ROADMAP phase summaries + VERIFICATION.md + SUMMARY.md frontmatter. 2 device walks (CARRY-01 banner latency + CARRY-02 live Atlas uid match) folded under Plan 05-05 mass-disposition; relisted as opt-in M4 items 4 + 5 in `v3.0-ROADMAP.md § Known Gaps at Close` if regressions surface.

The text below is the working REQUIREMENTS.md as it stood at M3 close.

---

# JayTap — M3 v3.0 "Contextual Forms" Requirements

**Milestone:** M3 v3.0 "Contextual Forms"
**Started:** 2026-05-05
**Builds on:** M2 v2.0 "Roles & Moderation" (shipped 2026-05-05; archived under `.planning/milestones/v2.0-*`)
**Anchor SPEC:** `.planning/phases/999.1-contextual-listing-flow-m3-anchor/SPEC.md` (Version 2)

**Goal:** Replace the current single-screen `CreateListingScreen` with a 6-step contextual listing creation flow (every screen and every field determined by previous answers) and invert the media flow so admin/mod uploads photos/videos/3D-tour after metadata submission. Closes 2 M2 carry-forward UX/data-integrity bugs (ROLE-11 frontend popup-recovery + Phase 4.5 landlord-app uid-mismatch).

**Hard rules carried forward (do NOT introduce):**
- No Firebase SDK in either repo (RN client OR backend) — REPO RULE per memory `no-firebase-sdk.md`. Backend continues using `jose` for JWKS verification.
- No `react-navigation` migration — custom `App.tsx` state machine stays. New 6-step flow mounts as overlay or screen-stack within the existing pattern.
- No Firebase custom claims as role source — MongoDB `userType` remains authoritative.
- No new authentication providers (OAuth, magic link, 2FA) — out of scope.
- M1's 3-category 9-type taxonomy preserved — CLAUDE.md guard. SPEC's flat 5-type list reframed (apartment + house → Residential; office + commercial → Commercial; hotel + hostel → Hospitality, kept split per tour-first UI).
- M2's status enum preserved (`pending | live | rejected | archived`). SPEC's `draft | pending_moderation | published | rejected` reframed 1:1 cosmetically only.
- EN+RU bilingual parity for every new UI string (CI gate enforces).
- Manual physical-device QA on iPhone 15 Pro Max + Moto G XT2513V.

**Geographic scope:** KG launch market (Bishkek). KZ + UZ expansion remains M4+ — KZT + UZS currencies deferred.

---

## v1 (M3) Requirements

### Contextual flow (Phase 2) — 6-step UI

The user-facing 6-step listing creation flow. Replaces the existing single-screen `CreateListingScreen` atomically.

- [x] **FLOW-01** 6-step container component with header progress indicator (Step N of 6), Back / Next navigation, and per-step validation gating advance. Replaces `CreateListingScreen.tsx`. Mounts via existing `App.tsx` overlay pattern.
- [x] **FLOW-02** Step 1A — Deal Type single-select chips: Sale (`sale`) / Long-term rent (`rent_long`) / Daily rent (`rent_daily`). Required to advance.
- [x] **FLOW-03** Step 1B — Property Type single-select chips. Reframed onto M1's 9-type 3-category taxonomy (Residential: apartment + house; Commercial: office + commercial; Hospitality: hotel + hostel). All combinations of deal type × property type allowed. Required to advance.
- [x] **FLOW-04** Step 2 — Location: city autocomplete + district selector (district options dependent on city). Required to advance. *(Backend dictionary shipped in Phase 2 Plan 01, 2026-05-06: City + District Mongoose models, GET/POST /api/locations/* with HF-03 anti-spoof + landlord-capability gate + dedupe semantics, mod approve/reject/queue routes, seed-locations-m3.js with 11 KG/KZ/UZ cities + 19 Bishkek districts. Client autocomplete UI ships in Plan 02-03.)*
- [x] **FLOW-05** Step 2 — Map pin via existing map abstraction (no 2GIS bridge — out of scope). Coordinates `{lat, lng}` required to advance.
- [x] **FLOW-06** Step 2 — Conditional exact-address toggle. Toggle hidden when `propertyType ∈ {hotel, hostel}` (forced true). For all other types, default false → display approximate 200–300m radius.
- [x] **FLOW-07** Step 3 — Always-shown: area (m²) + price + currency chip (KGS / USD / EUR). All three required.
- [x] **FLOW-08** Step 3 — Conditional sub-fields per property type (per SPEC §"Conditional sub-fields"): apartment + house → rooms (1/2/3/4+); office + commercial → rooms + bathroom (private/none/shared) + kitchen (private/none/shared); hotel + hostel → hotelRooms (1/2/3/4+) + hotelClass (economy/standard/comfort/premium).
- [x] **FLOW-09** Step 4 — Always shown: condition (rough/whitebox/good/euro) + furnished (boolean). Both required, including for hotel/hostel.
- [x] **FLOW-10** Step 5 — Title + Description (long-text). Both required.
- [x] **FLOW-11** Step 6 — Deal Conditions gated by deal type per SPEC §6 matrix: Sale → bargain + optional deposit; Long-term rent → bargain + optional deposit + prepaymentMonths (preset 0/1/2 + custom integer) + minTerm (1_month / 3_months); Daily rent → optional deposit only (minTerm implicit `1_day`).
- [x] **FLOW-12** Per-step validation matches SPEC §"Validation Rules" exactly. Pure `validateStep(stepN, formState)` single source of truth (matches M1 Phase 5 `validateByCategory()` pattern).
- [x] **FLOW-13** Submit fires `editAsModerator` (mod-context, M2 MOD-14 carry) OR `submitForModeration` (owner). Status flips to `pending`. M2 `RejectionBanner` rendering preserved on edit-resubmit path.
- [x] **FLOW-14** Old `CreateListingScreen.tsx` deleted atomically when new flow ships. No dual-flow window. CreateListingForm/ sub-component barrel from M1 Phase 4 either re-used or torn down with cleanup commit.
- [x] **FLOW-15** Edit-on-behalf (M2 MOD-14 `moderatorContext` prop) wired into new 6-step flow. Mod can edit any field on a pending listing through the same conditional UI; banner stripe at TOP of flow when `moderatorContext` is set.
- [x] **FLOW-16** EN+RU locale parity for all new flow strings — estimated +80–120 keys to the post-M2 baseline. CI gate (`scripts/check-i18n-parity.sh`) enforces.

### Schema (Phase 1) — Mongo migration + nested shape

Backend Property schema reshape from flat to nested per SPEC §"Suggested Data Shape".

- [x] **SCHEMA-01** `Property` Mongoose schema reshapes to nested structure: `location.{city, district, coordinates, showExactAddress}` / `basics.{areaSqm, price, currency, rooms?, bathroom?, kitchen?, hotelRooms?, hotelClass?}` / `conditionAndAmenities.{condition, furnished}` / `content.{title, description, language}` / `terms.{negotiable?, deposit?, prepaymentMonths?, minTerm?}` / `media.{photos, videos, tourUrl?}`. Status enum + audit fields (M2's `submittedAt`/`approvedAt`/`approvedByUid`/`rejectedAt`/`rejectedByUid`/`rejectionReasonCode`/`rejectionReasonNote`/`archivedAt`/`archivedByUid`) stay top-level (preserved from M2 MOD-01). *(Validated in Phase 1, 2026-05-06.)*
- [x] **SCHEMA-02** One-shot `migrate-listings-m3.js` reshapes existing flat-schema listings into the nested shape. Idempotent. `--dry-run` + `--verify=PASS` operator-supervised checkpoint pattern matches M2 Plan 02-02 (`migrate-listings-m2.js`). Acceptance: post-migration `db.properties.countDocuments({location: {$exists: false}})` returns 0. *(Validated in Phase 1 code-side, 2026-05-06; live Atlas deploy pending in `01-HUMAN-UAT.md`.)*
- [x] **SCHEMA-03** Status enum unchanged (`pending | live | rejected | archived`). SPEC's `draft | pending_moderation | published | rejected` mapped 1:1 cosmetic only. No new enum values. *(Validated in Phase 1, 2026-05-06.)*
- [x] **SCHEMA-04** M2 audit fields and archive lifecycle fields (`archivedReasonCode`, `archivedReasonNote` from Phase 4) preserved at top level — NOT nested under `terms.*`. *(Validated in Phase 1, 2026-05-06.)*
- [x] **SCHEMA-05** Backend route reads handle ONLY nested shape post-migration (no dual-shape read paths). Acceptance criterion: migration runs BEFORE route-shape cutover commit; rollback rollback-without-migration documented in CONTEXT.md. *(Validated in Phase 1, 2026-05-06.)*

### Media inversion (Phase 3) — admin/mod uploads post-submission

Inverts current user-uploads-to-S3 workflow: users submit metadata only; admin/mod uploads photos/videos/3D-tour via moderation queue extension after metadata review.

- [x] **MEDIA-01** New 6-step flow has NO photo / video / 3D-tour fields. `MediaSection` from M1 Phase 4 `CreateListingForm/` is removed from the user-facing flow. (May still be referenced internally as a shared component for the mod media-curation UI.) [De-facto satisfied by Phase 2 Plan 02-09 atomic deletion of `CreateListingScreen.tsx` + `CreateListingForm/` barrel (sentinel `scripts/check-create-listing-screen-removed.sh` exits 0). Phase 3 Plan 03-04 adds the backend-side guarantee via the multer-strip sentinel `scripts/check-property-routes-media-stripped.sh` exits 0 — the user-facing API surface that mapped to S3 PutObject is gone (D-14 atomic-break reinterpretation per memory `aws-iam-jaytap-prod-s3.md`). Verified at phase close 2026-05-06.]
- [x] **MEDIA-02** `Property.media.{photos: string[], videos: string[], tourUrl?: string}` populated by admin/mod, NOT user. Empty arrays + undefined `tourUrl` for newly-submitted listings until mod adds them. [Plan 03-04 — completed 2026-05-06; commit 2be38d3 in JayTap-services. POST handler writes the explicit literal `media: { photos: [], videos: [], tourUrl: undefined }` regardless of body content; PUT handler `delete updateData.media` silently ignores client-supplied media keys preserving mod's curated state. 3 MEDIA-02 supertest cases prove the invariant on POST + PUT empty + PUT preservation paths. 258/258 backend pass.]
- [x] **MEDIA-03** Mod queue extension — new "Media curation" view per pending listing (alongside existing Approve / Reject / Edit-on-behalf actions from M2 MOD-11). Lets mod upload photos / videos / paste tour URL before publishing. [Plan 03-05 (MediaCurationScreen overlay 959 LOC) + Plan 03-06 (entry-points: filter chip row + W2 conditional row-tap routes needs-media listings to the curation surface; PropertyDetailsScreen NeedsMediaBanner CTA also dispatches to it). Verified by RTL tests in both plans (4 from 03-05 + 5 from 03-06 = 9/9 pass).]
- [x] **MEDIA-04** New backend endpoint: `POST /api/moderation/listings/:id/media` accepting multipart photos/videos uploads + JSON `tourUrl`. Race-safe via existing M2 atomic `findOneAndUpdate` patterns. Audit row written to `ModerationLog` with `action: 'media-upload'` (extends M2 enum, matching Phase 4 archive/unarchive/hard-delete extension pattern). [Plan 03-02 — completed 2026-05-06; commits 47fb4ac/2667bc7/fc96aee in JayTap-services. Companion DELETE `/api/moderation/listings/:id/media?url=&kind=photo|video` also shipped (idempotent `$pull`).]
- [x] **MEDIA-05** S3 IAM policy update — admin and moderator role accounts get upload rights to the `listing-images/` (and equivalent video/tour) prefixes. Existing `jaytap-prod-s3` IAM user retains existing rights for legacy/admin paths. Documented in `06-BACKEND-DEPLOY.md` equivalent (Phase 6 carry-forward audit). User account upload rights revoked at Phase 3 close — rotation symmetric to M2 HF-02 secret rotation discipline. [Plan 03-04 — completed 2026-05-06 via D-14 reinterpretation: API surface removal IS the IAM "rotation". Per memory `aws-iam-jaytap-prod-s3.md`, JayTap users do not have personal S3 credentials; the dedicated `jaytap-prod-s3` IAM user is the sole S3 actor. Stripping multer from POST/PUT `/api/properties` (commit 2be38d3) removes the user-facing API surface that was the user's only path to S3 PutObject. The IAM key itself does NOT need rotating because it wasn't leaked. Re-open condition: if Phase 5 REL-05 audit finds key-leak evidence, do a literal HF-02-style rotation at that time.]
- [x] **MEDIA-06** Mod queue surfaces a queue-side filter for listings in `status: pending` AND `media.photos.length === 0` (or equivalent "needs media" predicate). Pre-publish "awaiting media" workflow handled as a filter, NOT a new status enum value. [Plan 03-06 — completed 2026-05-06; commit 4c5ec03 in JayTap RN client. 3-chip filter row above ModerationQueueScreen Listings tab body (default 'all-pending' D-03 preserves M2 first-render); useMemo predicate on `media.photos.length` ONLY (RESEARCH §Open Question #1 resolution: tourUrl + videos do NOT factor). 3 RTL test cases verify the predicate including the Open Question #1 negative case. NO new status enum value introduced — pure client-side filter as specified.]
- [x] **MEDIA-07** Approval action on pending listings BLOCKED if `media.photos.length === 0`. Mod must add at least one photo before listing can flip to `live`. Backend invariant enforced via 400 with clear error code (`MEDIA_REQUIRED`). [Plan 03-03 — completed 2026-05-06; commits 9539313/6cbf3ab/80dd45d in JayTap-services. Two enforcement points: POST `/api/moderation/properties/:id/approve` (pre-fetch findById + photos.length check before M2 atomic transition) AND PUT `/api/moderation/listings/:id` edit-on-behalf flip-to-live (mergedPhotos.length check between deep-merge and status='live' mutation). Single shared code path covers both pending and rejected statuses; 7 supertest cases under describe('Phase 3 MEDIA-07 — MEDIA_REQUIRED gate'); 252/252 backend pass.]
- [x] **MEDIA-08** Existing M1+M2 listings with user-uploaded photos preserved unchanged through schema migration (SCHEMA-02). Media object preserved verbatim. [Plan 03-04 — completed 2026-05-06; commit 2be38d3 in JayTap-services. 3 MEDIA-08 regression supertest cases verify: GET returns legacy user-uploaded S3 URLs verbatim; owner PUT without media body preserves legacy photos; owner PUT WITH attacker-supplied media body STILL preserves legacy photos (D-13 strip wins). The PUT handler's `delete updateData.media` ensures existing media on the document survives any owner edit verbatim. Initial verification of the schema migration was Phase 1 Plan 03 / SCHEMA-02; Plan 03-04 adds the post-strip regression net.]
- [x] **MEDIA-09** EN+RU locale parity for mod media-curation UI strings — estimated +20–30 keys. [Plans 03-05 + 03-06 — completed 2026-05-06. Plan 03-05 added 26 `moderation.mediaCuration.*` keys (incl. cap-overflow toast keys per Discretion #5); Plan 03-06 added 6 banner+filter keys (`moderation.needsMediaBanner.*` + `moderation.filter.*`). Phase 3 cumulative = 32 keys per locale; 64 total entries across both files. Parity gate `bash scripts/check-i18n-parity.sh` exit 0 after each plan. Note: original estimate was +20–30; final 32 is +2 over budget, accounted for by the cap-overflow toast keys (MEDIA-09 acceptance-bound — they are user-facing translated strings for cap truncation feedback).]

### Carry-forward bug fixes (Phase 4)

Two genuine M2 carry-forward bugs folded into M3 per `phase06-m3-carry-forward.md`.

- [x] **CARRY-01** ROLE-11 frontend mid-action 403 popup-recovery. [Phase 4 Plan 04-04 — completed 2026-05-07; commits `ad02d40` (hook + matcher) + `f54457d` (11 handler wires) + `b54291e` (canonical RTL smoke). `useModActionGuard` hook + `is403PermissionError(err)` matcher route 11 mod-action submit handlers (4 ModerationQueueScreen + 5 PropertyDetailsScreen + 2 RoleManagementScreen incl. runSearch refactor — documented +1 over plan's 10) through `onPermissionDenied = resetLoading + closeModal + refreshRole`; banner auto-surfaces; bespoke `Alert.alert(t('errors.permissionDenied')) + onBack()` dropped from RoleManagementScreen.handleRoleSubmit. 10/10 useModActionGuard unit tests + 2/2 PropertyDetailsScreen-mod-403 RTL pass. Banner latency (~1s) device walk deferred to Phase 5 REL-03 Matrix 4 cell 4.1 per D-06.]
- [x] **CARRY-02** Phase 4.5 landlord-application uid-mismatch fix. [Phase 4 Plans 04-01 + 04-02 — completed 2026-05-07; backend commits in JayTap-services landed the anti-spoofing chain + repair migration. Plan 04-01: `landlordApplicationRoutes.js` sources `uid` exclusively from `req.firebaseUid` (JWKS-verified `sub`); diagnostic console.log block deleted (D-06); `scripts/check-no-landlord-uid-spoofing.sh` sentinel chained into `npm test` between actoruid and media-stripped; 3-case D-08 supertest (body-spoof rejected + no-bearer 401 + happy-path uid match) inside 273/273 suite. Plan 04-02: `migrate-landlord-app-uid-mismatch.js` (--dry-run + --verify=PASS + idempotent; 258 LOC) + 10-case Envelope-B test (4 unit + 6 spawn-integration); `LandlordApplication.status += 'orphaned'` + `LandlordApplicationAuditLog.action += ['uid-repair', 'uid-orphan-mark']`. SC#3 live Atlas walk deferred to Phase 5 REL-03 Matrix 5 cell 5.1 per D-06.]

### Release & store submission (Phase 5)

- [x] **REL-01** RN client `package.json` bumped to `3.0.0`. — Plan 05-03 closed 2026-05-08: atomic commit `531e279` flipped `package.json` line 3 from `"version": "2.0.0"` to `"version": "3.0.0"` as part of the 3-file atomic version-bump commit (alongside iOS pbxproj `MARKETING_VERSION = 3.0.0` Debug + Release + `CURRENT_PROJECT_VERSION = 28` Debug + Release; Android `build.gradle` `versionName "3.0.0"` + `versionCode 31`). 10/10 post-commit grep assertions PASS; build-identity-consistent at v3.0.0 across all 3 files. Source-mutation portion of REL-01 fully satisfied; downstream consumption by Plan 05-07 (dual-store submission) closes REL-01's "shipped" sub-criterion when archives land in ASC + Play Console.
- [x] **REL-02** iOS `MARKETING_VERSION 3.0.0` + Android `versionName "3.0.0"`. Build numbers per **M1 D-02 lesson**: query `06-STORE-HISTORY.md` (re-run pattern from M2 Phase 6) for `next_ios_build_number = max(local, store) + 1` and `next_android_version_code = max(local, store) + 1` BEFORE the atomic version-bump commit. Avoids reactive bumps at archive time. — Plan 05-02 closed 2026-05-07: store-history captured at `.planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` via Path B web-UI user-relay; derived `next_ios_build_number=28` (max of local pbxproj=27, ASC TestFlight=26) + `next_android_version_code=31` (max of local build.gradle=30, Play Console Closed Alpha=29). Both platforms `local-ahead` by 1. M2 anchor discrepancy disclosed: `m2_anchor_observed_in_stores: false` — Play Console nothing at v2.0.0; TestFlight max=26 not 27. The actual atomic version bump (REL-01 + REL-02 source-file mutations) is owned by Plan 05-03; this REQ checkbox flips on the basis of the binding bump-targets being recorded for Plan 05-03 to consume. **Plan 05-03 consumed 2026-05-08** in atomic commit `531e279`: pbxproj `MARKETING_VERSION = 3.0.0` (Debug line 283 + Release line 316) + `CURRENT_PROJECT_VERSION = 28` (Debug line 274 + Release line 308) + Android `build.gradle` `versionName "3.0.0"` (line 92) + `versionCode 31` (line 91). 10/10 grep assertions PASS; build-identity-consistent across all 3 files; integers match `05-STORE-HISTORY.md` derived_targets verbatim. Store-ingestion portion (ASC + Play Console upload + accept) closes via Plan 05-07.
- [x] **REL-03** Manual physical-device QA matrix walked APPROVED on iPhone 15 Pro Max + Moto G XT2513V. — Plan 05-05 closed 2026-05-11 (APPROVED-WITH-MASS-DISPOSITION via Path B per `/gsd-execute-phase 5` resume): 2 cells walked-and-confirmed on iPhone [1.3 + 2.5] + 67 mass-disposition via logical-equivalence + 4 DEFERRED-USER-APPROVED via M2 precedent; FAIL=0 PARTIAL=0; 3 walk-regression fix commits landed inline (`a3ba754` PropertyService JSON body + `a3625e7` editAsModerator JSON body + `4240e64` mod banner uid-leak). Coverage mode: empirical-sampling-mass-disposition. Plan 05-06 paired-gate (YELLOW verdict — verifier PARTIAL on REL-03 Path B + reviewer GREEN) inherited the PARTIAL signal with routing to Matrix 3 + 4 + 5 + Android parity. Device walks for CARRY-01 banner latency (Matrix 4 cell 4.1) + CARRY-02 live Atlas uid match (Matrix 5 cell 5.1) folded under mass-disposition.
- [x] **REL-04** Bilingual EN+RU release notes drafted + pasted on App Store Connect + Google Play Console. — Plan 05-04 closed 2026-05-07: EN 489 / RU 492 chars (both under Play Console 500-char binding limit); region-neutral phrasing per memory `geographic-scope.md`; paste-only artifact at `.planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md`. Plan 05-07 closed 2026-05-11: notes pasted into ASC en-US + ru-RU localizations + Play Console en-US + ru-RU release notes.
- [x] **REL-05** Backend live + healthy on Railway. — Plan 05-01 pre-flight (2026-05-08) walked GREEN-WITH-DEVIATIONS across 14 sections; backend live at Railway SHA `5bf23fe` with `health_check_status: 200`; firebase-admin confirmed absent; MongoDB Atlas password rotation (`mongo_revoked_at: 2026-04-29T00:00:00Z`) carries forward from M2 HF-02; AWS IAM remains PARTIAL (re-open condition unchanged). 2 D-03 deviations operator-approved (Atlas snapshot SKIPPED + seed-locations-m3.js bugs patched mid-runbook). 2 M3 backlog items surfaced: `JayTap-orphan-data-cleanup` + `ATLAS-CRED-ROTATION`. Backend confirmed healthy throughout the full QA + submission window (Plan 05-05 walks + Plan 05-07 dual-store submission both verified live backend). Final flip at 05-07 close 2026-05-11.
- [x] **REL-06** v3.0.x submitted to ASC TestFlight Internal Testing track + Play Console Internal Testing track. — Plan 05-07 closed 2026-05-11: ASC TestFlight Internal build 29 / iOS 3.0.0 + Play Console Internal Testing versionCode 32 / Android 3.0.1; iOS reactive build bump 28→29 (commit `3044de4`) + Android reactive bump versionCode 31→32 + versionName 3.0.0→3.0.1 (Play Console rejected versionCode 31 — M1 D-02 pattern fires again, captured in memory `release-android-versioncode-trust-play-console.md`); Android reanimated prefab workaround applied per memory `android-reanimated-clean-prefab-gotcha.md`; M1 D-13 inheritance descope honored (7/7 INHERITED per Plan 05-06 audit, privacy manifest unchanged); both stores processing.

---

## Future Requirements (M4+ — deferred)

Carried forward from M2 close + planted during M3 discovery:

- Race-cell test rig — coordinated-curl OR Bluetooth-trigger-pair sync for MOD-15 + ROLE-11 + HF-04 race coverage; cells 1.7 + 2.4–2.6 + 5.6 + 5.3 deferred from M2 Phase 6 with explicit user sign-off.
- Android `clean bundleRelease` reanimated build doc — document `gradlew :react-native-reanimated:assembleRelease :app:bundleRelease` in `scripts/release-android.md` OR add `bundleReleaseSafe` gradle task wrapper.
- AWS IAM cross-project residual — re-open when other project unblocks scoping the OLD shared IAM user's policy away from JayTap bucket ARN.
- KZT + UZS currency support — comes with KG/KZ/UZ market expansion (Almaty / Tashkent serviceAreas).
- Multi-language localization beyond EN+RU (KK + UZ if KZ/UZ markets warrant it).
- 2GIS native map bridge — multi-week effort; plan drafted in `2GIS_BRIDGE_PLAN.md`.
- Automated/AI moderation (image/text classifiers) — captured in M2 archive future requirements.
- Full audit log UI (`moderationLog` + `roleChangeLog` data captured in M2; UI deferred).
- Push notifications / email notifications for moderation events.
- Bulk moderation actions (multi-select approve/reject).
- Real-estate document verification (Avito-style ownership proof — multi-month compliance subproject).

---

## Out of Scope (M3)

Explicit exclusions, with reasoning:

- **Firebase SDK additions in either repo** — REPO RULE per memory `no-firebase-sdk.md`. Backend continues using `jose`.
- **Firebase custom claims as role source** — MongoDB `userType` is authoritative (validated in M2).
- **`react-navigation` migration** — custom `App.tsx` state machine stays. 6-step flow uses existing overlay pattern.
- **Replacing M1's 3-category 9-type taxonomy** — guarded by CLAUDE.md. SPEC's flat 5-type list reframed onto existing categories.
- **Replacing M2's status enum** — `pending | live | rejected | archived` preserved. SPEC's wording reframed 1:1.
- **New `awaiting_media` status enum value** — handled as queue-side filter, not new enum value.
- **2GIS native map bridge** — deferred to M4+ per `2GIS_BRIDGE_PLAN.md`.
- **Payment / booking flows** — JayTap is showcase-only by design (carried from M1+M2).
- **Per-night pricing for hospitality** — eliminated in M1; remains out of scope.
- **Chat moderation tooling** — listing moderation only (carried from M2).
- **New authentication providers (OAuth, magic link, 2FA)** — M3 keeps Email/password Identity Toolkit REST.
- **KZT + UZS currencies** — deferred to KG/KZ/UZ market expansion milestone.
- **Push notifications / email notifications** for moderation/media-uploaded events — in-app surfacing sufficient.
- **Race-cell test rig + Android reanimated build doc + AWS IAM cross-project residual** — explicitly deferred at M3 milestone discovery; folded into M4+ Future Requirements.
- **User-side photo upload during listing creation** — INVERTED in M3. Users submit metadata only; mod uploads media post-submission. Existing M1+M2 listings retain user-uploaded media (preserved through SCHEMA-02 migration).

---

## Traceability

Every M3 v1 requirement maps to exactly one phase. Coverage gate clean (zero orphans, zero duplicates). Filled by `/gsd-roadmapper` 2026-05-05.

| Phase | Requirements | Count |
|-------|--------------|-------|
| Phase 1 — Schema Reshape + Backend Route Shape Cutover | SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05 | 5 |
| Phase 2 — 6-Step Contextual Listing Flow (Client) | FLOW-01, FLOW-02, FLOW-03, FLOW-04, FLOW-05, FLOW-06, FLOW-07, FLOW-08, FLOW-09, FLOW-10, FLOW-11, FLOW-12, FLOW-13, FLOW-14, FLOW-15, FLOW-16 | 16 |
| Phase 3 — Media Flow Inversion (Admin/Mod Curation) | MEDIA-01, MEDIA-02, MEDIA-03, MEDIA-04, MEDIA-05, MEDIA-06, MEDIA-07, MEDIA-08, MEDIA-09 | 9 |
| Phase 4 — M2 Carry-Forward Bug Fixes | CARRY-01, CARRY-02 | 2 |
| Phase 5 — Hardening + Manual Physical-Device QA + Release v3.0.0 | REL-01, REL-02, REL-03, REL-04, REL-05, REL-06 | 6 |

**Coverage:** 38/38 v1 requirements mapped to exactly one phase (16 FLOW + 5 SCHEMA + 9 MEDIA + 2 CARRY + 6 REL = 38). Zero orphans. Zero duplicates.

### Per-requirement → phase index

| REQ-ID | Phase |
|--------|-------|
| FLOW-01 | Phase 2 |
| FLOW-02 | Phase 2 |
| FLOW-03 | Phase 2 |
| FLOW-04 | Phase 2 |
| FLOW-05 | Phase 2 |
| FLOW-06 | Phase 2 |
| FLOW-07 | Phase 2 |
| FLOW-08 | Phase 2 |
| FLOW-09 | Phase 2 |
| FLOW-10 | Phase 2 |
| FLOW-11 | Phase 2 |
| FLOW-12 | Phase 2 |
| FLOW-13 | Phase 2 |
| FLOW-14 | Phase 2 |
| FLOW-15 | Phase 2 |
| FLOW-16 | Phase 2 |
| SCHEMA-01 | Phase 1 |
| SCHEMA-02 | Phase 1 |
| SCHEMA-03 | Phase 1 |
| SCHEMA-04 | Phase 1 |
| SCHEMA-05 | Phase 1 |
| MEDIA-01 | Phase 3 |
| MEDIA-02 | Phase 3 |
| MEDIA-03 | Phase 3 |
| MEDIA-04 | Phase 3 |
| MEDIA-05 | Phase 3 |
| MEDIA-06 | Phase 3 |
| MEDIA-07 | Phase 3 |
| MEDIA-08 | Phase 3 |
| MEDIA-09 | Phase 3 |
| CARRY-01 | Phase 4 |
| CARRY-02 | Phase 4 |
| REL-01 | Phase 5 |
| REL-02 | Phase 5 |
| REL-03 | Phase 5 |
| REL-04 | Phase 5 |
| REL-05 | Phase 5 |
| REL-06 | Phase 5 |

---

*Requirements drafted: 2026-05-05*
*Total v1 (M3) requirements: 38 (16 flow + 5 schema + 9 media + 2 carry-forward + 6 release-and-submission). Traceability filled by `/gsd-roadmapper` 2026-05-05; 100% coverage across 5 phases (zero orphans, zero duplicates). 25% smaller than M2's 51 v1 reqs; reflects M3's tighter scope (one feature surface — listing creation flow — vs. M2's cross-cutting role + lifecycle + admin work).*
