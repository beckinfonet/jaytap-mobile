---
phase: 02-6-step-contextual-listing-flow-client
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/PropertyDetailsScreen.tsx
autonomous: true
requirements: [FLOW-13]

must_haves:
  truths:
    - "PropertyDetailsScreen.tsx (1680 LOC) reads all flat M2 fields rewritten to nested Phase 1 shape: location.* + basics.* + conditionAndAmenities.* + content.* + terms.* + media.*."
    - "Map embed at lines ~959-979 + ~1165-1185 reads location.coordinates.lat/lng (was latitude/longitude flat)."
    - "Top-level maxGuests + amenities reads PRESERVED (D-21 — no changes to those reads)."
    - "Hospitality branch read of basics.hotelClass + media.tourUrl correct."
    - "RejectionBanner mounting preserved per FLOW-13 (M2 MOD-08/09 surface untouched)."
    - "Existing PropertyDetailsScreen test (if exists) still passes; snapshots updated if needed."
  artifacts:
    - path: "src/screens/PropertyDetailsScreen.tsx"
      provides: "Updated to read nested shape across all flat-field references in 1680 LOC"
  key_links:
    - from: "PropertyDetailsScreen map embed (2 sites)"
      to: "location.coordinates"
      via: "property.location?.coordinates?.lat / .lng"
      pattern: "location\\?\\.coordinates"
    - from: "PropertyDetailsScreen hospitality branch"
      to: "basics.hotelClass + media.tourUrl"
      via: "property.basics?.hotelClass + property.media?.tourUrl"
      pattern: "basics\\?\\.hotelClass|media\\?\\.tourUrl"
    - from: "PropertyDetailsScreen RejectionBanner"
      to: "M2 banner component (preserved)"
      via: "<RejectionBanner status={...} ... /> render unchanged"
      pattern: "<RejectionBanner|RejectionBanner"
---

<objective>
Read-path cutover for `src/screens/PropertyDetailsScreen.tsx` (1680 LOC, the largest screen — own plan per Plan Structure recommendation). Same swap mapping as Plan 02-05 but for a single large file.

This plan can run in PARALLEL with Plan 02-01, Plan 02-02, and Plan 02-05 (Wave 1) — it touches only PropertyDetailsScreen.tsx; no file overlap with any other plan.

Per CONTEXT.md D-20: PropertyDetailsScreen is the 10th of 9+1 read-path surfaces. Per D-21: top-level `maxGuests` + `amenities[]` STAY at top level (no changes to those reads). Per FLOW-13: M2 RejectionBanner rendering preserved.

Reference Plan 02-05's `<interfaces>` field-by-field swap table — the rules are identical; only the file is different.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-6-step-contextual-listing-flow-client/02-CONTEXT.md
@.planning/phases/02-6-step-contextual-listing-flow-client/02-RESEARCH.md
@.planning/phases/02-6-step-contextual-listing-flow-client/02-PATTERNS.md
@src/screens/PropertyDetailsScreen.tsx
@src/types/Property.ts
@src/components/RejectionBanner.tsx

<interfaces>
<!-- Field-by-field swap table — same as Plan 02-05 (CONTEXT D-04..D-15 + Phase 1 Property.ts nested shape). -->

| Flat M2 (BEFORE) | Nested Phase 1 (AFTER) |
|------------------|-----------------------|
| property.latitude, property.longitude | property.location?.coordinates?.lat, property.location?.coordinates?.lng |
| property.address | property.location?.city + property.location?.district (display joined) |
| property.city, property.district | property.location?.city, property.location?.district |
| property.price, property.currency | property.basics?.price, property.basics?.currency |
| property.areaSqm | property.basics?.areaSqm |
| property.bedrooms | property.basics?.rooms |
| property.bathrooms | property.basics?.bathroom |
| property.condition | property.conditionAndAmenities?.condition |
| property.furnished | property.conditionAndAmenities?.furnished |
| property.title, property.description | property.content?.title, property.content?.description |
| property.tours[0]?.url | property.media?.tourUrl |
| property.images | property.media?.photos |
| property.videos | property.media?.videos |
| property.maxGuests | property.maxGuests (UNCHANGED — top-level per D-21) |
| property.amenities | property.amenities (UNCHANGED — top-level per D-21) |
| property.hotelClass | property.basics?.hotelClass |
| property.negotiable, property.deposit, property.prepaymentMonths, property.minTerm | property.terms?.* |
| property.status | property.status (UNCHANGED — top-level per Phase 1 SCHEMA-04) |
| property.submittedAt, .approvedAt, .approvedByUid, .rejectedAt, .rejectedByUid, .rejectionReasonCode, .rejectionReasonNote, .archivedAt, .archivedByUid, .archivedReasonCode, .archivedReasonNote | UNCHANGED — top-level audit fields preserved per Phase 1 SCHEMA-04 + Phase 2 D-21 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PropertyDetailsScreen.tsx full nested-shape swap (1680 LOC)</name>
  <files>
    - src/screens/PropertyDetailsScreen.tsx (MODIFIED — large)
  </files>
  <read_first>
    - src/screens/PropertyDetailsScreen.tsx (full read; pay attention to map embed sites at lines 959-979 + 1165-1185 per PATTERNS.md row 57; hospitality branch lines; rejection banner site)
    - src/types/Property.ts (single source of truth for nested shape)
    - .planning/phases/02-6-step-contextual-listing-flow-client/02-PATTERNS.md row 57 (PropertyDetailsScreen swap mapping with line citations)
    - src/components/RejectionBanner.tsx (M2 MOD-08/09 component — verify props/render unchanged)
    - .planning/phases/02-6-step-contextual-listing-flow-client/02-05-SUMMARY.md (if Plan 02-05 already shipped — refer to its swap notes for any field-mapping nuances discovered)
  </read_first>
  <action>
    Pass through the entire file and replace every flat-shape read per the table in `<interfaces>`. Strategy:

    1. **Find every site** — use grep to enumerate:
       ```bash
       grep -nE "property\\.(price|currency|areaSqm|bedrooms|bathrooms|address|city|district|latitude|longitude|tours|images|videos|hotelClass|negotiable|deposit|prepaymentMonths|minTerm|condition|furnished|title|description)" src/screens/PropertyDetailsScreen.tsx
       ```
       Use the output as a checklist.

    2. **Map embed sites (lines ~959-979 + ~1165-1185)** — verify these are the only two map render points; swap `latitude={property.latitude}` → `latitude={property.location?.coordinates?.lat ?? 0}` and same for longitude. If `property.latitude` is also used for null-checks (e.g. `property.latitude && property.longitude`), rewrite to `property.location?.coordinates?.lat != null && property.location?.coordinates?.lng != null`.

    3. **Hospitality branch** — find where `property.maxGuests` and `property.amenities` are rendered (per D-21, those reads stay TOP-LEVEL — DO NOT add `.basics?.` prefix). Add `?.` for null-safety (`property.maxGuests ?? 0`, `property.amenities ?? []`). `property.hotelClass` becomes `property.basics?.hotelClass`. `property.tours[0]?.url` becomes `property.media?.tourUrl`.

    4. **Image gallery / videos** — `property.images` → `property.media?.photos ?? []`; `property.videos` → `property.media?.videos ?? []`.

    5. **Terms section / detail rows** — `property.negotiable`, `property.deposit`, `property.prepaymentMonths`, `property.minTerm` → `property.terms?.*`.

    6. **Status + audit fields** — UNCHANGED. `property.status`, `property.submittedAt`, `property.approvedAt`, etc. all stay top-level per Phase 1 SCHEMA-04 + D-21. The `<RejectionBanner>` continues consuming top-level audit fields verbatim — DO NOT alter rejection-banner mounting per FLOW-13.

    7. **Currency display** — if PropertyDetailsScreen rendered currency as `'$'` / `'сом'` legacy symbols, derive via the same inline `currencySymbol(c)` helper as Plan 02-05 (or copy the helper from PropertyCard.tsx if Plan 02-05 already shipped).

    8. **Edit / mod-action handlers** — `onEditProperty(property)` continues passing the full Property object; no signature change. App.tsx (Plan 02-07) will swap the destination from `<CreateListingScreen propertyToEdit={p} />` to `<ContextualListingFlow mode="edit-owner" initialListing={p} />` — handler unchanged.

    9. **Run grep again at end of task** — confirm zero remaining flat-shape reads (acceptance criterion below).

    Per Plan 02-05 SUMMARY (if available), reuse any patterns it established for currency display, label-vs-slug presentation, etc.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -cE "property\\.(price|currency|areaSqm|bedrooms|bathrooms|latitude|longitude|tours\\[|hotelClass|negotiable|deposit|prepaymentMonths|minTerm|condition|furnished)\\b" src/screens/PropertyDetailsScreen.tsx; test $? -eq 1 && (find src/screens/__tests__ -name "PropertyDetailsScreen*.test.*" 2>/dev/null | head -1 | xargs -I{} npx jest {} -x 2>/dev/null || echo "no PropertyDetailsScreen test — manual verify required")</automated>
  </verify>
  <done>
    - Zero flat-shape reads remaining (grep returns 0 matches for the listed flat field names).
    - Top-level maxGuests + amenities + status + audit fields PRESERVED (no `.basics?.maxGuests` or similar — those stay top-level).
    - Map embed coord field names rewritten at both sites.
    - RejectionBanner render unchanged (FLOW-13).
    - Existing test (if any) passes; snapshot updated if needed.
  </done>
  <acceptance_criteria>
    - `grep -cE "property\\.price\\b|property\\.bedrooms\\b|property\\.bathrooms\\b|property\\.latitude\\b|property\\.longitude\\b|property\\.areaSqm\\b|property\\.hotelClass\\b|property\\.negotiable\\b" src/screens/PropertyDetailsScreen.tsx` returns 0.
    - `grep -cE "property\\.basics\\?\\." src/screens/PropertyDetailsScreen.tsx` returns at least 5 (basics.price, .currency, .areaSqm, .rooms, .bathroom OR .hotelClass — at minimum 5 sites).
    - `grep -cE "property\\.location\\?\\.coordinates" src/screens/PropertyDetailsScreen.tsx` returns at least 2 (both map embed sites).
    - `grep -cE "property\\.media\\?\\." src/screens/PropertyDetailsScreen.tsx` returns at least 1 (photos / tourUrl read).
    - `grep -cE "property\\.maxGuests|property\\.amenities" src/screens/PropertyDetailsScreen.tsx` returns at least 2 (D-21 top-level reads preserved — but with optional chaining is fine).
    - `grep -c "<RejectionBanner" src/screens/PropertyDetailsScreen.tsx` returns at least 1 (M2 MOD-08 banner mounting preserved per FLOW-13).
    - File compiles standalone via `npx tsc --noEmit src/screens/PropertyDetailsScreen.tsx` (errors against missing types from `Property` only — they should be zero since Phase 1 type stub is full nested).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run RN full suite + i18n parity gate</name>
  <files>(verification only)</files>
  <read_first>(none)</read_first>
  <action>
    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/JayTap
    npm test
    bash scripts/check-i18n-parity.sh
    ```

    If PropertyDetailsScreen has a snapshot test that fails because of nested-shape rendering changes, update with `npx jest -u src/screens/__tests__/PropertyDetailsScreen.test.tsx` (or whatever the file is named). Review the diff to confirm only field-shape reads changed (no behavior regression).
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && npm test</automated>
  </verify>
  <done>
    - npm test exits 0.
    - i18n parity green (no key changes in this plan).
  </done>
  <acceptance_criteria>
    - `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npm test` exits 0.
    - `bash scripts/check-i18n-parity.sh` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>None — pure read-path swap on existing screen. No new HTTP endpoints. RejectionBanner mounting preserved per FLOW-13.</threat_model>

<verification>
- PropertyDetailsScreen has zero flat-shape reads.
- Map embed coord rename complete at both sites (~959-979 + ~1165-1185).
- D-21 top-level reads (`maxGuests`, `amenities`, `status`, audit fields) preserved.
- M2 RejectionBanner mounting preserved (FLOW-13).
- npm test green.
</verification>

<success_criteria>
- All 6 must_haves truths observable.
- Phase 1 atomic-break window for the largest screen (PropertyDetailsScreen) is now CLOSED. Combined with Plan 02-05, all 10 client read paths now consume nested shape.
- Plan 02-07 can wire the new flow into App.tsx without read-path concerns.
</success_criteria>

<output>
.planning/phases/02-6-step-contextual-listing-flow-client/02-06-SUMMARY.md captures:
- Number of flat-field reads swapped (count from initial grep before vs zero after).
- Snapshot updates required (file names + summary of diff).
- Note for Plan 02-08 dry-run: PropertyDetailsScreen on a real device should render nested data correctly — this is the visual verification cell that Phase 5 owns the full QA matrix for.
</output>
