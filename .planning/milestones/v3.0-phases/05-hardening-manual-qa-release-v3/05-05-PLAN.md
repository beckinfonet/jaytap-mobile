---
phase: 05-hardening-manual-qa-release-v3
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-03]
files_modified:
  - .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md
  - .planning/phases/01-schema-reshape-backend-route-shape-cutover/01-HUMAN-UAT.md
  - .planning/phases/02-6-step-contextual-listing-flow-client/02-HUMAN-UAT.md
  - .planning/phases/03-media-flow-inversion-admin-mod-curation/03-VERIFICATION.md
  - .planning/phases/04-m2-carry-forward-bug-fixes/VERIFICATION.md
autonomous: false
requirements: [REL-03]
requirements_addressed: [REL-03]
tags: [release, manual-qa, physical-device, iphone-15-pro-max, moto-g-xt2513v, six-step-flow, conditional-sub-fields, exact-address-toggle, mod-media-curation, role-11-recovery, carry-02-uid-fix, en-ru-parity, dark-light-parity, walk-once-count-for-both, satisfies-tagging, m2-phase-6-precedent, m4-deferred-race-cells]

must_haves:
  truths:
    - "05-QA-MATRIX.md exists in the phase directory as the SINGLE source of truth for all M3 device walks (D-06 supersedes 4 prior *-HUMAN-UAT.md files; the 4 prior files get a one-line header note 'Rolled into .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md 2026-05-XX; do not update further' and stop accepting status updates)"
    - "Frontmatter `walked_against_sha:` is populated with Plan 05-03's atomic-bump commit SHA; `backend_walked_against_sha:` is populated from 05-PREFLIGHT.md's `railway_deploy_sha`"
    - "Frontmatter `walk_disposition:` flips to APPROVED only when total FAIL count = 0 (post fix-loop); BLOCKED-BY-FAIL otherwise"
    - "iPhone 15 Pro Max walks every cell that's not iPhone-N/A; Moto G XT2513V walks every cell that's not Moto-N/A — closing the 12 deferred walks from 02-HUMAN-UAT.md + the 6 deferred walks from 03-VERIFICATION.md + the 2 deferred walks from 04-VERIFICATION.md per D-07 walk-once-count-for-both"
    - "Each cell row carries a `satisfies:` column listing the prior-phase deferred entry it closes (e.g., `satisfies: Phase 2 W1 + REL-03 cell apartment+rent_long`) — D-06 + Specifics convention. Greppable via `grep -c 'satisfies:' 05-QA-MATRIX.md` returning ≥ 50 cells"
    - "Total cell count is ~60 per D-08 estimate (15 happy-path × 2 devices = 30 + 6 edit-mode × 2 = 12 + 4 mod-media × 2 = 8 + 2 ROLE-11 cells + 1 CARRY-02 cell + ~8 EN+RU + ~8 dark/light)"
    - "Race cells (DEFERRED-USER-APPROVED per memory `phase06-m3-carry-forward.md`) explicitly recorded as M4+ deferred with the user's prior sign-off acknowledged in the cell rationale"
    - "Verdict taxonomy is exactly D-09 four-state: PASS / PARTIAL / DEFERRED-USER-APPROVED / FAIL"
    - "Build identity at walk time is verifiable on both physical devices: iOS About screen shows 3.0.0 (build <next_ios_build_number>); Android About shows versionName 3.0.0 + correct versionCode (matching 05-STORE-HISTORY.md)"
    - "tsc baseline gate: 05-PREFLIGHT.md `rn_tsc_baseline_error_count` ≤ 17 BEFORE walks start (deviation_protocol classification gate per CONTEXT.md `## Specific Ideas`)"
    - "4 prior *-HUMAN-UAT.md / *-VERIFICATION.md files updated with the one-line 'rolled into 05-QA-MATRIX.md' header note (D-06)"
  artifacts:
    - path: ".planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md"
      provides: "REL-03 — manual physical-device QA matrix; binding evidence for cross-cutting M3 surface coverage; gate artifact for Plan 05-07 release submission"
      contains: "iPhone 15 Pro Max"
      contains_also: "Moto G XT2513V"
  key_links:
    - from: "05-QA-MATRIX.md :: walk_disposition: APPROVED"
      to: "Plan 05-07 :: archive build + store submission"
      via: "Plan 05-07 only proceeds if walk_disposition is APPROVED"
      pattern: "walk_disposition: APPROVED"
    - from: "Plan 05-03 commit SHA"
      to: "05-QA-MATRIX.md :: walked_against_sha"
      via: "QA matrix walks the bumped binary, not pre-bump"
      pattern: "walked_against_sha:"
    - from: "05-PREFLIGHT.md :: railway_deploy_sha"
      to: "05-QA-MATRIX.md :: backend_walked_against_sha"
      via: "QA matrix walks against the live backend deploy from Plan 05-01"
      pattern: "backend_walked_against_sha:"
    - from: "05-QA-MATRIX.md :: cells with `satisfies:` tags"
      to: "Phase 2 W1-W12 + Phase 3 deferred 6-walk set + Phase 4 deferred 2-walk set"
      via: "D-07 walk-once-count-for-both — each device walk closes a prior-phase deferred entry AND a REL-03 cell from the same evidence; auditable via `grep -c 'Phase [0-9]' 05-QA-MATRIX.md`"
      pattern: "satisfies: Phase"
---

<objective>
Wave 3 of Phase 5 — the cross-cutting manual physical-device QA matrix walk. This is the gate that proves M3's full surface (Phases 1–4 deliverables + REL-03 happy-path matrix) works end-to-end on real iOS + Android devices before the v3.0.0 binary uploads to the stores.

Per CLAUDE.md, the M3 testing bar is manual physical-device QA on iPhone 15 Pro Max + Moto G XT2513V (M1 + M2 inheritance — same hardware). M3 Phases 2 + 3 + 4 deferred device-level walks to Phase 5 REL-03; Plan 05-05 absorbs all of those (~20 deferred walks) AND adds the REL-03 happy-path matrix (15 cells from 6-step flow × 5 property types × 3 deal types).

Key D-07 mechanic — walk-once-count-for-both: each device walk closes a prior-phase deferred entry AND a REL-03 cell from the same evidence. A `satisfies:` tag on each cell row makes the dual-coverage auditable. This avoids doubling physical-device time (~24+ walks → ~12 walks per device) with no coverage loss.

Per CONTEXT.md `## Specifics: cell-tagging convention`: each cell row in 05-QA-MATRIX.md MUST include a `satisfies:` column. M2 Phase 6's 06-QA-MATRIX.md did NOT carry this column — Phase 5 EXTENDS the format.

7 matrices per CONTEXT.md D-08 estimate (~60 cells total):
1. **Happy-path 6-step flow** (15 combos × 2 devices = 30 cells; satisfies REL-03 + Phase 2 W1-W6)
2. **Edit mode** (6 reps × 2 devices = 12 cells; satisfies Phase 2 W7-W9 mode='edit-owner' + mode='edit-mod')
3. **Mod media curation** (4 walks × 2 devices = 8 cells; satisfies Phase 3 deferred 6-walk set — filter-needs-media, MEDIA_REQUIRED block, EN+RU parity, cap-overflow)
4. **ROLE-11 demote-mid-action recovery** (2 paired-device cells; satisfies Phase 4 CARRY-01 banner-latency on iPhone)
5. **Phase 4.5 uid-mismatch repair** (1 cell; satisfies CARRY-02 SC#3 live Atlas walk)
6. **EN+RU parity** (~4-8 cells; covers 6-step flow + mod media curation in Russian)
7. **Dark/light parity** (~4-8 cells; covers M3-new surfaces — ContextualListingFlow, MediaCurationScreen, NeedsMediaBanner, RoleRefreshBanner)

Per CONTEXT.md `<deferred>`: race cells (M2 Phase 6 1.7 + 2.4-2.6 + 5.6 + 5.3 mismatch path) stay M4+ DEFERRED-USER-APPROVED per memory `phase06-m3-carry-forward.md`. The plan documents the deferral with the user's M2-precedent sign-off.

This plan is `autonomous: false` — the executor (Claude) builds the matrix scaffold (Task 1) and the user (the human) walks the cells (Task 2). The user resumes execution with the `approved` signal once the walk completes.

Output: `05-QA-MATRIX.md` walked APPROVED; user resume signal `approved`; phase ready for Plan 05-06 paired-gate verifier+reviewer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-CONTEXT.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-PATTERNS.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md
@.planning/phases/01-schema-reshape-backend-route-shape-cutover/01-HUMAN-UAT.md
@.planning/phases/02-6-step-contextual-listing-flow-client/02-HUMAN-UAT.md
@.planning/phases/03-media-flow-inversion-admin-mod-curation/03-VERIFICATION.md
@.planning/phases/04-m2-carry-forward-bug-fixes/VERIFICATION.md
@.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md
@.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-06-PLAN.md

<interfaces>
<!-- Devices + accounts (per CLAUDE.md M1+M2 testing bar inheritance into M3; same hardware): -->

Devices:
- iPhone 15 Pro Max / iOS 26.x (Hermes + Fabric, Release config)
- Moto G XT2513V / Android 16 (Fabric, Release config)

Accounts (per Phase 5 close + Phase 4 close — already provisioned):
- Admin: `beckprograms@gmail.com` (per memory `m2-shipped-2026-05-05.md` baseline + ROLE-07 anchor)
- Moderator: existing M2 moderator OR promoted at walk time via the v3.0.0 admin RoleManagementScreen (Phase 5 of M2 shipped this UI)
- Owner with rejected listing: created at walk time via Phase 3 reject-flow (or use an existing one from M2 testing)
- Plain user: existing test account

Build identity at walk time:
- Git SHA: <Plan 05-03 commit SHA — recorded in 05-QA-MATRIX.md frontmatter `walked_against_sha`>
- iOS build: `3.0.0 build <next_ios_build_number from 05-STORE-HISTORY.md>` (e.g., `3.0.0 build 28`)
- Android build: `3.0.0 versionCode <next_android_version_code from 05-STORE-HISTORY.md>` (e.g., `3.0.0 versionCode 31`)
- Backend deploy SHA: <recorded in 05-PREFLIGHT.md frontmatter `railway_deploy_sha`>

Per memory `nav-overlay-hides-bottom-nav.md`: overlays (PropertyDetails / ContextualListingFlow / MediaCurationScreen / etc.) hide the bottom tab bar by design. Cells that would require seeing the tab bar WHILE an overlay is still open are intentionally `— N/A`. DO NOT mark FAIL.

Per memory `last-admin-lockout-semantics.md`: ADMIN-03 last-admin lockout only fires on Promise.all race or count==1 self-demote. Don't write a manual UAT row expecting the lockout outside a concurrent race.

<!-- Phase 2 deferred walks (12 total; from 02-HUMAN-UAT.md lines 22-77): -->
- W1 (× iOS + Android) — mode='create' apartment + rent_long happy path
- W2 (× iOS + Android) — mode='edit-owner'
- W3 (× iOS + Android) — mode='edit-mod' (moderator account)
- W4 (× iOS + Android) — mode='create' rent_daily + hotel (D-19 thin Step 6)
- W5 (× iOS + Android) — Locations tab on ModerationQueueScreen
- W6 (× iOS + Android) — propertyType reflow mid-flow (FLOW-08 + FLOW-06)

<!-- Phase 3 deferred walks (6 total; from 03-VERIFICATION.md `deferred:` section + memory phase06-m3-carry-forward.md): -->
- filter-needs-media (mod queue chip predicate)
- MEDIA_REQUIRED 400 UX block (mod tries to approve, gets blocked, sees clear toast)
- EN+RU parity for media curation strings
- cap-overflow toast (D-05 client-side cap)
- has-media filter (Open Question #1 negative — tourUrl + videos do NOT count)
- per-asset DELETE round-trip (`/api/moderation/listings/:id/media?url=...&kind=photo|video`)

<!-- Phase 4 deferred walks (2 total; from 04-VERIFICATION.md): -->
- CARRY-01 banner latency on iPhone (RoleRefreshBanner appears within ~1s of demote)
- CARRY-02 SC#3 live Atlas uid match (re-submit landlord application; verify Atlas-stored uid === Firebase localId)

<!-- Phase 1 deferred operator items (4 total; rolled forward separately, NOT walks): -->
The 4 Phase 1 operator items (Atlas migration, restore-snapshot drill, tester comms, RN tsc capture) are CLOSED in Plan 05-01's runbook (Sections 1, 4, 0, 13 respectively). Plan 05-05's matrix does NOT need to re-walk these — they're operator-runbook items, not device walks. The 01-HUMAN-UAT.md header note still gets the "rolled into 05-QA-MATRIX.md / Plan 05-01" annotation for archive cleanliness.

<!-- Race cells deferred per memory phase06-m3-carry-forward.md: -->
M2 Phase 6 cells 1.7 (concurrent demote race) + 2.4-2.6 (TWO-DEVICE RACE) + 5.6 (socket attacker) + 5.3 mismatch path stayed DEFERRED-USER-APPROVED at M2 close. Phase 5 inherits the M4+ deferral; documents it explicitly with `## DEFERRED-USER-APPROVED` block in the matrix.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: PREREQUISITE GATES — confirm pre-flight green + build identity match before scaffolding</name>
  <files>.planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md</files>
  <read_first>
    - .planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md (read frontmatter — confirm `health_check_status: 200`, all 4 verify-PASS timestamps populated, `rn_tsc_baseline_error_count: <int ≤ 17>`)
    - .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md (read `phase_4_paired_gates_prerequisite.status: clear` + `derived_targets`)
    - package.json line 3 (confirm `"version": "3.0.0"` — Plan 05-03 atomic bump landed)
    - ios/JayTap.xcodeproj/project.pbxproj lines 270-320 (confirm 4 lines all flipped: 274/308 = next_ios_build_number; 283/316 = MARKETING_VERSION 3.0.0)
    - android/app/build.gradle lines 85-100 (confirm line 91 = versionCode <next_android_version_code>; line 92 = versionName "3.0.0")
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/gsd-verifier-misses-regressions.md (re-anchor paired-gates discipline — Plan 05-06 re-runs verifier+reviewer at end of Phase 5)
  </read_first>
  <action>
    Five-step gate check before any matrix scaffolding. The matrix walk is a high-stakes step (consumes ~60 cells × ~5 min each = ~5 hours of physical-device time × 2 devices = ~10 device-hours); proceeding against an inconsistent build wastes that time.

    **Step 1 — 05-PREFLIGHT.md gate:**
    ```bash
    grep -E '^\s*health_check_status:\s*(200|401)' .planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md
    grep -E '^\s*listings_m3_migration_verify_pass_at:\s*[0-9]{4}' .planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md
    grep -E '^\s*landlord_uid_repair_migration_verify_pass_at:\s*[0-9]{4}' .planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md
    grep -E '^\s*locations_seed_verify_pass_at:\s*[0-9]{4}' .planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md
    grep -E '^\s*rn_tsc_baseline_error_count:\s*([0-9]|1[0-7])\b' .planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md
    ```
    All five MUST exit 0. The tsc count grep matches integers 0-17 (Phase 4 baseline). If > 17, HALT and surface the deviation_protocol classification need.

    **Step 2 — 05-STORE-HISTORY.md gate:**
    ```bash
    grep -A 2 'phase_4_paired_gates_prerequisite:' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md | grep -E '^\s*status:\s*clear'
    grep -E '^\s*next_ios_build_number:\s*[0-9]+' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md
    grep -E '^\s*next_android_version_code:\s*[0-9]+' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md
    ```
    All MUST exit 0.

    **Step 3 — Build identity match gate:**
    ```bash
    NEXT_IOS=$(grep -E '^\s*next_ios_build_number:\s*[0-9]+' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md | grep -oE '[0-9]+')
    NEXT_AND=$(grep -E '^\s*next_android_version_code:\s*[0-9]+' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md | grep -oE '[0-9]+')
    [ "$(grep -c '"version": "3.0.0"' package.json)" = "1" ]
    [ "$(grep -c 'MARKETING_VERSION = 3.0.0' ios/JayTap.xcodeproj/project.pbxproj)" = "2" ]
    [ "$(grep -c "CURRENT_PROJECT_VERSION = $NEXT_IOS" ios/JayTap.xcodeproj/project.pbxproj)" = "2" ]
    [ "$(grep -c 'versionName "3.0.0"' android/app/build.gradle)" = "1" ]
    [ "$(grep -c "versionCode $NEXT_AND" android/app/build.gradle)" = "1" ]
    ```
    All MUST be true. If any fails, Plan 05-03 didn't land cleanly — HALT.

    **Step 4 — git SHA capture for matrix frontmatter:**
    ```bash
    PLAN05_03_SHA=$(git log --oneline | grep 'chore(05-03): bump version to 3.0.0' | head -1 | cut -d' ' -f1)
    BACKEND_SHA=$(grep -E '^\s*railway_deploy_sha:' .planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md | grep -oE '[0-9a-f]{7,40}')
    echo "RN client SHA: $PLAN05_03_SHA"
    echo "Backend SHA: $BACKEND_SHA"
    ```
    Both MUST be non-empty.

    **Step 5 — Decision:**
    If all gates pass, proceed to Task 1 (scaffold matrix).
    If any gate fails, surface the failure to the user and HALT. The user reads the failure context and decides per D-03 (triage in-place vs full-rollback to M2).
  </action>
  <verify>
    <automated>grep -E '^\s*health_check_status:\s*(200|401)' .planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md && grep -A 2 'phase_4_paired_gates_prerequisite:' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md | grep -E '^\s*status:\s*clear' && [ "$(grep -c '"version": "3.0.0"' package.json)" = "1" ] && [ "$(grep -c 'MARKETING_VERSION = 3.0.0' ios/JayTap.xcodeproj/project.pbxproj)" = "2" ] && [ "$(grep -c 'versionName "3.0.0"' android/app/build.gradle)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - All 5 gate checks (Steps 1-3) pass with exit 0
    - `PLAN05_03_SHA` captured (Plan 05-03 commit SHA — populates `walked_against_sha` in matrix frontmatter)
    - `BACKEND_SHA` captured from 05-PREFLIGHT.md (populates `backend_walked_against_sha`)
    - If any gate fails, the executor surfaces the failure with the failing gate name + diagnostic info before HALTing
  </acceptance_criteria>
  <done>
    All upstream gates confirmed in accept state; Plan 05-03's atomic bump is in place; backend live + healthy at recorded SHA; RN client tsc baseline preserved. Cleared to proceed to Task 1 (scaffold matrix).
  </done>
</task>

<task type="auto">
  <name>Task 1: Scaffold 05-QA-MATRIX.md with all 7 matrices + satisfies-tag column + DEFERRED-USER-APPROVED block</name>
  <files>.planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md</files>
  <read_first>
    - .planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md (full file — copy the multi-matrix shape: frontmatter, Cell Markers section, Test Environment table, Matrix tables, Sign-off block; lines 99-225 for cell-row structure pattern)
    - .planning/phases/05-hardening-manual-qa-release-v3/05-PATTERNS.md lines 232-307 (read the M3-specific 7-matrix mapping + satisfies-column extension + DEFERRED-USER-APPROVED block requirement)
    - .planning/phases/02-6-step-contextual-listing-flow-client/02-HUMAN-UAT.md (full file — extract the 12 deferred walks W1-W6 × iOS+Android with exact step descriptions; this is the source of truth for Matrix 1 + 2 cell content)
    - .planning/phases/03-media-flow-inversion-admin-mod-curation/03-VERIFICATION.md (read `deferred:` section + Manual Physical-Device QA Items list — extract the 6 mod-media-curation walks; source for Matrix 3)
    - .planning/phases/04-m2-carry-forward-bug-fixes/VERIFICATION.md (read `human_needed` rows in Goal Achievement section — extract the 2 deferred walks; source for Matrices 4 + 5)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/phase06-m3-carry-forward.md (re-anchor M4+ deferred race-cell list — populates the DEFERRED-USER-APPROVED block)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/nav-overlay-hides-bottom-nav.md (re-anchor overlay-hides-tabs invariant — populates the N/A cell rationale)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/geocoder-nominatim-hang-axios-network-error.md (re-anchor known issue — populates Matrix 1 cell rationale: if Step 2 location triggers Network Error, retry is the workaround; do NOT mark FAIL — track as PARTIAL)
  </read_first>
  <action>
    Author `05-QA-MATRIX.md` with the 7-matrix structure inherited from M2's `06-QA-MATRIX.md` template + the `satisfies:` column extension per CONTEXT.md `## Specifics`. Use the `Write` tool.

    **Section 1 — Frontmatter:**

    ```yaml
    ---
    phase: 05-hardening-manual-qa-release-v3
    plan: 05
    type: verification-matrix
    status: scaffolded
    created: <ISO 8601>
    walked: pending
    walked_against_sha: <PLAN05_03_SHA from Task 0>
    backend_walked_against_sha: <BACKEND_SHA from Task 0 / 05-PREFLIGHT.md>
    walk_disposition: pending
    walker: beckprograms@gmail.com
    devices:
      ios:
        model: iPhone 15 Pro Max
        os: iOS 26.x
        build: Fabric / Release
        binary: 3.0.0 build <next_ios_build_number from 05-STORE-HISTORY.md>
      android:
        model: Moto G XT2513V
        os: Android 16
        build: Fabric / Release
        binary: 3.0.0 versionCode <next_android_version_code from 05-STORE-HISTORY.md>
    accounts:
      admin: beckprograms@gmail.com
      moderator: "<existing M2 mod OR promoted at walk time via Phase 5 RoleManagementScreen>"
      owner_with_rejected: "<created at walk time via Phase 3 reject-flow OR existing>"
      plain_user: "<test account>"
    requirements_addressed: [REL-03]
    testing_bar: M3 manual physical-device QA per CLAUDE.md
    walk_scope: ~60-cell M3 cross-cutting (Phases 1-4 deferred walks + REL-03 happy-path)
    paired_gates_prerequisite_clear: true   # Phase 4 closed paired-gates per 05-STORE-HISTORY.md
    matrix_count: 7
    totals:
      total_cells: ~60   # populate exact count after walk completes
      pass: <int>
      partial: <int>
      deferred_user_approved: <int>
      fail: <int>
      na: <int>
    deferrals:
      race_cells:
        cells: ["matrix_1_concurrent_demote", "matrix_2_two_device_race", "matrix_5_socket_attacker"]
        rationale: "M4+ deferred per memory `phase06-m3-carry-forward.md` + M2 Phase 6 precedent. Backend supertest covers the atomic invariants (MOD-15 findOneAndUpdate guard + ROLE-11 roleRevokedAt invariant). Physical-device race coordination requires a coordinated-curl test rig (see Future Requirements in REQUIREMENTS.md). User explicitly signed off on M4+ deferral at M2 Phase 6 close."
        follow_up: "M4 backlog: build coordinated-curl test rig with token-capture mechanism."
    partials: {}   # populate during walk if any cell surfaces a minor cosmetic issue (e.g., geocoder Network Error retry-needed per memory)
    ---
    ```

    **Section 2 — Cell Markers + Test Environment + Memory note (verbatim from 06-QA-MATRIX.md:67-74):**

    ```markdown
    # Phase 5 — M3 v3.0.0 QA Matrix

    Authored by Plan 05-05 to walk every M3 surface on iPhone 15 Pro Max + Moto G XT2513V before
    v3.0.0 ships. D-06 makes this the canonical doc — supersedes 4 prior `*-HUMAN-UAT.md` /
    `*-VERIFICATION.md` files (those get a one-line "rolled into 05-QA-MATRIX.md" header note).

    Per D-07 walk-once-count-for-both: each device walk closes a prior-phase deferred entry AND a
    REL-03 cell from the same evidence. The `satisfies:` column on each row makes this dual-coverage
    auditable in a single grep.

    ## Cell verdict legend (D-09 four-state)

    - ✅ PASS — observed expected behavior on the listed device
    - ❌ FAIL — regression observed; bug-fix loop opens
    - ⚠ PARTIAL — main behavior PASS; minor cosmetic/UX issue noted (still ships; logged for M4 polish backlog)
    - 🅓 DEFERRED-USER-APPROVED — formally deferred per M2 Phase 6 precedent; follow-up backlog item recorded
    - — N/A — cell intentionally not applicable on this device (e.g., overlay-hides-tab-bar per memory `nav-overlay-hides-bottom-nav.md`)
    - ☐ Pending — not yet walked

    ## Test Environment

    | Field | Value |
    |-------|-------|
    | iOS device | iPhone 15 Pro Max / iOS 26.x / Fabric Release |
    | Android device | Moto G XT2513V / Android 16 / Fabric Release |
    | Walker | beckprograms@gmail.com |
    | Walked against RN SHA | <PLAN05_03_SHA> |
    | Walked against backend SHA | <BACKEND_SHA> |
    | iOS build | 3.0.0 (<next_ios_build_number>) |
    | Android build | 3.0.0 versionCode <next_android_version_code> |
    | Pre-flight verify-PASS | listings-m3 + landlord-uid-repair + locations-seed all green per 05-PREFLIGHT.md |

    Per memory `nav-overlay-hides-bottom-nav.md`: ContextualListingFlow / MediaCurationScreen / PropertyDetails overlays hide the bottom tab bar by design. Cells that would require seeing the tab bar WHILE an overlay is still open are intentionally `— N/A`. DO NOT mark FAIL.

    Per memory `geocoder-nominatim-hang-axios-network-error.md`: if Step 2 location triggers an axios "Network Error" during a Matrix 1 happy-path walk, retry once is the documented workaround (Nominatim hang). Mark the cell PARTIAL with the retry note in `partials:` frontmatter; do NOT FAIL.

    Per memory `last-admin-lockout-semantics.md`: ADMIN-03 last-admin lockout only fires on Promise.all race or count==1 self-demote. The Matrix 4 ROLE-11 cells walk a NON-last-admin demote; do not expect lockout.
    ```

    **Section 3 — Matrix 1: Happy-path 6-step flow (15 combos × 2 devices = 30 cells)**

    The 15 combinations = 5 property types × 3 deal types. Property types per FLOW-08 matrix: apartment, house, office (or commercial), hotel (or hostel). 5 chosen for cell-budget (apartment + house + office + commercial + hotel — drop hostel since hotel covers the hospitality tier). Deal types: sale + rent_long + rent_daily.

    Each cell walks the full 6-step flow end-to-end as a single PASS/FAIL/PARTIAL verdict per D-08. Sub-step issues noted in cell comments.

    ```markdown
    ## Matrix 1 — Happy-path 6-step contextual flow (REL-03)

    Walks: 15 combinations × 2 devices = 30 cells. Each cell = one device × one (deal_type, property_type) tuple. Walker signs into a non-mod owner account, taps "Create listing", walks all 6 steps to submission, confirms status flips to `pending` in OwnerListings.

    | Cell | Deal type | Property type | Device | Verdict | Evidence | satisfies |
    |------|-----------|---------------|--------|---------|----------|-----------|
    | 1.1  | sale | apartment | iOS | ☐ | | REL-03 cell apartment+sale |
    | 1.2  | sale | apartment | Android | ☐ | | REL-03 cell apartment+sale |
    | 1.3  | rent_long | apartment | iOS | ☐ | | Phase 2 W1 + REL-03 cell apartment+rent_long |
    | 1.4  | rent_long | apartment | Android | ☐ | | Phase 2 W1 + REL-03 cell apartment+rent_long |
    | 1.5  | rent_daily | apartment | iOS | ☐ | | REL-03 cell apartment+rent_daily |
    | 1.6  | rent_daily | apartment | Android | ☐ | | REL-03 cell apartment+rent_daily |
    | 1.7  | sale | house | iOS | ☐ | | REL-03 cell house+sale |
    | 1.8  | sale | house | Android | ☐ | | REL-03 cell house+sale |
    | 1.9  | rent_long | house | iOS | ☐ | | REL-03 cell house+rent_long |
    | 1.10 | rent_long | house | Android | ☐ | | REL-03 cell house+rent_long |
    | 1.11 | sale | office | iOS | ☐ | | REL-03 cell office+sale + Phase 2 W6 propertyType reflow |
    | 1.12 | sale | office | Android | ☐ | | REL-03 cell office+sale + Phase 2 W6 |
    | 1.13 | rent_long | office | iOS | ☐ | | REL-03 cell office+rent_long |
    | 1.14 | rent_long | office | Android | ☐ | | REL-03 cell office+rent_long |
    | 1.15 | sale | commercial | iOS | ☐ | | REL-03 cell commercial+sale |
    | 1.16 | sale | commercial | Android | ☐ | | REL-03 cell commercial+sale |
    | 1.17 | rent_long | commercial | iOS | ☐ | | REL-03 cell commercial+rent_long (verify FLOW-08 conditional sub-fields: rooms + bathroom + kitchen render) |
    | 1.18 | rent_long | commercial | Android | ☐ | | REL-03 cell commercial+rent_long |
    | 1.19 | rent_daily | hotel | iOS | ☐ | | Phase 2 W4 + REL-03 cell hotel+rent_daily (D-19 thin Step 6) |
    | 1.20 | rent_daily | hotel | Android | ☐ | | Phase 2 W4 + REL-03 cell hotel+rent_daily |
    | 1.21 | rent_long | hotel | iOS | ☐ | | REL-03 cell hotel+rent_long |
    | 1.22 | rent_long | hotel | Android | ☐ | | REL-03 cell hotel+rent_long |
    | 1.23 | sale | hotel | iOS | ☐ | | REL-03 cell hotel+sale |
    | 1.24 | sale | hotel | Android | ☐ | | REL-03 cell hotel+sale |
    | 1.25 | rent_daily | apartment | (re-walk EN+RU at W1 Phase 2) iOS | ☐ | | Phase 2 W1 cross-device |
    | 1.26 | rent_daily | apartment | Android | ☐ | | Phase 2 W1 cross-device |
    | 1.27 | sale | hotel (Bishkek districts validator) | iOS | ☐ | | Phase 2 W5 Locations + Bishkek districts |
    | 1.28 | sale | hotel (Bishkek districts validator) | Android | ☐ | | Phase 2 W5 Locations + Bishkek districts |
    | 1.29 | rent_long | apartment (FLOW-06 toggle visible) | iOS | ☐ | | REL-03 exact-address-toggle (visible for non-hotel/hostel) |
    | 1.30 | rent_daily | hotel (FLOW-06 toggle hidden) | Android | ☐ | | REL-03 exact-address-toggle hidden for hotel/hostel + Phase 2 W4 |

    **Walk pattern for each cell:**
    1. Sign in as non-mod owner.
    2. Tap "Create listing" — `<ContextualListingFlow>` opens at Step 1.
    3. Step 1: select Deal Type chip + Property Type chip (per cell row).
    4. Step 2: location autocomplete + map pin + (where applicable) exact-address toggle visible/hidden per FLOW-06.
    5. Step 3: area + price + currency chip + conditional sub-fields per FLOW-08 (apartment/house → rooms only; office/commercial → rooms + bathroom + kitchen; hotel/hostel → hotelRooms + hotelClass).
    6. Step 4: condition + furnished.
    7. Step 5: title + description.
    8. Step 6: deal-conditions matrix per FLOW-11 (Sale → bargain + optional deposit; rent_long → bargain + deposit + prepaymentMonths preset 0/1/2/custom + minTerm; rent_daily → optional deposit only).
    9. Submit.
    10. Verify status flips to `pending` in OwnerListings → Pending tab.
    ```

    **Section 4 — Matrix 2: Edit mode (6 reps × 2 devices = 12 cells)**

    Phase 2 W2 (mode='edit-owner') + W3 (mode='edit-mod') × 3 representative property types (apartment / hotel / commercial) × 2 devices = 12 cells.

    ```markdown
    ## Matrix 2 — Edit mode (FLOW-13 + FLOW-15 + Phase 2 W2 + W3)

    Walks: 6 representative cells × 2 devices = 12 cells. Phase 2 W7-W9 deferred set; modify a pending listing through edit-owner OR edit-mod paths and confirm submission flips status appropriately (M2 MOD-14 semantics preserved per FLOW-15).

    | Cell | Mode | Property type | Device | Verdict | Evidence | satisfies |
    |------|------|---------------|--------|---------|----------|-----------|
    | 2.1  | edit-owner | apartment | iOS | ☐ | | Phase 2 W2 + REL-03 edit-owner cell |
    | 2.2  | edit-owner | apartment | Android | ☐ | | Phase 2 W2 + REL-03 |
    | 2.3  | edit-owner | hotel | iOS | ☐ | | Phase 2 W2 cross-property |
    | 2.4  | edit-owner | hotel | Android | ☐ | | Phase 2 W2 cross-property |
    | 2.5  | edit-mod | apartment (mod-context banner top of Step 1) | iOS | ☐ | | Phase 2 W3 + REL-03 edit-mod cell + FLOW-15 |
    | 2.6  | edit-mod | apartment | Android | ☐ | | Phase 2 W3 + REL-03 |
    | 2.7  | edit-mod | commercial (FLOW-08 sub-fields preserved on edit) | iOS | ☐ | | Phase 2 W3 cross-property |
    | 2.8  | edit-mod | commercial | Android | ☐ | | Phase 2 W3 cross-property |
    | 2.9  | edit-mod-flip-to-live (rejected → edit-on-behalf submit → live) | apartment | iOS | ☐ | | M2 MOD-14 semantic preserved + FLOW-13 |
    | 2.10 | edit-mod-flip-to-live | apartment | Android | ☐ | | Same |
    | 2.11 | edit-owner re-submit rejected listing → status pending; banner auto-dismisses | apartment | iOS | ☐ | | RejectionBanner re-submit path |
    | 2.12 | edit-owner re-submit rejected | apartment | Android | ☐ | | Same |
    ```

    **Section 5 — Matrix 3: Mod media curation (4 walks × 2 devices = 8 cells)**

    Closes Phase 3's 6 deferred walks. The 4 unique walks × 2 devices = 8 cells; 2 walks (cap-overflow + EN+RU) cover 2 walks each.

    ```markdown
    ## Matrix 3 — Mod media curation (Phase 3 deferred 6-walk set + REL-03)

    Walks: 4 walks × 2 devices = 8 cells. Sign in as moderator; navigate to ModerationQueueScreen → "needs media" filter chip → tap a row with photoCount === 0 → MediaCurationScreen overlay opens → upload + manage media + return to queue.

    | Cell | Walk | Device | Verdict | Evidence | satisfies |
    |------|------|--------|---------|----------|-----------|
    | 3.1  | filter-needs-media chip predicate (chip filters to status: pending AND media.photos.length === 0; tourUrl + videos do NOT count per Open Question #1) | iOS | ☐ | | Phase 3 deferred walk: filter-needs-media + has-media-filter (Open Question #1 negative) |
    | 3.2  | filter-needs-media chip predicate | Android | ☐ | | Same |
    | 3.3  | MEDIA_REQUIRED 400 UX block (mod tries to Approve a listing with photoCount=0 from MediaCurationScreen + PropertyDetailsScreen + ModerationQueueScreen row-Approve — all 3 surfaces should show disabled-Approve OR clear toast on attempted POST per D-12) | iOS | ☐ | | Phase 3 deferred walk: MEDIA_REQUIRED 400 UX (D-12 3-surface gate) |
    | 3.4  | MEDIA_REQUIRED 400 UX block | Android | ☐ | | Same |
    | 3.5  | EN+RU parity for media curation strings (32 keys per locale per Phase 3 close — switch app locale to RU mid-MediaCurationScreen, confirm strings re-render in Cyrillic) + cap-overflow toast (D-05 client-side cap; mod attempts to upload more than the cap, sees translated truncation toast) | iOS | ☐ | | Phase 3 deferred walks: EN+RU parity + cap-overflow toast |
    | 3.6  | EN+RU + cap-overflow | Android | ☐ | | Same |
    | 3.7  | per-asset DELETE round-trip (mod uploads 3 photos; deletes 1 via swipe/long-press; verifies remaining 2 persist via re-fetch + DELETE /api/moderation/listings/:id/media?url=...&kind=photo round-trip) | iOS | ☐ | | Phase 3 deferred walk: per-asset DELETE round-trip |
    | 3.8  | per-asset DELETE round-trip | Android | ☐ | | Same |
    ```

    **Section 6 — Matrix 4: ROLE-11 demote-mid-action recovery (2 paired-device cells)**

    Closes Phase 4 CARRY-01 banner-latency-on-iPhone deferred walk. Both cells require two devices simultaneously (Device A is the demoting admin; Device B is the demoted moderator with a popup OPEN at demote moment).

    ```markdown
    ## Matrix 4 — ROLE-11 demote-mid-action recovery (CARRY-01)

    Walks: 2 paired-device cells. Device A (admin, iOS) demotes Device B (moderator) while B has a mod-action popup OPEN. CARRY-01 invariant: B's popup loading state resets, popup closes, RoleRefreshBanner surfaces within ~1s.

    | Cell | Scenario | Device A | Device B (demoted) | Verdict | Evidence | satisfies |
    |------|----------|----------|---------------------|---------|----------|-----------|
    | 4.1  | A demotes B; B has Approve popup open in ModerationQueueScreen → on B: popup closes; RoleRefreshBanner surfaces ≤1s; tap-to-reload works without app restart | iOS (admin) | iPhone 15 Pro Max (mod) | ☐ | | Phase 4 CARRY-01 deferred banner-latency on iPhone + REL-03 |
    | 4.2  | A demotes B; B has Reject popup open in PropertyDetailsScreen → same recovery semantics | iOS (admin) | Moto G XT2513V (mod) | ☐ | | CARRY-01 cross-device coverage |
    ```

    **Section 7 — Matrix 5: Phase 4.5 uid-mismatch repair (1 cell)**

    Closes Phase 4 CARRY-02 SC#3 deferred walk. End-to-end live Atlas walk: re-submit a landlord application from a fresh Firebase user; verify the Atlas-stored uid matches `firebase.user.localId`.

    ```markdown
    ## Matrix 5 — Phase 4.5 uid-mismatch repair (CARRY-02 SC#3)

    Walks: 1 cell (live Atlas walk). The supertest already proves the invariant in-memory; this cell walks the device-to-Atlas round-trip end-to-end after the Plan 05-01 uid-repair migration has run.

    | Cell | Scenario | Device | Verdict | Evidence | satisfies |
    |------|----------|--------|---------|----------|-----------|
    | 5.1  | Sign up as a new Firebase user → submit landlord application via the in-app flow → query Atlas (mongosh / temp admin debug endpoint / user-relay): the LandlordApplication doc's `uid` MUST equal `firebase.user.localId` from the device's auth state | iPhone 15 Pro Max | ☐ | | Phase 4 CARRY-02 SC#3 + REL-03 |
    ```

    **Section 8 — Matrix 6: EN+RU parity sweep (~8 cells)**

    Russian-language session covering the M3-NEW screens (the parity for M2 + M1 screens is presumed-stable). Add 4 cells × 2 devices = 8.

    ```markdown
    ## Matrix 6 — EN+RU parity (M3-new surfaces)

    Walks: 4 cells × 2 devices = 8 cells. Switch app locale to RU mid-session, verify all M3-new strings render in Cyrillic.

    | Cell | Screen | Device RU | Verdict | Evidence | satisfies |
    |------|--------|-----------|---------|----------|-----------|
    | 6.1  | ContextualListingFlow steps 1-6 (all step headers + chip labels + validation toasts) | iOS | ☐ | | FLOW-16 + REL-03 EN+RU parity |
    | 6.2  | ContextualListingFlow | Android | ☐ | | Same |
    | 6.3  | MediaCurationScreen + NeedsMediaBanner + filter chip row labels (Phase 3 32-keys-per-locale) | iOS | ☐ | | MEDIA-09 + REL-03 |
    | 6.4  | MediaCurationScreen | Android | ☐ | | Same |
    | 6.5  | RoleRefreshBanner copy on demote (CARRY-01) | iOS | ☐ | | REL-03 RU parity for ROLE-11 surface |
    | 6.6  | RoleRefreshBanner | Android | ☐ | | Same |
    | 6.7  | RejectionBanner copy in RU (M2 carry; verify Phase 4.5 paths render correctly post-migration) | iOS | ☐ | | M2 carry + REL-03 |
    | 6.8  | RejectionBanner | Android | ☐ | | Same |
    ```

    **Section 9 — Matrix 7: Dark/light parity (~8 cells)**

    Dark-theme session covers M3-new surfaces. Per CLAUDE.md "use `useTheme()` tokens — no hardcoded colors; dark/light parity required" — Phase 3 already used theme tokens (W6 zero-hex audit per 03-06-SUMMARY); this matrix is the empirical confirmation.

    ```markdown
    ## Matrix 7 — Dark/light parity (M3-new surfaces only)

    Walks: 4 cells × 2 devices = 8 cells. Toggle device-level dark mode; verify M3-new surfaces have theme parity.

    | Cell | Surface | Device dark | Verdict | Evidence | satisfies |
    |------|---------|-------------|---------|----------|-----------|
    | 7.1  | ContextualListingFlow steps 1-6 (chips + map pin + currency chip + Step 6 prepaymentMonths preset) | iOS dark | ☐ | | REL-03 dark parity for 6-step flow |
    | 7.2  | ContextualListingFlow | Android dark | ☐ | | Same |
    | 7.3  | MediaCurationScreen photo grid + 2-button action footer + cap-overflow toast (W6 zero-hex audit per Phase 3 03-05/03-06) | iOS dark | ☐ | | REL-03 dark parity for media curation |
    | 7.4  | MediaCurationScreen | Android dark | ☐ | | Same |
    | 7.5  | NeedsMediaBanner + ModerationQueueScreen filter chip row | iOS dark | ☐ | | REL-03 |
    | 7.6  | NeedsMediaBanner + filter row | Android dark | ☐ | | Same |
    | 7.7  | RoleRefreshBanner contrast on demote | iOS dark | ☐ | | REL-03 |
    | 7.8  | RoleRefreshBanner | Android dark | ☐ | | Same |
    ```

    **Section 10 — DEFERRED-USER-APPROVED block (race cells per memory `phase06-m3-carry-forward.md`):**

    ```markdown
    ## DEFERRED-USER-APPROVED — race cells (M4+)

    Per memory `phase06-m3-carry-forward.md` + M2 Phase 6 precedent (cells 1.7 + 2.4-2.6 + 5.6 + 5.3 mismatch path), the following race-condition cells stay M4+ deferred. The user explicitly signed off on this deferral at M2 Phase 6 close; the deferral carries forward to M3 unchanged.

    | Cell ID | Description | Reason for deferral | Backend coverage |
    |---------|-------------|---------------------|------------------|
    | M3-RACE-01 | Concurrent demote race (two admins simultaneously demote target moderator) | Physical-device race coordination requires coordinated-curl test rig | Backend supertest covers ROLE-11 atomic invariant |
    | M3-RACE-02 | Two-device mod-action race (Device A approves / Device B rejects same listing simultaneously) | Same — race rig needed | Backend supertest covers MOD-15 findOneAndUpdate guard |
    | M3-RACE-03 | Socket attacker (curl + websocat scripted; HF-04 verification) | Tooling-driven; out of REL-03 device-walk scope | Backend supertest covers HF-04 socket handshake JWKS |
    | M3-RACE-04 | HF-03 mismatch path attacker token | Tooling-driven; out of REL-03 device-walk scope | Backend supertest covers HF-03 firebaseUid mismatch 403 |

    These cells are walked NOT walked in Phase 5. They re-open in M4 when the coordinated-curl race rig is built (REQUIREMENTS.md Future Requirements).
    ```

    **Section 11 — Sign-off block:**

    ```markdown
    ## Sign-off

    | Field | Value |
    |-------|-------|
    | Walker | beckprograms@gmail.com |
    | Walk start | <ISO 8601> |
    | Walk end | <ISO 8601> |
    | Total cells walked | <count> |
    | PASS | <count> |
    | PARTIAL | <count, with brief rationale per cell> |
    | DEFERRED-USER-APPROVED | 4 (race cells, M4+) |
    | FAIL | <count, MUST be 0 for APPROVED disposition> |
    | N/A | <count, with brief rationale per N/A cell> |
    | Disposition | <APPROVED \| BLOCKED-BY-FAIL> |

    Bug-fix loop (per M1 D-08): If any cell FAILs, the executor opens a sub-task to fix the regression in-phase, re-walks the affected cell on both devices, then resumes. APPROVED disposition requires zero blocking FAILs. PARTIAL cells ship (logged for M4 polish backlog).

    ## Re-open conditions

    - If a backend redeploy lands during the walk, the matrix's `backend_walked_against_sha` is stale; re-walk affected cells against the new SHA.
    - If Plan 05-03's atomic bump is amended (force-push), the matrix's `walked_against_sha` is stale; re-walk all cells.
    - If the M4 race-cell rig is built, the DEFERRED-USER-APPROVED cells re-open and walk in that future phase.

    ## Cross-references

    - M2 Phase 6 precedent: `.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md`
    - Memory `nav-overlay-hides-bottom-nav.md` (overlay-hides-tab-bar invariant — N/A cell rationale)
    - Memory `phase06-m3-carry-forward.md` (M4+ race-cell deferral — DEFERRED-USER-APPROVED rationale)
    - Memory `geocoder-nominatim-hang-axios-network-error.md` (PARTIAL classification path for retry-on-Network-Error)
    - Memory `last-admin-lockout-semantics.md` (ADMIN-03 semantic clarification)
    - 02-HUMAN-UAT.md (12 deferred walks rolled forward — Matrices 1 + 2)
    - 03-VERIFICATION.md `deferred:` (6 deferred walks rolled forward — Matrix 3)
    - 04-VERIFICATION.md (2 deferred walks rolled forward — Matrices 4 + 5)
    - 05-PREFLIGHT.md (pre-flight gate `health_check_status: 200`)
    - 05-STORE-HISTORY.md (build identity binding values)
    ```

    **Step 12 — Tag prior-phase doc headers (D-06 cleanup):**

    Update the 4 prior `*-HUMAN-UAT.md` / `*-VERIFICATION.md` files with a one-line header note (right after the YAML frontmatter, before the first H1):

    ```markdown
    > **Status: rolled into `.planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` 2026-05-XX (D-06). Do not update further. Phase 5 owns these walks now.**
    ```

    Apply to:
    - `.planning/phases/01-schema-reshape-backend-route-shape-cutover/01-HUMAN-UAT.md` (4 operator items rolled into 05-PREFLIGHT.md sections, not the matrix per se — but close the doc with the same note for archive cleanliness)
    - `.planning/phases/02-6-step-contextual-listing-flow-client/02-HUMAN-UAT.md` (12 deferred walks → Matrix 1 + 2)
    - `.planning/phases/03-media-flow-inversion-admin-mod-curation/03-VERIFICATION.md` (6 deferred walks → Matrix 3 — add the note at the start of the `## Manual Physical-Device QA Items (deferred to Phase 5 REL-03)` section, NOT the file header, so the verification evidence above stays primary)
    - `.planning/phases/04-m2-carry-forward-bug-fixes/VERIFICATION.md` (2 deferred walks → Matrix 4 + 5 — same as Phase 3, add note at the relevant deferred-rows section)

    Use the Edit tool with `old_string` matching a unique anchor in each file (the first heading line is usually unique enough).

    Use the Write tool for the 05-QA-MATRIX.md scaffold. After scaffold is committed, the user walks the cells in Task 2 and updates the matrix in place. The Plan 05-05 close-out commit lands the walked matrix.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md && [ "$(grep -c 'iPhone 15 Pro Max' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md)" -ge "1" ] && [ "$(grep -c 'Moto G XT2513V' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md)" -ge "1" ] && [ "$(grep -c '## Matrix' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md)" -ge "7" ] && [ "$(grep -c 'satisfies:' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md)" -ge "20" ] && [ "$(grep -c 'DEFERRED-USER-APPROVED' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md)" -ge "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `05-QA-MATRIX.md` exists with 7 matrices (Happy-path / Edit mode / Mod media curation / ROLE-11 / Phase 4.5 uid / EN+RU / Dark+Light)
    - `grep -c '## Matrix' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns ≥ 7
    - `grep -c 'iPhone 15 Pro Max' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns ≥ 1 (Test Environment table)
    - `grep -c 'Moto G XT2513V' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns ≥ 1
    - `grep -E '^\| ([0-9]+\.[0-9]+) ' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md | wc -l` returns ≥ 50 (total cell row count across all matrices; D-08 estimate is ~60; allow some N/A consolidation)
    - `grep -c 'satisfies:' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns ≥ 50 (CONTEXT.md `## Specifics` cell-tagging convention — every cell row carries a satisfies entry; allows greppable dual-coverage audit per D-07)
    - `grep -c 'satisfies: Phase 2' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns ≥ 6 (Phase 2 W1-W6 closures distributed across Matrices 1 + 2)
    - `grep -c 'satisfies: Phase 3' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns ≥ 2 (Phase 3 deferred 6-walk set distributed across Matrix 3)
    - `grep -c 'satisfies: Phase 4' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns ≥ 2 (Phase 4 CARRY-01 + CARRY-02 in Matrices 4 + 5)
    - `grep -c 'walk_disposition: pending' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns 1 in scaffold form (flips to APPROVED at walk close)
    - `grep -c 'paired_gates_prerequisite_clear: true' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns 1
    - `grep -c '## DEFERRED-USER-APPROVED' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns 1 (race-cell deferral block)
    - frontmatter `walked_against_sha:` is populated with Plan 05-03's commit SHA (a real 7+ hex char SHA, not `<SHA>` placeholder)
    - frontmatter `backend_walked_against_sha:` is populated with the Railway deploy SHA from 05-PREFLIGHT.md
    - 4 prior phase docs (01-HUMAN-UAT.md, 02-HUMAN-UAT.md, 03-VERIFICATION.md, 04-VERIFICATION.md) each carry a "rolled into 05-QA-MATRIX.md" header note — `grep -l 'rolled into.*05-QA-MATRIX.md' .planning/phases/01-* .planning/phases/02-* .planning/phases/03-* .planning/phases/04-* | wc -l` returns ≥ 4
    - `grep -c '## Re-open conditions' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns 1
    - `grep -c '## Cross-references' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns 1
    - `grep -c 'verdict legend\|Cell verdict legend' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns ≥ 1 (D-09 four-state markers documented)
  </acceptance_criteria>
  <done>
    `05-QA-MATRIX.md` is committed in scaffolded form with 7 matrices + DEFERRED-USER-APPROVED block + every cell row carrying a `satisfies:` tag. The 4 prior phase docs are tagged with the rollup note. The user receives a walk-through-this-matrix instruction in the next checkpoint task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walk the QA matrix on iPhone 15 Pro Max + Moto G XT2513V (REL-03)</name>
  <what-built>
    Plan 05-03 produced a v3.0.0 binary; Plan 05-01 deployed the matching backend + ran both pre-flight migrations + locations seed. Plan 05-05 Task 1 scaffolded the QA matrix in `05-QA-MATRIX.md` with 7 matrices + ~60 cells + satisfies-tag column. This checkpoint pauses execution while the user walks every cell on both physical devices and flips the cell markers.
  </what-built>
  <how-to-verify>
    1. **Build + install v3.0.0 on both devices:**
       - iOS: open `ios/JayTap.xcworkspace` in Xcode 26.4+, select iPhone 15 Pro Max (physical device, USB-connected or wireless paired), Product → Run (Release scheme). Confirm app launches with build identity `3.0.0 (<next_ios_build_number>)` shown in About / debug surface (cross-reference against `05-STORE-HISTORY.md` derived target).
       - Android: connect Moto G XT2513V via USB; `cd android && ./gradlew assembleRelease && adb install -r app/build/outputs/apk/release/app-release.apk` (or use `npx react-native run-android --variant=release`). Confirm app launches with `3.0.0` versionName + correct versionCode in About / debug surface.

    2. **Walk every matrix in `05-QA-MATRIX.md`** — each cell is 1 row in 1 of the 7 matrices. For each cell:
       - Perform the listed steps on the listed device (refer to the Walk Pattern in Section 3 for Matrix 1 happy-path; other matrices have inline scenario columns).
       - Mark cell verdict per D-09 four-state: ✅ PASS or ❌ FAIL or ⚠ PARTIAL or — N/A (with rationale).
       - Update the cell's `Evidence` column with screenshot path / observed value / response code.
       - For PARTIAL cells, populate `partials:` frontmatter with the issue + cell ID + cosmetic-vs-functional classification.
       - For FAIL cells, attach evidence (screenshot path under `.planning/phases/05-hardening-manual-qa-release-v3/videos/` if needed; the videos dir is created on demand). The bug-fix loop opens (see step 4).

    3. **Known-issue handling per memories:**
       - If any Matrix 1 / 2 walk hits an axios "Network Error" on Step 2 location autocomplete, retry once is the documented workaround per memory `geocoder-nominatim-hang-axios-network-error.md`. Mark the cell PARTIAL with the retry note; do NOT mark FAIL.
       - If a cell's required affordance is hidden by an overlay (e.g., a tab-bar element while ContextualListingFlow is open), mark — N/A per memory `nav-overlay-hides-bottom-nav.md`; do NOT mark FAIL.
       - For Matrix 4 ROLE-11 cells, ADMIN-03 last-admin lockout will NOT fire (memory `last-admin-lockout-semantics.md` — only fires on count==1 self-demote or Promise.all race). The walk is for non-last-admin demote; lockout-not-firing is the EXPECTED behavior, not a FAIL.

    4. **Bug-fix loop (per M1 D-08):** If any cell FAILs, surface to the executor — the executor opens a sub-task to fix the regression in-phase, commits the fix atomically, then asks the walker to re-walk the affected cell on both devices.

    5. **Sign-off:**
       - Fill the Sign-off block at the bottom of `05-QA-MATRIX.md`: walker name (`beckprograms@gmail.com`), walk start + end timestamps, PASS / PARTIAL / DEFERRED-USER-APPROVED / FAIL / N/A counts, disposition.
       - Disposition is APPROVED iff FAIL count = 0 (post-fix-loop). PARTIAL cells ship (logged for M4 polish backlog). If any blocking FAIL remains, disposition is BLOCKED-BY-FAIL and the plan does NOT close.

    6. **Update frontmatter:**
       - `walk_disposition: pending` → `walk_disposition: APPROVED` (or `BLOCKED-BY-FAIL`).
       - `walked: pending` → `walked: <ISO 8601 walk-end timestamp>`.
       - `totals: { total_cells, pass, partial, deferred_user_approved, fail, na }` populated with actual counts.

    Resume by replying with `approved` once the matrix is walked and the disposition is APPROVED. If a FAIL is unresolvable in-phase (e.g., requires a Phase 5.5 backlog), the user explicitly downgrades the disposition and surfaces the carry-forward.
  </how-to-verify>
  <resume-signal>
    Reply with `approved` (after matrix walk + sign-off lands APPROVED), OR describe the blocking FAILs that need fix-loop iteration with cell IDs + observed-vs-expected.
  </resume-signal>
  <acceptance_criteria>
    - `grep -E '^\s*walk_disposition:\s*APPROVED\s*$' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns 1 line (operator-confirmed walk close)
    - `grep -E '^\s*walked:\s*[0-9]{4}-[0-9]{2}' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns 1 line (real ISO8601 timestamp, not pending)
    - The Sign-off block's FAIL row records `0` (zero blocking FAILs — APPROVED requires this)
    - `grep -cE 'PASS|PARTIAL|DEFERRED-USER-APPROVED|N/A|FAIL' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns ≥ 50 (every cell has a verdict — none left as `☐ Pending`)
    - `grep -c '☐ Pending' .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md` returns 0 (no pending cells in walked state — except the 4 race cells in DEFERRED-USER-APPROVED, which are `🅓` not `☐`)
    - Operator-confirmed observation: walker (beckprograms@gmail.com) walked all non-DEFERRED cells across both physical devices (iPhone 15 Pro Max + Moto G XT2513V) within the recorded `walked` timestamp window
    - Operator-confirmed observation: build identity at walk time matches `05-STORE-HISTORY.md` derived targets — iOS About showed `3.0.0 (<next_ios_build_number>)`; Android About showed `versionName 3.0.0 + versionCode <next_android_version_code>`
    - Operator-confirmed observation: any PARTIAL cells are recorded in `partials:` frontmatter with cosmetic-vs-functional classification and an M4-polish-backlog item description
    - The matrix's `totals:` frontmatter integers sum to the total cell count (`pass + partial + deferred_user_approved + fail + na == total_cells`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 05-03 binary → walked binary | The matrix walks the v3.0.0 archive built from Plan 05-03's commit SHA. Walking a stale binary (e.g., v2.0.0 still installed) would invalidate the entire matrix; the build-identity assertion in Step 1 of Task 2 guards this. |
| Plan 05-01 backend → walked backend | The matrix walks against the live Railway deploy from Plan 05-01. A backend rollback between Plan 05-01 close and Plan 05-05 walk would mean the QA results don't apply to the production state Plan 05-07 will submit against. |
| Walker self-approval → rubber-stamp risk | The user is the walker; the executor cannot self-approve. The resume signal `approved` is the user's binding sign-off. |
| Prior-phase deferred walks → satisfies-tag audit | D-07 walk-once-count-for-both depends on the satisfies-tag column being populated and accurate. A typo'd `satisfies: Phase 2 W7` (when actually closing W2) would corrupt the dual-coverage audit trail. |
| Race cells → M4+ deferral | The DEFERRED-USER-APPROVED block is binding — the matrix MUST NOT walk the race cells in Phase 5. Walking them with a non-rig setup would produce noisy data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-29 | Build-identity drift | Walker walks stale binary (v2.0.0 still installed) | mitigate | Step 1 of Task 2 requires the device's About screen to show `3.0.0 (<next_ios_build_number>)` / `versionName 3.0.0 + correct versionCode`. The executor cross-references this against `05-STORE-HISTORY.md` derived targets. If the device shows v2.0.0, the walker re-installs the v3.0.0 archive before walking. |
| T-05-30 | Tampering | walk_disposition flipped to APPROVED without walking | mitigate | The user is the walker; the executor cannot self-approve. The resume signal `approved` is the user's binding sign-off. The frontmatter `walk_disposition: pending` flips ONLY by the user's edit. Plan 05-07 (downstream) reads this disposition as a binding gate. |
| T-05-31 | Spoofing | satisfies-tag mismatch (cell claims to satisfy a prior-phase entry it doesn't actually exercise) | mitigate | Acceptance criterion `grep -c 'satisfies: Phase [0-9]'` returns ≥ 10 (auditable). The cross-references section explicitly maps each prior-phase deferred entry to its satisfying cell ID; a future auditor can verify by re-reading the cell scenario column against the prior-phase walk description. |
| T-05-32 | Repudiation | Walk evidence missing | mitigate | The matrix file is committed to git after walk completion. Verdict marks + sign-off block are auditable. For FAIL cells, evidence (screenshots / video clips) lives under `.planning/phases/05-hardening-manual-qa-release-v3/videos/` per M2 precedent. |
| T-05-33 | Information Disclosure | Test accounts' real emails / Mongo uids in committed matrix | accept | The matrix mentions `beckprograms@gmail.com` (the user's own email — already in committed git history per M1 + M2 precedent). For non-admin walks, refers to "<test account>" placeholders. No real PII for end users is committed. |
| T-05-34 | DoS | Geocoder Network Error makes walk untrackable | mitigate | Memory `geocoder-nominatim-hang-axios-network-error.md` documents the retry-once workaround. Cells affected by this are marked PARTIAL with the retry note in `partials:` frontmatter; do NOT mark FAIL. M4 backlog: add 5s AbortController to backend geocoder. |
| T-05-35 | Tampering | Race cell walked despite DEFERRED-USER-APPROVED | mitigate | The DEFERRED-USER-APPROVED block in Section 10 explicitly lists the 4 race cells with cell IDs (`M3-RACE-01..04`) and their backend-supertest coverage. The matrix totals row records `deferred_user_approved: 4` as a hard count. If the walker accidentally walks a race cell and gets a noisy result, the cell ID prefix (`M3-RACE-*`) makes it greppable for removal. |
| T-05-36 | Build-identity drift | Backend redeploy mid-walk | mitigate | Frontmatter `backend_walked_against_sha:` is set at walk start (Task 0 step 4). If a Railway redeploy lands during the walk window, the Re-open conditions in Section 11 instruct re-walking affected cells against the new SHA. Plan 05-01's deviation_protocol prevents mid-walk redeploys (the runbook is sequential: pre-flight green → walks → submission). |

Block-on severity: high.
- T-05-29 (HIGH) — blocks if mitigation absent (false-positive QA on stale binary)
- T-05-30 (HIGH) — blocks if mitigation absent (rubber-stamp approval defeats the gate)
- T-05-35 (HIGH) — blocks if mitigation absent (noisy race-cell data corrupts the matrix; user M2 sign-off deferral was load-bearing)
</threat_model>

<verification>
- `05-QA-MATRIX.md` exists with 7 matrices, all cells walked
- `walk_disposition: APPROVED` lands in frontmatter
- Zero blocking FAILs
- Both devices walked (matrix has both `iPhone 15 Pro Max` and `Moto G XT2513V` cell rows populated)
- Every cell row carries a `satisfies:` tag (D-07 dual-coverage audit)
- Phase 2 W1-W6 closures (≥ 6 satisfies-tags), Phase 3 6-walk set closures (≥ 2 satisfies-tags via 2 walks each), Phase 4 CARRY-01 + CARRY-02 closures (≥ 2 satisfies-tags)
- DEFERRED-USER-APPROVED block records 4 race cells with M2 precedent rationale
- 4 prior phase docs tagged with rollup note (D-06)
- Build identity confirmed at walk time on both devices (3.0.0 + correct build/versionCode)
</verification>

<success_criteria>
Plan 05-06 (paired-gate verifier+reviewer) reads `walk_disposition: APPROVED` from `05-QA-MATRIX.md` as one of its inputs. Plan 05-07 (Wave 4 — store submission) reads the same disposition as a binding gate before archive build + store upload. The matrix is the binding evidence that v3.0.0 ships with M3's full surface working end-to-end on both target devices, AND that the 20 deferred device-walks from Phases 1-4 are formally closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-manual-qa-release-v3/05-05-SUMMARY.md` with: walker, walk start + end, PASS/PARTIAL/DEFERRED/FAIL/N-A counts per matrix, disposition, the v3.0.0 binary's git SHA + backend deploy SHA, the satisfies-tag dual-coverage audit (Phase 2 W1-W6 + Phase 3 6-walk + Phase 4 CARRY-01 + CARRY-02 all closed via specific cells), any PARTIAL cells with M4-polish-backlog items, and the close-out commit SHA.
</output>
</content>
</invoke>