---
phase: 04-archive-lifecycle-owner-mod-admin
plan: 07
type: execute
wave: 4
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts
autonomous: false
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05]
must_haves:
  truths:
    - "RenterListingsScreen no longer mounts DeletePropertyModal or has handleDeleteProperty/propertyToDelete/deleteModalVisible state (D-10 + D-14, Pitfall 6 atomic strip)"
    - "RenterListingsScreen passes onArchive=handleArchiveProperty + onUnarchive={canSelfRestore? : undefined} on PropertyCard + HospitalitySection (D-11 + D-13, Pitfall 4 conditional gate)"
    - "PropertyDetailsScreen mod-action footer extends with Archive (mod), Restore (mod), Hard-Delete (admin) buttons gated by useRole + status (D-06)"
    - "PropertyDetailsScreen mounts ArchiveListingModal + DeletePropertyModal as siblings to existing RejectListingModal (D-08 + D-10)"
    - "5 existing locale key VALUES rewritten in BOTH en.ts + ru.ts (D-12 preserves key NAMES per Pitfall 3): unarchiveDialogTitle, unarchiveDialogMessage, unarchiveConfirm, unarchivedToastTitle, unarchivedToastMessage"
    - "App.tsx LOC stays at baseline 1191 (D-19 net-zero target, no new useState, OVERLAY_FLAGS, or back-handler additions)"
    - "Manual physical-device smoke matrix walked APPROVED on iPhone 15 Pro Max (Moto G XT2513V deferred to Phase 6)"
  artifacts:
    - path: "src/screens/RenterListingsScreen.tsx"
      provides: "owner-archive UX rewired; owner hard-delete stripped; onArchive + canSelfRestore-gated onUnarchive props re-mounted"
      contains: "canSelfRestore"
    - path: "src/screens/PropertyDetailsScreen.tsx"
      provides: "mod-action footer extension with Archive/Restore/Hard-Delete; ArchiveListingModal + DeletePropertyModal mounts"
      contains: "ArchiveListingModal"
    - path: "src/locales/en.ts"
      provides: "5 D-12 value rewrites (key names preserved)"
      contains: "back to the moderation queue"
    - path: "src/locales/ru.ts"
      provides: "5 D-12 value rewrites (key names preserved)"
      contains: "очередь модерации для проверки"
  key_links:
    - from: "RenterListingsScreen.handleArchiveProperty"
      to: "PropertyService.archiveOwnListing (Plan 05)"
      via: "onPress callback in Alert.alert flow"
      pattern: "PropertyService\\.archiveOwnListing"
    - from: "RenterListingsScreen.handleUnarchiveProperty"
      to: "PropertyService.restoreListing (Plan 05)"
      via: "onPress callback in Alert.alert flow"
      pattern: "PropertyService\\.restoreListing"
    - from: "PropertyDetailsScreen mod footer"
      to: "useRole + property.status predicates"
      via: "showArchiveBtn / showRestoreBtn / showHardDeleteBtn render predicates"
      pattern: "can\\('archiveAnyListing'\\)"
    - from: "PropertyDetailsScreen ArchiveListingModal mount"
      to: "PropertyService.archiveAnyListing (Plan 05)"
      via: "handleModArchiveSubmit onSubmit callback"
      pattern: "PropertyService\\.archiveAnyListing"
---

<objective>
Wave-4 client (depends on Plans 02 + 03 + 04 + 05 + 06): wire all client surfaces to the Wave-2 backend routes through the Plan 05 service layer + Plan 06 modal. This is the screen-level integration plan that makes the archive lifecycle observable end-to-end.

Three coordinated changes plus the locale value rewrite must land atomically:
1. PropertyDetailsScreen.tsx footer extension (D-06): mod/admin sees Archive button when status not 'archived'; Restore button when status is 'archived'; admin sees Hard-Delete button regardless of status (backend HARD_DELETE_REQUIRES_ARCHIVED still gates non-archived); mounts ArchiveListingModal + DeletePropertyModal as siblings to existing RejectListingModal.
2. RenterListingsScreen.tsx owner-side rewire (D-09 + D-10 + D-11 + D-13 + D-14): rewires handleArchiveProperty to call PropertyService.archiveOwnListing + handleUnarchiveProperty to call PropertyService.restoreListing; STRIPS handleDeleteProperty + confirmDelete + propertyToDelete + deleteModalVisible state + DeleteListingModal mount + onDelete prop on PropertyCard/HospitalitySection (Pitfall 6 atomic strip); RE-MOUNTS onArchive + onUnarchive=canSelfRestore-gated props (D-13 conditional gate per Pitfall 4).
3. Locale value rewrite (D-12 + Pitfall 3): rewrite the 5 existing property.unarchive* key VALUES in both en.ts and ru.ts to set the queue expectation cleanly per ARCH-04 verbatim ("It will go back to the moderation queue for review before becoming public again"). Key NAMES preserved (no rename cascade through call sites).

Then a manual physical-device QA checkpoint walks the 11-row smoke matrix on iPhone 15 Pro Max (Moto G XT2513V deferred to Phase 6 release-gate per Phase 3 escape-hatch precedent).

Output:
- RenterListingsScreen.tsx: net -10 to -25 LOC (strip handlers + state + modal mount; add archive props; declare canSelfRestore helper); Pitfall 6 grep gate returns 0 hits for handleDeleteProperty/propertyToDelete/deleteModalVisible/DeleteListingModal
- PropertyDetailsScreen.tsx: +50-80 LOC (state cluster + handlers + footer JSX + 2 modal mounts + imports)
- en.ts + ru.ts: 5 keys × 2 locales = 10 strings VALUE-rewritten; key NAMES preserved; i18n parity gate exits 0
- Manual physical-device QA matrix walked APPROVED on iPhone 15 Pro Max

Purpose: covers all 5 Phase 4 requirements end-to-end as observable UX (ARCH-01..ARCH-05) AND closes the validation loop with manual verification on iPhone (per Phase 3 SUMMARY precedent + CONVENTIONS.md "manual physical-device QA bar"). The plan is non-autonomous because of the closing checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-CONTEXT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-RESEARCH.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-PATTERNS.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-VALIDATION.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx
@/Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx
@/Users/beckmaldinVL/development/mobileApps/JayTap/src/components/PropertyCard.tsx
@/Users/beckmaldinVL/development/mobileApps/JayTap/src/components/RejectListingModal.tsx
@/Users/beckmaldinVL/development/mobileApps/JayTap/src/components/DeleteListingModal.tsx
@/Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts
@/Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts

<interfaces>
D-12 EN target values (rewrite VALUES, preserve key NAMES):
- property.unarchiveDialogTitle: 'Restore Listing' (was 'Unarchive Listing')
- property.unarchiveDialogMessage: 'Restore this listing? It will go back to the moderation queue for review before becoming public again.'
- property.unarchiveConfirm: 'Restore' (was 'Unarchive')
- property.unarchivedToastTitle: 'Listing sent to moderation' (was 'Listing moved to Drafts')
- property.unarchivedToastMessage: 'It will appear publicly once a moderator approves it.'

D-12 RU target values:
- property.unarchiveDialogTitle: 'Восстановить объявление'
- property.unarchiveDialogMessage: 'Восстановить это объявление? Оно вернётся в очередь модерации для проверки перед публикацией.'
- property.unarchiveConfirm: 'Восстановить'
- property.unarchivedToastTitle: 'Объявление отправлено на модерацию'
- property.unarchivedToastMessage: 'Оно появится публично после одобрения модератором.'

PropertyDetailsScreen state cluster additions (after existing isRejectModalOpen at lines 272-274):
  const [isArchiveModalOpen, setIsArchiveModalOpen] = useState(false);
  const [isHardDeleteModalOpen, setIsHardDeleteModalOpen] = useState(false);

PropertyDetailsScreen render predicates (after existing showModFooter at line 274):
  const showArchiveBtn = can('archiveAnyListing') && property?.status !== 'archived';
  const showRestoreBtn = can('archiveAnyListing') && property?.status === 'archived';
  const showHardDeleteBtn = can('hardDeleteListing'); // backend HARD_DELETE_REQUIRES_ARCHIVED still gates by status (Plan 04)

PropertyDetailsScreen handlers (mirror existing handleApprove + handleRejectSubmit shapes at lines 299-352):
- handleModArchiveSubmit({reasonCode, reasonNote}) → PropertyService.archiveAnyListing(property.id, reasonCode, reasonNote) → close modal → refetchProperty → 409 race-conflict path on error
- handleRestore() → Alert.alert with t('property.unarchiveDialog*') keys → on confirm, PropertyService.restoreListing(property.id) → refetchProperty
- confirmHardDelete() → PropertyService.hardDeleteListing(property.id) → close modal → toast t('property.permanentlyDeletedToast') → onRefreshProperty (caller pops navigation)

PropertyDetailsScreen imports to add at top:
  import ArchiveListingModal from '../components/ArchiveListingModal';
  import { DeleteListingModal } from '../components/DeleteListingModal';
  import { Archive, ArchiveRestore, Trash2 } from 'lucide-react-native';  // verify each missing import; only add the missing ones

RenterListingsScreen STRIP per D-10 + D-14 (Pitfall 6 atomic):
- handleDeleteProperty function (lines 111-115)
- confirmDelete function (lines 117-134)
- propertyToDelete useState (line 59)
- deleteModalVisible useState (line 58)
- DeleteListingModal JSX mount (lines 368-376)
- onDelete=handleDeleteProperty prop on PropertyCard (line 319)
- onDelete=handleDeleteProperty prop on HospitalitySection (line 359)

RenterListingsScreen REWIRE per D-09 + D-12:
- handleArchiveProperty's PropertyService.archiveProperty(property.id!) → PropertyService.archiveOwnListing(property.id!) at line 153
- handleUnarchiveProperty's PropertyService.unarchiveProperty(property.id!) → PropertyService.restoreListing(property.id!) at line 183

RenterListingsScreen ADD per D-13 (Pitfall 4 conditional gate):
  const canSelfRestore = (p: Property): boolean =>
    p.archivedByUid != null && p.archivedByUid === user?.localId;
(Plan 05 Task 2 added archivedByUid to the Property type; this helper depends on that.)

App.tsx D-19 net-zero verification: Plan 07 MUST NOT add to App.tsx. Verify post-edit wc -l App.tsx returns 1191 (unchanged from Plan 01 baseline lock).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Locale value rewrite — 5 keys × 2 locales = 10 strings (D-12 + Pitfall 3)</name>
  <files>/Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts, /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts</files>
  <read_first>
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts (locate existing keys via grep -n "property.unarchiveDialogTitle" src/locales/en.ts; current values from RESEARCH §"Existing relevant locale keys" lines 873-892)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts (mirror keys; same line range approximately)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-CONTEXT.md §D-12 "Restore confirm copy — direct re-moderation framing" + §"specifics" — verbatim EN+RU target values
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-PATTERNS.md §13 "Client locales — D-12 value rewrites" lines 1180-1192 (rewrite table)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-RESEARCH.md §"Pitfall 3: Locale-key VALUE rewrite without verification" — value-grep is the manual verification gate the bash parity script doesn't cover
  </read_first>
  <action>
    Apply 5 VALUE rewrites to BOTH src/locales/en.ts and src/locales/ru.ts in the same commit. Key NAMES are preserved (no rename cascade); only VALUES change.

    EN rewrites in src/locales/en.ts:
    1. property.unarchiveDialogTitle: rewrite value from 'Unarchive Listing' to 'Restore Listing'
    2. property.unarchiveDialogMessage: rewrite value to "Restore this listing? It will go back to the moderation queue for review before becoming public again."
    3. property.unarchiveConfirm: rewrite value from 'Unarchive' to 'Restore'
    4. property.unarchivedToastTitle: rewrite value to 'Listing sent to moderation'
    5. property.unarchivedToastMessage: rewrite value to 'It will appear publicly once a moderator approves it.'

    RU rewrites in src/locales/ru.ts:
    1. property.unarchiveDialogTitle: 'Восстановить объявление'
    2. property.unarchiveDialogMessage: 'Восстановить это объявление? Оно вернётся в очередь модерации для проверки перед публикацией.'
    3. property.unarchiveConfirm: 'Восстановить'
    4. property.unarchivedToastTitle: 'Объявление отправлено на модерацию'
    5. property.unarchivedToastMessage: 'Оно появится публично после одобрения модератором.'

    Constraints (Pitfall 3 mitigation):
    - Bash parity gate scripts/check-i18n-parity.sh only checks key NAMES — value drift is NOT caught. Acceptance criteria below grep for the new VALUES explicitly.
    - tsc TranslationKeys Record type only checks key PRESENCE — same blind spot.
    - Do NOT modify any other key in either file.
    - Use the exact apostrophe + quote style consistent with the rest of the file (verify via head -200 src/locales/en.ts | tail -50 for surrounding-keys patterns).

    Run i18n parity gate after the edits:
      cd /Users/beckmaldinVL/development/mobileApps/JayTap && bash scripts/check-i18n-parity.sh
    Expected: exit 0 with "OK   #1: en.ts and ru.ts key sets are identical".
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -F "back to the moderation queue" src/locales/en.ts && grep -F "очередь модерации для проверки" src/locales/ru.ts && bash scripts/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - EN value rewrite #1 (Title): grep -F "'property.unarchiveDialogTitle': 'Restore Listing'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts returns 1 line
    - EN value rewrite #2 (Message): grep -F "back to the moderation queue for review" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts returns 1 line
    - EN value rewrite #3 (Confirm): grep -F "'property.unarchiveConfirm': 'Restore'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts returns 1 line
    - EN value rewrite #4 (Toast Title): grep -F "'property.unarchivedToastTitle': 'Listing sent to moderation'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts returns 1 line
    - EN value rewrite #5 (Toast Message): grep -F "moderator approves" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts returns 1 line
    - RU value rewrite #1: grep -F "'property.unarchiveDialogTitle': 'Восстановить объявление'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts returns 1 line
    - RU value rewrite #2: grep -F "очередь модерации для проверки" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts returns 1 line
    - RU value rewrite #3: grep -F "'property.unarchiveConfirm': 'Восстановить'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts returns 1 line
    - RU value rewrite #4: grep -F "Объявление отправлено на модерацию" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts returns 1 line
    - RU value rewrite #5: grep -F "одобрения модератором" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts returns 1 line
    - Old (deprecated) values absent: grep -c "'Unarchive Listing'\|'Listing moved to Drafts'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts returns 0
    - i18n parity gate exits 0: bash /Users/beckmaldinVL/development/mobileApps/JayTap/scripts/check-i18n-parity.sh shows exit code 0
    - tsc baseline preserved: cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit | grep -v "ThemeContext" | grep -cE "error TS" returns 0 OR documented Plan-05 handoff errors only
  </acceptance_criteria>
  <done>5 EN + 5 RU value rewrites land atomically; i18n parity gate exits 0; new ARCH-04 user-facing copy confirms re-moderation expectation; old "Drafts" copy verbalized.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RenterListingsScreen — strip owner hard-delete + rewire archive/unarchive service calls + re-mount onArchive/onUnarchive props with D-13 conditional gate (D-09 + D-10 + D-11 + D-13 + D-14, Pitfalls 4 + 6)</name>
  <files>/Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx</files>
  <read_first>
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx (FULL FILE — 463 LOC; lines 58-59 useState; lines 111-134 handleDeleteProperty + confirmDelete; lines 141-167 handleArchiveProperty; lines 171-197 handleUnarchiveProperty; lines 313-321 PropertyCard JSX mount; lines 354-361 HospitalitySection mount; lines 368-376 DeleteListingModal mount)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-PATTERNS.md §11 "Client RenterListingsScreen.tsx — handler rewires + strip + re-mount (D-09 + D-10 + D-11 + D-14)" lines 893-1011 (full diff)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-RESEARCH.md §"Pitfall 4" + §"Pitfall 6" — atomic strip discipline + D-13 conditional gate
  </read_first>
  <action>
    Apply 6 atomic edits to src/screens/RenterListingsScreen.tsx in the same commit (Pitfall 6 atomic strip mitigation):

    Edit 1 — STRIP handleDeleteProperty + confirmDelete functions (lines 111-134). Delete both functions entirely.

    Edit 2 — STRIP propertyToDelete + deleteModalVisible state (lines 58-59 area). Delete both useState declarations.

    Edit 3 — STRIP DeleteListingModal JSX mount (lines 368-376 area). Delete the entire <DeleteListingModal> mount block + remove the DeleteListingModal import at the top of the file.

    Edit 4 — REWIRE service calls in handleArchiveProperty + handleUnarchiveProperty:
    - In handleArchiveProperty around line 153: change `await PropertyService.archiveProperty(property.id!)` to `await PropertyService.archiveOwnListing(property.id!)`
    - In handleUnarchiveProperty around line 183: change `await PropertyService.unarchiveProperty(property.id!)` to `await PropertyService.restoreListing(property.id!)`
    - The optimistic state update at lines 184-186 already correctly maps `'pending' as const` (Phase 2 Plan 06 set this up).

    Edit 5 — ADD canSelfRestore helper (D-13 + Pitfall 4). Locate the destructure of useAuth (look for `const { user } = useAuth()` or similar). After the hook destructures, add:
      const canSelfRestore = (p: Property): boolean =>
        p.archivedByUid != null && p.archivedByUid === user?.localId;
    (Plan 05 Task 2 added archivedByUid to the Property type — this helper depends on that.)

    Edit 6 — RE-MOUNT onArchive + onUnarchive props on PropertyCard + HospitalitySection (D-09 + D-11 + D-13).

    For PropertyCard JSX (currently around lines 313-321):
    - Remove the `onDelete={handleDeleteProperty}` prop
    - Add `onArchive={handleArchiveProperty}` prop
    - Add `onUnarchive={canSelfRestore(item) ? handleUnarchiveProperty : undefined}` prop (D-13 conditional)
    - Keep all other existing props (onPress, onViewTour, onViewVideo, onEdit, showEditButton)

    For HospitalitySection JSX (currently around lines 354-361):
    - Remove the `onDelete={handleDeleteProperty}` prop
    - Add `onArchive={handleArchiveProperty}` prop unconditionally
    - OMIT onUnarchive on HospitalitySection — D-Claude-Discretion: HospitalitySection's archived view is low-traffic; if a Hospitality listing gets archived and an owner needs to restore it, they can navigate via PropertyDetailsScreen footer (D-06). Document this scope choice in SUMMARY.

    Constraints (Pitfall 6 atomic-commit discipline):
    - All 6 edits MUST land in the same commit (so the codebase is never in a half-stripped state)
    - Strip must be COMPLETE — grep -nE "handleDeleteProperty|propertyToDelete|deleteModalVisible|DeleteListingModal" src/screens/RenterListingsScreen.tsx returns 0 hits post-edit
    - Service-call rewires use the EXACT method names from Plan 05: archiveOwnListing (not archiveProperty), restoreListing (not unarchiveProperty)
    - canSelfRestore MUST use user?.localId (matches AuthContext shape from Phase 1 — verify via grep "localId" src/context/AuthContext.tsx)
    - DO NOT modify the existing 4-tab segmented control (Phase 2 Plan 06's responsibility); the changes here are scoped to handler/state strip + JSX prop swap

    Run:
      cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -v "ThemeContext" | head -30
    Expected: 0 new tsc errors specific to RenterListingsScreen.tsx (the previous Plan 05-handoff tsc errors at archiveProperty/unarchiveProperty call sites RESOLVE because the call sites are now rewired to the new method names + Plan 05 already removed the broken stubs).

    Run bash scripts/check-i18n-parity.sh — should still exit 0 (no locale changes in this task).
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -nE "handleDeleteProperty|propertyToDelete|deleteModalVisible|DeleteListingModal" src/screens/RenterListingsScreen.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Pitfall 6 atomic strip verified: grep -nE "handleDeleteProperty|propertyToDelete|deleteModalVisible|DeleteListingModal" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx | wc -l returns 0
    - Service call rewires: grep -c "PropertyService.archiveOwnListing" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx returns 1 AND grep -c "PropertyService.restoreListing" returns 1
    - Old broken-stub calls absent: grep -c "PropertyService.archiveProperty\|PropertyService.unarchiveProperty\|PropertyService.deleteProperty" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx returns 0
    - canSelfRestore helper defined: grep -c "const canSelfRestore" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx returns 1; AND helper uses correct field: grep -A2 "const canSelfRestore" | grep -c "p.archivedByUid" returns 1
    - PropertyCard onArchive prop wired: grep -c "onArchive={handleArchiveProperty}" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx returns greater than or equal to 1
    - PropertyCard onUnarchive uses D-13 conditional gate: grep -c "onUnarchive={canSelfRestore(item) ? handleUnarchiveProperty : undefined}" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx returns 1
    - PropertyCard onDelete prop absent: grep -c "onDelete={handleDeleteProperty}\|onDelete=" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx returns 0
    - HospitalitySection onArchive prop wired: grep -A8 "<HospitalitySection" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx | grep -c "onArchive={handleArchiveProperty}" returns greater than or equal to 1
    - LOC delta net negative or small: wc -l /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx returns 430-465 (was 463; -10 to -25 LOC strip net per RESEARCH §11 estimate)
    - tsc Plan-05-handoff errors RESOLVED: cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit | grep -v "ThemeContext" | grep "error TS" | grep "RenterListingsScreen.tsx" | wc -l returns 0
    - Total tsc baseline preserved: cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit | grep -v "ThemeContext" | grep -cE "error TS" returns 0 (or less than or equal to 2 if Plan 07 introduces new PropertyDetailsScreen-territory errors that get resolved in Task 3)
  </acceptance_criteria>
  <done>RenterListingsScreen owner-side rewire complete: Pitfall 6 atomic strip (handlers + state + modal mount + onDelete prop), service-call rewire to Plan 05 methods, canSelfRestore D-13 conditional gate (Pitfall 4 mitigation), onArchive + onUnarchive props re-mounted on PropertyCard + HospitalitySection.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: PropertyDetailsScreen — mod-action footer extension + ArchiveListingModal + DeleteListingModal mounts (D-06 + D-10)</name>
  <files>/Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx</files>
  <read_first>
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx (FULL FILE — 2131 LOC; verify Phase 3 mod-footer state cluster at lines 272-274; predicate at line 274; handlers at lines 268-356; footer JSX at lines 1316-1376; RejectListingModal sibling at line 1382; mod-footer styles at lines 2106-2130; existing imports at top of file)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-PATTERNS.md §12 "Client PropertyDetailsScreen.tsx — mod footer extension + DeletePropertyModal mount (D-06 + D-10)" lines 1015-1168 (full state + handler + JSX additions)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/04-archive-lifecycle-owner-mod-admin/04-RESEARCH.md §"PropertyDetailsScreen mod action footer (D-06)" — full extension shape at lines 627-690
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/DeleteListingModal.tsx (full file — 206 LOC; props interface signature for the D-10 mount target)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ArchiveListingModal.tsx (Plan 06 output — verify it exists and exports ArchiveListingModal as default)
  </read_first>
  <action>
    Apply 5 atomic edits to src/screens/PropertyDetailsScreen.tsx in the same commit:

    Edit 1 — Add imports at top of file. Verify which imports are already present via grep first:
      grep -n "ArchiveListingModal\|DeleteListingModal\|Archive,\|ArchiveRestore,\|Trash2," /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx
    Add any missing imports:
      import ArchiveListingModal from '../components/ArchiveListingModal';
      import { DeleteListingModal } from '../components/DeleteListingModal';
    Verify Archive, ArchiveRestore, Trash2 are imported from lucide-react-native — likely some are already present (check, and only add the missing ones to the existing lucide import line).

    Edit 2 — Add state cluster (around lines 272-274). Locate the existing isRejectModalOpen and submittingAction state hooks. Add 2 new state hooks immediately after:
      const [isArchiveModalOpen, setIsArchiveModalOpen] = useState(false);
      const [isHardDeleteModalOpen, setIsHardDeleteModalOpen] = useState(false);

    Edit 3 — Add render predicates (around line 274 + after). Locate the existing showModFooter predicate. Add 3 new predicates immediately after:
      const showArchiveBtn = can('archiveAnyListing') && property?.status !== 'archived';
      const showRestoreBtn = can('archiveAnyListing') && property?.status === 'archived';
      const showHardDeleteBtn = can('hardDeleteListing'); // backend HARD_DELETE_REQUIRES_ARCHIVED still gates by status

    Edit 4 — Add 3 handlers (mirror existing handleApprove + handleRejectSubmit at lines 299-352).

    handleModArchiveSubmit({reasonCode, reasonNote}):
    - if no property.id, return
    - setSubmittingAction(true)
    - try: await PropertyService.archiveAnyListing(property.id, params.reasonCode, params.reasonNote); setIsArchiveModalOpen(false); await refetchProperty()
    - catch err: if 409 status, await handleRaceConflict(); else Alert.alert(common.error, err message OR common.errorGeneric)
    - finally: setSubmittingAction(false)

    handleRestore() — mirrors handleApprove Alert.alert flow:
    - Alert.alert with t('property.unarchiveDialogTitle'), t('property.unarchiveDialogMessage') (values rewritten in Task 1; key names preserved)
    - Cancel button + Confirm button using t('property.unarchiveConfirm')
    - On confirm: setSubmittingAction(true); try await PropertyService.restoreListing(property.id); await refetchProperty(); catch 409 → handleRaceConflict; finally setSubmittingAction(false)

    confirmHardDelete() — called from DeleteListingModal's onConfirm:
    - if no property.id, return
    - setSubmittingAction(true)
    - try: await PropertyService.hardDeleteListing(property.id); setIsHardDeleteModalOpen(false); Alert.alert(common.success, t('property.permanentlyDeletedToast')); if onRefreshProperty, await onRefreshProperty()
    - catch err: Alert.alert(common.error, err message OR common.errorGeneric)
    - finally: setSubmittingAction(false)

    Edit 5 — Add footer JSX + 2 modal mounts. Insert immediately after the closing View of the existing 3-button moderation footer (around line 1376) and before the existing RejectListingModal sibling (around line 1382).

    Footer JSX shape:
    Wrap in (showArchiveBtn || showRestoreBtn || showHardDeleteBtn) AND clause to render only when at least one button shows. Use existing modActionFooter style with backgroundColor colors.surface, borderTopColor colors.border, paddingBottom Math.max(insets.bottom, 12).

    Three buttons inside (each conditional on its predicate):
    - showArchiveBtn: TouchableOpacity with backgroundColor '#D97706' (amber, archive convention from PropertyCard); onPress sets isArchiveModalOpen true; disabled by submittingAction; activeOpacity 0.7; renders Archive icon size 18 color #FFF + Text with t('property.archive')
    - showRestoreBtn: TouchableOpacity with backgroundColor '#059669' (emerald, restorative convention); onPress=handleRestore; disabled by submittingAction; renders ArchiveRestore icon + Text with t('property.unarchive')
    - showHardDeleteBtn: TouchableOpacity with backgroundColor colors.error; onPress sets isHardDeleteModalOpen true; disabled by submittingAction; renders Trash2 icon + Text with t('common.delete')

    Modal mounts (sister to existing RejectListingModal at line 1382):
    - ArchiveListingModal with visible={isArchiveModalOpen}, onClose closes modal, onSubmit=handleModArchiveSubmit, submitting=submittingAction
    - DeleteListingModal with visible={isHardDeleteModalOpen}, onClose closes modal, onConfirm=confirmHardDelete, property={property}

    Constraints:
    - REUSE existing modActionFooter, modActionBtn, modActionBtnText styles at lines 2106-2130 — do NOT add new style entries
    - Use the same submittingAction state for all 4 mod actions (approve/reject/archive/restore/hard-delete) — single in-flight gate per the Phase 3 PATTERNS pattern
    - Use the same refetchProperty() and handleRaceConflict() callbacks as Phase 3 (already at lines 279-297) — do NOT duplicate
    - Use existing locale keys (property.archive, property.unarchive, common.delete, property.permanentlyDeletedToast) — do NOT add new keys (Plan 06's archive-specific keys are for the modal, not the footer button labels)
    - The new footer ROW renders ONLY when at least one of the 3 buttons should show — (showArchiveBtn || showRestoreBtn || showHardDeleteBtn) predicate prevents an empty bar from rendering
    - Pending listings render BOTH the existing 3-button row (Approve/Reject/Edit-on-behalf) AND the new row's Archive button per CONTEXT.md D-06 verbatim ("mod/admin viewing PENDING listings sees BOTH")

    Run:
      cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -v "ThemeContext" | head -30
    Expected: 0 new tsc errors. The new state hooks, handlers, and JSX should all type-check given the Plan 05 service signatures and Plan 06 modal exports.

    Run bash scripts/check-i18n-parity.sh — should still exit 0 (no locale changes here).

    Verify the App.tsx D-19 net-zero target post-Task-3:
      wc -l /Users/beckmaldinVL/development/mobileApps/JayTap/App.tsx
    Expected: 1191 (unchanged from Plan 01 lock; no new App.tsx state was needed).
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -E "ArchiveListingModal|DeleteListingModal|isArchiveModalOpen|isHardDeleteModalOpen|handleModArchiveSubmit|handleRestore|confirmHardDelete" src/screens/PropertyDetailsScreen.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Imports added: grep -c "from '../components/ArchiveListingModal'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx returns 1 AND grep -c "from '../components/DeleteListingModal'" returns 1
    - State cluster added: grep -c "isArchiveModalOpen" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx returns greater than or equal to 3 (state declaration + 2 setter usages); same for isHardDeleteModalOpen
    - Render predicates added: grep -c "showArchiveBtn\|showRestoreBtn\|showHardDeleteBtn" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx returns greater than or equal to 6 (3 declarations + 3 JSX usages)
    - All 3 handlers defined: grep -c "const handleModArchiveSubmit\|const handleRestore\|const confirmHardDelete" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx returns 3
    - PropertyService calls in handlers: grep -c "PropertyService.archiveAnyListing\|PropertyService.restoreListing\|PropertyService.hardDeleteListing" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx returns 3
    - 409 race-conflict reuse: grep -c "handleRaceConflict" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx returns greater than or equal to 3 (existing 1-2 + new in handleModArchiveSubmit + handleRestore — minimum is 1 existing from Phase 3 + 2 new = 3)
    - Footer button render predicates wired: grep -B2 -A1 "setIsArchiveModalOpen(true)" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx | grep -c "showArchiveBtn" returns greater than or equal to 1
    - 2 new modal mounts present (sibling to existing RejectListingModal): grep -c "<ArchiveListingModal" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx returns 1 AND grep -c "<DeleteListingModal" returns 1
    - Modal callbacks wired correctly: grep -A3 "<ArchiveListingModal" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx | grep -c "handleModArchiveSubmit" returns 1; grep -A3 "<DeleteListingModal" | grep -c "confirmHardDelete" returns 1
    - LOC delta within budget: wc -l /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx returns 2180-2220 (was 2131; +50-90 LOC for state + handlers + JSX + 2 modals)
    - tsc baseline preserved: cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit | grep -v "ThemeContext" | grep -cE "error TS" returns 0
    - i18n parity gate exits 0: bash /Users/beckmaldinVL/development/mobileApps/JayTap/scripts/check-i18n-parity.sh shows exit 0
    - App.tsx D-19 net-zero verified: wc -l /Users/beckmaldinVL/development/mobileApps/JayTap/App.tsx returns 1191 (unchanged from Plan 01 lock)
    - Anti-pattern grep gates carry forward: grep -nE "actorUid:\s*req\.(body|headers)" /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/routes/{property,moderation}Routes.js | wc -l returns 0
  </acceptance_criteria>
  <done>PropertyDetailsScreen extended with mod/admin Archive/Restore/Hard-Delete footer + ArchiveListingModal + DeleteListingModal mounts; all 5 ARCH-XX requirements have observable client-side surfaces; tsc + i18n parity preserved; App.tsx D-19 net-zero target preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual physical-device QA matrix on iPhone 15 Pro Max (Phase 3 escape-hatch precedent — Moto G XT2513V deferred to Phase 6)</name>
  <what-built>
    Tasks 1-3 wire all client-side archive lifecycle surfaces end-to-end:
    - Owner archives own listing via PropertyCard archive button (Alert.alert flow) — calls PropertyService.archiveOwnListing → backend Plan 02 POST /:id/archive
    - Owner restores own self-archived listing (Alert.alert with re-moderation copy from D-12 rewrite) — calls PropertyService.restoreListing → backend Plan 02 POST /:id/unarchive
    - Owner CANNOT see Restore button on mod-archived listings (canSelfRestore D-13 conditional gate hides affordance)
    - Mod/admin archives any listing via PropertyDetailsScreen footer Archive button (opens ArchiveListingModal with 4-chip + optional note) — calls PropertyService.archiveAnyListing → backend Plan 03 POST /moderation/properties/:id/archive
    - Mod/admin restores any listing via PropertyDetailsScreen footer Restore button (Alert.alert with re-moderation copy) — calls PropertyService.restoreListing (role-aware URL split) → backend Plan 03 POST /moderation/properties/:id/restore
    - Admin hard-deletes archived listing via PropertyDetailsScreen footer Hard Delete button (DeleteListingModal confirm) — calls PropertyService.hardDeleteListing → backend Plan 04 DELETE /:id
    - Plain user does NOT see Archive/Restore/Hard-Delete buttons on PropertyDetailsScreen footer (Plan 05 useRole canFromUser belt-and-suspenders + Plan 03 backend requireMinRole gate)
    - Archived listings hidden from public lanes (Home, Favorites, RenterListings public lanes) per Phase 2 D-07 client filter (preserved as regression)
    - 5 D-12 locale value rewrites land in EN+RU (re-moderation framing replaces M1 "to Drafts" copy)
  </what-built>
  <how-to-verify>
    Walk this 11-row matrix on iPhone 15 Pro Max (per CONVENTIONS.md "manual physical-device QA bar" + Phase 3 Plan 06 SUMMARY's escape-hatch precedent: Moto G XT2513V deferred to Phase 6 release-gate matrix per REL-03).

    Setup: iOS app pointing at staging or local backend with Plans 01-04 deployed. Test fixtures: 1 plain-user account (own_listings_owner), 1 mod-user, 1 admin-user. At least 3 test listings with statuses: live (owner_uid=own_listings_owner), pending (owner_uid=own_listings_owner), archived (one self-archived by own_listings_owner, one mod-archived by mod-user).

    Row 1 — Owner archives own live listing (ARCH-01, D-09):
    - Sign in as own_listings_owner
    - Navigate to RenterListings → Live tab
    - Tap Archive button (amber) on a live listing's PropertyCard
    - Expected: Alert.alert opens with t('property.archiveDialogTitle')="Archive Listing" + t('property.archiveDialogMessage') re-moderation copy
    - Tap Archive → expected: success Alert.alert "Listing archived"; listing moves to Archived tab; PropertyCard shows ArchiveRestore (emerald) Restore button + NO Edit button + NO Delete button

    Row 2 — Owner restores self-archived listing (ARCH-04, D-12 + D-13):
    - As own_listings_owner, navigate to RenterListings → Archived tab
    - Identify a listing where archivedByUid === user.localId (self-archived)
    - Tap Restore button (emerald)
    - Expected: Alert.alert opens with t('property.unarchiveDialogTitle')="Restore Listing" (D-12 rewritten value) + t('property.unarchiveDialogMessage') containing "back to the moderation queue for review before becoming public again"
    - Tap Restore (button label = "Restore", D-12 rewritten value) → expected: toast "Listing sent to moderation" (D-12 rewritten value); listing moves to Pending tab with FIFO re-queue position

    Row 3 — Owner CANNOT restore mod-archived listing (D-13 + Pitfall 4):
    - As own_listings_owner, locate a mod-archived listing in Archived tab (one where archivedByUid !== own_listings_owner)
    - Expected: NO Restore button visible on PropertyCard (canSelfRestore returns false; onUnarchive prop is undefined; PropertyCard hides the button)

    Row 4 — Mod archives a live listing via PropertyDetailsScreen footer (ARCH-02, D-06 + D-08):
    - Sign in as mod-user
    - Open any live listing's PropertyDetailsScreen via deep-link or browse
    - Scroll to the bottom action footer
    - Expected: existing 3-button row hidden (status !== 'pending') + new Archive button (amber) visible + NO Restore button (status !== 'archived') + Hard Delete hidden (mod is not admin)
    - Tap Archive button → expected: ArchiveListingModal opens with t('moderation.archive.modalTitle')="Archive Listing" + 4 chips (incomplete-info, prohibited-content, out-of-service-area, other) + optional note TextInput + Submit button labeled t('moderation.archive.submit')="Archive"
    - Pick reasonCode (e.g. prohibited-content), add reasonNote text, tap Archive → expected: modal closes; listing flips to archived status; refetchProperty pulls updated state; ModerationLog row written backend-side (verify via Plan 03 supertest + manual Mongo query if available)

    Row 5 — Mod restores any archived listing (ARCH-04, D-06):
    - As mod-user, open the listing archived in Row 4
    - Expected: footer shows Restore button (emerald) + NO Archive button (status === 'archived') + NO Hard Delete (mod is not admin)
    - Tap Restore → Alert.alert with rewritten copy (same as Row 2 but called from PropertyDetailsScreen instead of RenterListings)
    - Tap Restore confirm → listing flips to pending; visible in mod's queue (Phase 3 ModerationQueueScreen)

    Row 6 — Admin sees Hard Delete button on archived listing (ARCH-05, D-04 + D-10):
    - Sign in as admin-user
    - Open any archived listing via PropertyDetailsScreen
    - Expected: footer shows Restore button (emerald) + Hard Delete button (red, colors.error) + NO Archive button (status === 'archived')
    - Tap Hard Delete → DeleteListingModal opens with confirmation copy (existing M1 modal — content not changed in Phase 4, but the mount location is new)
    - Confirm delete → expected: success toast t('property.permanentlyDeletedToast')="Listing permanently deleted"; PropertyDetailsScreen pops back; listing gone from all lanes; backend ModerationLog row written with full before snapshot (verify via Plan 04 supertest)

    Row 7 — Plain user does NOT see Archive/Restore/Hard-Delete buttons (ARCH-05, D-06 + D-16):
    - Sign in as plain user (not own_listings_owner — i.e. someone who doesn't own the listing being viewed)
    - Open any listing via PropertyDetailsScreen
    - Expected: NO Archive/Restore/Hard-Delete row visible (all 3 predicates evaluate to false; the (showArchiveBtn || showRestoreBtn || showHardDeleteBtn) wrapper hides the entire row)

    Row 8 — Archived listings hidden from public lanes (ARCH-03 — Phase 2 D-07 regression):
    - As anonymous (signed out) or plain user, navigate to Home / Favorites
    - Expected: archived listings absent from these lanes (Phase 2 D-07 client filter (p.status ?? 'live') === 'live' continues to filter; no Phase 4 regression)
    - Confirm RenterListings public lanes (if visible to non-owner) also exclude archived

    Row 9 — Owner sees archived listings in their own Archived tab (ARCH-03):
    - As own_listings_owner, navigate to RenterListings → Archived tab
    - Expected: own_listings_owner's own archived listings (both self-archived + mod-archived) are visible; PropertyCard shows Restore button only on self-archived (Row 3 confirms gate)

    Row 10 — EN+RU locale parity per screen (D-12 + Plan 06):
    - With device language switched to RU, walk Rows 1-9 again (or at least Rows 1, 2, 4)
    - Expected: every label, modal title, button, alert message, toast renders in Russian — specifically:
      * Archive button: "В архив"
      * Restore Alert title: "Восстановить объявление"
      * Restore Alert message: "Восстановить это объявление? Оно вернётся в очередь модерации для проверки перед публикацией."
      * Restore confirm button: "Восстановить"
      * Restore success toast: "Объявление отправлено на модерацию"
      * ArchiveListingModal title: "Архивировать объявление"
      * ArchiveListingModal submit: "В архив"

    Row 11 — Dark/light mode parity:
    - Walk Rows 1-2 + Row 4 in dark mode
    - Expected: all colors via useTheme tokens render correctly; chip selected-state contrast in ArchiveListingModal works (Pitfall 7 mitigation — selectedTextColor isDark switch); footer buttons readable in both modes

    Document any failures in plan SUMMARY (Phase 3 SUMMARY precedent — judgment-call documentation per PATTERN D); minor polish issues are signal-not-block; CRITICAL failures (ARCH-01..ARCH-05 invariants violated) require fix before approval.

    Execute these post-walk gates after the matrix completes:
    - cd /Users/beckmaldinVL/development/mobileApps/JayTap && bash scripts/check-i18n-parity.sh shows exit 0
    - cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit shows greater-than-equal to baseline 2 ThemeContext errors only (0 new)
    - cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && nvm use 24 && npm test shows greater-than-equal to 105 passing (Plans 02 + 03 + 04 baselines preserved)
    - grep -nE "actorUid:\s*req\.(body|headers)" /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/routes/{property,moderation}Routes.js | wc -l returns 0 (anti-spoofing carry-forward)
    - wc -l /Users/beckmaldinVL/development/mobileApps/JayTap/App.tsx returns 1191 (D-19 net-zero verified)
  </how-to-verify>
  <resume-signal>
    Type "approved" to close Plan 07 + Phase 4 ready for /gsd-verify-work + /gsd-code-review per gsd-verifier-misses-regressions.md paired-gates discipline. Type a description of any failures observed; planner will re-open Plan 07 for fix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI gate (this plan's `<Gated>` predicates and conditional onUnarchive) → Service layer (Plan 05 canFromUser) → Backend gate (Plans 02/03/04 requireMinRole + ownership filter) | 3-tier defense in depth — backend is authoritative; manual QA verifies the UX-layer gates produce expected hidden/visible states across all role/status combinations |
| Locale value rewrites → user perception of ARCH-04 invariant | Pitfall 3 — value drift slips past bash + tsc gates; manual QA Row 2 + Row 10 walks the rewritten copy on iPhone in EN + RU |
| onUnarchive=canSelfRestore prop → D-13 owner-restore gate | Pitfall 4 — owner who can't restore must not see the affordance; manual QA Row 3 confirms the conditional hide |
| Pitfall 6 atomic strip → owner-side hard-delete affordance fully removed | Manual QA Row 1 + Row 9 confirm RenterListings shows no Delete button on any owner card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-AUTHZ-08 | Elevation of Privilege | Plain user sees Archive button on PropertyDetailsScreen footer | mitigate | Row 7 of manual QA matrix verifies no plain user sees the row; predicates use `can('archiveAnyListing')` (admin/mod only) and `can('hardDeleteListing')` (admin only) |
| T-04-AUTHZ-09 | Elevation of Privilege | Owner sees Restore button on mod-archived listing | mitigate | D-13 conditional gate `canSelfRestore` hides affordance when archivedByUid !== user.localId; Row 3 of manual QA matrix verifies; Pitfall 4 mitigation |
| T-04-INTEGRITY-01 | Tampering | Locale value rewrite drift (Pitfall 3) — old "Drafts" copy survives, ARCH-04 user expectation broken | mitigate | Task 1 acceptance criteria explicitly grep for new VALUES; manual QA Row 2 + Row 10 walks the rewritten copy on iPhone EN + RU; bash + tsc gates can't catch values |
| T-04-AUDIT-07 | Repudiation | Owner-side hard-delete handler half-stripped (Pitfall 6) — orphan UI element or runtime error | mitigate | Task 2 acceptance criterion `grep -nE "handleDeleteProperty|propertyToDelete|deleteModalVisible|DeleteListingModal" | wc -l` returns 0 — atomic strip enforced; manual QA Row 1 + Row 9 confirm |
| T-04-LOC-DRIFT-02 | (Performance / maintainability) | App.tsx LOC drift past hard ceiling 1180 | accept | D-19 net-zero target — Plan 07 mounts everything on existing screens (PropertyDetailsScreen + RenterListingsScreen); App.tsx not touched. Acceptance criterion `wc -l App.tsx` returns 1191 (unchanged). Signal-not-block per PATTERN J + Phase 2 PATTERN D precedent |
| T-04-MANUAL-QA-COVERAGE | Repudiation | iPhone-only QA without Moto G XT2513V | accept | Phase 3 Plan 06 SUMMARY explicit escape-hatch precedent — Moto G XT2513V deferred to Phase 6 release-gate matrix (REL-03 covers Android in cross-cutting QA before store submission). Documented choice; not a regression of M1 manual QA bar |

</threat_model>

<verification>
- All Task 1 D-12 value rewrites land in both locales: 5 EN + 5 RU value-greps each return 1 line
- i18n parity gate exits 0 across all Plan 07 commits: bash /Users/beckmaldinVL/development/mobileApps/JayTap/scripts/check-i18n-parity.sh shows exit 0
- Pitfall 6 atomic strip: grep -nE "handleDeleteProperty|propertyToDelete|deleteModalVisible|DeleteListingModal" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx | wc -l returns 0
- D-13 conditional gate landed: grep -c "canSelfRestore" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/RenterListingsScreen.tsx returns greater than or equal to 2 (helper definition + usage in JSX)
- PropertyDetailsScreen mod-footer extension landed: grep -c "showArchiveBtn\|showRestoreBtn\|showHardDeleteBtn" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx returns greater than or equal to 6
- 2 new modal mounts: grep -c "<ArchiveListingModal\|<DeleteListingModal" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx returns 2
- App.tsx D-19 net-zero: wc -l /Users/beckmaldinVL/development/mobileApps/JayTap/App.tsx returns 1191
- tsc baseline preserved: cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit | grep -v "ThemeContext" | grep -cE "error TS" returns 0
- Backend test suite still green: cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && nvm use 24 && npm test shows greater-than-equal to 105 passing
- Anti-spoofing carry-forward: grep -nE "actorUid:\s*req\.(body|headers)" /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/routes/{property,moderation}Routes.js | wc -l returns 0
- Manual physical-device QA matrix Task 4: 11 rows walked APPROVED on iPhone 15 Pro Max
</verification>

<success_criteria>
- ARCH-01 fully observable: owner archives own listing via PropertyCard archive button → backend POST /:id/archive → status flips to archived
- ARCH-02 fully observable: mod/admin archives any listing via PropertyDetailsScreen footer + ArchiveListingModal → backend POST /moderation/properties/:id/archive → audit row written
- ARCH-03 regression preserved: archived listings hidden from public lanes (Phase 2 D-07 client filter); visible to owner Archived tab + mod via PropertyDetailsScreen
- ARCH-04 fully observable: restore (owner OR mod) flips status to pending with FIFO re-queue + re-moderation copy + D-13 owner-restore gate prevents post-takedown bypass
- ARCH-05 fully observable: useRole.ts Action union extended; admin-only DELETE /:id; client surfaces gated by 3-tier defense (Gated → canFromUser → requireMinRole)
- 5 D-12 locale value rewrites confirm ARCH-04 user expectation
- App.tsx D-19 net-zero target preserved (1191 LOC unchanged)
- Manual QA matrix walked APPROVED on iPhone 15 Pro Max
- Phase 4 ready for /gsd-verify-work + /gsd-code-review per gsd-verifier-misses-regressions.md paired-gates discipline
</success_criteria>

<output>
After completion (post-Task-4 user approval), create .planning/phases/04-archive-lifecycle-owner-mod-admin/04-07-SUMMARY.md with:
- Locale rewrite count + verbatim new values logged
- RenterListingsScreen Pitfall 6 atomic-strip evidence (grep gate exits 0)
- PropertyDetailsScreen footer extension LOC + handler/state/JSX additions summary
- Manual physical-device QA matrix walk results (11 rows × PASS/FAIL/N/A)
- Documented escape hatches (e.g., Moto G deferred to Phase 6 per Phase 3 precedent; HospitalitySection onUnarchive omission per D-Claude-Discretion)
- App.tsx LOC delta (0 expected — D-19 net-zero verified)
- Backend test suite total preserved at greater-than-equal to 105
- Phase 4 close metadata: 5/5 ARCH-XX requirements complete; ready for paired-gates verifier+reviewer
</output>