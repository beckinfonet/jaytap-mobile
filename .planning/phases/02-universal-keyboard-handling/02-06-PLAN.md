---
phase: 02-universal-keyboard-handling
plan: 06
type: execute
wave: 3
depends_on: [03, 04, 05]
files_modified:
  - .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md
  - src/screens/HomeScreen.tsx
  - .planning/phases/02-universal-keyboard-handling/02-VALIDATION.md
autonomous: false
requirements: [KBD-01, KBD-02, KBD-04]
requirements_addressed: [KBD-01, KBD-02, KBD-04]
threat_model_complete: true
tags: [react-native, keyboard, verification, matrix, manual-qa, fabric, phase-exit]

must_haves:
  truths:
    - "02-KEYBOARD-MATRIX.md exists at .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md with the D-12 Tier 1 (8 rows) + Tier 2 (2 rows) cells defined per RESEARCH §7.4"
    - "Matrix header records device IDs (iPhone 15 Pro Max / iOS 26.4 + Moto G XT2513V / Android 16 per D-13), library versions installed (reanimated, worklets, keyboard-controller), and Fabric build flag confirmation (newArchEnabled=true)"
    - "Every Tier 1 cell (8 screens × 2 platforms = 16 cells) is filled with Pass / Fail / N/A"
    - "Every Tier 2 cell (2 screens × 2 platforms = 4 cells) is filled with Pass / Fail / N/A"
    - "Zero `Fail` cells in Tier 1 (phase exit gate per RESEARCH §7.5)"
    - "Phase-wide grep gate: `grep -rn 'keyboardVerticalOffset' src/` returns 0 (KBD-02 + §6 SC3.a phase exit predicate)"
    - "HomeScreen workaround comment removal decision documented in matrix (per CONTEXT D-Discretion + RESEARCH §3.6 — opportunistic; revert if smoke fails)"
    - "Multiline composer TextInputs (ChatThread, ChatCompose, ResetPassword linkOrCode) verified to grow correctly on physical device (L10 mitigation — per RESEARCH §8 L10 + RESEARCH §3.5 sibling-component layout impact)"
    - "Android hardware back button verified to dismiss keyboard without popping screen on chat (no conflict with Phase 1's BackHandler at App.tsx:184-297 — RESEARCH §3.5)"
    - "02-VALIDATION.md frontmatter updated: nyquist_compliant: true, status: complete (after all rows pass + cleanup decision documented)"
  artifacts:
    - path: ".planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md"
      provides: "Tier 1 + Tier 2 verification matrix with all cells filled; HomeScreen workaround removal decision; L10 multiline composer verification notes; Android back button verification note"
      contains: "## Tier 1"
    - path: "src/screens/HomeScreen.tsx"
      provides: "Discretionary cleanup (CONDITIONAL — only if smoke test passes): remove the {/* Header outside FlatList to prevent TextInput focus issues */} comment at line 465. Structural separation (renderHeaderContent as sibling above FlatList) STAYS regardless — that's a non-keyboard rendering choice per RESEARCH §3.6."
      contains: "search input working correctly under KeyboardProvider"
    - path: ".planning/phases/02-universal-keyboard-handling/02-VALIDATION.md"
      provides: "Frontmatter status flipped to complete + nyquist_compliant: true after matrix is all-Pass"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "02-KEYBOARD-MATRIX.md Tier 1 + Tier 2 cells"
      to: "Plans 03, 04, 05 deliverables (per-screen KASV/KAV wraps)"
      via: "Each matrix cell directly tests one of the screens wrapped in Plans 03/04/05"
      pattern: "## Tier 1"
    - from: "Phase exit gate (this plan completion)"
      to: "Phase 2 sign-off in STATE.md + ROADMAP.md"
      via: "All §6 mechanical gates PASS + matrix all-Pass + 02-VALIDATION.md complete"
      pattern: "Phase 2 complete"
---

<objective>
Execute Wave 3 of the phase: create the keyboard verification matrix document, walk it on
both physical devices per CONTEXT D-12 (Tier 1 full + Tier 2 smoke), make the HomeScreen
workaround removal decision (CONTEXT D-Discretion), and sign off the phase by flipping
02-VALIDATION.md frontmatter to complete.

Purpose: KBD-01 (per-screen verification on device), KBD-02 (phase-wide grep gate confirms
zero `keyboardVerticalOffset` survives), KBD-04 (Fabric build mode explicitly recorded in
the matrix per D-13). This plan is the phase exit gate per RESEARCH §4 Wave 3 and §6 SC1-SC4
combined exit gate.

Output: Three artifacts — a new matrix doc, a decision-recorded HomeScreen edit (or no edit),
and the VALIDATION frontmatter update.

Critical: This plan is `autonomous: false`. Manual physical-device QA cannot be automated;
per CLAUDE.md "Key Conventions" → "Testing bar: manual physical-device QA for M1." Each
matrix-walk task is a `checkpoint:human-verify` gate.

Wave 3 entry gate: Plans 03, 04, 05 must all be merged. Task 1 verifies via the Wave 2
combined grep gate from RESEARCH §4 (zero `keyboardVerticalOffset` matches; ≥7 KASV/KAV
imports across the relevant files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-universal-keyboard-handling/02-CONTEXT.md
@.planning/phases/02-universal-keyboard-handling/02-RESEARCH.md
@.planning/phases/02-universal-keyboard-handling/02-PATTERNS.md
@.planning/phases/02-universal-keyboard-handling/02-VALIDATION.md
@.planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md
@.planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md
@.planning/phases/02-universal-keyboard-handling/02-03-SUMMARY.md
@.planning/phases/02-universal-keyboard-handling/02-04-SUMMARY.md
@.planning/phases/02-universal-keyboard-handling/02-05-SUMMARY.md
@CLAUDE.md
@src/screens/HomeScreen.tsx

<interfaces>
<!-- VERBATIM matrix structure to create — derived from RESEARCH §7.4 + §7.6 -->

MATRIX DOC LOCATION (RESEARCH §7.6): .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md

MATRIX SKELETON (verbatim Tier 1 + Tier 2 rows from RESEARCH §7.4):

  # Phase 2: Universal Keyboard Handling — Verification Matrix

  **Captured:** {date — fill at execution time}
  **Devices:**
    - iOS: iPhone 15 Pro Max / iOS 26.4 (CONTEXT D-13)
    - Android: Moto G XT2513V / Android 16 (CONTEXT D-13)
  **Library versions:** (from 02-01-SUMMARY.md)
    - react-native-reanimated@<v>
    - react-native-worklets@<v>
    - react-native-keyboard-controller@<v>
  **Build mode:** Fabric / New Architecture (`newArchEnabled=true` confirmed by `cat android/gradle.properties | grep newArchEnabled` → 1 match) — KBD-04 evidence

  ## Cell markers
  ✅ Pass · ❌ Fail · — N/A

  ## Tier 1 (Full)

  | Row # | Screen | Input(s) tested | iOS | Android | Notes |
  |-------|--------|-----------------|-----|---------|-------|
  | 1 | LoginScreen | email TextInput, PasswordTextInput | ⬜ | ⬜ | TBD |
  | 2 | SignupScreen | email TextInput, password PasswordTextInput, confirm PasswordTextInput | ⬜ | ⬜ | TBD |
  | 3 | ForgotPasswordScreen | email TextInput (autoFocus initial-pop case) | ⬜ | ⬜ | TBD |
  | 4 | ResetPasswordScreen | linkOrCode TextInput (multiline — L10), new password, confirm password | ⬜ | ⬜ | TBD |
  | 5 | CreateListingScreen — admin-verify branch | (no inputs today; verify scroll & switch interaction unbroken) | ⬜ | ⬜ | TBD |
  | 6 | CreateListingScreen — main form | sample 3 inputs spanning top / middle / bottom of form (e.g. title, address, description) | ⬜ | ⬜ | TBD |
  | 7 | ChatThreadScreen | composer TextInput (multiline — L10) + back-button-dismisses-keyboard | ⬜ | ⬜ | TBD |
  | 8 | ChatComposeScreen | composer TextInput (multiline — L10) | ⬜ | ⬜ | TBD |

  ## Tier 2 (Smoke)

  | Row # | Screen | Input | iOS | Android | Notes |
  |-------|--------|-------|-----|---------|-------|
  | 9 | AccountSettingsScreen | one TextInput while in edit mode | ⬜ | ⬜ | TBD |
  | 10 | HomeScreen | search TextInput | ⬜ | ⬜ | TBD |

  ## Out-of-scope (informational)

  | Row # | Screen | Input | Status |
  |-------|--------|-------|--------|
  | — | ScheduleViewingScreen | (none) | N/A — no inputs today (D-12) |

  ## L10 multiline composer verification (RESEARCH §8 L10)

  Multiline TextInputs that need explicit "field grows when content grows" verification on
  physical device (per RESEARCH §3.5 + §8 L10 — multiline TextInputs inside ScrollView under
  Fabric refuse to grow):

  - ChatThreadScreen composer (multiline, maxLength 2000) — paste a 5-line message, confirm composer grows past 1 line
  - ChatComposeScreen composer (multiline, maxLength 2000) — same test
  - ResetPasswordScreen linkOrCode (multiline, minHeight:88, textAlignVertical:'top') — paste a long reset link, confirm field grows

  | Test | iOS | Android | Notes |
  |------|-----|---------|-------|
  | ChatThread composer grows | ⬜ | ⬜ | TBD |
  | ChatCompose composer grows | ⬜ | ⬜ | TBD |
  | ResetPassword linkOrCode grows | ⬜ | ⬜ | TBD |

  ## Android back-button regression check (RESEARCH §3.5)

  Phase 1's BackHandler at App.tsx:184-297 + library's keyboard-dismiss-on-back must not conflict.

  | Test | Android only | Notes |
  |------|--------------|-------|
  | ChatThread: open keyboard → press hardware back → keyboard dismisses, screen does NOT pop | ⬜ | TBD |
  | ChatThread: keyboard dismissed → press hardware back AGAIN → screen pops back per Phase 1 expected behavior | ⬜ | TBD |

  ## HomeScreen workaround removal decision (CONTEXT D-Discretion + RESEARCH §3.6)

  After Tier 2 row 10 smoke test passes:
  - DECISION: { remove the comment at HomeScreen.tsx:465 / leave the comment in place }
  - RATIONALE: { evidence from device test }
  - Structural separation (renderHeaderContent as sibling above FlatList) is preserved either
    way — that's a non-keyboard rendering choice per RESEARCH §3.6.

  ## Phase-wide grep gates (mechanical — re-verify after matrix walk)

  | Gate | Command | Expected | Actual |
  |------|---------|----------|--------|
  | KBD-02 / SC3.a — zero magic offsets | `grep -rn "keyboardVerticalOffset" src/` | 0 lines | ⬜ |
  | KBD-04 / SC4.a — Fabric on | `grep -c "newArchEnabled=true" android/gradle.properties` | 1 | ⬜ |
  | SC1.a — reanimated installed | `grep -c "react-native-reanimated" package.json` | 1 | ⬜ |
  | SC1.b — worklets installed | `grep -c "react-native-worklets" package.json` | 1 | ⬜ |
  | SC1.c — Babel plugin LAST | `tail -n 5 babel.config.js \| grep -c "react-native-worklets/plugin"` | 1 | ⬜ |
  | SC2.a — keyboard-controller installed | `grep -c "react-native-keyboard-controller" package.json` | 1 | ⬜ |
  | SC2.b — KeyboardProvider in App.tsx | `grep -cE "KeyboardProvider" App.tsx` | ≥ 2 | ⬜ |
  | KBD-02 — chat surface clean | `grep -rn "keyboardVerticalOffset" src/screens/Chat*Screen.tsx` | 0 lines | ⬜ |
  | KBD-01 (auth) — KASV imports | `grep -l "KeyboardAwareScrollView" src/screens/{Login,Signup,ForgotPassword,ResetPassword}Screen.tsx \| wc -l` | 4 | ⬜ |
  | KBD-01 (settings + listing) — KASV imports | `grep -c "KeyboardAwareScrollView" src/screens/CreateListingScreen.tsx` is ≥ 2 AND `grep -c "KeyboardAwareScrollView" src/screens/AccountSettingsScreen.tsx` is ≥ 1 | both | ⬜ |
  | KBD-01 (chat) — library KAV imports | `grep -l "from 'react-native-keyboard-controller'" src/screens/Chat*Screen.tsx \| wc -l` | 2 | ⬜ |

CURRENT HOMESCREEN STATE (PATTERNS lines 791-845, src lines 302-317 + 459-483):

The search bar is a TextInput inside the header View, which sits ABOVE a FlatList (line 467).
Header is OUTSIDE the FlatList — that is the workaround documented at line 465:

  {/* Header outside FlatList to prevent TextInput focus issues */}

The "cleanup" that is OPTIONALLY in scope per RESEARCH §3.6:
  - DELETE just the comment line at HomeScreen.tsx:465 IF Tier 2 row 10 device test confirms
    the search bar works correctly with KeyboardProvider live.
  - Leave the structural separation (renderHeaderContent as sibling above FlatList) intact —
    that is also a non-keyboard rendering choice (image-cache / perf).
  - DO NOT restructure to use ListHeaderComponent or renderScrollComponent in this phase.
    D-05's renderScrollComponent rule applies only when the FlatList itself contains inputs;
    HomeScreen's search bar is OUTSIDE the FlatList per the existing structure.

WAVE 3 entry gate (Task 1 verifies):
  - 02-03-SUMMARY.md, 02-04-SUMMARY.md, 02-05-SUMMARY.md all exist
  - Combined grep: `grep -rn "keyboardVerticalOffset" src/` returns 0 (RESEARCH §4 Wave 2 exit gate)
  - All 9 KASV/KAV import sites present across the relevant files
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [Wave 3 entry gate] Verify Wave 2 combined exit gate before matrix creation</name>
  <read_first>
    - .planning/phases/02-universal-keyboard-handling/02-03-SUMMARY.md (Plan 03 — auth)
    - .planning/phases/02-universal-keyboard-handling/02-04-SUMMARY.md (Plan 04 — settings + listing)
    - .planning/phases/02-universal-keyboard-handling/02-05-SUMMARY.md (Plan 05 — chat)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §4 Wave 2 go/no-go gate
  </read_first>
  <files></files>
  <action>
    Verify Wave 2 (all three parallel plans) actually completed before any matrix walk begins.
    No file edits.

      # Wave 2 SUMMARY presence:
      [ -f .planning/phases/02-universal-keyboard-handling/02-03-SUMMARY.md ]
      [ -f .planning/phases/02-universal-keyboard-handling/02-04-SUMMARY.md ]
      [ -f .planning/phases/02-universal-keyboard-handling/02-05-SUMMARY.md ]

      # Wave 2 combined grep gate (RESEARCH §4):
      [ $(grep -rn "keyboardVerticalOffset" src/ | wc -l) -eq 0 ]   # KBD-02 + SC3.a

      # Library imports across all 9 wrap sites — minimum count:
      grep -l "KeyboardAwareScrollView" src/screens/LoginScreen.tsx \
                                         src/screens/SignupScreen.tsx \
                                         src/screens/ForgotPasswordScreen.tsx \
                                         src/screens/ResetPasswordScreen.tsx \
                                         src/screens/AccountSettingsScreen.tsx \
                                         src/screens/CreateListingScreen.tsx | wc -l   # MUST be 6

      grep -l "from 'react-native-keyboard-controller'" src/screens/ChatThreadScreen.tsx \
                                                         src/screens/ChatComposeScreen.tsx | wc -l   # MUST be 2

    If any check fails, ABORT and surface to user — Wave 3 cannot proceed without all of Wave 2.

    No commit in this task — verification only.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-universal-keyboard-handling/02-03-SUMMARY.md && test -f .planning/phases/02-universal-keyboard-handling/02-04-SUMMARY.md && test -f .planning/phases/02-universal-keyboard-handling/02-05-SUMMARY.md && [ $(grep -rn "keyboardVerticalOffset" src/ | wc -l) -eq 0 ] && echo "WAVE_3_ENTRY_OK"</automated>
  </verify>
  <acceptance_criteria>
    - All three Wave 2 SUMMARY files exist (`test -f` exits 0 for each)
    - `grep -rn "keyboardVerticalOffset" src/ | wc -l` returns 0 (Wave 2 combined exit gate per RESEARCH §4)
    - 6 KASV-wrapping screens have the import (`grep -l "KeyboardAwareScrollView" {Login,Signup,ForgotPassword,ResetPassword,AccountSettings,CreateListing}.tsx | wc -l` returns 6)
    - 2 chat screens import library KAV (`grep -l "from 'react-native-keyboard-controller'" Chat*Screen.tsx | wc -l` returns 2)
    - No git commit in this task
  </acceptance_criteria>
  <done>Wave 3 entry gate verified — Wave 2 (all 3 parallel plans) complete. Plan cleared to proceed to matrix creation.</done>
</task>

<task type="auto">
  <name>Task 2: Create 02-KEYBOARD-MATRIX.md skeleton with all Tier 1 + Tier 2 + L10 + back-button + grep-gate cells stubbed</name>
  <read_first>
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §7.4 (Tier 1 + Tier 2 row definitions)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §7.6 (matrix doc location)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §6 (SC → evidence mechanical exit gate set)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §8 L10 (multiline composer growth verify)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §3.5 (Android back button regression check)
    - .planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md (read library version + device IDs to populate matrix header)
    - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md (Phase 1 matrix shape — inherit table conventions / cell markers)
  </read_first>
  <files>.planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md</files>
  <action>
    Create `.planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` with the
    verbatim skeleton from <interfaces> above. Fill the **Captured:** date with today's date,
    fill the **Library versions** block from 02-01-SUMMARY.md, fill the **Devices** block
    from 02-01-SUMMARY.md (or D-13 defaults if SUMMARY didn't capture them — D-13's defaults
    are iPhone 15 Pro Max / iOS 26.4 + Moto G XT2513V / Android 16).

    All cells in the Tier 1, Tier 2, L10, back-button, HomeScreen-decision, and grep-gate
    tables MUST be stubbed with `⬜` (pending marker per Phase 1's matrix convention) so
    Tasks 3-5 can fill them in.

    Cell markers (per Phase 1 matrix convention):
      ✅ Pass · ❌ Fail · — N/A · ⬜ Pending

    Document structure (verbatim from <interfaces> above) — no deviations. Section headers
    must match exactly so the §6 exit-gate verifier can grep for `## Tier 1`, `## Tier 2`,
    `## L10`, `## Android back-button`, `## HomeScreen workaround removal decision`,
    `## Phase-wide grep gates`.

    The HomeScreen workaround removal decision section starts with `DECISION: {pending}` and
    `RATIONALE: {pending}` — Task 5 fills it in after Tier 2 row 10 device test.

    Run a quick sanity-grep on the created file:

      grep -c "^## " .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md
      # MUST return ≥ 7 (Cell markers, Tier 1, Tier 2, Out-of-scope, L10, Android back-button,
      # HomeScreen decision, Phase-wide grep gates)

      grep -c "| ⬜ |" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md
      # MUST return ≥ 20 (Tier 1 has 8 rows × 2 platforms = 16 cells; Tier 2 has 2 × 2 = 4 cells;
      # L10 has 3 × 2 = 6 cells; back-button has 2 cells; grep gates have ≥ 11 — total ≥ 39
      # but rough lower bound is 20 to catch obvious dropped sections)

    Commit atomically:

      git add .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md
      git commit -m "docs(02): create keyboard verification matrix skeleton (KBD-01, KBD-04, RESEARCH §7.4 / §7.6)"

    Commit body: notes the matrix is skeleton-only at this commit; cells are pending; Tasks
    3-5 (human-verify) populate them on physical devices.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md && grep -c "^## " .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md && grep -c "Tier 1" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md && grep -c "Tier 2" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` exits 0
    - `grep -c "^## " .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns ≥ 7
    - `grep -c "## Tier 1" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns 1
    - `grep -c "## Tier 2" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns 1
    - `grep -c "## L10 multiline" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns 1
    - `grep -c "## Android back-button" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns 1
    - `grep -c "## HomeScreen workaround removal decision" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns 1
    - `grep -c "## Phase-wide grep gates" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns 1
    - `grep -c "iPhone 15 Pro Max" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns ≥ 1 (D-13 device captured)
    - `grep -c "Moto G XT2513V" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns ≥ 1 (D-13 device captured)
    - `grep -c "newArchEnabled=true" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns ≥ 1 (KBD-04 evidence anchor)
    - `grep -c "| ⬜ |" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns ≥ 20 (cells stubbed for human-fill)
    - `git diff HEAD~1 --name-only` returns ONLY `.planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md`
    - `git log --oneline -1` shows a commit message containing "matrix" and "RESEARCH §7"
  </acceptance_criteria>
  <done>02-KEYBOARD-MATRIX.md skeleton committed with all rows + cells stubbed and pending markers ready for human fill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Walk Tier 1 (8 screens × 2 platforms = 16 cells) + L10 multiline + back-button on both physical devices</name>
  <what-built>
    - 02-KEYBOARD-MATRIX.md skeleton ready for cell population
    - All 9 wrap sites landed across Plans 03/04/05:
      * 4 auth screens (KASV per Plan 03)
      * AccountSettings + CreateListing both branches (KASV per Plan 04)
      * Chat Thread + Compose (library KAV per Plan 05)
    - Wave 2 combined exit gate green (zero `keyboardVerticalOffset` matches in src/)
    - Library installed and KeyboardProvider wired (Plans 01 + 02)
  </what-built>
  <how-to-verify>
    Human, please walk Tier 1 of the matrix on BOTH devices. Edit
    `.planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` in place to fill
    the cells. The matrix is a markdown table — replace `⬜` with `✅ Pass` or `❌ Fail` per
    cell, plus a one-line note in the Notes column describing what was tested.

    **TIER 1 (FULL TEST per CONTEXT D-12):**

    For each screen below, on EACH platform (iOS iPhone 15 Pro Max + Android Moto G):

    Row 1 — LoginScreen:
      - Open Profile → Sign In modal
      - Tap email TextInput. Expected: keyboard pops, email field stays visible above keyboard.
      - Tap PasswordTextInput. Expected: same — password field stays visible above keyboard.
      - Tap the eye icon (PasswordTextInput visibility toggle) while keyboard is open. Expected:
        eye icon responds (visibility toggles); keyboardShouldPersistTaps='handled' should make
        this work without dismissing the keyboard first.
      - PASS criterion: both fields visible above keyboard; eye icon works while keyboard open.

    Row 2 — SignupScreen:
      - Profile → Sign In modal → Sign Up link
      - Tap email, password, confirm password (each). Expected: all three fields stay visible
        above keyboard.
      - PasswordRequirements component should remain visible (sits between password + confirm).
      - PASS criterion: all 3 inputs visible above keyboard.

    Row 3 — ForgotPasswordScreen:
      - Profile → Sign In modal → Forgot password? link
      - On screen mount, the email TextInput has autoFocus → keyboard pops IMMEDIATELY.
      - Expected: email field is visible above the keyboard from the moment the screen appears.
        (This is the autoFocus initial-pop case noted in PATTERNS line 424.)
      - PASS criterion: email field visible above keyboard at screen-mount time.

    Row 4 — ResetPasswordScreen (LOAD-BEARING — multiline L10 risk):
      - Reach the screen via deep link OR via Forgot password → Sent → tap "I have a code".
      - Tap linkOrCode TextInput. Expected: field visible above keyboard.
      - PASTE A LONG STRING (5+ lines) into linkOrCode. Expected: the multiline field GROWS
        to fit the content (RESEARCH §8 L10 — verify it doesn't refuse to grow under Fabric).
      - Tap newPassword PasswordTextInput. Expected: field visible above keyboard.
      - Tap confirm PasswordTextInput. Expected: field visible above keyboard.
      - PASS criterion: all 3 inputs visible above keyboard AND linkOrCode grows when content
        grows (capture in L10 section row 3 too).

    Row 5 — CreateListingScreen admin-verify branch:
      - Sign in as admin email. Profile → Create Listing → Edit existing listing → Verification
        section (admin only sees this branch).
      - This branch has no TextInputs today — verify the page scrolls cleanly and 3x Switch
        rows + save button respond. KASV degrades to inert ScrollView when no input is focused.
      - PASS criterion: page scrolls; switches toggle; save button taps register.

    Row 6 — CreateListingScreen main form:
      - Profile → Create Listing (any property type)
      - Sample 3 inputs spanning the form: title (top), address (middle), description (bottom).
      - Tap each. Expected: each focused field scrolls above the keyboard.
      - PASS criterion: all 3 sampled inputs visible above keyboard.

    Row 7 — ChatThreadScreen (LOAD-BEARING — multiline L10 risk + back-button regression):
      - Open any chat conversation
      - Tap composer TextInput. Expected: composer rises with keyboard; FlatList not pushed
        off-screen.
      - Type a long multi-line message (press return / shift-return). Expected: composer GROWS
        past 1 line to fit content (L10 verify — capture in L10 section row 1 too).
      - Send the message. Expected: message appears in the FlatList; composer clears.
      - **Android only:** With keyboard still open, press hardware back. Expected: keyboard
        dismisses; chat screen does NOT pop. (Capture in Android back-button section.)
      - **Android only:** Press hardware back AGAIN. Expected: chat screen pops back to chat
        list per Phase 1's BackHandler behavior.
      - PASS criterion: composer + L10 + back-button-no-conflict all green.

    Row 8 — ChatComposeScreen (LOAD-BEARING — multiline L10 risk):
      - From chat list → tap the new-message button (or equivalent route to ChatComposeScreen).
      - Tap composer TextInput. Expected: composer rises with keyboard.
      - Type a long multi-line message. Expected: composer GROWS past 1 line (L10 verify —
        capture in L10 section row 2 too).
      - PASS criterion: composer visible above keyboard + grows correctly.

    **L10 SECTION:** Capture each multi-line growth result (3 rows × 2 platforms = 6 cells).

    **ANDROID BACK-BUTTON SECTION:** Capture both back-button results (Android only — 2 cells).

    Decision rule for Tier 1 + L10 + back-button:
      - Every cell `Pass` → proceed to Task 4 (Tier 2 + HomeScreen decision).
      - ANY cell `Fail` → STOP. Do NOT proceed. Report the failure to the executor:
        * Identify which screen × platform failed.
        * Capture a short description (1-2 sentences) of what went wrong.
        * Most likely causes per RESEARCH:
          - Multiline composer doesn't grow on iOS Fabric (L10) → may require KeyboardStickyView
            (out-of-scope expansion D-10) — escalate to user
          - Field still covered by keyboard → KeyboardProvider misplacement (re-grep App.tsx)
          - Eye icon doesn't toggle while keyboard open → keyboardShouldPersistTaps regression
          - Android back pops chat → BackHandler conflict (RESEARCH §3.5 fix path documented)

    Capture device-test session metadata in your approval message:
      - Date / time of session
      - iOS device + OS version (confirm matches D-13 default or note device used)
      - Android device + OS version (same)
      - Confirmation that build was Fabric / New Arch (`grep -c "newArchEnabled=true"
        android/gradle.properties` returns 1)
  </how-to-verify>
  <acceptance_criteria>
    - Human reports: 02-KEYBOARD-MATRIX.md Tier 1 cells (16 total) all filled with `Pass` (or
      itemized failure description with which row × platform failed)
    - Human reports: L10 section cells (6 total) all filled with `Pass` (or itemized failure)
    - Human reports: Android back-button section cells (2 total) all filled with `Pass` (or
      itemized failure)
    - Mechanical re-grep: `grep -c "Fail" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns 0 in the Tier 1 + L10 + back-button regions (per RESEARCH §7.5 phase-exit predicate)
    - Mechanical re-grep: `grep -c "Pass" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns ≥ 24 (16 Tier 1 + 6 L10 + 2 back-button minimum)
    - Resume signal received: "Tier 1 + L10 + back-button all PASS" with device metadata
  </acceptance_criteria>
  <resume-signal>
    Type one of:
    - "Tier 1 + L10 + back-button all PASS — proceed to Tier 2 + HomeScreen decision"
      + iOS device confirmed + Android device confirmed + Fabric build confirmed
    - "Tier 1 FAILED on {row#} × {iOS|Android}" + describe the failure mode
    - "L10 FAILED on {ChatThread|ChatCompose|ResetPassword} × {iOS|Android}" + describe
    - "Android back-button FAILED" + describe what happened
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Walk Tier 2 (AccountSettings + HomeScreen smoke = 4 cells) on both devices</name>
  <what-built>
    - Tier 1 + L10 + back-button all PASS (per Task 3)
    - Matrix doc has Tier 1 cells filled; Tier 2 + HomeScreen-decision sections still pending
  </what-built>
  <how-to-verify>
    Human, please walk Tier 2 (smoke tier per CONTEXT D-12) on both devices. Same matrix file.

    **TIER 2 (SMOKE TEST per CONTEXT D-12):**

    Row 9 — AccountSettingsScreen:
      - Profile → Account Settings
      - Tap an info row (e.g. Name) to enter edit mode → renders TextInput inline.
      - Tap the TextInput. Expected: field visible above keyboard.
      - PASS criterion: at least one TextInput in edit mode is visible above keyboard.

    Row 10 — HomeScreen search:
      - Home tab (cold-start or navigate)
      - Tap the search TextInput at the top of the screen.
      - Expected: search field visible above the keyboard. Type a few characters; filtered
        results appear in the FlatList below.
      - PASS criterion: search field visible above keyboard; filtering works.

    **CRITICAL — HomeScreen workaround decision:** Row 10's outcome drives Task 5's decision.

    If Row 10 PASSES on both platforms (search field works correctly with KeyboardProvider live),
    you can:
      a. Approve removing the comment workaround (`{/* Header outside FlatList... */}`)
      b. Choose to leave it (no harm)
      → Either way, the structural separation (renderHeaderContent as sibling) STAYS.

    If Row 10 FAILS on either platform (search field doesn't work or shows the original
    focus issue):
      → DO NOT approve removing the comment. Task 5 will document the rationale and leave
        the workaround in place.

    Decision rule for Tier 2:
      - Both cells `Pass` → proceed to Task 5 (HomeScreen decision + grep-gate verify + sign-off)
      - Either cell `Fail` → smoke-tier failure is recoverable per RESEARCH §7.5; document in
        matrix; either accept-with-note OR open follow-up task. Does not block phase exit IF
        user accepts in writing during this checkpoint.

    Capture in your approval message:
      - AccountSettings result on each platform
      - HomeScreen search result on each platform
      - Recommendation for Task 5: `remove HomeScreen workaround comment` OR
        `keep HomeScreen workaround comment` (with rationale)
  </how-to-verify>
  <acceptance_criteria>
    - Human reports: 02-KEYBOARD-MATRIX.md Tier 2 cells (4 total) all filled with `Pass` or
      `Fail` (with note)
    - Human reports: explicit recommendation for HomeScreen workaround comment removal
      (`remove` / `keep` + rationale)
    - Mechanical: `grep -c "Pass" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns ≥ 28 (Tier 1 16 + L10 6 + back-button 2 + Tier 2 4)
    - If any Tier 2 cell `Fail`: human has explicitly written acceptance in the approval message OR opened a follow-up task — phase exit not blocked
    - Resume signal: "Tier 2 walk complete — recommend {remove|keep} HomeScreen workaround"
  </acceptance_criteria>
  <resume-signal>
    Type one of:
    - "Tier 2 PASS — recommend REMOVE HomeScreen comment workaround at line 465"
    - "Tier 2 PASS — recommend KEEP HomeScreen comment workaround (rationale: ...)"
    - "Tier 2 row {9|10} FAILED on {iOS|Android} — accepting as known issue OR opening follow-up"
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: HomeScreen decision execution + final phase-exit grep verification + sign-off</name>
  <read_first>
    - .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md (just-filled cells from Tasks 3+4; HomeScreen decision section still showing DECISION: {pending})
    - src/screens/HomeScreen.tsx (verify line 465 still has the workaround comment per the verbatim BEFORE state)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §3.6 (HomeScreen cleanup conditional logic)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §6 (combined exit-gate command set)
    - .planning/phases/02-universal-keyboard-handling/02-VALIDATION.md (frontmatter to flip)
  </read_first>
  <files>src/screens/HomeScreen.tsx, .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md, .planning/phases/02-universal-keyboard-handling/02-VALIDATION.md</files>
  <action>
    Read the human's recommendation from Task 4 resume-signal. Two branches:

    **BRANCH A — Recommendation was REMOVE the HomeScreen workaround comment:**

    Edit src/screens/HomeScreen.tsx line 465. Find this exact line (PATTERNS line 826):

      {/* Header outside FlatList to prevent TextInput focus issues */}

    DELETE the entire line. The structural code around it stays:

      ) : (
        <View style={styles.contentContainer}>
          {renderHeaderContent()}                      ← STAYS (sibling above FlatList)
          <FlatList ...>...</FlatList>                  ← STAYS
        </View>
      )

    Verify ONLY the comment was deleted (not the structural call to renderHeaderContent):

      grep -c "Header outside FlatList" src/screens/HomeScreen.tsx          # MUST return 0
      grep -c "renderHeaderContent()" src/screens/HomeScreen.tsx            # MUST return ≥ 1 (structural call preserved)

    Run TypeScript check, commit atomically:

      npx tsc --noEmit
      git add src/screens/HomeScreen.tsx
      git commit -m "chore(02): remove HomeScreen header-outside-FlatList workaround comment after KeyboardProvider verify (CONTEXT D-Discretion)"

    Update the matrix HomeScreen-decision section:
      - DECISION: REMOVE — comment deleted at line 465; structural separation preserved
      - RATIONALE: Tier 2 row 10 device test confirmed search bar works correctly with
        KeyboardProvider live on both platforms; comment is now stale.

    **BRANCH B — Recommendation was KEEP the HomeScreen workaround comment:**

    No HomeScreen.tsx edit. Update the matrix HomeScreen-decision section:
      - DECISION: KEEP — comment preserved at line 465; structural separation also preserved
      - RATIONALE: {paste user's rationale from Task 4 resume signal}

    --- Common to both branches: ---

    Run the final phase-wide grep gates and fill the matrix's "Phase-wide grep gates" table
    with `Actual` values:

      # Re-run each gate command, capture the actual return value, write into the matrix table:

      grep -rn "keyboardVerticalOffset" src/ | wc -l                          # Expected: 0
      grep -c "newArchEnabled=true" android/gradle.properties                  # Expected: 1
      grep -c "react-native-reanimated" package.json                            # Expected: 1
      grep -c "react-native-worklets" package.json                              # Expected: 1
      tail -n 5 babel.config.js | grep -c "react-native-worklets/plugin"        # Expected: 1
      grep -c "react-native-keyboard-controller" package.json                   # Expected: 1
      grep -cE "KeyboardProvider" App.tsx                                       # Expected: ≥ 2
      grep -rn "keyboardVerticalOffset" src/screens/Chat*Screen.tsx | wc -l    # Expected: 0
      grep -l "KeyboardAwareScrollView" src/screens/{Login,Signup,ForgotPassword,ResetPassword}Screen.tsx | wc -l  # Expected: 4
      grep -c "KeyboardAwareScrollView" src/screens/CreateListingScreen.tsx    # Expected: ≥ 2
      grep -c "KeyboardAwareScrollView" src/screens/AccountSettingsScreen.tsx  # Expected: 1 (or ≥ 1)
      grep -l "from 'react-native-keyboard-controller'" src/screens/Chat*Screen.tsx | wc -l   # Expected: 2

    Replace each `⬜` in the matrix's grep-gates table with the `Actual` value. Every row's
    Actual MUST match Expected. If any does NOT match, STOP — surface to user, do NOT sign
    off. Phase exit is blocked until all 12 gates pass.

    Update 02-VALIDATION.md frontmatter:

      ---
      phase: 2
      slug: universal-keyboard-handling
      status: complete                  # was draft
      nyquist_compliant: true           # was false
      wave_0_complete: true             # was false
      created: 2026-04-22
      completed: {today's date}
      ---

    Commit the matrix update + VALIDATION update together as the phase sign-off:

      git add .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md \
              .planning/phases/02-universal-keyboard-handling/02-VALIDATION.md
      git commit -m "docs(02): Phase 2 sign-off — matrix all-Pass, VALIDATION complete (KBD-01/02/03/04)"

    Commit body: lists the 4 Tier 1 + 2 Tier 2 + 3 L10 + 2 back-button + 12 grep-gate
    confirmations; references Plans 01-05 commit SHAs as the phase deliverable manifest;
    notes HomeScreen decision (REMOVE or KEEP) per the human's verified recommendation.
  </action>
  <verify>
    <automated>grep -c "Fail" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md && grep -c "DECISION: " .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md && grep -c "nyquist_compliant: true" .planning/phases/02-universal-keyboard-handling/02-VALIDATION.md && grep -c "status: complete" .planning/phases/02-universal-keyboard-handling/02-VALIDATION.md && grep -rn "keyboardVerticalOffset" src/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Matrix HomeScreen-decision section has `DECISION: REMOVE` OR `DECISION: KEEP` filled (no `pending` left)
    - Matrix grep-gates table has all `Actual` cells filled with the actual command return values
    - `grep -c "Fail" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns 0 in the Tier 1 region (phase exit predicate per RESEARCH §7.5)
    - **Phase-wide combined exit-gate set (RESEARCH §6) — ALL must hold:**
      * `grep -c "react-native-reanimated" package.json` returns 1 (SC1.a)
      * `grep -c "react-native-worklets" package.json` returns 1 (SC1.b)
      * `grep -c "react-native-worklets/plugin" babel.config.js` returns 1 (SC1.c)
      * `grep -c "react-native-keyboard-controller" package.json` returns 1 (SC2.a)
      * `grep -cE "KeyboardProvider" App.tsx` returns ≥ 2 (SC2.b)
      * `grep -rn "keyboardVerticalOffset" src/ | wc -l` returns 0 (SC3.a + KBD-02 phase exit)
      * `grep -c "newArchEnabled=true" android/gradle.properties` returns 1 (SC4.a + KBD-04)
    - `grep -c "nyquist_compliant: true" .planning/phases/02-universal-keyboard-handling/02-VALIDATION.md` returns 1
    - `grep -c "status: complete" .planning/phases/02-universal-keyboard-handling/02-VALIDATION.md` returns 1
    - If BRANCH A: `grep -c "Header outside FlatList" src/screens/HomeScreen.tsx` returns 0; `grep -c "renderHeaderContent()" src/screens/HomeScreen.tsx` returns ≥ 1
    - If BRANCH B: `grep -c "Header outside FlatList" src/screens/HomeScreen.tsx` returns ≥ 1 (preserved)
    - `npx tsc --noEmit` exits 0 (or no NEW errors)
    - 1-2 atomic commits in `git log --oneline | head -2`: optional HomeScreen edit (BRANCH A) + matrix/VALIDATION sign-off
  </acceptance_criteria>
  <done>HomeScreen decision executed; matrix all-Pass with grep-gates filled; 02-VALIDATION.md flipped to complete + nyquist_compliant. Phase 2 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan creates a new markdown matrix doc, optionally
deletes one comment line in HomeScreen.tsx (no functional change — the structural code stays),
and flips 02-VALIDATION.md frontmatter values. No code logic changes; no new auth surface,
HTTP, storage, or PII handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | I (Information Disclosure) | 02-KEYBOARD-MATRIX.md captures device IDs (iPhone 15 Pro Max / Moto G XT2513V) and session timestamps | accept | Device IDs are model + OS version (e.g. "iPhone 15 Pro Max / iOS 26.4") — not unique device serial numbers / IMEIs. No PII. Same convention as Phase 1's 01-REPRO-MATRIX.md. |
| T-02-06-02 | T (Tampering) | HomeScreen workaround removal accidentally deletes structural code, not just the comment | mitigate | Task 5 BRANCH A explicit acceptance criterion checks `grep -c "renderHeaderContent()" returns ≥ 1` AFTER the comment removal — proves the structural separation (renderHeaderContent as sibling above FlatList) is preserved. PATTERNS §HomeScreen line 847-851 explicitly states "leave the structural separation intact." |
| T-02-06-03 | T (Tampering) | Phase-wide grep gates report false-pass (matrix cells filled without the gate actually returning the expected value) | mitigate | Task 5 acceptance criteria re-run each gate command and check return values — not just check the matrix says Pass. The mechanical exit-gate set from RESEARCH §6 is enforced at the file-system level, not at the markdown-readback level. |

No S/R/D/E applicable. Block on `high` severity per project default; zero high-severity threats raised.
Phase 2 sign-off is presentational + verification — `<security_threat_model>` from the downstream
consumer brief explicitly noted "no new threat surface" for verification plans.
</threat_model>

<verification>
- 02-KEYBOARD-MATRIX.md exists; ≥ 7 sections; all Tier 1 + Tier 2 + L10 + back-button cells filled
- `grep -c "Fail" .planning/phases/02-universal-keyboard-handling/02-KEYBOARD-MATRIX.md` returns 0 in Tier 1 region
- All 12 phase-wide grep gates from RESEARCH §6 PASS (mechanical re-verification at sign-off)
- HomeScreen workaround removal decision documented (REMOVE or KEEP) with rationale
- If BRANCH A executed: HomeScreen comment removed, structural code preserved
- 02-VALIDATION.md frontmatter: status=complete, nyquist_compliant=true, wave_0_complete=true
- `npx tsc --noEmit` exits 0
- 2-3 atomic commits committed (matrix skeleton + optional HomeScreen edit + sign-off)
- Human verified Tier 1 + L10 + back-button all PASS on both physical devices (Tasks 3-4)
- Human verified Tier 2 PASS or accepted with note on both physical devices (Task 4)
</verification>

<success_criteria>
- KBD-01 ✓ — full Tier 1 (8 screens) + Tier 2 (2 screens) device verification matrix all-Pass
- KBD-02 ✓ — phase-wide grep `grep -rn "keyboardVerticalOffset" src/ | wc -l` returns 0 (zero magic-number anti-patterns survive)
- KBD-04 ✓ — Fabric build mode explicitly recorded in matrix; grep `newArchEnabled=true` confirms
- §6 SC1-SC4 combined exit-gate set ALL hold
- L10 multiline composer growth verified on 3 sites × 2 platforms = 6 cells
- Android back-button regression check verified — no conflict with Phase 1's BackHandler
- HomeScreen workaround comment decision executed (REMOVE or KEEP) with rationale documented
- 02-VALIDATION.md frontmatter complete + nyquist_compliant
- Phase 2 closed; STATE.md update task in 02-06-SUMMARY.md identifies the lines to amend
- Phase 3 (Role Gating Precursor) is unblocked — both Phase 1 (Nav Reliability) and Phase 2
  (Universal Keyboard Handling) are now complete; Phase 3 may begin per ROADMAP build order
</success_criteria>

<output>
After completion, create `.planning/phases/02-universal-keyboard-handling/02-06-SUMMARY.md` documenting:
- Matrix all-cells final state (paste a copy of the filled matrix or summarize cell counts:
  Tier 1 8/8 Pass × 2 platforms = 16/16; Tier 2 2/2 Pass × 2 platforms = 4/4; L10 3/3 × 2 = 6/6;
  back-button 2/2 Android = 2/2; grep gates 12/12)
- HomeScreen workaround removal decision (REMOVE or KEEP) + rationale
- HomeScreen.tsx commit SHA (only if BRANCH A executed)
- Matrix doc commit SHAs (Task 2 skeleton + Task 5 sign-off)
- 02-VALIDATION.md commit SHA (Task 5 sign-off)
- Library versions captured at sign-off time (reanimated, worklets, keyboard-controller)
- Device IDs captured at sign-off time (iOS + Android per D-13)
- Confirmation of phase-wide grep-gate results (paste the final 12-row Actual column from the matrix)
- Pointer to STATE.md update lines required:
  * Update **Last updated:** to today
  * Update **Phase:** from "Phase 2 — Universal Keyboard Handling (not started)" to
    "Phase 2 — Universal Keyboard Handling (complete — closed {date})"
  * Update **Status:** with Phase 2 closure summary + library versions installed
  * Update **Progress:** [██░░░░░░] 2/8 phases complete
  * Update **Phase pipeline** table — Phase 2 marked Complete with date
- Pointer to ROADMAP.md update lines required:
  * Update Phase 2 entry in **Phases** list — change `[ ]` to `[x]` and add completion date
  * Update Phase 2 **Plans:** TBD → 6 plans + list 02-01..02-06-PLAN.md with brief outcomes
  * Update Progress table at bottom — Phase 2 row marked Complete
- Explicit statement: "Phase 2 (Universal Keyboard Handling) is COMPLETE. Phase 3 (Role
  Gating Precursor) and any other downstream phase that requires keyboard reliability is
  unblocked. Next action: `/gsd-plan-phase 3`."
</output>
</content>
</invoke>