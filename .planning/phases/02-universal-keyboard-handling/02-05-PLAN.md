---
phase: 02-universal-keyboard-handling
plan: 05
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/screens/ChatThreadScreen.tsx
  - src/screens/ChatComposeScreen.tsx
autonomous: true
requirements: [KBD-01, KBD-02]
requirements_addressed: [KBD-01, KBD-02]
threat_model_complete: true
tags: [react-native, keyboard, kav, chat-screens, drop-in-replacement, magic-number-removal]

must_haves:
  truths:
    - "ChatThreadScreen imports KeyboardAvoidingView from 'react-native-keyboard-controller' (NOT from 'react-native')"
    - "ChatComposeScreen imports KeyboardAvoidingView from 'react-native-keyboard-controller' (NOT from 'react-native')"
    - "Both files have KeyboardAvoidingView REMOVED from their 'react-native' import block"
    - "Both files have keyboardVerticalOffset prop REMOVED entirely (this is the canonical anti-pattern PITFALLS §Pitfall 2 calls out)"
    - "Both files have behavior={Platform.OS === 'ios' ? 'padding' : undefined} prop REMOVED — library defaults handle behavior cross-platform"
    - "Platform import is preserved on both files (Platform.OS likely used elsewhere — pre-grep verifies before removing)"
    - "FlatList + composer body inside the KAV wrapper is unchanged — D-09 specifies ~5 lines per file changed; no FlatList rewrite, no inverted-list refactor"
    - "multiline TextInput on chat composer is preserved verbatim (RESEARCH §8 L10 — verify in Plan 06 D-12 Tier 1 rows 7-8)"
    - "KeyboardStickyView is NOT introduced (CONTEXT D-10 deferred to M2)"
    - "The grep gate `grep -rn 'keyboardVerticalOffset' src/` returns 0 after both files committed (KBD-02 + §6 SC3.a phase exit predicate is satisfied for chat surface)"
  artifacts:
    - path: "src/screens/ChatThreadScreen.tsx"
      provides: "Imports library KAV; removes RN KAV import; removes keyboardVerticalOffset + behavior props"
      contains: "from 'react-native-keyboard-controller'"
    - path: "src/screens/ChatComposeScreen.tsx"
      provides: "Imports library KAV; removes RN KAV import; removes keyboardVerticalOffset + behavior props"
      contains: "from 'react-native-keyboard-controller'"
  key_links:
    - from: "Both chat screens"
      to: "react-native-keyboard-controller package + KeyboardProvider context (Plan 01 + Plan 02)"
      via: "import { KeyboardAvoidingView } from 'react-native-keyboard-controller'; (drop-in replacement)"
      pattern: "from 'react-native-keyboard-controller'"
    - from: "Library's KeyboardAvoidingView component"
      to: "KeyboardProvider's frame events (height + animation duration)"
      via: "Library default behavior — no behavior or offset props needed; library reads keyboard frame from KeyboardProvider context"
      pattern: "<KeyboardAvoidingView style={styles.keyboardView}>"
---

<objective>
Drop-in replace React Native's built-in `KeyboardAvoidingView` with `react-native-keyboard-controller`'s
`KeyboardAvoidingView` in the two chat screens. Per CONTEXT D-09 + RESEARCH §3.5 this is a
~5-line diff per file: change the import source, remove the `keyboardVerticalOffset` prop
entirely, remove the `behavior` prop entirely. The library's default behavior handles offset
+ behavior cross-platform via the root `KeyboardProvider` (wired in Plan 02).

Purpose: KBD-01 for D-12 Tier 1 rows 7-8 (ChatThreadScreen + ChatComposeScreen) + KBD-02
satisfaction (the `keyboardVerticalOffset={Platform.OS === 'ios' ? 90 : 0}` magic number is
the canonical anti-pattern PITFALLS §Pitfall 2 calls out — Phase 2's exit gate
`grep -rn "keyboardVerticalOffset" src/` returns 0 only after this plan commits).

Output: Two files modified, two atomic commits (one per file so each can be bisected
independently if Plan 06 device verification surfaces a per-screen regression). Two structural
twins per PATTERNS §"Chat Screen Pattern" — same diff shape applied to both.

This plan is `autonomous: true`. Per-screen physical-device verification happens in Plan 06.
All grep gates here are mechanical and self-checking.

Critical: Wave 2 entry gate — Task 1 of this plan re-verifies App.tsx has KeyboardProvider
wired (same gate as Plans 03/04). Plans 03/04/05 are mutually independent (no shared file
edits) and parallel-safe.

KeyboardStickyView (composer pinned above keyboard, iMessage/Telegram-style) is EXPLICITLY
out of scope per CONTEXT D-10 (deferred to M2 Chat polish). Do NOT attempt it. The drop-in
KAV is sufficient for M1 ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-universal-keyboard-handling/02-CONTEXT.md
@.planning/phases/02-universal-keyboard-handling/02-RESEARCH.md
@.planning/phases/02-universal-keyboard-handling/02-PATTERNS.md
@.planning/phases/02-universal-keyboard-handling/02-VALIDATION.md
@.planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
@src/screens/ChatThreadScreen.tsx
@src/screens/ChatComposeScreen.tsx

<interfaces>
<!-- VERBATIM BEFORE state, copied from PATTERNS.md §"Chat Screen Pattern" -->
<!-- Both screens are structural twins per PATTERNS line 640. -->

CHATTHREADSCREEN — current import block (PATTERNS lines 644-657, src lines 1-13):

  import React, { useState, useEffect, useRef, useCallback } from 'react';
  import {
    View,
    Text,
    StyleSheet,
    FlatList,
    TextInput,
    TouchableOpacity,
    ActivityIndicator,
    Alert,
    KeyboardAvoidingView,         // ← REMOVE this entry
    Platform,                      // ← KEEP (Platform.OS may be used elsewhere — pre-grep verifies)
  } from 'react-native';

CHATTHREADSCREEN — current KeyboardAvoidingView opening tag (PATTERNS lines 663-667, src lines 183-187):

  <KeyboardAvoidingView
    style={styles.keyboardView}
    behavior={Platform.OS === 'ios' ? 'padding' : undefined}
    keyboardVerticalOffset={Platform.OS === 'ios' ? 90 : 0}
  >

CHATTHREADSCREEN — wrapped composer body (PATTERNS lines 673-705, src lines 188-219, UNCHANGED):

  <FlatList
    ref={flatListRef}
    data={messages}
    keyExtractor={(item) => item.id}
    renderItem={renderMessage}
    contentContainerStyle={styles.messagesList}
    onContentSizeChange={() => flatListRef.current?.scrollToEnd({ animated: true })}
  />
  <View style={[styles.inputRow, { backgroundColor: colors.surface, borderTopColor: colors.border }]}>
    <TextInput
      style={[styles.input, { color: colors.text, backgroundColor: colors.inputBackground }]}
      placeholder="Type a message..."
      placeholderTextColor={colors.textSecondary}
      value={inputText}
      onChangeText={setInputText}
      multiline                      {/* L10 risk — verify in Plan 06 row 7 */}
      maxLength={2000}
      editable={!sending}
    />
    <TouchableOpacity ...send button.../>
  </View>
  </KeyboardAvoidingView>

CHATCOMPOSESCREEN — current import block (PATTERNS lines 716-728, src lines 1-13):

  import React, { useState } from 'react';
  import {
    View,
    Text,
    StyleSheet,
    TextInput,
    TouchableOpacity,
    ActivityIndicator,
    Alert,
    KeyboardAvoidingView,         // ← REMOVE this entry
    Platform,                      // ← KEEP (pre-grep verifies)
    Image,
  } from 'react-native';

CHATCOMPOSESCREEN — current KeyboardAvoidingView opening tag (PATTERNS lines 734-738, src lines 114-118):

  <KeyboardAvoidingView
    style={styles.keyboardView}
    behavior={Platform.OS === 'ios' ? 'padding' : undefined}
    keyboardVerticalOffset={Platform.OS === 'ios' ? 90 : 0}
  >

CHATCOMPOSESCREEN — wrapped composer body (PATTERNS lines 744-766, src lines 119-142, UNCHANGED):

  <View style={[styles.inputRow, { backgroundColor: colors.surface, borderTopColor: colors.border }]}>
    <TextInput
      ...same multiline composer pattern as ChatThreadScreen...
      multiline                      {/* L10 risk — verify in Plan 06 row 8 */}
      maxLength={2000}
      editable={!sending}
    />
    <TouchableOpacity ...send button.../>
  </View>
  </KeyboardAvoidingView>

PATTERNS Pattern 2 — KAV import + props (apply to BOTH files):

  import { KeyboardAvoidingView } from 'react-native-keyboard-controller';

  <KeyboardAvoidingView style={styles.keyboardView}>
    {/* composer body — UNCHANGED */}
  </KeyboardAvoidingView>

REMOVED PROPS (per RESEARCH §3.5 step 3-4 + PATTERNS line 770-787):
  - behavior={Platform.OS === 'ios' ? 'padding' : undefined}    ← REMOVE entirely
  - keyboardVerticalOffset={Platform.OS === 'ios' ? 90 : 0}      ← REMOVE entirely

The library's default behavior handles offset + behavior cross-platform via KeyboardProvider's
frame events. Re-introducing keyboardVerticalOffset to fix any visual issue is EXPRESSLY out
of scope (KBD-02 + §6 SC3.a). If physical-device test in Plan 06 shows a 1-2px gap, the fix
is `paddingBottom` on `styles.inputRow` — NOT a re-introduced offset prop.

PLATFORM IMPORT GUARD (PATTERNS line 707, RESEARCH §3.5 step 1):

Before removing `Platform` from the RN import, the executor MUST grep for OTHER uses of
`Platform.` in each file. Both ChatThread and ChatCompose likely have additional Platform.OS
references (common in RN screens for keyboard/UI/version logic). If `Platform.OS` appears
ANYWHERE OTHER than the two prop expressions being removed, KEEP `Platform` in the import.
The ONLY guaranteed removal is `KeyboardAvoidingView` from the RN import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [Wave 2 entry gate] Verify Plan 02 (KeyboardProvider wiring) landed</name>
  <read_first>
    - .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md (must exist; must say "Wave 2 is unblocked")
    - App.tsx (verify KeyboardProvider import + JSX wrapper present)
  </read_first>
  <files></files>
  <action>
    Same gate as Plan 03 Task 1 / Plan 04 Task 1 — Wave 2 plans share the same precondition.
    No file edits.

      [ -f .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md ]
      grep -c "Wave 2 is unblocked" .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md  # MUST return ≥ 1
      grep -c "import { KeyboardProvider }" App.tsx                                                   # MUST return 1
      grep -c "<KeyboardProvider>" App.tsx                                                            # MUST return 1
      grep -B1 "<KeyboardProvider>" App.tsx | grep -c "<SafeAreaProvider>"                            # MUST return 1
      grep -A1 "<KeyboardProvider>" App.tsx | grep -c "<ThemeProvider>"                               # MUST return 1

    If any check fails, ABORT and surface to user. No commit in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md && grep -c "Wave 2 is unblocked" .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md && grep -c "<KeyboardProvider>" App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md` exits 0
    - `grep -c "Wave 2 is unblocked" .planning/phases/02-universal-keyboard-handling/02-02-SUMMARY.md` returns ≥ 1
    - `grep -c "import { KeyboardProvider }" App.tsx` returns 1
    - `grep -c "<KeyboardProvider>" App.tsx` returns 1
    - `grep -B1 "<KeyboardProvider>" App.tsx | grep -c "<SafeAreaProvider>"` returns 1
    - `grep -A1 "<KeyboardProvider>" App.tsx | grep -c "<ThemeProvider>"` returns 1
    - No git commit in this task
  </acceptance_criteria>
  <done>Wave 2 entry gate verified — Plan cleared to proceed to Tasks 2-3 (one per chat screen).</done>
</task>

<task type="auto">
  <name>Task 2: Drop-in swap ChatThreadScreen RN KeyboardAvoidingView → library KeyboardAvoidingView (KBD-01, KBD-02, D-09)</name>
  <read_first>
    - src/screens/ChatThreadScreen.tsx (verify imports at lines 1-13, KAV opening at lines 183-187 still match the verbatim BEFORE state in <interfaces>; abort if drift)
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md §ChatThreadScreen (lines 638-708) for the verbatim BEFORE excerpt
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md Pattern 2 (KAV import + props)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §3.5 (chat mechanics — steps 1-4)
    - .planning/phases/02-universal-keyboard-handling/02-CONTEXT.md D-09 (drop-in replacement; keyboardVerticalOffset removed)
    - .planning/phases/02-universal-keyboard-handling/02-CONTEXT.md D-10 (KeyboardStickyView NOT introduced)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §8 L10 (multiline composer + Fabric — verify in Plan 06)
  </read_first>
  <files>src/screens/ChatThreadScreen.tsx</files>
  <action>
    Pre-check (CRITICAL — verifies BEFORE state + Platform.OS usage guard):

      grep -c "from 'react-native-keyboard-controller'" src/screens/ChatThreadScreen.tsx  # MUST return 0
      grep -c "keyboardVerticalOffset" src/screens/ChatThreadScreen.tsx                   # MUST return 1 (current anti-pattern present)
      grep -c "KeyboardAvoidingView" src/screens/ChatThreadScreen.tsx                     # MUST return ≥ 3 (import + opening + closing tags)

      # PLATFORM IMPORT GUARD — count Platform usage outside the two props being removed:
      grep -nE "Platform\." src/screens/ChatThreadScreen.tsx
      # The current expected matches are at line ~185 (behavior prop) and ~186 (offset prop).
      # If Platform appears at OTHER lines (e.g., styles.android, Platform.select(), etc.),
      # KEEP `Platform` in the RN import after this edit. Document the additional uses found
      # in 02-05-SUMMARY.md so Plan 06 verifier knows whether Platform stays or goes.

    If KASV/KAV is already imported from the library, STOP — task already ran.

    Edit A — Remove `KeyboardAvoidingView,` from the react-native named-imports block
    (currently around line 11). Find this line:

        KeyboardAvoidingView,

    DELETE entirely (preserving 2-space indent of the other entries).

    Edit B — Conditional removal of `Platform,` from the react-native named-imports block:

      a. If pre-check showed `Platform.` references ONLY at the lines being removed (the
         `behavior=...` prop and the `keyboardVerticalOffset=...` prop), then ALSO delete
         the `Platform,` import line — `Platform` becomes unused.

      b. If pre-check showed `Platform.` references ELSEWHERE in the file (any line OTHER
         than ~185 / ~186), KEEP `Platform,` in the RN import. Do NOT remove it.

      Document the choice in the commit body and in 02-05-SUMMARY.md so the Plan 06 verifier
      can sanity-check.

    Edit C — Add KAV import in the third-party RN modules group. ChatThreadScreen does NOT
    currently import from `react-native-safe-area-context`, so the new import goes after the
    LAST RN-core line (after `} from 'react-native';`). Per CONVENTIONS.md import-organization
    rule, third-party RN modules are group 2:

      import { KeyboardAvoidingView } from 'react-native-keyboard-controller';

    The new import block (assuming Platform stays — choose b above) should look like:

      import {
        View,
        Text,
        StyleSheet,
        FlatList,
        TextInput,
        TouchableOpacity,
        ActivityIndicator,
        Alert,
        Platform,
      } from 'react-native';
      import { KeyboardAvoidingView } from 'react-native-keyboard-controller';

    OR (if Platform goes — choose a above):

      import {
        View,
        Text,
        StyleSheet,
        FlatList,
        TextInput,
        TouchableOpacity,
        ActivityIndicator,
        Alert,
      } from 'react-native';
      import { KeyboardAvoidingView } from 'react-native-keyboard-controller';

    Edit D — At lines 183-187, replace the 4-line KAV opening tag with the library's 1-line
    drop-in. Per RESEARCH §3.5 step 3-4 the `behavior` and `keyboardVerticalOffset` props
    are REMOVED entirely (library defaults handle them via KeyboardProvider).

    Before (verbatim — must match exactly, including the `behavior` and `keyboardVerticalOffset`
    Platform.OS expressions):

      <KeyboardAvoidingView
        style={styles.keyboardView}
        behavior={Platform.OS === 'ios' ? 'padding' : undefined}
        keyboardVerticalOffset={Platform.OS === 'ios' ? 90 : 0}
      >

    After (verbatim — exactly one line; only `style` prop preserved):

      <KeyboardAvoidingView style={styles.keyboardView}>

    The closing `</KeyboardAvoidingView>` tag (around line 219) is UNCHANGED — it still says
    `</KeyboardAvoidingView>`, which now resolves to the library's KAV via the new import.

    Do NOT touch:
    - The FlatList for messages
    - The composer View / TextInput / TouchableOpacity send button
    - multiline / maxLength / editable props on the TextInput (multiline is the L10 risk —
      verified in Plan 06 row 7, NOT here)
    - styles, theme tokens, i18n strings
    - Any other code in the file (callbacks, state, effects)
    - Phase 1's BackHandler effect at App.tsx — Plan 06 verifies no conflict on device

    Verify file consistency after all edits:

      grep -c "from 'react-native-keyboard-controller'" src/screens/ChatThreadScreen.tsx  # MUST return 1
      grep -c "<KeyboardAvoidingView" src/screens/ChatThreadScreen.tsx                    # MUST return 1 (only the opening tag)
      grep -c "</KeyboardAvoidingView>" src/screens/ChatThreadScreen.tsx                  # MUST return 1
      grep -c "keyboardVerticalOffset" src/screens/ChatThreadScreen.tsx                   # MUST return 0 (anti-pattern removed)
      grep -c "behavior=" src/screens/ChatThreadScreen.tsx                                # MUST return 0 (behavior prop removed)
      grep -c "multiline" src/screens/ChatThreadScreen.tsx                                # MUST return ≥ 1 (composer multiline preserved)

    Run TypeScript check:

      npx tsc --noEmit

    Commit atomically:

      git add src/screens/ChatThreadScreen.tsx
      git commit -m "feat(02): swap ChatThreadScreen RN KeyboardAvoidingView → library KAV; remove magic offset (KBD-01, KBD-02, D-09)"

    Commit body: notes the drop-in per CONTEXT D-09 + RESEARCH §3.5; lists the 3 props removed
    (KAV from RN import, behavior, keyboardVerticalOffset); states whether Platform was kept
    (b) or removed (a) per the pre-grep finding; notes multiline composer preserved for L10
    verify in Plan 06; notes KeyboardStickyView NOT introduced per D-10.
  </action>
  <verify>
    <automated>grep -c "from 'react-native-keyboard-controller'" src/screens/ChatThreadScreen.tsx && grep -c "keyboardVerticalOffset" src/screens/ChatThreadScreen.tsx && grep -c "behavior=" src/screens/ChatThreadScreen.tsx && grep -c "<KeyboardAvoidingView" src/screens/ChatThreadScreen.tsx && grep -c "</KeyboardAvoidingView>" src/screens/ChatThreadScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from 'react-native-keyboard-controller'" src/screens/ChatThreadScreen.tsx` returns exactly 1
    - `grep -c "import { KeyboardAvoidingView }" src/screens/ChatThreadScreen.tsx` returns exactly 1
    - `grep -c "<KeyboardAvoidingView" src/screens/ChatThreadScreen.tsx` returns exactly 1 (single opening tag — now one line)
    - `grep -c "</KeyboardAvoidingView>" src/screens/ChatThreadScreen.tsx` returns exactly 1
    - `grep -c "keyboardVerticalOffset" src/screens/ChatThreadScreen.tsx` returns 0 (canonical anti-pattern eliminated — KBD-02 + §6 SC3.a)
    - `grep -c "behavior=" src/screens/ChatThreadScreen.tsx` returns 0 (RN-style behavior prop removed — library defaults handle it)
    - `grep -nE "(^|[^a-zA-Z])KeyboardAvoidingView([^a-zA-Z]|$)" src/screens/ChatThreadScreen.tsx | grep -v "react-native-keyboard-controller"` returns 2 lines (opening + closing tags) — none of them in the RN import block
    - `grep -nE "from 'react-native'" src/screens/ChatThreadScreen.tsx` shows the RN import block does NOT contain `KeyboardAvoidingView` after this edit
    - `grep -c "multiline" src/screens/ChatThreadScreen.tsx` returns ≥ 1 (composer multiline preserved — L10 risk preserved verbatim for Plan 06 verify)
    - `grep -c "FlatList" src/screens/ChatThreadScreen.tsx` returns ≥ 2 (import + JSX usage — message list unchanged)
    - `npx tsc --noEmit` exits 0 (or no NEW errors vs pre-task baseline; if removing Platform import causes an unused-import warning, the task chose option a correctly)
    - `git diff HEAD~1 --name-only` returns ONLY `src/screens/ChatThreadScreen.tsx`
    - `git log --oneline -1` shows a commit message containing "ChatThread", "KeyboardAvoidingView", "KBD-01", "KBD-02", "D-09"
  </acceptance_criteria>
  <done>ChatThreadScreen uses library KAV; magic-number keyboardVerticalOffset removed; behavior prop removed; FlatList + composer + multiline TextInput preserved verbatim. Atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Drop-in swap ChatComposeScreen RN KeyboardAvoidingView → library KeyboardAvoidingView (KBD-01, KBD-02, D-09)</name>
  <read_first>
    - src/screens/ChatComposeScreen.tsx (verify imports at lines 1-13, KAV opening at lines 114-118 still match the verbatim BEFORE state in <interfaces>; abort if drift)
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md §ChatComposeScreen (lines 710-768) for the verbatim BEFORE excerpt
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md Pattern 2 (apply same shape as Task 2)
    - .planning/phases/02-universal-keyboard-handling/02-CONTEXT.md D-09 (drop-in)
  </read_first>
  <files>src/screens/ChatComposeScreen.tsx</files>
  <action>
    Pre-check (same Platform guard as Task 2):

      grep -c "from 'react-native-keyboard-controller'" src/screens/ChatComposeScreen.tsx  # MUST return 0
      grep -c "keyboardVerticalOffset" src/screens/ChatComposeScreen.tsx                   # MUST return 1
      grep -c "KeyboardAvoidingView" src/screens/ChatComposeScreen.tsx                     # MUST return ≥ 3

      # PLATFORM IMPORT GUARD — same logic as Task 2:
      grep -nE "Platform\." src/screens/ChatComposeScreen.tsx
      # If Platform appears ONLY at lines ~116 (behavior prop) and ~117 (offset prop), Platform
      # becomes unused after the edit and CAN be removed from the RN import.
      # If Platform appears elsewhere, KEEP it in the RN import.
      # Document choice in commit body + 02-05-SUMMARY.md.

    If library KAV is already imported, STOP.

    Edit A — Remove `KeyboardAvoidingView,` from the react-native named-imports block
    (around line 11):

        KeyboardAvoidingView,

    DELETE entirely.

    Edit B — Conditional removal of `Platform,` (same logic as Task 2):
      a. Platform unused elsewhere → remove `Platform,` line.
      b. Platform used elsewhere → KEEP `Platform,` in the RN import.

    Edit C — Add KAV import in the third-party RN modules group. ChatComposeScreen does NOT
    import from react-native-safe-area-context either, so the new import goes after the
    `} from 'react-native';` line:

      import { KeyboardAvoidingView } from 'react-native-keyboard-controller';

    New import block (Platform-stays variant — choose b):

      import {
        View,
        Text,
        StyleSheet,
        TextInput,
        TouchableOpacity,
        ActivityIndicator,
        Alert,
        Platform,
        Image,
      } from 'react-native';
      import { KeyboardAvoidingView } from 'react-native-keyboard-controller';

    OR (Platform-goes variant — choose a):

      import {
        View,
        Text,
        StyleSheet,
        TextInput,
        TouchableOpacity,
        ActivityIndicator,
        Alert,
        Image,
      } from 'react-native';
      import { KeyboardAvoidingView } from 'react-native-keyboard-controller';

    Note `Image` stays in the RN import either way (used internally by ChatComposeScreen).

    Edit D — At lines 114-118, replace the 4-line KAV opening tag with the library's 1-line
    drop-in.

    Before (verbatim):

      <KeyboardAvoidingView
        style={styles.keyboardView}
        behavior={Platform.OS === 'ios' ? 'padding' : undefined}
        keyboardVerticalOffset={Platform.OS === 'ios' ? 90 : 0}
      >

    After (verbatim — exactly one line):

      <KeyboardAvoidingView style={styles.keyboardView}>

    The closing `</KeyboardAvoidingView>` tag (around line 142) is UNCHANGED.

    Do NOT touch:
    - The View / TextInput / TouchableOpacity composer body
    - multiline / maxLength / editable props on the TextInput (L10 verify in Plan 06 row 8)
    - styles, theme tokens, i18n strings
    - Image import (used internally)

    Verify file consistency:

      grep -c "from 'react-native-keyboard-controller'" src/screens/ChatComposeScreen.tsx  # MUST return 1
      grep -c "<KeyboardAvoidingView" src/screens/ChatComposeScreen.tsx                    # MUST return 1
      grep -c "</KeyboardAvoidingView>" src/screens/ChatComposeScreen.tsx                  # MUST return 1
      grep -c "keyboardVerticalOffset" src/screens/ChatComposeScreen.tsx                   # MUST return 0
      grep -c "behavior=" src/screens/ChatComposeScreen.tsx                                # MUST return 0
      grep -c "multiline" src/screens/ChatComposeScreen.tsx                                # MUST return ≥ 1

    Run TypeScript check:

      npx tsc --noEmit

    Commit atomically:

      git add src/screens/ChatComposeScreen.tsx
      git commit -m "feat(02): swap ChatComposeScreen RN KeyboardAvoidingView → library KAV; remove magic offset (KBD-01, KBD-02, D-09)"

    Commit body: notes the drop-in per CONTEXT D-09 + RESEARCH §3.5; lists the 3 props removed;
    states whether Platform was kept or removed; notes multiline composer preserved for L10
    verify in Plan 06; notes KeyboardStickyView NOT introduced per D-10.

    After this commit lands, the phase exit grep gate is satisfied for the chat surface:

      grep -rn "keyboardVerticalOffset" src/screens/Chat*Screen.tsx | wc -l   # MUST return 0
  </action>
  <verify>
    <automated>grep -c "from 'react-native-keyboard-controller'" src/screens/ChatComposeScreen.tsx && grep -c "keyboardVerticalOffset" src/screens/ChatComposeScreen.tsx && grep -c "behavior=" src/screens/ChatComposeScreen.tsx && grep -rn "keyboardVerticalOffset" src/screens/ChatThreadScreen.tsx src/screens/ChatComposeScreen.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from 'react-native-keyboard-controller'" src/screens/ChatComposeScreen.tsx` returns exactly 1
    - `grep -c "import { KeyboardAvoidingView }" src/screens/ChatComposeScreen.tsx` returns exactly 1
    - `grep -c "<KeyboardAvoidingView" src/screens/ChatComposeScreen.tsx` returns exactly 1
    - `grep -c "</KeyboardAvoidingView>" src/screens/ChatComposeScreen.tsx` returns exactly 1
    - `grep -c "keyboardVerticalOffset" src/screens/ChatComposeScreen.tsx` returns 0
    - `grep -c "behavior=" src/screens/ChatComposeScreen.tsx` returns 0
    - `grep -c "multiline" src/screens/ChatComposeScreen.tsx` returns ≥ 1 (composer multiline preserved)
    - `grep -c "Image" src/screens/ChatComposeScreen.tsx` returns ≥ 1 (Image import preserved)
    - **Combined plan-exit grep:** `grep -rn "keyboardVerticalOffset" src/screens/ChatThreadScreen.tsx src/screens/ChatComposeScreen.tsx | wc -l` returns 0 (after BOTH Tasks 2 and 3 commit, the chat surface has zero magic-number anti-patterns — KBD-02 + §6 SC3.a satisfied for chat)
    - `npx tsc --noEmit` exits 0 (or no NEW errors)
    - `git diff HEAD~1 --name-only` returns ONLY `src/screens/ChatComposeScreen.tsx`
    - `git log --oneline -1` shows a commit message containing "ChatCompose", "KeyboardAvoidingView", "KBD-01", "KBD-02", "D-09"
  </acceptance_criteria>
  <done>ChatComposeScreen uses library KAV; magic-number keyboardVerticalOffset removed; behavior prop removed; composer + multiline TextInput preserved verbatim. Atomic commit landed. Combined chat-surface grep gate clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan replaces existing built-in `KeyboardAvoidingView`
elements with the library equivalent on two existing chat screens. No new auth surface, no new
HTTP, no new storage, no PII handling. The replacement component is from a trusted package
supply-chain-vetted in Plan 01.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | T (Tampering) | Magic-number keyboardVerticalOffset re-introduced post-merge to "fix" a visual issue | mitigate | Tasks 2+3 each include `grep -c "keyboardVerticalOffset" returns 0` acceptance criterion. Phase exit gate (Plan 06) re-greps `src/` recursively. PATTERNS Pattern 2 + RESEARCH §3.5 explicitly forbid re-introducing the offset prop — fix any 1-2px gap via `paddingBottom` on `styles.inputRow` instead. |
| T-02-05-02 | T (Tampering) | Platform import removal breaks unrelated Platform.OS usage in chat screens | mitigate | Tasks 2+3 each include a pre-grep guard checking for OTHER Platform.* references in the file before deciding to remove the Platform import. Document the choice in commit body + 02-05-SUMMARY.md. TypeScript compile is the secondary safety net. |
| T-02-05-03 | I (Information Disclosure) | Library KAV wrapping chat composer with user-typed message content | accept | KAV is a presentation-layer offset container; it does not access TextInput value, multiline state, or any other prop content. The same user content flowed through RN's built-in KAV before this plan; trust posture is unchanged. |
| T-02-05-04 | T (Tampering) | KeyboardStickyView accidentally introduced (out of scope per D-10) | mitigate | Tasks 2+3 explicitly forbid KeyboardStickyView introduction; acceptance criteria check `grep -c "KeyboardAvoidingView"` returns ≥ 2 per file (proving KAV is the wrapper, not StickyView). Commit body and 02-05-SUMMARY.md re-state that KeyboardStickyView is deferred to M2 per D-10. |

No S/R/D/E applicable. Block on `high` severity per project default; zero high-severity threats raised.
Phase 2 is presentational wiring — `<security_threat_model>` from the downstream consumer brief
noted "no new threat surface" with the magic-offset re-introduction risk being the only meaningful
mechanical safeguard.
</threat_model>

<verification>
- `grep -c "from 'react-native-keyboard-controller'" src/screens/ChatThreadScreen.tsx src/screens/ChatComposeScreen.tsx` totals 2 (one per file)
- `grep -c "<KeyboardAvoidingView" src/screens/ChatThreadScreen.tsx src/screens/ChatComposeScreen.tsx` totals 2 (one opening tag per file)
- `grep -c "</KeyboardAvoidingView>" src/screens/ChatThreadScreen.tsx src/screens/ChatComposeScreen.tsx` totals 2
- `grep -rn "keyboardVerticalOffset" src/screens/ChatThreadScreen.tsx src/screens/ChatComposeScreen.tsx` returns 0 lines (KBD-02 + §6 SC3.a — chat surface)
- `grep -rn "from 'react-native'" src/screens/ChatThreadScreen.tsx src/screens/ChatComposeScreen.tsx | head` shows neither file imports KeyboardAvoidingView from 'react-native' anymore
- `git diff HEAD~2 -- src/locales/en.json src/locales/ru.json` returns empty (no copy changes — Phase 2 introduces zero new strings)
- `npx tsc --noEmit` exits 0 after both commits
- 2 atomic commits in `git log --oneline | head -2` (one per file)
- 02-VALIDATION.md row "Wave 2 chat" PASSES
</verification>

<success_criteria>
- KBD-01 partial ✓ (2 of 8 Tier 1 screens — ChatThread + ChatCompose) — combined with Plan 03
  (4 auth) and Plan 04 (CreateListing both branches + AccountSettings) the full Tier 1+2 set
  is wired
- KBD-02 ✓ — chat surface is 100% magic-number-free; combined with the auth/settings/listing
  surface being clean too (Plans 03/04 do not introduce keyboardVerticalOffset), the entire
  src/ tree is now clean of the canonical anti-pattern PITFALLS §Pitfall 2 calls out
- Both chat screens import KeyboardAvoidingView from `react-native-keyboard-controller`
- Both chat screens have RN's KeyboardAvoidingView removed from imports
- Both chat screens have `behavior` and `keyboardVerticalOffset` props removed entirely
- multiline composer TextInput on both screens preserved verbatim (L10 verify in Plan 06)
- Platform import retention decision documented per file
- KeyboardStickyView NOT introduced (D-10 deferred)
- 2 atomic commits — each per-file change can be bisected independently if Plan 06 device
  verification surfaces a per-screen regression
- Plan 06 (Wave 3 verification matrix) Tier 1 rows 7-8 ready to test on device
</success_criteria>

<output>
After completion, create `.planning/phases/02-universal-keyboard-handling/02-05-SUMMARY.md` documenting:
- 2 commit SHAs (one per chat screen)
- Per-file Platform-import retention decision (kept / removed) with the grep evidence that drove the choice
- Confirmation: keyboardVerticalOffset zero-occurrence on both chat screens (mechanical grep)
- Confirmation: behavior prop zero-occurrence on both chat screens
- Confirmation: en.json + ru.json zero-diff (verified by `git diff HEAD~2 -- src/locales/`)
- Verbatim diff snippet for ChatThreadScreen showing the BEFORE (4-line KAV with 2 magic props) / AFTER (1-line library KAV)
- Note: ChatComposeScreen is structurally identical; same diff shape
- Note: multiline TextInput on BOTH screens preserved for L10 verify in Plan 06 D-12 Tier 1 rows 7-8 — paste a multi-line message during device test, confirm composer grows
- Note: Phase 1's BackHandler at App.tsx:184-297 confirmed no conflict during code review (RESEARCH §3.5 Android back button section); device verify in Plan 06 confirms on-device
- Explicit statement: "Wave 2 (chat slice) complete — Plan 06 Tier 1 rows 7-8 ready for device matrix walk. Combined with Plans 03 + 04, ALL of Wave 2 is now landed and Plan 06 may begin."
</output>
</content>
</invoke>