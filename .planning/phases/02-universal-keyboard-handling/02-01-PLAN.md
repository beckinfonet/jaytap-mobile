---
phase: 02-universal-keyboard-handling
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - babel.config.js
  - ios/Podfile.lock
autonomous: false
requirements: [KBD-03, KBD-04]
requirements_addressed: [KBD-03, KBD-04]
threat_model_complete: true
tags: [react-native, keyboard, install-precondition, reanimated, worklets, fabric, new-architecture]

must_haves:
  truths:
    - "react-native-reanimated@^4.x is present in package.json dependencies"
    - "react-native-worklets@^0.8.x is present in package.json dependencies (separate peer of reanimated v4 — load-bearing correction per RESEARCH §2)"
    - "react-native-keyboard-controller@^1.21.6 is present in package.json dependencies"
    - "babel.config.js has a plugins array whose LAST entry is 'react-native-worklets/plugin' (NOT 'react-native-reanimated/plugin' — that's the legacy v3 name)"
    - "iOS pod install regenerated Podfile.lock with RNReanimated, RNWorklets, and RNKeyboardController (or equivalent pod names)"
    - "Android ./gradlew clean exits 0; subsequent android run-android build succeeds on Moto G XT2513V"
    - "iOS run-ios build succeeds on iPhone 15 Pro Max"
    - "App launches on both physical devices with no 'Reanimated is not configured properly' Metro error and no red box"
    - "Rollback plan documented in 02-01-SUMMARY.md so a future failure can revert to the pre-Wave-0 commit cleanly"
  artifacts:
    - path: "package.json"
      provides: "Three new dependency entries: react-native-reanimated, react-native-worklets, react-native-keyboard-controller"
      contains: "react-native-keyboard-controller"
    - path: "babel.config.js"
      provides: "plugins array with 'react-native-worklets/plugin' as the LAST entry"
      contains: "react-native-worklets/plugin"
    - path: "ios/Podfile.lock"
      provides: "Regenerated lockfile pulling new native modules via use_native_modules! autolink"
      contains: "RNKeyboardController"
  key_links:
    - from: "babel.config.js"
      to: "node_modules/react-native-worklets/plugin"
      via: "Babel plugins array entry — MUST be last so future plugins do not break worklet transformation"
      pattern: "react-native-worklets/plugin"
    - from: "package.json"
      to: "ios/Podfile.lock"
      via: "use_native_modules! autolink in ios/Podfile (no manual pod entries — autolink reads node_modules)"
      pattern: "react-native-keyboard-controller"
---

<objective>
Install the keyboard-controller library plus its load-bearing peer dependencies (reanimated v4 + worklets v0.8) in the load-bearing order specified by RESEARCH §2 and CONTEXT D-02. This wave is the BLOCKING gate for the entire phase: if any verification step fails, Wave 1 (KeyboardProvider wiring) MUST NOT begin until Wave 0's go/no-go criteria all pass per RESEARCH §2.5.

Purpose: KBD-03 (reanimated peer dep installed) + KBD-04 (Fabric / New Architecture verified on physical-device builds). Per RESEARCH §2, the v4 plugin name `'react-native-worklets/plugin'` is load-bearing — using the legacy v3 name `'react-native-reanimated/plugin'` will silently no-op, leaving every downstream `KeyboardProvider`/`KeyboardAwareScrollView` non-functional with no compile error.

Output: Three new package.json dependencies, one new babel.config.js plugins array, regenerated ios/Podfile.lock, two physical-device build verifications, and a documented rollback plan in 02-01-SUMMARY.md.

Critical: This plan is `autonomous: false`. Wave 0 touches lockfiles + native build state on the user's machine — needs human eyes on `pod install` output and `./gradlew clean` results before signing off the Wave 1 entry gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-universal-keyboard-handling/02-CONTEXT.md
@.planning/phases/02-universal-keyboard-handling/02-RESEARCH.md
@.planning/phases/02-universal-keyboard-handling/02-PATTERNS.md
@.planning/phases/02-universal-keyboard-handling/02-VALIDATION.md
@CLAUDE.md
@package.json
@babel.config.js
@ios/Podfile

<interfaces>
<!-- Verbatim BEFORE state of the three files this plan touches, copied from PATTERNS.md -->
<!-- Do not re-read PATTERNS.md to look these up — they are reproduced here for the executor. -->

VERBATIM CURRENT package.json dependencies block (PATTERNS §package.json, lines 47-66):

  "dependencies": {
    "@react-native-async-storage/async-storage": "^2.2.0",
    "@react-native-community/datetimepicker": "^8.6.0",
    "@react-native/new-app-screen": "0.84.0",
    "axios": "^1.13.5",
    "lucide-react-native": "^0.564.0",
    "react": "19.2.3",
    "react-native": "0.84.0",
    "react-native-image-picker": "^8.2.1",
    "react-native-linear-gradient": "^2.8.3",
    "react-native-maps": "1.27.1",
    "react-native-safe-area-context": "^5.5.2",
    "react-native-svg": "^15.15.3",
    "react-native-webview": "^13.16.0",
    "socket.io-client": "^4.8.3"
  }

Confirmed absent: react-native-reanimated, react-native-worklets, react-native-keyboard-controller (CONTEXT D-02 verified `grep -c "react-native-reanimated" package.json` = 0).

VERBATIM CURRENT babel.config.js (PATTERNS §babel.config.js — full file):

  module.exports = {
    presets: ['module:@react-native/babel-preset'],
  };

VERBATIM TARGET babel.config.js (RESEARCH §2.3 + PATTERNS Pattern 5):

  module.exports = {
    presets: ['module:@react-native/babel-preset'],
    plugins: [
      'react-native-worklets/plugin', // MUST be last; reanimated v4 ships its plugin here
    ],
  };

The `// MUST be last` comment is documentation-as-load-bearing. Preserve it verbatim.

VERBATIM CURRENT ios/Podfile (PATTERNS §ios/Podfile, lines 117-153) — DO NOT EDIT.
The file uses `use_native_modules!` (line 137) which auto-discovers native modules from
node_modules. Phase 2's iOS work is exclusively `cd ios && pod install`. If anyone proposes
adding `pod 'react-native-keyboard-controller'` to Podfile, REJECT — that is the legacy
pre-autolinking pattern and will conflict with use_native_modules!.

Verified pin sources (RESEARCH §2.1 table):

| Package | Pin | Source |
|---------|-----|--------|
| react-native-reanimated | ^4.3.0 | npm view 2026-04-22 — peer accepts RN 0.81-0.85 (JayTap is 0.84 — within range) |
| react-native-worklets | ^0.8.0 | Required peer of reanimated v4; hosts the Babel plugin |
| react-native-keyboard-controller | ^1.21.6 | CONTEXT D-01 locked; peer is reanimated >=3.0.0 so v4 satisfies it |

§2.5 Wave 0 entry-gate checklist (the executor's exit criteria for this plan):

  - [ ] grep -c "react-native-reanimated" package.json returns 1
  - [ ] grep -c "react-native-worklets" package.json returns 1
  - [ ] grep -c "react-native-keyboard-controller" package.json returns 1
  - [ ] tail -n 5 babel.config.js | grep "react-native-worklets/plugin" returns 1 (and it is the last plugins-array entry)
  - [ ] cd ios && pod install && cd .. exits 0; Podfile.lock shows RNReanimated, RNWorklets, RNKeyboardController
  - [ ] npm run android builds and launches on Moto G XT2513V; no red box, no "Reanimated is not configured properly" Metro log
  - [ ] npm run ios builds and launches on iPhone 15 Pro Max; same condition
  - [ ] App opens to home screen unchanged (Wave 0 added zero render-tree changes)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-install snapshot + install reanimated v4 + worklets (load-bearing peer pair)</name>
  <read_first>
    - package.json (verify dependencies block matches the verbatim BEFORE state above; abort if drift)
    - babel.config.js (verify it is still the 3-line file above; abort if drift)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §2.2 steps 1-2 (rollback baseline + install command)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §2.4 landmines L1-L3 (Fabric-only; v4 plugin name renamed; worklets is a SEPARATE peer)
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md §package.json (alphabetized insertion expectation)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Pre-check: confirm clean working tree before starting (rollback baseline). Run:

      git status                                  # MUST report clean working tree; abort if not
      git rev-parse HEAD                          # record commit SHA — write to 02-01-SUMMARY.md as rollback target
      cp package.json /tmp/package.json.bak       # belt + suspenders local backup
      cp ios/Podfile.lock /tmp/Podfile.lock.bak   # iOS rollback artifact

    Install reanimated + worklets together in ONE npm command (per RESEARCH §2.2 step 2 — they are a peer pair and must resolve together):

      npm install react-native-reanimated react-native-worklets

    Why one command: npm peer-dep resolver picks compatible versions (reanimated 4.3.x peer requires worklets 0.8.x). Splitting into two `npm install` calls can resolve incompatible majors.

    DO NOT run `npm install react-native-keyboard-controller` in this task — it goes in Task 3 AFTER reanimated is fully wired (babel + pod install + clean android build). Per RESEARCH §2.2 step 6 the order is load-bearing.

    Verify the install landed correctly:

      npm ls react-native-reanimated react-native-worklets

    Expected output: `react-native-reanimated@4.3.x` and `react-native-worklets@0.8.x` (exact patch versions depend on dist-tag at install time). If versions resolve outside the 4.3.x / 0.8.x lines, STOP and surface to user — A1/A7 in RESEARCH §9 covers this risk.

    Verify package.json was modified:

      grep -c "react-native-reanimated" package.json   # MUST return 1
      grep -c "react-native-worklets" package.json     # MUST return 1

    Per PATTERNS §package.json, npm preserves alphabetical order; expected slots:
    - react-native-reanimated between safe-area-context and svg
    - react-native-worklets between webview and socket.io-client (NOTE: socket.io-client starts with "s" so worklets goes BETWEEN webview and socket.io-client only if alphabetized strictly — actually `socket.io-client` < `react-native-worklets` is FALSE; the correct slot is AFTER react-native-webview, since `react-native-w...` < `socket.io-client`). The exact slot is npm's call; just verify both names appear in the dependencies object after install.

    Do NOT commit yet — Task 2 edits babel.config.js as part of the same logical install step. Single commit at end of Task 2.
  </action>
  <verify>
    <automated>grep -c "react-native-reanimated" package.json && grep -c "react-native-worklets" package.json && npm ls react-native-reanimated react-native-worklets 2>&1 | grep -E "react-native-(reanimated@|worklets@)"</automated>
  </verify>
  <acceptance_criteria>
    - `git status` BEFORE this task starts showed a clean working tree (recorded in 02-01-SUMMARY.md)
    - `git rev-parse HEAD` BEFORE this task ran is recorded in 02-01-SUMMARY.md as the Wave 0 rollback target
    - `grep -c "react-native-reanimated" package.json` returns exactly 1
    - `grep -c "react-native-worklets" package.json` returns exactly 1
    - `npm ls react-native-reanimated` shows version `4.x.x` (target 4.3.x; if resolution picks a different 4.x patch that's acceptable as long as worklets peer is 0.8.x)
    - `npm ls react-native-worklets` shows version `0.8.x`
    - `npm ls` reports zero peer-dependency warnings for these two packages
    - `package-lock.json` was updated by the install (visible in `git diff --name-only`)
    - NO commit yet — Task 2 will commit the reanimated install + babel plugin together
  </acceptance_criteria>
  <done>reanimated@4.x and worklets@0.8.x are installed and visible in `npm ls`. package.json + package-lock.json modified but not yet committed.</done>
</task>

<task type="auto">
  <name>Task 2: Add 'react-native-worklets/plugin' as the LAST entry in babel.config.js + commit reanimated install</name>
  <read_first>
    - babel.config.js (current 3-line state per the BEFORE block in <interfaces>)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §2.3 (exact target file shape — copy verbatim)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §2.4 L2 (silent runtime failure if plugin name is wrong)
    - .planning/phases/02-universal-keyboard-handling/02-PATTERNS.md Pattern 5 (load-bearing trailing comment)
  </read_first>
  <files>babel.config.js</files>
  <action>
    Replace the entire contents of babel.config.js with this verbatim target (RESEARCH §2.3):

      module.exports = {
        presets: ['module:@react-native/babel-preset'],
        plugins: [
          'react-native-worklets/plugin', // MUST be last; reanimated v4 ships its plugin here
        ],
      };

    LOAD-BEARING SPECIFICS:

    1. The plugin name is `'react-native-worklets/plugin'` — NOT `'react-native-reanimated/plugin'`.
       The v3 name (`'react-native-reanimated/plugin'`) still passes `npm install` and Babel
       parsing without errors but produces a non-worklet bundle. Animations and the entire
       keyboard-controller library would then silently no-op at runtime. Per RESEARCH §2.4 L2
       and L4, this is THE highest-risk encoding error in Wave 0.

    2. The trailing `// MUST be last` comment is intentional documentation. Preserve verbatim.
       If a future Babel addition is needed, it goes ABOVE this line.

    3. No other plugins exist today — the plugins array has exactly 1 entry after this edit.

    After saving, clear Metro cache (Metro caches the babel.config.js and may serve stale
    bundles otherwise — RESEARCH §2.3):

      npx react-native start --reset-cache &
      sleep 2 && kill %1   # OR equivalent: just delete the cache dir
      # Alternative cache reset (works without starting Metro):
      rm -rf $TMPDIR/metro-* 2>/dev/null
      rm -rf $TMPDIR/haste-map-* 2>/dev/null

    Commit Task 1 + Task 2 together with one atomic message:

      git add package.json package-lock.json babel.config.js
      git commit -m "chore(02): install reanimated v4 + worklets, register Babel plugin LAST (KBD-03, D-02)"

    Commit body should note: (1) reanimated 4.x is Fabric-only and JayTap is on Fabric per
    `newArchEnabled=true`; (2) worklets is a separate peer of reanimated v4; (3) the plugin
    name is the v4 name (`react-native-worklets/plugin`), NOT the v3 name; (4) Metro cache
    cleared so the next build picks up the new transform.
  </action>
  <verify>
    <automated>tail -n 5 babel.config.js | grep -c "react-native-worklets/plugin" && grep -c "react-native-reanimated/plugin" babel.config.js && git log --oneline -1 babel.config.js | grep -c "02"</automated>
  </verify>
  <acceptance_criteria>
    - `tail -n 5 babel.config.js | grep -c "react-native-worklets/plugin"` returns exactly 1
    - `grep -c "react-native-reanimated/plugin" babel.config.js` returns exactly 0 (the legacy v3 name MUST NOT appear)
    - `grep -c "// MUST be last" babel.config.js` returns exactly 1 (load-bearing comment preserved)
    - `wc -l babel.config.js` returns 6 lines (per the verbatim target above)
    - The plugins array is present and `'react-native-worklets/plugin'` is its only / last entry
    - `git log --oneline -1` shows a single commit whose message contains "reanimated", "worklets", and "KBD-03"
    - Commit touches exactly 3 files: `package.json`, `package-lock.json`, `babel.config.js` (verify via `git show --name-only HEAD`)
    - Metro cache directory(ies) under $TMPDIR have been cleared (or Metro will be cold-started in Task 4 anyway)
  </acceptance_criteria>
  <done>babel.config.js has the v4 worklets plugin as the LAST plugins-array entry. The reanimated install + Babel plugin are committed together as one logical change.</done>
</task>

<task type="auto">
  <name>Task 3: Install react-native-keyboard-controller + first iOS pod install + Android gradle clean</name>
  <read_first>
    - package.json (verify Task 2's commit landed: reanimated + worklets present)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §2.2 steps 4-7 (pod install + gradle clean + keyboard-controller install + re-pod sequence)
    - .planning/phases/02-universal-keyboard-handling/02-RESEARCH.md §2.4 L4 + L6 (bridgeless mode + windowSoftInputMode already correct per D-06)
    - ios/Podfile (verify use_native_modules! is at line ~137; do NOT edit)
  </read_first>
  <files>package.json, package-lock.json, ios/Podfile.lock</files>
  <action>
    Per RESEARCH §2.2 the Wave 0 install is staged: reanimated FIRST (Tasks 1-2 above), THEN
    pod install + gradle clean to materialize reanimated's native module, THEN keyboard-controller,
    THEN a final pod install for keyboard-controller's native module. This task does steps 4-7.

    Step 4 — iOS pod install (materialize reanimated + worklets native modules):

      cd ios && pod install && cd ..

    Watch the output. RESEARCH §2.2 expects to see "Installing RNReanimated" and
    "Installing RNWorklets" (or similar pod names — exact strings vary by lib version) in the
    pod install log. If pod install fails:

    - Diagnostic: `cd ios && rm -rf Pods Podfile.lock && pod install && cd ..` (RESEARCH §2.4 L4)
    - Last resort: delete Xcode DerivedData (`rm -rf ~/Library/Developer/Xcode/DerivedData/JayTap-*`)
    - If still failing: pause execution and consult the user — this is the Xcode 26 / iOS 26
      SDK risk flagged in RESEARCH §9 A4.

    Step 5 — Android gradle clean (clear stale native module registration cache per
    RESEARCH §2.4 L5; required between native module additions):

      cd android && ./gradlew clean && cd ..

    Verify exit code 0. If clean fails, surface to user — most likely a Gradle daemon
    state issue, fixable via `./gradlew --stop && ./gradlew clean`.

    Step 6 — Install react-native-keyboard-controller AFTER reanimated is fully wired:

      npm install react-native-keyboard-controller

    Verify the install:

      npm ls react-native-keyboard-controller   # expect 1.21.x
      grep -c "react-native-keyboard-controller" package.json   # expect 1

    If npm resolves a version below 1.21.6, escalate — CONTEXT D-01 pins to 1.21.x specifically
    (RN 0.83.x/Fabric maintenance branch).

    Step 7 — Re-pod for keyboard-controller's native module:

      cd ios && pod install && cd ..

    Watch for "Installing RNKeyboardController" (or equivalent) in the output.

    Commit atomically (this is one logical "wire keyboard-controller" change):

      git add package.json package-lock.json ios/Podfile.lock
      git commit -m "chore(02): install react-native-keyboard-controller + regenerate Podfile.lock (KBD-03, D-01)"

    Commit body: notes the load-bearing order (reanimated FIRST in commit a-prior, keyboard-controller
    SECOND here) per RESEARCH §2.2; notes the two pod installs run in this task (one for reanimated
    after step 4, one for keyboard-controller after step 7); explicitly notes Podfile itself was NOT
    edited (autolink via use_native_modules!).
  </action>
  <verify>
    <automated>grep -c "react-native-keyboard-controller" package.json && grep -c "RNKeyboardController\|react-native-keyboard-controller" ios/Podfile.lock && grep -c "RNReanimated\|react-native-reanimated" ios/Podfile.lock</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "react-native-keyboard-controller" package.json` returns exactly 1
    - `npm ls react-native-keyboard-controller` shows version `1.21.x` (>= 1.21.6 per CONTEXT D-01)
    - `npm ls react-native-reanimated react-native-worklets react-native-keyboard-controller` reports zero peer-dependency warnings
    - `grep -c "react-native-keyboard-controller\|RNKeyboardController" ios/Podfile.lock` returns ≥ 1 (pod install registered the new pod)
    - `grep -c "react-native-reanimated\|RNReanimated" ios/Podfile.lock` returns ≥ 1 (pod install registered reanimated's pod from the earlier install)
    - `cd android && ./gradlew clean && cd ..` exited 0 (recorded in 02-01-SUMMARY.md as a Wave 0 build artifact)
    - `git diff --name-only HEAD~1` shows ONLY: `package.json`, `package-lock.json`, `ios/Podfile.lock`
    - `git log --oneline -1` shows a commit message containing "react-native-keyboard-controller" and "Podfile.lock"
    - ios/Podfile itself is NOT in the diff (autolink — Podfile must remain untouched)
  </acceptance_criteria>
  <done>react-native-keyboard-controller@1.21.x installed; Podfile.lock regenerated with all three native modules; android gradle clean ran successfully. Library tree fully assembled, ready for device-build verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Physical-device build verification on both iOS + Android</name>
  <what-built>
    - reanimated@4.x + worklets@0.8.x + keyboard-controller@1.21.x installed (Tasks 1-3)
    - babel.config.js has 'react-native-worklets/plugin' as last entry (Task 2)
    - iOS Podfile.lock regenerated with all three native pods (Task 3)
    - Android gradle clean ran successfully (Task 3)

    The native module wiring is complete. Wave 1 (KeyboardProvider wiring in App.tsx) MUST NOT
    begin until both physical devices boot the app cleanly with the new native modules linked.
    Per RESEARCH §2.5 + the downstream consumer contract, this checkpoint is the BLOCKING gate
    between Wave 0 and Wave 1.
  </what-built>
  <how-to-verify>
    Human, please run BOTH physical-device builds and report results before approving Wave 1 entry.

    1. **iOS build on iPhone 15 Pro Max / iOS 26.4** (CONTEXT D-13 mandatory device):

       ```
       npx react-native run-ios --device "iPhone 15 Pro Max"
       ```

       Expected:
       - Build completes (xcodebuild exits 0)
       - App launches on the physical device
       - Home screen renders identically to pre-Wave-0 state (Wave 0 added zero render-tree changes)
       - Metro logs show ZERO occurrences of "Reanimated is not configured properly"
       - No red box at startup
       - No "RNReanimated module is null" or "RNKeyboardController module is null" errors

       If any of the above fail, do NOT approve. The most likely failure modes per RESEARCH §2.4:
       - L4 (bridgeless + worklets plugin missing) — re-verify Task 2 grep on babel.config.js
       - Stale DerivedData — `rm -rf ~/Library/Developer/Xcode/DerivedData/JayTap-*` and rebuild
       - Xcode 26 SDK missing (RESEARCH §9 A4) — pause Phase 2 and run Phase 8's SDK upgrade first

    2. **Android build on Moto G XT2513V / Android 16** (CONTEXT D-13 mandatory device):

       ```
       npx react-native run-android
       ```

       Expected:
       - Build completes (gradle exits 0)
       - App launches on the physical device
       - Home screen renders identically to pre-Wave-0 state
       - Logcat shows ZERO occurrences of "Reanimated is not configured properly"
       - No red box at startup
       - No native-module-null errors

       If failing on Android: check that Task 3's `./gradlew clean` actually ran. Re-run if
       needed: `cd android && ./gradlew clean && cd .. && npx react-native run-android`.

    3. **Smoke regression check (both devices):**

       - App opens to home screen
       - Property cards render
       - Bottom navigation taps still work (Phase 1 fix preserved — quick sanity)
       - Open Login modal (Profile → sign in if not authed) — keyboard pops when tapping email,
         field is NOT covered by keyboard (tells us KeyboardProvider's downstream lib is alive
         even though we haven't wrapped App.tsx yet — the per-screen KASV adoption is Wave 2)

       Wave 0's render-tree contract is "no visible change." If you see ANY visible change other
       than the smoke-check above, do NOT approve — surface what changed and we'll diagnose.

    4. **Capture build artifact metadata** in your approval message for the rollback record:

       - iOS device ID + iOS version (e.g. "iPhone 15 Pro Max / iOS 26.4")
       - Android device ID + Android version (e.g. "Moto G XT2513V / Android 16")
       - reanimated version installed (from `npm ls react-native-reanimated` output)
       - worklets version installed
       - keyboard-controller version installed
       - Pre-Wave-0 commit SHA from Task 1's `git rev-parse HEAD` snapshot

    If verification fails and recovery is not possible within 30 minutes, execute the rollback
    plan documented in 02-01-SUMMARY.md (`git checkout package.json package-lock.json
    babel.config.js`; restore Podfile.lock from /tmp backup; `cd android && ./gradlew clean`)
    and re-plan Wave 0 with the failure diagnostic surfaced.
  </how-to-verify>
  <acceptance_criteria>
    - Human reports: iOS build on iPhone 15 Pro Max / iOS 26.4 SUCCEEDED — app launched, no red box, Metro clean of Reanimated errors
    - Human reports: Android build on Moto G XT2513V / Android 16 SUCCEEDED — app launched, no red box, logcat clean of Reanimated errors
    - Human reports: smoke regression passed — home screen + bottom nav + Login modal email field works as expected
    - Human reports: device IDs + lib versions captured (will be written to 02-01-SUMMARY.md by the executor after this task)
    - Resume signal received with explicit "Wave 0 verified — proceed to Wave 1" OR an itemized failure report
    - On the Wave 0 entry-gate checklist (interfaces block §2.5):
      * grep -c "react-native-reanimated" package.json returns 1 ✓ (Task 1)
      * grep -c "react-native-worklets" package.json returns 1 ✓ (Task 1)
      * grep -c "react-native-keyboard-controller" package.json returns 1 ✓ (Task 3)
      * tail -n 5 babel.config.js shows 'react-native-worklets/plugin' as the last array entry ✓ (Task 2)
      * cd ios && pod install (last run in Task 3) exited 0; Podfile.lock contains the three pods ✓
      * npm run android builds + launches ✓ (this task)
      * npm run ios builds + launches ✓ (this task)
      * App opens to home screen unchanged ✓ (smoke regression)
    - All 8 §2.5 entry-gate items now PASS — Wave 1 is unblocked
  </acceptance_criteria>
  <resume-signal>
    Type one of:
    - "Wave 0 verified — proceed to Wave 1" + the captured device IDs and lib versions
    - "Wave 0 FAILED on {iOS|Android}" + paste the failing build output / red-box / Metro log
    - "Wave 0 ROLLBACK" + initiate the §2.6 rollback procedure (executor will assist)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Three new third-party native dependencies enter the runtime: `react-native-reanimated@^4.x`,
`react-native-worklets@^0.8.x`, `react-native-keyboard-controller@^1.21.x`. All three are
installed via npm into `node_modules/` and linked natively via React Native's autolink
(`use_native_modules!` on iOS; React Native CLI on Android). No new code is written; no new
auth, HTTP, storage, or PII surface is introduced. The trust boundary added is the supply-chain
boundary on these three packages.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T (Tampering) | npm install of 3 third-party packages | mitigate | Pin via `^X.Y.Z` ranges (per RESEARCH §2.1 — reanimated `^4.3.0`, worklets `^0.8.0`, keyboard-controller `^1.21.6`); commit `package-lock.json` so the resolved versions are reproducible across machines; `npm ls` gate in Tasks 1+3 verifies no peer-dep warnings (which can mask substituted dependencies). |
| T-02-01-02 | I (Information Disclosure) | reanimated v4 worklets runtime | accept | Worklets execute on the UI thread but only run code Phase 2 explicitly registers (zero — Phase 2 only wires KeyboardProvider, no custom worklets are authored). The library's own worklets are reviewed code from a maintainer-active project (kirillzyusko/react-native-keyboard-controller, mid-2026 active). No PII / token / user content flows through worklets in M1. |
| T-02-01-03 | T (Tampering) | Babel plugin order (worklets MUST be last) | mitigate | Documented as a load-bearing comment in babel.config.js (`// MUST be last`); enforced by Task 2 acceptance criterion grep + the §6 SC1.c grep gate the Wave 3 verifier runs (`tail -n 5 babel.config.js | grep "react-native-worklets/plugin"` returns 1). Future contributors who append a plugin without reading the comment trigger the same grep failure on the next verify run. |
| T-02-01-04 | E (Elevation of Privilege) | Native modules added to iOS/Android via autolink | accept | Both platforms' autolink mechanisms (Cocoapods `use_native_modules!`, React Native CLI for Android) execute installer code from `node_modules/<pkg>/scripts/`. Standard supply-chain risk — no different from any other RN native module install (e.g. `react-native-image-picker` already in dependencies). Mitigation is the package pinning in T-02-01-01. |

No S/R/D applicable — this plan adds no auth surface, no repudiation-relevant state, and no DoS surface.
Block on `high` severity per project default (`security_enforcement: enabled`); zero high-severity threats raised in this plan.
</threat_model>

<verification>
- `grep -c "react-native-reanimated" package.json` returns 1 (KBD-03)
- `grep -c "react-native-worklets" package.json` returns 1 (KBD-03 — load-bearing peer)
- `grep -c "react-native-keyboard-controller" package.json` returns 1 (D-01)
- `tail -n 5 babel.config.js | grep -c "react-native-worklets/plugin"` returns 1 (v4 plugin name, NOT v3)
- `grep -c "react-native-reanimated/plugin" babel.config.js` returns 0 (legacy v3 name MUST NOT appear)
- `grep -c "// MUST be last" babel.config.js` returns 1 (load-bearing comment preserved)
- `grep -c "react-native-keyboard-controller\|RNKeyboardController" ios/Podfile.lock` returns ≥ 1
- `grep -c "react-native-reanimated\|RNReanimated" ios/Podfile.lock` returns ≥ 1
- Human-verified: iOS physical-device build (iPhone 15 Pro Max / iOS 26.4) launches, no red box, Metro clean
- Human-verified: Android physical-device build (Moto G XT2513V / Android 16) launches, no red box, logcat clean
- Three atomic commits in `git log --oneline | head -3` with messages referencing 02 / KBD-03 / install
</verification>

<success_criteria>
- KBD-03 ✓ — reanimated v4 + worklets installed; Babel plugin LAST entry per RESEARCH §2.3
- KBD-04 ✓ — Fabric / New Architecture verified by physical-device build success on BOTH platforms (CONTEXT D-13 device IDs captured)
- §2.5 Wave 0 entry-gate checklist all 8 items PASS
- ios/Podfile UNTOUCHED (autolink via use_native_modules!) — verified by `git diff --name-only HEAD~3 -- ios/Podfile` returning empty
- Wave 1 (Plan 02 — KeyboardProvider wiring) is now UNBLOCKED
- Rollback plan documented in 02-01-SUMMARY.md with the pre-Wave-0 commit SHA captured
- 02-VALIDATION.md row "Wave 0 install" gates all PASS
</success_criteria>

<output>
After completion, create `.planning/phases/02-universal-keyboard-handling/02-01-SUMMARY.md` documenting:
- Pre-Wave-0 commit SHA (rollback target) captured in Task 1
- Three atomic commit SHAs from Tasks 2 and 3
- Resolved versions: reanimated `<v>`, worklets `<v>`, keyboard-controller `<v>`
- Device IDs verified at the Wave 0 gate: iOS `<device/version>`, Android `<device/version>`
- Pod install output snippet showing RNReanimated, RNWorklets, RNKeyboardController being installed
- gradle clean exit code (must be 0)
- Explicit rollback recipe (verbatim from RESEARCH §2.6) the user can paste if Wave 1 surfaces an unexpected install-related failure:

    git checkout package.json package-lock.json babel.config.js
    cp /tmp/Podfile.lock.bak ios/Podfile.lock
    cd ios && pod install && cd ..
    cd android && ./gradlew clean && cd ..
    npx react-native start --reset-cache

- Explicit statement: "Wave 1 is unblocked — Plan 02 (KeyboardProvider wiring) may proceed."
</output>
</content>
</invoke>