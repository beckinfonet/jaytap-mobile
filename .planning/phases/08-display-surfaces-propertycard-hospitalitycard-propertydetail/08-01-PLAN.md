---
phase: 08-display-surfaces-propertycard-hospitalitycard-propertydetail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locales/en.ts
  - src/locales/ru.ts
autonomous: true
requirements:
  - DISP-05
tags:
  - i18n
  - locale
  - parity
must_haves:
  truths:
    - "EN locale exports 3 new specs-row keys: property.specs.bedrooms, property.specs.bathrooms, property.specs.rooms"
    - "RU locale exports the same 3 keys with Russian values (Спальни / Ванные / Комнаты)"
    - "scripts/check-i18n-parity.sh exits 0 (lockstep parity preserved)"
    - "Existing keys property.beds, property.baths, property.bathroomPrivate/Shared/None, hospitality.rooms, hospitality.bathrooms are LEFT UNTOUCHED"
  artifacts:
    - path: "src/locales/en.ts"
      provides: "EN translation table with 3 new property.specs.* keys"
      contains: "property.specs.bedrooms"
    - path: "src/locales/ru.ts"
      provides: "RU translation table with 3 new property.specs.* keys (lockstep)"
      contains: "property.specs.bedrooms"
  key_links:
    - from: "src/locales/en.ts"
      to: "src/locales/ru.ts"
      via: "lockstep key parity (each EN key has matching RU key)"
      pattern: "property\\.specs\\.(bedrooms|bathrooms|rooms)"
---

<objective>
Add the 3 new specs-row label keys to the EN+RU locale tables that downstream Phase 8 plans (PropertyCard redesign, PropertyDetailsScreen specs-row flip) will consume via `t()`. This is the i18n foundation for the entire phase — every JSX surface that lands later in the wave reads these keys.

Purpose: DISP-05 requires every new specs-row label routed through `t()`; the keys must exist BEFORE the JSX changes that consume them so the parity gate stays green commit-by-commit.

Output: 3 new key strings present in both `en.ts` and `ru.ts` with Phase-8 annotation comment; `scripts/check-i18n-parity.sh` exits 0.

LOAD-BEARING CORRECTION (from gsd-pattern-mapper): `hospitality.bathrooms` ALREADY EXISTS at `en.ts:281` / `ru.ts:283`. CONTEXT.md "4 new keys" is incorrect — Phase 8's real delta is exactly 3 keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-CONTEXT.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md
@CLAUDE.md
@src/locales/en.ts
@src/locales/ru.ts
@scripts/check-i18n-parity.sh

<interfaces>
<!-- Existing structure of locale tables. -->
<!-- Both files export `const translations: Record<string, string>` shaped objects. -->
<!-- Key format is dot-separated namespace.subkey strings as object property names. -->

From src/locales/en.ts (existing cluster around line 121-128):
```
'property.available': 'Available:',
'property.now': 'now',
'property.beds': 'Beds',
'property.baths': 'Baths',
'property.bathroomPrivate': 'Private',
'property.bathroomShared': 'Shared',
'property.bathroomNone': 'None',
'property.details': 'Details',
```

From src/locales/en.ts:281 (already shipped — DO NOT re-add):
```
'hospitality.bathrooms': 'Bathrooms',
```

From src/locales/ru.ts:283 (already shipped — DO NOT re-add):
```
'hospitality.bathrooms': 'Ванные',
```

From src/locales/en.ts:672-678 (Phase-7 annotation precedent — copy this comment-header style):
```
// Phase 07 (Plan 07-02) — bedrooms + bathroomCount stepper keys (FORM-02, FORM-04).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 new property.specs.* keys to en.ts and ru.ts in lockstep</name>
  <files>src/locales/en.ts, src/locales/ru.ts</files>
  <read_first>
    - src/locales/en.ts (read the full file — small, single-pass extraction; locate the existing property.* cluster around line 121-128 AND the Phase-7 annotation header around line 672 as the comment-style precedent)
    - src/locales/ru.ts (read the full file — small, single-pass; verify line 283 already holds 'hospitality.bathrooms': 'Ванные' AND locate Phase-7 annotation around line 674)
    - scripts/check-i18n-parity.sh (read the full script to know exactly what the parity gate verifies — key set equality between EN and RU)
    - .planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md (re-read the Pre-flight Correction at lines 7-9 — confirms hospitality.bathrooms is NOT a new key)
  </read_first>
  <action>
    Add exactly 3 new keys to BOTH locale files, in lockstep. Do NOT modify or re-add `hospitality.bathrooms` (already exists at en.ts:281 / ru.ts:283). Do NOT touch existing keys `property.beds`, `property.baths`, `property.bathroomPrivate`, `property.bathroomShared`, `property.bathroomNone`, `hospitality.rooms`, `hospitality.maxGuests`.

    Per D-08 (CONTEXT.md), exact key strings and values:
    - en.ts: `'property.specs.bedrooms': 'Bedrooms'` ; `'property.specs.bathrooms': 'Bathrooms'` ; `'property.specs.rooms': 'Rooms'`
    - ru.ts: `'property.specs.bedrooms': 'Спальни'` ; `'property.specs.bathrooms': 'Ванные'` ; `'property.specs.rooms': 'Комнаты'`

    Placement: append the 3 keys immediately after the existing `property.bathroomNone` line in the property.* cluster (around line 127 in en.ts and the symmetric position in ru.ts). Maintain the alphabetical-ish grouping convention; keep both files line-symmetric so a future diff reads side-by-side cleanly.

    Annotation: prepend a single-line comment header on the line immediately above the 3 new keys in BOTH files (mirroring the Phase-7 annotation style at en.ts:672 / ru.ts:674). Exact comment text:
      `// Phase 08 (Plan 08-01) — specs-row labels for PropertyCard + PropertyDetailsScreen (DISP-01..DISP-05).`

    No type-union changes (TranslationKeys regeneration is downstream; consumers will use `as any` casts per the existing PropertyCard.tsx:291 / PropertyDetailsScreen.tsx:1060-1064 pattern).
  </action>
  <verify>
    <automated>grep -c "'property.specs.bedrooms'" src/locales/en.ts | grep -q "^1$" && grep -c "'property.specs.bathrooms'" src/locales/en.ts | grep -q "^1$" && grep -c "'property.specs.rooms'" src/locales/en.ts | grep -q "^1$" && grep -c "'property.specs.bedrooms'" src/locales/ru.ts | grep -q "^1$" && grep -c "'property.specs.bathrooms'" src/locales/ru.ts | grep -q "^1$" && grep -c "'property.specs.rooms'" src/locales/ru.ts | grep -q "^1$" && grep -q "'Спальни'" src/locales/ru.ts && grep -q "'Ванные'" src/locales/ru.ts && grep -q "'Комнаты'" src/locales/ru.ts && bash scripts/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - en.ts contains exactly one occurrence of `'property.specs.bedrooms': 'Bedrooms'`
    - en.ts contains exactly one occurrence of `'property.specs.bathrooms': 'Bathrooms'`
    - en.ts contains exactly one occurrence of `'property.specs.rooms': 'Rooms'`
    - ru.ts contains exactly one occurrence of `'property.specs.bedrooms': 'Спальни'`
    - ru.ts contains exactly one occurrence of `'property.specs.bathrooms': 'Ванные'`
    - ru.ts contains exactly one occurrence of `'property.specs.rooms': 'Комнаты'`
    - `hospitality.bathrooms` count in en.ts remains exactly 1 (no duplicate add)
    - `hospitality.bathrooms` count in ru.ts remains exactly 1
    - Existing keys present unchanged: `property.beds`, `property.baths`, `property.bathroomPrivate`, `property.bathroomShared`, `property.bathroomNone`, `hospitality.rooms`, `hospitality.maxGuests`
    - `bash scripts/check-i18n-parity.sh` exits with code 0
    - Comment header `// Phase 08 (Plan 08-01) — specs-row labels for PropertyCard + PropertyDetailsScreen (DISP-01..DISP-05).` appears exactly once in en.ts AND once in ru.ts
    - No TypeScript errors introduced: `npx tsc --noEmit` produces no NEW errors versus a `git stash` baseline run on the same checkout (pre-existing tsc errors in unrelated files are acceptable)
  </acceptance_criteria>
  <done>3 new keys present in both EN and RU locale tables; parity gate green; existing keys untouched; Phase-08 annotation comment present in both files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No new trust boundary in this plan. Locale tables are static literals compiled into the RN bundle. No untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering / Information Disclosure | src/locales/en.ts + src/locales/ru.ts | accept | Static string literals; keys/values are not user-controllable; no PII; ASVS L1 not applicable. Verdict per phase threat-model gate: read-side rendering only; no new attack surface introduced. Threats: (a) i18n key injection — N/A (keys are static literals); (b) XSS via property data — N/A (RN Text components escape by default; no `dangerouslySetInnerHTML` equivalent). |
</threat_model>

<verification>
- `scripts/check-i18n-parity.sh` exits 0
- `grep -c "'property.specs"` returns 3 in en.ts AND 3 in ru.ts
- `grep -c "hospitality.bathrooms"` returns 1 in en.ts AND 1 in ru.ts (regression fence against accidental duplicate add)
- `npx tsc --noEmit` produces zero net-new errors vs the pre-plan baseline
</verification>

<success_criteria>
- 3 keys added to en.ts: `property.specs.bedrooms` → `"Bedrooms"`, `property.specs.bathrooms` → `"Bathrooms"`, `property.specs.rooms` → `"Rooms"`
- 3 keys added to ru.ts (matching key strings): `"Спальни"`, `"Ванные"`, `"Комнаты"`
- `scripts/check-i18n-parity.sh` exit 0
- No existing keys mutated or removed
- Phase-08 annotation comment present in both locale files
- Git diff scope: exactly 2 files (en.ts, ru.ts); 4 lines added per file (1 comment + 3 keys); zero deletions
</success_criteria>

<output>
After completion, create `.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-01-SUMMARY.md` capturing:
- Exact line numbers where the 3 keys landed in each locale file
- `scripts/check-i18n-parity.sh` exit code (0)
- `grep -c "'property.specs"` count in each file (must be 3)
- `hospitality.bathrooms` count in each file post-edit (must remain 1 — regression fence)
- Confirmation that no existing keys were touched
- Commit SHA of the locale additions
</output>
