---
phase: 08-display-surfaces-propertycard-hospitalitycard-propertydetail
plan: 05
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/ListingMetaTable.tsx
  - src/components/__tests__/ListingMetaTable.test.tsx
autonomous: true
requirements:
  - DISP-03
tags:
  - react-native
  - component
  - cleanup
  - surgical-removal
must_haves:
  truths:
    - "ListingMetaTable extras grid no longer renders the rooms row (lines 163-167 removed; D-09)"
    - "ListingMetaTable extras grid no longer renders the bathroom enum row (lines 168-176 removed; D-09)"
    - "The const rooms (line 68) and const bathroom (line 69) derivations are removed (no unused-variable lint)"
    - "The hasExtras chain (lines 78-87) no longer references rooms or bathroom as contributors"
    - "All OTHER extras grid rows remain intact (condition, furnished, negotiable, deposit, prepayment, minTerm)"
    - "ID + availability rows preserved unchanged"
    - "After Phase 8, PropertyDetailsScreen surfaces beds/baths exclusively through the canonical specs row (DISP-03 'canonical specs row only; no duplicate')"
    - "New co-located ListingMetaTable.test.tsx exists with surgical-removal regression fence + happy-path extras coverage"
  artifacts:
    - path: "src/components/ListingMetaTable.tsx"
      provides: "Extras grid cleanup: 2 rows + 2 derivations + 2 hasExtras chain links removed; mirrors 260525-i2i surgical pattern"
      contains: "hasExtras"
    - path: "src/components/__tests__/ListingMetaTable.test.tsx"
      provides: "New co-located test asserting the rooms row + bathroom enum row no longer render under any condition; other extras rows render correctly"
      contains: "ListingMetaTable"
  key_links:
    - from: "src/components/ListingMetaTable.tsx"
      to: "src/screens/PropertyDetailsScreen.tsx"
      via: "ListingMetaTable is rendered by PropertyDetailsScreen; the rows being removed previously duplicated PropertyDetailsScreen's specs-row data ~50px above (260525-i2i precedent)"
      pattern: "<ListingMetaTable property={property}"
---

<objective>
Surgically remove the `rooms != null && ...` row (lines 163-167) and the `bathroom != null && ...` row (lines 168-176) from `ListingMetaTable.tsx`'s extras grid. Also remove the now-unused `const rooms` (line 68) and `const bathroom` (line 69) derivations AND the `rooms != null ||` + `bathroom != null ||` links in the `hasExtras` chain (lines 78-87). After Phase 8, PropertyDetailsScreen surfaces beds/baths exclusively through the canonical specs row at lines 1048-1076 — the previous duplication (two competing renders ~50px apart) is eliminated. Add a co-located test (NEW file — none exists pre-Phase-8) covering the surgical-removal regression fence + happy-path extras rendering.

Purpose: Closes DISP-03's "canonical specs row only; no duplicate" sub-clause. Mirrors the 260525-i2i quick-task pattern (commit `15f1010`) where the duplicate m² rendering was surgically removed from the same extras grid. Same file, same surgical posture, different rows.

Output: ListingMetaTable.tsx cleanup with ~14 lines deleted (2 derivations + 14-line row block); zero replacement (no new fields, no new rows); new ListingMetaTable.test.tsx with 6 cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-CONTEXT.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-01-SUMMARY.md
@CLAUDE.md
@src/components/ListingMetaTable.tsx
@src/components/__tests__/PropertyCard.specChip.test.tsx
@src/components/__tests__/StepperInput.test.tsx
@src/types/Property.ts

<interfaces>
<!-- Exact lines to delete, edit. -->

From src/components/ListingMetaTable.tsx (current state — verified by grep):
- Line 68: `const rooms = property?.basics?.rooms;` (DELETE)
- Line 69: `const bathroom = property?.basics?.bathroom;` (DELETE)
- Lines 78-87: `hasExtras` chain — currently:
  ```
  const hasExtras =
    rooms != null ||
    bathroom != null ||
    areaSqm != null ||
    condition != null ||
    furnished != null ||
    negotiable != null ||
    deposit != null ||
    prepaymentMonths != null ||
    minTerm != null;
  ```
  After: drop `rooms != null ||` and `bathroom != null ||` lines; keep all other chain links unchanged.
- Lines 163-167: `{rooms != null && (<Text ...>...{t('property.beds')}: {rooms}</Text>)}` block (DELETE entirely)
- Lines 168-176: `{bathroom != null && (<Text ...>...{t('property.baths')}: ...</Text>)}` block (DELETE entirely)
- Line 161: `{hasExtras && (` opener — UNCHANGED
- Lines 177-208: other extras rows (areaSqm already removed by 260525-i2i; condition / furnished / negotiable / deposit / prepayment / minTerm rows present) — UNCHANGED

From src/types/Property.ts:
- `basics.rooms?: string | number` (M3; still captured by form Step 3 for cultural "3-room apartment" semantics — Out of Scope in M4 to remove)
- `basics.bathroom?: 'private' | 'shared' | 'none'` (M3 enum; still captured on office/commercial — Out of Scope to remove)
- Both fields stay alive in the schema and form; Phase 8 only removes their READ paths from this particular surface.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgically remove rooms + bathroom-enum rows + derivations + hasExtras links from ListingMetaTable.tsx</name>
  <files>src/components/ListingMetaTable.tsx</files>
  <read_first>
    - src/components/ListingMetaTable.tsx (read the FULL file — ~290 LOC; verify the exact current line numbers for: the `const rooms` + `const bathroom` derivations near lines 68-69, the `hasExtras` chain near lines 78-87, the two row JSX blocks near lines 163-176, and the surrounding context — line numbers may have drifted by 1-2 if any whitespace edits landed since the PATTERNS.md mapping; use grep + Read to pin the exact ranges)
    - .planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-CONTEXT.md (re-read D-09 + D-10 sections + the Claude's Discretion clause about `rooms` + `bathroom` derivation removal at lines 90-91)
    - .planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md (re-read ListingMetaTable section lines 330-391 — the exact lines to delete are reproduced verbatim there)
    - .planning/quick/260525-i2i-remove-duplicate-m-rendering-from-listin/ (read the directory listing and the 260525-i2i summary if present — the surgical-removal precedent for this file; commit `15f1010` is the analog)
  </read_first>
  <action>
    Apply four surgical edits to `src/components/ListingMetaTable.tsx`. Do NOT replace the removed content with anything new (D-10: no replacement, no new fields, no new rows). All other content in the file remains untouched.

    Edit 1 — Delete the `rooms != null && ...` row block:
    Locate the JSX block (currently around lines 163-167) of the shape:
    ```
    {rooms != null && (
      <Text style={[styles.extraText, { color: colors.text, fontSize: valueSize }]} numberOfLines={1}>
        {t('property.beds')}: {rooms}
      </Text>
    )}
    ```
    Delete the entire block including the opening `{rooms != null && (` and the closing `)}`. This is the duplicate-render that PropertyDetailsScreen's specs-row Beds cell already surfaces ~50px below.

    Edit 2 — Delete the `bathroom != null && ...` row block:
    Locate the JSX block (currently around lines 168-176) of the shape:
    ```
    {bathroom != null && (
      <Text style={[styles.extraText, { color: colors.text, fontSize: valueSize }]} numberOfLines={1}>
        {t('property.baths')}: {bathroom === 'private'
          ? t('property.bathroomPrivate' as any)
          : bathroom === 'shared'
            ? t('property.bathroomShared' as any)
            : t('property.bathroomNone' as any)}
      </Text>
    )}
    ```
    Delete the entire block. This is the bathroom-enum render that competed with the canonical bathroomCount surface in Phase 8 plan 04.

    Edit 3 — Delete the `const rooms` and `const bathroom` derivations:
    Locate (currently lines 68-69):
    ```
    const rooms = property?.basics?.rooms;
    const bathroom = property?.basics?.bathroom;
    ```
    Delete both lines. No JSX consumer remains after Edit 1 + 2, and the `hasExtras` chain will lose its references in Edit 4.

    Edit 4 — Drop `rooms != null ||` and `bathroom != null ||` from the `hasExtras` chain:
    Locate (currently lines 78-87):
    ```
    const hasExtras =
      rooms != null ||
      bathroom != null ||
      areaSqm != null ||
      condition != null ||
      furnished != null ||
      negotiable != null ||
      deposit != null ||
      prepaymentMonths != null ||
      minTerm != null;
    ```
    After edit:
    ```
    const hasExtras =
      areaSqm != null ||
      condition != null ||
      furnished != null ||
      negotiable != null ||
      deposit != null ||
      prepaymentMonths != null ||
      minTerm != null;
    ```
    Keep `areaSqm` (and every other chain link) verbatim. The `hasExtras` boolean still evaluates correctly because it remains an OR-chain over 7 other contributing fields; dropping two contributors does not break the gating.

    Boundary anchors (do NOT touch):
    - The `{hasExtras && (` opener at line 161 — keep verbatim
    - The other extras rows below the deletion site (`condition`, `furnished`, `negotiable`, `deposit`, `prepaymentMonths`, `minTerm`) — keep verbatim
    - The `areaSqm` derivation (already present from the 260525-i2i prior state; planner may leave it alone — verifying the variable usage is outside Phase 8 scope per PATTERNS.md L383)
    - The ID + availability rows above the extras grid — keep verbatim
    - The StyleSheet block — zero edits (no new styles needed; styles for the removed rows were the shared `styles.extraText` which other rows still use)
    - Any other imports or component logic — keep verbatim
    - The `t('property.beds')`, `t('property.baths')`, `t('property.bathroomPrivate')`, etc. KEYS — they remain in the locale tables; this plan removes their CONSUMPTION here only

    Diff scope expectation: ~14 deleted lines (5 from Edit 1, 9 from Edit 2, 2 from Edit 3) + 2 lines edited in Edit 4 (the two chain links dropped) — total ~16 line-change footprint, zero insertions.

    Mirrors the 260525-i2i pattern: commit `15f1010` removed the duplicate `m²` render from the same extras grid (5-line deletion, zero replacement). This plan applies the same surgical posture to the two M3-era basics rows that Phase 8 supersedes with the canonical specs row.
  </action>
  <verify>
    <automated>grep -c "rooms != null" src/components/ListingMetaTable.tsx | grep -q "^0$" && grep -c "bathroom != null" src/components/ListingMetaTable.tsx | grep -q "^0$" && grep -c "const rooms = property" src/components/ListingMetaTable.tsx | grep -q "^0$" && grep -c "const bathroom = property" src/components/ListingMetaTable.tsx | grep -q "^0$" && grep -c "property.beds" src/components/ListingMetaTable.tsx | grep -q "^0$" && grep -c "property.baths" src/components/ListingMetaTable.tsx | grep -q "^0$" && grep -c "property.bathroomPrivate" src/components/ListingMetaTable.tsx | grep -q "^0$" && grep -c "hasExtras" src/components/ListingMetaTable.tsx | grep -qE "^[2-9]$" && grep -c "areaSqm" src/components/ListingMetaTable.tsx | grep -qE "^[1-9]$" && bash scripts/check-i18n-parity.sh && (grep -rn "keyboardVerticalOffset" src/ | wc -l | tr -d ' ' | grep -q "^0$")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rooms != null" src/components/ListingMetaTable.tsx` returns 0 (Edit 1 + Edit 4 verified)
    - `grep -c "bathroom != null" src/components/ListingMetaTable.tsx` returns 0 (Edit 2 + Edit 4 verified)
    - `grep -c "const rooms = property" src/components/ListingMetaTable.tsx` returns 0 (Edit 3a verified)
    - `grep -c "const bathroom = property" src/components/ListingMetaTable.tsx` returns 0 (Edit 3b verified)
    - `grep -c "property.beds" src/components/ListingMetaTable.tsx` returns 0 (no `t('property.beds')` consumption remains in this file)
    - `grep -c "property.baths" src/components/ListingMetaTable.tsx` returns 0
    - `grep -c "property.bathroomPrivate" src/components/ListingMetaTable.tsx` returns 0
    - `grep -c "hasExtras" src/components/ListingMetaTable.tsx` returns at least 2 (declaration + gate; chain still works for the other 7 contributors)
    - `grep -c "areaSqm" src/components/ListingMetaTable.tsx` returns at least 1 (areaSqm chain link preserved)
    - Existing extras rows still render: `grep -c "condition\|furnished\|negotiable\|deposit\|prepaymentMonths\|minTerm" src/components/ListingMetaTable.tsx` returns 6 or more (one match per remaining row, possibly more if same identifier referenced multiple times)
    - `bash scripts/check-i18n-parity.sh` exit 0 (no locale changes; this plan is JSX-only)
    - `grep -rn "keyboardVerticalOffset" src/ | wc -l | tr -d ' ' | grep -q "^0$"` succeeds (KBD-02 invariant)
    - `npx tsc --noEmit` zero net-new errors (specifically: no unused-variable lint for `rooms` / `bathroom` because they were removed; no missing-reference error in `hasExtras` because the references were dropped together)
    - Git diff scope: ONLY src/components/ListingMetaTable.tsx; net line count change is negative (~14-16 lines removed, zero added)
  </acceptance_criteria>
  <done>ListingMetaTable's extras grid no longer renders the rooms row or the bathroom-enum row; unused derivations removed; hasExtras chain cleaned; mirrors 260525-i2i surgical-removal pattern; no collateral damage to other extras rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create new co-located ListingMetaTable.test.tsx with surgical-removal regression fence + happy-path extras cases</name>
  <files>src/components/__tests__/ListingMetaTable.test.tsx</files>
  <read_first>
    - src/components/__tests__/StepperInput.test.tsx (read FULL — canonical co-located component test shape; lift the mock pattern + render helper structure)
    - src/components/__tests__/PropertyCard.specChip.test.tsx (read FULL — clone the collectTexts helper at lines 111-124)
    - src/components/ListingMetaTable.tsx (read the POST-Task-1 file — verify the surgical edits landed; inventory imports to know which modules need jest.mock entries)
    - src/types/Property.ts (read FULL — small; use real Property type for fixtures)
    - .planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md (re-read ListingMetaTable test section lines 561-579 — confirms 6 cases)
  </read_first>
  <behavior>
    NOTE on assertion strings: because the `t` mock returns the key verbatim (`t: (key: string) => key`), the rendered Beds/Baths row Text would surface as `'property.beds'` (the key) followed by `': '` and the value — NOT the resolved EN string `'Beds:'`. All assertions below check for the LITERAL i18n key strings `'property.beds'` and `'property.baths'` (and `'property.bathroomPrivate'` for the enum-value resolution that no longer happens), matching the t-mock-key-verbatim convention used in PropertyCard.specChip.test.tsx and across the Phase 8 test stack.

    - Test 1 (rooms-only property — regression fence for Edit 1): `basics: { rooms: '3' }` and NO other extras-grid-triggering fields → collectTexts does NOT contain the literal key string `'property.beds'` (the row is deleted) AND the entire extras grid does NOT render (because hasExtras evaluates to false after Edit 4 dropped the rooms chain link). Asserts both Edit 1 (row removed) and Edit 4 (chain link dropped).
    - Test 2 (bathroom-only property — regression fence for Edit 2): `basics: { bathroom: 'private' }` and no other extras → collectTexts does NOT contain `'property.baths'` (key string) AND does NOT contain `'property.bathroomPrivate'`. Extras grid does not render.
    - Test 3 (rooms + bathroom only — combined regression): `basics: { rooms: '2', bathroom: 'shared' }` and no other extras → entire extras grid does NOT render (proves the `hasExtras` chain no longer treats either field as a contributor). collectTexts does not contain `'property.beds'` or `'property.baths'` (literal i18n key strings).
    - Test 4 (rooms + deposit — proves rooms no longer contributes): `basics: { rooms: '3' }` + `terms: { deposit: { amount: 1000, currency: 'KGS' } }` (or whatever shape ListingMetaTable expects for deposit — check the file) → extras grid renders ONE row (the deposit row); does NOT render a rooms row. collectTexts contains `'1000'` or `'KGS'` (deposit amount) but NOT `'property.beds'`.
    - Test 5 (bathroom + condition — proves bathroom no longer contributes): `basics: { bathroom: 'shared' }` + `conditionAndAmenities: { condition: 'renovated' }` (or whatever path; inspect the file) → extras grid renders ONE row (the condition row); does NOT render a bathroom row. collectTexts contains the condition value but NOT `'property.baths'` or `'property.bathroomShared'`.
    - Test 6 (all extras except rooms + bathroom — proves no collateral damage): provide a Property with EVERY other extras-contributing field populated (`areaSqm`, `condition`, `furnished`, `negotiable`, `deposit`, `prepaymentMonths`, `minTerm`) but NO `rooms` and NO `bathroom` → extras grid renders all 7 other rows correctly; collectTexts contains expected values for each.
  </behavior>
  <action>
    Create `src/components/__tests__/ListingMetaTable.test.tsx` as a NEW file (PATTERNS.md confirms no existing ListingMetaTable test in the repo).

    File structure:
    1. Imports: `React`, `TestRenderer` + `act` from `react-test-renderer`, `Text` from `react-native`, `ListingMetaTable` from `'../ListingMetaTable'`, `type Property` from `'../../types/Property'`.
    2. jest.mock block (lean — ListingMetaTable is simpler than PropertyCard):
       - `jest.mock('../../theme/ThemeContext', () => ({ useTheme: jest.fn() }))`
       - `jest.mock('../../context/LanguageContext', () => ({ useLanguage: jest.fn() }))`
       - Any subcomponent imports — inspect the file and mock to `() => null` (likely minimal; ListingMetaTable is mostly Text elements)
    3. Mock-setup helper: configure `useTheme()` to return `{ colors: { text: '#000', textSecondary: '#666', surface: '#fff', border: '#ccc', /* any other tokens read */ } }`; configure `useLanguage()` to return `{ t: (key: string) => key, locale: 'en' }` — RETURN KEY VERBATIM convention used across the Phase 8 test stack.
    4. `collectTexts(tree)` helper — clone verbatim from PropertyCard.specChip.test.tsx:111-124.
    5. Render helper `renderMeta(overrides: Partial<Property>)` building a minimal Property fixture, then `TestRenderer.create(<ListingMetaTable property={fixture} />)` inside `act`.
    6. Six `test(...)` cases per the behavior block above.

    Critical assertion strategy (corrected — uses literal i18n key strings, NOT resolved EN values):

    Because `t` returns the key verbatim, if the deleted rows had rendered they would have surfaced strings containing the literal key `'property.beds'` and `'property.baths'` in the Text tree — NOT `'Beds:'` / `'Baths:'`. Using `.includes('Beds:')` would silently fail to detect regressions (the assertion would always be false, even if the row were accidentally re-introduced). Use the i18n key strings instead.

    - "Extras grid does not render" assertions:
      - `expect(collectTexts(tree).some(t => t.includes('property.beds'))).toBe(false)` — proves the Beds row is gone (Edit 1 verified)
      - `expect(collectTexts(tree).some(t => t.includes('property.baths'))).toBe(false)` — proves the Baths row is gone (Edit 2 verified)
      - `expect(collectTexts(tree).some(t => t.includes('property.bathroomPrivate') || t.includes('property.bathroomShared') || t.includes('property.bathroomNone'))).toBe(false)` — proves the enum-resolution path is gone
    - For Test 3 specifically (the most important regression fence — proves the hasExtras chain works correctly after dropping two contributors), the assertion is that with ONLY rooms + bathroom populated, the entire extras grid View should not be in the tree. Implement this as: `expect(collectTexts(tree).some(t => t.includes('property.beds') || t.includes('property.baths'))).toBe(false)` — proves both rows + the gate change. Using the LITERAL i18n key strings here is load-bearing — `.includes('Beds:')` would always be false (the t mock never returns `'Beds:'`) and would NOT catch a regression.
    - For Test 4 and Test 5, the assertion is one row renders (the deposit / condition row) and zero rooms/bathroom rows render — proves no collateral damage AND no chain coupling. Use the same literal-key-string assertion shape.

    Optional reinforcement: if ListingMetaTable exposes a `testID="extras-grid"` on the extras container, use `tree.root.findAll(node => node.props?.testID === 'extras-grid').length === 0` for tighter assertions. If no testID exists, the literal-key-string absence check above is the deterministic fallback.

    Property fixture defaults: minimal valid shape — `id`, `propertyType: 'apartment'`, `title`, `price`, `location`, `photos: []`, `basics: {}` (overridable per-test).

    Pass any required ListingMetaTable props (likely just `property`) — inspect the file's component signature to confirm; pass `jest.fn()` for any callbacks.
  </action>
  <verify>
    <automated>npx jest src/components/__tests__/ListingMetaTable.test.tsx --no-coverage 2>&1 | grep -E "Tests:.*6 passed|6 passed, 6 total"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/__tests__/ListingMetaTable.test.tsx` exists
    - Test suite contains exactly 6 `test(...)` or `it(...)` cases matching the behavior block
    - All 6 cases pass: `npx jest src/components/__tests__/ListingMetaTable.test.tsx --no-coverage` exits 0 with "6 passed"
    - Test 1 + Test 2 + Test 3 explicitly assert ABSENCE of the LITERAL i18n key strings `'property.beds'` and `'property.baths'` from `collectTexts` output (NOT the resolved EN values `'Beds:'` / `'Baths:'` — those would never appear under the t-mock-returns-key-verbatim convention and would mask regressions) — the surgical-removal regression fence
    - Test 2 also asserts absence of `'property.bathroomPrivate'` (the enum-resolution key that would only appear if the deleted enum-branch row rendered)
    - Test 4 + Test 5 prove no chain coupling (deposit / condition rows still render normally; literal-key absence check applies to rooms/bathroom rows)
    - Test 6 proves no collateral damage (other extras rows all render)
    - Test stack matches repo convention: `react-test-renderer` + `act` ONLY; no `@testing-library/*`; no `jest-native`
    - `t` mock returns key verbatim — all assertions check for literal key strings, not resolved EN/RU values
    - `npx tsc --noEmit` zero net-new errors
  </acceptance_criteria>
  <done>New ListingMetaTable.test.tsx with 6 cases passes; surgical-removal regression fence in place (asserting on literal i18n keys to match the t-mock convention); no collateral damage proven; test stack matches repo conventions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 8 is read-side rendering only; no new attack surface introduced. ListingMetaTable receives Property data from parent screen (already through trust boundary at API fetch). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering / Information Disclosure | src/components/ListingMetaTable.tsx (surgical deletion of read paths) | accept | Deletion-only edit; no new rendering surface introduced. The removed rows were duplicating data already shown in PropertyDetailsScreen's specs row. RN Text components escape children by default — XSS via property data N/A. |
| T-08-05-02 | Tampering | src/components/__tests__/ListingMetaTable.test.tsx | accept | Test-only mock; not bundled into production app. |

Verdict: no new threats. ASVS L1 not applicable. This plan REDUCES attack surface (fewer render paths reading user-supplied basics fields) rather than expanding it.
</threat_model>

<verification>
- `grep -c "rooms != null\|bathroom != null\|const rooms = property\|const bathroom = property" src/components/ListingMetaTable.tsx` returns 0 (all four edit targets removed)
- `grep -c "property.beds\|property.baths\|property.bathroomPrivate\|property.bathroomShared\|property.bathroomNone" src/components/ListingMetaTable.tsx` returns 0 (no consumption remains)
- `npx jest src/components/__tests__/ListingMetaTable.test.tsx --no-coverage` exits 0 with 6 passed
- `bash scripts/check-i18n-parity.sh` exit 0
- `grep -rn "keyboardVerticalOffset" src/ | wc -l | tr -d ' ' | grep -q "^0$"` succeeds (KBD-02 invariant)
- `npx tsc --noEmit` zero net-new errors
- All other extras row identifiers (condition, furnished, negotiable, deposit, prepaymentMonths, minTerm) still present in the file
</verification>

<success_criteria>
- ListingMetaTable.tsx no longer renders the rooms row or the bathroom-enum row under any condition
- The two derivations + the two hasExtras chain links are removed (no unused-variable lint)
- Other extras rows (condition / furnished / negotiable / deposit / prepayment / minTerm) and areaSqm chain link preserved
- DISP-03 sub-clause "canonical specs row only; no duplicate" holds: PropertyDetailsScreen surfaces beds/baths only through plan 04's specs row
- New test file with 6 passing cases lands (assertions on literal i18n keys, matching t-mock-key-verbatim convention)
- Mirrors the 260525-i2i surgical-removal pattern (zero replacement, deletion-only)
</success_criteria>

<output>
After completion, create `.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-05-SUMMARY.md` capturing:
- Exact diff scope: net line count (~14-16 lines removed, zero added)
- Confirmation of all four grep-zero invariants (rooms != null, bathroom != null, const rooms, const bathroom)
- Confirmation of `hasExtras` chain post-edit (lists the 7 remaining contributors)
- New test file path + 6-case list + jest exit code (0)
- Cross-reference to 260525-i2i quick task (commit `15f1010`) as the surgical-removal precedent
- Confirmation that test assertions use literal i18n key strings (`property.beds`, `property.baths`) per the t-mock-key-verbatim convention
- KBD-02 grep count post-edit (must be 0)
- Commit SHA(s)
</output>
