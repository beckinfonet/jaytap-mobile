---
phase: 08-display-surfaces-propertycard-hospitalitycard-propertydetail
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/PropertyCard.tsx
  - src/components/__tests__/PropertyCard.test.tsx
autonomous: true
requirements:
  - DISP-01
  - DISP-02
  - DISP-05
tags:
  - react-native
  - component
  - presentational
  - i18n
must_haves:
  truths:
    - "PropertyCard Beds cell on apartment with basics.bedrooms=2 renders '2' + 'Bedrooms'/'Спальни' (D-01, D-03, DISP-01)"
    - "PropertyCard Beds cell with no bedroom data renders '-' (DISP-01 fallback)"
    - "PropertyCard Beds cell on office or commercial property type renders NOTHING — the View is omitted entirely (D-02)"
    - "PropertyCard Baths cell renders basics.bathroomCount when present (decimals like 1.5 preserved) and '-' when absent (DISP-02)"
    - "PropertyCard Baths cell uses 'Bathrooms'/'Ванные' label (D-04, DISP-02)"
    - "Dead fallback chain basics?.hotelRooms ?? basics?.rooms is REMOVED (D-03)"
    - "Bathroom enum render (property.bathroomPrivate/Shared/None branch) is REMOVED from PropertyCard (D-04)"
    - "New co-located PropertyCard.test.tsx exists alongside existing PropertyCard.specChip.test.tsx (regression fence preserved)"
  artifacts:
    - path: "src/components/PropertyCard.tsx"
      provides: "Redesigned specs-strip branch: icon/value/label per cell; office/commercial Beds-cell hide; bathroomCount-only Baths read; specItem flexDirection mutated to column; specLabel style added"
      contains: "property.specs.bedrooms"
    - path: "src/components/__tests__/PropertyCard.test.tsx"
      provides: "New co-located test asserting Phase-8 anatomy on 5 cases (apartment full, house bedroom-only, office Beds-hidden, commercial Baths-only, apartment empty)"
      contains: "property.specs.bedrooms"
  key_links:
    - from: "src/components/PropertyCard.tsx"
      to: "src/locales/en.ts + src/locales/ru.ts"
      via: "t('property.specs.bedrooms' as any) + t('property.specs.bathrooms' as any) calls"
      pattern: "t\\('property\\.specs\\."
    - from: "src/components/__tests__/PropertyCard.test.tsx"
      to: "src/components/PropertyCard.tsx"
      via: "react-test-renderer + jest.mock pattern from PropertyCard.specChip.test.tsx"
      pattern: "TestRenderer\\.create\\(.*<PropertyCard"
---

<objective>
Redesign PropertyCard's specs-strip branch to render the new icon/value/label cell anatomy that surfaces the M4 `basics.bedrooms` and `basics.bathroomCount` fields with localized labels distinguishing residential ("Bedrooms") from generic ("Bathrooms"). Drop the now-dead `basics?.hotelRooms ?? basics?.rooms` defensive fallback (PropertyCard never sees hospitality data post-260525-ggp routing fix). Replace the bathroom enum branch with a single `bathroomCount` read. Hide the Beds cell entirely on office/commercial property types per D-02. Add a co-located test alongside the existing `PropertyCard.specChip.test.tsx` regression fence.

Purpose: Closes DISP-01 (Beds cell reads bedrooms with Bedrooms label), DISP-02 (Baths cell reads bathroomCount), and contributes to DISP-05 (all new labels routed through `t()`). PropertyCard is the highest-traffic browse surface (Home, Favorites, Owner Listings, Moderation Queue all render via it) so this plan covers ~70% of the user-facing M4 display work.

Output: PropertyCard.tsx specs-strip branch rewritten with `showBedsCell` gate + new cell anatomy + StyleSheet mutation (specItem flexDirection → column, new specLabel style); new PropertyCard.test.tsx with 5 cases; PropertyCard.specChip.test.tsx LEFT UNTOUCHED (historical regression fence for 260525-ggp).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-CONTEXT.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-01-SUMMARY.md
@CLAUDE.md
@src/components/PropertyCard.tsx
@src/screens/PropertyDetailsScreen.tsx
@src/components/__tests__/PropertyCard.specChip.test.tsx
@src/types/Property.ts

<interfaces>
<!-- Key contracts and identifiers for PropertyCard redesign. -->

From src/types/Property.ts (basics subdocument — already extended in Phase 6):
- `basics.bedrooms?: number` (M4 new — residential only)
- `basics.bathroomCount?: number` (M4 new — all 6 property types; 0.5-step)
- `basics.hotelRooms?: number` (M3 — hospitality only; NOT read by PropertyCard per D-03)
- `basics.rooms?: number` (M3 — captured by form for cultural "3-room apartment" semantics; NOT read by PropertyCard per D-03)
- `basics.bathroom?: 'private' | 'shared' | 'none'` (M1 enum — captured on office/commercial; NOT read by PropertyCard per D-04)
- `propertyType: 'apartment' | 'house' | 'hotel' | 'hostel' | 'office' | 'commercial'` (M3 6-type set; top-level, lowercase enum)

From src/components/PropertyCard.tsx (current specs-strip branch — lines 279-300):
- Container: `<View style={[styles.specsContainer, { backgroundColor: colors.chipBackground, borderColor: colors.chipBorder }]}>` — KEEP unchanged
- Bed icon: `<Bed size={16} color={colors.textSecondary} strokeWidth={2} />` (Lucide; already imported)
- Bath icon: `<Bath size={16} color={colors.textSecondary} strokeWidth={2} />` (Lucide; already imported)
- Existing styles (lines 449-476): `specsContainer`, `specItem` (currently `flexDirection: 'row'` — MUST mutate to column per D-01), `specValue`, `specDivider`
- Missing style: `specLabel` (must be ADDED — mirroring PropertyDetailsScreen.tsx:2004-2006)

From src/components/__tests__/PropertyCard.specChip.test.tsx (canonical test stack to clone):
- `react-test-renderer` + `act()` (NO React Testing Library, NO jest-native)
- `jest.mock('../../theme/ThemeContext', () => ({ useTheme: jest.fn() }))`
- `jest.mock('../../context/AuthContext', () => ({ useAuth: jest.fn() }))`
- `jest.mock('../../context/LanguageContext', () => ({ useLanguage: jest.fn() }))`
- `jest.mock('../ListingMetaTable', () => ({ ListingMetaTable: () => null }))`
- `jest.mock('../StatusPill', () => ({ StatusPill: () => null }))`
- `jest.mock('lucide-react-native', () => ({ Heart: () => null, Bed: () => null, Bath: () => null, Pencil: () => null, Trash2: () => null, Archive: () => null, ArchiveRestore: () => null }))`
- `t` mock returns the key verbatim: `(key: string) => key`
- `collectTexts(tree)` helper for text-content assertions (lines 111-124)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite PropertyCard specs-strip branch with new cell anatomy + StyleSheet mutation</name>
  <files>src/components/PropertyCard.tsx</files>
  <read_first>
    - src/components/PropertyCard.tsx (read the WHOLE file in one pass — small at ~530 LOC; extract: the `showEditButton` ternary structure starting around line 215, the current specs-strip branch lines 279-300, the StyleSheet block lines 449-476 with the four existing spec-related styles)
    - src/screens/PropertyDetailsScreen.tsx (read only lines 1042-1080 AND lines 1980-2010 — for the canonical icon/value/label cell anatomy and the existing specLabel style shape: `{ fontSize: 12 }`)
    - src/types/Property.ts (read the full file — small; verify `basics.bedrooms?: number` and `basics.bathroomCount?: number` exist on the basics subdocument per Phase 6 SCHEMA-03)
    - .planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-CONTEXT.md (re-read D-01, D-02, D-03, D-04 sections + the specifics block at lines 211-235)
    - .planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md (re-read the `specItem flexDirection conflict` discussion at lines 131-145 — recommended option is mutation, not parallel style addition)
  </read_first>
  <action>
    Modify ONLY the specs-strip `else`-branch of the `showEditButton` ternary (current lines 279-300 in PropertyCard.tsx) AND the related StyleSheet block (lines 449-476). Do not touch any other section of PropertyCard.tsx.

    Step A — Compute the office/commercial gate at the top of the specs-strip branch (or at component-render top scope, planner's choice — recommend co-locating just above the `<View style={styles.specsContainer}>` line for readability):
    - Identifier: `showBedsCell`
    - Expression: `!(property.propertyType === 'office' || property.propertyType === 'commercial')`
    - Per D-02 rationale: bedrooms is a residential concept; rendering "Bedrooms: -" on commercial is semantically wrong, and an asymmetric one-cell row is the lowest-confusion path.

    Step B — Replace the inner content of `<View style={styles.specsContainer}>` (lines 281-298 today) with two conditional cells + a conditional divider:

    First cell (BEDS — render only when `showBedsCell`):
    - Wrap in `{showBedsCell && (<View style={styles.specItem}>...</View>)}`
    - Children of the cell (in this exact order — icon top, value middle, label bottom per D-01):
      1. `<Bed size={16} color={colors.textSecondary} strokeWidth={2} />` (Lucide import already present at line ~16)
      2. `<Text style={[styles.specValue, { color: colors.text }]}>{property.basics?.bedrooms ?? '-'}</Text>` — SINGLE READ; do NOT use the dead `basics?.hotelRooms ?? basics?.rooms` fallback (D-03 explicitly removes it)
      3. `<Text style={[styles.specLabel, { color: colors.textSecondary }]}>{t('property.specs.bedrooms' as any)}</Text>` (the `as any` cast follows the existing PropertyCard.tsx:291 pattern until TranslationKeys regenerates)

    Divider (render only when `showBedsCell` — paired with the Beds cell):
    - `{showBedsCell && (<View style={[styles.specDivider, { backgroundColor: colors.border }]} />)}`

    Second cell (BATHS — ALWAYS renders, even on office/commercial/land):
    - `<View style={styles.specItem}>...</View>` (no gate)
    - Children:
      1. `<Bath size={16} color={colors.textSecondary} strokeWidth={2} />`
      2. `<Text style={[styles.specValue, { color: colors.text }]}>{property.basics?.bathroomCount ?? '-'}</Text>` — SINGLE READ; the legacy `basics?.bathroom === 'private' ? ... : 'shared' ? ... : 'none' ? ... : '-'` enum branch is REMOVED entirely per D-04
      3. `<Text style={[styles.specLabel, { color: colors.textSecondary }]}>{t('property.specs.bathrooms' as any)}</Text>`

    Decimal formatting: render the bathroomCount number directly through `{...}` — React Native will coerce `1.5` to `"1.5"`, `2` to `"2"`. Do not pre-format. This matches CONTEXT D-04's "decimals like 1.5 preserved" with the simplest path.

    Step C — Mutate `styles.specItem` (currently lines 463-467) AND add a new `styles.specLabel`:

    Existing `specItem` (`flexDirection: 'row', alignItems: 'center', gap: 4`) becomes column-stacked per D-01 (icon top, value middle, label below):
    - `flexDirection: 'column'` (replace `'row'`)
    - `alignItems: 'center'` (unchanged)
    - `gap: 4` (unchanged)

    Add a new `specLabel` style entry to the same StyleSheet block (mirroring PropertyDetailsScreen.tsx:2004-2006):
    - `fontSize: 12`
    - No fontWeight; no color (color is applied inline via the style array per the theme-aware inline-array convention)

    Step D — Boundary anchors (do NOT touch):
    - The `showEditButton ? (...) : (...)` outer ternary structure (line ~215 onward) — Phase 8 only changes the `else` branch.
    - Any owner-action chrome (Edit/Archive/Unarchive/Delete buttons in the `then` branch).
    - The Heart/favorite button.
    - The `<Image source={...} />` hero photo block.
    - The `ListingMetaTable` import or invocation.
    - The Lucide icon imports (Bed + Bath already present — D-03 reads use the same icons).
    - Existing styles other than `specItem` mutation + `specLabel` addition.
    - Existing `t('property.bathroomPrivate' as any)` / `t('property.bathroomShared' as any)` / `t('property.bathroomNone' as any)` keys — the keys stay defined in the locale tables for other surfaces; only the consumption in PropertyCard goes away.

    Per CONTEXT D-03: PropertyCard never sees hospitality data in production after the 260525-ggp routing fix (`propertyCategory.ts:65` Pascal-vs-lowercase normalization). The defensive `basics?.hotelRooms ?? basics?.rooms` fallback is dead code and is DROPPED. Single read: `basics?.bedrooms ?? '-'`.
  </action>
  <verify>
    <automated>
      # Each assertion is a discrete sequential check using set -e so the first failure halts and is visible in the log.
      # Identical KBD-02 grep shape as Plans 03/04/05 (count | tr -d ' ' | grep -q "^0$") — no shortcircuit, no fallthrough.
      set -e
      # --- D-03 dead-code removals ---
      echo "[check] D-03: basics?.hotelRooms removed"
      grep -c "basics\?\.hotelRooms" src/components/PropertyCard.tsx | grep -q "^0$"
      # --- D-04 enum-branch removal ---
      echo "[check] D-04: bathroom === 'private' enum branch removed"
      grep -c "basics\?\.bathroom === 'private'" src/components/PropertyCard.tsx | grep -q "^0$"
      # --- DISP-01 / DISP-02 single reads ---
      echo "[check] DISP-01: basics?.bedrooms single read"
      grep -c "basics\?\.bedrooms" src/components/PropertyCard.tsx | grep -q "^1$"
      echo "[check] DISP-02: basics?.bathroomCount single read"
      grep -c "basics\?\.bathroomCount" src/components/PropertyCard.tsx | grep -q "^1$"
      # --- DISP-05 i18n key consumption ---
      echo "[check] DISP-05: property.specs.bedrooms key present"
      grep -c "property.specs.bedrooms" src/components/PropertyCard.tsx | grep -q "^1$"
      echo "[check] DISP-05: property.specs.bathrooms key present"
      grep -c "property.specs.bathrooms" src/components/PropertyCard.tsx | grep -q "^1$"
      # --- D-02 office/commercial gate ---
      echo "[check] D-02: showBedsCell gate identifier present (declaration + 2 usages = 3 occurrences expected)"
      grep -c "showBedsCell" src/components/PropertyCard.tsx | grep -qE "^[2-9]$"
      # --- D-01 StyleSheet mutation + specLabel addition ---
      echo "[check] D-01: specLabel style added"
      grep -c "specLabel" src/components/PropertyCard.tsx | grep -qE "^[3-9]$"
      echo "[check] D-01: specItem flexDirection mutated to column"
      grep -E "specItem:" -A 4 src/components/PropertyCard.tsx | grep -q "flexDirection: 'column'"
      # --- i18n parity ---
      echo "[check] i18n parity holds"
      bash scripts/check-i18n-parity.sh
      # --- KBD-02 invariant (project-banned identifier — same shape as Plans 03/04/05) ---
      echo "[check] KBD-02: keyboardVerticalOffset count is 0 across src/"
      grep -rn "keyboardVerticalOffset" src/ | wc -l | tr -d ' ' | grep -q "^0$"
      echo "[ok] all checks passed"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "basics?.hotelRooms" src/components/PropertyCard.tsx` returns 0 (D-03 dead-code removal verified)
    - `grep -c "basics?.bathroom === 'private'" src/components/PropertyCard.tsx` returns 0 (D-04 enum-branch removal verified)
    - `grep -c "basics?.bedrooms" src/components/PropertyCard.tsx` returns exactly 1 (single read in the Beds cell)
    - `grep -c "basics?.bathroomCount" src/components/PropertyCard.tsx` returns exactly 1 (single read in the Baths cell)
    - `grep -c "property.specs.bedrooms" src/components/PropertyCard.tsx` returns exactly 1
    - `grep -c "property.specs.bathrooms" src/components/PropertyCard.tsx` returns exactly 1
    - `grep -c "showBedsCell" src/components/PropertyCard.tsx` returns 3 or more (declaration + Beds-cell gate + divider gate)
    - `styles.specItem` block contains `flexDirection: 'column'` (mutation applied)
    - `styles.specLabel` is defined with at least `fontSize: 12`
    - `bash scripts/check-i18n-parity.sh` exit 0 (no parity regression)
    - `grep -rn "keyboardVerticalOffset" src/ | wc -l | tr -d ' ' | grep -q "^0$"` succeeds (M1 KBD-02 grep gate held across 3 milestones — must not regress; same shape as Plans 03/04/05; project-banned per memory m1-keyboard-kbd-02-invariants.md and CLAUDE.md)
    - `npx tsc --noEmit` produces zero net-new errors vs the pre-task baseline
    - Visual contract preserved: `<View style={styles.specsContainer}>` outer container present (rounded-pill chrome unchanged); `<Bed>` + `<Bath>` Lucide icons preserved at `size={16}`
  </acceptance_criteria>
  <done>PropertyCard specs-strip branch surfaces basics.bedrooms + basics.bathroomCount through icon/value/label anatomy with Bedrooms/Bathrooms labels; Beds cell hidden on office/commercial; specItem column-direction; dead fallback chain and bathroom enum branch removed; KBD-02 invariant held.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create new co-located PropertyCard.test.tsx with 5 Phase-8 anatomy cases</name>
  <files>src/components/__tests__/PropertyCard.test.tsx</files>
  <read_first>
    - src/components/__tests__/PropertyCard.specChip.test.tsx (read the FULL existing file — small; clone the test stack: imports, jest.mock block, mock setup helpers, `collectTexts(tree)` helper at lines 111-124, render helper, theme/auth/language mock return-shapes)
    - src/components/__tests__/StepperInput.test.tsx (read header lines 1-40 — canonical mock-pattern seed for theme/language hooks)
    - src/components/PropertyCard.tsx (read the post-Task-1 file as committed — verify the specs-strip branch matches the contracts asserted below)
    - src/types/Property.ts (read fully — small; use the real type for the `Property` fixture casts; do NOT redefine the type in the test)
    - .planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md (re-read the Cases to cover block at lines 499-505 — confirms the 5 cases)
  </read_first>
  <behavior>
    - Test 1 (apartment full): `propertyType: 'apartment'` + `basics: { bedrooms: 2, bathroomCount: 1.5 }` → collectTexts contains `'2'`, `'1.5'`, `'property.specs.bedrooms'`, `'property.specs.bathrooms'`. The `'2'` and `'property.specs.bedrooms'` co-occur in the same render tree (Beds cell renders).
    - Test 2 (house bedroom-only): `propertyType: 'house'` + `basics: { bedrooms: 3 }` (no bathroomCount) → collectTexts contains `'3'`, `'-'`, `'property.specs.bedrooms'`, `'property.specs.bathrooms'`. The `'-'` appears because bathroomCount is undefined.
    - Test 3 (office Beds-hidden): `propertyType: 'office'` + `basics: { bathroomCount: 1 }` (no bedrooms) → collectTexts does NOT contain `'property.specs.bedrooms'` (Beds cell omitted entirely per D-02). collectTexts DOES contain `'1'` and `'property.specs.bathrooms'`. Asserts the View-omission pattern (not visibility:'hidden').
    - Test 4 (commercial Beds-hidden, integer baths): `propertyType: 'commercial'` + `basics: { bathroomCount: 2 }` → collectTexts does NOT contain `'property.specs.bedrooms'`. collectTexts DOES contain `'2'` and `'property.specs.bathrooms'`.
    - Test 5 (apartment empty basics): `propertyType: 'apartment'` + `basics: {}` (both undefined) → collectTexts contains `'property.specs.bedrooms'`, `'property.specs.bathrooms'`, and at least TWO occurrences of `'-'` (one per cell). Labels still present even when values absent.
  </behavior>
  <action>
    Create `src/components/__tests__/PropertyCard.test.tsx` as a NEW file (alongside the existing `PropertyCard.specChip.test.tsx` — do NOT modify or replace specChip; it is the historical 260525-ggp regression fence and must remain intact per CONTEXT.md L26 + the planner's option (b) recommendation).

    File structure (mirror PropertyCard.specChip.test.tsx for everything except the test cases):
    1. Imports: `React`, `TestRenderer` + `act` from `react-test-renderer`, `Text` from `react-native`, `PropertyCard` from `'../PropertyCard'`, `type Property` from `'../../types/Property'`.
    2. jest.mock block (clone verbatim from specChip): theme/auth/language contexts; ListingMetaTable; StatusPill; lucide-react-native icon set (Heart, Bed, Bath, Pencil, Trash2, Archive, ArchiveRestore — all return `() => null`).
    3. Mock-setup helper that re-requires the mocked modules and configures: `useTheme()` to return a `colors` shape with the keys the component reads (`text`, `textSecondary`, `chipBackground`, `chipBorder`, `border`, plus the few others PropertyCard reads — copy from specChip's setup); `useAuth()` to return `{ user: null }`; `useLanguage()` to return `{ t: (key: string) => key, locale: 'en' }` so assertions can match the i18n key string directly.
    4. `collectTexts(tree)` helper — clone verbatim from `PropertyCard.specChip.test.tsx:111-124`.
    5. Render helper `renderCard(propertyOverrides: Partial<Property>)` — clone from specChip's pattern; merge over a baseline `Property` fixture.
    6. Five `test(...)` cases per the behavior block above.

    Property fixture baseline (a minimal valid Property — use the canonical shape that PropertyCard accepts; copy the baseline from specChip's `makeBaseProperty()` helper if it exists or construct minimally with required fields). Use TypeScript `as Property` casts where convenient. The fixture must include enough top-level fields that PropertyCard renders without throwing (id, propertyType, title, price, location, photos array, etc. — copy the minimal baseline from specChip's existing test).

    For each test: render with `act(() => { TestRenderer.create(<PropertyCard property={fixture} ... />) })`, extract texts via `collectTexts(tree)`, assert presence/absence via `expect(texts).toContain(...)` / `expect(texts).not.toContain(...)`.

    Do NOT use `await` for renders (sync render via `act` is the existing pattern). Do NOT use `@testing-library/*`.

    Pass the `onPress` (and any other required PropertyCard props) as `jest.fn()` no-ops.
  </action>
  <verify>
    <automated>npx jest src/components/__tests__/PropertyCard.test.tsx --no-coverage 2>&1 | grep -E "Tests:.*5 passed|5 passed, 5 total"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/__tests__/PropertyCard.test.tsx` exists
    - `src/components/__tests__/PropertyCard.specChip.test.tsx` is UNTOUCHED (git diff scope: zero changes to specChip)
    - Test suite contains exactly 5 `test(...)` or `it(...)` cases matching the behavior block
    - All 5 cases pass: `npx jest src/components/__tests__/PropertyCard.test.tsx --no-coverage` exits 0 with "5 passed" in the Tests line
    - Test stack matches repo convention: `react-test-renderer` + `act` ONLY; no `@testing-library/*` imports; no `jest-native` matchers
    - `t` mock returns key verbatim — assertions check for the literal key string `'property.specs.bedrooms'`, NOT the EN/RU value
    - Test 3 + Test 4 explicitly assert ABSENCE of `'property.specs.bedrooms'` from `collectTexts` output (proving View omission, not hidden render)
    - `npx tsc --noEmit` zero net-new errors
    - Existing specChip suite still passes: `npx jest src/components/__tests__/PropertyCard.specChip.test.tsx --no-coverage` exits 0 (regression fence preserved)
  </acceptance_criteria>
  <done>New PropertyCard.test.tsx with 5 cases passes; specChip suite untouched and still passes; test stack matches repo conventions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 8 is read-side rendering only; no new attack surface introduced. PropertyCard receives Property data from parent screen (already through trust boundary at API fetch). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | src/components/PropertyCard.tsx (basics.bedrooms / basics.bathroomCount render) | accept | No PII; counts are public listing attributes already returned by the public `GET /api/properties` endpoint. RN Text components escape children by default — XSS via property data N/A. |
| T-08-02-02 | Tampering | src/components/__tests__/PropertyCard.test.tsx (key-verbatim t mock) | accept | Test-only mock; not bundled into production app. Static key strings; no runtime evaluation. |

Verdict: no new threats. ASVS L1 not applicable to read-only presentational components.
</threat_model>

<verification>
- `grep -c "property.specs.bedrooms\|property.specs.bathrooms" src/components/PropertyCard.tsx` returns at least 2
- `grep -c "basics?.hotelRooms\|basics?.bathroom === " src/components/PropertyCard.tsx` returns 0 (dead-code removal proven)
- `npx jest src/components/__tests__/PropertyCard.test.tsx --no-coverage` exits 0 with 5 passed
- `npx jest src/components/__tests__/PropertyCard.specChip.test.tsx --no-coverage` exits 0 (regression fence)
- `bash scripts/check-i18n-parity.sh` exits 0
- `grep -rn "keyboardVerticalOffset" src/ | wc -l | tr -d ' ' | grep -q "^0$"` succeeds (KBD-02 invariant)
- `npx tsc --noEmit` zero net-new errors
</verification>

<success_criteria>
- PropertyCard's specs-strip branch reads ONLY `basics.bedrooms` and `basics.bathroomCount` from the basics subdocument
- Beds cell omits entirely on `propertyType === 'office'` and `propertyType === 'commercial'`
- Both cells render localized labels via `t('property.specs.bedrooms' as any)` and `t('property.specs.bathrooms' as any)`
- StyleSheet: `specItem.flexDirection === 'column'` and `specLabel.fontSize === 12` (new style added)
- New co-located test file with 5 passing cases lands alongside the preserved specChip regression fence
- No KBD-02 regression; no i18n parity regression; no new tsc errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-02-SUMMARY.md` capturing:
- Exact diff scope: PropertyCard.tsx specs-strip branch + StyleSheet block (with line counts)
- Confirmation of D-03 dead-code removal (grep counts for `hotelRooms` and `bathroom === 'private'`)
- New test file path + 5-case list + jest exit code (0)
- Confirmation specChip.test.tsx UNTOUCHED (git diff line-count = 0 for that file)
- KBD-02 grep count post-edit (must be 0)
- Commit SHA(s)
</output>
