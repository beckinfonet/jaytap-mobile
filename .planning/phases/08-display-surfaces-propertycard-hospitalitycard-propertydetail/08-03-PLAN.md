---
phase: 08-display-surfaces-propertycard-hospitalitycard-propertydetail
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/HospitalityCard.tsx
  - src/components/__tests__/HospitalityCard.test.tsx
autonomous: true
requirements:
  - DISP-04
  - DISP-05
tags:
  - react-native
  - component
  - hospitality
  - i18n
must_haves:
  truths:
    - "HospitalityCard meta line renders hotelRooms + Rooms label unchanged (M1 Phase 6 D-10 tour-first invariant preserved)"
    - "HospitalityCard meta line adds inline `· {bathroomCount} Bathrooms` fragment between Rooms and Max Guests when basics.bathroomCount is defined (D-06)"
    - "When basics.bathroomCount is undefined, the fragment renders NOTHING — no orphan separator, no '-' placeholder (D-06)"
    - "Fragment uses the existing `hospitality.bathrooms` key (already shipped at en.ts:281 / ru.ts:283 — no locale add)"
    - "numberOfLines={1} clamp on the meta Text remains in place (RU/EN truncation handling preserved)"
    - "New co-located HospitalityCard.test.tsx exists with conditional-fragment cases"
  artifacts:
    - path: "src/components/HospitalityCard.tsx"
      provides: "Meta-line children expression extended with conditional bathroomCount fragment; zero StyleSheet changes"
      contains: "basics?.bathroomCount"
    - path: "src/components/__tests__/HospitalityCard.test.tsx"
      provides: "New co-located test asserting D-06 fragment present/absent cases + D-07 sentence-case rendering"
      contains: "hospitality.bathrooms"
  key_links:
    - from: "src/components/HospitalityCard.tsx"
      to: "src/locales/en.ts + src/locales/ru.ts"
      via: "t('hospitality.bathrooms') call (existing key — no add)"
      pattern: "t\\('hospitality\\.bathrooms'\\)"
---

<objective>
Extend HospitalityCard's single meta-line render with a conditional inline `· {bathroomCount} Bathrooms` fragment positioned between `Rooms` and `Max Guests`, surfacing the M4 `basics.bathroomCount` field on the hospitality browse surface. The fragment must respect the M1 Phase 6 D-10 invariant (tour-first, price-free, NO specs-strip block, NO second meta row) — a strict in-place children-expression edit, zero structural change. Add a co-located test (NEW file — none existed pre-Phase-8) covering both fragment-present and fragment-absent cases plus the orphan-separator regression fence.

Purpose: Closes DISP-04 (HospitalityCard adds bathroomCount line when present); contributes to DISP-05 (label routed through `t()`).

Output: Single-line edit inside the existing `<Text style={styles.meta}>` element; new HospitalityCard.test.tsx with 3+ cases.

LOAD-BEARING REUSE: `hospitality.bathrooms` key already exists (en.ts:281 / ru.ts:283 → `"Bathrooms"` / `"Ванные"`). This plan ADDS NO locale keys; it just consumes the existing one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-CONTEXT.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md
@.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-01-SUMMARY.md
@CLAUDE.md
@src/components/HospitalityCard.tsx
@src/components/__tests__/PropertyCard.specChip.test.tsx
@src/types/Property.ts

<interfaces>
<!-- Existing meta-line target and key reuse contracts. -->

From src/components/HospitalityCard.tsx (current meta line at lines 206-212):
```
<Text style={[styles.meta, { color: colors.textSecondary }]} numberOfLines={1}>
  {(property.basics?.hotelRooms ?? property.basics?.rooms ?? '-')} {t('hospitality.rooms')} · {property.maxGuests ?? 0}{' '}
  {t('hospitality.maxGuests')}
</Text>
```

From src/locales/en.ts:281 and src/locales/ru.ts:283 (already shipped — REUSE, do not re-add):
- `'hospitality.bathrooms': 'Bathrooms'` (EN)
- `'hospitality.bathrooms': 'Ванные'` (RU)

From src/types/Property.ts:
- `basics.bathroomCount?: number` (M4 SCHEMA-02; 0.5-step; may be decimal like 1.5)
- `basics.hotelRooms?: number` (M3; kept as-is — D-03 does NOT remove the existing hotelRooms read on HospitalityCard, only on PropertyCard)

From M1 Phase 6 D-10 invariant (load-bearing):
"Tour-first, price-free, no meta table" — HospitalityCard renders a single inline meta line, NO specs-strip block. Phase 8 D-06 explicitly preserves this verbatim — inline fragment only, no structural change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert conditional bathroomCount fragment into HospitalityCard meta line</name>
  <files>src/components/HospitalityCard.tsx</files>
  <sequential_gate>
    Task 2 (the jest suite below with the `countSeparators` helper) is the LOAD-BEARING GATE for the D-06 separator-orphan invariant. Task 1's grep verify alone CANNOT prove the orphan-separator regression fence — the grep only asserts the conditional gate's identifier exists; it does NOT prove the rendered output omits the leading `' · '` when bathroomCount is undefined.

    Executor sequence (binding):
    1. Run Task 1 first. Its verify must pass (grep + parity + KBD-02).
    2. THEN run Task 2. Task 2 must pass before this plan is considered done.
    3. If Task 2 fails for ANY reason (TS error, missing mock, async/render exception, assertion mismatch), DO NOT mark this plan complete on Task 1's grep gate alone. Fix Task 2 (or escalate per the executor-escalation pattern in Plan 04 Task 2) before declaring plan-done.
    4. Plan-done = Task 1 verify PASS && Task 2 verify PASS && both files committed.

    Rationale: the lesson from memory `gsd-verifier-misses-regressions` (M2 Phase 1) — grep-only gates miss render-level regressions. D-06 separator-orphan is render-level by definition; only the jest suite catches it.
  </sequential_gate>
  <read_first>
    - src/components/HospitalityCard.tsx (read the FULL file — ~280 LOC; locate the meta-line `<Text>` at lines 206-212 and confirm the existing children expression matches the interfaces block above)
    - src/locales/en.ts (Grep the line containing `'hospitality.bathrooms'` to confirm it exists and value is `'Bathrooms'`)
    - src/locales/ru.ts (Grep the line containing `'hospitality.bathrooms'` to confirm value is `'Ванные'`)
    - .planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-CONTEXT.md (re-read D-06 and D-07 in the `<decisions>` block + the HospitalityCard target pattern at lines 238-246 in `<specifics>`)
    - .planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md (re-read the HospitalityCard section at lines 174-204 — confirms zero structural change + no StyleSheet additions + the React.Fragment `<>...</>` pattern for the conditional)
  </read_first>
  <action>
    Modify ONLY the children expression inside the existing `<Text style={styles.meta}>` element at lines 206-212 of HospitalityCard.tsx. Do NOT add any new JSX element, do NOT change the styles.meta selector, do NOT touch any other section of the file.

    Replace the current children expression (which renders `{rooms} {t('hospitality.rooms')} · {maxGuests} {t('hospitality.maxGuests')}`) with the following children expression (preserving the leading `(property.basics?.hotelRooms ?? property.basics?.rooms ?? '-')` read — D-03 does NOT remove this fallback on HospitalityCard, only on PropertyCard):

    New children (in this exact order — split across lines for readability):
    1. `{(property.basics?.hotelRooms ?? property.basics?.rooms ?? '-')}` — unchanged read
    2. `{' '}{t('hospitality.rooms')}` — unchanged label
    3. Conditional fragment: `{property.basics?.bathroomCount !== undefined ? (<>{' · '}{property.basics.bathroomCount} {t('hospitality.bathrooms')}</>) : null}` — D-06 critical invariant: when `bathroomCount` is `undefined`, the React Fragment returns `null` so NO orphan ` · ` separator appears
    4. `{' · '}{property.maxGuests ?? 0} {t('hospitality.maxGuests')}` — unchanged trailing segment

    Invariants:
    - Use `!== undefined` (NOT truthiness like `bathroomCount &&`) so the value `0` still renders the fragment — D-04 says every Baths cell reads `bathroomCount` and `0` is a valid count (e.g., a hotel room with zero in-unit bathrooms / shared bathroom-down-the-hall scenario).
    - The separator BEFORE the fragment (`' · '`) lives INSIDE the conditional, paired with the fragment. The separator AFTER the fragment lives in the trailing segment (already part of the existing string between `hospitality.rooms`/`hospitality.bathrooms` and `maxGuests`).
    - React Fragment shorthand `<>...</>` is the canonical pattern for the in-line conditional per PATTERNS.md line 192.
    - Sentence-case parity (D-07): the EN value `"Bathrooms"` keeps the same capitalization as `"Rooms"` and `"Max Guests"` — no compact-lowercase form (rejected during /gsd-discuss-phase).
    - `numberOfLines={1}` on the wrapping Text remains in place (RU/EN truncation handling).
    - Decimal formatting: render `{property.basics.bathroomCount}` directly; React Native coerces `1.5` → `"1.5"`, `2` → `"2"`. No pre-format helper needed (matches CONTEXT D-04 / Phase 7 D-04 decimal-style inheritance).

    Reminder: Task 1's grep verify proves the gate IDENTIFIER exists; the D-06 separator-orphan render invariant is proven by Task 2's `countSeparators` helper. Do not declare this plan done on Task 1 alone — see `<sequential_gate>` above.

    Boundary anchors (do NOT touch):
    - styles.meta (no StyleSheet edits — D-06 explicit: "No StyleSheet additions")
    - The wrapping `<Text>` element itself (no new style array, no new prop)
    - Any other render block in HospitalityCard.tsx (hero photo, tour-first chrome, amenities row, status pill)
    - The M1 Phase 6 D-10 "tour-first, price-free, no meta table" invariant
    - Imports (no new imports needed; `t` and `useLanguage` already in scope)
  </action>
  <verify>
    <automated>grep -c "basics?.bathroomCount !== undefined" src/components/HospitalityCard.tsx | grep -q "^1$" && grep -c "t('hospitality.bathrooms')" src/components/HospitalityCard.tsx | grep -q "^1$" && grep -c "numberOfLines={1}" src/components/HospitalityCard.tsx | grep -qE "^[1-9][0-9]*$" && grep -c "t('hospitality.rooms')" src/components/HospitalityCard.tsx | grep -q "^1$" && grep -c "t('hospitality.maxGuests')" src/components/HospitalityCard.tsx | grep -q "^1$" && grep -c "styles.specsContainer" src/components/HospitalityCard.tsx | grep -q "^0$" && bash scripts/check-i18n-parity.sh && grep -rn "keyboardVerticalOffset" src/ | wc -l | tr -d ' ' | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "basics?.bathroomCount !== undefined" src/components/HospitalityCard.tsx` returns exactly 1 (the conditional gate present — IDENTIFIER check only; render-level orphan-separator proof is Task 2's `countSeparators` helper)
    - `grep -c "t('hospitality.bathrooms')" src/components/HospitalityCard.tsx` returns exactly 1 (fragment consumes existing key)
    - `grep -c "t('hospitality.rooms')" src/components/HospitalityCard.tsx` returns exactly 1 (existing read unchanged)
    - `grep -c "t('hospitality.maxGuests')" src/components/HospitalityCard.tsx` returns exactly 1 (existing read unchanged)
    - `grep -c "styles.specsContainer" src/components/HospitalityCard.tsx` returns 0 (M1 Phase 6 D-10 "no specs-strip block" invariant — must NOT have introduced one)
    - The meta line `<Text>` element still carries `numberOfLines={1}`
    - The `<>...</>` Fragment shorthand pattern is used (NOT a wrapping `<View>` or `<Text>`)
    - `bash scripts/check-i18n-parity.sh` exit 0
    - `grep -rn "keyboardVerticalOffset" src/ | wc -l | tr -d ' ' | grep -q "^0$"` succeeds (KBD-02 invariant held)
    - `npx tsc --noEmit` zero net-new errors
    - Git diff scope: ONLY the meta-line children expression touched (5-15 lines max); zero StyleSheet changes
    - SEQUENTIAL GATE: Task 2 must also pass before this plan is considered done (see `<sequential_gate>` above). Task 1 grep verify alone is NOT sufficient — the D-06 separator-orphan invariant is render-level and only proven by Task 2's `countSeparators` helper.
  </acceptance_criteria>
  <done>HospitalityCard meta line surfaces bathroomCount inline with `Bathrooms` label when present, hides cleanly when absent; M1 Phase 6 D-10 invariants preserved; zero StyleSheet churn; Task 2 separator-count assertion also passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create new co-located HospitalityCard.test.tsx covering D-06 fragment present/absent</name>
  <files>src/components/__tests__/HospitalityCard.test.tsx</files>
  <read_first>
    - src/components/__tests__/PropertyCard.specChip.test.tsx (read FULL — clone the test stack: react-test-renderer + act, jest.mock pattern, collectTexts helper at lines 111-124, useTheme/useLanguage mock shapes)
    - src/components/HospitalityCard.tsx (read the post-Task-1 file — small ~280 LOC; verify the meta-line children expression matches the contracts below; inventory imports to know which modules need jest.mock entries — specifically the lucide-react-native icon set and any utility imports like `hospitalityAmenities`)
    - src/types/Property.ts (small; use the real type for fixtures)
    - .planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-PATTERNS.md (re-read HospitalityCard test section lines 511-538 — confirms mocks needed and 3 cases)
  </read_first>
  <behavior>
    - Test 1 (fragment present, all fields populated): hotel + `basics: { hotelRooms: 4, bathroomCount: 1.5 }` + `maxGuests: 8` → collectTexts contains `'4'`, `'1.5'`, `'8'`, `'hospitality.rooms'`, `'hospitality.bathrooms'`, `'hospitality.maxGuests'`. Separator dot count: exactly 2 occurrences of `' · '` in the rendered text stream (one between rooms/bathrooms, one between bathrooms/maxGuests).
    - Test 2 (fragment absent — no bathroomCount): hostel + `basics: { hotelRooms: 1 }` + `maxGuests: 2` → collectTexts contains `'1'`, `'2'`, `'hospitality.rooms'`, `'hospitality.maxGuests'`. collectTexts does NOT contain `'hospitality.bathrooms'` (fragment omitted entirely). Separator dot count: exactly 1 occurrence of `' · '` (only between rooms/maxGuests — D-06 orphan-separator regression fence).
    - Test 3 (empty basics — fallback render): hotel + `basics: {}` + no maxGuests → collectTexts contains `'-'` (rooms fallback), `'0'` (maxGuests default per existing line `?? 0`), `'hospitality.rooms'`, `'hospitality.maxGuests'`. Does NOT contain `'hospitality.bathrooms'`. Separator dot count: 1.
    - Test 4 (bathroomCount === 0, edge case — integer zero IS defined): hotel + `basics: { hotelRooms: 5, bathroomCount: 0 }` + `maxGuests: 10` → collectTexts contains `'0'` (the bathroomCount value), `'hospitality.bathrooms'`, AND also contains `'10'` (maxGuests). Fragment renders because `0 !== undefined` is true. Separator dot count: 2. Asserts the `!== undefined` check (NOT truthiness).
  </behavior>
  <action>
    Create `src/components/__tests__/HospitalityCard.test.tsx` as a NEW file (PATTERNS.md confirms no existing HospitalityCard test in the repo).

    File structure:
    1. Imports: `React`, `TestRenderer` + `act` from `react-test-renderer`, `Text` from `react-native`, `HospitalityCard` from `'../HospitalityCard'`, `type Property` from `'../../types/Property'`.
    2. jest.mock block:
       - `jest.mock('../../theme/ThemeContext', () => ({ useTheme: jest.fn() }))`
       - `jest.mock('../../context/LanguageContext', () => ({ useLanguage: jest.fn() }))`
       - `jest.mock('lucide-react-native', () => ({ Heart: () => null, Pencil: () => null, Trash2: () => null, Archive: () => null, ArchiveRestore: () => null }))` (and any other icons HospitalityCard imports — read the file to confirm; add Camera/Image/Star/etc. as needed, all returning `() => null`)
       - `jest.mock('../../utils/hospitalityAmenities', () => ({ AMENITY_ICONS: new Proxy({}, { get: () => () => null }) }))` — IF the file imports this utility (verify via read)
       - Mock any other heavy subcomponents to `() => null` (PATTERNS.md line 515-530 lists the canonical set; planner inspects HospitalityCard.tsx imports and replicates)
    3. Mock-setup helper: configure `useTheme()` to return `{ colors: { text: '#000', textSecondary: '#666', surface: '#fff', border: '#ccc', chipBackground: '#eee', chipBorder: '#ddd' /* + any other tokens HospitalityCard reads */ } }`; configure `useLanguage()` to return `{ t: (key: string) => key, locale: 'en' }`.
    4. `collectTexts(tree)` helper — clone verbatim from PropertyCard.specChip.test.tsx:111-124.
    5. Helper to count separator occurrences in `collectTexts` output: `const countSeparators = (texts: string[]) => texts.filter(t => t === ' · ').length` — used for the orphan-separator assertions. This helper is the LOAD-BEARING gate for D-06 (see Task 1's `<sequential_gate>` block).
    6. Render helper `renderCard(overrides: Partial<Property>)` building a minimal Property fixture with hospitality propertyType + the override fields, then `TestRenderer.create(<HospitalityCard property={fixture} ... />)` inside `act`.
    7. Four `test(...)` cases per the behavior block.

    Critical: The `' · '` separator-counting assertion is the D-06 regression fence. When `bathroomCount` is undefined, the children expression must NOT emit two ` · ` separators (which would surface as `Rooms · · Max Guests` visually). Counting separator occurrences in the rendered Text tree is the deterministic way to prove this — and the ONLY way (Task 1's grep cannot see render output).

    Property fixture defaults: `propertyType: 'hotel'`, `id`, `title`, `price`, `location`, `photos: []` (or whatever minimal shape HospitalityCard accepts without throwing — read the file to confirm required props).

    Pass `onPress` and any other required HospitalityCard props as `jest.fn()` no-ops.
  </action>
  <verify>
    <automated>npx jest src/components/__tests__/HospitalityCard.test.tsx --no-coverage 2>&1 | grep -E "Tests:.*4 passed|4 passed, 4 total"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/__tests__/HospitalityCard.test.tsx` exists
    - Test suite contains exactly 4 `test(...)` or `it(...)` cases matching the behavior block
    - All 4 cases pass: `npx jest src/components/__tests__/HospitalityCard.test.tsx --no-coverage` exits 0 with "4 passed"
    - Test 2 explicitly asserts separator-count is exactly 1 (orphan-separator regression fence — the load-bearing D-06 gate)
    - Test 4 explicitly asserts the fragment renders when `bathroomCount === 0` (proves `!== undefined` not truthiness)
    - Test stack matches repo convention: `react-test-renderer` + `act` ONLY; no `@testing-library/*`; no `jest-native`
    - `t` mock returns key verbatim — assertions check for the literal key string `'hospitality.bathrooms'`
    - `npx tsc --noEmit` zero net-new errors
  </acceptance_criteria>
  <done>New HospitalityCard.test.tsx with 4 cases passes; orphan-separator regression fence in place; test stack matches repo conventions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 8 is read-side rendering only; no new attack surface introduced. HospitalityCard receives Property data from parent screen (already through trust boundary at API fetch). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Information Disclosure | src/components/HospitalityCard.tsx (basics.bathroomCount render) | accept | No PII; counts are public listing attributes already returned by the public GET /api/properties endpoint. RN Text components escape children by default — XSS via property data N/A. |
| T-08-03-02 | Tampering | src/components/__tests__/HospitalityCard.test.tsx | accept | Test-only mock; not bundled into production app. Static key strings; no runtime evaluation. |

Verdict: no new threats. ASVS L1 not applicable to read-only presentational components.
</threat_model>

<verification>
- `grep -c "t('hospitality.bathrooms')" src/components/HospitalityCard.tsx` returns exactly 1
- `grep -c "basics?.bathroomCount !== undefined" src/components/HospitalityCard.tsx` returns exactly 1
- `grep -c "styles.specsContainer" src/components/HospitalityCard.tsx` returns 0 (no specs-strip block introduced)
- `npx jest src/components/__tests__/HospitalityCard.test.tsx --no-coverage` exits 0 with 4 passed (LOAD-BEARING D-06 gate; not optional)
- `bash scripts/check-i18n-parity.sh` exit 0 (no new keys; existing parity preserved)
- `grep -rn "keyboardVerticalOffset" src/ | wc -l | tr -d ' ' | grep -q "^0$"` succeeds (KBD-02 invariant)
- `npx tsc --noEmit` zero net-new errors
</verification>

<success_criteria>
- HospitalityCard meta line surfaces `bathroomCount` inline as `· {n} Bathrooms` between Rooms and Max Guests when defined; renders nothing extra when undefined; separator orphan invariant proven by test
- Existing reads (`hotelRooms`, `rooms` fallback, `maxGuests`, `Rooms`/`Max Guests` labels) preserved verbatim
- No StyleSheet additions; no new JSX elements added
- New test file with 4 cases passes
- M1 Phase 6 D-10 "no specs-strip block" invariant held
</success_criteria>

<output>
After completion, create `.planning/phases/08-display-surfaces-propertycard-hospitalitycard-propertydetail/08-03-SUMMARY.md` capturing:
- Exact diff scope on HospitalityCard.tsx (line count; should be ~5-15 lines added inside the existing Text element; zero deletions of structural elements)
- Confirmation that the `hospitality.bathrooms` key was NOT re-added to locale files (PATTERNS.md pre-flight correction honored)
- New test file path + 4-case list + jest exit code (0)
- Confirmation no styles.specsContainer was introduced (regression fence for M1 Phase 6 D-10)
- Confirmation Task 1 + Task 2 both passed (sequential gate honored per Task 1's `<sequential_gate>` block)
- KBD-02 grep count post-edit (must be 0)
- Commit SHA(s)
</output>
