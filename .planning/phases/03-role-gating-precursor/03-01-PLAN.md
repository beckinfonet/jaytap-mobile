---
phase: 03-role-gating-precursor
plan: 01
plan_id: 03-01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/hooks/__tests__/useRole.test.ts
  - src/services/__tests__/PropertyService.test.ts
  - src/components/__tests__/Gated.test.tsx
  - .planning/codebase/CONVENTIONS.md
autonomous: true
requirements:
  - GATE-01
  - GATE-04
requirements_addressed:
  - GATE-01
  - GATE-04
tags:
  - test-scaffolding
  - conventions
user_setup: []

must_haves:
  truths:
    - "Test files exist with at least one failing assertion each (RED phase of TDD)"
    - "`src/hooks/` directory convention is documented in .planning/codebase/CONVENTIONS.md"
    - "Jest can discover and attempt to run the new test files (no 'no tests found' error)"
  artifacts:
    - path: "src/hooks/__tests__/useRole.test.ts"
      provides: "Failing test stubs for canFromUser priority ladder + allowlist case-insensitivity"
      contains: "describe('canFromUser priority ladder'"
    - path: "src/services/__tests__/PropertyService.test.ts"
      provides: "Failing test stub for patchPlatformVerifications permission-denied path"
      contains: "describe('PropertyService.patchPlatformVerifications guard"
    - path: "src/components/__tests__/Gated.test.tsx"
      provides: "Failing test stub for Gated render logic"
      contains: "describe('Gated"
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "One-liner documenting src/hooks/ as canonical React hooks location"
      contains: "src/hooks/"
  key_links:
    - from: "src/hooks/__tests__/useRole.test.ts"
      to: "src/hooks/useRole.ts"
      via: "import { canFromUser } from '../useRole'"
      pattern: "from '../useRole'"
    - from: "src/services/__tests__/PropertyService.test.ts"
      to: "src/services/PropertyService.ts"
      via: "import { PropertyService } from '../PropertyService'"
      pattern: "from '../PropertyService'"
---

<objective>
Wave 0 preconditions for Phase 3 Role Gating Precursor. Scaffold the three test files referenced by VALIDATION.md with failing stubs (RED phase — Nyquist Dim 8 compliance), and document the new `src/hooks/` directory convention.

Purpose: Every downstream task in Waves 1-3 has an automated verify command that points to one of these test files. Without scaffolding, downstream tasks cite "tests that don't exist yet." Per VALIDATION.md §Wave 0 Requirements, all four ❌ W0 entries must be scaffolded before Wave 1 executes.

Output: Three new `.test.ts(x)` files in their co-located `__tests__/` directories, each with at least one failing test that references the yet-to-be-created symbols. One-line CONVENTIONS.md update documenting `src/hooks/` as the canonical location for React hooks (per CONTEXT code_context line 171).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-role-gating-precursor/03-CONTEXT.md
@.planning/phases/03-role-gating-precursor/03-RESEARCH.md
@.planning/phases/03-role-gating-precursor/03-PATTERNS.md
@.planning/phases/03-role-gating-precursor/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Existing test file for reference (the ONLY existing test in the repo) -->
<!-- __tests__/App.test.tsx, lines 1-13: -->

```typescript
/**
 * @format
 */

import React from 'react';
import ReactTestRenderer from 'react-test-renderer';
import App from '../App';

test('renders correctly', async () => {
  await ReactTestRenderer.act(() => {
    ReactTestRenderer.create(<App />);
  });
});
```

<!-- jest.config.js contents: -->
```javascript
module.exports = {
  preset: 'react-native',
};
```

<!-- package.json has `"test": "jest"` and `jest@^29.6.3` devDep, `@types/jest@^29.5.13` -->
<!-- tsconfig.json includes `"types": ["jest"]` -->
<!-- `@testing-library/react-native` is NOT installed — tests must use pure functions or jest.mock() -->
<!-- No `src/hooks/`, `src/services/__tests__/`, or `src/components/__tests__/` directories exist yet -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create failing test stub for useRole priority ladder + allowlist case-insensitivity</name>
  <files>src/hooks/__tests__/useRole.test.ts</files>
  <read_first>
    - __tests__/App.test.tsx (existing test — shows `/** @format */` header + `test(...)` style, NOT `it(...)`)
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §3.8 Test 1 (authoritative template for canFromUser tests; the in-repo App.test.tsx is the WRONG shape per PATTERNS §No Analog Found)
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md D-19 (two-test requirement), D-06 (lowercase-trimmed allowlist comparison)
    - .planning/phases/03-role-gating-precursor/03-VALIDATION.md §Wave 0 Requirements (first bullet)
    - jest.config.js (confirms `preset: 'react-native'` only; no custom setup)
  </read_first>
  <behavior>
    - Test 1 (fails at import — useRole.ts does not exist yet): `canFromUser` is importable from `'../useRole'` as a named export.
    - Test 2 (fails at runtime): admin user with `backendProfile.userType === 'admin'` returns true for `canFromUser(admin, 'editVerifications')`.
    - Test 3 (fails at runtime): allowlisted email `'beckprograms@gmail.com'` returns true for `canFromUser(allowlisted, 'editVerifications')`.
    - Test 4 (fails at runtime — D-06): UPPERCASE allowlisted email `'BECKPROGRAMS@GMAIL.COM'` returns true for `canFromUser(upper, 'editVerifications')`.
    - Test 5 (fails at runtime — D-12): renter `backendProfile.userType === 'renter'` returns true for `canFromUser(renter, 'manageListings')`; plain user returns false.
    - Test 6 (fails at runtime): `null` user returns false for `canFromUser(null, 'editVerifications')` (guest path).
  </behavior>
  <action>
Create `src/hooks/__tests__/useRole.test.ts` with this exact content (the import of `../useRole` is expected to fail until Plan 02 creates that file — that is the RED phase):

```typescript
/**
 * @format
 */

import { canFromUser } from '../useRole';

describe('canFromUser priority ladder', () => {
  const admin = { email: 'admin@foo.com', backendProfile: { userType: 'admin' } };
  const allowlisted = { email: 'beckprograms@gmail.com', backendProfile: {} };
  const renter = { email: 'r@foo.com', backendProfile: { userType: 'renter' } };
  const plainUser = { email: 'u@foo.com', backendProfile: { userType: 'user' } };

  test('userType=admin grants editVerifications (branch 2)', () => {
    expect(canFromUser(admin, 'editVerifications')).toBe(true);
  });

  test('allowlisted email grants editVerifications (M1 branch 3 — D-03)', () => {
    expect(canFromUser(allowlisted, 'editVerifications')).toBe(true);
  });

  test('allowlist email comparison is case-insensitive (D-06)', () => {
    const upper = { email: 'BECKPROGRAMS@GMAIL.COM', backendProfile: {} };
    expect(canFromUser(upper, 'editVerifications')).toBe(true);
  });

  test('allowlist email comparison trims whitespace (D-06)', () => {
    const padded = { email: '  beckprograms@gmail.com  ', backendProfile: {} };
    expect(canFromUser(padded, 'editVerifications')).toBe(true);
  });

  test('non-allowlisted plain user cannot editVerifications', () => {
    expect(canFromUser(plainUser, 'editVerifications')).toBe(false);
  });

  test('null user (guest) cannot editVerifications', () => {
    expect(canFromUser(null, 'editVerifications')).toBe(false);
  });

  test('manageListings (D-12) true for admin OR renter userType', () => {
    expect(canFromUser(admin, 'manageListings')).toBe(true);
    expect(canFromUser(renter, 'manageListings')).toBe(true);
    expect(canFromUser(plainUser, 'manageListings')).toBe(false);
  });

  test('editMatterportUrl and editPanoramicUrl are admin-only', () => {
    expect(canFromUser(admin, 'editMatterportUrl')).toBe(true);
    expect(canFromUser(admin, 'editPanoramicUrl')).toBe(true);
    expect(canFromUser(allowlisted, 'editMatterportUrl')).toBe(true);
    expect(canFromUser(plainUser, 'editMatterportUrl')).toBe(false);
    expect(canFromUser(renter, 'editPanoramicUrl')).toBe(false);
  });
});
```

The `/** @format */` header matches existing `__tests__/App.test.tsx` Prettier convention. Use `test(...)` (not `it(...)`) to match repo style. Named import from the relative `'../useRole'` path — no path aliases per CONVENTIONS.md.
  </action>
  <verify>
    <automated>test -f src/hooks/__tests__/useRole.test.ts &amp;&amp; grep -c "describe('canFromUser priority ladder'" src/hooks/__tests__/useRole.test.ts | grep -q '^1$' &amp;&amp; grep -c "case-insensitive" src/hooks/__tests__/useRole.test.ts | grep -q '^1$' &amp;&amp; grep -q "from '../useRole'" src/hooks/__tests__/useRole.test.ts &amp;&amp; (! npx jest src/hooks/__tests__/useRole.test.ts --runInBand --silent 2>&amp;1 | grep -q "^PASS ") &amp;&amp; (npx jest src/hooks/__tests__/useRole.test.ts --runInBand 2>&amp;1 | grep -qE "Cannot find module|Tests:.*failed")</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/hooks/__tests__/useRole.test.ts` exits 0 (file exists)
    - `grep -c "describe('canFromUser priority ladder'" src/hooks/__tests__/useRole.test.ts` returns `1`
    - `grep -c "test(" src/hooks/__tests__/useRole.test.ts` returns a number `>= 7` (at least 7 test blocks)
    - `grep -q "BECKPROGRAMS@GMAIL.COM" src/hooks/__tests__/useRole.test.ts` succeeds (case-insensitivity case present)
    - `grep -q "case-insensitive (D-06)" src/hooks/__tests__/useRole.test.ts` succeeds
    - `grep -q "manageListings" src/hooks/__tests__/useRole.test.ts` succeeds (D-12 coverage)
    - `grep -q "from '../useRole'" src/hooks/__tests__/useRole.test.ts` succeeds (import path)
    - `grep -q "^/\*\* @format \*/$" src/hooks/__tests__/useRole.test.ts` succeeds (Prettier header matching App.test.tsx)
    - Running `npx jest useRole.test --runInBand 2>&1 | head -20` MUST output a compile-error or "Cannot find module '../useRole'" (expected RED — Plan 02 creates useRole.ts)
    - Machine-verifiable RED assertion: `(! npx jest src/hooks/__tests__/useRole.test.ts --runInBand --silent 2>&1 | grep -q "^PASS ")` exits 0 — the suite MUST NOT pass at Wave 0 end.
    - Machine-verifiable RED assertion: `npx jest src/hooks/__tests__/useRole.test.ts --runInBand 2>&1 | grep -qE "Cannot find module|Tests:.*failed"` exits 0 — jest output MUST contain either a module-resolution error or a failed-tests summary line.
  </acceptance_criteria>
  <done>
    File exists at exact path with exact content. Jest discovers the file. Jest errors referencing the missing `../useRole` module (RED phase — correct state; Plan 02 turns this GREEN). No existing tests regress.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create failing test stub for PropertyService service-layer guard + Gated component</name>
  <files>src/services/__tests__/PropertyService.test.ts, src/components/__tests__/Gated.test.tsx</files>
  <read_first>
    - src/services/PropertyService.ts lines 189-213 (patchPlatformVerifications method — where the guard will be inserted at Plan 04)
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §3.8 Test 2 (authoritative service-guard test template)
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md D-17 (throw `PermissionDeniedError` with message `E_PERMISSION_DENIED`)
    - .planning/phases/03-role-gating-precursor/03-VALIDATION.md §Wave 0 Requirements (4th bullet for Gated; 3rd bullet for PropertyService)
  </read_first>
  <behavior>
    PropertyService.test.ts:
    - Test 1 (fails): non-admin user data via mocked AuthService causes `PropertyService.patchPlatformVerifications` to reject with an error whose message includes `'E_PERMISSION_DENIED'`.
    - Test 2 (fails): admin user data via mocked AuthService does NOT reject; calls `axios.patch` exactly once.

    Gated.test.tsx:
    - Test 1 (fails at import): `Gated` component is importable from `'../Gated'`.
    - Test 2 (fails at runtime / structural check): When `can(action)` returns true for the passed action, `Gated` renders its children. When false, renders fallback (or null by default). Use `react-test-renderer` (already installed — per PATTERNS.md) to render; mock `useRole` from `../../hooks/useRole`.
  </behavior>
  <action>
Create TWO files.

**File 1: `src/services/__tests__/PropertyService.test.ts`** (creates `src/services/__tests__/` directory):

```typescript
/**
 * @format
 */

import axios from 'axios';
import { PropertyService } from '../PropertyService';
import { AuthService } from '../AuthService';

jest.mock('axios');
jest.mock('../AuthService');

describe('PropertyService.patchPlatformVerifications guard (D-17)', () => {
  beforeEach(() => {
    jest.resetAllMocks();
  });

  test('throws with E_PERMISSION_DENIED message for non-admin user', async () => {
    (AuthService.getUserData as jest.Mock).mockResolvedValue({
      localId: 'uid_non_admin',
      email: 'random@example.com',
      backendProfile: { userType: 'user' },
    });

    await expect(
      PropertyService.patchPlatformVerifications('property_id', {
        ownershipDocuments: true,
        ownerIdentityVerified: false,
        stateIssuedDocumentsVerified: false,
      }),
    ).rejects.toThrow('E_PERMISSION_DENIED');
    expect(axios.patch).not.toHaveBeenCalled();
  });

  test('does NOT throw for admin user and calls axios.patch once', async () => {
    (AuthService.getUserData as jest.Mock).mockResolvedValue({
      localId: 'uid_admin',
      email: 'a@x.com',
      backendProfile: { userType: 'admin' },
    });
    (axios.patch as jest.Mock).mockResolvedValue({
      data: { _id: 'property_id', platformVerifications: {} },
    });

    await expect(
      PropertyService.patchPlatformVerifications('property_id', {
        ownershipDocuments: true,
        ownerIdentityVerified: false,
        stateIssuedDocumentsVerified: false,
      }),
    ).resolves.toMatchObject({ id: 'property_id' });
    expect(axios.patch).toHaveBeenCalledTimes(1);
  });

  test('does NOT throw for allowlisted-email user (M1 branch)', async () => {
    (AuthService.getUserData as jest.Mock).mockResolvedValue({
      localId: 'uid_owner',
      email: 'beckprograms@gmail.com',
      backendProfile: {},
    });
    (axios.patch as jest.Mock).mockResolvedValue({
      data: { _id: 'property_id' },
    });

    await expect(
      PropertyService.patchPlatformVerifications('property_id', {
        ownershipDocuments: true,
        ownerIdentityVerified: false,
        stateIssuedDocumentsVerified: false,
      }),
    ).resolves.toBeDefined();
  });
});
```

**File 2: `src/components/__tests__/Gated.test.tsx`** (creates `src/components/__tests__/` directory):

```typescript
/**
 * @format
 */

import React from 'react';
import ReactTestRenderer from 'react-test-renderer';
import { Text } from 'react-native';
import { Gated } from '../Gated';

jest.mock('../../hooks/useRole', () => ({
  useRole: jest.fn(),
}));

// eslint-disable-next-line @typescript-eslint/no-var-requires
const { useRole } = require('../../hooks/useRole');

describe('Gated', () => {
  beforeEach(() => {
    (useRole as jest.Mock).mockReset();
  });

  test('renders children when can(action) returns true', async () => {
    (useRole as jest.Mock).mockReturnValue({
      can: (a: string) => a === 'editVerifications',
    });

    let tree: ReactTestRenderer.ReactTestRenderer | null = null;
    await ReactTestRenderer.act(() => {
      tree = ReactTestRenderer.create(
        <Gated action="editVerifications">
          <Text testID="child">admin-only</Text>
        </Gated>,
      );
    });
    const json = tree!.toJSON();
    expect(JSON.stringify(json)).toContain('admin-only');
  });

  test('renders fallback (default null) when can(action) returns false', async () => {
    (useRole as jest.Mock).mockReturnValue({
      can: () => false,
    });

    let tree: ReactTestRenderer.ReactTestRenderer | null = null;
    await ReactTestRenderer.act(() => {
      tree = ReactTestRenderer.create(
        <Gated action="editVerifications">
          <Text testID="child">admin-only</Text>
        </Gated>,
      );
    });
    expect(JSON.stringify(tree!.toJSON() || {})).not.toContain('admin-only');
  });

  test('renders explicit fallback prop when can(action) returns false', async () => {
    (useRole as jest.Mock).mockReturnValue({
      can: () => false,
    });

    let tree: ReactTestRenderer.ReactTestRenderer | null = null;
    await ReactTestRenderer.act(() => {
      tree = ReactTestRenderer.create(
        <Gated action="editVerifications" fallback={<Text>no-access</Text>}>
          <Text>admin-only</Text>
        </Gated>,
      );
    });
    const json = JSON.stringify(tree!.toJSON());
    expect(json).toContain('no-access');
    expect(json).not.toContain('admin-only');
  });
});
```

Both files use `/** @format */` header per Prettier convention. Both are expected to fail (RED) until Plans 02 and 04 supply the real modules. Use named imports + relative paths per CONVENTIONS.md "Import Organization."
  </action>
  <verify>
    <automated>test -f src/services/__tests__/PropertyService.test.ts &amp;&amp; test -f src/components/__tests__/Gated.test.tsx &amp;&amp; grep -q "rejects.toThrow('E_PERMISSION_DENIED')" src/services/__tests__/PropertyService.test.ts &amp;&amp; grep -q "jest.mock('../../hooks/useRole'" src/components/__tests__/Gated.test.tsx &amp;&amp; (! npx jest src/services/__tests__/PropertyService.test.ts --runInBand --silent 2>&amp;1 | grep -q "^PASS ") &amp;&amp; (! npx jest src/components/__tests__/Gated.test.tsx --runInBand --silent 2>&amp;1 | grep -q "^PASS ")</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/services/__tests__/PropertyService.test.ts` exits 0
    - `test -f src/components/__tests__/Gated.test.tsx` exits 0
    - `grep -c "describe('PropertyService.patchPlatformVerifications guard" src/services/__tests__/PropertyService.test.ts` returns `1`
    - `grep -q "rejects.toThrow('E_PERMISSION_DENIED')" src/services/__tests__/PropertyService.test.ts` succeeds
    - `grep -c "jest.mock('axios')" src/services/__tests__/PropertyService.test.ts` returns `1`
    - `grep -c "jest.mock('../AuthService')" src/services/__tests__/PropertyService.test.ts` returns `1`
    - `grep -q "describe('Gated'" src/components/__tests__/Gated.test.tsx` succeeds
    - `grep -c "test(" src/components/__tests__/Gated.test.tsx` returns `3`
    - `grep -q "from '../Gated'" src/components/__tests__/Gated.test.tsx` succeeds
    - Running `npx jest PropertyService.test --runInBand 2>&1 | head -20` MUST show the guard test failing or throwing (expected RED — Plan 04 turns this GREEN). `npx jest Gated.test --runInBand 2>&1 | head -20` MUST fail on the missing `../Gated` module (expected RED — Plan 02 supplies it).
    - Machine-verifiable RED assertion (PropertyService): `(! npx jest src/services/__tests__/PropertyService.test.ts --runInBand --silent 2>&1 | grep -q "^PASS ")` exits 0 — suite MUST NOT pass at Wave 0 end (Plan 04 lands the guard in Wave 2).
    - Machine-verifiable RED assertion (Gated): `(! npx jest src/components/__tests__/Gated.test.tsx --runInBand --silent 2>&1 | grep -q "^PASS ")` exits 0 — suite MUST NOT pass at Wave 0 end (Plan 02 lands `Gated` in Wave 1).
    - Machine-verifiable RED assertion (Gated module resolution): `npx jest src/components/__tests__/Gated.test.tsx --runInBand 2>&1 | grep -qE "Cannot find module|Tests:.*failed"` exits 0 — jest output MUST contain either a module-resolution error (for `../Gated` or `../../hooks/useRole`) or a failed-tests summary line.
  </acceptance_criteria>
  <done>
    Both test files exist at exact paths with exact content. Jest discovers both files. RED state confirmed — tests fail on the missing modules they reference, which is correct.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document src/hooks/ directory convention in CONVENTIONS.md</name>
  <files>.planning/codebase/CONVENTIONS.md</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (lines 1-20 — current "Naming Patterns" section, lines 5-18 specifically — where to insert)
    - .planning/phases/03-role-gating-precursor/03-CONTEXT.md code_context line 171 ("No `src/hooks/` directory today ... Phase 3 establishes `src/hooks/` as the canonical location for React hooks. Add a one-line `.planning/codebase/CONVENTIONS.md` entry")
    - .planning/phases/03-role-gating-precursor/03-RESEARCH.md §3.1 ("Hook-directory convention note")
  </read_first>
  <behavior>
    - CONVENTIONS.md line listing file/directory conventions includes `src/hooks/` as the canonical location for React custom hooks.
    - Co-located test convention `src/**/__tests__/` is documented (Phase 3 is the first user of this pattern).
  </behavior>
  <action>
Open `.planning/codebase/CONVENTIONS.md`. Find the `**Files:**` bullet list inside `## Naming Patterns` (around lines 7-18). Add these two new bullets immediately after the `- Tests: co-located in root-level __tests__/...` line (line 17):

```markdown
- React custom hooks: `camelCase.ts` starting with `use` prefix, placed in `src/hooks/` (directory established Phase 3; reserve `src/hooks/` for React hooks — pure utilities stay in `src/utils/`)
- Co-located unit tests: `src/<subdir>/__tests__/<file>.test.ts(x)` for per-module tests (established Phase 3 for hooks/services/components). Root-level `__tests__/` continues to host app-level smoke tests (e.g., `__tests__/App.test.tsx`).
```

The existing Tests bullet (line 17) stays as-is. The two new bullets sit immediately after it so the file/directory taxonomy flows naturally.
  </action>
  <verify>
    <automated>grep -q "React custom hooks:" .planning/codebase/CONVENTIONS.md &amp;&amp; grep -q "src/hooks/" .planning/codebase/CONVENTIONS.md &amp;&amp; grep -q "Co-located unit tests:" .planning/codebase/CONVENTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "React custom hooks:" .planning/codebase/CONVENTIONS.md` returns `1`
    - `grep -c "src/hooks/" .planning/codebase/CONVENTIONS.md` returns a number `>= 1`
    - `grep -c "Co-located unit tests:" .planning/codebase/CONVENTIONS.md` returns `1`
    - `grep -q "__tests__" .planning/codebase/CONVENTIONS.md` succeeds (existing + new references present)
  </acceptance_criteria>
  <done>
    CONVENTIONS.md has two new bullets under `## Naming Patterns > **Files:**` documenting `src/hooks/` and co-located `__tests__/` conventions. Existing content unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (meta-task) | This plan only creates test scaffolds and documentation. No runtime code paths touched. No user input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Test stubs land but run in CI against future production modules | accept | Test stubs are expected to fail (RED phase) until Wave 1/2 modules land. If Wave 1/2 fail to deliver, Wave 4's 4-part grep invariant and test suite gate will catch the missing modules. No independent risk. |

Seed threats T1-T4 from the security_gate do not apply here (no runtime code, no auth path). T1 (privilege escalation) is addressed by Plan 04 (service guard) + Plan 07 (backend coordination). T2 (case-variation) has its test already scaffolded in Task 1. T3 (stale auth memo) is a Plan 02 concern. T4 (regression to inline userType check) is a Plan 07 grep invariant concern.
</threat_model>

<verification>
All three test files exist at their exact co-located paths. All three files contain the expected `describe`/`test` blocks per the acceptance criteria. CONVENTIONS.md documents `src/hooks/` and `src/**/__tests__/` conventions. Running `npx jest --listTests 2>/dev/null | grep -E "(useRole|PropertyService|Gated)"` returns all three discovered test files.

RED phase is CORRECT state at Wave 0 end: tests fail on missing modules (`../useRole`, `../Gated`). Plans 02, 03, 04 turn individual tests GREEN as they deliver the production modules.
</verification>

<success_criteria>
- Three new test files exist at exact paths (one in `src/hooks/__tests__/`, one in `src/services/__tests__/`, one in `src/components/__tests__/`).
- Each test file has `/** @format */` Prettier header matching `__tests__/App.test.tsx`.
- CONVENTIONS.md includes two new bullets documenting the hooks directory + co-located test conventions.
- `npx jest --listTests | wc -l` returns at least 4 (previous 1 + new 3).
- `npx jest --runInBand 2>&1 | tail -5` shows failures on the 3 new suites (RED phase); the pre-existing `App.test.tsx` still passes.
- No production `.ts` / `.tsx` files under `src/` are modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-role-gating-precursor/03-01-SUMMARY.md` documenting:
- Exact file paths created (3 test files + 1 doc edit).
- Expected RED state of the 3 new test suites (list which tests fail, which modules are missing).
- Confirmation that `npx jest --listTests` discovered all 3 new files.
- Any deviations (e.g., if a Prettier format-on-save reformatted something, note it).
</output>
