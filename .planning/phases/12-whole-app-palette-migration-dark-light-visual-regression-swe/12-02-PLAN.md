---
phase: 12-whole-app-palette-migration-dark-light-visual-regression-swe
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-VERIFICATION.md
autonomous: false
requirements: [PAL-03]
requirements_addressed: [PAL-03]
tags: [theme, palette, qa, m6, sweep, manual-device-qa]

must_haves:
  truths:
    - "The 15-screen risk-target list (from SPEC §Boundaries) is walked end-to-end on iPhone 15 Pro Max + Moto G XT2513V × EN/RU × light/dark."
    - "12-VERIFICATION.md exists at .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/."
    - "Mass-disposition table format per D-05 — one row per (screen × device), inline EN/RU and light/dark columns; cell vocabulary PASS / FAIL / INHERITS-FROM-<row> (M3 Plan 05-05 precedent)."
    - "FAIL count summary line at top of file; FAIL=0 is the PAL-03 acceptance condition."
    - "Mass-disposition rationale section explains every claimed INHERITS-FROM edge."
    - "Sign-off block at bottom — operator email (beckprograms@gmail.com) + device UDID-suffix (last-4 of UDID) + ISO timestamp. No initials field — the email + UDID-suffix + timestamp combination is the audit trail."
    - "Two-plan split per D-06 honored — this plan does not touch src/ (no source-file edits in Plan 12-02; only .planning/ artifact)."
    - "The 4 SPEC sweep checkboxes (iPhone walk + Moto walk + 12-VERIFICATION.md written + FAIL=0) all checked."
  artifacts:
    - path: .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-VERIFICATION.md
      provides: "Mass-disposition table with one row per (screen × device) for the 15 SPEC risk-target screens × 2 devices; cell verdicts PASS / FAIL / INHERITS-FROM-<row>; FAIL-count summary; mass-disposition rationale; sign-off block."
      contains: "## Mass-disposition rationale"
  key_links:
    - from: src/theme/colors.ts (post-12-01 HEAD)
      to: "75 useTheme() consumer surfaces across the 15 risk-target screens"
      via: "operator-driven physical-device walk on iPhone 15 Pro Max + Moto G XT2513V"
      pattern: "<screen file path>"
    - from: .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-VERIFICATION.md
      to: "PAL-03 requirement closure"
      via: "FAIL=0 mass-disposition over the 15-screen × 2-device matrix"
      pattern: "FAIL.*0"
---

<objective>
Walk the 15-screen risk-target list (locked in SPEC §Boundaries lines 64-80) on iPhone 15 Pro Max + Moto G XT2513V × EN/RU × light/dark using the Plan 12-01 token build, and write `12-VERIFICATION.md` as the auditable artifact capturing the sweep results in the mass-disposition format locked by CONTEXT D-05.

Purpose: PAL-03 closes only when an operator has visually verified that the new palette renders without broken contrast, illegible text, or theme drift on both target devices in both locales and both modes. The artifact is the audit trail.

Output: A single new file `12-VERIFICATION.md` under the phase directory. No `src/` changes (per D-06 two-plan split). Atomic commit on the artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-CONTEXT.md
@.planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-SPEC.md
@.planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-PATTERNS.md
@.planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-01-PLAN.md
@src/theme/colors.ts

<sweep_runbook>
<!-- 15-screen risk-target list, locked in SPEC §Boundaries lines 64-80 and confirmed on disk
     in PATTERNS.md §"15-Screen Risk-Target List" lines 270-292. -->

| # | Screen | File path |
|---|--------|-----------|
| 1 | HomeScreen | src/screens/HomeScreen.tsx |
| 2 | PropertyCard | src/components/PropertyCard.tsx |
| 3 | PropertyDetailsScreen + details/* sub-components (HeaderInfoCard, AttributeList, MapPreviewCard, KeyStatsCard) | src/screens/PropertyDetailsScreen.tsx + src/components/details/*.tsx |
| 4 | HospitalityCard | src/components/HospitalityCard.tsx |
| 5 | ChatScreen | src/screens/ChatScreen.tsx |
| 6 | ChatThreadScreen | src/screens/ChatThreadScreen.tsx |
| 7 | ChatComposeScreen | src/screens/ChatComposeScreen.tsx |
| 8 | ContextualListingFlow (orchestrator index.tsx + Step1..Step6 directory) | src/components/ContextualListingFlow/index.tsx + Step{1..6}*.tsx |
| 9 | MediaCurationScreen | src/screens/MediaCurationScreen.tsx |
| 10 | ProfileScreen | src/screens/ProfileScreen.tsx |
| 11 | AccountSettingsScreen | src/screens/AccountSettingsScreen.tsx |
| 12 | ModerationQueueScreen | src/screens/ModerationQueueScreen.tsx |
| 13 | LandlordApplicationQueueScreen | src/screens/LandlordApplicationQueueScreen.tsx |
| 14 | RoleManagementScreen | src/screens/RoleManagementScreen.tsx |
| 15 | BottomNavigator | src/components/BottomNavigator.tsx |

Sweep ordering (per CONTEXT §Specifics line 210):
- For each device: walk LIGHT mode first, then flip to DARK (minimizes mode-switch taps).
- For each (device × mode): walk EN first, then RU (RU is the inheritance-source candidate per M3 Plan 05-05 logical-equivalence pattern — text-only changes inherit cleanly).

Per-cell verdict vocabulary (per D-05 + PATTERNS.md §"Per-cell vocabulary"):
- `PASS`     — observed expected behavior on the listed (device × locale × mode).
- `FAIL`     — contrast / legibility / theme-drift finding; bug-fix loop opens — MUST = 0 at close per PAL-03.
- `INHERITS-FROM-<row>`  — logical-equivalence shortcut (e.g. `RU-dark` = `INHERITS-FROM-EN-dark` if only locale strings change with no token drift).

Cells where the operator can demonstrably argue logical-equivalence are eligible for INHERITS-FROM; cells where RU might reflow surface (long-string wrapping changes layout) MUST be walked explicitly.

What is NOT walked (out of scope per SPEC lines 86-87): ~10 lower-risk screens (Auth flows — Login/Signup/Forgot/Reset; list screens that render PropertyCard — Favorites, RenterListings, OwnerListings, Appointments; Tour3DScreen + TourSelectionScreen — WebView containers; ScheduleViewingScreen; ApplicantProfileScreen; AdminVerificationScreen; ListingAdminScreen). They inherit token behavior from shared components already walked. Adding rows for them to the matrix is scope creep (PATTERNS.md anti-pattern #11).
</sweep_runbook>

<artifact_template>
<!-- The exact shape of 12-VERIFICATION.md. Derived from PATTERNS.md §"12-VERIFICATION.md"
     (M3 Plan 05-05 precedent + CONTEXT D-05 column shape). -->

Top of file:

---
phase: 12-whole-app-palette-migration-dark-light-visual-regression-swe
plan: 02
type: verification-matrix
status: closed-mass-disposition
walked: <ISO timestamp at sweep close>
walked_against_sha: <git rev-parse HEAD at sweep close — Plan 12-01 commit SHA>
walk_disposition: APPROVED   # or APPROVED-WITH-MASS-DISPOSITION
coverage_mode: empirical-sampling-mass-disposition
walker: beckprograms@gmail.com
devices:
  ios:
    model: iPhone 15 Pro Max
    os: <iOS version at walk>
    build: Fabric / Release
  android:
    model: Moto G XT2513V
    os: <Android version at walk>
    build: Fabric / Release
requirements_addressed: [PAL-03]
testing_bar: M6 manual physical-device QA per CLAUDE.md
walk_scope: 15-screen risk-target × 2 devices × 2 locales × 2 modes
matrix_count: 1
verdict_taxonomy:
  - PASS
  - FAIL
  - INHERITS-FROM-<row>
totals:
  total_rows: 30   # 15 screens × 2 devices; each row has 4 cells (EN-light, EN-dark, RU-light, RU-dark)
  total_cells: 120
  pass: <fill at close>
  inherits: <fill at close>
  fail: <fill at close>   # MUST = 0 per SPEC acceptance
mass_disposition:
  approved_by: beckprograms@gmail.com
  approved_at: <ISO>
  rationale_section: "## Mass-disposition rationale"
---

# Phase 12 Visual-Regression Sweep — Verification Matrix

**FAIL count: <N>** (must = 0 to close PAL-03)

## Sweep matrix

| # | Screen | Device | EN-light | EN-dark | RU-light | RU-dark | Notes |
|---|--------|--------|----------|---------|----------|---------|-------|
| 1 | HomeScreen | iPhone 15 Pro Max | <EN-light> | <EN-dark> | <RU-light> | <RU-dark> | <fill-during-walk> |
| 1 | HomeScreen | Moto G XT2513V | <EN-light> | <EN-dark> | <RU-light> | <RU-dark> | <fill-during-walk> |
| 2 | PropertyCard | iPhone 15 Pro Max | ... | ... | ... | ... | — |
| 2 | PropertyCard | Moto G XT2513V | ... | ... | ... | ... | — |
| ... |
| 15 | BottomNavigator | iPhone 15 Pro Max | ... | ... | ... | ... | — |
| 15 | BottomNavigator | Moto G XT2513V | ... | ... | ... | ... | — |

(30 rows total — one per (screen × device). Cell placeholders `<EN-light>` / `<EN-dark>` / `<RU-light>` / `<RU-dark>` are intentional — the operator MUST overwrite each with a verdict token from {PASS, FAIL, INHERITS-FROM-<row>}. Acceptance criteria 4 rejects any row that still contains literal placeholder tokens, preventing verbatim copy-paste shipping per checker INFO #8.)

## Mass-disposition rationale

### What WAS walked-and-confirmed this session

| Screen | Device | (Locale × Mode) walked | Notes |
|--------|--------|------------------------|-------|
| HomeScreen | iOS | EN-dark | <evidence sentence — e.g. "background reads #121214, filter chips render new border tone, language pill stays in header per memory" > |
| ... |

### What was NOT walked (inheritance shortcuts claimed)

| Inheriting cell | Inherits from | Logical-equivalence argument |
|-----------------|---------------|------------------------------|
| HomeScreen iOS RU-dark | HomeScreen iOS EN-dark | RU locale changes text strings only; no `colors.X` read differs between locales |
| ... |

### Why this is defensible (and where it isn't)

**Defensible:** RU is a string-translation layer; LanguageContext does not branch on color token reads.
**Where it isn't:** Any screen where a long RU string visibly reflows surface and could expose a token boundary not seen in EN — list each one explicitly here. Suggested candidates from history (per memory `260515-iqi`): EmailVerifyBanner column-wrap; any chip strip where Russian text overflows a fixed-width pill. These rows MUST be walked explicitly in RU, not inherited.

## FAIL findings (one section per FAIL cell)

(If any FAIL: one section per finding. Sentence-level description + screenshot path optional. Format:

### FAIL-01 — <Screen> on <Device> at <Locale × Mode>
- **What:** <one sentence — e.g. "MediaCurationScreen action-dock background reads M3-era #25282F instead of new #1c1c20, visible as a 1-2 shade band against surrounding surfaces" >
- **Root cause hypothesis:** <e.g. "action-dock uses inline style not theme token" — this is a Phase 13+ ticket, NOT a Phase 12 fix per PATTERNS.md anti-pattern #1>
- **Screenshot:** <optional path under .planning/phases/12-.../screenshots/ — operator's call>
- **Disposition:** <one of: deferred-to-phase-13+ / open / fixed-mid-sweep>

If FAIL count > 0 at close: PAL-03 does NOT close. Either open a Phase 12.5 fix plan OR confirm each FAIL is a documented deferred-to-Phase-N item with explicit user sign-off. Either way, do not check the FAIL=0 SPEC checkbox.)

## Sign-off

| Field | Value |
|-------|-------|
| Walker | beckprograms@gmail.com |
| Walk start | <ISO timestamp> |
| Walk end | <ISO timestamp> |
| iPhone IMEI/UDID-suffix | <last-4 of UDID — for audit trail> |
| Moto IMEI/UDID-suffix | <last-4 of UDID — for audit trail> |
| Total rows | 30 |
| Total cells | 120 |
| PASS | <count> |
| INHERITS-FROM | <count> |
| FAIL | 0   (or <count> if non-zero — and PAL-03 does NOT close) |
| Disposition | APPROVED-WITH-MASS-DISPOSITION |
</artifact_template>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator walks the 15 × 2 × 4 sweep matrix on iPhone 15 Pro Max + Moto G XT2513V × EN/RU × light/dark</name>
  <files>
    .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-SPEC.md §Boundaries lines 64-80 (the 15-screen risk-target list with operator focus areas per screen)
    - .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-CONTEXT.md §Decisions D-05 lines 81-89 (mass-disposition table format + cell vocabulary)
    - .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-CONTEXT.md §Specifics line 210 (sweep ordering hint)
    - .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-PATTERNS.md §"12-VERIFICATION.md" lines 132-244 (artifact template + frontmatter structure + sign-off block)
    - .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-PATTERNS.md §"Anti-Patterns" lines 369-396 (especially #1 no-per-call-site-edit, #9 no surface2/surface3 expected, #11 no out-of-list screens, #12 paired-gates if any FAIL)
    - .planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-01-PLAN.md (Plan 12-01 must be merged before this sweep starts — the new tokens are what's being walked)
    - .planning/milestones/v3.0-phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md (M3 mass-disposition precedent — Frontmatter at lines 1-60, cell-verdict legend at 77-84, rationale section at 111-157, sign-off block at 326-339)
    - src/theme/colors.ts (post-Plan-12-01 — confirm 31-key shape and value strings are what the device build is rendering)
  </read_first>
  <what-built>
Plan 12-01 has shipped the MoveIn handoff palette in `src/theme/colors.ts` (31 keys × 2 modes, MODE_INDEPENDENT_PALETTE extracted). All 75 `useTheme()` call sites now read the new tokens. The app is buildable on both target devices against the new token build.
  </what-built>
  <how-to-verify>
1. **Pre-flight (one-time):**
   - Confirm `src/theme/colors.ts` HEAD = Plan 12-01's commit SHA. (`git log --oneline -1 -- src/theme/colors.ts`)
   - Build the app to both target devices: iPhone 15 Pro Max (Fabric / Release config) + Moto G XT2513V (Fabric / Release config; if `gradlew clean bundleRelease` is used, apply the reanimated workaround per memory `android-reanimated-clean-prefab-gotcha.md`: `gradlew :react-native-reanimated:assembleRelease :app:bundleRelease`).
   - Confirm both builds install and launch.

2. **Create the verification artifact** at `.planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-VERIFICATION.md` using `<artifact_template>` above as the skeleton. Fill in:
   - Frontmatter: `walked_against_sha` = Plan 12-01 commit SHA; `walked` = ISO timestamp at sweep start; OS versions; device UDID-suffix-last-4.
   - Pre-populate the matrix table with all 30 rows (15 screens × 2 devices) — cell values blank, to be filled per walk.

3. **Walk the matrix.** Sweep ordering per CONTEXT §Specifics line 210:
   - **Device 1 (iPhone 15 Pro Max):**
     - Set system theme = LIGHT, locale = EN. Walk all 15 screens in order from `<sweep_runbook>`. For each, observe:
       - Does the background read `#f3f3f6` (light) or `#121214` (dark)?
       - Does surface read `#ffffff` / `#1c1c20`?
       - Does text read `#16161a` / `#f4f4f6`?
       - Does any accent / CTA / active state render `#ff5a6f`?
       - Hairline borders read `rgba(0,0,0,0.08)` / `rgba(255,255,255,0.08)` (subtle, not the heavy old `#E0E0E0` / `#2E3238` strokes)?
       - Is text readable? Any contrast call-out (WCAG AA — operator judgment, not automated)?
       - Any visible "old palette bleeding through" artifact — e.g. an M5-era component rendering a hardcoded hex that drifts from the screen's new surface?
       - Mark per-cell as PASS or FAIL. Single-sentence note on FAIL.
     - Flip system theme = DARK. Walk same 15 screens. Same observations.
     - Flip locale = RU. Walk light then dark. For each cell, judge whether RU changes only string content (→ INHERITS-FROM-<EN-row>) or whether long-RU-string reflow exposes a new token boundary (→ explicit walk; PASS or FAIL).
   - **Device 2 (Moto G XT2513V):** repeat the full 4-cell sequence for all 15 screens.

4. **Operator focus areas per screen (extracted from SPEC §Boundaries — operator should pay attention to these specifically):**
   - HomeScreen: filter chips, location pill, theme + language toggles, search header, result count strip. (Language pill MUST stay in header per memory `m6-language-pill-stays-in-header.md`.)
   - PropertyCard: multiple surfaces, accent pills, status badges, sale/rent dot, owner-action buttons.
   - PropertyDetailsScreen + details/*: HeaderInfoCard, AttributeList, MapPreviewCard, KeyStatsCard — heaviest screen; many surface layers, sticky bars, accent CTAs, modal overlays.
   - HospitalityCard: tour-first layout distinct from PropertyCard.
   - ChatScreen / ChatThreadScreen / ChatComposeScreen: bubble surfaces, KAV layout. KBD-02 invariant still holds (no keyboard regressions).
   - ContextualListingFlow: stepper + form chrome + photo dropzone in admin-mod mode; all 6 steps.
   - MediaCurationScreen: photo grid, action dock, scrim overlay, accent CTA buttons.
   - ProfileScreen: identity card, landlord banner, role-gated tiles, ADMIN TOOLS section (note: Phase 16 will reskin this; Phase 12 sweep checks the CURRENT layout reads the new tokens, not the future Phase 16 layout).
   - AccountSettingsScreen: card list + section labels + Delete-account row (Phase 15 reskin pending; same note as Profile).
   - ModerationQueueScreen / LandlordApplicationQueueScreen / RoleManagementScreen: queue rows + status pills + filter chips.
   - BottomNavigator: active/inactive tab states with accent indicator.

5. **EXPECTED non-fail patterns (do NOT mark these as FAIL):**
   - Per PATTERNS.md anti-pattern #9: a screen "looking flat without `surface2` layering" is EXPECTED — Phase 14 is the first consumer of surface2/surface3. Plan 12-02 only verifies the tokens render where consumed; un-consumed new tokens are not failures.
   - Old-token surfaces sitting alongside new-token surfaces (e.g. a dark-mode form input at `#2E3238` next to a `#121214` background) is the explicit CONTEXT D-01 effect — the sweep verifies READABILITY + CONTRAST, not visual-token uniformity. This is PASS unless it actually breaks readability / WCAG AA.
   - Phase 16 / Phase 15 / Phase 14 surfaces look unchanged: also PASS — those phases are pending, the sweep here is "do the new tokens render correctly", not "is the new design shipped".

6. **Fill in the artifact**:
   - All 30 rows complete (no blank cells).
   - Compute totals: PASS / INHERITS-FROM / FAIL.
   - Write the `## Mass-disposition rationale` section: which inheritance edges were claimed, why they're defensible, where they aren't (be honest — flag any RU cell where long-string reflow could mask a token boundary).
   - If FAIL > 0: write one `### FAIL-NN` section per finding with the format in `<artifact_template>`.
   - Fill the sign-off block: walker email, walk start/end ISO timestamps, iPhone + Moto UDID-suffix-last-4, totals, disposition.

7. **Commit** the artifact atomically: `git add .planning/phases/12-.../12-VERIFICATION.md && git commit -m "docs(12-02): visual-regression sweep + 12-VERIFICATION.md (PAL-03)"`.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-VERIFICATION.md` exists.
    - File has YAML frontmatter matching the structure in `<artifact_template>` (phase, plan, type, status, walked, walked_against_sha, devices block, requirements_addressed [PAL-03], walk_scope, verdict_taxonomy, totals block, mass_disposition block).
    - Mass-disposition table has exactly 30 rows (one per screen × device for the 15 SPEC risk-target screens).
    - Every cell value is one of: `PASS`, `FAIL`, `INHERITS-FROM-<row>`. No blank cells, no literal placeholder tokens (the `<EN-light>` / `<EN-dark>` / `<RU-light>` / `<RU-dark>` placeholders from the template MUST be overwritten — per checker INFO #8 — verified by `! grep -qE '<(EN|RU)-(light|dark)>' 12-VERIFICATION.md`), no other vocabulary (`grep -E '^\| [0-9]+ \|' 12-VERIFICATION.md` row count == 30; every cell extractable to one of the three verdict tokens).
    - FAIL count summary line at top of file (`grep -E '^\*\*FAIL count:' 12-VERIFICATION.md`).
    - `## Mass-disposition rationale` section present and explains each claimed INHERITS-FROM edge with a defensibility argument.
    - For every FAIL row (if any): a `### FAIL-NN` section follows with the screen / device / locale × mode + a 1-sentence finding.
    - Sign-off block at bottom contains walker email (beckprograms@gmail.com), ISO walk-start + walk-end timestamps, device UDID-suffix-last-4 for both devices, totals (PASS / INHERITS-FROM / FAIL counts that sum to 120), and disposition line (APPROVED or APPROVED-WITH-MASS-DISPOSITION).
    - **PAL-03 close condition**: `FAIL = 0`. If FAIL > 0, the operator must either (a) open a Phase 12.5 fix plan, OR (b) confirm with user that each FAIL is a documented deferred-to-Phase-N+ item with explicit sign-off — and PAL-03 does NOT close.
    - The 4 SPEC sweep checkboxes (SPEC.md lines 136-138 — iPhone walk + Moto walk + artifact written + FAIL=0) are all checked.
    - `git diff --name-only HEAD` shows only the new file `.planning/phases/.../12-VERIFICATION.md` — no `src/` changes (D-06 two-plan split; PATTERNS.md anti-pattern #1).
  </acceptance_criteria>
  <resume-signal>
Type "approved" if FAIL count = 0 and all acceptance criteria pass. If any FAIL was logged, describe disposition (Phase 12.5 fix / deferred-to-Phase-N+ with reasoning / open as new GSD ticket) and Phase 12 does NOT close until that loop is resolved.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → verification artifact | The only untrusted-input crossing is the operator filling in cell verdicts honestly. No runtime input, no network. Surface is bounded by the artifact diff. |
| operator → physical device | Operator runs the production-config build on physical hardware and judges contrast / legibility by eye. No automated WCAG tool. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering / Sweep skip | 12-VERIFICATION.md row coverage | mitigate | A theme-drift artifact surfaces in production because the sweep skipped a screen. Mitigation: the 15-screen risk-target list is explicit in `<sweep_runbook>`; the artifact template forces 30 rows (no blank cells); the acceptance criteria require row count == 30 with every cell in the verdict taxonomy. Inheritance shortcuts require explicit rationale in the `## Mass-disposition rationale` section. Severity LOW (catchable in next release cycle if missed). |
| T-12-06 | Tampering / cell-verdict lying | 12-VERIFICATION.md cell values | accept | Operator marks a cell PASS that should be FAIL (intentional or by oversight). Mitigation: this is bounded by operator integrity + the M2/M3 paired-gates pattern (memory `gsd-verifier-misses-regressions.md`) — if a Phase 12 visual bug surfaces in Phase 14+ device QA, the FAIL is caught one phase later (still pre-release). Accepted because automated visual regression is out of scope (SPEC §Out of scope). Severity LOW. |
| T-12-07 | Repudiation / sign-off | sign-off block | mitigate | Sweep is marked APPROVED without an actual walk. Mitigation: sign-off block requires walker email + ISO timestamps + device UDID-suffix-last-4 — provides audit trail. If the sign-off block is incomplete, acceptance criteria fail. Severity LOW. |
| T-12-08 | Information Disclosure | FAIL evidence screenshots | accept | Screenshots in `.planning/phases/.../screenshots/` could capture device UI containing test-user PII (test phone numbers, test emails). Mitigation: operator's discretion to redact before commit; the existing project pattern is screenshot paths are optional in the row Notes column, not mandatory. Test data in this project is the developer's own test accounts — no real-user data is at risk. Severity LOW. |
| T-12-09 | Scope creep | src/ changes during sweep | mitigate | Operator notices a hardcoded hex during sweep and "incidentally fixes it" — bringing a per-call-site edit into Plan 12-02. Mitigation: acceptance criteria require `git diff --name-only HEAD` shows ZERO `src/` changes; PATTERNS.md anti-pattern #1 explicitly forbids in-sweep src/ edits; D-06 two-plan split is precisely to prevent this. Any hex-cleanup is a Phase 14+/15+/16+ ticket. Severity LOW. |

Severity: all LOW. No `[BLOCKING]` security tasks. Phase 12 sweep is operator-driven QA over a tokens-only refactor; no network / IO / authentication surface.
</threat_model>

<verification>
- Acceptance criteria above are exhaustive for the artifact shape and the sweep coverage.
- The 4 SPEC sweep checkboxes (SPEC.md acceptance criteria lines 136-138 — iPhone walk + Moto walk + 12-VERIFICATION.md written + FAIL=0) all pass to close PAL-03.
- Diff scope: only `.planning/phases/.../12-VERIFICATION.md` (D-06 two-plan split honored).
- `walked_against_sha` in frontmatter ties the sweep to Plan 12-01's commit — auditable provenance.
</verification>

<success_criteria>
- `12-VERIFICATION.md` exists at `.planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/`.
- Mass-disposition table walked end-to-end: 30 rows (15 screens × 2 devices), 120 cells (× 4 EN/RU × light/dark per row), every cell in the {PASS, FAIL, INHERITS-FROM-<row>} vocabulary.
- FAIL count = 0 → PAL-03 closes. (If FAIL > 0 → either open Phase 12.5 fix plan or deferred-to-Phase-N+ with explicit user sign-off — PAL-03 does NOT close until resolved.)
- `## Mass-disposition rationale` section justifies every INHERITS-FROM edge.
- Sign-off block at file bottom — walker email + ISO walk-start/end + device UDID-suffix-last-4 for both devices + totals + disposition.
- Atomic single-file commit on `.planning/phases/.../12-VERIFICATION.md`. Zero `src/` changes.
- Phase 12 ready to close ROADMAP row.
</success_criteria>

<output>
After completion, create `.planning/phases/12-whole-app-palette-migration-dark-light-visual-regression-swe/12-02-SUMMARY.md` capturing:
- Walk start / end ISO timestamps + total elapsed minutes.
- Final cell counts: PASS / INHERITS-FROM / FAIL.
- Sweep disposition (APPROVED vs APPROVED-WITH-MASS-DISPOSITION).
- If any FAILs: per-FAIL disposition (Phase 12.5 fix / deferred-to-Phase-N+ / open).
- Confirmation that `git diff --name-only HEAD` showed only the new `.planning/` file (no `src/` changes — D-06 two-plan split honored).
- Atomic commit SHA for the artifact.
- Forward signal to Phase 13: tokens are shipped + visually verified; M6 data-model phase can begin.
</output>
