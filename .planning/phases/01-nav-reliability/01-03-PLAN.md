---
phase: 01-nav-reliability
plan: 03
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - App.tsx
  - .planning/codebase/CONVENTIONS.md
  - src/components/BottomNavigator.tsx
autonomous: true
requirements: [NAV-01, NAV-02]
requirements_addressed: [NAV-01, NAV-02]
tags: [react-native, navigation, pointer-events, state-machine]

must_haves:
  truths:
    - "mainStackScreenStyle(visible) returns pointerEvents: visible ? 'auto' : 'none' in addition to the existing display: visible ? 'flex' : 'none'"
    - "fullScreenOverlayWrap consumers (RenterListings at App.tsx:780, CreateListing at App.tsx:798, PropertyDetails wrapper at App.tsx:716-725) apply style-based pointerEvents tracking each overlay's visibility"
    - "hideMainStackUnderOverlay is derived from an OVERLAY_FLAGS array placed adjacent to state declarations (near App.tsx:76, after tabEverMounted), containing all 5 overlay conditions per D-09 amended"
    - "OVERLAY_FLAGS array explicitly includes !!activeTourUrl and !!activePhotosUrl even though Tour3D uses early-return — future-proofing per D-10 amended"
    - "Temporary [NAV] diagnostic logs are present at the 4 instrumentation sites per RESEARCH §2.1 to support C1 confirmation during the Plan 04 device re-run"
    - "CONVENTIONS.md has a new '## Keep-alive and overlay screens' subsection explaining the pattern and OVERLAY_FLAGS review checklist"
    - "No deprecated prop-form pointerEvents introduced — all new pointerEvents use the style form per RESEARCH §4.2"
  artifacts:
    - path: "App.tsx"
      provides: "Updated mainStackScreenStyle with pointerEvents; OVERLAY_FLAGS array + hideMainStackUnderOverlay derivation adjacent to state; pointerEvents on 3 overlay wrapper sites; 3 temporary [NAV] diagnostic logs"
      contains: "OVERLAY_FLAGS"
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "New '## Keep-alive and overlay screens' subsection with keep-alive pattern + overlay pattern + OVERLAY_FLAGS review-checklist note"
      contains: "## Keep-alive and overlay screens"
    - path: "src/components/BottomNavigator.tsx"
      provides: "Temporary [NAV] tab-tap-received log at the onPress handler entry"
      contains: "NAV] tab tap received"
  key_links:
    - from: "App.tsx:76 (end of tabEverMounted useState)"
      to: "App.tsx:~77 (new OVERLAY_FLAGS + hideMainStackUnderOverlay block)"
      via: "adjacent state-declaration placement per D-10"
      pattern: "OVERLAY_FLAGS = \\["
    - from: "App.tsx:525 (consumer of hideMainStackUnderOverlay)"
      to: "App.tsx:~77 (new derivation)"
      via: "unchanged variable name — consumer continues to work without edit"
      pattern: "hideMainStackUnderOverlay \\? 'none' : 'flex'"
    - from: "App.tsx:502-506 (mainStackScreenStyle helper)"
      to: "App.tsx:527, 545, 560, 571, 603, 622 (6 keep-alive tab wrappers)"
      via: "single helper edit propagates pointerEvents to all 6 consumers"
      pattern: "mainStackScreenStyle\\(show"
---

<objective>
Apply the belt-and-suspenders pointerEvents mitigation (D-05/D-06), refactor hideMainStackUnderOverlay to the OVERLAY_FLAGS single-source-of-truth shape (D-09/D-10 amended), install the temporary [NAV] diagnostic logs needed for Plan 04 to confirm or rule out C1, and document the convention in CONVENTIONS.md (D-07).

Purpose: NAV-01 + NAV-02. The pointerEvents change is always applied per D-05 because it is the highest-leverage/lowest-risk prevention of this class of bug. The OVERLAY_FLAGS refactor addresses C1 (the most likely root cause) and future-proofs against M2 overlays being added without re-hiding the main stack. The diagnostic logs are Plan 04's data source for the D-04 go/no-go gate.

Output: App.tsx changes + CONVENTIONS.md update + BottomNavigator.tsx log. Committed atomically per task per the wave-guidance protocol.

Critical ordering: This plan MUST NOT run before Plan 02's baseline commit has landed. Plan 02's commit SHA should be the parent of Plan 03's first commit (verified in Task 1 pre-check). Violating this order violates NAV-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-nav-reliability/01-CONTEXT.md
@.planning/phases/01-nav-reliability/01-RESEARCH.md
@.planning/phases/01-nav-reliability/01-PATTERNS.md
@.planning/phases/01-nav-reliability/01-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@App.tsx

<interfaces>
Exact current code at each edit site — copied verbatim from App.tsx via PATTERNS.md. Use these as match strings.

Edit site 1 — mainStackScreenStyle (App.tsx:502-506):

    const mainStackScreenStyle = (visible: boolean) =>
      ({
        flex: 1,
        display: visible ? 'flex' : 'none',
      }) as const;

Edit site 2 — hideMainStackUnderOverlay derivation (App.tsx:508-509):

    const hideMainStackUnderOverlay =
      !!selectedProperty || isRenterListingsOpen || (!!user && isCreateListingOpen);

Target placement for new derivation: after App.tsx:76 (end of tabEverMounted useState), before App.tsx:79 (first useEffect). Old lines 508-509 DELETED; consumer at App.tsx:525 continues to reference `hideMainStackUnderOverlay` by name.

Edit site 3 — RenterListings wrapper (App.tsx:780):

            <View style={[fullScreenOverlayWrap, { display: isRenterListingsOpen ? 'flex' : 'none' }]}>

Edit site 4 — CreateListing wrapper (App.tsx:798):

            <View style={fullScreenOverlayWrap}>

Edit site 5 — PropertyDetails wrapper (App.tsx:715-726):

          {selectedProperty && (
            <View
              style={{
                position: 'absolute',
                top: 0,
                left: 0,
                right: 0,
                bottom: 0,
                zIndex: 2,
                elevation: 4,
              }}
            >

Relevant useState sources (App.tsx:36-47, unchanged — referenced by OVERLAY_FLAGS):
- App.tsx:36 — selectedProperty
- App.tsx:37 — activeTourUrl
- App.tsx:38 — activePhotosUrl
- App.tsx:43 — isCreateListingOpen
- App.tsx:47 — isRenterListingsOpen
- App.tsx:33 — `const { user, loading } = useAuth();` (destructured — not useState)

mainStackScreenStyle consumers (verified via PATTERNS.md):
- App.tsx:527 Favorites (showFavorites)
- App.tsx:545 Appointments (showAppointments)
- App.tsx:560 AccountSettings (showAccountSettings)
- App.tsx:571 Profile (showProfile)
- App.tsx:603 Chat (showChat)
- App.tsx:622 Home (showHome)

CONVENTIONS.md insertion point: after the existing "## Styling Approach" section, before the trailing `---`. The pre-written block is in RESEARCH §4.4 — copy verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply pointerEvents to mainStackScreenStyle + 3 overlay wrappers (D-05/D-06)</name>
  <read_first>
    - App.tsx (verify current state of lines 502-506, 780, 798, 715-726 still matches PATTERNS.md quotes; if drift, pause and alert)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §4.1 (target shapes for each edit site)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §4.2 (style form vs prop form — use style form only)
    - .planning/phases/01-nav-reliability/01-PATTERNS.md edit sites 1, 3, 4 verbatim target shapes
  </read_first>
  <files>App.tsx</files>
  <action>
    Pre-check (abort if fails): Verify git history — the previous commit on this branch must be the Plan 02 "baseline" commit. Run `git log --oneline -5 -- .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` and confirm the most recent entry matches Plan 02 (message contains "baseline"). If not, stop and alert — NAV-03 ordering about to be violated.

    Edit A — mainStackScreenStyle at App.tsx:502-506. Replace the 4-field return object with a 5-field version that adds `pointerEvents: visible ? 'auto' : 'none'`:

    Before (verbatim):

        const mainStackScreenStyle = (visible: boolean) =>
          ({
            flex: 1,
            display: visible ? 'flex' : 'none',
          }) as const;

    After (verbatim):

        const mainStackScreenStyle = (visible: boolean) =>
          ({
            flex: 1,
            display: visible ? 'flex' : 'none',
            pointerEvents: visible ? 'auto' : 'none',
          }) as const;

    This single helper edit propagates pointerEvents to all 6 keep-alive tab wrappers (App.tsx:527, 545, 560, 571, 603, 622). Per RESEARCH §4.1 / PATTERNS "Edit site 1": no per-caller changes needed. The `as const` narrowing already handles the `'auto' | 'none'` literal type correctly.

    Edit B — RenterListings wrapper at App.tsx:780. Append a third style entry for pointerEvents:

    Before (verbatim):

                <View style={[fullScreenOverlayWrap, { display: isRenterListingsOpen ? 'flex' : 'none' }]}>

    After (verbatim):

                <View style={[
                  fullScreenOverlayWrap,
                  { display: isRenterListingsOpen ? 'flex' : 'none' },
                  { pointerEvents: isRenterListingsOpen ? 'auto' : 'none' },
                ]}>

    Per RESEARCH §4.1: RenterListings stays mounted while hidden via display:'none' (kept alive via renterListingsEverMounted), so pointerEvents MUST track isRenterListingsOpen — not be hardcoded.

    Edit C — CreateListing wrapper at App.tsx:798. Convert to style-array with `pointerEvents: 'auto'`:

    Before (verbatim):

                <View style={fullScreenOverlayWrap}>

    After (verbatim):

                <View style={[fullScreenOverlayWrap, { pointerEvents: 'auto' }]}>

    Per RESEARCH §4.1: CreateListing is conditionally mounted via `{!!user && isCreateListingOpen && (...)}` at line 797, so when the wrapper is in the tree it IS visible. Hardcoded `'auto'` is correct.

    Edit D — PropertyDetails wrapper at App.tsx:715-726. Add `pointerEvents: 'auto'` to the inline style object:

    Before (verbatim):

            {selectedProperty && (
              <View
                style={{
                  position: 'absolute',
                  top: 0,
                  left: 0,
                  right: 0,
                  bottom: 0,
                  zIndex: 2,
                  elevation: 4,
                }}
              >

    After (verbatim):

            {selectedProperty && (
              <View
                style={{
                  position: 'absolute',
                  top: 0,
                  left: 0,
                  right: 0,
                  bottom: 0,
                  zIndex: 2,
                  elevation: 4,
                  pointerEvents: 'auto',
                }}
              >

    Per PATTERNS "Edit site 4" and RESEARCH §4.1: PropertyDetails is conditionally mounted, always visible when rendered. Hardcoded `'auto'` is correct.

    Do NOT touch:
    - Tour3D (early-return at App.tsx:461-478 — no wrapper to gate)
    - Auth modal at App.tsx:864 (zIndex:1000 — EXPLICITLY out of scope per D-06; C3 territory only)
    - TourHeroCard.tsx:39 prop-form pointerEvents (orthogonal, unchanged per RESEARCH §5.1)

    After all four edits: run TypeScript check (`npx tsc --noEmit` or project-equivalent) to confirm no type errors. Commit atomically with message: `fix(01): apply pointerEvents belt-and-suspenders to keep-alive + overlay wrappers (NAV-01/NAV-02, D-05/D-06)`. Body explains: helper propagates to 6 keep-alive wrappers; overlay wrappers get style-based pointerEvents; Tour3D + auth modal unchanged (out of scope per D-06); style form only (prop form deprecated per RESEARCH §4.2).
  </action>
  <verify>
    <automated>grep -nF "pointerEvents: visible ? 'auto' : 'none'" App.tsx && grep -nF "pointerEvents: isRenterListingsOpen" App.tsx && grep -c "pointerEvents: 'auto'" App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nF "pointerEvents: visible ? 'auto' : 'none'" App.tsx` returns exactly one match on or near line 506
    - `grep -nF "pointerEvents: isRenterListingsOpen ? 'auto' : 'none'" App.tsx` returns exactly one match near line 780
    - `grep -c "pointerEvents: 'auto'" App.tsx` returns ≥ 2 (CreateListing line 798 + PropertyDetails inline style)
    - `grep -n 'pointerEvents="' App.tsx` returns zero matches (no prop-form introduced — style form only per RESEARCH §4.2)
    - `grep -c "pointerEvents" App.tsx` returns ≥ 4 (helper + 3 overlay sites, possibly more from style array forms)
    - `git diff HEAD~1 --name-only` shows ONLY `App.tsx` (single-file atomic commit)
    - `git log --oneline -1 -- App.tsx` returns a commit whose message contains "pointerEvents" and "belt-and-suspenders"
    - TypeScript compilation succeeds (no new type errors)
    - Satisfies RESEARCH §6 SC4 evidence predicate (App.tsx:502-506 diff shows pointerEvents in mainStackScreenStyle; overlay wrapper sites show style-based pointerEvents) and RESEARCH §8 item 6 (mainStackScreenStyle has pointerEvents) and item 7 (overlay wraps use style-based pointerEvents)
  </acceptance_criteria>
  <done>pointerEvents belt-and-suspenders applied at 4 edit sites via style form. Committed atomically. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor hideMainStackUnderOverlay to OVERLAY_FLAGS array (D-09/D-10 amended)</name>
  <read_first>
    - App.tsx (re-verify lines 32-76 state declarations unchanged; re-verify lines 508-509 derivation unchanged; re-verify line 525 consumer references hideMainStackUnderOverlay by name)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.1 "Fix if C1 confirmed" (exact OVERLAY_FLAGS code shape)
    - .planning/phases/01-nav-reliability/01-CONTEXT.md D-08/D-09/D-10 amended (Tour3D early-return interpretation — include !!activeTourUrl / !!activePhotosUrl for future-proofing)
    - .planning/phases/01-nav-reliability/01-PATTERNS.md edit site 2 (deletion + insertion placement)
  </read_first>
  <files>App.tsx</files>
  <action>
    Pre-read confirmation (per D-10 amended "planner MUST still include a short code-read sub-task in Wave 1 to reconfirm this list against the current App.tsx state variables"):

    Run: `grep -n "useState<.*null\|useState(false)\|useState(true)" App.tsx | head -30`. Verify the 5 overlay-relevant state declarations exist at the expected lines:
    - App.tsx:36 selectedProperty
    - App.tsx:37 activeTourUrl
    - App.tsx:38 activePhotosUrl
    - App.tsx:43 isCreateListingOpen
    - App.tsx:47 isRenterListingsOpen

    Also check if any NEW overlay state variables have been added since 2026-04-22 (look for additional `use*Open` booleans or `active*Url` strings between lines 32 and 76). If new ones exist, include them in OVERLAY_FLAGS alongside the canonical 5. If the 5 canonical ones have moved lines, adjust — but list content stays the same.

    Edit A — Insert OVERLAY_FLAGS block after App.tsx:76 (end of tabEverMounted useState), before App.tsx:79 (first useEffect). Insert this block immediately after the closing brace/paren of `setTabEverMounted` at line 76:

        // Single source of truth for "is the main stack currently eclipsed by a full-screen overlay?"
        // Adding a new overlay = add its flag here. Code-review checklist: if you added a new
        // full-screen overlay state, did you add it to OVERLAY_FLAGS?
        // activeTourUrl / activePhotosUrl currently drive Tour3D via early-return (App.tsx:461-478),
        // so they don't strictly need to be here today — included for future-proofing per
        // CONTEXT D-10 (amended 2026-04-22) so a future refactor to a zIndex overlay Just Works.
        const OVERLAY_FLAGS = [
          !!selectedProperty,
          isRenterListingsOpen,
          !!user && isCreateListingOpen,
          !!activeTourUrl,
          !!activePhotosUrl,
        ];
        const hideMainStackUnderOverlay = OVERLAY_FLAGS.some(Boolean);

    Note: the `user` variable is destructured from `useAuth()` at App.tsx:33, which is BEFORE line 76, so it is in scope at the insertion point. Same for the 5 useState declarations.

    Edit B — Delete the old derivation at App.tsx:508-509:

        const hideMainStackUnderOverlay =
          !!selectedProperty || isRenterListingsOpen || (!!user && isCreateListingOpen);

    (plus the blank line after if present to keep `fullScreenOverlayWrap` sitting one blank line below `as const;` of `mainStackScreenStyle`.)

    The consumer at App.tsx:525 (`<View style={{ flex: 1, display: hideMainStackUnderOverlay ? 'none' : 'flex' }}>`) continues to reference hideMainStackUnderOverlay by name unchanged.

    After both edits: run TypeScript check. Commit atomically with message: `refactor(01): hoist hideMainStackUnderOverlay to OVERLAY_FLAGS array adjacent to state (NAV-02, D-09/D-10)`. Body: replaces 3-term expression at App.tsx:508-509 with OVERLAY_FLAGS array placed adjacent to state declarations; explicitly includes !!activeTourUrl / !!activePhotosUrl for future-proofing; consumer at App.tsx:525 unchanged.
  </action>
  <verify>
    <automated>grep -nF "const OVERLAY_FLAGS = [" App.tsx && grep -c "!!activeTourUrl" App.tsx && grep -c "!!activePhotosUrl" App.tsx && grep -c "OVERLAY_FLAGS.some(Boolean)" App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nF "const OVERLAY_FLAGS = [" App.tsx` returns exactly one match
    - `grep -c "!!activeTourUrl" App.tsx` returns ≥ 1 (inside OVERLAY_FLAGS)
    - `grep -c "!!activePhotosUrl" App.tsx` returns ≥ 1 (inside OVERLAY_FLAGS)
    - First match line number for `grep -n "!!selectedProperty" App.tsx | head -1` is > 76 and < 100 (adjacent to state declarations, NOT at the old 508 location)
    - `grep -c "const hideMainStackUnderOverlay" App.tsx` returns exactly 1 and the single match line is `const hideMainStackUnderOverlay = OVERLAY_FLAGS.some(Boolean);`
    - `grep -c "hideMainStackUnderOverlay" App.tsx` returns exactly 2 (declaration + consumer at ~line 525)
    - `grep -cF "!!selectedProperty || isRenterListingsOpen || (!!user" App.tsx` returns 0 (old derivation deleted)
    - TypeScript compilation succeeds — `user`, `selectedProperty`, `isRenterListingsOpen`, `isCreateListingOpen`, `activeTourUrl`, `activePhotosUrl` all in scope at insertion point
    - `git log --oneline -1 -- App.tsx` returns a commit whose message contains "OVERLAY_FLAGS"
    - Satisfies RESEARCH §8 item 8 (App.tsx has OVERLAY_FLAGS array matching D-10 shape) and SC4 grep criterion (`grep "OVERLAY_FLAGS" App.tsx` returns the array definition)
  </acceptance_criteria>
  <done>OVERLAY_FLAGS array is the single source of truth for hideMainStackUnderOverlay; includes all 5 overlay conditions per D-09 amended. Committed atomically.</done>
</task>

<task type="auto">
  <name>Task 3: Add temporary [NAV] diagnostic logs at 4 instrumentation sites (C1 confirmation)</name>
  <read_first>
    - App.tsx (current state after Tasks 1+2; line numbers will have shifted by +~15 lines from OVERLAY_FLAGS insertion)
    - src/components/BottomNavigator.tsx (locate the tab onPress handler — its entry is the log site)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.1 "Instrumentation (temporary — strip per D-02)" for exact log strings
    - .planning/phases/01-nav-reliability/01-CONTEXT.md D-02 (logs are temporary; Plan 06 strips them)
  </read_first>
  <files>App.tsx, src/components/BottomNavigator.tsx</files>
  <action>
    Per RESEARCH §2.1 "Instrumentation", insert 4 temporary logs tagged `[NAV]` so Plan 06 can grep-strip them. All exact strings below are verbatim from RESEARCH §2.1.

    Log A — overlay-flags state dump. Insert immediately after the new `const hideMainStackUnderOverlay = OVERLAY_FLAGS.some(Boolean);` line (the OVERLAY_FLAGS block you inserted in Task 2):

        console.log('[NAV] overlay-flags', {
          selectedProperty: !!selectedProperty,
          isRenterListingsOpen,
          isCreateListingOpen,
          activeTourUrl: !!activeTourUrl,
          activePhotosUrl: !!activePhotosUrl,
          hide: hideMainStackUnderOverlay,
        });

    Per RESEARCH §2.1 this logs on every render — that is intentional for C1 evidence gathering.

    Log B — mainStack render log. Find the `<View style={{ flex: 1, display: hideMainStackUnderOverlay ? 'none' : 'flex' }}>` block (was at App.tsx:525 pre-refactor; shifted after Task 2). Insert immediately before this `<View>`:

        console.log('[NAV] mainStack render, hide=', hideMainStackUnderOverlay);

    Per RESEARCH §2.1 this fires once per render of the main-stack wrapper.

    Log C — BottomNavigator tab-tap-received log. Locate the onPress handler in `src/components/BottomNavigator.tsx` (grep for `onPress` within the tab Pressable/TouchableOpacity). At the TOP of the onPress handler body — before any existing logic — insert:

        console.log('[NAV] tab tap received', { tab, ts: Date.now() });

    If the handler signature is `onPress={(tab) => onTabChange(tab)}`, convert to an explicit block so the log can be added:

        onPress={(tab) => {
          console.log('[NAV] tab tap received', { tab, ts: Date.now() });
          onTabChange(tab);
        }}

    Match the existing bracket/indent style of BottomNavigator.tsx. If `tab` is bound differently (e.g., closed-over), adjust the object keys accordingly but keep the `[NAV] tab tap received` tag verbatim.

    Log D — tab handler entered log. In App.tsx, find the onTabChange callback region (RESEARCH §2.1 references App.tsx:697-712 but the line number will have shifted by Task 1+2 insertions). Grep: `grep -n "onTabChange\|handleTabChange" App.tsx`. At the TOP of whichever callback handles incoming tab changes (the one passed as prop to BottomNavigator), before any state-setter calls:

        console.log('[NAV] tab handler entered', tab);

    If the handler has multiple branches per tab, add the log at the entry of the function body, BEFORE the branching. One log per handler invocation — NOT one per branch.

    Do NOT strip existing unrelated logs. Do NOT add logs at any other location. The 4 logs above are exhaustive for C1 diagnosis.

    Build + smoke check: compile. Run the app briefly in dev (simulator is fine — we are confirming the logs emit, not testing the bug; the bug is tested on device in Plan 04). Tap one tab, verify the 4 log tags appear in Metro:
    - `[NAV] overlay-flags` (at least one line)
    - `[NAV] mainStack render, hide=` (at least one line)
    - `[NAV] tab tap received` (fires on the tap)
    - `[NAV] tab handler entered` (fires on the tap, after the above)

    Commit atomically with message: `chore(01): add temporary [NAV] diagnostic logs for C1 confirmation (RESEARCH §2.1, D-02 temporary)`. Body: lists the 4 log sites; notes all tagged `[NAV]` so Plan 06 can strip via the composite grep; notes temporary per CONTEXT D-02.
  </action>
  <verify>
    <automated>grep -c "\[NAV\]" App.tsx && grep -c "\[NAV\] tab tap received" src/components/BottomNavigator.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[NAV\]" App.tsx` returns ≥ 3 (overlay-flags + mainStack render + tab handler entered)
    - `grep -c "\[NAV\]" src/components/BottomNavigator.tsx` returns exactly 1 (tab tap received)
    - `grep -n "\[NAV\] overlay-flags" App.tsx` returns one match inside the OVERLAY_FLAGS block region
    - `grep -n "\[NAV\] mainStack render" App.tsx` returns one match near the `display: hideMainStackUnderOverlay` wrapper
    - `grep -n "\[NAV\] tab tap received" src/components/BottomNavigator.tsx` returns one match inside an onPress handler
    - `grep -n "\[NAV\] tab handler entered" App.tsx` returns one match inside an onTabChange-style callback
    - `grep -n "\[BACK\]\|\[C3\]\|\[C4\]" App.tsx src/components/BottomNavigator.tsx` returns ZERO (those tags reserved for Plan 05 C2/C3/C4 escalation, not this plan)
    - Metro/dev build compiles without errors
    - `git log --oneline -1` returns a commit message containing "[NAV]" or "diagnostic logs"
    - Pairs with Plan 06's strip task — `grep -n "\[NAV\]\|\[BACK\]\|\[C3\]\|\[C4\]" App.tsx src/components/BottomNavigator.tsx` returns zero AFTER Plan 06 runs (this plan's logs are exactly what Plan 06 must remove; no other log tags introduced)
  </acceptance_criteria>
  <done>4 [NAV] diagnostic logs installed at the exact sites RESEARCH §2.1 specifies, ready for Plan 04 device re-run to collect evidence for C1 go/no-go gate. Committed atomically.</done>
</task>

<task type="auto">
  <name>Task 4: Append "## Keep-alive and overlay screens" subsection to CONVENTIONS.md (D-07)</name>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (current state — verify the "## Styling Approach" section exists around line 230-256 and the trailing `---` around line 258 per PATTERNS.md)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §4.4 (the pre-written block to copy verbatim)
    - .planning/phases/01-nav-reliability/01-PATTERNS.md "CONVENTIONS.md" section (insertion point + house style)
    - .planning/phases/01-nav-reliability/01-CONTEXT.md D-07 (documentation-only convention, no abstraction component)
  </read_first>
  <files>.planning/codebase/CONVENTIONS.md</files>
  <action>
    Append a new `## Keep-alive and overlay screens` subsection AFTER the existing `## Styling Approach` section's closing content and BEFORE the first `---` rule that follows.

    Per PATTERNS.md CONVENTIONS.md analysis: the insertion point is immediately after the last bullet / content line of "## Styling Approach" (around line 256 in the current file) and before the `---` horizontal rule at ~line 258. If the file's line numbers have shifted, locate by content: find the last line of Styling Approach (likely a code block or the "Fonts" bullet per PATTERNS), then find the next `---`, and insert between them.

    Copy the following block verbatim (from RESEARCH §4.4 — already reviewed and house-style-compliant per PATTERNS). Insert exactly this block, at column 0 (no leading spaces), with one blank line before the new `## ` heading and one blank line after the last bullet of the new section (before the existing `---` rule):

````
## Keep-alive and overlay screens

Keep-alive (tab) screens and full-screen overlays in `App.tsx` MUST use BOTH `display`
and `pointerEvents` to gate visibility, not either one alone.

**Keep-alive tab pattern:**
```typescript
const mainStackScreenStyle = (visible: boolean) =>
  ({ flex: 1, display: visible ? 'flex' : 'none', pointerEvents: visible ? 'auto' : 'none' }) as const;
```

**Full-screen overlay pattern:**
```typescript
<View style={[fullScreenOverlayWrap, { pointerEvents: isOpen ? 'auto' : 'none' }]}>
```

**Why:** `display: 'none'` removes the view from the layout tree, but on RN's responder system
(both old and Fabric) an absolute-positioned wrapper or descendant can still capture touches
meant for the bottom nav. See `.planning/phases/01-nav-reliability/01-RESEARCH.md` §4 and
[RNav #12824](https://github.com/react-navigation/react-navigation/issues/12824). Use style-based
`pointerEvents` (not the deprecated prop form).

**On adding a new full-screen overlay:** add the overlay's visibility flag to the `OVERLAY_FLAGS`
array in `App.tsx` so `hideMainStackUnderOverlay` keeps working. Review checklist item.
````

    Commit atomically with message: `docs(01): add 'Keep-alive and overlay screens' convention (D-07)`. Body: documents the display + pointerEvents pattern applied in this phase; enforcement via code review — no reusable component abstraction per D-07; review-checklist note: new full-screen overlays MUST be added to OVERLAY_FLAGS.
  </action>
  <verify>
    <automated>grep -n "^## Keep-alive and overlay screens$" .planning/codebase/CONVENTIONS.md && grep -c "pointerEvents: visible" .planning/codebase/CONVENTIONS.md && grep -c "OVERLAY_FLAGS" .planning/codebase/CONVENTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## Keep-alive and overlay screens$" .planning/codebase/CONVENTIONS.md` returns exactly one match
    - `grep -c "pointerEvents: visible" .planning/codebase/CONVENTIONS.md` returns ≥ 1 (keep-alive tab pattern present)
    - `grep -c "OVERLAY_FLAGS" .planning/codebase/CONVENTIONS.md` returns ≥ 1 (review-checklist reference present)
    - `grep -n "## Styling Approach" .planning/codebase/CONVENTIONS.md` and `grep -n "^## Keep-alive and overlay screens$" .planning/codebase/CONVENTIONS.md` show the Keep-alive section comes AFTER Styling Approach in the file
    - `git log --oneline -1 -- .planning/codebase/CONVENTIONS.md` returns a commit whose message contains "Keep-alive" or "D-07"
    - Satisfies RESEARCH §6 SC4 evidence (`.planning/codebase/CONVENTIONS.md` contains the new `## Keep-alive and overlay screens` subsection) and RESEARCH §8 item 5 (`.planning/codebase/CONVENTIONS.md` has the new subsection)
  </acceptance_criteria>
  <done>CONVENTIONS.md has the new keep-alive-and-overlay convention documented, satisfying D-07. Committed atomically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan modifies App.tsx state-machine logic (pointerEvents + OVERLAY_FLAGS refactor), adds one log per file to two source files, and appends documentation. No new I/O, no new auth paths, no new user-input handling, no new network calls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | T (Tampering) | OVERLAY_FLAGS array | accept | Array is local to `AppContent` render; no external input reaches it. Values derived from useState / useAuth — same trust posture as the pre-refactor derivation at line 508-509. Refactor is a code reshape, not a trust-surface change. |
| T-01-03-02 | I (Information Disclosure) | [NAV] diagnostic logs | mitigate | Logs dump boolean overlay flags and tab IDs — no PII, no tokens, no user content. Temporary per D-02. Plan 06 strips all [NAV]/[BACK]/[C3]/[C4] tagged logs before phase gate. Acceptance criterion on Plan 06 Task 1 encodes the grep-verified strip. |
| T-01-03-03 | I | pointerEvents mitigation audit scope (auth modal at App.tsx:864 OUT OF SCOPE per D-06) | accept | Auth modal remains unchanged this wave. If Plan 04's re-run shows auth-modal-related capture (C3 territory), Plan 05 Task 3 owns that audit — its threat-model block includes a specific re-consideration of the auth modal's pointerEvents posture. Not a blocker for this plan. |

No S/R/D/E applicable — pointerEvents is a responder-system flag (not auth, not repudiation-relevant, not DoS surface, no elevation path). Block on: high severity only per config default — no high-severity threats raised.
</threat_model>

<verification>
- `grep -n "OVERLAY_FLAGS" App.tsx` shows the array definition
- `grep -n "pointerEvents" App.tsx` shows ≥ 4 style-form references (no prop-form)
- `grep -n "\[NAV\]" App.tsx` shows 3 logs; `grep -n "\[NAV\]" src/components/BottomNavigator.tsx` shows 1 log
- `grep -n "^## Keep-alive and overlay screens$" .planning/codebase/CONVENTIONS.md` shows the new section
- TypeScript compiles
- `git log --oneline App.tsx | head -4` shows 3 atomic commits from this plan (Tasks 1, 2, 3)
- `git log --oneline .planning/codebase/CONVENTIONS.md | head -1` shows Task 4 commit
</verification>

<success_criteria>
- NAV-01 / NAV-02 progress: belt-and-suspenders pointerEvents + OVERLAY_FLAGS refactor in place; app still compiles and runs
- [NAV] diagnostic logs installed (data source for Plan 04 go/no-go gate)
- D-07 convention documented
- All 4 changes committed atomically
- Plan 04 (Wave 2 device re-run gate) can now run
</success_criteria>

<output>
After completion, create `.planning/phases/01-nav-reliability/01-03-SUMMARY.md`:
- Enumerate the 4 atomic commits + their SHAs
- Confirm TypeScript compiled clean
- Note pre-check outcome: Plan 02 baseline commit was indeed the parent
- List the 4 [NAV] log sites (line numbers after refactor) so Plan 04 knows where to look for evidence
- Confirm: Tour3D early-return, auth modal zIndex:1000, and TourHeroCard prop-form pointerEvents all remain untouched per D-06
</output>
