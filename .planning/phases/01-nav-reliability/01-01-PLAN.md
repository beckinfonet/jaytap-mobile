---
phase: 01-nav-reliability
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md
  - .planning/phases/01-nav-reliability/videos/.gitkeep
autonomous: false
requirements: [NAV-03]
requirements_addressed: [NAV-03]
tags: [react-native, navigation, qa-matrix, validation-scaffolding]

must_haves:
  truths:
    - "Reproduction matrix document exists at .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md with skeleton sections Baseline / Post-Wave-1 / Post-Wave-N"
    - "videos/ directory exists under the phase dir with a .gitkeep anchor so the dir is tracked"
    - "Human has confirmed exact iOS device/OS version and Android device/OS version and recorded them at top of matrix"
    - "At least one Railway test listing is confirmed to have ≥1 Matterport tour so S9 / HR-4 / HR-5 cells are runnable"
    - "Matrix skeleton has all 9 starting-state rows × 5 target-tab columns = 45 cells × 2 platforms = 90 cells stubbed with placeholder markers"
  artifacts:
    - path: ".planning/phases/01-nav-reliability/01-REPRO-MATRIX.md"
      provides: "Baseline / Post-Wave-1 / Post-Wave-N tables + HR-1..HR-6 video-evidence list + device header"
      contains: "Baseline (pre-fix)|Post-Wave-1|Post-Wave-N|High-risk transitions"
    - path: ".planning/phases/01-nav-reliability/videos/.gitkeep"
      provides: "git-tracked anchor for the video-evidence directory"
  key_links:
    - from: ".planning/phases/01-nav-reliability/01-REPRO-MATRIX.md"
      to: ".planning/phases/01-nav-reliability/videos/"
      via: "relative path references in HR-1..HR-6 video evidence lines"
      pattern: "videos/\\{platform\\}-S[0-9]+-T[0-9]+\\.mp4"
---

<objective>
Create the validation scaffolding the rest of Phase 1 depends on: the reproduction-matrix document (NAV-03's satisfying artifact), the videos/ directory for evidence, and recorded agreement on which physical devices will be used. NO App.tsx edits in this plan — the matrix must exist in-repo before any touch-handling change lands (per NAV-03 "recorded before the fix").

Purpose: Establish a pre-fix evidence baseline so regressions are detectable later. This is the scaffolding wave; the human does not run the matrix yet — that is Plan 02.

Output: 01-REPRO-MATRIX.md skeleton + videos/.gitkeep + device-identification header filled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-nav-reliability/01-CONTEXT.md
@.planning/phases/01-nav-reliability/01-RESEARCH.md
@.planning/phases/01-nav-reliability/01-PATTERNS.md
@.planning/phases/01-nav-reliability/01-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- No code interfaces in this plan — pure markdown + empty-dir scaffolding. -->
<!-- The matrix skeleton is authoritative per RESEARCH §3.9 (lines 499-527) — copy verbatim. -->

Matrix skeleton structure (authoritative, from RESEARCH §3.9):
- H1: "# Phase 1: Bottom-Nav Reproduction Matrix"
- Metadata: **Baseline captured:** {date}, commit {sha-pre-wave-1} | **Platforms:** iOS {device/ver}, Android {device/ver}
- ## Baseline (pre-fix) with iOS + Android 9x5 tables
- ## Post-Wave-1 (pointerEvents + OVERLAY_FLAGS)
- ## Post-Wave-N (if C2/C3/C4 escalation reached)
- ### High-risk transitions (HR-1…HR-6) — video-evidenced

Starting states (rows, from RESEARCH §3.2):
- S1 Home | S2 Favorites | S3 Chat | S4 Profile | S5 Appointments
- S6 PropertyDetails overlay | S7 CreateListing overlay | S8 RenterListings overlay | S9 Tour3D active

Target tabs (columns, from RESEARCH §3.3):
- T1 Home | T2 Favorites | T3 Add (opens CreateListing overlay) | T4 Chat | T5 Profile

Cell markers (from RESEARCH §3.7):
- ✅ PASS | ❌ FAIL | — N/A | ? FLAKY

High-risk transitions (RESEARCH §3.5):
- HR-1: S6 → any tab (PropertyDetails close)
- HR-2: S7 → any tab (CreateListing close)
- HR-3: S8 → any tab (RenterListings close)
- HR-4: S9 → (back) → tap tab (Tour3D teardown)
- HR-5: Nested S6 → Tour3D → back → S6 → back → tap tab
- HR-6: Any state immediately after Android hardware back
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create videos/ directory with .gitkeep anchor</name>
  <read_first>
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §3.7 (Evidence format — video storage path)
    - .planning/phases/01-nav-reliability/01-PATTERNS.md "videos/ directory + .gitkeep" section
  </read_first>
  <files>.planning/phases/01-nav-reliability/videos/.gitkeep</files>
  <action>
    Create the directory `.planning/phases/01-nav-reliability/videos/` and add an empty `.gitkeep` file to anchor it in git. Per RESEARCH §3.7 video evidence is stored here at paths like `videos/ios-S6-T1.mp4` / `videos/android-S6-T1.mp4`. Per PATTERNS "videos/ directory" section, no analog exists in the repo — use the standard empty-.gitkeep convention.

    Run: `mkdir -p .planning/phases/01-nav-reliability/videos && touch .planning/phases/01-nav-reliability/videos/.gitkeep`.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-nav-reliability/videos/.gitkeep && echo "GITKEEP_EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/01-nav-reliability/videos/.gitkeep` exits 0
    - `ls -la .planning/phases/01-nav-reliability/videos/` shows `.gitkeep` present
    - `git status .planning/phases/01-nav-reliability/videos/.gitkeep` shows the file is trackable (new file, not ignored)
  </acceptance_criteria>
  <done>videos/ directory exists and is tracked by git via .gitkeep.</done>
</task>

<task type="auto">
  <name>Task 2: Create 01-REPRO-MATRIX.md skeleton with all 90 cells stubbed</name>
  <read_first>
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §3 (full section: §3.1 structure, §3.2 starting states, §3.3 target tabs, §3.5 high-risk, §3.6 pass/fail predicate, §3.7 evidence format, §3.9 skeleton template)
    - .planning/phases/01-nav-reliability/01-PATTERNS.md "01-REPRO-MATRIX.md (phase doc, CREATE)" section (structural conventions)
    - .planning/phases/01-nav-reliability/01-CONTEXT.md (confirm S9 Tour3D uses early-return, affects cell N/A marking)
  </read_first>
  <files>.planning/phases/01-nav-reliability/01-REPRO-MATRIX.md</files>
  <action>
    Create `.planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` following the RESEARCH §3.9 skeleton verbatim, expanded to cover all 9 starting states × 5 target tabs × 2 platforms = 90 cells. Match PATTERNS document-structure conventions (H1 title pattern `# Phase 1: <subject>`, bold-labeled metadata block, H2 section headings, pipe-delimited tables).

    Document structure (produce exactly this):

    ```markdown
    # Phase 1: Bottom-Nav Reproduction Matrix

    **Baseline captured:** TBD (fill at Wave 0.5 commit time), commit TBD (SHA of Wave 0.5 baseline commit)
    **Platforms:** iOS TBD (device/OS — fill from device agreement), Android TBD (device/OS — fill from device agreement)
    **Test listing with ≥1 Matterport tour:** TBD (listing ID / title — confirm with human)

    ## Starting states (rows)

    | ID | State | How to reach |
    |----|-------|--------------|
    | S1 | Home tab | Cold-start → Home |
    | S2 | Favorites tab | Home → tap Favorites in BottomNavigator |
    | S3 | Chat tab | Home → tap Chat |
    | S4 | Profile tab | Home → tap Profile |
    | S5 | Appointments screen | Profile → Appointments |
    | S6 | PropertyDetails overlay open (selectedProperty ≠ null, zIndex:2) | Home → tap property card |
    | S7 | CreateListing overlay open (full-screen zIndex:3) | Profile → Create Listing |
    | S8 | RenterListings overlay open (full-screen zIndex:3) | Profile → View My Listings |
    | S9 | Tour3D active (early-return, activeTourUrl ≠ null) | PropertyDetails → Open Tours → single tour |

    ## Target tabs (columns)

    | T1 Home | T2 Favorites | T3 Add (opens CreateListing) | T4 Chat | T5 Profile |

    ## Cell markers

    ✅ PASS · ❌ FAIL · — N/A · ? FLAKY

    ## Baseline (pre-fix)

    ### iOS

    | From \ To | T1 Home | T2 Favorites | T3 Add | T4 Chat | T5 Profile |
    |-----------|---------|--------------|--------|---------|------------|
    | S1 Home | — N/A | ⬜ | ⬜ | ⬜ | ⬜ |
    | S2 Favorites | ⬜ | — N/A | ⬜ | ⬜ | ⬜ |
    | S3 Chat | ⬜ | ⬜ | ⬜ | — N/A | ⬜ |
    | S4 Profile | ⬜ | ⬜ | ⬜ | ⬜ | — N/A |
    | S5 Appointments | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
    | S6 PropertyDetails | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
    | S7 CreateListing | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
    | S8 RenterListings | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
    | S9 Tour3D | ⬜ | ⬜ | — N/A | ⬜ | ⬜ |

    ### Android

    {Copy the same 9-row × 5-col table shape; cells ⬜ pending}

    ### High-risk transitions (HR-1…HR-6) — video-evidenced

    - HR-1 iOS (S6 PropertyDetails → T1..T5): ⬜ — video: videos/ios-S6-T{n}.mp4 — notes: TBD
    - HR-1 Android (S6 PropertyDetails → T1..T5): ⬜ — video: videos/android-S6-T{n}.mp4 — notes: TBD
    - HR-2 iOS (S7 CreateListing → T1..T5): ⬜ — video: videos/ios-S7-T{n}.mp4 — notes: TBD
    - HR-2 Android: ⬜ — video: videos/android-S7-T{n}.mp4 — notes: TBD
    - HR-3 iOS (S8 RenterListings → T1..T5): ⬜ — video: videos/ios-S8-T{n}.mp4 — notes: TBD
    - HR-3 Android: ⬜ — video: videos/android-S8-T{n}.mp4 — notes: TBD
    - HR-4 iOS (S9 Tour3D → back → tab): ⬜ — video: videos/ios-S9-back-T{n}.mp4 — notes: TBD
    - HR-4 Android: ⬜ — video: videos/android-S9-back-T{n}.mp4 — notes: TBD
    - HR-5 iOS (Nested S6 → Tour3D → back → S6 → back → tab): ⬜ — video: videos/ios-HR5-T{n}.mp4 — notes: TBD
    - HR-5 Android: ⬜ — video: videos/android-HR5-T{n}.mp4 — notes: TBD
    - HR-6 iOS (Post Android-back state → tab): — N/A (iOS has no hardware back)
    - HR-6 Android (Post hardware-back state → tab): ⬜ — video: videos/android-HR6-T{n}.mp4 — notes: TBD

    ## Pass/fail predicate (from RESEARCH §3.6)

    **PASS** for (Sn, Tm): within 1 s of tapping Tm, the app visually shows Tm's content (or overlay opens for T3); BottomNavigator remains responsive to a subsequent tap.

    **FAIL**: Tm's content does NOT appear within 1 s; OR tap registers (ripple) but no state change; OR tap appears ignored (no ripple, no state change); OR new tab shows but old overlay content is still visible beneath / receives taps.

    ## Post-Wave-1 (pointerEvents + OVERLAY_FLAGS)

    _Filled after Plan 03 lands and Plan 04 runs. Same 9×5 table shape × 2 platforms, plus HR-1..HR-6 re-run._

    ## Post-Wave-N (if C2/C3/C4 escalation reached)

    _Filled only if Wave 2 gate fails and escalation plans run. Otherwise labeled "Not reached — Wave 1 achieved 0% repro"._
    ```

    Use `⬜` as the pending-cell marker (matches the PATTERNS "✅ PASS · ❌ FAIL · — N/A · ? FLAKY" convention with an explicit pending state). Mark cells that are architecturally N/A per RESEARCH §3.2 note (S9 → T3 Add, and self-transitions like S1→T1) exactly as shown above.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md && grep -c "^| S[0-9]" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` exits 0
    - `grep -c "^## " .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` returns ≥ 6 (sections: Starting states, Target tabs, Cell markers, Baseline, Post-Wave-1, Post-Wave-N, Pass/fail predicate)
    - `grep -c "HR-[1-6]" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` returns ≥ 11 (6 HR IDs, each appearing at least on iOS + Android rows except HR-6-iOS which is N/A)
    - `grep -c "^| S[1-9]" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` returns ≥ 18 (9 rows × 2 platforms)
    - `grep "Baseline captured.*TBD" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` finds the placeholder line (Plan 02 will fill it)
    - `grep "videos/ios-\|videos/android-" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md | wc -l` returns ≥ 10 (HR video-path references exist)
  </acceptance_criteria>
  <done>Matrix skeleton exists with 9 starting-state rows × 5 target-tab columns × 2 platforms = 90 cells stubbed (all ⬜ pending, with architectural N/A cells pre-marked), HR-1..HR-6 lines present with video path placeholders, and device/commit header placeholders present for Plan 02 to fill.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Device agreement + Railway tour-listing check</name>
  <what-built>
    - videos/ directory tracked
    - 01-REPRO-MATRIX.md skeleton with device/commit placeholders
    Ready to fill with the human's device identifiers and confirm a Railway listing with ≥1 tour exists.
  </what-built>
  <how-to-verify>
    Human, please confirm the following and edit `01-REPRO-MATRIX.md` header in place (or tell the executor which values to write):

    1. **iOS test device**: specific model and iOS version you will run the 45-cell matrix on. Example: "iPhone 14 Pro, iOS 17.4.1". Per RESEARCH §11 and §10 Q1, this is release-blocking — if no iOS device is available, Phase 1 cannot exit.

    2. **Android test device**: specific model and Android version. Example: "Pixel 7, Android 14". Same release-blocking constraint.

    3. **Railway test listing with ≥1 Matterport tour**: Browse the app on either device → find a listing that has a working 3D tour. Note its title/ID. Per RESEARCH §10 Q3, this is required for S9 / HR-4 / HR-5 cells to be runnable. If NO listing has a tour, seed one (add a Matterport URL to any test listing) before proceeding.

    Edit the 3 TBD placeholder lines at the top of `01-REPRO-MATRIX.md`:
    - `**Platforms:** iOS {device}/{version}, Android {device}/{version}`
    - `**Test listing with ≥1 Matterport tour:** {listing title or ID}`

    Leave the `commit TBD` placeholder — that is filled by Plan 02 when the baseline is committed.
  </how-to-verify>
  <acceptance_criteria>
    - `grep "iOS TBD\|Android TBD" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` returns zero matches (all three TBDs on the device + listing lines are filled)
    - `grep "Test listing with ≥1 Matterport tour:" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md | grep -v TBD` returns one line (the filled value)
    - Human has signed off in chat: "Both devices confirmed, tour-listing present" (required resume-signal)
    - `commit TBD` still present on the `**Baseline captured:**` line (Plan 02's responsibility, not this plan's)
  </acceptance_criteria>
  <resume-signal>Type "devices confirmed: iOS {model/ver}, Android {model/ver}, tour listing {id/title}" or describe blockers (missing device / no tour listing).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan only creates documentation scaffolding and an empty directory.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | I (Information Disclosure) | videos/ directory | accept | Videos captured later (Plan 02) may contain app screen content (Bishkek property listings, chat UI). Content is non-PII (public listings) and non-sensitive (no auth screens should be recorded during matrix runs per RESEARCH §3.5 HR transitions — auth flow is out of Phase 1 scope per CONTEXT D-06). Risk is LOW. If a recording inadvertently shows a logged-in profile screen with email, human reviews and removes before commit. |

No S/T/R/D/E applicable — no code, no network I/O, no auth paths, no user input processing.
</threat_model>

<verification>
- `test -f .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` exits 0
- `test -f .planning/phases/01-nav-reliability/videos/.gitkeep` exits 0
- Grep of matrix skeleton confirms 9 rows × 5 cols × 2 platforms structure and HR-1..HR-6 references
- Device header placeholders filled by human checkpoint
</verification>

<success_criteria>
- Matrix skeleton in place with all 90 cells stubbed
- videos/ dir created and tracked
- Device agreement captured in matrix header
- Railway tour-listing confirmation captured
- NO `App.tsx` changes in this plan (verified: `git diff --name-only HEAD` after this plan lands shows only `.planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` and `.planning/phases/01-nav-reliability/videos/.gitkeep` — this is a prerequisite for NAV-03's "before the fix" ordering)
</success_criteria>

<output>
After completion, create `.planning/phases/01-nav-reliability/01-01-SUMMARY.md` documenting:
- Matrix skeleton path + row/column counts
- Device identifiers recorded in header
- Railway tour-listing reference captured
- Explicit statement: "NO App.tsx edits in this plan — NAV-03 ordering preserved"
</output>
