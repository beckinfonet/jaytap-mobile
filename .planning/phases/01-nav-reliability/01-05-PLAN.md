---
phase: 01-nav-reliability
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - App.tsx
  - src/components/BottomNavigator.tsx
  - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md
  - .planning/phases/01-nav-reliability/videos/
autonomous: false
requirements: [NAV-01, NAV-02]
requirements_addressed: [NAV-01, NAV-02]
tags: [react-native, navigation, escalation, diagnostics, backhandler, touch-capture]

must_haves:
  truths:
    - "This plan runs ONLY if Plan 04 Task 3 decision was B (ESCALATE-TO-WAVE-3 unambiguous). Decisions A/C/D skip this plan entirely."
    - "Each candidate (C2, C3, C4) is a self-contained diagnostic→fix→re-verify cycle with its own go/no-go gate; candidates run sequentially in order C2 → C3 → C4"
    - "Every diagnostic code change is committed atomically and gated behind a device re-run BEFORE the next candidate begins — no stacking unverified fixes"
    - "Diagnostic logs for each candidate use the candidate's tag ([BACK] for C2, [C3] for C3, [C4] for C4) so Plan 06 can strip all diagnostic tags in a single grep"
    - "If C2 + C3 + C4 all fail to fix, D-04 'pause, reassess, re-discuss' triggers with the human — this plan does NOT silently give up"
  artifacts:
    - path: "App.tsx"
      provides: "(If C2 confirmed) BackHandler useEffect refactored to ref-pattern (RESEARCH §2.2 fix). (If C3 confirmed) auth-modal-wrapper or other specific wrapper gets style-based pointerEvents. (If C4 confirmed) keep-alive unmount timer — but C4 triggers D-04 pause-and-reassess since it redesigns keep-alive semantics."
      contains: "conditional — see per-task acceptance"
    - path: ".planning/phases/01-nav-reliability/01-REPRO-MATRIX.md"
      provides: "Post-Wave-N section (N = 2, 3, 4 for each candidate that runs) with per-candidate device re-run results"
      contains: "## Post-Wave-N"
  key_links:
    - from: "Plan 04 Decision B"
      to: "Plan 05 Task 1 (C2 diagnostic)"
      via: "orchestrator routes to Plan 05 ONLY after explicit Decision B recorded in matrix"
      pattern: "Next plan to run: Plan 05 Task 1"
    - from: "Each candidate's device re-run result"
      to: "next candidate (if FAIL) or Plan 06 (if PASS)"
      via: "per-candidate go/no-go gate inside each task"
      pattern: "Post-Wave-N decision line"
---

<objective>
Sequential escalation through C2 → C3 → C4 per RESEARCH §2.2 / §2.3 / §2.4. Each candidate follows the cycle:
1. Add candidate-specific diagnostic instrumentation ([BACK] / [C3] / [C4] tags)
2. Human runs device repro to collect evidence
3. Evaluate candidate's confirmed/ruled-out predicate
4. If confirmed: apply the candidate's fix (per RESEARCH §2.x "Fix if Cx confirmed")
5. Human re-runs matrix; record Post-Wave-N results
6. If 0% repro: proceed to Plan 06 (strip + finalize)
7. If still FAIL: advance to next candidate
8. If all 3 candidates fail: D-04 pause-and-reassess

Purpose: NAV-01 + NAV-02. This plan exists because Plan 03's C1-targeted fix + belt-and-suspenders was insufficient. Each candidate is architecturally distinct — C2 is BackHandler stale closure (Android-only), C3 is invisible-wrapper touch capture (cross-platform), C4 is idle-touchable RN bug (cross-platform, lifecycle-dependent).

IMPORTANT: This plan is marked `autonomous: false`. The orchestrator (/gsd-execute-phase) must NOT auto-run it. It runs ONLY if Plan 04 Task 3 decision was B. Decisions A (clean fix) / D (zero-baseline) skip to Plan 06. Decision C (D-04 reassess) blocks this plan until explicit human re-authorization with a revised hypothesis.

Output: One or more diagnostic + fix cycles landed, each with its own device re-run recorded in the matrix. Either 0% repro achieved (proceed to Plan 06) or all candidates exhausted (trigger D-04 pause-and-reassess).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-nav-reliability/01-CONTEXT.md
@.planning/phases/01-nav-reliability/01-RESEARCH.md
@.planning/phases/01-nav-reliability/01-PATTERNS.md
@.planning/phases/01-nav-reliability/01-VALIDATION.md
@.planning/phases/01-nav-reliability/01-REPRO-MATRIX.md
@.planning/phases/01-nav-reliability/01-04-SUMMARY.md
@App.tsx
@src/components/BottomNavigator.tsx

<interfaces>
C2 — 23-dep BackHandler useEffect stale closure (RESEARCH §2.2):
- Location: App.tsx:184-297 (pre-Plan-03 line numbers; shifted +~22 lines after Plan 03 Tasks 2 and 3). Grep current location: `grep -n "BackHandler.addEventListener" App.tsx`
- Android-only (Platform.OS !== 'android' early-return)
- Confirmed predicate: Android-only repro + [BACK] handler effectCount shows stale value + handler returns true when should return false
- Fix: ref-pattern refactor — register-once effect with empty deps + mirror effect updates a ref every render (full code in RESEARCH §2.2 "Fix if C2 confirmed")
- Regression risk per RESEARCH §5.2: 20 branches in handler body; back-nav order must be preserved; returnToProfileAfter* paths most subtle; 12-point Android back-regression checklist MUST run

C3 — Absolute-positioned wrapper captures touches invisibly (RESEARCH §2.3):
- Target under C3 if reached: the auth modal at App.tsx:864 (zIndex:1000) — explicitly OUT OF SCOPE in Wave 1 per D-06; now in-scope because Wave 1 didn't fix it
- Confirmed predicate: colored border visible over BottomNavigator when should be gone; OR [C3] wrapper captured touch fires on bottom-nav taps
- Fix: augment auth modal wrapper style with pointerEvents tracking the 4 auth-modal visibility flags (RESEARCH §2.3 fix-if-C3-confirmed)

C4 — Idle-touchable retention (RESEARCH §2.4):
- Upstream: facebook/react-native#36710, unresolved in RN 0.84
- Confirmed predicate: onPressIn + onPressOut fire but onPress does not; appears after ~5+ min sustained use
- Fix changes keep-alive semantics (removeClippedSubviews + unmount timer), causing user-visible state loss
- RESEARCH §2.4 explicitly notes: if C4 confirms, D-04 "pause, reassess" applies — Phase 1 scope has expanded into keep-alive redesign. This plan does NOT auto-apply the C4 fix; it pauses for human re-scoping.

Strip-pairing: the composite grep `grep -n "NAV\]\|BACK\]\|C3\]\|C4\]" App.tsx src/components/BottomNavigator.tsx` must return zero AFTER Plan 06's strip task — this plan's logs feed into that strip list.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Route gate — run ONLY if Plan 04 Decision was B</name>
  <what-built>
    Plan 04's Go/No-Go Decision block in the matrix. Before this plan runs any diagnostic, confirm routing.
  </what-built>
  <read_first>
    - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md (the Post-Wave-1 Go/No-Go Decision section)
    - .planning/phases/01-nav-reliability/01-04-SUMMARY.md (orchestrator routing line)
  </read_first>
  <decision>
    Read the matrix's "Next plan to run:" line. Classify:

    - If "Plan 05 Task 1 (C2 diagnostic)" → PROCEED with Task 1 below.
    - If "Plan 06" → STOP this plan; it should not have been routed here. Ask the orchestrator to re-evaluate.
    - If "HOLD — reassess required" → STOP. Signal the human: "Plan 05 is gated on D-04 reassess outcome. What is the revised hypothesis?"
    - If the line is missing or ambiguous → STOP. Signal for manual routing.
  </decision>
  <options>
    <option id="proceed-c2">
      <name>Proceed to Task 1 (C2 diagnostic)</name>
      <pros>Correct routing per Plan 04 Decision B.</pros>
      <cons>None.</cons>
    </option>
    <option id="stop-misrouted">
      <name>Stop — misrouted (Decision was A/D)</name>
      <pros>Prevents unnecessary diagnostic churn.</pros>
      <cons>None.</cons>
    </option>
    <option id="stop-reassess">
      <name>Stop — D-04 reassess required (Decision was C)</name>
      <pros>Honors D-04 pause.</pros>
      <cons>Human must re-scope before this plan resumes.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Route decision is recorded in chat or in a comment at the top of the matrix
    - If route is "stop-misrouted" or "stop-reassess", no code changes land in this plan run — exit cleanly
    - If route is "proceed-c2", Task 1 begins
  </acceptance_criteria>
  <resume-signal>Select: option-proceed-c2 or option-stop-misrouted or option-stop-reassess</resume-signal>
</task>

<task type="auto">
  <name>Task 1: C2 diagnostic — add [BACK] instrumentation to BackHandler useEffect</name>
  <read_first>
    - App.tsx (current location of BackHandler useEffect — grep for "BackHandler.addEventListener")
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.2 "Instrumentation" for exact [BACK] log strings
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.2 "Repro trigger" (Android-only sequence)
  </read_first>
  <files>App.tsx</files>
  <action>
    Locate the BackHandler useEffect. Pre-Plan-03 it was at App.tsx:184-297; line numbers will have shifted by ~22 lines due to Plan 03 insertions. Grep: `grep -n "BackHandler.addEventListener" App.tsx` to find the current location.

    Insert 3 temporary [BACK] logs per RESEARCH §2.2 (exact strings, verbatim):

    At the TOP of the useEffect body (immediately after the `useEffect(() => {` opening):

        const __backEffectCount = (window as any).__backEffectCount = ((window as any).__backEffectCount ?? 0) + 1;
        console.log('[BACK] effect register #', __backEffectCount);

    Inside the `onBackPress = () => {` handler body (immediately after the opening brace):

        console.log('[BACK] handler called, deps snapshot:', {
          showResetPasswordModal, showForgotPasswordModal, showLoginModal, showSignupModal,
          showAuthPrompt, propertyForTourSelection: !!propertyForTourSelection,
          activeTourUrl: !!activeTourUrl, activePhotosUrl: !!activePhotosUrl,
          isRenterListingsOpen, ownerListingsUid: !!ownerListingsUid,
          isCreateListingOpen, selectedProperty: !!selectedProperty,
          isScheduleViewingOpen, isFavoritesOpen, isAppointmentsOpen,
          isProfileOpen, isChatOpen,
          effectCount: __backEffectCount,
        });

    Immediately before `return () => sub.remove()` (the cleanup return):

        console.log('[BACK] effect cleanup #', __backEffectCount);

    All state variable names in the deps-snapshot object are names currently in scope in that effect (verified against RESEARCH §2.2 which referenced the actual dep array at App.tsx:277-297).

    TypeScript check. Commit atomically with message: `chore(01): add [BACK] diagnostic logs for C2 confirmation (RESEARCH §2.2)`. Body notes: 3 logs tagged [BACK]; Android-only behavior checked via Platform.OS guard; temporary per D-02, stripped by Plan 06.
  </action>
  <verify>
    <automated>grep -c "BACK\]" App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BACK\]" App.tsx` returns ≥ 3 (effect register, handler called, effect cleanup)
    - `grep -n "BACK\] effect register" App.tsx` returns one match at TOP of BackHandler useEffect
    - `grep -n "BACK\] handler called" App.tsx` returns one match inside onBackPress body
    - `grep -n "BACK\] effect cleanup" App.tsx` returns one match immediately before `return () => sub.remove()`
    - TypeScript compilation succeeds
    - `git log --oneline -1 -- App.tsx` returns a commit whose message contains "[BACK]" or "C2"
  </acceptance_criteria>
  <done>3 [BACK] logs installed. Plan 05 Task 2 (human device run) can now proceed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: C2 device re-run — human collects [BACK] logs on Android + evaluates predicate</name>
  <what-built>
    [BACK] instrumentation installed. Build + install on Android device.
  </what-built>
  <how-to-verify>
    On your Android device (iOS is irrelevant for C2 — Platform.OS guards the effect):

    1. Build fresh from HEAD; install on Android device.

    2. Open Metro / adb logcat with `adb logcat | grep BACK` to filter.

    3. Run the C2 repro trigger sequence from RESEARCH §2.2:
       - Cold-start, land on Home
       - Open Profile tab (state change — expect one [BACK] effect register increment)
       - Open Favorites from Profile (state change — another increment)
       - Confirm [BACK] effect register # increments on each state change
       - Open the system keyboard in any text input
       - Tap BACK
       - Tap a bottom-nav tab in quick succession (<500 ms)
       - If the tab fails to change: inspect the last [BACK] handler called log — compare effectCount there to the current __backEffectCount. If they differ, the back-handler fired with a stale closure.

    4. Save the log output: `adb logcat | grep BACK > .planning/phases/01-nav-reliability/videos/android-c2-logs.txt` while reproducing.

    5. Evaluate the C2 confirmed predicate (RESEARCH §2.2):
       - CONFIRMED: Android-only repro + [BACK] handler called log shows effectCount lower than current __backEffectCount + the stale call returned true when should return false
       - RULED-OUT: Bug reproduces on iOS (C2 is Android-only — cannot be); OR Android-only but [BACK] handler called never fires during the miss (tap didn't go through BackHandler); OR handler fires with fresh state and returns false correctly but nav still traps

    6. Record the evaluation in the matrix as a new subsection titled `## C2 Diagnostic — Evidence Review`:

        ## C2 Diagnostic — Evidence Review

        **Log file:** videos/android-c2-logs.txt ({N} lines)
        **Evaluation:** CONFIRMED or RULED-OUT
        **Specific evidence:** {one-sentence quote or paraphrase of the decisive log lines}
        **Decision:** Apply C2 fix (Task 3) OR skip to C3 (Task 5)
  </how-to-verify>
  <read_first>
    - App.tsx (state of [BACK] logs from Task 1)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.2 "Repro trigger" + "Confirmed predicate" + "Ruled-out predicate"
  </read_first>
  <acceptance_criteria>
    - `test -f .planning/phases/01-nav-reliability/videos/android-c2-logs.txt` exits 0
    - `grep -c "BACK\]" .planning/phases/01-nav-reliability/videos/android-c2-logs.txt` returns ≥ 10
    - `grep "C2 Diagnostic — Evidence Review" .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` returns one match
    - `grep "^\*\*Evaluation:\*\* " .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md` shows CONFIRMED or RULED-OUT
  </acceptance_criteria>
  <resume-signal>Type "C2 CONFIRMED — apply fix" or "C2 RULED-OUT — advance to C3" with one-sentence evidence summary.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: C2 fix (only if Task 2 CONFIRMED) — ref-pattern refactor of BackHandler useEffect</name>
  <read_first>
    - App.tsx (current state of the BackHandler useEffect with [BACK] logs — find via grep)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.2 "Fix if C2 confirmed" (exact ref-pattern code shape)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §5.2 (regression risks + 12-point Android back regression checklist)
    - .planning/phases/01-nav-reliability/01-CONTEXT.md Claude's Discretion: "Whether to preemptively refactor the 23-dep BackHandler useEffect"
  </read_first>
  <files>App.tsx</files>
  <action>
    SKIP this task if Task 2 Evaluation was RULED-OUT. Proceed directly to Task 5 (C3 diagnostic).

    If CONFIRMED, apply the ref-pattern refactor per RESEARCH §2.2 "Fix if C2 confirmed". Full replacement shape (the exact content is in RESEARCH §2.2 — do NOT paraphrase; copy it verbatim):

    - Replace the existing 20-dep useEffect with two effects and one ref:
      1. `const backStateRef = useRef({...all 19 state fields...});` initialized from the current render
      2. Mirror effect (no deps OR same deps as the current one) that copies current state into backStateRef.current on every render
      3. Register-once effect with EMPTY DEPS that reads from `backStateRef.current` inside the onBackPress handler, not from closed-over state. Listener attached once at mount; cleanup removes it.

    Preserve every branch of the existing onBackPress body — all 20+ if/else branches must be copied into the new handler with only the read sites changed from `<name>` to `s.<name>` where `s = backStateRef.current`. Setter calls stay unchanged (React guarantees setter identity across renders).

    Per RESEARCH §5.2 warnings:
    - Back-navigation order must match pre-refactor exactly (20 branches; one accidentally reordered changes pop order)
    - returnToProfileAfterFavorites / returnToProfileAfterAppointments paths are the most subtle
    - Platform.OS !== 'android' early-return stays at the top of the register-once effect

    TypeScript check. Commit atomically with message: `fix(01): ref-pattern refactor of BackHandler useEffect (C2 confirmed)`. Body: resolves stale-closure hazard from the 20-dep useEffect; register-once pattern with empty deps + ref-backed state read; all 20 branches preserved; returnToProfileAfter* paths preserved; Platform.OS guard preserved.
  </action>
  <verify>
    <automated>grep -c "backStateRef" App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - If Task 2 was RULED-OUT: this task is SKIPPED (no code changes) and acceptance is vacuous — proceed to Task 5
    - If Task 2 was CONFIRMED:
      - `grep -c "backStateRef" App.tsx` returns ≥ 3 (useRef declaration + mirror effect write + handler read)
      - `grep -n "BackHandler.addEventListener" App.tsx` returns one match inside a `useEffect(() => { ... }, [])` with EMPTY deps (grep "BackHandler.addEventListener" then inspect the surrounding useEffect closing `}, []);`)
      - All 20 original branch cases still present: `grep -c "setShow\|setIs\|setActive\|setOwner\|setProperty\|setReturn" App.tsx` returns the same or greater count than before the refactor (spot-check: the original counts from pre-refactor App.tsx)
      - TypeScript compilation succeeds
      - `git log --oneline -1 -- App.tsx` commit contains "ref-pattern" or "C2"
  </acceptance_criteria>
  <done>If CONFIRMED: ref-pattern refactor landed. Task 4 (human Android re-run + 12-point checklist) proceeds. If RULED-OUT: skipped; Task 5 proceeds.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: C2 post-fix device re-run (only if Task 3 landed) — full matrix + 12-point Android back regression checklist</name>
  <what-built>
    Ref-pattern BackHandler refactor landed. High regression risk per RESEARCH §5.2 — Android back behavior must be re-verified comprehensively.
  </what-built>
  <how-to-verify>
    SKIP if Task 3 was skipped (C2 was RULED-OUT in Task 2). Advance to Task 5.

    If Task 3 landed:

    1. Build + install fresh on BOTH iOS and Android devices (iOS to confirm no regression; Android to verify fix + run 12-point checklist).

    2. Run full 45-cell matrix on both platforms. Record in a new matrix subsection `## Post-Wave-2 (C2 ref-pattern)` with iOS and Android tables. Videos → `videos/wave2-{platform}-...mp4`.

    3. Run the 12-point Android back regression checklist from RESEARCH §5.2 verbatim — all 12 MUST pass. Record checkmarks in matrix under `### Android back regression (12-point)`:
       - From Home, press back → app backgrounds
       - From PropertyDetails overlay, press back → PropertyDetails closes, Home visible
       - From CreateListing overlay, press back → closes, returns to active tab
       - From RenterListings, press back → closes
       - From Tour3D (early-return), press back → closes, returns to PropertyDetails (or Home)
       - From Profile → Favorites, press back → Favorites closes, Profile re-opens
       - From Profile → Appointments, press back → Appointments closes, Profile re-opens
       - From Chat thread, press back → Chat closes
       - From Login modal (no user), press back → modal closes, main stack visible
       - From nested Login → ForgotPassword → ResetPassword, press back 3× → unwinds in order
       - From AuthPrompt, press back → AuthPrompt closes
       - From PropertyDetails → Tour3D, press back 2× → Tour3D closes, PropertyDetails closes, Home visible

    4. Apply the same Go/No-Go classification as Plan 04 Task 3 (A/B/C/D):
       - A (PROCEED-TO-WAVE-4): every cell PASS + all 12 back checklist PASS → proceed to Plan 06
       - B (ESCALATE to C3): some cells still FAIL → Task 5
       - C (D-04 reassess): partial-success pattern → pause
       - If any of the 12 back-checklist items REGRESSED from Wave 1 → revert the C2 commit, reassess

    5. Add decision block `## Post-Wave-2 Go/No-Go Decision` matching Plan 04's format.
  </how-to-verify>
  <read_first>
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §5.2 (12-point Android back regression checklist — copy verbatim)
    - .planning/phases/01-nav-reliability/01-REPRO-MATRIX.md (format of Post-Wave-1 Decision for matching style)
  </read_first>
  <acceptance_criteria>
    - SKIP-case: if Task 3 was skipped, this task acceptance is vacuous
    - NO-SKIP case:
      - Matrix contains `## Post-Wave-2 (C2 ref-pattern)` section with filled iOS + Android tables + Android back regression subsection
      - All 12 back checklist items marked PASS or FAIL with one-line notes
      - Matrix contains `## Post-Wave-2 Go/No-Go Decision` block with Decision A/B/C
      - ≥ 5 Wave-2 videos captured per platform
      - If ANY of the 12 back-checklist items FAIL: matrix notes REVERT trigger; the C2 commit MUST be reverted before continuing
  </acceptance_criteria>
  <resume-signal>Type "Post-Wave-2: A PROCEED" or "B ESCALATE to C3" or "C D-04 reassess" or "REVERT — back regression".</resume-signal>
</task>

<task type="auto">
  <name>Task 5: C3 diagnostic — add [C3] instrumentation + temporary diagnostic borders to wrappers</name>
  <read_first>
    - App.tsx (current state of the 4 overlay wrappers + fullScreenOverlayWrap + auth modal wrapper at ~App.tsx:857-867)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.3 "Instrumentation" (exact border + log shapes)
  </read_first>
  <files>App.tsx</files>
  <action>
    SKIP this task if Post-Wave-2 Decision was A (PROCEED). Advance to Task 9 (final Go/No-Go) to route to Plan 06.

    Otherwise, per RESEARCH §2.3:

    1. Temporarily add diagnostic borders to the 3 absolute-positioned wrappers:
       - `fullScreenOverlayWrap` (RenterListings + CreateListing): add `borderWidth: 3, borderColor: 'magenta'` — mark both fields with TEMP comments for Plan 06 strip
       - PropertyDetails inline wrapper style (~App.tsx:716-725 region, now shifted): add `borderWidth: 3, borderColor: 'cyan'`
       - Auth modal wrapper (~App.tsx:857-867 region): add `borderWidth: 3, borderColor: 'yellow'`

    2. Add an `onStartShouldSetResponder` log to each of the 3 wrappers, returning `false` to not capture (just log the attempt):

        onStartShouldSetResponder={() => {
          console.log('[C3] wrapper captured touch', 'PropertyDetails'); // or 'RenterListings' or 'CreateListing' or 'authModal'
          return false;
        }}

    Each wrapper gets its own label string matching the wrapper it guards.

    3. TypeScript check. Commit atomically with message: `chore(01): add [C3] diagnostic borders + touch logs to overlay wrappers (RESEARCH §2.3)`. Body: borders and onStartShouldSetResponder logs are temporary per D-02; Plan 06 strips both borders (grep for `// TEMP C3 diagnostic`) and [C3] logs.
  </action>
  <verify>
    <automated>grep -c "C3\]" App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SKIP-case: vacuous
    - NO-SKIP case:
      - `grep -c "C3\] wrapper captured touch" App.tsx` returns ≥ 3
      - `grep -c "TEMP C3 diagnostic" App.tsx` returns ≥ 3 (one per wrapper with added border)
      - `grep -c "borderColor: 'magenta'\|borderColor: 'cyan'\|borderColor: 'yellow'" App.tsx` returns ≥ 3
      - TypeScript compiles
      - Commit message contains "[C3]"
  </acceptance_criteria>
  <done>C3 borders + [C3] logs installed. Task 6 (human device run) proceeds.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: C3 device re-run — visual border observation + log collection on iOS + Android</name>
  <what-built>
    C3 borders + [C3] logs are visible on the build. Borders are bright-colored boxes per wrapper.
  </what-built>
  <how-to-verify>
    SKIP if Task 5 skipped.

    1. Build + install on both platforms. Reproduce the canonical trap sequence (RESEARCH §2.1) with borders visible.

    2. Watch for: colored border visible OVER the bottom nav when the user is visually on the main stack (overlay should be gone). Note border color — that identifies the culprit wrapper.

    3. Collect [C3] logs: `adb logcat | grep C3 > videos/android-c3-logs.txt` and Metro console for iOS → `videos/ios-c3-logs.txt`.

    4. Evaluate C3 confirmed predicate (RESEARCH §2.3):
       - CONFIRMED: visible border over BottomNavigator when overlay should be gone, OR `[C3] wrapper captured touch` fires on bottom-nav taps with a wrapper name
       - RULED-OUT: no border visible when trap reproduces AND `[C3] wrapper captured touch` never fires during a trapped tap

    5. Record in matrix under `## C3 Diagnostic — Evidence Review` with Evaluation + identified wrapper name (if CONFIRMED).
  </how-to-verify>
  <read_first>
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.3 Confirmed + Ruled-out predicates
  </read_first>
  <acceptance_criteria>
    - SKIP-case: vacuous
    - NO-SKIP case:
      - Both platform log files exist: `videos/ios-c3-logs.txt` and `videos/android-c3-logs.txt`
      - Matrix contains `## C3 Diagnostic — Evidence Review` with Evaluation CONFIRMED (identifying specific wrapper) or RULED-OUT
      - Human has posted evidence screenshot/video if border was visible over BottomNavigator
  </acceptance_criteria>
  <resume-signal>Type "C3 CONFIRMED — culprit wrapper: {name}" or "C3 RULED-OUT — advance to C4".</resume-signal>
</task>

<task type="auto">
  <name>Task 7: C3 fix (only if Task 6 CONFIRMED) — apply style pointerEvents to the identified wrapper</name>
  <read_first>
    - App.tsx (state after Task 5 — wrappers have borders + [C3] logs)
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.3 "Fix if C3 confirmed" (exact auth-modal pointerEvents shape)
    - .planning/phases/01-nav-reliability/01-CONTEXT.md D-06 (auth modal explicitly in scope once C3 reached)
  </read_first>
  <files>App.tsx</files>
  <action>
    SKIP if Task 6 RULED-OUT.

    If CONFIRMED and the culprit wrapper is the auth modal (most likely per RESEARCH §2.3 — PropertyDetails / RenterListings / CreateListing were already gated in Plan 03 Task 1), locate the auth modal at App.tsx:857-867 (line numbers shifted by all preceding insertions). Grep: `grep -n "zIndex: 1000" App.tsx`.

    Augment the auth modal style object with a conditional pointerEvents:

        pointerEvents: (showResetPasswordModal || showForgotPasswordModal || showLoginModal || showSignupModal) ? 'auto' : 'none',

    Do NOT change the existing conditional mount guard at lines ~853-856 — both gates coexist.

    If the culprit is a different wrapper (unusual but possible), apply the equivalent style-based pointerEvents pattern to that wrapper, tracking its visibility flag.

    TypeScript check. Commit atomically with message: `fix(01): gate auth modal wrapper with pointerEvents (C3 confirmed, D-06 scope expansion)`. Body: Wave 1 D-06 scoped out the auth modal; Plan 04 re-run showed residual FAIL routing to C3; this commit closes the remaining gap.
  </action>
  <verify>
    <automated>grep -c "pointerEvents: (showResetPasswordModal" App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SKIP-case: vacuous
    - NO-SKIP case:
      - `grep -c "pointerEvents: (showResetPasswordModal\|pointerEvents: showResetPasswordModal" App.tsx` returns ≥ 1 (if auth modal was culprit) OR equivalent style-pointerEvents added to the identified wrapper
      - TypeScript compiles
      - Commit message contains "C3"
  </acceptance_criteria>
  <done>If CONFIRMED: C3 fix landed. Task 8 (human re-run) proceeds. If RULED-OUT: skipped; Task 9 C4 diagnostic proceeds.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8: C3 post-fix device re-run (if Task 7 landed)</name>
  <what-built>
    C3 fix (pointerEvents on identified wrapper) landed. Verify via full matrix re-run + Go/No-Go decision.
  </what-built>
  <how-to-verify>
    SKIP if Task 7 skipped.

    Same pattern as Plan 04 Task 3 / Plan 05 Task 4: full 45×2 matrix on both platforms, record in `## Post-Wave-3 (C3 fix)`, apply A/B/C classification for next step. Videos → `videos/wave3-{platform}-...mp4`.

    - A (PROCEED-TO-WAVE-4): 0% repro → Plan 06
    - B (ESCALATE to C4): some cells still FAIL → Task 9
    - C (D-04 reassess): partial-success pattern → pause
  </how-to-verify>
  <read_first>
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §6 SC1 (0% repro bar)
  </read_first>
  <acceptance_criteria>
    - SKIP-case: vacuous
    - NO-SKIP case:
      - Matrix contains `## Post-Wave-3 (C3 fix)` with filled tables + Decision block
      - `## Post-Wave-3 Go/No-Go Decision` with A/B/C labeled and Next-plan-to-run line
  </acceptance_criteria>
  <resume-signal>Type "Post-Wave-3: A PROCEED" or "B ESCALATE to C4" or "C D-04 reassess".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 9: C4 diagnostic — long-idle touchable probe on Android (only if previous candidates RULED-OUT)</name>
  <what-built>
    C2 and C3 both failed to reach 0% repro. Per RESEARCH §2.4, C4 requires time-dependent device testing (~10 minutes sustained use) + [C4] log instrumentation on BottomNavigator.
  </what-built>
  <how-to-verify>
    SKIP if Post-Wave-2 Decision was A or Post-Wave-3 Decision was A.

    If reached (all prior CONFIRMED fixes still left FAIL cells):

    1. Add [C4] onPressIn / onPressOut / onPress logs to each tab touchable in `src/components/BottomNavigator.tsx` per RESEARCH §2.4 exact strings:

        onPressIn={() => console.log('[C4] bottomNav onPressIn', tab, Date.now())}
        onPressOut={() => console.log('[C4] bottomNav onPressOut', tab, Date.now())}
        onPress={() => { console.log('[C4] bottomNav onPress', tab, Date.now()); onTabChange(tab); }}

    Commit: `chore(01): add [C4] diagnostic logs on BottomNavigator (RESEARCH §2.4)`.

    2. Human runs the time-dependent repro on Android (RESEARCH §2.4 repro trigger):
       - Cold-start, note start time
       - Use the app normally for 10 minutes
       - Every 2 minutes, tap every bottom-nav tab, log result
       - Watch for: `[C4] bottomNav onPressIn` fired, `[C4] bottomNav onPressOut` fired within <50 ms, but `[C4] bottomNav onPress` did NOT fire — this is the #36710 signature

    3. Evaluate per RESEARCH §2.4 predicate:
       - CONFIRMED: onPressIn + onPressOut but NOT onPress; appears only after sustained usage; hot-reload resets
       - RULED-OUT: bug reproduces on cold start within seconds (not lifecycle-dependent); OR onPress fires but nav doesn't transition
       - ALL-THREE-RULED-OUT: per RESEARCH §2.4 fallback — D-04 "pause, reassess" triggers

    4. Record matrix `## C4 Diagnostic — Evidence Review` with Evaluation.

    5. IMPORTANT PER RESEARCH §2.4: if C4 CONFIRMED, do NOT auto-apply the fix (removeClippedSubviews + unmount timer). Per RESEARCH §2.4 note: "this fix changes semantics of the keep-alive pattern — the user loses tab state (scroll position, chat draft, filter state) when the unmount timer fires. If C4 confirms, re-open discussion: D-04 applies ('pause, reassess'), because at this point Phase 1's scope has expanded beyond 'bug fix' into 'redesign the keep-alive model.'" Signal the human for re-scoping BEFORE any C4 fix commit.
  </how-to-verify>
  <read_first>
    - .planning/phases/01-nav-reliability/01-RESEARCH.md §2.4 Instrumentation + Repro trigger + Confirmed + Ruled-out predicates + Fix-if-C4-confirmed
    - .planning/phases/01-nav-reliability/01-CONTEXT.md D-04 (pause-and-reassess rule)
  </read_first>
  <acceptance_criteria>
    - SKIP-case: vacuous
    - NO-SKIP case:
      - `grep -c "C4\]" src/components/BottomNavigator.tsx` returns ≥ 3
      - Matrix contains `## C4 Diagnostic — Evidence Review` with Evaluation
      - If C4 CONFIRMED: matrix notes `D-04 triggered — scope expansion into keep-alive redesign. Phase 1 pauses; human re-scoping required.` Plan 06 does NOT run.
      - If C4 RULED-OUT and C2 + C3 were both RULED-OUT too: matrix notes `All candidates C2/C3/C4 ruled out. D-04 all-candidates-exhausted — Phase 1 pauses; human re-scoping required.` Plan 06 does NOT run.
      - If C4 RULED-OUT and a prior candidate's fix did land + achieved 0% repro on its wave: this task shouldn't have been reached (logic error upstream)
  </acceptance_criteria>
  <resume-signal>Type "C4 CONFIRMED — D-04 scope expansion required" or "C4 RULED-OUT — all candidates exhausted, D-04 pause" or "C4 RULED-OUT — prior fix sufficient, advance to Plan 06".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in the diagnostic phases (logs + borders are observational). The C2 ref-pattern refactor is a code reshape that preserves the existing auth trust posture — no new code paths into auth state. The C3 fix on the auth modal wrapper adds a pointerEvents gate that more tightly gates input to the auth flow (strictly narrowing the attack surface, not expanding it).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | T (Tampering) | C2 ref-pattern refactor — backStateRef read inside onBackPress | mitigate | Ref reads can go stale if the mirror effect doesn't fire; mitigated by the mirror effect running on every render with same deps as the original. Device re-run (Task 4) + 12-point Android back regression checklist (RESEARCH §5.2) verifies every branch path preserves its semantics. |
| T-01-05-02 | I (Information Disclosure) | [BACK] handler deps-snapshot log | mitigate | The snapshot object dumps boolean flags for 17 auth/overlay state fields — no tokens, no emails, no user content. `propertyForTourSelection` and `ownerListingsUid` are coerced to `!!` so only the existence bit is logged, not the underlying ID. Plan 06 strips all [BACK] logs. |
| T-01-05-03 | I | C3 auth-modal pointerEvents — identified specifically because Wave 1 scoped it out per D-06 | mitigate | When reached (only via Task 6 CONFIRMED), the threat consideration from `<threat_model_requirement>` applies: the auth modal's pointerEvents becomes explicit and style-based, conditional on the 4 auth-visibility booleans. This tightens — not loosens — interaction gating for auth flows. No new input surface introduced; the existing conditional-mount guard (lines ~853-856) remains in place as a first gate. Verification: Task 8 full-matrix re-run exercises all 4 auth screens (Login / Signup / ForgotPassword / ResetPassword) to confirm none became non-interactive. |
| T-01-05-04 | D (Denial of Service) | C4 fix (unmount timer — NOT auto-applied) | transfer | If C4 confirms, D-04 pause triggers. The fix itself would cause user-visible state loss (semantic DoS on tab state). This is transferred back to the human re-scoping decision; plan does not land the fix autonomously. |
| T-01-05-05 | I | C4 [C4] onPress* logs include `tab` + `Date.now()` | accept | Tab IDs are non-sensitive strings; timestamps are non-sensitive. Plan 06 strips. |

Block on: high severity only per config default. The high-severity consideration surfaced by `<threat_model_requirement>` — auth modal pointerEvents audit in C3 escalation — is addressed by T-01-05-03 above.
</threat_model>

<verification>
- Each candidate (C2, C3, C4) has an atomic instrumentation commit, a human device re-run capturing evidence, a predicate evaluation, and (if CONFIRMED) a separate atomic fix commit + a device re-run gate
- Matrix accumulates `## Post-Wave-2`, `## Post-Wave-3` sections (as appropriate) and ends with either a PROCEED-TO-WAVE-4 decision for Plan 06 OR a D-04 pause-and-reassess block
- All diagnostic logs land under the strippable tags [BACK] / [C3] / [C4] — Plan 06's composite grep removes them all
- If any candidate's fix lands but subsequent re-run shows regression, that commit is reverted before advancing
</verification>

<success_criteria>
- Either 0% repro achieved at some Post-Wave-N (proceed to Plan 06) OR human has been signalled for D-04 pause-and-reassess with clear evidence of which candidate(s) were tried and ruled out
- The 12-point Android back regression checklist (RESEARCH §5.2) passes if the C2 ref-pattern refactor landed
- The auth modal remains interactive (all 4 auth screens: Login / Signup / ForgotPassword / ResetPassword) if the C3 fix landed
- No candidate's diagnostic logs remain in App.tsx or BottomNavigator.tsx after Plan 06 strips (that is Plan 06's gate, not this one)
</success_criteria>

<output>
After completion, create `.planning/phases/01-nav-reliability/01-05-SUMMARY.md`:
- Which candidates were exercised (C2 / C3 / C4)
- Each candidate's outcome (CONFIRMED + fix landed / RULED-OUT)
- Final Post-Wave-N decision (PROCEED / D-04 pause)
- Commit SHAs for every diagnostic + every fix
- Explicit next-plan routing: "Run Plan 06" | "HOLD — D-04 reassess required"
- List of ALL diagnostic tags introduced (subset of [BACK], [C3], [C4]) so Plan 06 knows the exact strip scope for this phase run
</output>
