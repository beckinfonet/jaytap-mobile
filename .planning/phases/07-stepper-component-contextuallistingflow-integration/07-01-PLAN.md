---
phase: 07-stepper-component-contextuallistingflow-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/StepperInput.tsx
  - src/components/__tests__/StepperInput.test.tsx
autonomous: true
requirements: [FORM-01, FORM-03]
tags: [react-native, component, stepper, accessibility, tap-target]

must_haves:
  truths:
    - "StepperInput component file exists at src/components/StepperInput.tsx with a named export and the FORM-01 props contract"
    - "Tapping + from undefined initializes the value to min (calls onChange(min))"
    - "Tapping − at value === min is a no-op (onChange NOT called)"
    - "Tapping + at value === max is a no-op (onChange NOT called)"
    - "Boundary buttons set disabled={true} and drop only the glyph color to colors.textSecondary"
    - "Hit-slop on both ± buttons yields a ≥44pt effective tap target (FORM-03)"
    - "Center value cell renders em-dash '—' (U+2014) when value === undefined; integers strip trailing .0; 0.5-step values render as 1.5"
    - "Co-located unit tests under src/components/__tests__/StepperInput.test.tsx cover the 6 cases in D-11"
  artifacts:
    - path: "src/components/StepperInput.tsx"
      provides: "StepperInput component (FORM-01 + FORM-03)"
      exports: ["StepperInput"]
      contains: "interface StepperInputProps"
    - path: "src/components/__tests__/StepperInput.test.tsx"
      provides: "Co-located unit tests, 6 cases per D-11"
  key_links:
    - from: "src/components/StepperInput.tsx"
      to: "react-native Pressable + hitSlop"
      via: "circular ± buttons with accessibilityRole='button'"
      pattern: "hitSlop.*\\{.*top:.*\\}"
    - from: "src/components/__tests__/StepperInput.test.tsx"
      to: "src/components/StepperInput"
      via: "react-test-renderer + act() + findByProps({testID})"
      pattern: "TestRenderer\\.create\\(<StepperInput"
---

<objective>
Ship the reusable `<StepperInput>` component (FORM-01) with tap-only `±` controls, ≥44pt hit-slop (FORM-03), boundary-disabled glyph dimming (D-03), em-dash empty-state (D-02), and plain-decimal formatting (D-04) — plus the 6 co-located unit tests locked in D-11. The component is consumed by Plan 07-04 (Step3BasicInfo integration); shipping it isolated lets 07-02 and 07-03 parallelize in the same wave.

Purpose: One reusable numeric-control primitive that Step 3 wires twice (bedrooms + bathroomCount). Visually distinct from existing chip rows so users don't confuse "tap to increment" with "tap to select chip" (D-01).

Output: One new TSX component + one new co-located test file. No edits to existing files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<interfaces>
<!-- StepperInput props contract — FORM-01 locked. Caller does t() lookup. -->

```typescript
interface StepperInputProps {
  value: number | undefined;
  onChange: (v: number | undefined) => void;
  min: number;
  max: number;
  step: number; // 1 or 0.5
  label: string;            // already-localized — caller does the t() lookup
  testID?: string;          // base testID; component derives `${testID}-minus`, `${testID}-value`, `${testID}-plus`
  accessibilityLabel?: string; // Claude's Discretion — optional, passed to row container
}
```

<!-- Format helper — inline, no new utility file -->
```typescript
const formatValue = (v: number | undefined): string =>
  v === undefined ? '—' : Number.isInteger(v) ? String(v) : v.toFixed(1);
```

<!-- Composite analog sources (PATTERNS.md): -->
<!-- - src/components/AuthModalCloseButton.tsx (TAP=44, circular pressable, hitSlop, accessibilityRole) -->
<!-- - src/components/PasswordTextInput.tsx (hitSlop on small target, disabled prop) -->
<!-- - src/components/PropertyCard.tsx (React.FC<Props> shape, theme inline-style array, co-located StyleSheet) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StepperInput component (FORM-01 + FORM-03)</name>
  <files>src/components/StepperInput.tsx</files>
  <read_first>
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md (D-01..D-04, D-11; Specific Ideas L217-237 — props + formatValue helper)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"Pattern Assignments — NEW Files — StepperInput.tsx" (Analogs A/B/C verbatim excerpts)
    - src/components/AuthModalCloseButton.tsx (full file — TAP=44 + circular pressable + hitSlop + accessibilityRole pattern)
    - src/components/PasswordTextInput.tsx lines 21-28 (hitSlop + disabled prop precedent)
    - src/components/PropertyCard.tsx lines 1-60 (React.FC&lt;Props&gt; + useTheme + named export shape)
    - src/theme/ThemeContext.tsx (confirm `colors.text`, `colors.textSecondary`, `colors.border`, `colors.activeChipBackground`, `colors.surface` tokens exist on the theme; `isDark` available)
    - .planning/codebase/CONVENTIONS.md lines 207-230 (component conventions: PascalCase, named export, props interface above component, co-located StyleSheet, theme inline-style array, hitSlop ≥44pt)
  </read_first>
  <behavior>
    - Renders a horizontal row: `−` round button (left) + value cell (center) + `+` round button (right) + label `<Text>` ABOVE the row using `commonStyles.sectionLabel`-equivalent shape (D-01). NOTE: component does NOT import `commonStyles` from ContextualListingFlow — that namespace is Step3-internal; replicate the label-above-row visual treatment locally so the component stays generic for future reuse outside Step3 (CONTEXT.md "Deferred Ideas" §"reusability across the codebase").
    - Buttons are circles with diameter = `ICON_CIRCLE` (34px) inside a `TAP` (44px) hit-area, exactly mirroring AuthModalCloseButton.tsx:8-9 sizing. Buttons use `<Pressable>` (not `TouchableOpacity` — matches AuthModalCloseButton + supports `({pressed}) => style` press-feedback per CONVENTIONS L228).
    - `hitSlop` set explicitly on both buttons (per FORM-03) — use the AuthModalCloseButton.tsx:23 shape `{ top: 10, bottom: 10, left: 10, right: 10 }` (44pt circle + ≥10pt slop on every side = effective ≥64pt — exceeds FORM-03's 44pt minimum). Always set hitSlop regardless of `disabled` state (Claude's Discretion: "always 44pt regardless of state").
    - `accessibilityRole="button"` + `accessibilityLabel` on each button. Use `accessibilityState={{ disabled: isMinDisabled }}` / `{{ disabled: isMaxDisabled }}` so screenreaders announce inert state (CONVENTIONS L228).
    - Test 1 (D-11.1): renders em-dash `—` (U+2014, code point 8212) when `value === undefined`. Center value cell uses `formatValue(value)`.
    - Test 2 (D-11.2): renders integer `5` when `value === 5` and `step === 1`; renders `1.5` when `value === 1.5` and `step === 0.5`. Verified by `Number.isInteger(v) ? String(v) : v.toFixed(1)` — `Number.isInteger(1.5)` is `false` so it routes to `toFixed(1)`.
    - Test 3 (D-11.3): tap `+` from `value === undefined` calls `onChange(min)`. Internal logic: `const handleIncrement = () => onChange(value === undefined ? min : Math.min(value + step, max))`.
    - Test 4 (D-11.4): tap `−` at `value === min` does NOT call `onChange`. Decrement handler short-circuits when at min boundary: `if (value === undefined || value <= min) return;`. CRITICAL: when `value === undefined`, `−` is a no-op (no `undefined` regression, no negatives).
    - Test 5 (D-11.5): tap `+` at `value === max` does NOT call `onChange`. Increment handler short-circuits when at max: derive `isMaxDisabled = value !== undefined && value >= max` and pass to `disabled` prop on `+` Pressable.
    - Test 6 (D-11.6): at boundary, `disabled={true}` set on the boundary button AND glyph color resolves to `colors.textSecondary` (D-03). Outline + background of the circle stay at the active treatment — only the `−`/`+` `<Text>` `color` drops. Active-state glyph uses `colors.text`.
    - Test IDs: the component derives `${testID}-minus`, `${testID}-value`, `${testID}-plus` from the optional `testID` prop. If `testID` is undefined, omit the testID attributes (don't render `undefined-minus`). PATTERNS.md §"Shared Patterns — testID discipline" L709.
    - Behavior contract NOT shipped (deferred to M5+): long-press accelerated increment; `1½` Unicode glyph (D-04 ships `1.5`).
  </behavior>
  <action>
    Create `src/components/StepperInput.tsx`. Structure (PATTERNS.md §"Composite analog"):

    1. Imports block (CONVENTIONS L57-67 ordering): React → react-native (`View`, `Text`, `Pressable`, `StyleSheet`) → `useTheme` from `../theme/ThemeContext`.
    2. Local constants near the top (forked from AuthModalCloseButton.tsx:8-9 — looser coupling per Claude's Discretion): `const TAP = 44; const ICON_CIRCLE = 34;`. Do NOT import from AuthModalCloseButton — forking keeps coupling looser per CONTEXT.md "Reusable Assets" note.
    3. Inline `formatValue` helper (per CONTEXT Specific Ideas L233-236): `const formatValue = (v: number | undefined): string => v === undefined ? '—' : Number.isInteger(v) ? String(v) : v.toFixed(1);`. Em-dash literal is U+2014 ('—').
    4. `interface StepperInputProps` immediately above the component (CONVENTIONS L207). Props per the contract above.
    5. Component as named export: `export const StepperInput: React.FC<StepperInputProps> = ({ value, onChange, min, max, step, label, testID, accessibilityLabel }) => { ... }`. Use `React.FC<Props>` shape (matches PropertyCard.tsx:39).
    6. Inside component: `const { colors } = useTheme();` at top. Derive `isMinDisabled = value === undefined || value <= min;` (per D-03 — undefined-state `−` is disabled because tapping `−` from undefined is a no-op per FORM-01 spec). Derive `isMaxDisabled = value !== undefined && value >= max;`. Derive `handleDecrement` (no-op short-circuit if `isMinDisabled`; otherwise `onChange(value! - step)`) and `handleIncrement` (no-op if `isMaxDisabled`; otherwise `onChange(value === undefined ? min : Math.min(value + step, max))`).
    7. JSX shape:
       - Outer `<View>` with `accessibilityLabel` (optional) passed through.
       - `<Text>` label row above the control (use a local label style — replicate `commonStyles.sectionLabel` shape: marginBottom 8, fontSize 14, fontWeight '500' (sample from Step3BasicInfo.tsx label rows), color `colors.text` via inline style).
       - `<View style={styles.controlRow}>` containing three children:
         - `<Pressable onPress={handleDecrement} disabled={isMinDisabled} hitSlop={{top:10,bottom:10,left:10,right:10}} accessibilityRole="button" accessibilityLabel={...} accessibilityState={{disabled:isMinDisabled}} testID={testID ? \`${testID}-minus\` : undefined} style={({pressed}) => [styles.hitArea, styles.circle, {backgroundColor: colors.activeChipBackground ?? colors.border, opacity: pressed ? 0.7 : 1}]}>` → `<Text style={[styles.glyph, {color: isMinDisabled ? colors.textSecondary : colors.text}]}>−</Text>`. Use `−` (U+2212 MINUS SIGN, not ASCII `-`) for visual symmetry with `+`.
         - `<Text style={[styles.value, {color: colors.text}]} testID={testID ? \`${testID}-value\` : undefined}>{formatValue(value)}</Text>`.
         - `<Pressable onPress={handleIncrement} disabled={isMaxDisabled} hitSlop={...} accessibilityRole="button" accessibilityLabel={...} accessibilityState={{disabled:isMaxDisabled}} testID={testID ? \`${testID}-plus\` : undefined} style={...}>` → `<Text style={[styles.glyph, {color: isMaxDisabled ? colors.textSecondary : colors.text}]}>+</Text>`.
    8. `StyleSheet.create({...})` at file bottom (CONVENTIONS L223): `hitArea` (width/height TAP, center align), `circle` (width/height ICON_CIRCLE, borderRadius ICON_CIRCLE/2, center align), `glyph` (fontSize 22, fontWeight '600'), `controlRow` (flexDirection 'row', alignItems 'center', gap 16), `value` (fontSize 20, fontWeight '600', minWidth 48, textAlign 'center'), `label` (fontSize 14, fontWeight '500', marginBottom 8).
    9. NO `commonStyles` import — component is generic per Deferred Ideas §"reusability across the codebase".
    10. accessibilityLabel for `−` / `+`: derive from prop `label` if available, e.g. `accessibilityLabel={\`Decrease ${label}\`}` / `\`Increase ${label}\``. If `accessibilityLabel` prop is provided on the component, attach to the outer `<View>` only — do not override the button-level labels.

    DO NOT inline a code fence in this plan — implementation goes in the source file.
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        test -f src/components/StepperInput.tsx &amp;&amp; \
        grep -q "export const StepperInput" src/components/StepperInput.tsx &amp;&amp; \
        grep -q "interface StepperInputProps" src/components/StepperInput.tsx &amp;&amp; \
        grep -q "const TAP = 44" src/components/StepperInput.tsx &amp;&amp; \
        grep -q "hitSlop" src/components/StepperInput.tsx &amp;&amp; \
        grep -q "accessibilityRole=\"button\"" src/components/StepperInput.tsx &amp;&amp; \
        grep -q "—" src/components/StepperInput.tsx &amp;&amp; \
        grep -c "^import.*react-native-keyboard-controller\\|keyboardVerticalOffset" src/components/StepperInput.tsx | grep -q "^0$" &amp;&amp; \
        npx tsc --noEmit -p . 2>&amp;1 | grep -v "Property\\.tours" | grep -E "StepperInput\\.tsx" | grep -v "MISSING — Wave 0 must create" | wc -l | grep -q "^0$"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/components/StepperInput.tsx` exists, exports `StepperInput` as a named export, declares `interface StepperInputProps` with the 7 required + 2 optional props.
    - Source assertion: file contains `const TAP = 44` and `const ICON_CIRCLE = 34` near the top (forked from AuthModalCloseButton; no import from it).
    - Source assertion: at least two `hitSlop` occurrences (one per ± Pressable).
    - Source assertion: at least two `accessibilityRole="button"` occurrences.
    - Source assertion: file contains the em-dash literal `—` (U+2014).
    - Source assertion: file contains `Math.min` (max-clamping in increment handler) and a short-circuit guard for the min boundary (e.g. `if (isMinDisabled) return;` or equivalent — verifiable via `grep -E "isMinDisabled|value === undefined.*\\|\\|.*<=|<= min"`).
    - KBD-02 grep gate preserved: `keyboardVerticalOffset` count in `src/` stays 0 (`grep -RIn "keyboardVerticalOffset" src/ | wc -l` returns 0).
    - Behavior assertion: when invoked as `<StepperInput value={undefined} min={0} max={10} step={1} onChange={fn} label="Bedrooms" />` and `+` is tapped (via test in Task 2), `fn` is called with `0`.
    - Behavior assertion: tapping `−` with `value === 0` and `min === 0` does NOT call `onChange` (covered by Task 2 test 4).
    - tsc: zero net new TypeScript errors attributable to `StepperInput.tsx` (pre-existing `Property.tours` errors excluded per memory).
  </acceptance_criteria>
  <done>StepperInput.tsx exists with FORM-01 props contract; circular ± Pressables with ≥44pt hit-slop + accessibilityRole; em-dash empty-state and Number.isInteger / toFixed(1) value formatting; boundary disabled state dims glyph to colors.textSecondary while preserving circle outline; zero new tsc errors; KBD-02 grep gate still 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Co-located StepperInput unit tests (D-11 — 6 cases)</name>
  <files>src/components/__tests__/StepperInput.test.tsx</files>
  <read_first>
    - src/components/StepperInput.tsx (the component from Task 1 — testID derivation, props contract, em-dash literal)
    - src/components/__tests__/EmailVerifyBanner.test.tsx (full file — primary analog: react-test-renderer + act() + jest.mock for useTheme/useLanguage; findByProps testID pattern)
    - src/components/__tests__/PropertyCard.specChip.test.tsx lines 1-40 (secondary analog: comment at L20-23 documents "no RTL/jest-native in dev deps" — pinned project convention)
    - src/components/ContextualListingFlow/__tests__/Step3.test.tsx lines 1-80 (tertiary analog: `findByTestID` / `tryFindByTestID` helpers)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"src/components/__tests__/StepperInput.test.tsx" (full excerpt — D-11 6 cases verbatim)
  </read_first>
  <behavior>
    - Test 1: renders em-dash when `value === undefined`. Assertion: `expect(valueCell.props.children).toBe('—');`.
    - Test 2a: renders integer `'5'` when `value === 5` and `step === 1`.
    - Test 2b: renders `'1.5'` when `value === 1.5` and `step === 0.5` (NOT `'1.50'` or `'1,5'` — exactly one decimal, ASCII period).
    - Test 3: tap `+` from `value === undefined` calls `onChange` with the configured `min` (test with `min: 0` AND `min: 2` to prove it's not hardcoded).
    - Test 4: tap `−` at `value === 0` with `min: 0` → `onChange` NOT called (`expect(onChange).not.toHaveBeenCalled();`). Also verify the `−` Pressable has `disabled={true}` via `findByProps({testID: 'stepper-minus'}).props.disabled === true`.
    - Test 5: tap `+` at `value === 10` with `max: 10` → `onChange` NOT called. Also verify `+` Pressable has `disabled={true}`.
    - Test 6: at boundary (e.g. `value === 0, min: 0`), the `−` glyph `<Text>` style array contains `{color: colors.textSecondary}` while the `+` glyph contains `{color: colors.text}`. Use `findByProps({testID:'stepper-minus'})` then walk to its child `<Text>` and inspect the `style` prop (which is an array per RN convention). Assert via `.style.some(s => s?.color === '<textSecondary-mock-value>')`.
    - All 6 tests use `react-test-renderer` + `act()` — NO `@testing-library/react-native`, NO `fireEvent` (project does not have RTL in dev deps per PropertyCard.specChip.test.tsx:20-23).
  </behavior>
  <action>
    Create `src/components/__tests__/StepperInput.test.tsx`. Structure (mirror `EmailVerifyBanner.test.tsx`):

    1. Imports: `React` from 'react'; `TestRenderer, { act }` from 'react-test-renderer'; `{ StepperInput }` from `'../StepperInput'`.
    2. `jest.mock('../../theme/ThemeContext', () => ({ useTheme: jest.fn() }))` at module top (mirrors EmailVerifyBanner.test.tsx:16-18 — drop the AuthContext + LanguageContext mocks since StepperInput uses neither).
    3. Below imports: `const { useTheme } = require('../../theme/ThemeContext');`.
    4. `beforeEach`: `useTheme.mockReturnValue({ colors: { text: '#TEXT', textSecondary: '#TS', border: '#B', activeChipBackground: '#A', surface: '#S', error: '#E' }, isDark: false });`. Use sentinel string values like `'#TEXT'` / `'#TS'` so Test 6 can assert `.toBe('#TS')` on the dim-glyph color.
    5. `findByTestID` helper (mirror Step3.test.tsx:49-56): `const findByTestID = (root, testID) => root.findByProps({ testID });`. Also `tryFindByTestID` returning null if not found.
    6. `render` helper wrapped in `act()` (mirror EmailVerifyBanner.test.tsx:61-67) — accepts props and returns the TestRenderer instance.
    7. Cases — 6 `test(...)` blocks per D-11:
       - Test 1 'renders em-dash when value is undefined': render with `value={undefined}, min:0, max:10, step:1, testID:"stepper", onChange:jest.fn(), label:"Bedrooms"`. Find `stepper-value`. Assert `expect(node.props.children).toBe('—');`.
       - Test 2 'renders integer 5 with step 1 and 1.5 with step 0.5': render with `value:5, step:1` → assert children is `'5'`. Re-render with `value:1.5, step:0.5` → assert children is `'1.5'`.
       - Test 3 'tap + from undefined calls onChange(min)': render with `value:undefined, min:0`. Inside `await act(async () => { findByTestID('stepper-plus').props.onPress(); })`. Assert `expect(onChange).toHaveBeenCalledWith(0)`. Re-render with `min:2` and assert called with `2`.
       - Test 4 'tap − at value === min is a no-op AND minus button is disabled': render with `value:0, min:0`. Trigger `findByTestID('stepper-minus').props.onPress()`. Assert `expect(onChange).not.toHaveBeenCalled()`. Also assert `findByTestID('stepper-minus').props.disabled === true`.
       - Test 5 'tap + at value === max is a no-op AND plus button is disabled': symmetric to Test 4 with `value:10, max:10`.
       - Test 6 'disabled boundary buttons drop glyph color to textSecondary': render with `value:0, min:0`. Find `stepper-minus`, walk to its child `<Text>` (first child of the Pressable's `children` or via `findAll(n => n.type === 'Text')[index]` scoped under the minus Pressable — simplest path: `findByTestID('stepper-minus').findByType('Text')`). Inspect `style` prop; assert at least one entry has `color: '#TS'` (the mocked `colors.textSecondary`). Symmetric check for `stepper-plus` confirms its glyph is `colors.text` (`'#TEXT'`).
    8. All onPress invocations wrapped in `await act(async () => { ... })`.

    DO NOT use `@testing-library/react-native`, `fireEvent`, or `render` from RTL. Project does not have RTL in dev deps.
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        test -f src/components/__tests__/StepperInput.test.tsx &amp;&amp; \
        grep -q "react-test-renderer" src/components/__tests__/StepperInput.test.tsx &amp;&amp; \
        grep -cE "^\\s*test\\(" src/components/__tests__/StepperInput.test.tsx | grep -qE "^[6-9]$|^[1-9][0-9]+$" &amp;&amp; \
        grep -cE "@testing-library/react-native|fireEvent" src/components/__tests__/StepperInput.test.tsx | grep -q "^0$" &amp;&amp; \
        npx jest src/components/__tests__/StepperInput.test.tsx --silent 2>&amp;1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/components/__tests__/StepperInput.test.tsx` exists.
    - Source assertion: at least 6 `test(...)` blocks (the D-11 minimum; planner-allowed to expand).
    - Source assertion: imports `react-test-renderer` and uses `act` — does NOT import `@testing-library/react-native` or use `fireEvent` (`grep -cE "@testing-library/react-native|fireEvent" returns 0`).
    - Source assertion: at least one `jest.mock('../../theme/ThemeContext'` call.
    - Test execution: `npx jest src/components/__tests__/StepperInput.test.tsx` exits 0 with all 6+ cases passing.
    - Behavior assertion (proven by tests): tapping `+` from undefined calls `onChange(min)`; tapping `−` at min OR `+` at max is a no-op; em-dash renders when value is undefined; `1.5` renders for 0.5-step values; boundary buttons set `disabled={true}` and dim glyph to `colors.textSecondary`.
  </acceptance_criteria>
  <done>StepperInput.test.tsx ships with ≥6 passing test cases covering all D-11 assertions; no RTL/jest-native used; jest runner exits 0 on the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → form input | User taps `+` / `−` buttons; numeric values flow into FormBag.basics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering (input) | StepperInput.tsx | mitigate | UI clamps via Math.min(value+step, max) + min-boundary short-circuit prevents out-of-range writes; backend defense-in-depth via Phase 6 validators (M4_BEDROOMS_INVALID / M4_BATHROOM_STEP_INVALID) catches any direct-write code paths. No raw text input — tap-only, so no injection surface. |
| T-07-02 | Denial of Service | StepperInput.tsx | accept | Rapid tap-spam is rate-limited by React's onPress event handling; no expensive operations per tap (pure local state, no network). Low-value target. |
</threat_model>

<verification>
- StepperInput.tsx exists with exported component + props interface.
- Jest suite `src/components/__tests__/StepperInput.test.tsx` exits 0 with ≥6 cases.
- No `@testing-library/react-native` or `fireEvent` references in the new test file.
- KBD-02 grep gate intact: `grep -RIn "keyboardVerticalOffset" src/ | wc -l` returns 0.
- tsc passes with zero net new errors attributable to `StepperInput.tsx` (pre-existing Property.tours errors excluded).
- Accessibility: each Pressable has accessibilityRole + accessibilityLabel + accessibilityState; hitSlop set on both ±.
</verification>

<success_criteria>
- FORM-01: `<StepperInput>` component renders `±` + value + label; tap `+` from undefined → onChange(min); tap `−` at min → no-op; testID prop exposed.
- FORM-03: `±` buttons at min/max render `disabled={true}` with glyph color `colors.textSecondary`; hit-slop on both buttons ≥44pt total tap area.
- All 6 D-11 unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/07-stepper-component-contextuallistingflow-integration/07-01-SUMMARY.md` after completion.
</output>
