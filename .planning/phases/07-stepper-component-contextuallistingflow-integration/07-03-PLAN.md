---
phase: 07-stepper-component-contextuallistingflow-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ContextualListingFlow/types.ts
  - src/components/ContextualListingFlow/adapters.ts
  - src/components/ContextualListingFlow/validators.ts
  - src/components/ContextualListingFlow/__tests__/adapters.test.ts
  - src/components/ContextualListingFlow/__tests__/validators.test.ts
autonomous: true
requirements: [FORM-04, FORM-05]
tags: [types, adapters, validators, formbag, undefined-preserving]

must_haves:
  truths:
    - "FormBag.basics has two new optional numeric fields: bedrooms?: number and bathroomCount?: number"
    - "propertyToFormBag reads p.basics?.bedrooms and p.basics?.bathroomCount through to FormBag (undefined-preserving)"
    - "formBagToPropertyPayload passes v.basics.bedrooms and v.basics.bathroomCount through verbatim (no coerce-to-zero, undefined survives)"
    - "validators.ts FIELD_ORDER_PER_STEP[3] includes 'basics.bedrooms' and 'basics.bathroomCount' appended after 'basics.hotelClass'"
    - "validateStep(3, ...) is DEFENSIVE-ONLY for the two new fields — never required, only validates when value !== undefined AND out-of-range/non-step (FORM-04 lock)"
    - "adapters round-trip test extended: bedrooms=3 and bathroomCount=1.5 survive Property→FormBag→Property unchanged; undefined values survive unchanged (no coerce-to-0)"
    - "validators test extended: bedrooms outside [0,10] OR non-integer surfaces bedroomsInvalid key; bathroomCount outside [0,10] OR non-0.5-step surfaces bathroomCountInvalid key; undefined NEVER surfaces an error"
  artifacts:
    - path: "src/components/ContextualListingFlow/types.ts"
      provides: "FormBag.basics extended with bedrooms + bathroomCount"
      contains: "bedrooms?: number"
    - path: "src/components/ContextualListingFlow/adapters.ts"
      provides: "Bidirectional adapter wiring for new fields"
      contains: "bedrooms: v.basics.bedrooms"
    - path: "src/components/ContextualListingFlow/validators.ts"
      provides: "Defensive Step-3 checks + FIELD_ORDER extension"
      contains: "basics.bedrooms"
    - path: "src/components/ContextualListingFlow/__tests__/adapters.test.ts"
      provides: "Round-trip integrity tests for new fields"
    - path: "src/components/ContextualListingFlow/__tests__/validators.test.ts"
      provides: "Defensive validator tests + undefined-tolerant assertions"
  key_links:
    - from: "src/components/ContextualListingFlow/adapters.ts"
      to: "src/types/Property.ts"
      via: "reads p.basics?.bedrooms (added in Phase 6 SCHEMA-03)"
      pattern: "p\\.basics\\?\\.(bedrooms|bathroomCount)"
    - from: "src/components/ContextualListingFlow/validators.ts"
      to: "src/locales/en.ts (Plan 07-02 keys)"
      via: "error keys: 'contextualListing.step3.bedroomsInvalid', 'contextualListing.step3.bathroomCountInvalid'"
      pattern: "contextualListing\\.step3\\.(bedrooms|bathroomCount)Invalid"
---

<objective>
Wire the two new numeric fields end-to-end through the FormBag pipeline:

1. **types.ts** — extend `FormBag.basics` with `bedrooms?: number` + `bathroomCount?: number` (D-07).
2. **adapters.ts** — bidirectional, undefined-preserving (D-08): `propertyToFormBag` reads new fields; `formBagToPropertyPayload` passes them through verbatim.
3. **validators.ts** — extend `FIELD_ORDER_PER_STEP[3]` with the two new field keys (scroll-to-first-error ordering parity) and add defensive `!== undefined` out-of-range checks (D-09; FORM-04 lock — never require, only validate-if-present).
4. **adapters.test.ts** + **validators.test.ts** — extend existing co-located tests to cover the new fields (round-trip integrity + defensive-only validation).

Purpose: This plan is the form-half foundation. Plan 07-04 (Step3 integration) needs the FormBag type to compile with the new fields and the validator to recognize them in FIELD_ORDER. Plan 07-05 (orchestrator wiring) doesn't depend on this plan but the M4_* error keys it uses are the same ones validators.ts references.

Output: 3 source edits (small, additive) + 2 test extensions.

This plan runs in Wave 1 alongside Plans 07-01 and 07-02. No file overlap with either — `types.ts` / `adapters.ts` / `validators.ts` are not touched by any other Wave 1 plan, and the test extensions live under `ContextualListingFlow/__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Current FormBag.basics (types.ts L21-31) — PATTERNS.md verbatim excerpt -->
```typescript
// === Step 3 (REQUIRED area + price + currency; conditional sub-fields) ===
basics: {
  areaSqm: string;
  price: string;
  currency: 'KGS' | 'USD' | 'EUR' | '';
  rooms?: '1' | '2' | '3' | '4+';
  bathroom?: 'private' | 'none' | 'shared';
  kitchen?: 'private' | 'none' | 'shared';
  hotelRooms?: '1' | '2' | '3' | '4+';
  hotelClass?: 'economy' | 'standard' | 'comfort' | 'premium';
};
```

<!-- Target FormBag.basics (D-07 — append two lines) -->
```typescript
basics: {
  areaSqm: string;
  price: string;
  currency: 'KGS' | 'USD' | 'EUR' | '';
  rooms?: '1' | '2' | '3' | '4+';
  bathroom?: 'private' | 'none' | 'shared';
  kitchen?: 'private' | 'none' | 'shared';
  hotelRooms?: '1' | '2' | '3' | '4+';
  hotelClass?: 'economy' | 'standard' | 'comfort' | 'premium';
  bedrooms?: number;        // M4 FORM-02 — integer 0-10; apartment/house only
  bathroomCount?: number;   // M4 FORM-02 — 0.5-step 0-10; all 6 types
};
```

<!-- Target FIELD_ORDER_PER_STEP[3] (validators.ts L17-26 — append two entries) -->
```typescript
3: [
  'basics.areaSqm',
  'basics.price',
  'basics.currency',
  'basics.rooms',
  'basics.bathroom',
  'basics.kitchen',
  'basics.hotelRooms',
  'basics.hotelClass',
  'basics.bedrooms',
  'basics.bathroomCount',
],
```

<!-- Defensive validator block (D-09 — inserted after hotel/hostel branch, before else if stepN === 4) -->
<!-- Both checks gate on !== undefined first — never require, only validate-if-present -->
<!-- Error keys are added by Plan 07-02; this plan only references them -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend FormBag.basics type (D-07)</name>
  <files>src/components/ContextualListingFlow/types.ts</files>
  <read_first>
    - src/components/ContextualListingFlow/types.ts (full file — 89 LOC; current FormBag.basics at L21-31)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"src/components/ContextualListingFlow/types.ts (MOD)" (verbatim before/after)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §D-07 (FormBag invariant rationale + ?: optionality pattern)
    - src/types/Property.ts (Phase 6 SCHEMA-03 added `bedrooms?: number` + `bathroomCount?: number` under `basics`; verify by grep — adapters need this on the source side)
  </read_first>
  <action>
    Open `src/components/ContextualListingFlow/types.ts`. Locate the `FormBag.basics` interface block (L21-31 per PATTERNS.md). After the `hotelClass?: 'economy' | 'standard' | 'comfort' | 'premium';` line, append exactly two new lines (matching the `?:` optionality of every other Step-3 conditional field):

    - `bedrooms?: number;` — M4 FORM-02 — integer 0-10; apartment/house only
    - `bathroomCount?: number;` — M4 FORM-02 — 0.5-step 0-10; apartment/house/hotel/hostel/office/commercial

    Add a brief inline comment on each new line referencing FORM-02 + the apply-set (per PATTERNS.md target shape). Do NOT introduce a discriminated union or split FormBag.basics into per-property-type variants — D-07 explicitly locks the flat `?:` optionality pattern matching every other Step-3 sub-field (rooms, bathroom, kitchen, hotelRooms, hotelClass). Discriminated unions are intentionally deferred (would force a propertyType-aware migration of every existing adapter test).

    Do not touch other fields. Do not touch the `FormErrorBag` type below. The file remains under 100 LOC.
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        grep -c "bedrooms\\?: number" src/components/ContextualListingFlow/types.ts | grep -q "^1$" &amp;&amp; \
        grep -c "bathroomCount\\?: number" src/components/ContextualListingFlow/types.ts | grep -q "^1$" &amp;&amp; \
        npx tsc --noEmit -p . 2>&amp;1 | grep -v "Property\\.tours" | grep -E "ContextualListingFlow/types\\.ts" | wc -l | grep -q "^0$"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `FormBag.basics` interface contains `bedrooms?: number` and `bathroomCount?: number` (exactly one occurrence each).
    - Source assertion: other Step-3 fields unchanged (`rooms?:`, `bathroom?:`, `kitchen?:`, `hotelRooms?:`, `hotelClass?:` all present with original string-literal unions).
    - tsc passes: no net new errors attributable to `types.ts`.
    - File remains ≤95 LOC (was 89; +6 lines max including comments).
  </acceptance_criteria>
  <done>FormBag.basics extended with two optional numeric fields per D-07; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bidirectional adapters (D-08)</name>
  <files>src/components/ContextualListingFlow/adapters.ts, src/components/ContextualListingFlow/__tests__/adapters.test.ts</files>
  <read_first>
    - src/components/ContextualListingFlow/adapters.ts (full file — 109 LOC; propertyToFormBag at L12-58, formBagToPropertyPayload at L66-109)
    - src/components/ContextualListingFlow/__tests__/adapters.test.ts (existing tests — append to cover new fields)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"src/components/ContextualListingFlow/adapters.ts (MOD)" (verbatim before/after of both regions)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §D-08 (undefined-preserving rationale; no string coercion; verbatim passthrough)
    - src/types/Property.ts (confirm `basics.bedrooms?: number` + `basics.bathroomCount?: number` exist — Phase 6 SCHEMA-03)
  </read_first>
  <action>
    Open `src/components/ContextualListingFlow/adapters.ts`.

    **Edit 1 — propertyToFormBag (L27-36 region):** After the `hotelClass: p.basics?.hotelClass as FormBag['basics']['hotelClass'],` line, append two new lines (per PATTERNS.md target shape):

    - `bedrooms: p.basics?.bedrooms,` — number | undefined, no string coercion (new fields are numeric end-to-end, unlike `areaSqm`/`price` which are string-at-form-time)
    - `bathroomCount: p.basics?.bathroomCount,` — number | undefined

    **Edit 2 — formBagToPropertyPayload (L76-85 region):** After the `hotelClass: v.basics.hotelClass,` line, append two new lines (verbatim passthrough — undefined survives):

    - `bedrooms: v.basics.bedrooms,` — passthrough; backend silently tolerates undefined per Phase 6 schema-permissive validator
    - `bathroomCount: v.basics.bathroomCount,` — passthrough

    Do not alter any other field. Preserve the existing "Strip undefined leaves for clean payload" comment region (L106-108) unchanged — it already covers the new fields.

    **Edit 3 — adapters.test.ts extension:** Append a test block:

    - Test A: `propertyToFormBag` with `{ basics: { ..., bedrooms: 3, bathroomCount: 1.5 } }` → returned FormBag has `basics.bedrooms === 3` AND `basics.bathroomCount === 1.5`.
    - Test B: `propertyToFormBag` with `{ basics: { ... } }` (no bedrooms/bathroomCount keys) → returned FormBag has `basics.bedrooms === undefined` AND `basics.bathroomCount === undefined` (NOT 0; NOT empty string).
    - Test C: `formBagToPropertyPayload` with `{ basics: { ..., bedrooms: 3, bathroomCount: 1.5 } }` → returned Property has `basics.bedrooms === 3` AND `basics.bathroomCount === 1.5`.
    - Test D: `formBagToPropertyPayload` with `{ basics: { ..., bedrooms: undefined, bathroomCount: undefined } }` → returned Property has `basics.bedrooms === undefined` AND `basics.bathroomCount === undefined` (NOT coerced to 0; FORM-05 invariant).
    - Test E (round-trip): start with `Property` having `bedrooms: 3, bathroomCount: 2.5`. Apply `propertyToFormBag` then `formBagToPropertyPayload`. Result has identical `bedrooms` and `bathroomCount`. Proves no information loss.

    Use the existing test file's setup pattern (look at first test for the helper-builder shape — likely `buildProperty()` or inline object literal). Use jest `expect(...).toBe(...)` / `expect(...).toBeUndefined()`.
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        grep -c "bedrooms: p\\.basics\\?\\.bedrooms" src/components/ContextualListingFlow/adapters.ts | grep -q "^1$" &amp;&amp; \
        grep -c "bathroomCount: p\\.basics\\?\\.bathroomCount" src/components/ContextualListingFlow/adapters.ts | grep -q "^1$" &amp;&amp; \
        grep -c "bedrooms: v\\.basics\\.bedrooms" src/components/ContextualListingFlow/adapters.ts | grep -q "^1$" &amp;&amp; \
        grep -c "bathroomCount: v\\.basics\\.bathroomCount" src/components/ContextualListingFlow/adapters.ts | grep -q "^1$" &amp;&amp; \
        npx jest src/components/ContextualListingFlow/__tests__/adapters.test.ts --silent 2>&amp;1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `propertyToFormBag` has exactly one `bedrooms: p.basics?.bedrooms` and one `bathroomCount: p.basics?.bathroomCount` line.
    - Source assertion: `formBagToPropertyPayload` has exactly one `bedrooms: v.basics.bedrooms` and one `bathroomCount: v.basics.bathroomCount` line.
    - Behavior assertion (proven by tests): undefined round-trips as undefined (no coerce-to-zero); 3 and 1.5 round-trip unchanged.
    - Jest: `npx jest src/components/ContextualListingFlow/__tests__/adapters.test.ts` exits 0; new test cases pass.
    - tsc passes: no net new errors attributable to `adapters.ts`.
  </acceptance_criteria>
  <done>Adapters bidirectional, undefined-preserving for both new fields; adapters.test.ts covers Property↔FormBag round-trip + undefined-survival + non-coerce invariants.</done>
</task>

<task type="auto">
  <name>Task 3: Extend FIELD_ORDER + defensive validators (D-09 / FORM-04)</name>
  <files>src/components/ContextualListingFlow/validators.ts, src/components/ContextualListingFlow/__tests__/validators.test.ts</files>
  <read_first>
    - src/components/ContextualListingFlow/validators.ts (full file — 103 LOC; FIELD_ORDER_PER_STEP at L17-26; step-3 validator block at L55-75)
    - src/components/ContextualListingFlow/__tests__/validators.test.ts (existing tests — append cases)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"src/components/ContextualListingFlow/validators.ts (MOD)" (verbatim before/after of both edits)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §D-09 (defensive-only; never required; FORM-04 lock)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §Specific Ideas L64-75 (defensive form snippets)
  </read_first>
  <action>
    Open `src/components/ContextualListingFlow/validators.ts`.

    **Edit 1 — FIELD_ORDER_PER_STEP[3] (L17-26):** After the `'basics.hotelClass',` entry, append exactly two new string entries:

    - `'basics.bedrooms',`
    - `'basics.bathroomCount',`

    This preserves scroll-to-first-error ordering parity (per D-09). Order: bedrooms before bathroomCount (matches Step3 display order — D-06).

    **Edit 2 — step-3 validator block (L55-75):** After the `else if (pt === 'hotel' || pt === 'hostel')` branch's closing `}` (around L73), but BEFORE the `} else if (stepN === 4) {` line (around L75), insert two defensive `if` blocks. Both gate on `!== undefined` first — never required, only validate-if-present (FORM-04 lock):

    - Bedrooms check: if `values.basics.bedrooms !== undefined` AND (`!Number.isInteger(values.basics.bedrooms)` OR `values.basics.bedrooms < 0` OR `values.basics.bedrooms > 10`) → set `errors['basics.bedrooms'] = 'contextualListing.step3.bedroomsInvalid'`.
    - BathroomCount check: if `values.basics.bathroomCount !== undefined` AND (`!Number.isInteger(values.basics.bathroomCount * 2)` OR `values.basics.bathroomCount < 0` OR `values.basics.bathroomCount > 10`) → set `errors['basics.bathroomCount'] = 'contextualListing.step3.bathroomCountInvalid'`. The `bathroomCount * 2` integer check enforces 0.5-step (matches Phase 6 SCHEMA-02 backend validator shape).

    Add a brief comment above the two blocks: `// NEW M4 FORM-04 — defensive only; stepper UI clamps prevent these from firing. Catches direct-write code paths.` (per PATTERNS.md target shape).

    These error keys (`contextualListing.step3.bedroomsInvalid` / `bathroomCountInvalid`) are added by Plan 07-02; this plan only references them. No new imports needed.

    **Edit 3 — validators.test.ts extension:** Append cases:

    - Test 1 (FORM-04 undefined-OK): `validateStep(3, {...minimal-valid-apartment, basics: {...basics, bedrooms: undefined, bathroomCount: undefined}})` → `errors['basics.bedrooms']` is undefined; `errors['basics.bathroomCount']` is undefined. Submit-OK invariant for FORM-04 on every propertyType.
    - Test 2 (bedrooms out-of-range): `bedrooms: 11` → `errors['basics.bedrooms'] === 'contextualListing.step3.bedroomsInvalid'`.
    - Test 3 (bedrooms non-integer): `bedrooms: 2.5` → `errors['basics.bedrooms'] === 'contextualListing.step3.bedroomsInvalid'`.
    - Test 4 (bedrooms valid): `bedrooms: 3` → no error key set.
    - Test 5 (bathroomCount non-0.5-step): `bathroomCount: 1.3` → `errors['basics.bathroomCount'] === 'contextualListing.step3.bathroomCountInvalid'`.
    - Test 6 (bathroomCount valid 0.5-step): `bathroomCount: 1.5` → no error key set.
    - Test 7 (bathroomCount out-of-range): `bathroomCount: 11` → error set.
    - Test 8 (FIELD_ORDER parity): `FIELD_ORDER_PER_STEP[3]` contains `'basics.bedrooms'` AND `'basics.bathroomCount'` in that order, appended after `'basics.hotelClass'`.

    Use the existing test file's setup pattern (likely a `buildValuesFor(propertyType)` helper — reuse or inline).
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        grep -cE "'basics\\.bedrooms'," src/components/ContextualListingFlow/validators.ts | grep -qE "^[1-9]" &amp;&amp; \
        grep -cE "'basics\\.bathroomCount'," src/components/ContextualListingFlow/validators.ts | grep -qE "^[1-9]" &amp;&amp; \
        grep -q "values\\.basics\\.bedrooms !== undefined" src/components/ContextualListingFlow/validators.ts &amp;&amp; \
        grep -q "values\\.basics\\.bathroomCount !== undefined" src/components/ContextualListingFlow/validators.ts &amp;&amp; \
        grep -q "Number\\.isInteger(values\\.basics\\.bathroomCount \\* 2)" src/components/ContextualListingFlow/validators.ts &amp;&amp; \
        grep -q "contextualListing\\.step3\\.bedroomsInvalid" src/components/ContextualListingFlow/validators.ts &amp;&amp; \
        grep -q "contextualListing\\.step3\\.bathroomCountInvalid" src/components/ContextualListingFlow/validators.ts &amp;&amp; \
        npx jest src/components/ContextualListingFlow/__tests__/validators.test.ts --silent 2>&amp;1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `FIELD_ORDER_PER_STEP[3]` array contains `'basics.bedrooms'` and `'basics.bathroomCount'` (verifiable via grep on the two string literals).
    - Source assertion: defensive checks present: `values.basics.bedrooms !== undefined` AND `values.basics.bathroomCount !== undefined` guard the validation blocks.
    - Source assertion: bathroomCount validator uses `Number.isInteger(values.basics.bathroomCount * 2)` for 0.5-step enforcement.
    - Source assertion: error keys reference `contextualListing.step3.bedroomsInvalid` / `bathroomCountInvalid` (matching Plan 07-02 keys).
    - Behavior assertion (proven by tests): undefined → no error; 11 → error; 2.5 bedrooms → error; 1.3 bathroomCount → error; 1.5 bathroomCount → no error; 3 bedrooms → no error.
    - Jest: `npx jest src/components/ContextualListingFlow/__tests__/validators.test.ts` exits 0; all new cases pass.
    - tsc passes: no net new errors attributable to `validators.ts`.
  </acceptance_criteria>
  <done>FIELD_ORDER extended; defensive Step-3 checks added; validators.test.ts proves FORM-04 (undefined-OK on every propertyType) + out-of-range / non-step rejection paths; references Plan 07-02 error keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FormBag → backend payload | FormBag values flow through `formBagToPropertyPayload` to backend POST/PUT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering (input) | validators.ts | mitigate | Defensive `!== undefined` + bounds + step-multiple checks catch direct-write code paths (e.g., copy/paste from another listing) before the form submits. Mongoose validate in Phase 6 is the second layer; route-layer 400 codes are the third. |
| T-07-05 | Information Disclosure | adapters.ts | accept | New fields are numeric counts (bedrooms / bathroomCount) — no PII; same exposure class as the existing `rooms`/`bathroom`/`kitchen` enums. Round-trip preserves all values verbatim (no truncation, no silent loss). |
</threat_model>

<verification>
- types.ts: `FormBag.basics` has the two new optional numeric fields.
- adapters.ts: bidirectional, undefined-preserving for both new fields.
- validators.ts: FIELD_ORDER extended; defensive Step-3 checks added; FORM-04 lock honored (undefined never errors).
- Jest: `npx jest src/components/ContextualListingFlow/__tests__/{adapters,validators}.test.ts` exits 0.
- tsc passes with zero net new errors.
</verification>

<success_criteria>
- FORM-04: `validateStep(3, ...)` accepts `undefined` for both new fields on every applicable property type. Out-of-range / non-step values surface inline error keys (which Step3 renders via Plan 07-04's wiring).
- FORM-05 foundation: adapters round-trip Property↔FormBag without coerce-to-zero. Edit-mode pre-population (Plan 07-04) and submit verbatim-undefined (Plan 07-05) both inherit this invariant.
</success_criteria>

<output>
Create `.planning/phases/07-stepper-component-contextuallistingflow-integration/07-03-SUMMARY.md` after completion.
</output>
