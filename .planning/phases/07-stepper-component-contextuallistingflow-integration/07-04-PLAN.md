---
phase: 07-stepper-component-contextuallistingflow-integration
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - src/components/ContextualListingFlow/Step3BasicInfo.tsx
  - src/components/ContextualListingFlow/__tests__/Step3.test.tsx
autonomous: true
requirements: [FORM-02, FORM-05]
tags: [step3, conditional-rendering, stepper-integration, edit-mode]

must_haves:
  truths:
    - "Step 3 renders a bedrooms stepper row for apartment + house only (D-05 / D-06 / FORM-02)"
    - "Step 3 renders a bathroomCount stepper row for apartment + house + hotel + hostel + office + commercial (FORM-02)"
    - "Land propertyType renders NEITHER new stepper row"
    - "Bedrooms stepper sits above bathroomCount stepper when both render (D-06)"
    - "Both new rows sit AFTER the existing chip-row conditionals (D-05 — areaSqm → price → currency → existing chips → bedrooms → bathroomCount)"
    - "Stepper rows are wired to FormBag.basics via setBasics({ bedrooms: v }) / setBasics({ bathroomCount: v }) (D-08)"
    - "Edit-owner / edit-mod modes pre-populate steppers from property.basics.bedrooms / bathroomCount (FORM-05)"
    - "Inline error row below each stepper reads errors['basics.bedrooms'] / errors['basics.bathroomCount'] via t() (D-09 + D-10 surfacing path)"
    - "Step3 integration tests cover the 4+ cases in D-12"
  artifacts:
    - path: "src/components/ContextualListingFlow/Step3BasicInfo.tsx"
      provides: "Two new conditional stepper rows at the bottom of Step 3"
      contains: "StepperInput"
    - path: "src/components/ContextualListingFlow/__tests__/Step3.test.tsx"
      provides: "Integration cases covering FORM-02 conditional render + FORM-05 edit-mode pre-population"
  key_links:
    - from: "src/components/ContextualListingFlow/Step3BasicInfo.tsx"
      to: "src/components/StepperInput.tsx (Plan 07-01)"
      via: "import { StepperInput } from '../StepperInput'"
      pattern: "from '\\.\\./StepperInput'"
    - from: "src/components/ContextualListingFlow/Step3BasicInfo.tsx"
      to: "src/locales/en.ts (Plan 07-02 keys)"
      via: "t('contextualListing.step3.bedroomsLabel') / t('contextualListing.step3.bathroomCountLabel')"
      pattern: "contextualListing\\.step3\\.(bedrooms|bathroomCount)Label"
    - from: "src/components/ContextualListingFlow/Step3BasicInfo.tsx"
      to: "src/components/ContextualListingFlow/types.ts (Plan 07-03 FormBag extension)"
      via: "values.basics.bedrooms / values.basics.bathroomCount via setBasics patch"
      pattern: "setBasics\\(\\{ (bedrooms|bathroomCount):"
---

<objective>
Integrate `<StepperInput>` into `Step3BasicInfo` as two conditional rows per FORM-02 + D-05 + D-06 placement rules — and extend the Step3 integration tests to cover the 4+ cases locked in D-12 (FORM-02 conditional-render matrix + FORM-05 edit-mode pre-population + verbatim-undefined submit).

Purpose: This is the user-visible half of FORM-02 + FORM-05. The component (Plan 07-01) and the keys (Plan 07-02) and the FormBag wiring (Plan 07-03) all exist by the time this plan runs — this plan is the integration step that makes the form actually show the steppers.

Output: One additive source edit (two conditional `<View>` blocks appended at the bottom of Step3's return) + one test extension.

Wave-2 dependency: requires 07-01 (StepperInput exists), 07-02 (i18n keys exist), 07-03 (FormBag.basics types + adapters compile with the new keys).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-01-PLAN.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-02-PLAN.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-03-PLAN.md

<interfaces>
<!-- StepperInput props (from Plan 07-01) -->
```typescript
interface StepperInputProps {
  value: number | undefined;
  onChange: (v: number | undefined) => void;
  min: number;
  max: number;
  step: number; // 1 or 0.5
  label: string;
  testID?: string;
  accessibilityLabel?: string;
}
```

<!-- New rows shape (PATTERNS.md verbatim target) — placed after the hotel/hostel block at L271 -->
<!-- Bedrooms row: pt === 'apartment' || pt === 'house' -->
<!-- BathroomCount row: pt === 'apartment' || pt === 'house' || pt === 'hotel' || pt === 'hostel' || pt === 'office' || pt === 'commercial' -->
<!-- Both rows wrap a single <View style={commonStyles.section}> containing StepperInput + optional inline error <Text> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bedrooms + bathroomCount stepper rows to Step3BasicInfo (FORM-02)</name>
  <files>src/components/ContextualListingFlow/Step3BasicInfo.tsx</files>
  <read_first>
    - src/components/ContextualListingFlow/Step3BasicInfo.tsx (full file — 274 LOC; setBasics at L42-48; existing hotel/hostel conditional block closing at L271; closing `</View>` at L272)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"src/components/ContextualListingFlow/Step3BasicInfo.tsx (MOD)" (verbatim before/after — new rows target shape)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §D-05 (bottom placement after existing chip rows), §D-06 (bedrooms above bathroomCount when both render), §Specific Ideas L246-264 (verbatim conditional-render shape)
    - src/components/StepperInput.tsx (from Plan 07-01 — props contract + testID derivation)
    - src/components/ContextualListingFlow/styles.ts (verify `commonStyles.section`, `commonStyles.sectionLabel`, `commonStyles.errorText` exist — used by every other Step3 row)
  </read_first>
  <action>
    Open `src/components/ContextualListingFlow/Step3BasicInfo.tsx`.

    **Edit 1 — Add import:** At the top of the file, alongside the existing `import` lines, add `import { StepperInput } from '../StepperInput';`. Use relative path `../StepperInput` since this file is in `src/components/ContextualListingFlow/` and the new component is in `src/components/`. CONVENTIONS L57-67 ordering: add it after the local-sibling imports (after the existing `'./...'` imports if any, or after the `'../...'` block alongside the other `../components/...` imports). If unsure, place it immediately after the `import { useLanguage } from '../../context/LanguageContext';`-like block.

    **Edit 2 — Add two conditional stepper row blocks** at the bottom of the return JSX, AFTER the existing hotel/hostel conditional block closes (line 271 `</>`) but BEFORE the closing `</View>` of the outer container (line 272). Insert in this exact order (D-06 — bedrooms above bathroomCount):

    Block A — Bedrooms stepper (apartment + house only):
    - Wrap in `{pt === 'apartment' || pt === 'house' ? (...) : null}` conditional.
    - Inside: a `<View style={commonStyles.section}>` containing:
      - `<StepperInput testID="basics-bedrooms" label={t('contextualListing.step3.bedroomsLabel')} value={values.basics.bedrooms} onChange={(v) => setBasics({ bedrooms: v })} min={0} max={10} step={1} />`
      - Inline error row (same shape as every other Step3 sub-field — see L117-121 / L144-148 / L163-167 pattern): `{errors['basics.bedrooms'] ? (<Text style={[commonStyles.errorText, { color: colors.error }]}>{t(errors['basics.bedrooms'] as TranslationKeys)}</Text>) : null}`

    Block B — BathroomCount stepper (apartment + house + hotel + hostel + office + commercial):
    - Wrap in `{pt === 'apartment' || pt === 'house' || pt === 'hotel' || pt === 'hostel' || pt === 'office' || pt === 'commercial' ? (...) : null}` conditional. Equivalent: `pt && pt !== 'land'` — but use the explicit OR-chain to mirror the existing chip-row conditional style and so reviewers can directly verify the 6-type apply-set.
    - Inside: `<View style={commonStyles.section}>` containing:
      - `<StepperInput testID="basics-bathroomCount" label={t('contextualListing.step3.bathroomCountLabel')} value={values.basics.bathroomCount} onChange={(v) => setBasics({ bathroomCount: v })} min={0} max={10} step={0.5} />`
      - Inline error row reading `errors['basics.bathroomCount']` (same shape as Block A).

    NOTE on type narrowing for `errors[...]`: the existing rows cast the error key with `as TranslationKeys` because `errors[K]` returns `string | undefined` but `t()` requires a known key. Follow the existing pattern verbatim — see PATTERNS.md §"Existing conditional chip-row block" excerpt L283.

    **Do NOT modify** the existing chip rows (rooms, bathroom, kitchen, hotelRooms, hotelClass), the setBasics helper, the SectionProps interface, the imports unrelated to StepperInput, or the file's exported function signature. The file grows from 274 LOC to ~315 LOC (estimate: 2 conditional blocks × ~20 LOC each = ~40 LOC added).
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        grep -q "import { StepperInput } from '\\.\\./StepperInput'" src/components/ContextualListingFlow/Step3BasicInfo.tsx &amp;&amp; \
        grep -q 'testID="basics-bedrooms"' src/components/ContextualListingFlow/Step3BasicInfo.tsx &amp;&amp; \
        grep -q 'testID="basics-bathroomCount"' src/components/ContextualListingFlow/Step3BasicInfo.tsx &amp;&amp; \
        grep -q "contextualListing\\.step3\\.bedroomsLabel" src/components/ContextualListingFlow/Step3BasicInfo.tsx &amp;&amp; \
        grep -q "contextualListing\\.step3\\.bathroomCountLabel" src/components/ContextualListingFlow/Step3BasicInfo.tsx &amp;&amp; \
        grep -q "setBasics({ bedrooms: v })" src/components/ContextualListingFlow/Step3BasicInfo.tsx &amp;&amp; \
        grep -q "setBasics({ bathroomCount: v })" src/components/ContextualListingFlow/Step3BasicInfo.tsx &amp;&amp; \
        grep -q "errors\\['basics\\.bedrooms'\\]" src/components/ContextualListingFlow/Step3BasicInfo.tsx &amp;&amp; \
        grep -q "errors\\['basics\\.bathroomCount'\\]" src/components/ContextualListingFlow/Step3BasicInfo.tsx &amp;&amp; \
        grep -cE "^#" src/components/ContextualListingFlow/Step3BasicInfo.tsx | xargs -I{} test {} -eq 0 2>/dev/null; \
        (grep -v '^[[:space:]]*//\\|^[[:space:]]*\\*\\|^[[:space:]]*/\\*\\|^[[:space:]]*\\*/' src/components/ContextualListingFlow/Step3BasicInfo.tsx | grep -c "keyboardVerticalOffset" | grep -q "^0$") &amp;&amp; \
        npx tsc --noEmit -p . 2>&amp;1 | grep -v "Property\\.tours" | grep -E "Step3BasicInfo\\.tsx" | wc -l | grep -q "^0$"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `import { StepperInput } from '../StepperInput'` present at the top of the file.
    - Source assertion: exactly one `testID="basics-bedrooms"` and one `testID="basics-bathroomCount"` in the file.
    - Source assertion: both new label keys (`bedroomsLabel`, `bathroomCountLabel`) referenced via `t(...)`.
    - Source assertion: both new onChange handlers use `setBasics({ ... })` (FormBag write path per D-08).
    - Source assertion: both error rows reference `errors['basics.bedrooms']` / `errors['basics.bathroomCount']` for inline-render path (D-09 + D-10 surfacing).
    - Source assertion: bedrooms row guards on `pt === 'apartment' || pt === 'house'`; bathroomCount row guards on the 6-type OR-chain.
    - Order assertion: bedrooms row appears BEFORE bathroomCount row in source (D-06). Verifiable: line number of `testID="basics-bedrooms"` is less than line number of `testID="basics-bathroomCount"`.
    - KBD-02 grep gate preserved: `grep -RIn "keyboardVerticalOffset" src/ | wc -l` returns 0 (excluding comments — stepper is tap-only, must not introduce keyboard surface).
    - tsc passes: zero net new errors attributable to `Step3BasicInfo.tsx` (pre-existing Property.tours errors excluded).
  </acceptance_criteria>
  <done>Step3 renders bedrooms (apartment/house) + bathroomCount (6 types) stepper rows at the bottom, after existing chip rows, in D-06 order; wired to FormBag via setBasics; inline-error path live; tsc clean; KBD-02 gate intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Step3 integration tests (D-12 — min 4 cases)</name>
  <files>src/components/ContextualListingFlow/__tests__/Step3.test.tsx</files>
  <read_first>
    - src/components/ContextualListingFlow/__tests__/Step3.test.tsx (full file — review existing setup, build helpers, and test patterns; D-12 extends this file)
    - src/components/ContextualListingFlow/Step3BasicInfo.tsx (post-Task-1 state — verifies test selectors hit the new testIDs)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §D-12 (4-case minimum locked)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"src/components/__tests__/StepperInput.test.tsx" (act + react-test-renderer convention; same in Step3.test.tsx)
  </read_first>
  <behavior>
    - D-12.1: Apartment with no values (basics.bedrooms === undefined, basics.bathroomCount === undefined) → both stepper rows visible (`tryFindByTestID('basics-bedrooms-value')` and `'basics-bathroomCount-value'` both return non-null nodes); both value cells render em-dash `—`.
    - D-12.2: Hotel propertyType → bathroomCount stepper visible (`basics-bathroomCount-value` exists); bedrooms stepper absent (`tryFindByTestID('basics-bedrooms-value')` returns null). Repeat for hostel + office + commercial (parameterize via test.each or three explicit tests). For land → BOTH absent.
    - D-12.3: Edit-owner mode with `property.basics.bedrooms === 3` → bedrooms stepper's value cell renders `'3'` after propertyToFormBag pre-populates. Submit-without-changing dispatches `bedrooms: 3` verbatim through the FormBag→payload adapter (proven by inspecting `formBagToPropertyPayload(values).basics.bedrooms === 3` OR by spying on PropertyService — pick whichever the existing edit-mode tests use; reuse precedent).
    - D-12.4: Edit-owner mode with `property.basics.bedrooms === undefined` → bedrooms stepper renders em-dash `—`. Submit-without-changing dispatches `bedrooms: undefined` verbatim (NOT 0; NOT empty; NOT null). Same path for bathroomCount.
    - Optional D-12.5 (planner expansion): edit-mod variant — render `<ContextualListingFlow mode="edit-mod" initialListing={...} moderatorContext={...} />` with a residential listing; stepper rows still appear; moderator can backfill bedrooms via stepper without touching other fields. Confirms FORM-05 edit-mod parity.
  </behavior>
  <action>
    Open `src/components/ContextualListingFlow/__tests__/Step3.test.tsx`. Read existing setup to identify:
    - The mount helper (likely renders `<Step3BasicInfo values={...} onChange={...} errors={...} />` directly, OR mounts the full `<ContextualListingFlow ... />` orchestrator).
    - The `findByTestID` / `tryFindByTestID` helpers (PATTERNS.md confirms they exist at L49-56 of the original Step3.test.tsx).
    - The mock setup (likely `jest.mock('../../theme/ThemeContext'` and `'../../context/LanguageContext'`).

    Append the new D-12 cases at the bottom of the file (after the last existing `describe` block or `test` block). Use the same `act`/`react-test-renderer` pattern. Reuse the existing build helpers if present (e.g. `buildValuesForApartment()` or similar — extend with new fields if the helper enforces them).

    Implementation notes for each case:

    - Case 1 — apartment with undefined values:
      - Build `values` with `propertyType: 'apartment'`, `basics.bedrooms: undefined`, `basics.bathroomCount: undefined`.
      - Mount `<Step3BasicInfo>` (or full orchestrator at Step 3).
      - Assert `tryFindByTestID(root, 'basics-bedrooms-value')` is non-null; assert `tryFindByTestID(root, 'basics-bathroomCount-value')` is non-null.
      - Assert both `.props.children === '—'`.

    - Case 2 — hotel hides bedrooms:
      - Build `values` with `propertyType: 'hotel'`. Mount.
      - Assert `tryFindByTestID(root, 'basics-bedrooms-value')` returns null.
      - Assert `tryFindByTestID(root, 'basics-bathroomCount-value')` returns non-null.
      - Repeat for `hostel`, `office`, `commercial` (parameterize via `test.each([['hotel'], ['hostel'], ['office'], ['commercial']])` if the existing file uses `test.each`; otherwise four explicit tests).
      - Land case: `propertyType: 'land'` → both `tryFindByTestID(...)` return null.

    - Case 3 — edit-owner pre-populated bedrooms === 3:
      - If the existing Step3 tests mount only `<Step3BasicInfo>` directly with a synthesized `values` object, this case can synthesize `values: { propertyType: 'apartment', basics: { ..., bedrooms: 3, bathroomCount: undefined } }` and assert `findByTestID('basics-bedrooms-value').props.children === '3'`.
      - Round-trip integrity: pass the `values` through `formBagToPropertyPayload(values)` (import from `../adapters`) and assert `.basics.bedrooms === 3`. Proves no coerce.
      - If the existing tests mount the full orchestrator with `mode='edit-owner'` and `initialListing`, use that path instead — pass `initialListing.basics.bedrooms = 3` and verify the stepper renders `'3'` after `propertyToFormBag` runs at mount.

    - Case 4 — edit-owner with undefined survives:
      - Build `values` with `propertyType: 'apartment'`, `basics.bedrooms: undefined`. Mount.
      - Assert value cell renders `—`.
      - Apply `formBagToPropertyPayload(values)` → assert `.basics.bedrooms === undefined` (FORM-05 verbatim-undefined invariant). Symmetric check for bathroomCount.

    All `onPress` invocations inside `await act(async () => { ... })`. Use `findByProps({ testID: '...' })` to walk to specific elements. Use `tryFindByTestID` for null-checks (existence assertions).
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        test -f src/components/ContextualListingFlow/__tests__/Step3.test.tsx &amp;&amp; \
        grep -c "basics-bedrooms" src/components/ContextualListingFlow/__tests__/Step3.test.tsx | grep -qE "^[3-9]|^[1-9][0-9]+" &amp;&amp; \
        grep -c "basics-bathroomCount" src/components/ContextualListingFlow/__tests__/Step3.test.tsx | grep -qE "^[3-9]|^[1-9][0-9]+" &amp;&amp; \
        grep -q "bedrooms: 3" src/components/ContextualListingFlow/__tests__/Step3.test.tsx &amp;&amp; \
        grep -q "bedrooms: undefined" src/components/ContextualListingFlow/__tests__/Step3.test.tsx &amp;&amp; \
        npx jest src/components/ContextualListingFlow/__tests__/Step3.test.tsx --silent 2>&amp;1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file references both `basics-bedrooms` and `basics-bathroomCount` testIDs in at least 3 places each (rendering existence + value assertions + null-check absence assertions).
    - Source assertion: at least one test asserts `bedrooms: undefined` round-trips as undefined (FORM-05 verbatim-undefined lock).
    - Source assertion: at least one test asserts `bedrooms: 3` pre-populates the stepper (FORM-05 edit-mode pre-population).
    - Source assertion: at least one test covers each of {hotel, hostel, office, commercial} hiding the bedrooms stepper (FORM-02 conditional matrix); at least one covers land hiding both.
    - Jest: `npx jest src/components/ContextualListingFlow/__tests__/Step3.test.tsx` exits 0; all existing cases still pass AND all new D-12 cases pass.
    - No new test infrastructure introduced (still react-test-renderer + act + findByProps — no RTL).
  </acceptance_criteria>
  <done>Step3.test.tsx covers D-12 (≥4 new cases): apartment shows both rows; hotel/hostel/office/commercial show only bathroom; land shows neither; edit-owner pre-populates and round-trips bedrooms verbatim including undefined. Jest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → Step3 form state | User taps stepper buttons; values flow into FormBag.basics via setBasics |
| FormBag → backend payload (via formBagToPropertyPayload) | Numeric values flow through unchanged on submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering (UI bypass) | Step3BasicInfo conditional render | mitigate | Conditional render gates bedrooms on `pt === 'apartment' || pt === 'house'` and bathroomCount on the explicit 6-type OR-chain. If a user somehow forces a hotel listing with bedrooms set (via direct API), backend body-strip (Phase 6 SCHEMA-04) silently drops it. Defense-in-depth holds. |
| T-07-07 | Information Disclosure | Step3 error rendering | accept | Inline error row pattern is identical to existing Step3 rows — no PII surface; error text is localized validation message. |
</threat_model>

<verification>
- Step3BasicInfo.tsx: 2 new conditional `<View>` blocks at the bottom, bedrooms before bathroomCount.
- StepperInput import path correct (`../StepperInput`).
- All FormBag write paths use `setBasics({ ... })` patch shape (preserves D-02 single-source-of-truth).
- Error rendering reads `errors['basics.bedrooms']` / `errors['basics.bathroomCount']` — consumes Plan 07-05's M4_* discriminator output and Plan 07-03's defensive validator output through the same channel.
- Jest: Step3.test.tsx exits 0 with D-12 cases passing.
- KBD-02 grep gate intact.
- tsc passes with zero net new errors.
</verification>

<success_criteria>
- FORM-02: Step 3 conditional matrix correct — apartment/house show BOTH rows; hotel/hostel/office/commercial show ONLY bathroom; land shows NEITHER. Bedrooms above bathroomCount.
- FORM-05 (visible half): edit-mode pre-populates from `property.basics.bedrooms` / `bathroomCount`; em-dash when absent; submit dispatches undefined verbatim.
- D-12 4-case minimum met (parameterized variants count toward the bar).
</success_criteria>

<output>
Create `.planning/phases/07-stepper-component-contextuallistingflow-integration/07-04-SUMMARY.md` after completion.
</output>
