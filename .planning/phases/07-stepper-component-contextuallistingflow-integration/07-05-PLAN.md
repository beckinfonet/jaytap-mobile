---
phase: 07-stepper-component-contextuallistingflow-integration
plan: 05
type: execute
wave: 2
depends_on: [07-02, 07-03]
files_modified:
  - src/components/ContextualListingFlow/index.tsx
autonomous: true
requirements: [FORM-05]
tags: [orchestrator, propertytype-reflow, submit-catch, m4-error-discriminator]

must_haves:
  truths:
    - "propertyType change in Step 1 clears values.basics.bedrooms (uniformly, on every propertyType switch — Claude's Discretion in CONTEXT.md)"
    - "values.basics.bathroomCount is INTENTIONALLY NOT cleared on propertyType change (applies to all 6 types per D-08; preserves entered value across switches)"
    - "Submit catch handler discriminates backend 400 codes: M4_BEDROOMS_INVALID → set errors['basics.bedrooms'] = 'contextualListing.step3.bedroomsInvalid' + setCurrentStep(3) (D-10)"
    - "Submit catch handler discriminates M4_BATHROOM_STEP_INVALID → set errors['basics.bathroomCount'] = 'contextualListing.step3.bathroomCountInvalid' + setCurrentStep(3) (D-10)"
    - "Generic Alert fallback preserved for non-M4 errors (FORM-05 doesn't introduce regression)"
    - "setErrors and setCurrentStep are reused from existing useState declarations at L56-57 (no new state introduced)"
  artifacts:
    - path: "src/components/ContextualListingFlow/index.tsx"
      provides: "propertyType-clear extension + submit-catch M4_* discriminator"
      contains: "M4_BEDROOMS_INVALID"
  key_links:
    - from: "src/components/ContextualListingFlow/index.tsx"
      to: "src/locales/en.ts (Plan 07-02 keys)"
      via: "errors['basics.bedrooms'] = 'contextualListing.step3.bedroomsInvalid'"
      pattern: "contextualListing\\.step3\\.(bedrooms|bathroomCount)Invalid"
    - from: "src/components/ContextualListingFlow/index.tsx (catch)"
      to: "src/components/ContextualListingFlow/Step3BasicInfo.tsx (inline error rendering)"
      via: "errors map shared via FormErrorBag → Step3 renders errors['basics.bedrooms']"
      pattern: "setErrors.*basics\\.bedrooms"
---

<objective>
Wire the orchestrator-level glue that completes FORM-05:

1. **propertyType-change clear extension** — Add `bedrooms: undefined` to the Step-3-clear block at index.tsx L73-82 (per Claude's Discretion in CONTEXT.md L111). `bathroomCount` is INTENTIONALLY OMITTED — it applies to all 6 types, so preserving entered value across propertyType switches is the better UX.

2. **Submit-catch M4_* discriminator** — Extend the catch handler at index.tsx L171-177 to discriminate backend 400 codes from Phase 6 D-11 (`M4_BEDROOMS_INVALID` / `M4_BATHROOM_STEP_INVALID`). On match: write the localized error key into the FormErrorBag at the matching dotted-path key AND navigate to Step 3 via `setCurrentStep(3)`. Non-matching errors fall through to the existing generic `Alert.alert` path (preserve current behavior for non-M4 failures).

Purpose: The backend codes were added in Phase 6 D-11 explicitly so Phase 7 can discriminate. Without this wiring, a malformed payload (practically only reachable via direct-write code paths since the stepper UI clamps) would surface as a generic toast, defeating the purpose of the dedicated 400 codes.

Output: Two surgical edits to `index.tsx` (L73-82 region + L171-177 region). No new files.

Wave-2 dependency: requires 07-02 (i18n keys for `bedroomsInvalid` / `bathroomCountInvalid` must exist) and 07-03 (FormBag.basics must compile with `bedrooms`/`bathroomCount` so the clear-list extension type-checks). Does NOT depend on 07-01 (StepperInput) or 07-04 (Step3 integration) — this plan owns only orchestrator-level wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-02-PLAN.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-03-PLAN.md

<interfaces>
<!-- Existing propertyType-change clear block (index.tsx L73-82 — PATTERNS.md verbatim) -->
```typescript
if (field === 'propertyType' && prev.propertyType !== value) {
  next.basics = {
    ...prev.basics,
    rooms: undefined,
    bathroom: undefined,
    kitchen: undefined,
    hotelRooms: undefined,
    hotelClass: undefined,
  };
}
```

<!-- Target (add bedrooms: undefined; OMIT bathroomCount) -->
```typescript
if (field === 'propertyType' && prev.propertyType !== value) {
  next.basics = {
    ...prev.basics,
    rooms: undefined,
    bathroom: undefined,
    kitchen: undefined,
    hotelRooms: undefined,
    hotelClass: undefined,
    bedrooms: undefined,        // M4 — residential-only; clear on any propertyType switch
    // bathroomCount intentionally NOT cleared — applies to all 6 types (D-08 + Claude's Discretion L111)
  };
}
```

<!-- Existing submit catch (index.tsx L171-177 — verified by current Read) -->
```typescript
} catch (e: unknown) {
  const msg =
    (e as { message?: string }).message ?? t('common.errorGeneric');
  Alert.alert(t('common.error'), msg);
} finally {
  setIsSubmitting(false);
}
```

<!-- Target (discriminate M4_* before generic fallback; D-10 + Specific Ideas L270-282) -->
<!-- setErrors at L56, setCurrentStep at L57 — already in scope inside this useCallback -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend propertyType-change clear block (Claude's Discretion L111)</name>
  <files>src/components/ContextualListingFlow/index.tsx</files>
  <read_first>
    - src/components/ContextualListingFlow/index.tsx lines 64-94 (full onChange callback with both the propertyType-clear block at L73-82 AND the dealType-clear block at L83-89)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"src/components/ContextualListingFlow/index.tsx (MOD) — propertyType-clear" (verbatim before/after)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §"Claude's Discretion" §"propertyType-change reflow handling for the two new fields" (L111 — exact rationale for clearing bedrooms uniformly + omitting bathroomCount)
    - src/components/ContextualListingFlow/types.ts (post-Plan-07-03 state — FormBag.basics has `bedrooms?: number` so the clear-list extension type-checks)
  </read_first>
  <action>
    Open `src/components/ContextualListingFlow/index.tsx`. Locate the propertyType-change clear block (L73-82 — anchored by the comment `// ROADMAP SC#3 (Plan 02-03): switching propertyType in Step 1 reflows Step 3 —`).

    Edit the `next.basics = { ... }` object literal: append exactly one new line after the `hotelClass: undefined,` entry:

    `bedrooms: undefined,        // M4 — residential-only; clear on any propertyType switch`

    Add an inline comment IMMEDIATELY below it (no code, just a comment) documenting the intentional omission of `bathroomCount` (otherwise a future maintainer will think it's an oversight):

    `// bathroomCount intentionally NOT cleared — applies to all 6 types (CONTEXT.md Claude's Discretion L111)`

    Rationale (locked in CONTEXT.md L111):
    - `bedrooms` is residential-only (apply-set = `{apartment, house}`). Switching `apartment → hotel` makes it semantically invalid. Uniform clear on every propertyType switch is the simplest rule that matches the existing pattern (the existing block clears `rooms`, `bathroom`, `kitchen`, `hotelRooms`, `hotelClass` uniformly).
    - `bathroomCount` applies to all 6 types except `land`. Switching `apartment → hotel` does NOT invalidate the entered value — the user reasonably wants to keep their bathroom count. So omit from the clear list.

    Edge case: `apartment → land` switch. `land` is the one propertyType where `bathroomCount` doesn't apply. But the conditional render in Step3BasicInfo (Plan 07-04) gates the bathroomCount row on the 6-type OR-chain, so the value just becomes hidden — it's not displayed and not validated. If the user then switches back from land to apartment, the value reappears (preserved). This is the "minor UX regression" trade-off documented in CONTEXT.md L111 as "acceptable trade" — and it actually behaves correctly (preserved-value is the better UX). No further handling needed.

    **Do NOT modify** the dealType-clear block at L83-89 (`terms` clear). It's unrelated to FORM-05 and clearing it would regress M3 Phase 2 D-04.
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        grep -A1 "hotelClass: undefined," src/components/ContextualListingFlow/index.tsx | grep -q "bedrooms: undefined" &amp;&amp; \
        grep -q "bathroomCount intentionally NOT cleared" src/components/ContextualListingFlow/index.tsx &amp;&amp; \
        grep -c "bathroomCount: undefined" src/components/ContextualListingFlow/index.tsx | grep -q "^0$" &amp;&amp; \
        npx tsc --noEmit -p . 2>&amp;1 | grep -v "Property\\.tours" | grep -E "ContextualListingFlow/index\\.tsx" | wc -l | grep -q "^0$"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the propertyType-clear block now contains `bedrooms: undefined` immediately after `hotelClass: undefined,`.
    - Source assertion: an inline comment documents the intentional omission of `bathroomCount` (verifiable via `grep -q "bathroomCount intentionally NOT cleared"`).
    - Source assertion: `bathroomCount: undefined` count in index.tsx is 0 (must NOT be added to clear list — the test would catch an accidental over-clear).
    - tsc passes: zero net new errors attributable to `index.tsx` (FormBag.basics from Plan 07-03 already declares `bedrooms?: number`).
  </acceptance_criteria>
  <done>bedrooms added to propertyType-clear list; bathroomCount intentionally omitted with inline comment; dealType-clear block untouched; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Submit-catch M4_* discriminator (D-10)</name>
  <files>src/components/ContextualListingFlow/index.tsx</files>
  <read_first>
    - src/components/ContextualListingFlow/index.tsx lines 130-179 (full submit handler with the catch block at L171-177)
    - src/components/ContextualListingFlow/index.tsx lines 50-60 (confirms `setErrors` declared at L56 and `setCurrentStep` declared at L57 — both in scope inside the catch's enclosing `useCallback`)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"src/components/ContextualListingFlow/index.tsx (MOD) — submit catch" (verbatim before/after)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §D-10 (inline-error pattern; M4_* discriminator routes to Step 3 + writes localized key)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §"Specific Ideas" L268-282 (verbatim catch-handler skeleton)
    - .planning/phases/06-schema-extension-backend-mongoose-rn-type-stub-body-strip-va/06-CONTEXT.md §D-11 (verifies backend 400 codes are `M4_BEDROOMS_INVALID` + `M4_BATHROOM_STEP_INVALID` — the literal strings to match in the discriminator)
  </read_first>
  <action>
    Open `src/components/ContextualListingFlow/index.tsx`. Locate the submit catch block (L171-177):

    Replace the existing 3-line catch body (`const msg = ...; Alert.alert(...)`) with an extended block that:

    1. Extracts the error code from the rejected Promise — supports both axios-shaped `e.response.data.code` AND direct `e.code` shapes:
       - Use TypeScript type assertion to narrow `e: unknown` to read `.response?.data?.code` and `.code`. Mirror the existing `(e as { message?: string }).message` pattern but with deeper field access. Suggested form: `const code = (e as { response?: { data?: { code?: string } }; code?: string }).response?.data?.code ?? (e as { code?: string }).code;`.

    2. Branches on the code:
       - If `code === 'M4_BEDROOMS_INVALID'`: call `setErrors((prev) => ({ ...prev, ['basics.bedrooms']: 'contextualListing.step3.bedroomsInvalid' }));` then `setCurrentStep(3);`.
       - Else if `code === 'M4_BATHROOM_STEP_INVALID'`: call `setErrors((prev) => ({ ...prev, ['basics.bathroomCount']: 'contextualListing.step3.bathroomCountInvalid' }));` then `setCurrentStep(3);`.
       - Else (fall-through to existing behavior): `const msg = (e as { message?: string }).message ?? t('common.errorGeneric'); Alert.alert(t('common.error'), msg);` — preserve the current generic error path verbatim for non-M4 failures.

    3. Wrap the new branches in a single `if (code === 'M4_BEDROOMS_INVALID') { ... } else if (code === 'M4_BATHROOM_STEP_INVALID') { ... } else { /* existing generic Alert */ }`.

    4. Preserve the `finally { setIsSubmitting(false); }` block unchanged.

    5. Add a brief leading comment in the new catch body: `// M4 D-10 — discriminate Phase 6 backend 400 codes; route to inline error row Step3 renders. Practically unreachable via stepper UI clamping, but Phase 6 D-11 wired these codes explicitly so Phase 7 can discriminate.`

    The `setCurrentStep(3)` call is safe — `currentStep` is typed `StepIndex` (1..6 per L57's `useState<StepIndex>(1)`) and `3` is a valid literal. If TypeScript narrows the literal `3` as `number` (not `StepIndex`), use a cast: `setCurrentStep(3 as StepIndex)`. The `StepIndex` type is already imported into index.tsx (used at L57 — verify via grep before adding the cast; add it only if tsc complains).

    The `setErrors` setter signature is `React.Dispatch<React.SetStateAction<FormErrorBag>>`. `FormErrorBag` keys are `string` (or a narrower type — check types.ts) — the dotted-path keys `'basics.bedrooms'` / `'basics.bathroomCount'` already work because every existing Step-3 inline-error path uses the same shape (verified in Step3BasicInfo.tsx at L117 `errors['basics.areaSqm']`). No new type work.

    No new imports needed — `setErrors`, `setCurrentStep`, `Alert`, and `t` are all in scope.
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        grep -q "M4_BEDROOMS_INVALID" src/components/ContextualListingFlow/index.tsx &amp;&amp; \
        grep -q "M4_BATHROOM_STEP_INVALID" src/components/ContextualListingFlow/index.tsx &amp;&amp; \
        grep -q "setErrors.*basics\\.bedrooms.*contextualListing\\.step3\\.bedroomsInvalid" src/components/ContextualListingFlow/index.tsx &amp;&amp; \
        grep -q "setErrors.*basics\\.bathroomCount.*contextualListing\\.step3\\.bathroomCountInvalid" src/components/ContextualListingFlow/index.tsx &amp;&amp; \
        grep -q "setCurrentStep(3" src/components/ContextualListingFlow/index.tsx &amp;&amp; \
        grep -q "Alert.alert(t('common.error')" src/components/ContextualListingFlow/index.tsx &amp;&amp; \
        npx tsc --noEmit -p . 2>&amp;1 | grep -v "Property\\.tours" | grep -E "ContextualListingFlow/index\\.tsx" | wc -l | grep -q "^0$"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: both error code string literals present (`M4_BEDROOMS_INVALID` + `M4_BATHROOM_STEP_INVALID`).
    - Source assertion: both setErrors calls present, each setting the matching `errors['basics.X']` key to the matching `contextualListing.step3.XInvalid` value.
    - Source assertion: `setCurrentStep(3)` (or `setCurrentStep(3 as StepIndex)`) appears in the catch branches at least twice (once per M4 branch).
    - Source assertion: existing generic `Alert.alert(t('common.error'), ...)` fallback preserved (verifiable via grep — the existing `Alert.alert` call must still exist in the file).
    - Behavior assertion (manual verification on real device or via Plan 07-04 integration tests if expanded): submitting a payload that triggers M4_BEDROOMS_INVALID from the backend results in `errors['basics.bedrooms']` populated AND step navigation to 3 (so the inline error row Plan 07-04 renders becomes visible).
    - tsc passes: zero net new errors attributable to `index.tsx`.
    - Defensive note: this code path is practically unreachable from the stepper UI (UI clamping prevents bad values). It catches direct-write code paths (e.g., a future copy-from-another-listing feature that bypasses the stepper). The discriminator's existence is the contract that lets Phase 6's dedicated 400 codes have a real consumer.
  </acceptance_criteria>
  <done>Submit-catch discriminates M4_BEDROOMS_INVALID and M4_BATHROOM_STEP_INVALID, writes localized error keys into the FormErrorBag, navigates to Step 3; generic Alert fallback preserved for non-M4 errors; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend response → FormErrorBag | Server-returned error codes (`M4_*`) flow into client error state and surface as localized inline rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering (response substitution) | submit catch | accept | An attacker controlling the response could spoof an `M4_BEDROOMS_INVALID` code to redirect users to Step 3. Low impact — no privilege escalation, no data loss; only navigation + a visible error string. The same surface exists for every existing error path. |
| T-07-09 | Information Disclosure | catch error logging | mitigate | The discriminator does NOT log the full error envelope (which could contain Mongoose validation messages exposing schema internals). It reads only `.code` and `.message` (fall-through path). Matches the existing handler's minimal-disclosure posture (CONVENTIONS L102-103 — `console.error('Error ...:', error)` in services only, not in UI catch). |
</threat_model>

<verification>
- index.tsx: `bedrooms: undefined` added to propertyType-clear list (after hotelClass, before closing brace).
- index.tsx: `bathroomCount: undefined` NOT added to clear list (verified by `grep -c` returning 0).
- index.tsx: submit catch discriminates both M4_* codes; sets errors map; navigates to Step 3; generic Alert fallback preserved.
- tsc passes with zero net new errors attributable to index.tsx.
- KBD-02 grep gate intact.
- No new state introduced (reuses setErrors at L56 + setCurrentStep at L57).
</verification>

<success_criteria>
- FORM-05 (orchestrator half): propertyType change clears stale bedrooms; bathroomCount preserved across switches; submit-catch routes Phase 6 backend 400 codes to inline-error rows via the FormErrorBag.
- Backend ↔ frontend contract from Phase 6 D-11 is now bidirectional: backend emits the codes; frontend consumes them with localized surfacing.
</success_criteria>

<output>
Create `.planning/phases/07-stepper-component-contextuallistingflow-integration/07-05-SUMMARY.md` after completion.
</output>
