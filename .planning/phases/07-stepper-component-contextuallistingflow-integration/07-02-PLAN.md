---
phase: 07-stepper-component-contextuallistingflow-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locales/en.ts
  - src/locales/ru.ts
autonomous: true
requirements: [FORM-02, FORM-04]
tags: [i18n, locales, en, ru, parity-gate]

must_haves:
  truths:
    - "Four new label/error keys land in both en.ts and ru.ts: bedroomsLabel, bathroomCountLabel, bedroomsInvalid, bathroomCountInvalid"
    - "EN+RU parity gate (scripts/check-i18n-parity.sh) exits 0 after the edits"
    - "Keys live in the existing contextualListing.step3.* flat namespace — no new namespace"
    - "Copy matches D-11 Claude's Discretion §i18n keys + label copy verbatim"
  artifacts:
    - path: "src/locales/en.ts"
      provides: "4 new EN strings under contextualListing.step3.*"
      contains: "contextualListing.step3.bedroomsLabel"
    - path: "src/locales/ru.ts"
      provides: "4 new RU strings under contextualListing.step3.*"
      contains: "contextualListing.step3.bedroomsLabel"
  key_links:
    - from: "src/locales/en.ts"
      to: "src/locales/ru.ts"
      via: "scripts/check-i18n-parity.sh enforces key-set equality"
      pattern: "contextualListing\\.step3\\.(bedrooms|bathroomCount)(Label|Invalid)"
---

<objective>
Add the 4 new i18n keys (per locale = 8 total entries) that Plans 07-04 and 07-05 will consume: two `*Label` strings (rendered by the Step3 stepper rows) and two `*Invalid` strings (surfaced by the validators.ts defensive check from Plan 07-03 AND by the index.tsx submit-catch M4_* discriminator from Plan 07-05). Edit BOTH `en.ts` and `ru.ts` in lockstep so the parity gate stays green.

Purpose: Unblock downstream plans without forcing them to also touch locales. EN+RU parity is a CI gate (`scripts/check-i18n-parity.sh`) — a partial commit would fail it.

Output: 4 new keys per locale, surrounded by no other edits.

CRITICAL doc-bug correction: CONTEXT.md cites `en.json` / `ru.json` but the on-disk files are `en.ts` / `ru.ts` (flat TypeScript `Record<string, string>` maps marked `as const`). Plan against `.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md
@.planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Locked copy from CONTEXT.md §Claude's Discretion + Specific Ideas L240-243 -->

| Key | EN | RU |
|-----|----|----|
| `contextualListing.step3.bedroomsLabel` | `Bedrooms` | `Спальни` |
| `contextualListing.step3.bathroomCountLabel` | `Bathrooms` | `Ванные комнаты` |
| `contextualListing.step3.bedroomsInvalid` | `Bedrooms must be a whole number between 0 and 10.` | `Количество спален должно быть целым числом от 0 до 10.` |
| `contextualListing.step3.bathroomCountInvalid` | `Bathrooms must be a whole or half number between 0 and 10.` | `Количество ванных должно быть целым или с шагом 0.5 от 0 до 10.` |

<!-- Existing namespace anchor (PATTERNS.md):
en.ts L638-671 / ru.ts L640-673 hold the existing contextualListing.step3.*Label keys -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 new EN strings to src/locales/en.ts</name>
  <files>src/locales/en.ts</files>
  <read_first>
    - src/locales/en.ts (full file or `grep -n "contextualListing.step3" src/locales/en.ts` to identify the existing block at L638-671 — the new keys go inside this region for keep-related-keys-near locality)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"src/locales/en.ts + src/locales/ru.ts" (verbatim excerpt + additions)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §"i18n keys + label copy" (D-11 Claude's Discretion — EN copy locked)
    - scripts/check-i18n-parity.sh (read to understand parity-gate mechanism — key-set equality between locales)
  </read_first>
  <action>
    Open `src/locales/en.ts`. Locate the existing `contextualListing.step3.*Label` block (around line 638-671 — find via `grep -n "contextualListing.step3.hotelClassLabel" src/locales/en.ts` to anchor near the block tail).

    Add EXACTLY 4 new entries, somewhere AFTER the `hotelClassLabel` line and before any unrelated namespace (e.g. before `contextualListing.step4.*` if that's the next block; otherwise the simplest insertion point is immediately after the last `contextualListing.step3.*` entry). Group the two `bedrooms*` keys together and the two `bathroomCount*` keys together for readability. Use single quotes (Prettier `singleQuote: true`) and trailing comma on each entry (Prettier `trailingComma: 'all'`):

    - `'contextualListing.step3.bedroomsLabel': 'Bedrooms',`
    - `'contextualListing.step3.bedroomsInvalid': 'Bedrooms must be a whole number between 0 and 10.',`
    - `'contextualListing.step3.bathroomCountLabel': 'Bathrooms',`
    - `'contextualListing.step3.bathroomCountInvalid': 'Bathrooms must be a whole or half number between 0 and 10.',`

    Do not edit any other key. Do not reorder existing keys. Do not change quotes or formatting on neighboring lines.

    The file is typed `as const` so TypeScript will narrow each value to its literal string type — no `Record<string, string>` cast needed; just append within the existing object literal.
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        grep -c "contextualListing\\.step3\\.bedroomsLabel" src/locales/en.ts | grep -q "^1$" &amp;&amp; \
        grep -c "contextualListing\\.step3\\.bedroomsInvalid" src/locales/en.ts | grep -q "^1$" &amp;&amp; \
        grep -c "contextualListing\\.step3\\.bathroomCountLabel" src/locales/en.ts | grep -q "^1$" &amp;&amp; \
        grep -c "contextualListing\\.step3\\.bathroomCountInvalid" src/locales/en.ts | grep -q "^1$" &amp;&amp; \
        grep -q "'Bedrooms',\\|\"Bedrooms\"," src/locales/en.ts &amp;&amp; \
        grep -q "Bathrooms must be a whole or half number between 0 and 10" src/locales/en.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: exactly one occurrence of each of the 4 new keys in `src/locales/en.ts`.
    - Source assertion: EN copy matches verbatim — `'Bedrooms'`, `'Bathrooms'`, `'Bedrooms must be a whole number between 0 and 10.'`, `'Bathrooms must be a whole or half number between 0 and 10.'`.
    - Source assertion: keys live in the existing `contextualListing.step3.*` block — verifiable by `grep -B5 "bedroomsLabel" src/locales/en.ts` showing nearby `contextualListing.step3.*` entries.
    - tsc passes: no net new errors from `en.ts` (TypeScript `as const` typing inferred for new entries).
  </acceptance_criteria>
  <done>4 new EN keys added in the contextualListing.step3.* block, verbatim copy, no other edits.</done>
</task>

<task type="auto">
  <name>Task 2: Add 4 new RU strings to src/locales/ru.ts and verify parity</name>
  <files>src/locales/ru.ts</files>
  <read_first>
    - src/locales/ru.ts (full file or `grep -n "contextualListing.step3" src/locales/ru.ts` — block at L640-673)
    - src/locales/en.ts (verify the 4 keys are present after Task 1 — parity gate compares both files)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-PATTERNS.md §"src/locales/en.ts + src/locales/ru.ts" (verbatim RU additions)
    - .planning/phases/07-stepper-component-contextuallistingflow-integration/07-CONTEXT.md §"i18n keys + label copy" (RU copy locked — "Спальни", "Ванные комнаты")
    - scripts/check-i18n-parity.sh
  </read_first>
  <action>
    Open `src/locales/ru.ts`. Locate the parallel `contextualListing.step3.*Label` block (around line 640-673 — anchor via `grep -n "contextualListing.step3.hotelClassLabel" src/locales/ru.ts`).

    Add EXACTLY 4 new entries with the same insertion strategy as Task 1 — group bedrooms together, bathroomCount together. Use single quotes + trailing commas:

    - `'contextualListing.step3.bedroomsLabel': 'Спальни',`
    - `'contextualListing.step3.bedroomsInvalid': 'Количество спален должно быть целым числом от 0 до 10.',`
    - `'contextualListing.step3.bathroomCountLabel': 'Ванные комнаты',`
    - `'contextualListing.step3.bathroomCountInvalid': 'Количество ванных должно быть целым или с шагом 0.5 от 0 до 10.',`

    Cyrillic characters are UTF-8; the file is already UTF-8 (verified by existing RU entries). Single straight quotes around RU strings — do not use guillemets (`«»`).

    After saving, run the parity gate locally to confirm green: `bash scripts/check-i18n-parity.sh` should exit 0. If it exits non-zero, the script will print which keys are missing on one side; fix the missing side before commit.
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; \
        grep -c "contextualListing\\.step3\\.bedroomsLabel" src/locales/ru.ts | grep -q "^1$" &amp;&amp; \
        grep -c "contextualListing\\.step3\\.bedroomsInvalid" src/locales/ru.ts | grep -q "^1$" &amp;&amp; \
        grep -c "contextualListing\\.step3\\.bathroomCountLabel" src/locales/ru.ts | grep -q "^1$" &amp;&amp; \
        grep -c "contextualListing\\.step3\\.bathroomCountInvalid" src/locales/ru.ts | grep -q "^1$" &amp;&amp; \
        grep -q "Спальни" src/locales/ru.ts &amp;&amp; \
        grep -q "Ванные комнаты" src/locales/ru.ts &amp;&amp; \
        bash scripts/check-i18n-parity.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: exactly one occurrence of each of the 4 new keys in `src/locales/ru.ts`.
    - Source assertion: RU copy matches verbatim — `'Спальни'`, `'Ванные комнаты'`, `'Количество спален должно быть целым числом от 0 до 10.'`, `'Количество ванных должно быть целым или с шагом 0.5 от 0 до 10.'`.
    - Parity gate: `bash scripts/check-i18n-parity.sh` exits 0 (key-set equality between en.ts and ru.ts).
    - tsc passes: no net new errors from `ru.ts`.
  </acceptance_criteria>
  <done>4 new RU keys added with verbatim Cyrillic copy; `scripts/check-i18n-parity.sh` exits 0 confirming EN+RU lockstep parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Static string additions to locale files; no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | locale strings | accept | All 4 strings are user-facing labels with no PII, no internal identifiers, no system paths. RU translation is colloquially-correct formal Russian per CONTEXT.md D-11. |
</threat_model>

<verification>
- en.ts contains 4 new keys with EN copy verbatim.
- ru.ts contains 4 new keys with RU copy verbatim (Cyrillic).
- `scripts/check-i18n-parity.sh` exits 0.
- No other keys reordered or modified.
- tsc passes with zero net new errors.
</verification>

<success_criteria>
- FORM-02 unblocker: Step3BasicInfo (Plan 07-04) can call `t('contextualListing.step3.bedroomsLabel')` / `t('contextualListing.step3.bathroomCountLabel')` and resolve to localized strings.
- FORM-04 unblocker: validators.ts (Plan 07-03) and index.tsx submit-catch (Plan 07-05) can route to `contextualListing.step3.bedroomsInvalid` / `bathroomCountInvalid` for inline error rows.
- EN+RU parity gate green.
</success_criteria>

<output>
Create `.planning/phases/07-stepper-component-contextuallistingflow-integration/07-02-SUMMARY.md` after completion.
</output>
