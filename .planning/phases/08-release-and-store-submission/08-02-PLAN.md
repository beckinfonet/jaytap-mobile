---
phase: 08-release-and-store-submission
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - package.json
  - ios/JayTap.xcodeproj/project.pbxproj
autonomous: true
requirements: [REL-01, REL-02]
requirements_addressed: [REL-01, REL-02]
tags: [release, version-bump, atomic-commit, ios-marketing-version, package-version]

must_haves:
  truths:
    - "package.json `\"version\"` field is exactly \"1.0.4\" (REL-01)"
    - "ios/JayTap.xcodeproj/project.pbxproj contains MARKETING_VERSION = 1.0.4 in BOTH Debug and Release build configurations (2 occurrences total) per D-01 (REL-02 iOS marketing)"
    - "ios/JayTap.xcodeproj/project.pbxproj `CURRENT_PROJECT_VERSION = 21` is preserved (already at v1.0.4 target per D-02; Plan 08-02 does NOT touch this — bump to 22 is conditional in Plan 08-05 only on TestFlight collision per D-03)"
    - "android/app/build.gradle is NOT modified (`versionCode 25` + `versionName \"1.0.24\"` already at v1.0.4 target per D-02)"
    - "Both files commit atomically as one unit with shared intent (D-01 + Phase 1–6 atomic-commit convention)"
    - "Phase-3/4 CI gates (check-role-grep / check-land-removed / check-i18n-parity) all still exit 0 — version bumps cannot regress source invariants"
  artifacts:
    - path: "package.json"
      provides: "REL-01 — user-facing version string at 1.0.4"
      contains: '"version": "1.0.4"'
    - path: "ios/JayTap.xcodeproj/project.pbxproj"
      provides: "REL-02 iOS — MARKETING_VERSION at 1.0.4 in both Debug + Release configs"
      contains: "MARKETING_VERSION = 1.0.4"
  key_links:
    - from: "package.json :: version field"
      to: "ios/JayTap.xcodeproj/project.pbxproj :: MARKETING_VERSION"
      via: "shared-intent atomic commit (per D-01 + commit_docs: true)"
      pattern: "1\\.0\\.4"
    - from: "Plan 08-01 Wave-0 verify-only assertions"
      to: "Plan 08-02 bump"
      via: "Wave 0 must have PASSED before Plan 08-02 runs (depends_on: [08-01]); Wave-0 SHA is the baseline this plan branches from"
      pattern: "wave: 0 -> wave: 1"
---

<objective>
Wave 1 of Phase 8 — atomic version bump for the v1.0.4 release.

Two file edits in a single commit per D-01:
1. `package.json` `"version"`: `"1.0.3"` → `"1.0.4"` (REL-01).
2. `ios/JayTap.xcodeproj/project.pbxproj` `MARKETING_VERSION`: `1.0.3` → `1.0.4` (2 occurrences — Debug + Release configs) (REL-02 iOS marketing portion).

The other three values (pbxproj `CURRENT_PROJECT_VERSION = 21`, gradle `versionCode 25`, gradle `versionName "1.0.24"`) are ALREADY at their v1.0.4 targets per D-02 — they were advanced in earlier work (PROJECT.md row 129 + STATE.md). Plan 08-02 does NOT touch them. Plan 08-05 will conditionally bump pbxproj `CURRENT_PROJECT_VERSION` to 22 ONLY if TestFlight upload collides with an existing build under the same MARKETING_VERSION (D-03).

Purpose: This is the surgical edit that makes the binary archive identifiable as v1.0.4. App Store Connect's ingestion checks gate on `MARKETING_VERSION` + `CURRENT_PROJECT_VERSION` being a previously-unseen pair under the bundle ID; a missed pbxproj line means the archive uploads as v1.0.3 (rejected as duplicate) or fails Apple's "increasing version number" check.

Output: 2 file edits, 1 atomic commit, six post-bump grep assertions confirming the new state matches D-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-release-and-store-submission/08-CONTEXT.md
@.planning/phases/08-release-and-store-submission/08-01-SUMMARY.md
@package.json
@ios/JayTap.xcodeproj/project.pbxproj

<interfaces>
<!-- Exact pre-edit line content. Plan 08-01 verified these values still hold; Plan 08-02 -->
<!-- replaces the literal `1.0.3` substring with `1.0.4` in three lines (1 in package.json, -->
<!-- 2 in pbxproj). NO regex broadening — `MARKETING_VERSION = 1.0.3;` is the unique key. -->

`package.json` line 3 (pre-edit, single occurrence — verified by Plan 08-01 Task 1 step 1):
```
  "version": "1.0.3",
```
Post-edit target:
```
  "version": "1.0.4",
```

`ios/JayTap.xcodeproj/project.pbxproj` (pre-edit, EXACTLY 2 occurrences — verified by Plan 08-01 Task 1 step 3 returning `2`):
- Line 283 (Debug build configuration): `				MARKETING_VERSION = 1.0.3;`
- Line 316 (Release build configuration): `				MARKETING_VERSION = 1.0.3;`

Post-edit target on both lines:
- `				MARKETING_VERSION = 1.0.4;`

DO NOT TOUCH (each appears 2× in pbxproj, both already at target per D-02):
- Line 274 + 308: `				CURRENT_PROJECT_VERSION = 21;` — preserved verbatim (Plan 08-05 conditional bump only on TestFlight collision per D-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump package.json + pbxproj MARKETING_VERSION (atomic commit per D-01)</name>
  <files>package.json, ios/JayTap.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/08-release-and-store-submission/08-CONTEXT.md (re-anchor D-01 + D-02 + D-05 — confirm scope is 2 file edits, NOT 4)
    - .planning/phases/08-release-and-store-submission/08-01-SUMMARY.md (confirm Plan 08-01 Wave-0 PASSED — if any assertion failed, HALT and surface to user)
    - package.json (read full file before edit — confirm only one `"version":` key exists at line 3 and the value is still "1.0.3")
    - ios/JayTap.xcodeproj/project.pbxproj line range 270-320 (read this slice — confirm lines 274/308 are CURRENT_PROJECT_VERSION = 21 and lines 283/316 are MARKETING_VERSION = 1.0.3)
  </read_first>
  <action>
    Two surgical edits via the Edit tool. Bias atomic-commit per D-01 + Phase 1–6 atomic-commit convention.

    **Edit 1 — package.json line 3:**
    Use Edit tool with `old_string` = `  "version": "1.0.3",` and `new_string` = `  "version": "1.0.4",`. Two-space leading indent is the file's existing convention; preserve it. Single occurrence (Plan 08-01 confirmed `grep -c '"version":' package.json` = 1).

    **Edit 2 — ios/JayTap.xcodeproj/project.pbxproj (BOTH occurrences):**
    The string `				MARKETING_VERSION = 1.0.3;` appears EXACTLY 2× (Debug config line 283, Release config line 316; Plan 08-01 confirmed via `grep -c 'MARKETING_VERSION = 1.0.3' ... = 2`). The Edit tool needs unique anchors per call OR use `replace_all: true` since both occurrences must change to the SAME post-value.

    Recommended approach: ONE Edit call with `replace_all: true`. `old_string` = `MARKETING_VERSION = 1.0.3;` and `new_string` = `MARKETING_VERSION = 1.0.4;`. Both Debug + Release configs flip in one operation, preserving leading tabs (pbxproj uses tab indent).

    **DO NOT touch any of the following:**
    - `CURRENT_PROJECT_VERSION = 21` (lines 274 + 308) — already at target per D-02; Plan 08-05 handles the conditional bump-to-22 ONLY on TestFlight collision per D-03.
    - `android/app/build.gradle` versionCode / versionName — already at targets per D-02 (Plan 08-01 verified).
    - Any other pbxproj field (PRODUCT_NAME, BUNDLE_IDENTIFIER, code-signing, deployment-target, etc.).
    - Any `Info.plist` — `MARKETING_VERSION` reads through to Info.plist via `$(MARKETING_VERSION)` substitution; no plist edit is needed.

    **Post-edit verification BEFORE commit (D-05 literal assertions):**
    1. `grep '"version": "1.0.4"' package.json` MUST exit 0 (D-05).
    2. `grep -c 'MARKETING_VERSION = 1.0.4' ios/JayTap.xcodeproj/project.pbxproj` MUST return `2` (D-05).
    3. `grep -c 'MARKETING_VERSION = 1.0.3' ios/JayTap.xcodeproj/project.pbxproj` MUST return `0` (no stale references).
    4. `grep 'CURRENT_PROJECT_VERSION = 21' ios/JayTap.xcodeproj/project.pbxproj` MUST exit 0 (preserved — D-05 verify-only).
    5. `grep 'versionCode 25' android/app/build.gradle` MUST exit 0 (preserved — D-05 verify-only).
    6. `grep 'versionName "1.0.24"' android/app/build.gradle` MUST exit 0 (preserved — D-05 verify-only).

    If any assertion fails, HALT — do not commit; surface to user with the failed assertion + diff.

    **Phase-invariant regression floor (run BEFORE commit):**
    7. `./scripts/check-role-grep.sh` MUST exit 0 (Phase 3 D-14 invariant — version bumps cannot regress this; if it fails the failure is a pre-existing drift unrelated to this plan).
    8. `./scripts/check-land-removed.sh` MUST exit 0 (Phase 4 invariant).
    9. `./scripts/check-i18n-parity.sh` MUST exit 0 (Phase 4 invariant).

    **Atomic commit per D-01 + `commit_docs: true`:**
    Stage exactly two files: `git add package.json ios/JayTap.xcodeproj/project.pbxproj`. Do NOT include `08-QA-MATRIX.md` (already committed in Plan 08-01) or any other path.

    Commit message (HEREDOC):
    ```
    chore(08-02): bump version to 1.0.4 (REL-01 + REL-02 iOS marketing)

    - package.json "version": 1.0.3 -> 1.0.4 (REL-01)
    - ios/JayTap.xcodeproj/project.pbxproj MARKETING_VERSION: 1.0.3 -> 1.0.4
      (Debug + Release configs, 2 occurrences) (REL-02 iOS marketing)

    Three values are already at v1.0.4 targets and intentionally not touched:
    - pbxproj CURRENT_PROJECT_VERSION = 21 (D-02 — preserved; Plan 08-05
      handles conditional bump to 22 only on TestFlight collision per D-03)
    - android/app/build.gradle versionCode 25 (D-02 — preserved)
    - android/app/build.gradle versionName "1.0.24" (D-02 — preserved)

    Verifications (D-05):
    - grep '"version": "1.0.4"' package.json exits 0
    - grep -c 'MARKETING_VERSION = 1.0.4' ios/...pbxproj returns 2
    - grep 'CURRENT_PROJECT_VERSION = 21' ios/...pbxproj exits 0
    - grep 'versionCode 25' android/app/build.gradle exits 0
    - grep 'versionName "1.0.24"' android/app/build.gradle exits 0
    - xcodebuild -version | grep '^Xcode 26' exits 0 (REL-04 SDK gate stays cleared)
    ```

    Capture post-commit `git rev-parse HEAD` for SUMMARY (this SHA is what Plan 08-05 archives).
  </action>
  <verify>
    <automated>grep '"version": "1.0.4"' package.json && [ "$(grep -c 'MARKETING_VERSION = 1.0.4' ios/JayTap.xcodeproj/project.pbxproj)" = "2" ] && [ "$(grep -c 'MARKETING_VERSION = 1.0.3' ios/JayTap.xcodeproj/project.pbxproj)" = "0" ] && grep 'CURRENT_PROJECT_VERSION = 21' ios/JayTap.xcodeproj/project.pbxproj && grep 'versionCode 25' android/app/build.gradle && grep 'versionName "1.0.24"' android/app/build.gradle && ./scripts/check-role-grep.sh && ./scripts/check-land-removed.sh && ./scripts/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"version": "1.0.4"' package.json` exits 0 (D-05 — REL-01)
    - `grep -c 'MARKETING_VERSION = 1.0.4' ios/JayTap.xcodeproj/project.pbxproj` outputs `2` (D-05 — REL-02 iOS marketing both configs)
    - `grep -c 'MARKETING_VERSION = 1.0.3' ios/JayTap.xcodeproj/project.pbxproj` outputs `0` (no stale references — Plan 08-02 fully transitioned the file)
    - `grep 'CURRENT_PROJECT_VERSION = 21' ios/JayTap.xcodeproj/project.pbxproj` exits 0 (D-05 — verify-only, preserved per D-02)
    - `grep 'versionCode 25' android/app/build.gradle` exits 0 (D-05 — verify-only, preserved per D-02)
    - `grep 'versionName "1.0.24"' android/app/build.gradle` exits 0 (D-05 — verify-only, preserved per D-02)
    - `./scripts/check-role-grep.sh` exits 0 (Phase 3 D-14 invariant)
    - `./scripts/check-land-removed.sh` exits 0 (Phase 4 invariant)
    - `./scripts/check-i18n-parity.sh` exits 0 (Phase 4 invariant)
    - `git diff --name-only HEAD~1` outputs exactly 2 paths: `package.json` + `ios/JayTap.xcodeproj/project.pbxproj` (no scope creep)
    - `git log -1 --format=%s` includes the literal token `08-02` AND `1.0.4`
    - Post-bump SHA recorded in SUMMARY (this SHA is what Plan 08-05 archives)
  </acceptance_criteria>
  <done>v1.0.4 user-facing version string and iOS MARKETING_VERSION are committed atomically; pbxproj CURRENT_PROJECT_VERSION + gradle versionCode/versionName are preserved unchanged; all six D-05 grep assertions PASS; phase-invariant CI gates exit 0; commit SHA captured for Plan 08-05 to archive against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced this plan) | Plan 08-02 introduces no new attack surface — version-string edits in declarative project metadata. v1.0.3 declarations inherited per D-13. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | pbxproj scope creep (Edit tool replacing more than the 2 intended `MARKETING_VERSION` lines, e.g., bumping the wrong target's marketing version, touching CURRENT_PROJECT_VERSION) | mitigate | Two assertions: `grep -c 'MARKETING_VERSION = 1.0.4' ... = 2` (intended targets flipped) AND `grep 'CURRENT_PROJECT_VERSION = 21' ...` exits 0 (no collateral). `git diff --name-only HEAD~1` confirms only 2 paths changed. |
| T-08-04 | Repudiation | Out-of-band drift on android/app/build.gradle (a prior session reset the gradle baseline; bump now silently misses Android) | mitigate | Three gradle assertions in Task 1 step 5–6 + Plan 08-01 Wave-0 confirms baseline at gradle = 25 / "1.0.24" before any work. If gradle drifts mid-plan, the post-edit verify catches it. |
| T-08-05 | (no further threats) | n/a | accept | No new attack surface; v1.0.3 ASC App Privacy + Play Data Safety inherited per D-13. |
</threat_model>

<verification>
Phase-level (cross-cutting checks for this plan only):

1. Atomic-commit invariant: `git diff --name-only HEAD~1` returns exactly `package.json` + `ios/JayTap.xcodeproj/project.pbxproj` — no other paths.
2. Post-bump grep coverage: all six D-05 literals match the expected post-state.
3. Phase invariants: 3 CI gates exit 0 (no source touched ⇒ no regression).
4. No `pod install` was needed (Plan 08-02 did not change Podfile or any native dep — pbxproj `MARKETING_VERSION` does not invalidate the CocoaPods cache).
5. Working-tree clean after commit: `git status --short` returns empty (nothing left uncommitted).
</verification>

<success_criteria>
1. v1.0.4 substring lands in `package.json` (1 occurrence) and `ios/JayTap.xcodeproj/project.pbxproj` (2 occurrences for `MARKETING_VERSION`).
2. Atomic commit per D-01 with `08-02` and `1.0.4` in the message.
3. Six D-05 assertions PASS post-commit (3 update-targets + 3 preserve-targets).
4. Phase-3/4 CI gates exit 0.
5. Working tree clean post-commit.
6. Post-bump SHA captured in SUMMARY for Plan 08-05 to archive against.
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-and-store-submission/08-02-SUMMARY.md` documenting:
- Pre-edit state of all 3 changed lines (literal text, line numbers)
- Post-edit state of all 3 changed lines (literal text, line numbers)
- Six D-05 grep results post-commit (command + output per row)
- Three CI gate results (script + exit code per row)
- Pre-bump SHA (from Plan 08-01) → post-bump SHA (this plan's commit)
- Confirmation that no other path drifted (`git diff --name-only HEAD~1` literal output)
</output>
