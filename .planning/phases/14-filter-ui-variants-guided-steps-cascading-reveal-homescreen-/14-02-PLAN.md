---
phase: 14
plan_id: 14-02
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/filters/CascadingFilter.tsx
  - src/components/filters/__tests__/CascadingFilter.test.tsx
  - src/screens/HomeScreen.tsx
  - src/locales/en.ts
  - src/locales/ru.ts
autonomous: true
requirements: [FILT-02]
tags: [filter-cascading, homescreen-extract, m6]

must_haves:
  truths:
    - "No hex color literals in src/components/filters/**; all colors via useTheme()"
    - "EN+RU parity enforced via scripts/check-i18n-parity.sh after every commit touching src/locales/"
    - "KBD-02 invariant: zero keyboardVerticalOffset in src/"
    - "Test convention: __tests__/*.test.tsx subdirectories (NOT flat sibling)"
    - "No new dependencies; all capabilities in current package set"
    - "Subagent CWD-drift: prepend cd to every Bash; verify git branch before commit"
  artifacts:
    - path: "src/components/filters/CascadingFilter.tsx"
      provides: "FILT-02 inline panel — segmented Rent/Buy + underlined Category tabs + multi-select Type chips + left nesting rail"
    - path: "src/components/filters/__tests__/CascadingFilter.test.tsx"
      provides: "Component test: multi-select chip toggle, category-change clears types, liveCount prop usage, rent/buy toggle"
    - path: "src/screens/HomeScreen.tsx"
      provides: "Mounts <CascadingFilter /> behind filterStyle==='cascading' && isFiltersExpanded gate; deletes ~169 LOC of inline JSX + 10 orphan StyleSheet keys"
  key_links:
    - from: "src/components/filters/CascadingFilter.tsx"
      to: "src/components/filters/primitives/DealToggle.tsx (Plan 14-01)"
      via: "import + usage"
      pattern: "from '\\./primitives/DealToggle'"
    - from: "src/components/filters/CascadingFilter.tsx"
      to: "src/components/filters/primitives/TypeIcon.tsx (Plan 14-01)"
      via: "icon dispatch for active chip glyph"
      pattern: "from '\\./primitives/TypeIcon'"
    - from: "src/screens/HomeScreen.tsx"
      to: "src/context/FilterStyleContext.tsx (Phase 13)"
      via: "useFilterStyle() hook"
      pattern: "useFilterStyle"
    - from: "src/screens/HomeScreen.tsx"
      to: "src/components/filters/CascadingFilter.tsx"
      via: "conditional mount with filterStyle gate"
      pattern: "<CascadingFilter "
---

<objective>
Ship `<CascadingFilter>` extracted from HomeScreen's inline filter JSX block, wired behind `filterStyle === 'cascading' && isFiltersExpanded` in HomeScreen. Atomically delete the inline JSX block (lines 521-642, ~120 LOC) AND its 10 orphan StyleSheet keys (lines 866-914, ~49 LOC) — total ~169 LOC delete.

Purpose: Closes the FILT-02 success criterion (inline cascading panel under search bar with segmented Rent/Buy → underlined Category tabs → multi-select Type chips + left nesting rail). Establishes the "filter variants live in `src/components/filters/`" pattern Plan 14-03 builds on for GuidedFilterSheet.

Output: 1 new component + 1 test + HomeScreen surgical edit. One atomic commit. Mid-execution state is "broken filter" only between the delete and the mount — both must land in the same commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-RESEARCH.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-VALIDATION.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-01-SUMMARY.md
@src/screens/HomeScreen.tsx
@src/utils/propertyCategory.ts
@src/utils/buildFilterQuery.ts
@src/context/FilterStyleContext.tsx
@src/components/filters/primitives/DealToggle.tsx
@src/components/filters/primitives/CheckSquare.tsx
@src/components/filters/primitives/TypeIcon.tsx
@src/components/details/__tests__/KeyStatsCard.test.tsx
@src/components/__tests__/EmailVerifyBanner.test.tsx
@src/theme/colors.ts

<interfaces>
<!-- Plan 14-01 primitives (already shipped). Compose, do not re-derive. -->

From src/components/filters/primitives/DealToggle.tsx:
```
export default function DealToggle(props: { value: 'Rent' | 'Buy'; onChange: (v: 'Rent' | 'Buy') => void }): JSX.Element;
```

From src/components/filters/primitives/TypeIcon.tsx:
```
export default function TypeIcon(props: { type: PropertyType; size?: number; color?: string }): JSX.Element | null;
```

From src/components/filters/primitives/CheckSquare.tsx:
```
export default function CheckSquare(props: { checked: boolean }): JSX.Element;
```

From src/context/FilterStyleContext.tsx (Phase 13):
```
export function useFilterStyle(): { filterStyle: 'guided' | 'cascading' | 'master' | 'sentence'; setFilterStyle: (v: ...) => Promise<void> };
```

From src/utils/propertyCategory.ts:
```
export type PropertyCategory = 'Residential' | 'Commercial' | 'Hospitality';
export type PropertyType = 'Apartment'|'House'|'Townhome'|'Condo'|'Office'|'Retail'|'Warehouse'|'Industrial'|'Hostel'|'Hotel';
export const RESIDENTIAL_TYPES, COMMERCIAL_TYPES, HOSPITALITY_TYPES;
```

From src/screens/HomeScreen.tsx (verbatim state shapes Phase 14-02 hands off):
```
transactionType: 'rent' | 'sale';            // setter: setTransactionType
selectedCategory: PropertyCategory;          // setter: setSelectedCategory
types: string[];                             // setter: setTypes
isFiltersExpanded: boolean;
filteredProperties: Property[];              // existing useMemo result; pass .length as liveCount
```
</interfaces>

<delete_checklist>
<!-- LOAD-BEARING: HomeScreen.tsx delete spec (per RESEARCH.md verified at planner-time). -->

1. **JSX block delete:** `src/screens/HomeScreen.tsx` lines 521-642 — the `{isFiltersExpanded && (<View style={styles.filterSection}>...)}` block (segmented Rent/Buy + category chips + property-type chips). ~120 LOC.

2. **StyleSheet orphan-key delete:** `src/screens/HomeScreen.tsx` lines 866-914 — the 10 keys:
   - `filterSection`, `segmentedControl`, `segmentButton`, `segmentText`, `categoryToggleRow`, `categoryChip`, `filterRow`, `filterList`, `filterChip`, `filterText`
   ~49 LOC.

3. **Total:** ~169 LOC removed.

4. **PRESERVE:**
   - `resultCount` style at line 915+ (line 644-646 `<Text style={styles.resultCount}>` mount stays).
   - `isFiltersExpanded` + `setIsFiltersExpanded` state at line 96 (still drives filter-button + Cascading mount).
   - `toggleFiltersExpanded` handler at ~line 331 (still toggles `isFiltersExpanded` with LayoutAnimation).
   - `togglePropertyType` function at ~line 319 (still useful for the chip toggle — but CascadingFilter calls `setTypes` directly via the prop-setter; HomeScreen's `togglePropertyType` can stay or be deleted — deletion preferred if no other call sites; verify with `grep -n "togglePropertyType" src/screens/HomeScreen.tsx`).
   - Filter button (lines 492-506) — paints accent when `isFiltersExpanded`. Unchanged.
   - Line 14 `Modal` import — stays (used by other existing modals).
</delete_checklist>
</context>

<tasks>

<task id="14-02-01" type="auto" tdd="true">
  <name>Task 1: Build &lt;CascadingFilter&gt; component + co-located test (FILT-02)</name>
  <files>src/components/filters/CascadingFilter.tsx, src/components/filters/__tests__/CascadingFilter.test.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (lines 521-642 — the inline JSX block being extracted, ALL OF IT)
    - src/components/filters/primitives/DealToggle.tsx (Plan 14-01 shipped — usage shape)
    - src/components/filters/primitives/TypeIcon.tsx (Plan 14-01 shipped — for the chip glyph)
    - src/components/details/__tests__/KeyStatsCard.test.tsx (test mock pattern)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md §"CascadingFilter.tsx" (extraction shape, multi-select chip toggle, category-change clears types pattern)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md §Layout §"CascadingFilter anatomy" (full ASCII layout + spec values)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md §"Cascading panel" + §"Specifics" (nesting rail, tab underline, chip anatomy)
  </read_first>
  <behavior>
    - Renders DealToggle (shared primitive) for Rent/Buy switch at top
    - Renders 3 underlined category tabs (Residential / Commercial / Hospitality) with accent underline on active
    - Renders TYPE section header + "pick any" hint
    - Renders multi-select Type chip row (chips per active category from RESIDENTIAL_TYPES / COMMERCIAL_TYPES / HOSPITALITY_TYPES)
    - Tapping an inactive chip adds the type to `types`; tapping an active chip removes it (OR-set toggle semantics)
    - Tapping a different category tab calls `setSelectedCategory(cat)` AND `setTypes([])` — load-bearing per RESEARCH.md Pitfall 4 + PATTERNS Pitfall 4
    - Left nesting rail (2pt-wide absolute `<View>`) joining Category section to Type section visually
    - All styling via `useTheme().colors.*` tokens — no hex literals
    - Pure controlled component — does NOT own state; reads props, calls setters
  </behavior>
  <action>
  Create `src/components/filters/CascadingFilter.tsx`.

  **Props interface:**
  ```
  type Props = {
    transactionType: 'rent' | 'sale';
    setTransactionType: (v: 'rent' | 'sale') => void;
    selectedCategory: PropertyCategory;
    setSelectedCategory: (c: PropertyCategory) => void;
    types: string[];
    setTypes: (next: string[] | ((prev: string[]) => string[])) => void;
    liveCount: number;
  }
  ```

  **Imports:** `React, { useMemo }`; `View, Text, StyleSheet, Pressable, FlatList` from `react-native`; `Check` from `lucide-react-native`; `useTheme` from `'../../theme/ThemeContext'`; `useLanguage` from `'../../context/LanguageContext'`; `RESIDENTIAL_TYPES, COMMERCIAL_TYPES, HOSPITALITY_TYPES, type PropertyCategory, type PropertyType` from `'../../utils/propertyCategory'`; `DealToggle` from `'./primitives/DealToggle'`; `TypeIcon` from `'./primitives/TypeIcon'`.

  **Layout per 14-UI-SPEC §"CascadingFilter anatomy":**

  Outer `<View>`: `marginHorizontal: 18, paddingTop: 6, paddingBottom: 18, gap: 18, flexDirection: 'column'`.

  Section 1 — DealToggle: `<DealToggle value={transactionType === 'rent' ? 'Rent' : 'Buy'} onChange={(v) => setTransactionType(v === 'Rent' ? 'rent' : 'sale')} />`.

  Section 2 — nested wrapper: `<View style={{ position: 'relative', paddingLeft: 18 }}>`. First child is absolute nesting rail (`<View style={{ position:'absolute', left: 4, top: 6, bottom: 6, width: 2, borderRadius: 2, backgroundColor: colors.surface2 }} />`).

  Inside nested wrapper:
  - Section header `<Text>` rendering `t('filters.cascading.categoryHeader')`. Style: `fontSize: 11.5, fontWeight: '700', letterSpacing: 1.2, textTransform: 'uppercase', color: colors.textTertiary, marginBottom: 10`.
  - Category tab strip: horizontal `<View>` with `flexDirection: 'row', gap: 22, borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: colors.border, marginBottom: 18`. Iterate `(['Residential', 'Commercial', 'Hospitality'] as PropertyCategory[]).map((cat) => ...)`. Each tab is a `<Pressable>` with `accessibilityRole='button', accessibilityState={{ selected: selectedCategory === cat }}, hitSlop: { top: 4, bottom: 4, left: 4, right: 4 }`. On press: `setSelectedCategory(cat); setTypes([]);`. Tab contains row of (Lucide icon 17pt) + `<Text>` (label = `t('filters.category.residential|.commercial|.hospitality')` if Plan 14-03 ships those, else fall back to bare `cat` string until 14-03 lands). Active tab: bottom positioned absolute View (`position:'absolute', left:0, right:0, bottom:-1, height:2.5, borderRadius:2, backgroundColor: colors.accent`). Static underline per UI-SPEC §Motion (no animation in v1).
  - TYPE header row: `<View flexDirection:'row', alignItems:'center', gap:10, marginBottom:10>` containing two `<Text>` elements — `t('filters.cascading.typeHeader')` (`fontSize:11.5, fontWeight:'700', letterSpacing:1.2, textTransform:'uppercase', color: colors.text`) and `· ${t('filters.cascading.typeHint')}` (`fontSize:11, fontWeight:'600', color: colors.textTertiary, opacity: 0.8`).
  - Chip row: `<View flexDirection:'row', flexWrap:'wrap', gap:9>`. Compute chip set via `useMemo`: `const chipTypes = selectedCategory === 'Residential' ? RESIDENTIAL_TYPES : selectedCategory === 'Commercial' ? COMMERCIAL_TYPES : HOSPITALITY_TYPES;`. Map each `t` in chipTypes → `<Pressable key={t}>`:
    - Style: `paddingVertical:10, paddingLeft:12, paddingRight:15, borderRadius:999, borderWidth:1.5`.
    - Active state: `borderColor: 'transparent', backgroundColor: colors.accent`; label color `#fff` (the one permitted exception, matches ShowButton + project pattern).
    - Inactive state: `borderColor: colors.hair2, backgroundColor: 'transparent'`; label color `colors.textSecondary`.
    - Glyph: 16pt — `<Check>` from lucide when active, `<TypeIcon type={t} size={16} color={...} />` when not.
    - Label: `<Text>{t}</Text>` at `fontSize:14.5, fontWeight:'600'`.
    - accessibilityRole='button', accessibilityState={{ selected: types.includes(t) }}, hitSlop 4.
    - onPress: `setTypes(prev => prev.includes(t) ? prev.filter(x => x !== t) : [...prev, t]);` — OR-set toggle.

  **Live result line** — UI-SPEC §Layout §"CascadingFilter anatomy" final note: result-line stays in HomeScreen.tsx:644-646 (not owned by CascadingFilter in v1). Do NOT add a result line inside the component. The `liveCount` prop is accepted for forward-fit but not currently rendered; document this in component JSDoc.

  **Test file** — `src/components/filters/__tests__/CascadingFilter.test.tsx`. Mock theme + language per KeyStatsCard.test.tsx pattern. Mock DealToggle/TypeIcon/CheckSquare with `jest.mock(...)` shallow stubs that just render their props. Test cases:
  1. Renders Residential by default with 4 chips (Apartment, House, Townhome, Condo).
  2. Multi-select chip toggle: tap inactive chip → `setTypes` called with array containing that type.
  3. Tap active chip → `setTypes` called with array NOT containing that type.
  4. Tap different category tab → `setSelectedCategory` called AND `setTypes([])` called (TWO setter calls, in that order).
  5. DealToggle stub receives `value='Rent'` when prop `transactionType='rent'` and `value='Buy'` when `transactionType='sale'`; on stub callback, parent receives correct rent/sale mapping.
  6. accessibilityState.selected propagates correctly on active/inactive chips and category tabs.
  </action>
  <acceptance_criteria>
    - File `src/components/filters/CascadingFilter.tsx` exists with default export `CascadingFilter`.
    - File `src/components/filters/__tests__/CascadingFilter.test.tsx` exists.
    - Source assertion (uses Plan 14-01 primitives): `grep -c "from './primitives/DealToggle'" src/components/filters/CascadingFilter.tsx` >= 1.
    - Source assertion (uses TypeIcon): `grep -c "from './primitives/TypeIcon'" src/components/filters/CascadingFilter.tsx` >= 1.
    - Source assertion (no hex literals except #fff): `cd "$(git rev-parse --show-toplevel)" && grep -nE "#[0-9a-fA-F]{3,6}" src/components/filters/CascadingFilter.tsx | grep -viE "'#fff'|'#FFFFFF'" | wc -l` == 0.
    - Source assertion (load-bearing setTypes([]) on category change): `grep -c "setTypes(\\[\\])" src/components/filters/CascadingFilter.tsx` >= 1.
    - Source assertion (KBD-02 invariant): `grep -c "keyboardVerticalOffset" src/components/filters/CascadingFilter.tsx` == 0.
    - Test command: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/__tests__/CascadingFilter.test.tsx` exits 0. All 6 test cases pass.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/__tests__/CascadingFilter.test.tsx --silent` exits 0.
  </verification>
</task>

<task id="14-02-02" type="auto">
  <name>Task 2: Add Plan 14-02 i18n subset to en.ts + ru.ts (FILT-02)</name>
  <files>src/locales/en.ts, src/locales/ru.ts</files>
  <read_first>
    - src/locales/en.ts (existing structure + the Plan 14-01 additions from 14-01-SUMMARY)
    - src/locales/ru.ts (matching structure)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md §"Copywriting Contract" (full key table)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-01-SUMMARY.md (verify which keys Plan 14-01 shipped to avoid duplicates)
  </read_first>
  <action>
  Add the **Plan 14-02 subset** of `filters.*` keys to BOTH `src/locales/en.ts` and `src/locales/ru.ts`. Verify against 14-01-SUMMARY which keys are already present (Plan 14-01 shipped `filters.title`, `filters.step.*`, `filters.close`, `filters.showHomes.*`, `filters.type.multiHint`, `filters.deal.rent`, `filters.deal.buy`).

  **Plan 14-02 subset** (Cascading-owned strings):

  | Key | EN | RU |
  |-----|----|----|
  | `filters.cascading.categoryHeader` | CATEGORY | КАТЕГОРИЯ |
  | `filters.cascading.typeHeader` | TYPE | ТИП |
  | `filters.cascading.typeHint` | pick any | выберите любой |
  | `filters.category.residential` | Residential | Жилая |
  | `filters.category.commercial` | Commercial | Коммерческая |
  | `filters.category.hospitality` | Hospitality | Гостеприимство |

  (Note on category labels: UI-SPEC §Copywriting flags that the project may already expose `categoryToggle.residential` etc. — Plan 14-02 executor MUST grep `src/locales/en.ts` for `'category` and either REUSE existing keys (if they exist verbatim) or add new `filters.category.*` keys. Document the decision in commit message.)

  Insert with inline comment block: `// Phase 14 Plan 14-02 — Cascading panel (FILT-02) — D-07 namespace subset`. Order: preserve same key order in en.ts and ru.ts.
  </action>
  <acceptance_criteria>
    - 6 new keys present in en.ts AND in ru.ts (OR fewer if existing `category*` keys are reused — document in commit body).
    - Cascading-namespace keys present: `grep -c "filters.cascading" src/locales/en.ts` >= 3; same for ru.ts.
    - EN+RU parity gate: `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` exits 0.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` exits 0.
    Source assertion: `cd "$(git rev-parse --show-toplevel)" && grep -c "filters.cascading" src/locales/en.ts` >= 3 AND same for ru.ts.
  </verification>
</task>

<task id="14-02-03" type="auto">
  <name>Task 3: HomeScreen surgical edit — delete inline JSX + 10 orphan styles, mount &lt;CascadingFilter /&gt;, add useFilterStyle() (FILT-02)</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (FULL file — lines 1-948; especially lines 14 Modal import, 22-24 LayoutAnimation setup, 44 buildFilterQuery import, 82-93 hook block + state, 96 isFiltersExpanded, 186-247 filteredProperties memo, 319-340 togglePropertyType, 492-506 filter button, 521-642 INLINE BLOCK TO DELETE, 644-646 result count, 866-914 STYLE KEYS TO DELETE, 915+ resultCount style which STAYS)
    - src/context/FilterStyleContext.tsx (Phase 13 — exports useFilterStyle)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md §"HomeScreen.tsx (Plan 14-02 edit)" (verbatim conditional mount + import + hook patterns + delete sentinel)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md §"HomeScreen edits (Plans 14-02 + 14-03)" (8-step delta table)
  </read_first>
  <action>
  Surgical edit on `src/screens/HomeScreen.tsx`. ALL of these changes land in this single task to keep the codebase compilable between commits:

  **Step 1 — Add imports** (near other existing imports around line 44):
  - `import { useFilterStyle } from '../context/FilterStyleContext';`
  - `import CascadingFilter from '../components/filters/CascadingFilter';`
  (Use default import — `src/components/filters/CascadingFilter.tsx` default-exports the component per Task 1.)

  **Step 2 — Add hook call** (near the existing `useTheme()` / `useAuth()` / `useLanguage()` cluster around line 82-84):
  - `const { filterStyle } = useFilterStyle();`

  **Step 3 — DELETE inline JSX block** at lines 521-642 (verify line numbers via `grep -n "isFiltersExpanded &&" src/screens/HomeScreen.tsx` first — may have drifted by 1-2 lines from Phase 13 SUMMARY edits; the block is identified by the opening `{isFiltersExpanded && (<View style={styles.filterSection}>` and the closing `)}` paired with it).

  **Step 4 — INSERT replacement conditional mount** at the same JSX position:
  ```
  {filterStyle === 'cascading' && isFiltersExpanded && (
    <CascadingFilter
      transactionType={transactionType}
      setTransactionType={setTransactionType}
      selectedCategory={selectedCategory}
      setSelectedCategory={setSelectedCategory}
      types={types}
      setTypes={setTypes}
      liveCount={filteredProperties.length}
    />
  )}
  ```

  (Per D-05: the `filterStyle === 'guided'` branch is added in Plan 14-03 — DO NOT add it here.)

  **Step 5 — DELETE 10 orphan StyleSheet keys** at lines 866-914 (verify exact line numbers via `grep -n "filterSection: {" src/screens/HomeScreen.tsx`). Delete keys: `filterSection`, `segmentedControl`, `segmentButton`, `segmentText`, `categoryToggleRow`, `categoryChip`, `filterRow`, `filterList`, `filterChip`, `filterText`. PRESERVE `resultCount` style at line 915+.

  **Step 6 — Check `togglePropertyType` orphan-ness:** run `grep -n "togglePropertyType" src/screens/HomeScreen.tsx`. If the function appears ONLY at its definition (around line 319) and zero call sites remain in JSX after the inline block delete, delete the function too (it was only called from the deleted chip row). If it has other call sites, leave it. Document the decision in commit body.

  **Step 7 — Verify the inline block deletion did not leave dangling JSX braces** by running `npx tsc --noEmit src/screens/HomeScreen.tsx 2>&1 | grep -c "error TS"` and confirming the count has not jumped beyond the Phase 13 baseline (8). If a syntactic error appears, the JSX delete bracket-matching is off — find and fix before committing.
  </action>
  <acceptance_criteria>
    - Source assertion (`<CascadingFilter />` mounted exactly once): `cd "$(git rev-parse --show-toplevel)" && grep -nE "<CascadingFilter " src/screens/HomeScreen.tsx | wc -l` == 1.
    - Source assertion (filterStyle gate present): `cd "$(git rev-parse --show-toplevel)" && grep -nE "filterStyle === 'cascading'" src/screens/HomeScreen.tsx | wc -l` >= 1.
    - Source assertion (useFilterStyle hook imported + called): `cd "$(git rev-parse --show-toplevel)" && grep -c "useFilterStyle" src/screens/HomeScreen.tsx` >= 2 (import + call).
    - Source assertion (CascadingFilter imported): `cd "$(git rev-parse --show-toplevel)" && grep -c "from '../components/filters/CascadingFilter'" src/screens/HomeScreen.tsx` == 1.
    - **Delete sentinel — 10 orphan StyleSheet keys gone (FIXED REGEX — proper ERE alternation, no escaped pipes inside `-E`):** `cd "$(git rev-parse --show-toplevel)" && grep -nE "(filterSection|segmentedControl|segmentButton|segmentText|categoryToggleRow|categoryChip|filterRow|filterList|filterChip|filterText): \{" src/screens/HomeScreen.tsx | wc -l` == 0.
    - **Delete sentinel — inline JSX block gone:** `cd "$(git rev-parse --show-toplevel)" && grep -nE "styles\\.(filterSection|segmentedControl|segmentButton|segmentText|categoryToggleRow|categoryChip|filterRow|filterList|filterChip|filterText)" src/screens/HomeScreen.tsx | wc -l` == 0.
    - Preserve `resultCount` style: `cd "$(git rev-parse --show-toplevel)" && grep -c "resultCount: \{" src/screens/HomeScreen.tsx` == 1.
    - Preserve `isFiltersExpanded` + `setIsFiltersExpanded` state: `cd "$(git rev-parse --show-toplevel)" && grep -c "isFiltersExpanded" src/screens/HomeScreen.tsx` >= 4 (declaration + filter button + Cascading mount gate + result-count region).
    - KBD-02 invariant: `cd "$(git rev-parse --show-toplevel)" && grep -rn "keyboardVerticalOffset" src/ | wc -l` == 0.
    - tsc baseline: `cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -c "error TS"` <= 8.
    - Approximate delete magnitude: `cd "$(git rev-parse --show-toplevel)" && git diff src/screens/HomeScreen.tsx | grep -c "^-"` >= 130 (≈ 169 LOC delete vs ~12 lines added; net delete confirms surgical extract).
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns a number <= 8.
    Source assertion: `cd "$(git rev-parse --show-toplevel)" && grep -nE "styles\\.filterSection" src/screens/HomeScreen.tsx | wc -l` == 0.
    Source assertion: `cd "$(git rev-parse --show-toplevel)" && grep -nE "<CascadingFilter " src/screens/HomeScreen.tsx | wc -l` == 1.
  </verification>
</task>

<task id="14-02-04" type="auto">
  <name>Task 4: Full-suite gates + atomic commit on main</name>
  <files>(no source edits — gate run + commit)</files>
  <read_first>
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md §"Gate Commands"
    - Memory `subagent-cwd-drift-recurring.md`
  </read_first>
  <action>
  CWD-drift mitigation: prepend `cd "$(git rev-parse --show-toplevel)" && ` to EVERY Bash command. Verify `git branch --show-current` outputs `main` before commit (FF-to-main pattern per STATE.md).

  **Gate suite (single Bash block, sequential):**
  1. `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/ src/screens/__tests__/ 2>&1 | tail -20` — all filter + any HomeScreen tests must exit 0.
  2. `cd "$(git rev-parse --show-toplevel)" && grep -rn "keyboardVerticalOffset" src/ | wc -l` — must equal `0`.
  3. `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` — must exit 0.
  4. `cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -c "error TS"` — must be `<= 8`.
  5. **HomeScreen delete sentinels** (Plan 14-02 specific — ERE alternation, no escaped pipes):
     - `cd "$(git rev-parse --show-toplevel)" && grep -nE "(filterSection|segmentedControl|segmentButton|segmentText|categoryToggleRow|categoryChip|filterRow|filterList|filterChip|filterText): \{" src/screens/HomeScreen.tsx | wc -l` MUST equal `0`.
     - `cd "$(git rev-parse --show-toplevel)" && grep -nE "<CascadingFilter " src/screens/HomeScreen.tsx | wc -l` MUST equal `1`.

  If ALL gates pass:

  6. `cd "$(git rev-parse --show-toplevel)" && git branch --show-current` — must print `main`.
  7. `cd "$(git rev-parse --show-toplevel)" && git add src/components/filters/CascadingFilter.tsx src/components/filters/__tests__/CascadingFilter.test.tsx src/screens/HomeScreen.tsx src/locales/en.ts src/locales/ru.ts`.
  8. Single atomic commit on main with message: `feat(14-02): CascadingFilter component + HomeScreen extract (FILT-02)` body explaining the ~169 LOC delete + the conditional mount + Cascading-namespace i18n subset.
  </action>
  <acceptance_criteria>
    - All gates pass (jest, KBD-02, parity, tsc, delete sentinels).
    - Branch verified: `cd "$(git rev-parse --show-toplevel)" && git branch --show-current` == `main`.
    - Single atomic commit lands: `cd "$(git rev-parse --show-toplevel)" && git log -1 --pretty=%s | grep -c "feat(14-02)"` >= 1.
    - Commit touches expected file set: `cd "$(git rev-parse --show-toplevel)" && git diff HEAD~1 HEAD --name-only | sort -u` matches the 5 paths in `files_modified`.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && git log -1 --pretty=%s | grep -c "feat(14-02)"` >= 1.
  </verification>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — N/A for Phase 14) | Phase 14-02 is purely presentational extraction of inline JSX into a controlled component. No auth, no network, no input parsing. Existing HomeScreen filter state shape is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Spoofing | (none) | accept | No identity bearing in Plan 14-02 surface. |
| T-14-08 | Tampering | HomeScreen filter state | accept | State shape unchanged (pre-existing `types: string[]` from Phase 13). CascadingFilter calls setters via props; no direct mutation of HomeScreen's local state. |
| T-14-09 | Repudiation | (none) | accept | No audit-bearing actions. |
| T-14-10 | Information Disclosure | liveCount prop | accept | Plain integer derived from `filteredProperties.length`. Same value already visible at HomeScreen.tsx:644-646. |
| T-14-11 | Denial of Service | Multi-select chip toggle | accept | Setter call is a simple array predicate. No unbounded loop. React reconciliation handles re-render diff. |
| T-14-12 | Elevation of Privilege | (none) | accept | No privilege surface. |
</threat_model>

<verification>
- `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/` exits 0.
- `cd "$(git rev-parse --show-toplevel)" && grep -rn "keyboardVerticalOffset" src/ | wc -l` == 0.
- `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` exits 0.
- `cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -c "error TS"` <= 8.
- HomeScreen delete sentinels both green (using ERE alternation, no escaped pipes).
- One atomic commit on `main`.
- No GuidedFilterSheet code introduced (Plan 14-03 territory).
</verification>

<success_criteria>
- `<CascadingFilter>` component shipped at `src/components/filters/CascadingFilter.tsx` with default export and a 6-case co-located test.
- HomeScreen.tsx mounts `<CascadingFilter />` behind `filterStyle === 'cascading' && isFiltersExpanded` exactly once.
- HomeScreen.tsx is ~169 LOC lighter: 10 orphan StyleSheet keys + inline JSX block both deleted.
- `useFilterStyle()` hook called once near the existing context-hook cluster.
- Cascading-namespace i18n keys (categoryHeader, typeHeader, typeHint, category labels) shipped to EN+RU with parity gate green.
- KBD-02 invariant preserved; tsc baseline preserved.
- One atomic commit on `main`.
- Mid-execution there is NO compilable "broken filter" state because all 4 tasks land in the SAME commit.
</success_criteria>

<output>
After completion, create `.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-02-SUMMARY.md` capturing:
- Files shipped/modified (full list)
- HomeScreen LOC delta (lines added vs lines removed)
- togglePropertyType disposition (deleted as orphan / kept with reason)
- Existing `category*` keys: reused or new `filters.category.*` keys added (document the decision)
- Gate results
- Commit SHA
- Any surprises Plan 14-03 should know about (e.g. tab-strip rendering nits in the test renderer).
</output>
</output>
