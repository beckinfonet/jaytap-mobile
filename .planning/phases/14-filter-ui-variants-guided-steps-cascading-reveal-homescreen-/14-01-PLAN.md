---
phase: 14
plan_id: 14-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/filters/__tests__/ModalProbe.test.tsx
  - src/components/filters/primitives/DealToggle.tsx
  - src/components/filters/primitives/CheckSquare.tsx
  - src/components/filters/primitives/Stepper.tsx
  - src/components/filters/primitives/MultiHint.tsx
  - src/components/filters/primitives/ShowButton.tsx
  - src/components/filters/primitives/Breadcrumb.tsx
  - src/components/filters/primitives/TypeIcon.tsx
  - src/components/filters/primitives/joinTypes.ts
  - src/components/filters/primitives/__tests__/DealToggle.test.tsx
  - src/components/filters/primitives/__tests__/CheckSquare.test.tsx
  - src/components/filters/primitives/__tests__/Stepper.test.tsx
  - src/components/filters/primitives/__tests__/MultiHint.test.tsx
  - src/components/filters/primitives/__tests__/ShowButton.test.tsx
  - src/components/filters/primitives/__tests__/Breadcrumb.test.tsx
  - src/components/filters/primitives/__tests__/TypeIcon.test.tsx
  - src/components/filters/primitives/__tests__/joinTypes.test.ts
  - src/locales/en.ts
  - src/locales/ru.ts
autonomous: true
requirements: [FILT-01, FILT-02]
tags: [filter-primitives, i18n, modal-probe, m6]

must_haves:
  truths:
    - "No hex color literals in src/components/filters/**; all colors via useTheme()"
    - "EN+RU parity enforced via scripts/check-i18n-parity.sh after every commit touching src/locales/"
    - "KBD-02 invariant: zero keyboardVerticalOffset in src/"
    - "Test convention: __tests__/*.test.tsx subdirectories (NOT flat sibling)"
    - "No new dependencies; all capabilities in current package set"
    - "Subagent CWD-drift: prepend cd to every Bash; verify git branch before commit"
  artifacts:
    - path: "src/components/filters/__tests__/ModalProbe.test.tsx"
      provides: "Wave-0 Modal-mount probe; informs Plan 14-03 testability strategy"
    - path: "src/components/filters/primitives/DealToggle.tsx"
      provides: "Sliding-pill Rent/Buy segmented toggle (size='lg'); shared by GuidedFilterSheet and CascadingFilter"
    - path: "src/components/filters/primitives/CheckSquare.tsx"
      provides: "22x22 rounded-square checkbox affordance (multi-select cue)"
    - path: "src/components/filters/primitives/Stepper.tsx"
      provides: "3-pill 1-2-3 stepper bar with done/active/reachable/future states + connector bars"
    - path: "src/components/filters/primitives/MultiHint.tsx"
      provides: "'Choose one or more' pill with mini-check glyph"
    - path: "src/components/filters/primitives/ShowButton.tsx"
      provides: "Full-width accent CTA 'Show N homes' with shadow + pluralization"
    - path: "src/components/filters/primitives/Breadcrumb.tsx"
      provides: "Chevron-separated selection trail with collapse rule"
    - path: "src/components/filters/primitives/TypeIcon.tsx"
      provides: "Lucide-icon map for 10 property types"
    - path: "src/components/filters/primitives/joinTypes.ts"
      provides: "Pure utility: collapse types[] to natural-language string"
    - path: "src/locales/en.ts"
      provides: "filters.* namespace English keys per D-07 subset"
    - path: "src/locales/ru.ts"
      provides: "filters.* namespace Russian keys per D-07 subset (parity with en.ts)"
  key_links:
    - from: "src/components/filters/primitives/Stepper.tsx"
      to: "src/locales/en.ts (filters.step.deal|category|type)"
      via: "useLanguage().t(`filters.step.${STEPS[i]}`)"
      pattern: "filters\\.step\\."
    - from: "src/components/filters/primitives/ShowButton.tsx"
      to: "src/locales/en.ts (filters.showHomes.one|many)"
      via: "count === 1 ? t('filters.showHomes.one') : t('filters.showHomes.many', { count })"
      pattern: "filters\\.showHomes\\."
    - from: "src/components/filters/primitives/TypeIcon.tsx"
      to: "lucide-react-native"
      via: "static Record<PropertyType, LucideIcon> map"
      pattern: "import.*lucide-react-native"
---

<objective>
Ship the filter primitive component set + new `filters.*` i18n namespace subset + Wave-0 Modal-render probe — all isolated, fully tested, zero HomeScreen impact. Foundation for Plans 14-02 (CascadingFilter) and 14-03 (GuidedFilterSheet).

Purpose: Primitives ship in isolation so 14-02 and 14-03 can compose them without bikeshedding shape decisions during variant assembly. The Modal probe (Task 1) determines whether Plan 14-03 can directly test the full `<GuidedFilterSheet>` Modal or must extract a `GuidedFilterSheetContent` inner component — a strategy decision that depends on react-test-renderer Modal capability.

Output: 8 primitives + 1 utility + 8 primitive tests + 1 Modal probe + EN/RU i18n subset. One atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-RESEARCH.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-VALIDATION.md
@src/theme/colors.ts
@src/theme/ThemeContext.tsx
@src/context/LanguageContext.tsx
@src/locales/index.ts
@src/locales/en.ts
@src/locales/ru.ts
@src/utils/propertyCategory.ts
@src/utils/buildFilterQuery.ts
@src/components/DeleteAccountModal.tsx
@src/components/StepperInput.tsx
@src/components/details/AttributeList.tsx
@src/components/__tests__/EmailVerifyBanner.test.tsx
@src/components/details/__tests__/KeyStatsCard.test.tsx
@src/utils/__tests__/buildFilterQuery.test.ts

<interfaces>
<!-- Key types and exports executor needs. Do not explore further. -->

From src/utils/propertyCategory.ts (Phase 14 consumes these — DO NOT MODIFY):
```
export type PropertyCategory = 'Residential' | 'Commercial' | 'Hospitality';
export type PropertyType = 'Apartment'|'House'|'Townhome'|'Condo'|'Office'|'Retail'|'Warehouse'|'Industrial'|'Hostel'|'Hotel';
export const RESIDENTIAL_TYPES: readonly PropertyType[] = ['Apartment','House','Townhome','Condo'];
export const COMMERCIAL_TYPES:  readonly PropertyType[] = ['Office','Retail','Warehouse','Industrial'];
export const HOSPITALITY_TYPES: readonly PropertyType[] = ['Hostel','Hotel'];
```

From src/locales/index.ts:
```
export function t(language: Language, key: TranslationKeys, params?: Record<string, string>): string;
export type { TranslationKeys } from './en';
```

From src/context/LanguageContext.tsx (consumer pattern):
```
const { t, language } = useLanguage();
t('filters.title')                                       // -> string
t('filters.showHomes.many', { count: String(n) })        // {count} interpolation
```

From src/theme/ThemeContext.tsx:
```
const { colors, isDark, theme } = useTheme();
colors.background | surface | surface2 | surface3 | accent | accentSoft | accentLine
colors.text | textSecondary | textTertiary | landlordGreen | scrim | border | hair2
colors.iconChipFg
```
</interfaces>
</context>

<tasks>

<task id="14-01-00" type="auto">
  <name>Task 1 (Wave 0): Modal-render probe — gate Plan 14-03 strategy</name>
  <files>src/components/filters/__tests__/ModalProbe.test.tsx</files>
  <read_first>
    - src/components/__tests__/EmailVerifyBanner.test.tsx (canonical react-test-renderer + act pattern, lines 1-70)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-RESEARCH.md §A5 (Modal-test-renderer risk)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-VALIDATION.md §"Wave 0 Requirements"
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md §"`src/components/filters/__tests__/ModalProbe.test.tsx`"
  </read_first>
  <action>
  Create the smallest possible probe verifying react-test-renderer can mount and unmount an RN `<Modal>` without crashing. Imports: `React`, `TestRenderer, { act } from 'react-test-renderer'`, `{ Modal, Text, View } from 'react-native'`. Two `test()` cases:

  1. **"mounts a visible Modal without throwing"** — `act(() => { tree = TestRenderer.create(<Modal visible transparent animationType="none" onRequestClose={() => {}}><View><Text>x</Text></View></Modal>); })` and assert `tree.toJSON() !== null`.

  2. **"unmounts cleanly when visible toggles to false"** — render with `visible={true}`, then `act(() => tree.update(<Modal visible={false} transparent animationType="none" onRequestClose={() => {}}><View><Text>x</Text></View></Modal>))`, assert no throw.

  File-header JSDoc must declare: "Phase 14 Plan 14-01 Wave-0 probe. If this passes, Plan 14-03 ships GuidedFilterSheet.test.tsx rendering the full Modal. If it fails, Plan 14-03 extracts a `GuidedFilterSheetContent` inner component (sheet body without Modal wrapper) and tests that directly. See 14-VALIDATION.md §Wave 0 Requirements."

  NO `@testing-library/react-native` import (not in devDeps per 14-PATTERNS §Test Framework Pattern). NO jest-native matchers — use plain expect.
  </action>
  <acceptance_criteria>
    - File exists at `src/components/filters/__tests__/ModalProbe.test.tsx`.
    - Source assertion: `grep -c "import.*react-test-renderer" src/components/filters/__tests__/ModalProbe.test.tsx` == 1.
    - Source assertion: `grep -c "@testing-library" src/components/filters/__tests__/ModalProbe.test.tsx` == 0.
    - Test command: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/__tests__/ModalProbe.test.tsx` exits 0 (the Plan 14-03 happy-path is confirmed) OR exits non-zero (Plan 14-03 takes the inner-component fallback). EITHER outcome is acceptable here — this probe is a diagnostic, not a gate. Record the outcome verbatim in the plan SUMMARY for Plan 14-03 to consume.
    - File-header JSDoc names "Plan 14-01 Wave-0 probe" and references 14-VALIDATION.md §Wave 0 Requirements.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/__tests__/ModalProbe.test.tsx --silent 2>&1 | tail -20` — capture output for SUMMARY (PASS/FAIL informs 14-03 strategy).
    Source assertion: `cd "$(git rev-parse --show-toplevel)" && grep -c "Wave-0 probe" src/components/filters/__tests__/ModalProbe.test.tsx` >= 1.
  </verification>
</task>

<task id="14-01-01" type="auto" tdd="true">
  <name>Task 2: joinTypes pure utility + unit test (FILT-01, FILT-02)</name>
  <files>src/components/filters/primitives/joinTypes.ts, src/components/filters/primitives/__tests__/joinTypes.test.ts</files>
  <read_first>
    - src/utils/buildFilterQuery.ts (lines 1-43 JSDoc header convention, lines 68-92 body shape)
    - src/utils/__tests__/buildFilterQuery.test.ts (pure-fn unit test pattern, full file)
    - src/utils/propertyCategory.ts (PropertyCategory + PropertyType types + TYPE_SETS)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md §"joinTypes.ts"
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md §"Specifics" (joinTypes algorithm + handoff lines 75-79 reference)
  </read_first>
  <behavior>
    - `joinTypes('Residential', [])` → `''` (empty array)
    - `joinTypes('Residential', ['Apartment'])` → `'Apartment'` (one type)
    - `joinTypes('Residential', ['Apartment', 'House'])` → `'Apartment or House'` (two types)
    - `joinTypes('Residential', ['Apartment', 'House', 'Townhome'])` → `'Apartment, House or Townhome'` (3+ types)
    - `joinTypes('Residential', ['Apartment'], true)` → `'apartment'` (lower-cased per `lower=true`)
    - `joinTypes('Hospitality', ['Hostel','Hotel'])` → `'Hostel or Hotel'` (works for any category)
  </behavior>
  <action>
  Create `src/components/filters/primitives/joinTypes.ts`. Named export `function joinTypes(category: PropertyCategory, types: string[], lower = false): string`. Pure function — NO React imports. JSDoc header MUST cite "handoff filters-shared.jsx:75-79 (ported)" and "Convention: matches buildFilterQuery.ts shape (named export, no React imports, no side effects)".

  Algorithm (per handoff lines 75-79):
  - if `types.length === 0` → return `''`
  - if `types.length === 1` → return `lower ? types[0].toLowerCase() : types[0]`
  - if `types.length === 2` → return `${a} or ${b}` (with lowercasing if `lower`)
  - if `types.length >= 3` → return `${a}, ${b} or ${c}` for first 3 (handoff stops at 3 — verify against handoff source in /tmp; if /tmp stale, ship "first N-1 comma-joined, then ' or {last}'" generalization). Lowercase each segment if `lower=true`.

  Import `PropertyCategory` from `../../../utils/propertyCategory`. `category` parameter is accepted to match handoff signature even though current logic does not branch on it (forward-fit for category-specific connective words in M7+).

  Companion test file: `src/components/filters/primitives/__tests__/joinTypes.test.ts`. Header per buildFilterQuery.test.ts convention. Cover all 6 behavior cases above with `describe('joinTypes', () => { describe('zero types', ...); describe('one type', ...); ... })` block nesting.
  </action>
  <acceptance_criteria>
    - File `src/components/filters/primitives/joinTypes.ts` exists with named export `joinTypes`.
    - Source assertion: `grep -c "^import.*react" src/components/filters/primitives/joinTypes.ts` == 0 (no React imports — pure fn).
    - Source assertion: `grep -c "export function joinTypes" src/components/filters/primitives/joinTypes.ts` == 1.
    - Source assertion: `grep -c "filters-shared.jsx" src/components/filters/primitives/joinTypes.ts` >= 1 (handoff citation in JSDoc).
    - Test command: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/primitives/__tests__/joinTypes.test.ts` exits 0.
    - Test command (behavior gate): all 6 behavior cases pass.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/primitives/__tests__/joinTypes.test.ts --silent` exits 0.
  </verification>
</task>

<task id="14-01-02" type="auto" tdd="true">
  <name>Task 3: TypeIcon primitive — Lucide icon map for 10 property types (FILT-01, FILT-02)</name>
  <files>src/components/filters/primitives/TypeIcon.tsx, src/components/filters/primitives/__tests__/TypeIcon.test.tsx</files>
  <read_first>
    - src/components/details/AttributeList.tsx (lines 11-19 + 28-32 + 139, 152 — static LucideIcon Record + render-via-component-from-map pattern)
    - src/utils/propertyCategory.ts (PropertyType type definition)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md §"TypeIcon.tsx"
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-RESEARCH.md §"Lucide Icon Verification" (locked 10-type map)
  </read_first>
  <behavior>
    - `<TypeIcon type="Apartment" />` renders a `Building` Lucide component
    - `<TypeIcon type="House" />` renders `House`
    - `<TypeIcon type="Townhome" />` renders `Building2`
    - `<TypeIcon type="Condo" />` renders `Building2`
    - `<TypeIcon type="Office" />` renders `Briefcase`
    - `<TypeIcon type="Retail" />` renders `Store`
    - `<TypeIcon type="Warehouse" />` renders `Warehouse`
    - `<TypeIcon type="Industrial" />` renders `Factory`
    - `<TypeIcon type="Hostel" />` renders `BedDouble`
    - `<TypeIcon type="Hotel" />` renders `Hotel`
    - Accepts `size?: number` (default 19) and `color?: string` props; passes through to Lucide; `strokeWidth={1.75}` default.
  </behavior>
  <action>
  Create `src/components/filters/primitives/TypeIcon.tsx`. Default-export `TypeIcon: React.FC<{ type: PropertyType; size?: number; color?: string }>`. Also named-export the map `ICON_MAP`.

  Imports from `lucide-react-native`: `Building, House, Building2, Briefcase, Store, Warehouse, Factory, BedDouble, Hotel, type LucideIcon`. Note: collision between local export name `House`/`Hotel`/`Warehouse` and Lucide named imports is intentional (TS handles this via static map key syntax). Import `PropertyType` from `../../../utils/propertyCategory`.

  Build `const ICON_MAP: Record<PropertyType, LucideIcon> = { Apartment: Building, House: House, Townhome: Building2, Condo: Building2, Office: Briefcase, Retail: Store, Warehouse: Warehouse, Industrial: Factory, Hostel: BedDouble, Hotel: Hotel };` — VERBATIM from planning_context §"Lucide icon map (TypeIcon.tsx) is locked".

  Component body: `const Icon = ICON_MAP[type]; if (!Icon) return null; return <Icon size={size} color={color} strokeWidth={1.75} />;`.

  Companion test file: cover all 10 types map to expected Lucide component (use `tree.root.findByType(Building)` etc. to assert the right Lucide component rendered). Mock theme not needed (no useTheme call in TypeIcon).
  </action>
  <acceptance_criteria>
    - File exists with default export `TypeIcon` + named export `ICON_MAP`.
    - Source assertion: `grep -c "Apartment: Building" src/components/filters/primitives/TypeIcon.tsx` == 1.
    - Source assertion: `grep -c "Hostel: BedDouble" src/components/filters/primitives/TypeIcon.tsx` == 1.
    - Source assertion: `grep -c "Hotel: Hotel" src/components/filters/primitives/TypeIcon.tsx` == 1.
    - Source assertion: `grep -c "strokeWidth={1.75}" src/components/filters/primitives/TypeIcon.tsx` >= 1.
    - Test command: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/primitives/__tests__/TypeIcon.test.tsx` exits 0. All 10 type-mapping tests pass.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/primitives/__tests__/TypeIcon.test.tsx --silent` exits 0.
  </verification>
</task>

<task id="14-01-03" type="auto" tdd="true">
  <name>Task 4: CheckSquare + MultiHint + Breadcrumb display primitives (FILT-01)</name>
  <files>src/components/filters/primitives/CheckSquare.tsx, src/components/filters/primitives/MultiHint.tsx, src/components/filters/primitives/Breadcrumb.tsx, src/components/filters/primitives/__tests__/CheckSquare.test.tsx, src/components/filters/primitives/__tests__/MultiHint.test.tsx, src/components/filters/primitives/__tests__/Breadcrumb.test.tsx</files>
  <read_first>
    - src/components/details/AttributeList.tsx (lines 166-175 boolean badge with `✓` glyph; lines 216-228 StyleSheet shape)
    - src/components/details/__tests__/KeyStatsCard.test.tsx (theme + language mock pattern — full file)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md §"CheckSquare.tsx" + §"Breadcrumb"
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md §Layout §"GuidedFilterSheet anatomy" (Breadcrumb row spec lines 336-343) + §Color + §Typography
  </read_first>
  <behavior>
    - **CheckSquare:** `<CheckSquare checked={true} />` renders 22×22 rounded-6 square with `colors.accent` background + `✓` glyph in white. `checked={false}` renders transparent background + `colors.hair2` 1.5pt border, no glyph. Pure display — no callbacks.
    - **MultiHint:** `<MultiHint />` renders a small pill containing a tiny check glyph + text `t('filters.type.multiHint')` (i.e. "Choose one or more" / "Выберите один или несколько"). Style: 11pt/600, letterSpacing 1.2, color `colors.textTertiary`.
    - **Breadcrumb:** `<Breadcrumb deal={'Rent' | 'Buy' | null} category={PropertyCategory | null} types={string[]} />` renders a flex-wrap row of labels separated by Lucide `ChevronRight` icons (14pt, `colors.textTertiary`). Collapse rule: 1 type → bare label; N>1 types → `{firstType} +{N-1}`. Labels are 13.5/600 `colors.text`. minHeight 24, gap 7. If all 3 props are null/empty, render an empty placeholder (no separators, no labels).
  </behavior>
  <action>
  **CheckSquare** — see 14-PATTERNS.md verbatim shape. Default-export `CheckSquare: React.FC<{ checked: boolean }>`. Imports `useTheme` from `'../../../theme/ThemeContext'`. StyleSheet: `checkSquare: { width: 22, height: 22, borderRadius: 6, alignItems: 'center', justifyContent: 'center' }, glyph: { color: '#FFFFFF', fontSize: 13, lineHeight: 15, fontWeight: '700' }`. Backgrounds inline via `colors.accent` (checked) / `'transparent'` (unchecked); border via `colors.accent` (checked) / `colors.hair2` (unchecked), `borderWidth: 1.5`.

  **MultiHint** — default-export `MultiHint: React.FC<{}>`. Imports `useTheme` + `useLanguage`. Renders a `<View>` pill (`paddingHorizontal 10, paddingVertical 4, borderRadius 999, backgroundColor colors.surface2`) containing inline check glyph (`Check` from `lucide-react-native`, 10pt, `colors.textTertiary`) + `<Text>` rendering `t('filters.type.multiHint')` at 11pt/600 letterSpacing 1.2 `colors.textTertiary`.

  **Breadcrumb** — default-export `Breadcrumb: React.FC<{ deal: 'Rent' | 'Buy' | null; category: PropertyCategory | null; types: string[] }>`. Imports `useTheme`, `useLanguage`, `joinTypes` from `./joinTypes`, `ChevronRight` from `lucide-react-native`, `PropertyCategory` from `'../../../utils/propertyCategory'`. Compute labels array: `[deal, category, types.length === 0 ? null : (types.length === 1 ? types[0] : `${types[0]} +${types.length - 1}`)].filter(Boolean)`. Render as flex-row wrap with `ChevronRight` (14pt) between each non-first label. Container style: `flexDirection: 'row', flexWrap: 'wrap', alignItems: 'center', gap: 7, minHeight: 24`. Label style per UI-SPEC §Typography "Breadcrumb / footer body": `fontSize: 13.5, fontWeight: '600', color: colors.text`.

  **All three test files** — use KeyStatsCard.test.tsx mock pattern: `jest.mock('../../../theme/ThemeContext', ...)` + `jest.mock('../../../context/LanguageContext', ...)`. CheckSquare test verifies presence of `✓` glyph when `checked=true` and absence when `checked=false`. MultiHint test verifies `t` is called with `'filters.type.multiHint'` key. Breadcrumb test cases: (a) all null → empty render; (b) deal='Rent' only → 1 label, 0 chevrons; (c) deal+category+1 type → 3 labels, 2 chevrons; (d) deal+category+3 types → collapse to "{first} +2" (3 labels, 2 chevrons).

  Hit-slop NOT applicable (display-only). Theme: `useTheme()` only — no destructure of `isDark`.
  </action>
  <acceptance_criteria>
    - All 3 source files + all 3 test files exist.
    - Source assertion (no hex literals): `grep -nE "#[0-9a-fA-F]{3,6}" src/components/filters/primitives/{CheckSquare,MultiHint,Breadcrumb}.tsx | grep -v "'#FFFFFF'" | wc -l` == 0 (only #FFFFFF allowed for the check glyph color per pattern).
    - Source assertion (Breadcrumb imports joinTypes): `grep -c "from './joinTypes'" src/components/filters/primitives/Breadcrumb.tsx` >= 0 (note: joinTypes is imported even though collapse rule is inlined; if the rule is delegated entirely to joinTypes, this should be == 1; if inlined, can be 0 — either path is acceptable).
    - Source assertion (no ChevronRight in CheckSquare/MultiHint, only in Breadcrumb): `grep -c "ChevronRight" src/components/filters/primitives/Breadcrumb.tsx` == 1.
    - Test command: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/primitives/__tests__/CheckSquare.test.tsx src/components/filters/primitives/__tests__/MultiHint.test.tsx src/components/filters/primitives/__tests__/Breadcrumb.test.tsx` exits 0.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/primitives/__tests__/CheckSquare.test.tsx src/components/filters/primitives/__tests__/MultiHint.test.tsx src/components/filters/primitives/__tests__/Breadcrumb.test.tsx --silent` exits 0.
  </verification>
</task>

<task id="14-01-04" type="auto" tdd="true">
  <name>Task 5: DealToggle + Stepper + ShowButton interactive primitives (FILT-01, FILT-02)</name>
  <files>src/components/filters/primitives/DealToggle.tsx, src/components/filters/primitives/Stepper.tsx, src/components/filters/primitives/ShowButton.tsx, src/components/filters/primitives/__tests__/DealToggle.test.tsx, src/components/filters/primitives/__tests__/Stepper.test.tsx, src/components/filters/primitives/__tests__/ShowButton.test.tsx</files>
  <read_first>
    - src/components/StepperInput.tsx (lines 29-32 imports; lines 109-130 Pressable + hitSlop + boundary-disabled state)
    - src/components/__tests__/EmailVerifyBanner.test.tsx (full file — react-test-renderer + act + findByLabel walk-and-fire)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md §"Stepper.tsx" + §"ShowButton.tsx" + §"DealToggle"
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md §"Motion / Animation" (DealToggle thumb slide 200ms easeInOut; Stepper no animation; ShowButton no animation) + §Color §"Accent reserved for" + §Typography
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md §"Specifics" (Stepper visual states verbatim per handoff line 99-110)
  </read_first>
  <behavior>
    - **DealToggle:** `<DealToggle value={'Rent' | 'Buy'} onChange={(v) => void} />` renders a horizontal pill (44pt high, borderRadius 22, background `colors.surface2`) with two segments. Active segment paints `colors.accent` background + `#fff` label; inactive label `colors.textSecondary`. Tapping inactive segment fires `onChange(other)`. Sliding-thumb animation: `Animated.Value` position interpolated to slide between left/right halves, 200ms `Easing.inOut(Easing.cubic)`, `useNativeDriver: true`. Labels render via `t('filters.deal.rent')` / `t('filters.deal.buy')`. accessibilityRole='button', accessibilityState={{ selected }}, hitSlop 4 per side.
    - **Stepper:** `<Stepper step={0|1|2} onStepPress={(i: number) => void} reached={(i: number) => boolean} />` renders 3 horizontally-arranged pills with 2 connector bars between. Pill is 26pt circle. States per CONTEXT.md D-09 + §Specifics: done (`step > i`) → `colors.landlordGreen` bg + white `Check` glyph + dim label; active (`step === i`) → `colors.accent` bg + `#fff` "1/2/3" + full-text label; reachable (`step < i && reached(i)`) → `colors.surface2` bg + mute number + mute label; future → same as reachable but disabled (no press). Connector bars: 2pt height, `colors.landlordGreen` when `step > i`, else `colors.surface2`. Labels via `t('filters.step.deal'|'.category'|'.type')` at 14/600. accessibilityRole='button', accessibilityState={{ disabled: !reached(i), selected: step === i }}, accessibilityLabel={t(`filters.step.${STEPS[i]}`)}, hitSlop 4 per side.
    - **ShowButton:** `<ShowButton count={number} onPress={() => void} />` renders a full-width pill (height 54, borderRadius 16, backgroundColor `colors.accent`) with label "Show {count} homes" / "Show 1 home" via pluralization. Label: 17/700 `#fff`. Shadow per 14-UI-SPEC §Layout §"GuidedFilterSheet anatomy" (`shadowColor '#000', shadowOpacity 0.4, shadowOffset {0, 12}, shadowRadius 26, elevation 8`). Always enabled per D-14 (even when `count === 0`). Pressed opacity 0.85. hitSlop 4. accessibilityRole='button', accessibilityLabel set to the rendered label.
  </behavior>
  <action>
  **DealToggle** — imports `Animated, Easing, Pressable, View, Text, StyleSheet` from `react-native`; `useTheme`, `useLanguage`; `TranslationKeys` from `'../../../locales'`. Internal `Animated.Value` for thumb x-position (0 or 1). On `value` change, `Animated.timing(thumbPos, { toValue: value === 'Rent' ? 0 : 1, duration: 200, easing: Easing.inOut(Easing.cubic), useNativeDriver: true }).start()`. Render `<View>` container with absolute-positioned `Animated.View` thumb (50% width, full height, `colors.accent`, borderRadius 22) translated via `translateX` interpolation `[0, '100%']`. Two `Pressable` segments overlaid, accessibilityLabel = `t('filters.deal.rent')` / `t('filters.deal.buy')`.

  **Stepper** — imports `Pressable, StyleSheet, Text, View, Animated` from `react-native`; `Check` from `lucide-react-native`; `useTheme`, `useLanguage`. Internal `const STEPS = ['deal', 'category', 'type'] as const;`. Loop `STEPS.map((s, i) => ...)`. Pill background resolution per behavior table above. Connector bar between pills (only between 0-1 and 1-2). No animation — snap-swap on state change.

  **ShowButton** — imports `Pressable, Text, StyleSheet` from `react-native`; `useTheme`, `useLanguage`. Pluralization: `const label = count === 1 ? t('filters.showHomes.one') : t('filters.showHomes.many', { count: String(count) });`. Per D-14, button always enabled — no `disabled` prop, no opacity-disabled state. Pressed opacity 0.85 via `style={({ pressed }) => [styles.button, { backgroundColor: colors.accent, ..., opacity: pressed ? 0.85 : 1 }]}`.

  **All 3 tests** — use EmailVerifyBanner.test.tsx pattern: mock ThemeContext + LanguageContext (with `t` returning the key). DealToggle test: render `value='Rent'`, walk tree, simulate press on Buy segment, assert `onChange('Buy')` called. Stepper test cases: (a) renders 3 pills + 2 connectors; (b) `reached(0)=true, reached(1)=false, reached(2)=false, step=0`: only pill 0 is pressable, pill 1+2 have `accessibilityState.disabled=true`; (c) `step=1`: pill 0 shows done-state (look for `Check` glyph or landlordGreen marker); (d) pressing pill 0 when reached fires `onStepPress(0)`. ShowButton test cases: (a) `count=1` calls `t('filters.showHomes.one')`; (b) `count=5` calls `t('filters.showHomes.many', { count: '5' })`; (c) `count=0` calls many path with count: '0' (NOT disabled — D-14); (d) press fires onPress.
  </action>
  <acceptance_criteria>
    - All 3 source files + all 3 test files exist.
    - Source assertion (DealToggle uses Animated): `grep -c "Animated.timing" src/components/filters/primitives/DealToggle.tsx` >= 1.
    - Source assertion (Stepper has 3 step constants): `grep -c "deal.*category.*type" src/components/filters/primitives/Stepper.tsx` >= 1.
    - Source assertion (ShowButton always-enabled, D-14): `grep -c "disabled" src/components/filters/primitives/ShowButton.tsx` == 0 (no `disabled` prop usage).
    - Source assertion (no banned `keyboardVerticalOffset`): `grep -c "keyboardVerticalOffset" src/components/filters/primitives/{DealToggle,Stepper,ShowButton}.tsx` == 0.
    - Source assertion (hex literal scan — only `#fff` / `#FFFFFF` permitted for text-on-accent): `grep -nE "#[0-9a-fA-F]{3,6}" src/components/filters/primitives/{DealToggle,Stepper,ShowButton}.tsx | grep -viE "'#fff'|'#FFFFFF'|'#000'" | wc -l` == 0.
    - Source assertion (hitSlop on every Pressable in interactive primitives): `grep -c "hitSlop" src/components/filters/primitives/{DealToggle,Stepper,ShowButton}.tsx` >= 3.
    - Test command: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/primitives/__tests__/DealToggle.test.tsx src/components/filters/primitives/__tests__/Stepper.test.tsx src/components/filters/primitives/__tests__/ShowButton.test.tsx` exits 0.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/primitives/__tests__/DealToggle.test.tsx src/components/filters/primitives/__tests__/Stepper.test.tsx src/components/filters/primitives/__tests__/ShowButton.test.tsx --silent` exits 0.
    Source assertion (KBD-02): `cd "$(git rev-parse --show-toplevel)" && grep -rn "keyboardVerticalOffset" src/components/filters/ | wc -l` == 0.
  </verification>
</task>

<task id="14-01-05" type="auto">
  <name>Task 6: i18n filters.* namespace subset — EN + RU atomic add (FILT-01, FILT-02)</name>
  <files>src/locales/en.ts, src/locales/ru.ts</files>
  <read_first>
    - src/locales/en.ts (read lines 1-50 for export pattern + lines 100-110 for `home.rejection.banner.singular/plural` pluralization precedent)
    - src/locales/ru.ts (read lines 100-110 for matching pluralization precedent)
    - scripts/check-i18n-parity.sh (parity gate command — confirm presence)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md §"Copywriting Contract" (full key table + EN+RU translations)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md §D-07 (i18n namespace decision)
  </read_first>
  <action>
  Add the **Plan 14-01 subset** of `filters.*` keys to BOTH `src/locales/en.ts` and `src/locales/ru.ts` atomically. Plans 14-02 and 14-03 ship the remaining subsets per UI-SPEC §"Component Inventory" §"i18n keys".

  **Plan 14-01 subset** (the primitives this plan ships own these surfaces):

  | Key | EN | RU |
  |-----|----|----|
  | `filters.title` | Filters | Фильтры |
  | `filters.step.deal` | Deal | Тип сделки |
  | `filters.step.category` | Category | Категория |
  | `filters.step.type` | Type | Тип |
  | `filters.close` | Close filters | Закрыть фильтры |
  | `filters.showHomes.one` | Show 1 home | Показать 1 объект |
  | `filters.showHomes.many` | Show {count} homes | Показать {count} объектов |
  | `filters.type.multiHint` | Choose one or more | Выберите один или несколько |
  | `filters.deal.rent` | Rent | Аренда |
  | `filters.deal.buy` | Buy | Купить |

  Note: `filters.deal.rent` + `filters.deal.buy` are shipped here (not 14-02 as in UI-SPEC suggestion) because the DealToggle primitive lives in this plan and its tests need the keys. UI-SPEC §"Distribution across plans is up to planner discretion" — confirm here.

  Insert each key in BOTH files. Add at the end of the existing export object, before the closing `};`, with an inline comment block: `// Phase 14 Plan 14-01 — filter UI primitives (FILT-01, FILT-02) — D-07 namespace subset`.

  Order: preserve the same key order in en.ts and ru.ts. Russian translations are LOCKED per UI-SPEC §Copywriting Contract — do not invent alternatives.
  </action>
  <acceptance_criteria>
    - All 10 keys present in en.ts: `grep -c "^  'filters\\." src/locales/en.ts` >= 10.
    - All 10 keys present in ru.ts: `grep -c "^  'filters\\." src/locales/ru.ts` >= 10.
    - Specific keys present: `grep -c "filters.title" src/locales/en.ts` >= 1; `grep -c "filters.title" src/locales/ru.ts` >= 1; `grep -c "filters.showHomes.one" src/locales/en.ts` >= 1; same in ru.ts; `grep -c "filters.showHomes.many" src/locales/{en,ru}.ts` >= 2 (one per file).
    - Interpolation present in many-key: `grep -c "Show {count} homes" src/locales/en.ts` == 1; `grep -c "Показать {count} объектов" src/locales/ru.ts` == 1.
    - EN+RU parity gate: `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` exits 0.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` exits 0.
    Source assertion: `cd "$(git rev-parse --show-toplevel)" && grep -c "^  'filters\\." src/locales/en.ts` >= 10 AND `cd "$(git rev-parse --show-toplevel)" && grep -c "^  'filters\\." src/locales/ru.ts` >= 10.
  </verification>
</task>

<task id="14-01-06" type="auto">
  <name>Task 7: Full-suite gates + atomic commit on main</name>
  <files>(no source edits — gate run + commit)</files>
  <read_first>
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md §"Gate Commands"
    - Memory `subagent-cwd-drift-recurring.md` (CWD-drift mitigation)
    - Memory `m1-keyboard-kbd-02-invariants.md` (KBD-02 grep gate)
  </read_first>
  <action>
  Run all phase gates from a single Bash invocation, capture outcomes, then commit all Plan 14-01 files atomically on `main`. CWD-drift mitigation: prepend `cd "$(git rev-parse --show-toplevel)" && ` to EVERY Bash command. Before commit, verify `git branch --show-current` outputs `main` (this project's pattern is FF-to-main per STATE.md M5 Phase 1 + Phase 13 close lines).

  **Gate suite (single Bash block, sequential):**
  1. `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/` — all primitive + probe tests must exit 0.
  2. `cd "$(git rev-parse --show-toplevel)" && grep -rn "keyboardVerticalOffset" src/ | wc -l` — must equal `0`.
  3. `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` — must exit 0.
  4. `cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -c "error TS"` — must be `<= 8` (Phase 13 baseline per CONTEXT.md §"Gate Commands"; do NOT regress).

  If ALL gates pass:

  5. `cd "$(git rev-parse --show-toplevel)" && git branch --show-current` — must print `main`.
  6. `cd "$(git rev-parse --show-toplevel)" && git add src/components/filters/ src/locales/en.ts src/locales/ru.ts`.
  7. `cd "$(git rev-parse --show-toplevel)" && git commit -m "feat(14-01): filter primitives + filters.* i18n + Modal probe (FILT-01, FILT-02)$(printf '\n\n')- DealToggle, CheckSquare, Stepper, MultiHint, ShowButton, Breadcrumb, TypeIcon primitives$(printf '\n')- joinTypes pure utility (handoff filters-shared.jsx:75-79)$(printf '\n')- Wave-0 Modal-render probe (gates Plan 14-03 test strategy)$(printf '\n')- filters.* namespace subset in en.ts + ru.ts (parity green)$(printf '\n\n')Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"`.

  Do NOT push. Do NOT touch HomeScreen.tsx in this plan.
  </action>
  <acceptance_criteria>
    - Gate 1 passes: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/ --silent` exits 0.
    - Gate 2 passes (KBD-02): `cd "$(git rev-parse --show-toplevel)" && [ $(grep -rn "keyboardVerticalOffset" src/ | wc -l) -eq 0 ]` exits 0.
    - Gate 3 passes (i18n parity): `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` exits 0.
    - Gate 4 passes (tsc baseline): `cd "$(git rev-parse --show-toplevel)" && [ $(npx tsc --noEmit 2>&1 | grep -c "error TS") -le 8 ]` exits 0.
    - Branch verified: `cd "$(git rev-parse --show-toplevel)" && git branch --show-current` outputs exactly `main`.
    - Single atomic commit lands on main: `cd "$(git rev-parse --show-toplevel)" && git log -1 --pretty=%s` includes `feat(14-01)`.
    - Commit touches the expected file set: `cd "$(git rev-parse --show-toplevel)" && git diff HEAD~1 HEAD --name-only | grep -c "^src/components/filters/"` >= 16 (8 primitives + 8 tests + probe = 17; allow >=16 for off-by-one in primitives/utility counts).
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && git log -1 --pretty=%s | grep -c "feat(14-01)"` >= 1.
    Source assertion: `cd "$(git rev-parse --show-toplevel)" && git diff HEAD~1 HEAD --name-only | grep -E "^src/(components/filters/|locales/(en|ru)\.ts)" | wc -l` >= 18 (8 primitives + 8 tests + probe + 2 locales = 19 ish; allow some buffer).
  </verification>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — N/A for Phase 14) | Phase 14 is purely presentational: client-only primitives + i18n keys. No auth, no data handling, no network calls, no user input parsing, no protected routes. No new attack surface introduced. Existing HomeScreen filter state shape is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing | (none) | accept | Phase 14-01 ships no auth/session-bearing surface. Primitives render based on caller-supplied props only. |
| T-14-02 | Tampering | i18n keys (en.ts/ru.ts) | accept | Locale files are static, bundled at build time. No runtime mutation. Standard RN bundle integrity covers this. |
| T-14-03 | Repudiation | (none) | accept | No audit-bearing actions in Plan 14-01 surface. |
| T-14-04 | Information Disclosure | (none) | accept | Primitives render only the data passed via props. No backend, no user data. The `count` prop on ShowButton is a derived integer (`filteredProperties.length`) — same data already visible elsewhere on HomeScreen. |
| T-14-05 | Denial of Service | DealToggle Animated.Value | accept | Single 200ms `Animated.timing` per toggle press. `useNativeDriver: true` keeps it off JS thread. No unbounded loop, no allocation surge. |
| T-14-06 | Elevation of Privilege | (none) | accept | No privilege surface in primitives. |
</threat_model>

<verification>
- All Phase 14-01 primitive + probe tests pass: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/` exits 0.
- KBD-02 grep gate: `cd "$(git rev-parse --show-toplevel)" && grep -rn "keyboardVerticalOffset" src/ | wc -l` == 0.
- i18n EN+RU parity: `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` exits 0.
- tsc baseline: `cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -c "error TS"` <= 8 (Phase 13 baseline).
- No hex literals in primitives (except permitted `#fff`/`#FFFFFF`/`#000` for text-on-accent + shadow): grep sweep returns 0 unauthorized lines.
- No HomeScreen.tsx edits: `cd "$(git rev-parse --show-toplevel)" && git diff HEAD~1 HEAD --name-only | grep -c "HomeScreen.tsx"` == 0.
- Wave-0 probe outcome recorded in SUMMARY for Plan 14-03 strategy decision.
</verification>

<success_criteria>
- 8 primitive components shipped at `src/components/filters/primitives/{DealToggle,CheckSquare,Stepper,MultiHint,ShowButton,Breadcrumb,TypeIcon}.tsx` + 1 utility `joinTypes.ts`.
- 9 co-located tests under `src/components/filters/primitives/__tests__/` + 1 Modal-probe test under `src/components/filters/__tests__/ModalProbe.test.tsx`.
- `filters.*` namespace subset (10 keys) added to BOTH `src/locales/en.ts` and `src/locales/ru.ts` with EN+RU parity gate green.
- Wave-0 Modal-probe result recorded in SUMMARY (PASS → 14-03 ships full GuidedFilterSheet.test.tsx; FAIL → 14-03 extracts GuidedFilterSheetContent inner component).
- KBD-02 grep gate `keyboardVerticalOffset` count in `src/` == 0.
- tsc baseline not regressed beyond Phase 13's 8 known pre-existing failures.
- One atomic commit on `main` branch.
- Zero HomeScreen.tsx changes.
- Zero new npm dependencies (verified by `git diff HEAD~1 HEAD package.json` showing no diff).
</success_criteria>

<output>
After completion, create `.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-01-SUMMARY.md` capturing:
- Files shipped (full list)
- **Wave-0 Modal-probe outcome** (PASS or FAIL with verbatim test output) — this is load-bearing for Plan 14-03 test strategy.
- i18n keys added (the 10 in the Plan 14-01 subset)
- Gate results (jest exit, KBD-02 count, parity exit, tsc error count)
- Commit SHA
- Any deviations or surprises for Plan 14-02 / 14-03 to consume.
</output>
