---
phase: 14
plan_id: 14-03
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - src/components/filters/GuidedFilterSheet.tsx
  - src/components/filters/__tests__/GuidedFilterSheet.test.tsx
  - src/screens/HomeScreen.tsx
  - src/locales/en.ts
  - src/locales/ru.ts
autonomous: true
requirements: [FILT-01, FILT-03]
tags: [filter-guided, modal-sheet, animated-slide, m6, phase-anchor]

must_haves:
  # PHASE-LEVEL goal anchor — Plan 14-03 carries the phase verification mass.
  # Derived from ROADMAP.md §Phase 14 Success Criteria 1-5.
  truths:
    # Cross-cutting invariants (appear across all 3 plans):
    - "No hex color literals in src/components/filters/**; all colors via useTheme()"
    - "EN+RU parity enforced via scripts/check-i18n-parity.sh after every commit touching src/locales/"
    - "KBD-02 invariant: zero keyboardVerticalOffset in src/"
    - "Test convention: __tests__/*.test.tsx subdirectories (NOT flat sibling)"
    - "No new dependencies; all capabilities in current package set"
    - "Subagent CWD-drift: prepend cd to every Bash; verify git branch before commit"
    # Phase 14 outcome truths (ROADMAP SC1-SC5):
    - "Tapping HomeScreen filter button when filterStyle === 'guided' opens GuidedFilterSheet bottom-sheet (SC1)"
    - "Tapping HomeScreen filter button when filterStyle === 'cascading' opens CascadingFilter inline (SC2)"
    - "Switching filterStyle live-swaps the next filter-button press without app restart (SC3)"
    - "Both variants share {transactionType, selectedCategory, types} state — selections persist across switches (SC4)"
    - "ShowButton count display = filteredProperties.length (SC4 — same memoized count both variants reference)"
  artifacts:
    - path: "src/components/filters/GuidedFilterSheet.tsx"
      provides: "FILT-01 hand-rolled Modal + Animated.View slide-up bottom-sheet wizard. 1-2-3 stepper Deal->Category->Type, auto-advance, multi-select TypeGrid, footer breadcrumb + ShowButton."
      exports: ["default"]
    - path: "src/components/filters/__tests__/GuidedFilterSheet.test.tsx"
      provides: "Component test covering: renders nothing when open=false, opens with localOpen=true, auto-advance step 0->1->2, re-pick category clears types, tap-scrim fires onClose, ShowButton press fires onClose."
    - path: "src/screens/HomeScreen.tsx"
      provides: "Mounts <GuidedFilterSheet /> behind filterStyle==='guided' alongside Cascading mount. Both variants share state."
    - path: "src/locales/en.ts"
      provides: "Plan 14-03 i18n subset — Deal blurbs, Category prompt + blurbs, Type prompt"
    - path: "src/locales/ru.ts"
      provides: "Plan 14-03 i18n subset Russian translations (parity with en.ts)"
  key_links:
    - from: "src/screens/HomeScreen.tsx"
      to: "src/components/filters/GuidedFilterSheet.tsx"
      via: "conditional mount with filterStyle === 'guided' gate"
      pattern: "<GuidedFilterSheet "
    - from: "src/components/filters/GuidedFilterSheet.tsx"
      to: "src/components/filters/primitives/Stepper.tsx (Plan 14-01)"
      via: "header stepper bar"
      pattern: "from '\\./primitives/Stepper'"
    - from: "src/components/filters/GuidedFilterSheet.tsx"
      to: "src/components/filters/primitives/ShowButton.tsx (Plan 14-01)"
      via: "footer CTA"
      pattern: "from '\\./primitives/ShowButton'"
    - from: "src/components/filters/GuidedFilterSheet.tsx"
      to: "src/components/filters/primitives/Breadcrumb.tsx (Plan 14-01)"
      via: "footer selection trail"
      pattern: "from '\\./primitives/Breadcrumb'"
    - from: "src/components/filters/GuidedFilterSheet.tsx"
      to: "react-native Modal + Animated"
      via: "load-bearing localOpen shadow + Animated.parallel slide"
      pattern: "localOpen|Animated.parallel"
---

<objective>
Ship `<GuidedFilterSheet>` (hand-rolled Modal + Animated.View slide-up bottom sheet) and wire HomeScreen variant dispatch — closing FILT-01 and FILT-03. This plan carries the phase-level goal anchor (ROADMAP §Phase 14 SC1-SC5).

**Phase Goal (ROADMAP):** Two interchangeable filter UIs render on top of Phase 13's shared data model, and the HomeScreen filter button launches the variant matching the user's `filterStyle` preference — switching the preference in AccountSettings live-swaps the variant on the next filter-button press with no app restart required.

Purpose: Delivers the visible product value of M6 ("variants are the point" — memory `m6-filter-variants-are-the-point.md`). After this plan ships, the user can pick either variant via dev fixture (Phase 15 ships the picker UI). Live-swap is enabled by Phase 13's React Context: setting `filterStyle` triggers HomeScreen rerender → next filter-button press opens new variant.

Output: 1 new component (Modal + Animated, with load-bearing `localOpen` shadow), 1 test (strategy depends on Plan 14-01 Wave-0 Modal-probe outcome), HomeScreen variant dispatch addition, 14-03 i18n subset. One atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-RESEARCH.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-VALIDATION.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-01-SUMMARY.md
@.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-02-SUMMARY.md
@src/screens/HomeScreen.tsx
@src/components/filters/CascadingFilter.tsx
@src/components/filters/primitives/Stepper.tsx
@src/components/filters/primitives/DealToggle.tsx
@src/components/filters/primitives/CheckSquare.tsx
@src/components/filters/primitives/MultiHint.tsx
@src/components/filters/primitives/ShowButton.tsx
@src/components/filters/primitives/Breadcrumb.tsx
@src/components/filters/primitives/TypeIcon.tsx
@src/components/DeleteAccountModal.tsx
@src/utils/propertyCategory.ts
@src/context/FilterStyleContext.tsx
@src/theme/colors.ts

<interfaces>
<!-- Plan 14-01 + 14-02 primitives already shipped. Compose only. -->

From src/components/filters/primitives/Stepper.tsx:
```
export default function Stepper(props: {
  step: 0 | 1 | 2;
  onStepPress: (i: number) => void;
  reached: (i: number) => boolean;
}): JSX.Element;
```

From src/components/filters/primitives/ShowButton.tsx:
```
export default function ShowButton(props: { count: number; onPress: () => void }): JSX.Element;
```

From src/components/filters/primitives/Breadcrumb.tsx:
```
export default function Breadcrumb(props: {
  deal: 'Rent' | 'Buy' | null;
  category: PropertyCategory | null;
  types: string[];
}): JSX.Element;
```

From src/components/filters/primitives/CheckSquare.tsx + TypeIcon.tsx + MultiHint.tsx — see Plan 14-01.

From src/screens/HomeScreen.tsx (state surface after Plan 14-02):
```
transactionType: 'rent' | 'sale';            // setter: setTransactionType
selectedCategory: PropertyCategory;          // setter: setSelectedCategory
types: string[];                             // setter: setTypes
isFiltersExpanded: boolean;                  // setter: setIsFiltersExpanded
filteredProperties: Property[];              // existing useMemo; pass .length as liveCount
filterStyle: 'guided' | 'cascading' | 'master' | 'sentence';  // from useFilterStyle() — Plan 14-02 added the hook call
```
</interfaces>

<wave_0_outcome>
<!-- LOAD-BEARING: Plan 14-01 Task 1 ModalProbe.test.tsx outcome determines test strategy below. -->

Read `.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-01-SUMMARY.md` for the Wave-0 probe outcome:

- **If probe PASSED** (react-test-renderer mounted `<Modal>` without crashing): Plan 14-03's test renders the full `<GuidedFilterSheet>` Modal in TestRenderer.create(...) and asserts on tree structure via `tree.root.findByType(Modal)` / `findAllByProps({ accessibilityLabel: ... })`. Default path.

- **If probe FAILED** (Modal threw in react-test-renderer): Plan 14-03 extracts an inner-component `<GuidedFilterSheetContent>` containing the Stepper/ScrollView/Footer without the Modal wrapper, exports it as a NAMED export alongside the default `GuidedFilterSheet`, and the test imports `GuidedFilterSheetContent` directly. The default `GuidedFilterSheet` still wraps `GuidedFilterSheetContent` in `<Modal>` for production rendering.

This conditional is encoded into Task 2's `<action>` block.
</wave_0_outcome>

<animation_pattern>
<!-- LOAD-BEARING: copy verbatim into GuidedFilterSheet.tsx. Without the localOpen shadow, slide-out animation never plays. -->

The Modal + Animated.View slide-up pattern uses a shadow `localOpen` state to delay unmount until the slide-out animation completes. Reference: 14-PATTERNS.md §"`localOpen` shadow + Animated.parallel slide pattern" (which copies RESEARCH.md Pattern 1 lines 217-308).

**Skeleton (Task 2 must implement this VERBATIM, adapted to project token names):**

```
const SHEET_HEIGHT = Dimensions.get('window').height * 0.82;
const SLIDE_IN_MS = 300;
const SLIDE_OUT_MS = 250;

const translateY = useRef(new Animated.Value(SHEET_HEIGHT)).current;
const scrimOpacity = useRef(new Animated.Value(0)).current;
const [localOpen, setLocalOpen] = useState(open);

useEffect(() => {
  if (open) {
    setLocalOpen(true);
    Animated.parallel([
      Animated.timing(translateY, { toValue: 0, duration: SLIDE_IN_MS, easing: Easing.out(Easing.cubic), useNativeDriver: true }),
      Animated.timing(scrimOpacity, { toValue: 1, duration: SLIDE_IN_MS, easing: Easing.out(Easing.cubic), useNativeDriver: true }),
    ]).start();
  } else if (localOpen) {
    Animated.parallel([
      Animated.timing(translateY, { toValue: SHEET_HEIGHT, duration: SLIDE_OUT_MS, easing: Easing.in(Easing.cubic), useNativeDriver: true }),
      Animated.timing(scrimOpacity, { toValue: 0, duration: SLIDE_OUT_MS, easing: Easing.in(Easing.cubic), useNativeDriver: true }),
    ]).start(({ finished }) => {
      if (finished) setLocalOpen(false);  // unmount AFTER slide-out completes
    });
  }
}, [open]);

return (
  <Modal
    visible={localOpen}                  // localOpen — NOT raw `open` prop (load-bearing)
    transparent
    animationType="none"                  // hand-driven Animated.View slide
    onRequestClose={onClose}              // REQUIRED on Android
    statusBarTranslucent
  >
    <Animated.View style={[StyleSheet.absoluteFill, { opacity: scrimOpacity }]}>
      <Pressable
        style={[StyleSheet.absoluteFill, { backgroundColor: colors.scrim }]}
        onPress={onClose}
      />
    </Animated.View>
    <Animated.View style={[styles.sheet, { backgroundColor: colors.background, transform: [{ translateY }] }]}>
      {/* Drag handle, Header, Stepper, ScrollView body, Footer */}
    </Animated.View>
  </Modal>
);
```
</animation_pattern>
</context>

<tasks>

<task id="14-03-01" type="auto">
  <name>Task 1: Add Plan 14-03 i18n subset to en.ts + ru.ts (FILT-01)</name>
  <files>src/locales/en.ts, src/locales/ru.ts</files>
  <read_first>
    - src/locales/en.ts (existing structure + Plan 14-01 + 14-02 additions)
    - src/locales/ru.ts (matching structure)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md §"Copywriting Contract" (full key table)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-01-SUMMARY.md (keys shipped by Plan 14-01)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-02-SUMMARY.md (keys shipped by Plan 14-02)
  </read_first>
  <action>
  Add the **Plan 14-03 subset** of `filters.*` keys to BOTH `src/locales/en.ts` and `src/locales/ru.ts`. Verify against prior SUMMARYs that none are duplicates.

  **Plan 14-03 subset** (Guided-sheet-owned strings per UI-SPEC §Component Inventory):

  | Key | EN | RU |
  |-----|----|----|
  | `filters.deal.rentBlurb` | Lease a place month-to-month | Снять помесячно |
  | `filters.deal.buyBlurb` | Purchase a property to own | Купить в собственность |
  | `filters.category.prompt` | What kind of property? | Что за недвижимость? |
  | `filters.category.residentialBlurb` | Homes to live in | Жильё |
  | `filters.category.commercialBlurb` | Offices & retail | Офисы и торговля |
  | `filters.category.hospitalityBlurb` | Stays & lodging | Размещение и проживание |
  | `filters.type.prompt` | Pick a type | Выберите тип |

  Insert with inline comment block: `// Phase 14 Plan 14-03 — Guided sheet wizard (FILT-01) — D-07 namespace subset`. Order: preserve same key order in en.ts and ru.ts.
  </action>
  <acceptance_criteria>
    - 7 new keys present in en.ts: `cd "$(git rev-parse --show-toplevel)" && grep -c "^  'filters\\.deal\\.rentBlurb'\\|^  'filters\\.deal\\.buyBlurb'\\|^  'filters\\.category\\.prompt'\\|^  'filters\\.category\\.residentialBlurb'\\|^  'filters\\.category\\.commercialBlurb'\\|^  'filters\\.category\\.hospitalityBlurb'\\|^  'filters\\.type\\.prompt'" src/locales/en.ts` >= 7.
    - Same 7 keys in ru.ts.
    - EN+RU parity gate: `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` exits 0.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` exits 0.
  </verification>
</task>

<task id="14-03-02" type="auto" tdd="true">
  <name>Task 2: Build &lt;GuidedFilterSheet&gt; with localOpen shadow + co-located test (FILT-01)</name>
  <files>src/components/filters/GuidedFilterSheet.tsx, src/components/filters/__tests__/GuidedFilterSheet.test.tsx</files>
  <read_first>
    - src/components/DeleteAccountModal.tsx (Modal-shape precedent, lines 1-12 imports + 75-81 Modal-prop trio)
    - src/components/filters/primitives/Stepper.tsx (props + behavior)
    - src/components/filters/primitives/ShowButton.tsx (props + always-enabled-at-0 semantic)
    - src/components/filters/primitives/Breadcrumb.tsx (props + collapse rule)
    - src/components/filters/primitives/CheckSquare.tsx (multi-select cue)
    - src/components/filters/primitives/TypeIcon.tsx (per-type icon)
    - src/components/filters/primitives/MultiHint.tsx (used in TypeGrid prompt)
    - src/components/filters/primitives/joinTypes.ts (breadcrumb collapse may delegate)
    - src/utils/propertyCategory.ts (RESIDENTIAL/COMMERCIAL/HOSPITALITY arrays for TypeGrid)
    - src/components/__tests__/EmailVerifyBanner.test.tsx (test pattern + findByLabel helper)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md §"GuidedFilterSheet.tsx" (full pattern including imports, Modal-prop trio, localOpen + Animated.parallel skeleton)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md §Layout §"GuidedFilterSheet anatomy" (ASCII layout + exact spacing/typography values for each section)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md §"Motion / Animation" (300ms slide-in / 250ms slide-out / scrim parallel)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md §D-09 (stepper reached(i) semantics — re-pick category clears types)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-01-SUMMARY.md §"Wave-0 Modal-probe outcome" (CONDITIONAL — determines test strategy)
  </read_first>
  <behavior>
    - Renders nothing in the React tree when `open === false` AND no animation is in-flight (i.e. `localOpen === false`).
    - When `open` flips false→true: `localOpen` flips true, scrim opacity animates 0→1 (300ms), sheet translateY animates SHEET_HEIGHT→0 (300ms parallel), both `Easing.out(Easing.cubic)`, `useNativeDriver: true`.
    - When `open` flips true→false: parallel reverse animation (250ms `Easing.in(Easing.cubic)`); on completion, `localOpen` flips false to unmount the Modal.
    - Stepper shows 3 pills (Deal, Category, Type) with reached state per CONTEXT.md D-09: step 0 always reached; step 1 reached iff `transactionType` is set (always true given props start with 'rent'|'sale'); step 2 reached iff `selectedCategory` is set (always true given props default 'Residential'). Internal `step: 0|1|2` state. Auto-advance: picking Deal advances to 1; picking Category advances to 2.
    - **Step 0 (Deal):** Renders 2 big cards (Rent + Buy) with icons + label + blurb. Tapping a card calls `setTransactionType` and advances step to 1.
    - **Step 1 (Category):** Renders prompt `t('filters.category.prompt')` + 3 big cards (Residential + Commercial + Hospitality) with icons + label + blurb. Tapping a card calls `setSelectedCategory` AND `setTypes([])` (load-bearing — handoff line 137 + CONTEXT.md D-09) and advances step to 2.
    - **Step 2 (Type):** Renders `<MultiHint />` + prompt + 2-column TypeGrid. Grid items from per-category arrays (4 for Residential, 4 for Commercial, 2 for Hospitality). Each type card shows CheckSquare + TypeIcon + label. Tapping toggles `types` OR-set semantics (`setTypes(prev => prev.includes(t) ? prev.filter(x => x !== t) : [...prev, t])`).
    - **Header:** "Filters" title (serif 22/600) + X close button (Lucide `X` icon, 36pt + 4 hitSlop). Close button fires `onClose`.
    - **Stepper:** uses `<Stepper>` primitive. Clicking a reached earlier step jumps back without clearing later selections (re-pick category in the body clears, but tapping the Category step pill does NOT — handoff line 137 distinction).
    - **Footer (fixed):** `<Breadcrumb>` row (deal/category/types collapsed) + `<ShowButton count={liveCount} onPress={onClose} />`. ShowButton always enabled.
    - **Scrim:** `<Pressable>` covering full screen, `backgroundColor: colors.scrim`. Press fires `onClose`.
  </behavior>
  <action>
  Create `src/components/filters/GuidedFilterSheet.tsx`.

  **Wave-0 conditional (BEFORE writing code):**
  Read `.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-01-SUMMARY.md` §"Wave-0 Modal-probe outcome".
  - If PASSED: write `GuidedFilterSheet` as a single default-exported component (Modal wraps the entire body in one file). Test renders the full component via `TestRenderer.create(<GuidedFilterSheet open={true} ...props />)`.
  - If FAILED: refactor — extract a NAMED export `GuidedFilterSheetContent: React.FC<Omit<Props,'open'|'onClose'> & {step, setStep}>` containing Stepper + ScrollView body + Footer (no Modal, no Animated wrapper). The default `GuidedFilterSheet` component composes the Modal + Animated wrappers + `<GuidedFilterSheetContent ... />` inside. Test imports `{ GuidedFilterSheetContent }` and renders it directly. Document the choice in component JSDoc + commit message.

  **Props interface:**
  ```
  type Props = {
    open: boolean;
    onClose: () => void;
    transactionType: 'rent' | 'sale';
    setTransactionType: (v: 'rent' | 'sale') => void;
    selectedCategory: PropertyCategory;
    setSelectedCategory: (c: PropertyCategory) => void;
    types: string[];
    setTypes: (next: string[] | ((prev: string[]) => string[])) => void;
    liveCount: number;
  }
  ```

  **Imports:** `React, { useEffect, useRef, useState, useMemo }`; `View, Text, Modal, StyleSheet, Pressable, ScrollView, Animated, Easing, Dimensions` from `react-native`; `X, Building, Briefcase, Hotel, type LucideIcon` from `lucide-react-native` (for Deal-card + Category-card big-icon glyphs — Rent uses `Building`, Buy uses `Briefcase` or per UI-SPEC; Category icons use 14-PATTERNS list); `useTheme` from `'../../theme/ThemeContext'`; `useLanguage` from `'../../context/LanguageContext'`; `RESIDENTIAL_TYPES, COMMERCIAL_TYPES, HOSPITALITY_TYPES, type PropertyCategory, type PropertyType` from `'../../utils/propertyCategory'`; `Stepper` from `'./primitives/Stepper'`; `ShowButton` from `'./primitives/ShowButton'`; `Breadcrumb` from `'./primitives/Breadcrumb'`; `CheckSquare` from `'./primitives/CheckSquare'`; `TypeIcon` from `'./primitives/TypeIcon'`; `MultiHint` from `'./primitives/MultiHint'`.

  **Body — implement the localOpen shadow + Animated.parallel skeleton from `<animation_pattern>` block above (in <context>) VERBATIM.** This is non-negotiable per CONTEXT.md D-01 + RESEARCH.md "load-bearing trick" + UI-SPEC §Motion anti-pattern 3.

  **Layout per 14-UI-SPEC §Layout §"GuidedFilterSheet anatomy":**
  - Sheet: `position: 'absolute', bottom: 0, left: 0, right: 0, maxHeight: '82%', borderTopLeftRadius: 32, borderTopRightRadius: 32, backgroundColor: colors.background`.
  - Drag handle View: `width: 36, height: 4, borderRadius: 2, marginTop: 10, marginBottom: 8, alignSelf: 'center', backgroundColor: colors.surface2, accessible: false`.
  - Header row: `paddingHorizontal: 22, paddingVertical: 16, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: colors.border`. Title `<Text>` per UI-SPEC §Typography "Display (serif count)": `fontSize: 22, fontWeight: '600', color: colors.text, fontFamily: Platform.select({ ios: 'Georgia', android: 'serif' })`. X button: 36pt visual + `hitSlop: { top: 4, bottom: 4, left: 4, right: 4 }`, `accessibilityLabel: t('filters.close')`.
  - Stepper container: `paddingHorizontal: 22, paddingTop: 8, paddingBottom: 16`. Pass `step`, `onStepPress: (i) => setStep(i as 0|1|2)`, `reached: (i) => i === 0 || (i === 1 && true) || (i === 2 && true)` (per `<behavior>` — both deal and category always have a value).
  - ScrollView body: `contentContainerStyle: { paddingHorizontal: 22, paddingBottom: 8, gap: 12 }`. Conditional `step === 0 && <DealCards />`, `step === 1 && <CategoryCards />`, `step === 2 && <TypeGrid />`.
  - Footer: `paddingHorizontal: 22, paddingTop: 12, paddingBottom: 26, borderTopWidth: StyleSheet.hairlineWidth, borderTopColor: colors.border`. Contains `<Breadcrumb deal={transactionType === 'rent' ? 'Rent' : 'Buy'} category={selectedCategory} types={types} />` (marginBottom 12) + `<ShowButton count={liveCount} onPress={onClose} />`.

  **DealCards (inline component or inline JSX):** 2 big cards (`paddingVertical: 18, paddingHorizontal: 20, borderRadius: 18`). Default `backgroundColor: colors.surface`. Active (when `transactionType` matches): `backgroundColor: colors.accentSoft, borderWidth: 1.5, borderColor: colors.accentLine`. Each card: 48pt icon-chip (square `borderRadius: 14, backgroundColor: colors.surface2` default OR `colors.accent` active; icon color `colors.iconChipFg` default OR `#fff` active) + 16pt gap + flex column (label `fontSize: 18, fontWeight: '700', color: colors.text` + blurb `fontSize: 13.5, fontWeight: '600', color: colors.textSecondary`). Card 1: Rent — icon `Building`, label `t('filters.deal.rent')`, blurb `t('filters.deal.rentBlurb')`, onPress: `setTransactionType('rent'); setStep(1);`. Card 2: Buy — icon `Briefcase`, label `t('filters.deal.buy')`, blurb `t('filters.deal.buyBlurb')`, onPress: `setTransactionType('sale'); setStep(1);`.

  **CategoryCards:** prompt `<Text>` at `fontSize: 13.5, color: colors.textSecondary, marginBottom: 8` rendering `t('filters.category.prompt')`. Then 3 cards same anatomy as DealCards: Residential (icon `Building`, blurb residentialBlurb), Commercial (icon `Briefcase`, blurb commercialBlurb), Hospitality (icon `Hotel`, blurb hospitalityBlurb). Each onPress: `setSelectedCategory(cat); setTypes([]); setStep(2);`. Active state when `selectedCategory === cat`.

  **TypeGrid:** `<MultiHint />` + prompt `<Text>` rendering `t('filters.type.prompt')`. Then a 2-column flex grid: container `flexDirection: 'row', flexWrap: 'wrap', gap: 10`. Compute `const chipTypes = selectedCategory === 'Residential' ? RESIDENTIAL_TYPES : selectedCategory === 'Commercial' ? COMMERCIAL_TYPES : HOSPITALITY_TYPES;`. Each card: `width: '47%', padding: 14, borderRadius: 14, flexDirection: 'column', gap: 10`. Default: `backgroundColor: colors.surface`. Active (when `types.includes(t)`): `backgroundColor: colors.accentSoft, borderWidth: 1.5, borderColor: colors.accentLine`. Card content: top row `<CheckSquare checked={types.includes(t)} />` + `<TypeIcon type={t as PropertyType} size={19} color={types.includes(t) ? colors.accent : colors.iconChipFg} />`, then label `<Text>{t}</Text>` at `fontSize: 15, fontWeight: '600', color: colors.text`. onPress: OR-set toggle. accessibilityRole='button', accessibilityState={{ selected: types.includes(t) }}, hitSlop 4.

  **Test file** — `src/components/filters/__tests__/GuidedFilterSheet.test.tsx`. Header per EmailVerifyBanner.test.tsx convention + the animation testing caveat note from UI-SPEC §Motion ("react-test-renderer fires Animated callbacks synchronously — assert only initial + final state").

  Strategy depends on Plan 14-01 SUMMARY:
  - PASSED branch: import default `GuidedFilterSheet`; render with `TestRenderer.create(<GuidedFilterSheet open={true} ...props />)` wrapped in `act()`.
  - FAILED branch: import `{ GuidedFilterSheetContent }`; render directly without Modal wrapper.

  Test cases (6):
  1. **renders nothing when open=false initially** — assert `tree.toJSON()` is null OR Modal.visible is false.
  2. **opens with localOpen=true when open=true** — assert Modal.visible is true.
  3. **stepper auto-advance: picking Deal-card calls setTransactionType + advances step to 1** — render at `step=0`, walk to Rent card, fire onPress, assert `setTransactionType('rent')` was called and step state became 1 (test internal state via re-render assertion or by finding the next-step children).
  4. **re-picking Category clears types** (regression guard for Pitfall 4) — render at `step=1` with `types=['Apartment']`, walk to Commercial card, fire onPress, assert `setTypes([])` was called.
  5. **tap-scrim fires onClose** — fire scrim Pressable's onPress, assert `onClose` was called.
  6. **ShowButton tap fires onClose** — fire ShowButton's onPress (via accessibilityLabel walk), assert `onClose` was called.

  Mock Stepper, ShowButton, Breadcrumb, CheckSquare, TypeIcon, MultiHint with shallow stubs via `jest.mock(...)` to keep the test focused on GuidedFilterSheet's own logic.
  </action>
  <acceptance_criteria>
    - File `src/components/filters/GuidedFilterSheet.tsx` exists with default export `GuidedFilterSheet`.
    - File `src/components/filters/__tests__/GuidedFilterSheet.test.tsx` exists.
    - Source assertion (load-bearing localOpen shadow): `cd "$(git rev-parse --show-toplevel)" && grep -c "localOpen" src/components/filters/GuidedFilterSheet.tsx` >= 3 (state declaration + Modal `visible={localOpen}` + setLocalOpen callbacks).
    - Source assertion (load-bearing Animated.parallel): `cd "$(git rev-parse --show-toplevel)" && grep -c "Animated.parallel" src/components/filters/GuidedFilterSheet.tsx` >= 2 (slide-in + slide-out).
    - Source assertion (Modal animationType="none" — anti-pattern guard): `cd "$(git rev-parse --show-toplevel)" && grep -c 'animationType="none"' src/components/filters/GuidedFilterSheet.tsx` == 1.
    - Source assertion (Modal animationType is NOT slide/fade): `cd "$(git rev-parse --show-toplevel)" && grep -cE 'animationType="(slide|fade)"' src/components/filters/GuidedFilterSheet.tsx` == 0.
    - Source assertion (onRequestClose present — Android back button): `cd "$(git rev-parse --show-toplevel)" && grep -c "onRequestClose" src/components/filters/GuidedFilterSheet.tsx` >= 1.
    - Source assertion (load-bearing setTypes([]) on category change): `cd "$(git rev-parse --show-toplevel)" && grep -c "setTypes(\\[\\])" src/components/filters/GuidedFilterSheet.tsx` >= 1.
    - Source assertion (uses Plan 14-01 primitives): `cd "$(git rev-parse --show-toplevel)" && grep -c "from './primitives/Stepper'" src/components/filters/GuidedFilterSheet.tsx` >= 1 AND `grep -c "from './primitives/ShowButton'" src/components/filters/GuidedFilterSheet.tsx` >= 1 AND `grep -c "from './primitives/Breadcrumb'" src/components/filters/GuidedFilterSheet.tsx` >= 1.
    - Source assertion (no hex literals except permitted #fff/#000): `cd "$(git rev-parse --show-toplevel)" && grep -nE "#[0-9a-fA-F]{3,6}" src/components/filters/GuidedFilterSheet.tsx | grep -viE "'#fff'|'#FFFFFF'|'#000'" | wc -l` == 0.
    - Source assertion (KBD-02 — no keyboardVerticalOffset): `cd "$(git rev-parse --show-toplevel)" && grep -c "keyboardVerticalOffset" src/components/filters/GuidedFilterSheet.tsx` == 0.
    - Test command: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/__tests__/GuidedFilterSheet.test.tsx` exits 0. All 6 test cases pass.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/__tests__/GuidedFilterSheet.test.tsx --silent` exits 0.
    Source assertion (localOpen pattern present): `cd "$(git rev-parse --show-toplevel)" && grep -c "localOpen\\|setLocalOpen" src/components/filters/GuidedFilterSheet.tsx` >= 4.
  </verification>
</task>

<task id="14-03-03" type="auto">
  <name>Task 3: HomeScreen variant dispatch — add &lt;GuidedFilterSheet /&gt; mount alongside CascadingFilter (FILT-03)</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (FULL file — post-Plan-14-02; especially CascadingFilter mount position, imports, useFilterStyle hook call)
    - src/components/filters/GuidedFilterSheet.tsx (default export shape from Task 2)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-PATTERNS.md §"HomeScreen.tsx (Plan 14-03 edit)" (verbatim conditional mount + imports)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md §D-05 (full conditional render pattern)
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-UI-SPEC.md §"HomeScreen variant dispatch placement" (placement note: GuidedFilterSheet mounts at HomeScreen JSX root alongside other Modals; CascadingFilter mounts inline)
  </read_first>
  <action>
  Surgical edit on `src/screens/HomeScreen.tsx` (single change):

  **Step 1 — Add import** (alongside the `CascadingFilter` import from Plan 14-02):
  - `import GuidedFilterSheet from '../components/filters/GuidedFilterSheet';`

  **Step 2 — Add conditional mount.** Per CONTEXT.md D-05 and UI-SPEC §"HomeScreen variant dispatch placement", `<GuidedFilterSheet>` mounts at the HomeScreen JSX root (alongside any other top-level Modals) rather than inline. Find a sensible position near the existing CascadingFilter mount (or at the bottom of the main JSX before SafeAreaView/screen close). The conditional gate:

  ```
  {filterStyle === 'guided' && (
    <GuidedFilterSheet
      open={isFiltersExpanded}
      onClose={() => setIsFiltersExpanded(false)}
      transactionType={transactionType}
      setTransactionType={setTransactionType}
      selectedCategory={selectedCategory}
      setSelectedCategory={setSelectedCategory}
      types={types}
      setTypes={setTypes}
      liveCount={filteredProperties.length}
    />
  )}
  ```

  Both variants now share the same state props — `selectedCategory={selectedCategory}` appears in BOTH conditional mounts, proving SC4 ("Both variants share state").

  **Step 3 — Filter button left untouched.** The button at lines ~492-506 already paints `colors.accent` when `isFiltersExpanded`. No code change needed; D-13 says both variants use the same filter-button visual state.

  **Step 4 — Verify tsc + variant-share grep.** Run `npx tsc --noEmit 2>&1 | grep -c "error TS"` and confirm baseline preserved (≤ 8). Confirm both variants share state via `grep -c "selectedCategory={selectedCategory}" src/screens/HomeScreen.tsx` >= 2 (one occurrence per variant mount).
  </action>
  <acceptance_criteria>
    - Source assertion (`<GuidedFilterSheet />` mounted exactly once): `cd "$(git rev-parse --show-toplevel)" && grep -nE "<GuidedFilterSheet " src/screens/HomeScreen.tsx | wc -l` == 1.
    - Source assertion (filterStyle === 'guided' gate present): `cd "$(git rev-parse --show-toplevel)" && grep -nE "filterStyle === 'guided'" src/screens/HomeScreen.tsx | wc -l` >= 1.
    - Source assertion (GuidedFilterSheet imported): `cd "$(git rev-parse --show-toplevel)" && grep -c "from '../components/filters/GuidedFilterSheet'" src/screens/HomeScreen.tsx` == 1.
    - **State sharing assertion (SC4):** `cd "$(git rev-parse --show-toplevel)" && grep -c "selectedCategory={selectedCategory}" src/screens/HomeScreen.tsx` >= 2 (both variants receive the same state prop).
    - **State sharing assertion (SC4):** `cd "$(git rev-parse --show-toplevel)" && grep -c "types={types}" src/screens/HomeScreen.tsx` >= 2.
    - **State sharing assertion (SC4):** `cd "$(git rev-parse --show-toplevel)" && grep -c "liveCount={filteredProperties.length}" src/screens/HomeScreen.tsx` >= 2.
    - CascadingFilter mount preserved from Plan 14-02: `cd "$(git rev-parse --show-toplevel)" && grep -nE "<CascadingFilter " src/screens/HomeScreen.tsx | wc -l` == 1.
    - tsc baseline: `cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -c "error TS"` <= 8.
    - KBD-02 invariant: `cd "$(git rev-parse --show-toplevel)" && grep -rn "keyboardVerticalOffset" src/ | wc -l` == 0.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -c "error TS"` <= 8.
    Source assertion: both mounts present (`<GuidedFilterSheet ` and `<CascadingFilter ` each appear exactly once).
  </verification>
</task>

<task id="14-03-04" type="auto">
  <name>Task 4: Full-suite gates + atomic commit on main — phase verification anchor</name>
  <files>(no source edits — gate run + commit)</files>
  <read_first>
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-CONTEXT.md §"Gate Commands"
    - .planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-VALIDATION.md §"Manual-Only Verifications"
    - Memory `subagent-cwd-drift-recurring.md`
  </read_first>
  <action>
  CWD-drift mitigation: prepend `cd "$(git rev-parse --show-toplevel)" && ` to EVERY Bash command. Verify `git branch --show-current` outputs `main` before commit.

  **Phase-anchor gate suite (this is the LAST automated checkpoint before Phase 14 VERIFICATION/UAT):**

  1. **Full filter subtree tests:** `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/ --silent` exits 0.
  2. **Full test suite spot-check** (catch regressions): `cd "$(git rev-parse --show-toplevel)" && npx jest --silent 2>&1 | tail -10` — all green or no new failures beyond baseline. (Project does not enforce a hard global gate per VALIDATION.md, but record the tail output in SUMMARY.)
  3. **KBD-02 invariant:** `cd "$(git rev-parse --show-toplevel)" && grep -rn "keyboardVerticalOffset" src/ | wc -l` == 0.
  4. **EN+RU parity:** `cd "$(git rev-parse --show-toplevel)" && bash scripts/check-i18n-parity.sh` exits 0.
  5. **tsc baseline:** `cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -c "error TS"` <= 8.
  6. **Phase 14 SC1 source assertion:** `cd "$(git rev-parse --show-toplevel)" && grep -nE "<GuidedFilterSheet " src/screens/HomeScreen.tsx | wc -l` == 1.
  7. **Phase 14 SC2 source assertion:** `cd "$(git rev-parse --show-toplevel)" && grep -nE "<CascadingFilter " src/screens/HomeScreen.tsx | wc -l` == 1.
  8. **Phase 14 SC4 source assertion (state sharing):** `cd "$(git rev-parse --show-toplevel)" && grep -c "selectedCategory={selectedCategory}" src/screens/HomeScreen.tsx` >= 2 AND `cd "$(git rev-parse --show-toplevel)" && grep -c "types={types}" src/screens/HomeScreen.tsx` >= 2.
  9. **Phase 14 SC3 (live-swap) is MANUAL-only** per 14-VALIDATION.md §Manual-Only Verifications. Document in SUMMARY that it requires dev fixture / Phase 15 picker. Record an explicit dev-fixture walk-through if feasible.
  10. **No new dependencies:** `cd "$(git rev-parse --show-toplevel)" && git diff main..HEAD package.json package-lock.json 2>&1 | wc -l` — should be 0 or near-0 (commit hasn't landed yet, so this is "pending working tree shows no dep diff").

  If ALL gates pass:

  11. `cd "$(git rev-parse --show-toplevel)" && git branch --show-current` — must print `main`.
  12. `cd "$(git rev-parse --show-toplevel)" && git add src/components/filters/GuidedFilterSheet.tsx src/components/filters/__tests__/GuidedFilterSheet.test.tsx src/screens/HomeScreen.tsx src/locales/en.ts src/locales/ru.ts`.
  13. Atomic commit on main: `feat(14-03): GuidedFilterSheet + HomeScreen variant dispatch (FILT-01, FILT-03)` body explaining: (a) GuidedFilterSheet shipped with load-bearing localOpen + Animated.parallel slide; (b) HomeScreen dispatches between variants on `filterStyle`; (c) Wave-0 Modal-probe outcome from Plan 14-01 (PASSED → full Modal test / FAILED → inner-component fallback); (d) Phase 14 SC1+SC2+SC4 automated-verified; SC3 manual-only.
  </action>
  <acceptance_criteria>
    - All 10 gates pass.
    - Branch verified: `cd "$(git rev-parse --show-toplevel)" && git branch --show-current` == `main`.
    - Atomic commit lands: `cd "$(git rev-parse --show-toplevel)" && git log -1 --pretty=%s | grep -c "feat(14-03)"` >= 1.
    - Commit touches expected file set: `cd "$(git rev-parse --show-toplevel)" && git diff HEAD~1 HEAD --name-only | sort -u` matches the 5 paths in `files_modified`.
    - No package.json / package-lock.json diff in the commit: `cd "$(git rev-parse --show-toplevel)" && git diff HEAD~1 HEAD --name-only | grep -cE "package(-lock)?\\.json"` == 0.
  </acceptance_criteria>
  <verification>
    Automated: `cd "$(git rev-parse --show-toplevel)" && git log -1 --pretty=%s | grep -c "feat(14-03)"` >= 1.
    Source assertion: both variants mounted; both share state via identical prop expressions.
    KBD-02 invariant: `cd "$(git rev-parse --show-toplevel)" && grep -rn "keyboardVerticalOffset" src/ | wc -l` == 0.
  </verification>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — N/A for Phase 14) | Phase 14-03 ships purely presentational components. No auth, no network, no input parsing, no data persistence beyond the existing Phase 13 `filterStyle` AsyncStorage hook (which is unchanged). Existing HomeScreen filter state shape unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Spoofing | (none) | accept | No identity bearing in Plan 14-03 surface. |
| T-14-14 | Tampering | HomeScreen filter state | accept | State shape and storage unchanged. GuidedFilterSheet is a controlled component calling setters via props. |
| T-14-15 | Repudiation | (none) | accept | No audit-bearing actions; filter selection is per-device transient. |
| T-14-16 | Information Disclosure | liveCount prop | accept | Plain integer derived from `filteredProperties.length` (same value already visible at HomeScreen.tsx:644-646). |
| T-14-17 | Denial of Service | Animated.parallel slide animations | accept | Single 300ms parallel pair on open; single 250ms parallel pair on close. `useNativeDriver: true` keeps work off JS thread. `localOpen` shadow guarantees one start callback per open/close cycle (no accumulation). |
| T-14-18 | Denial of Service | Modal-tree mount/unmount | accept | localOpen ensures Modal mounts ONLY while sheet is open or animating out; unmount happens via setLocalOpen(false) after slide-out completes. No leak surface. |
| T-14-19 | Elevation of Privilege | (none) | accept | No privilege surface. |
</threat_model>

<verification>
- All Phase 14-03 filter tests pass: `cd "$(git rev-parse --show-toplevel)" && npx jest src/components/filters/` exits 0.
- KBD-02 grep gate: 0.
- i18n EN+RU parity: exit 0.
- tsc baseline: <= 8.
- Both variants mounted in HomeScreen with identical state-prop expressions (SC4).
- No new dependencies (`package.json`/`package-lock.json` unchanged in this commit).
- Wave-0 Modal-probe outcome from Plan 14-01 documented in commit body + SUMMARY.
- **MANUAL — phase exit gate:**
  1. (SC1) Set `filterStyle='guided'` via dev fixture, tap HomeScreen filter button on iPhone 15 Pro Max — sheet slides up over 300ms. EN + RU verified.
  2. (SC2) Set `filterStyle='cascading'`, tap filter button — inline panel reveals via LayoutAnimation.
  3. (SC3) Switch the value at runtime (via Phase 15 picker once available; until then, AsyncStorage poke or dev fixture); next filter-button press opens new variant without app restart.
  4. (SC4) Pick filters in Guided sheet, dismiss, switch to Cascading via dev fixture, open — selections still visible in Cascading. Reverse direction also persists.
  5. (SC5) RU text fits in stepper labels + breadcrumb on Moto G XT2513V; ShowButton singular/plural fires correctly across counts 0/1/N.
</verification>

<success_criteria>
- `<GuidedFilterSheet>` shipped at `src/components/filters/GuidedFilterSheet.tsx` with load-bearing `localOpen` shadow + `Animated.parallel` slide-up + co-located test (6 cases).
- HomeScreen mounts `<GuidedFilterSheet />` behind `filterStyle === 'guided'` alongside the existing `<CascadingFilter />` mount from Plan 14-02.
- Both variants receive identical state props (`transactionType`, `selectedCategory`, `types`, `liveCount={filteredProperties.length}` and their setters) — SC4 source-asserted via grep count >= 2 per state-key prop.
- Plan 14-03 i18n subset (7 keys: deal blurbs, category prompt + 3 blurbs, type prompt) shipped to EN + RU with parity gate green.
- Wave-0 Modal-probe outcome from Plan 14-01 honored in test strategy (default path or inner-component fallback) and documented.
- KBD-02 invariant preserved across the phase.
- tsc baseline (≤ 8 errors) preserved.
- One atomic commit on `main`.
- SC3 (live-swap) is manual-only — documented in VERIFICATION/SUMMARY for the post-phase walk-through (and re-tested formally when Phase 15 picker ships).
</success_criteria>

<output>
After completion, create `.planning/phases/14-filter-ui-variants-guided-steps-cascading-reveal-homescreen-/14-03-SUMMARY.md` capturing:
- Files shipped/modified
- Wave-0 Modal-probe outcome honored (PASSED → full-Modal test / FAILED → inner-component fallback) — explain the path taken
- All gate results (jest, KBD-02, parity, tsc, source assertions for SC1/SC2/SC4, package-json non-diff)
- Manual SC3 + SC5 walk-through results (if performed in the same session)
- Commit SHA
- Phase 14 closure recommendation: ready for `/gsd-verify-work` and operator UAT on iPhone 15 Pro Max + Moto G XT2513V × EN/RU × light/dark.

This SUMMARY also drives the Phase 14 VERIFICATION.md when `/gsd-verify-work` runs.
</output>
