---
phase: 02-6-step-contextual-listing-flow-client
plan: 08
type: execute
wave: 6
depends_on: ["02-07"]
files_modified: []
autonomous: false
requirements: [FLOW-01, FLOW-02, FLOW-03, FLOW-04, FLOW-05, FLOW-06, FLOW-07, FLOW-08, FLOW-09, FLOW-10, FLOW-11, FLOW-13, FLOW-15]

must_haves:
  truths:
    - "Operator (or developer) walks the new 6-step flow on a physical device against the cutover backend BEFORE Plan 02-09 deletes the safety net."
    - "All 3 modes verified working: create + edit-owner + edit-mod."
    - "Map pin behavior verified on iPhone 15 Pro Max + Moto G XT2513V — primary draggable path tested; tap-to-move fallback tested separately to confirm both paths work (Pitfall 1 mitigation)."
    - "Mod-context banner verified persisting across Steps 1-6 when in edit-mod mode (FLOW-15)."
    - "Step 6 daily-rent thin step verified rendering only deposit (D-19)."
    - "Phase 5 inputs captured early — any failures here become Phase 5 QA cells, not Phase 2 blockers."
  artifacts: []
  key_links: []
---

<objective>
Verification-only plan — no new code. Operator-supervised dry-run of the new flow on physical devices BEFORE Plan 02-09 atomically deletes the old `CreateListingScreen.tsx`. This is the rehearsal that catches catastrophic-to-fix bugs (e.g. map drag fully broken on Android Fabric — Pitfall 1) while the safety net is still in place.

Per CONTEXT.md D-22 / RESEARCH.md §"Why Plan 02-08 (verify-only)": ROADMAP SC#5 requires the OLD `CreateListingScreen.tsx` to be deleted in the same commit chain that ships the new flow. Plan 02-08 is the rehearsal — operator (or developer) walks the new flow on a physical device against the cutover backend BEFORE Plan 02-09 deletes the safety net. Catches the Pitfall 1 (map drag fail) early.

Per CONTEXT.md D-04: full QA matrix is Phase 5 REL-03's responsibility. Plan 02-08's scope is a rehearsal smoke walk of the happy paths, not the full 15-cell matrix.

This is a **checkpoint plan** — `autonomous: false`. The executor pauses for operator confirmation at the end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-6-step-contextual-listing-flow-client/02-CONTEXT.md
@.planning/phases/02-6-step-contextual-listing-flow-client/02-RESEARCH.md
@.planning/phases/02-6-step-contextual-listing-flow-client/02-VALIDATION.md §"Manual-Only Verifications"
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator dry-run on iPhone 15 Pro Max + Moto G XT2513V</name>
  <what-built>
    Plans 02-01 through 02-07 have shipped. The new `<ContextualListingFlow>` is wired into App.tsx (with `isContextualListingFlowOpen` flag), backend Location dictionary is live on Railway with seeded KG/KZ/UZ cities, and 9 RN client read-path surfaces consume nested shape. Old `<CreateListingScreen.tsx>` is dead code (still imported but never mounts).

    This checkpoint verifies the new flow works end-to-end on physical devices BEFORE Plan 02-09 atomically deletes the old screen.
  </what-built>
  <how-to-verify>
    Operator (or developer) performs the following walks on iPhone 15 Pro Max + Moto G XT2513V (both — Pitfall 1 risk is Android-specific):

    **Walk 1 — mode='create' apartment + rent_long happy path:**
    1. Open app on the device. Login if needed.
    2. Tap "Create listing" (whatever the M2 entry point is — toolbar plus button, etc.).
    3. **Step 1:** Tap "Long-term rent" chip + "Apartment" chip → tap Next.
    4. **Step 2:**
       - Verify city chip row populated with KG/KZ/UZ cities (Bishkek, Osh, etc.).
       - Tap "Bishkek" → district chips appear.
       - Tap a district (e.g. "Asanbay").
       - **Map pin test (load-bearing per Pitfall 1):**
         a. Tap on the map → marker appears at tap location.
         b. Drag the marker to a new position. Lift finger. Verify marker stays in the dragged position (`onDragEnd` fired). Check console log if needed.
         c. **If drag silently fails on Android (marker snaps back):** verify the tap-to-move fallback works — tap a new location on the map → marker moves to the new tap location. Both paths converge on the same `setCoords`.
       - Toggle exact-address — verify it's hidden when you go BACK to Step 1 and switch propertyType to 'hotel' (FLOW-06 cell — toggle disappears + forced true).
       - Switch back to 'apartment'; verify toggle reappears defaulted false.
       - Tap Next.
    5. **Step 3:** Type areaSqm=80, price=1000, tap currency=KGS, tap rooms=2 → tap Next.
    6. **Step 4:** Tap condition=good, tap furnished=Yes → tap Next.
    7. **Step 5:** Type title="Cozy 2BR Bishkek", description="Test listing from Phase 2 dry-run" → tap Next.
    8. **Step 6:** Tap negotiable=Yes, tap prepayment=1, tap minTerm=1_month → tap Submit.
    9. Verify success: app navigates to RenterListings 'pending' tab with the new listing visible.

    **Walk 2 — mode='edit-owner' for a listing created in Walk 1:**
    1. From RenterListings, tap the listing's edit button.
    2. Verify the new flow opens with all fields pre-populated from the saved listing (location, basics, content, terms all match).
    3. Change description text. Tap Submit.
    4. Verify success destination: RenterListings 'pending' tab refreshed.

    **Walk 3 — mode='edit-mod' (requires moderator account):**
    1. Login as moderator.
    2. Navigate to ModerationQueueScreen → tap the pending listing → tap "Edit on behalf" (or whatever the M2 mod-action entry is).
    3. Verify the new flow opens with the mod-context banner ABOVE the step body on Step 1.
    4. Tap Next → verify banner persists on Step 2. Repeat through Step 6 (FLOW-15 / D-18 — banner persists across all steps).
    5. Make a small edit (e.g. change rooms from 2 to 3). Tap Submit.
    6. Verify success destination: returns to ModerationQueueScreen with refreshed counts.

    **Walk 4 — mode='create' rent_daily + hotel (D-19 thin Step 6):**
    1. Open new listing.
    2. Step 1: dealType='Daily rent', propertyType='Hotel'.
    3. Walk Steps 2-5 (verify Step 2 exact-address toggle is HIDDEN — FLOW-06 hospitality forces true).
    4. **Step 6:** Verify ONLY the deposit input renders (no negotiable, no prepayment, no minTerm). Tap Submit immediately (deposit optional — D-19).
    5. Verify success.

    **Walk 5 — Locations tab on ModerationQueueScreen:**
    1. As moderator, open ModerationQueueScreen.
    2. Verify 2-tab segmented control: "Listings (N)" + "Locations (M)".
    3. Tap "Locations" tab. Verify any pending cities/districts (created in Walk 1's "Other" if you used "Other" — otherwise empty state visible).
    4. Switch back to "Listings" tab → verify the existing pending-listings list still functional + scroll position preserved (Pitfall 3 mitigation cell).

    **Failures during dry-run:**
    - Drag pin fails on Android → tap-to-move fallback works → log as Phase 5 QA cell (NOT a Phase 2 blocker per Pitfall 1).
    - Anything else (e.g. submit returns 400, mod banner missing, step-3 sub-fields don't reflow on switch) → STOP. File as Phase 2 blocker. Do NOT proceed to Plan 02-09.
  </how-to-verify>
  <resume-signal>Type "approved — proceed to atomic deletion" to continue to Plan 02-09. If issues found, describe them and STOP — Plan 02-09 must not run until blockers resolved.</resume-signal>
</task>

</tasks>

<threat_model>None — verification only.</threat_model>

<verification>
- All 5 walks completed on both devices.
- Map pin: at least ONE of (draggable, tap-to-move) works on each platform.
- Mod-context banner persists across all 6 steps in edit-mod mode.
- Step 6 daily-rent renders only deposit.
- ModerationQueueScreen tab switching preserves listing-list state (Pitfall 3 mitigation).
- Operator typed "approved — proceed to atomic deletion".
</verification>

<success_criteria>
- 6 must_haves truths observable on physical devices.
- Phase 5 device QA matrix (REL-03) inputs captured: any partial failures (e.g. Android drag) recorded for Phase 5 to expand into full matrix coverage.
- Plan 02-09 unblocked.
</success_criteria>

<output>
.planning/phases/02-6-step-contextual-listing-flow-client/02-08-SUMMARY.md captures:
- Devices walked (iPhone 15 Pro Max — pass/fail per walk; Moto G XT2513V — pass/fail per walk).
- Map pin behavior on each device: drag worked? tap-to-move worked? both? (one of two MUST work; both ideal).
- Phase 5 cells inflated by this dry-run (e.g. "Android drag silently fails — Phase 5 to validate tap-to-move on N more Android devices").
- Operator approval timestamp + signature.
- Any blockers found that prevented "approved" signal — list separately + flag as Phase 2 work-to-do (NOT Plan 02-09).
</output>
