---
phase: 02-6-step-contextual-listing-flow-client
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/HomeScreen.tsx
  - src/screens/FavoritesScreen.tsx
  - src/screens/RenterListingsScreen.tsx
  - src/screens/OwnerListingsScreen.tsx
  - src/components/PropertyCard.tsx
  - src/components/HospitalitySection.tsx
  - src/components/HospitalityCard.tsx
  - src/components/ListingMetaTable.tsx
  - src/components/PropertyMap.tsx
autonomous: true
requirements: [FLOW-13]

must_haves:
  truths:
    - "HomeScreen reads listings via nested shape (location.coordinates, basics.price/currency/areaSqm/rooms/bathroom, dealType !== 'sale' for hospitality strip)."
    - "FavoritesScreen reads via nested shape; hospitality derivation matches HomeScreen."
    - "RenterListingsScreen 4-tab control + edit entry point still functional; reads nested; passes property to edit handler (full nested Property object)."
    - "OwnerListingsScreen mirrors RenterListingsScreen nested-shape swap."
    - "PropertyCard reads basics.price/currency/areaSqm/rooms/bathroom + media.photos[0]."
    - "HospitalityCard reads top-level maxGuests + amenities (preserved per D-21) + media.tourUrl + basics.hotelClass."
    - "HospitalitySection.tsx unchanged — caller-side derivation only (Tradeoff §K): hospitalityProperties = filter(p => p.dealType !== 'sale' && propertyTypeToCategory(p.propertyType) === 'Hospitality')."
    - "ListingMetaTable reads basics.* + conditionAndAmenities.* + terms.*."
    - "PropertyMap reads location.coordinates.{lat,lng} (replacing flat latitude/longitude)."
    - "HomeScreen district filter chips REMAIN HARDCODED (D-10 — M4 owns dynamic-dictionary swap); only the listing-RENDER reads switch to nested."
    - "Existing test files for affected components still pass (any snapshot/light unit tests in __tests__/)."
  artifacts:
    - path: "src/screens/HomeScreen.tsx"
      provides: "Updated to read nested shape; district filter chip source unchanged (D-10)"
    - path: "src/screens/FavoritesScreen.tsx"
      provides: "Updated to read nested shape"
    - path: "src/screens/RenterListingsScreen.tsx"
      provides: "Updated to read nested shape; hospitality + other derivation use dealType per Tradeoff §K"
    - path: "src/screens/OwnerListingsScreen.tsx"
      provides: "Updated to read nested shape"
    - path: "src/components/PropertyCard.tsx"
      provides: "Updated to read basics.* + media.photos[0]"
    - path: "src/components/HospitalityCard.tsx"
      provides: "Updated to read media.tourUrl + basics.hotelClass; preserves top-level maxGuests + amenities (D-21)"
    - path: "src/components/HospitalitySection.tsx"
      provides: "Unchanged (caller-side derivation per Tradeoff §K)"
    - path: "src/components/ListingMetaTable.tsx"
      provides: "Updated to read basics + conditionAndAmenities + terms"
    - path: "src/components/PropertyMap.tsx"
      provides: "Updated to read location.coordinates.lat/lng"
  key_links:
    - from: "HomeScreen + FavoritesScreen + RenterListingsScreen + OwnerListingsScreen"
      to: "PropertyCard + HospitalitySection + HospitalityCard"
      via: "shared card components — single update propagates"
      pattern: "<PropertyCard|<HospitalityCard|<HospitalitySection"
    - from: "HomeScreen hospitalityProperties derivation"
      to: "dealType !== 'sale' filter (Tradeoff §K)"
      via: "useMemo filter on properties array"
      pattern: "dealType !== 'sale'"
    - from: "PropertyMap"
      to: "location.coordinates"
      via: "property.location?.coordinates?.lat / .lng (replacing flat property.latitude / property.longitude)"
      pattern: "location\\?\\.coordinates"
---

<objective>
Read-path cutover for 9 RN client surfaces (M2 list/card screens that currently consume the flat M2 shape and need to switch to the nested Phase-1 shape). This closes the Phase 1 atomic-break window (Pitfall 4): testers' M2 client builds (TestFlight 27 / Play Internal 30) currently see broken Home / PropertyCard etc. because backend writes nested but client reads flat. Shipping this plan + 02-06 BEFORE the new flow lands in App.tsx (Plan 02-07) means testers unblock the day the read paths land.

Per CONTEXT.md D-20: 9 surfaces in scope. Per D-21: top-level `maxGuests` + `amenities[]` STAY at top level (Phase 1 D-09 — preserved verbatim from M1 P6). Per Tradeoff §K: HospitalitySection caller-side derivation rewrites `transactionType: 'rent'` to `dealType !== 'sale'`. Per D-10: HomeScreen district filter chips REMAIN HARDCODED (M4 owns dynamic-dictionary swap).

PropertyDetailsScreen (1680 LOC) is split out into Plan 02-06 (own plan) due to size.

This plan can run in PARALLEL with Plan 02-01 (backend), Plan 02-02 (RN flow skeleton), and Plan 02-06 (PropertyDetailsScreen) — Wave 1 — because none of these touch the same files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-6-step-contextual-listing-flow-client/02-CONTEXT.md
@.planning/phases/02-6-step-contextual-listing-flow-client/02-RESEARCH.md
@.planning/phases/02-6-step-contextual-listing-flow-client/02-PATTERNS.md
@.planning/phases/02-6-step-contextual-listing-flow-client/02-VALIDATION.md
@src/types/Property.ts
@src/screens/HomeScreen.tsx
@src/screens/FavoritesScreen.tsx
@src/screens/RenterListingsScreen.tsx
@src/screens/OwnerListingsScreen.tsx
@src/components/PropertyCard.tsx
@src/components/HospitalityCard.tsx
@src/components/HospitalitySection.tsx
@src/components/ListingMetaTable.tsx
@src/components/PropertyMap.tsx
@src/utils/propertyCategory.ts

<interfaces>
<!-- Field-by-field swap reference (FLAT M2 → NESTED Phase 1, per CONTEXT D-04..D-15 + 02-PATTERNS.md rows 53-64). -->
<!-- Property.ts is the source of truth for nested shape. -->

| Flat M2 (BEFORE) | Nested Phase 1 (AFTER) |
|------------------|-----------------------|
| property.type ('rent' | 'sale') — used in hospitality filter | property.dealType ('sale' | 'rent_long' | 'rent_daily'); filter rule: `dealType !== 'sale'` |
| property.latitude, property.longitude | property.location?.coordinates?.lat, property.location?.coordinates?.lng |
| property.address (string) | property.location?.city + property.location?.district (separate fields; display joined per locale) |
| property.city | property.location?.city (slug; lookup label via cities feed if needed; OR keep showing slug as-is for read paths until M4 ships dynamic dictionary lookup) |
| property.price, property.currency | property.basics?.price, property.basics?.currency |
| property.areaSqm | property.basics?.areaSqm |
| property.bedrooms (M2 name) | property.basics?.rooms (M3 SPEC name; Phase 1 migration mapped) |
| property.bathrooms (count) | property.basics?.bathroom ('private' | 'none' | 'shared'); read sites that displayed "X bathrooms" need adjustment per Phase 1 D-04..D-15 mapping |
| property.condition, property.furnished | property.conditionAndAmenities?.condition, property.conditionAndAmenities?.furnished |
| property.title, property.description | property.content?.title, property.content?.description |
| property.tours[0]?.url | property.media?.tourUrl (Phase 1 D-12 first-tour-wins flatten) |
| property.images[0] | property.media?.photos?.[0] |
| property.maxGuests | property.maxGuests (UNCHANGED — top-level per D-21) |
| property.amenities | property.amenities (UNCHANGED — top-level per D-21) |
| property.hotelClass | property.basics?.hotelClass |
| property.negotiable, property.deposit, property.prepaymentMonths, property.minTerm | property.terms?.* |

<!-- HomeScreen district filter (lines 65-93) STAYS HARDCODED per D-10. Only the listing-RENDER reads switch. -->

<!-- HospitalitySection.tsx (the section wrapper component) — per Tradeoff §K, the COMPONENT is unchanged; the CALLER derives hospitalityProperties using the new dealType-based rule. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PropertyCard.tsx (single shared card — read nested)</name>
  <files>
    - src/components/PropertyCard.tsx (MODIFIED)
  </files>
  <read_first>
    - src/components/PropertyCard.tsx (current state — to find every flat field read; look for property.price, property.currency, property.images, property.bedrooms, etc.)
    - src/types/Property.ts (Phase 1 nested type — single source of truth)
    - .planning/phases/02-6-step-contextual-listing-flow-client/02-PATTERNS.md row 58 (PropertyCard swap mapping)
  </read_first>
  <action>
    Update `src/components/PropertyCard.tsx` to read the nested shape per the field-by-field table in `<interfaces>`. Steps:

    1. Find every read of `property.price`, `property.currency`, `property.areaSqm`, `property.bedrooms`, `property.bathrooms`, `property.images[0]`, `property.title`, `property.description`, `property.address` etc.
    2. Replace per the swap table:
       - `property.price` → `property.basics?.price`
       - `property.currency` → `property.basics?.currency` (now 'KGS' | 'USD' | 'EUR' per D-03; if rendering shows '$'/'сом', may need a small currency-symbol map utility — see Step 4)
       - `property.areaSqm` → `property.basics?.areaSqm`
       - `property.bedrooms` → `property.basics?.rooms` (NOTE: type changes — was number? in M2; now string '1'|'2'|'3'|'4+' in M3. Adjust display)
       - `property.bathrooms` → `property.basics?.bathroom` ('private'|'none'|'shared') OR maintain count display via i18n if the card showed "X bathrooms" (per Phase 1 D-04..D-15 — reconcile with what migrate-listings-m3.js produced)
       - `property.images[0]` → `property.media?.photos?.[0]`
       - `property.title` → `property.content?.title`
       - `property.address` → `property.location?.city + ', ' + property.location?.district` (or whatever existing read pattern was; preserve label-vs-slug semantics — render slug as-is; M4 owns dynamic dictionary lookup)

    3. Use optional chaining + null-safe defaults at every read site (`?? ''` for strings, `?? 0` for numbers); a legacy listing missing nested fields should NOT crash the card.

    4. If the card displays currency as a symbol (e.g. `'$1,500'`), introduce a small inline `currencySymbol(c)` helper:
       ```typescript
       const currencySymbol = (c?: string) => c === 'USD' ? '$' : c === 'EUR' ? '€' : c === 'KGS' ? 'KGS' : '';
       ```
       This is acceptable — D-03 says new tokens used in FormBag/payload; legacy display symbols can be derived.

    5. Run any existing snapshot/unit tests for PropertyCard.tsx in `src/components/__tests__/` after the swap; update snapshots if they exist.

    NOTE: Don't change PropertyCardProps interface unless required — the prop is `property: Property`, and Property is now nested per Phase 1 type stub. Field reads inside the component change; the prop interface doesn't.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit src/components/PropertyCard.tsx 2>&1 | grep -E "error TS" | head -10 || true; (find src/components/__tests__ -name "PropertyCard*.test.*" 2>/dev/null | head -1 | xargs -I{} npx jest {} -x 2>/dev/null || echo "no PropertyCard test — manual verify required")</automated>
  </verify>
  <done>
    - PropertyCard.tsx has zero references to `.price`, `.currency`, `.areaSqm`, `.bedrooms`, `.bathrooms`, `.images`, `.address` (all replaced with nested reads).
    - Existing snapshot/unit tests pass (or are updated if they reference flat fields).
  </done>
  <acceptance_criteria>
    - `grep -cE "property\\.price\\b|property\\.currency\\b|property\\.areaSqm\\b|property\\.bedrooms\\b|property\\.bathrooms\\b" src/components/PropertyCard.tsx` returns 0 (all flat reads replaced).
    - `grep -c "property\\.basics" src/components/PropertyCard.tsx` returns at least 4 (basics.price + basics.currency + basics.areaSqm + basics.rooms).
    - `grep -c "property\\.media" src/components/PropertyCard.tsx` returns at least 1 (media.photos read).
    - `grep -c "property\\.content" src/components/PropertyCard.tsx` returns at least 1 (content.title read).
    - `grep -c "property\\.location" src/components/PropertyCard.tsx` returns at least 1 (location.city or .district read).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update HospitalityCard.tsx + HospitalitySection caller-side derivation</name>
  <files>
    - src/components/HospitalityCard.tsx (MODIFIED — nested reads + preserve top-level maxGuests/amenities per D-21)
    - src/components/HospitalitySection.tsx (UNCHANGED if it doesn't filter — caller-side derivation only per Tradeoff §K. VERIFY; if it does filter internally, refactor to caller-side)
  </files>
  <read_first>
    - src/components/HospitalityCard.tsx (current state — find reads of `property.maxGuests`, `property.amenities`, `property.tours`, `property.hotelClass`)
    - src/components/HospitalitySection.tsx (current state — verify it does NOT filter properties internally; if it does, the caller-side derivation goes in HomeScreen/Favorites/RenterListings/OwnerListings instead)
    - src/utils/propertyCategory.ts (verify `propertyTypeToCategory` function — used in caller-side derivation; if it exists in another util file, find it via grep)
    - .planning/phases/02-6-step-contextual-listing-flow-client/02-PATTERNS.md rows 59-60
    - .planning/phases/02-6-step-contextual-listing-flow-client/02-CONTEXT.md D-21 (top-level preserved) + Claude's Discretion §"Nested-shape compat" (caller-side derivation pattern)
  </read_first>
  <action>
    Step 1 — Update `HospitalityCard.tsx`:
    - Read `property.basics?.hotelClass` (was `property.hotelClass`).
    - Read `property.media?.tourUrl` (was `property.tours[0]?.url` per Phase 1 D-12 first-tour-wins flatten).
    - PRESERVE `property.maxGuests` and `property.amenities` reads VERBATIM — these are top-level per D-21 (Phase 1 D-09 explicit). Adding `?.` for safety against legacy listings is fine.

    Step 2 — Verify `HospitalitySection.tsx` is a presentational wrapper (just renders the strip; doesn't filter). If it doesn't filter — done, no change needed.

    If it DOES filter internally on `transactionType` or similar — refactor to caller-side: remove the filter from HospitalitySection, expose just the props it needs (e.g. `properties: Property[]`), and add the filter at the caller (HomeScreen/Favorites/RenterListings/OwnerListings — done in Task 3 below).

    Per Tradeoff §K — the caller-side derivation rule is:
    ```typescript
    const hospitalityProperties = useMemo(
      () => properties.filter(p => p.dealType !== 'sale' && propertyTypeToCategory(p.propertyType) === 'Hospitality'),
      [properties]
    );
    ```

    Step 3 — Verify `propertyTypeToCategory` utility exists. M3 should preserve the M1 9-type 3-category taxonomy per CLAUDE.md guard. The utility maps `'apartment' | 'house'` → `'Residential'`, `'office' | 'commercial'` → `'Commercial'`, `'hotel' | 'hostel'` → `'Hospitality'`. If it doesn't exist or signature changed, add/adjust as needed in `src/utils/propertyCategory.ts`.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -nE "property\.tours\[|property\.hotelClass\b" src/components/HospitalityCard.tsx; test $? -eq 1 && grep -c "property\.basics\?\.hotelClass\|property\.media\?\.tourUrl" src/components/HospitalityCard.tsx</automated>
  </verify>
  <done>
    - HospitalityCard reads `property.basics?.hotelClass` + `property.media?.tourUrl`.
    - HospitalityCard PRESERVES `property.maxGuests` + `property.amenities` reads (D-21).
    - HospitalitySection unchanged (or refactored to caller-side derivation if it had internal filter).
  </done>
  <acceptance_criteria>
    - `grep -cE "property\\.tours\\[|property\\.hotelClass\\b" src/components/HospitalityCard.tsx` returns 0 (flat reads removed).
    - `grep -c "property\\.basics\\?\\.hotelClass" src/components/HospitalityCard.tsx` returns at least 1.
    - `grep -c "property\\.media\\?\\.tourUrl" src/components/HospitalityCard.tsx` returns at least 1.
    - `grep -c "property\\.maxGuests\\|property\\.amenities" src/components/HospitalityCard.tsx` returns at least 2 (D-21 top-level reads preserved).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update 4 list screens (HomeScreen, FavoritesScreen, RenterListingsScreen, OwnerListingsScreen)</name>
  <files>
    - src/screens/HomeScreen.tsx
    - src/screens/FavoritesScreen.tsx
    - src/screens/RenterListingsScreen.tsx
    - src/screens/OwnerListingsScreen.tsx
  </files>
  <read_first>
    - src/screens/HomeScreen.tsx (lines 65-93 — district filter chips STAY HARDCODED per D-10; lines ~168 + 224-228 — flat-shape filter logic to swap)
    - src/screens/FavoritesScreen.tsx (current — sibling pattern from HomeScreen)
    - src/screens/RenterListingsScreen.tsx (lines 326-355 — segmented control + FlatList + HospitalitySection in ListHeaderComponent; line 343 — HospitalitySection props)
    - src/screens/OwnerListingsScreen.tsx (sibling pattern)
    - .planning/phases/02-6-step-contextual-listing-flow-client/02-PATTERNS.md rows 53-56
    - .planning/phases/02-6-step-contextual-listing-flow-client/02-CONTEXT.md D-10 (Home district filter STAYS hardcoded) + Tradeoff §K (caller-side hospitality derivation)
  </read_first>
  <action>
    For each of the 4 screens, swap flat-shape reads → nested per the field-by-field table in `<interfaces>`. Common swaps:

    A. **Filter logic** that uses `p.type === 'rent'` etc. → `p.dealType !== 'sale'` (Tradeoff §K).
    B. **Render reads** of `p.price`, `p.currency`, `p.areaSqm`, `p.bedrooms`, `p.bathrooms`, `p.images`, `p.title`, `p.address` → nested equivalents.
    C. **Edit handler** in RenterListingsScreen + OwnerListingsScreen — `onEditProperty(p)` continues to receive the full `Property` object; no signature change needed since Property is now nested per Phase 1 type stub.

    Specifically for **HomeScreen**:
    1. Lines 65-93 (BISHKEK_DISTRICTS const + district filter chip source): UNCHANGED per D-10.
    2. Line ~168 flat `transactionType` filter: change `if (pType && pType !== transactionType) return false;` to `if (pType === 'sale' && p.dealType !== 'sale') return false;` (or whatever the actual filter logic was — preserve the semantics, not the shape).
    3. Lines 224-228 hospitality derivation: rewrite per Tradeoff §K:
       ```typescript
       const hospitalityProperties = useMemo(
         () => properties.filter(p => p.dealType !== 'sale' && propertyTypeToCategory(p.propertyType) === 'Hospitality'),
         [properties]
       );
       ```
    4. Map embed coord rename: any inline `latitude={p.latitude}` → `latitude={p.location?.coordinates?.lat}` (PropertyMap.tsx is updated separately in Task 5).
    5. District-matching read sites: the district filter chips compare against `p.district` (M2 flat) — change to `p.location?.district`.

    Specifically for **RenterListingsScreen**:
    1. Update `hospitalityProperties` + `otherProperties` derivation per Tradeoff §K.
    2. Edit entry point continues calling `onEditProperty(property)` — that handler is wired to App.tsx state today; Plan 02-07 changes the App.tsx side from `<CreateListingScreen propertyToEdit={p} />` to `<ContextualListingFlow mode="edit-owner" initialListing={p} />`. NO change needed in this screen for the entry-point dispatch.

    Specifically for **FavoritesScreen + OwnerListingsScreen**:
    Same nested-shape swap pattern; no Other-tab structure complexity.

    DO NOT alter:
    - HomeScreen district filter chip source (D-10).
    - Any state structure / hook / refresh-key state (App.tsx state shape unchanged).
    - The HospitalitySection component prop interface (Task 2 — caller-side derivation only).

    For each screen, run any existing test files in `src/screens/__tests__/` after changes; update snapshots if needed.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && for f in src/screens/HomeScreen.tsx src/screens/FavoritesScreen.tsx src/screens/RenterListingsScreen.tsx src/screens/OwnerListingsScreen.tsx; do echo "=== $f ==="; grep -cE "property\\.price\\b|property\\.bedrooms\\b|property\\.areaSqm\\b|property\\.transactionType\\b|p\\.transactionType\\b" "$f" || true; done</automated>
  </verify>
  <done>
    - All 4 screens have zero remaining flat-shape reads (`p.price`, `p.bedrooms`, `p.transactionType`, etc.) — verified by grep.
    - HomeScreen district filter chip source unchanged (D-10 preserved).
    - HospitalityProperties derivations rewritten per Tradeoff §K (`dealType !== 'sale'`).
    - Existing tests pass.
  </done>
  <acceptance_criteria>
    - `grep -cE "p\\.transactionType\\b|property\\.transactionType\\b" src/screens/HomeScreen.tsx src/screens/FavoritesScreen.tsx src/screens/RenterListingsScreen.tsx src/screens/OwnerListingsScreen.tsx` returns 0 (M2 transactionType filter rewritten in all 4).
    - `grep -cE "p\\.dealType !== 'sale'|p\\.dealType !=='sale'|dealType !== 'sale'" src/screens/HomeScreen.tsx src/screens/FavoritesScreen.tsx src/screens/RenterListingsScreen.tsx src/screens/OwnerListingsScreen.tsx` returns at least 4 (Tradeoff §K rule applied — at minimum 1 occurrence per screen if all 4 had hospitality derivations).
    - `grep -c "BISHKEK_DISTRICTS" src/screens/HomeScreen.tsx` returns at least 1 (D-10 — hardcoded const preserved).
    - `grep -cE "p\\.basics\\?\\.|property\\.basics\\?\\.|p\\.location\\?\\.|property\\.location\\?\\." src/screens/HomeScreen.tsx` returns at least 3 (multiple nested reads in HomeScreen).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update PropertyMap.tsx + ListingMetaTable.tsx</name>
  <files>
    - src/components/PropertyMap.tsx (MODIFIED)
    - src/components/ListingMetaTable.tsx (MODIFIED)
  </files>
  <read_first>
    - src/components/PropertyMap.tsx (lines 39-50 — current flat-coordinate read)
    - src/components/ListingMetaTable.tsx (current — find reads of basics + conditionAndAmenities + terms equivalents)
    - .planning/phases/02-6-step-contextual-listing-flow-client/02-PATTERNS.md rows 61-62
  </read_first>
  <action>
    **PropertyMap.tsx** — change coordinate source:
    ```typescript
    // BEFORE (lines 39-50 baseline):
    if (property.latitude && property.longitude) {
      coordinates = { latitude: property.latitude, longitude: property.longitude };
    }

    // AFTER:
    if (property.location?.coordinates?.lat != null && property.location?.coordinates?.lng != null) {
      coordinates = { latitude: property.location.coordinates.lat, longitude: property.location.coordinates.lng };
    }
    ```
    PRESERVE the hash-fallback for missing coords (lines 46-50 baseline) — keep verbatim. Only the read source changes.

    **ListingMetaTable.tsx** — swap reads per the field-by-field table:
    - `property.bedrooms` → `property.basics?.rooms`
    - `property.bathrooms` → `property.basics?.bathroom`
    - `property.areaSqm` → `property.basics?.areaSqm`
    - `property.condition` → `property.conditionAndAmenities?.condition`
    - `property.furnished` → `property.conditionAndAmenities?.furnished`
    - `property.deposit`, `property.prepaymentMonths`, `property.minTerm`, `property.negotiable` → `property.terms?.*`
    - PRESERVE i18n keys for row labels (no new keys here).
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -nE "property\.latitude\b|property\.longitude\b" src/components/PropertyMap.tsx; test $? -eq 1 && grep -c "property\.location\?\.coordinates" src/components/PropertyMap.tsx</automated>
  </verify>
  <done>
    - PropertyMap reads location.coordinates.lat/lng (no flat latitude/longitude).
    - ListingMetaTable reads basics + conditionAndAmenities + terms.
  </done>
  <acceptance_criteria>
    - `grep -cE "property\\.latitude\\b|property\\.longitude\\b" src/components/PropertyMap.tsx` returns 0.
    - `grep -c "property\\.location\\?\\.coordinates" src/components/PropertyMap.tsx` returns at least 1.
    - `grep -cE "property\\.bedrooms\\b|property\\.bathrooms\\b|property\\.condition\\b|property\\.furnished\\b" src/components/ListingMetaTable.tsx` returns 0.
    - `grep -c "property\\.basics\\?\\.\\|property\\.conditionAndAmenities\\?\\.\\|property\\.terms\\?\\." src/components/ListingMetaTable.tsx` returns at least 5.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Run RN client full test suite + i18n parity gate (no regressions)</name>
  <files>(verification only — no file modifications)</files>
  <read_first>(none)</read_first>
  <action>
    Verify no regressions from this plan's swaps:
    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/JayTap
    npm test
    bash scripts/check-i18n-parity.sh
    ```

    If any existing snapshot/unit test fails because of legitimate shape change (e.g. PropertyCard snapshot now renders nested-derived currency token), update the snapshot with `npx jest -u` for that specific test file. Do NOT mass-update all snapshots — only the ones touching the swapped components.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && npm test</automated>
  </verify>
  <done>
    - `npm test` exits 0.
    - `scripts/check-i18n-parity.sh` exits 0.
    - Any updated snapshots reviewed (visual diff in execute-phase log).
  </done>
  <acceptance_criteria>
    - `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npm test` exits 0.
    - `bash scripts/check-i18n-parity.sh` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>None — pure read-path field-name swaps in existing presentational components. No new HTTP endpoints. No auth-adjacent surface change.</threat_model>

<verification>
- All 9 surfaces grep-clean of flat-field reads (`property.price`, `property.bedrooms`, etc.).
- HomeScreen district filter chip source unchanged (D-10 preserved).
- D-21 top-level reads (`maxGuests` + `amenities`) preserved on HospitalityCard.
- RN client full suite green.
- i18n parity green (no key changes in this plan).
- testers' M2 builds (TestFlight 27 / Play Internal 30) regain working list/card screens once this ships (Pitfall 4 mitigation).
</verification>

<success_criteria>
- 11 must_haves truths observable.
- Phase 1 atomic-break window CLOSED for the 9 read-path surfaces (PropertyDetailsScreen — Plan 02-06 — closes the 10th).
</success_criteria>

<output>
.planning/phases/02-6-step-contextual-listing-flow-client/02-05-SUMMARY.md captures:
- For each of 9 files modified: which fields swapped + any snapshot updates needed.
- Confirmed: HomeScreen district filter chip source unchanged.
- Confirmed: D-21 top-level reads preserved.
- Note for Plan 02-06: PropertyDetailsScreen still on flat reads; same swap mapping applies (largest screen — needs careful pass).
</output>
