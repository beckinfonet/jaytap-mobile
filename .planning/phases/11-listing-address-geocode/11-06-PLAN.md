---
phase: 11-listing-address-geocode
plan: 06
type: execute
wave: 4
depends_on:
  - 11-01
  - 11-02
  - 11-03
  - 11-04
  - 11-05
files_modified:
  - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-QA-MATRIX.md
autonomous: false
requirements:
  - GEO-01
  - GEO-02
  - GEO-03
  - GEO-04
  - GEO-05
  - GEO-06
tags:
  - qa
  - manual
  - physical-device
  - release

must_haves:
  truths:
    - "A physical-device QA matrix file `.planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` exists with ≥ 12 cells covering: forward-geocode happy path (EN), forward-geocode happy path (RU), reverse-geocode happy path (EN), reverse-geocode happy path (RU), forward-geocode failure (anti-'random pin' defense — typed gibberish does NOT move pin), reverse-geocode + pre-existing typed text (anti-clobber), hospitality propertyType (hotel/hostel) always-shown address input, non-hospitality propertyType (apartment/house/office/commercial) gated address input behind toggle, edit-owner mode round-trip (open an existing listing with address, see it pre-populated), pre-Phase-11 listing without address renders fallback (district, city), iOS device 1 walked, Android device 1 walked."
    - "Each cell records PASS / FAIL / DEFERRED-USER-APPROVED status with a one-line evidence note (matches M2 / M3 REL-03 convention)."
    - "Devices walked: iPhone 15 Pro Max (iOS) + Moto G XT2513V (Android) per memory `m1-shipped-2026-04-28.md` baseline (same devices used for M1, M2, M3 REL-03)."
    - "Backend live + healthy on Railway at the post-Plans-11-01/02 commit SHA (live route smoke included in matrix)."
    - "EN+RU parity verified on-device (not just in unit tests) — RU locale shows Russian address-input strings + Russian Nominatim displayName results."
    - "Anti-'random pin' defense empirically confirmed: typing 'gibberish-asdfasdf' returns the 'Couldn\\'t find that address' label AND the pin does NOT move from its previous location."
    - "Backwards compat empirically confirmed: opening an existing M3 / M5-Phase-1 listing (one created before Phase 11 lands; verifiable via Mongo `db.listings.findOne({ 'location.address': { $exists: false } })`) renders the details screen with district+city fallback as today; no crash, no visual regression."
    - "T-11-18 race cell (drop pin → type within reverse-geocode response window → closure-stale address overwrites just-typed character) is EXPLICITLY present in the matrix as Cell 2.7 with the accepted-race semantics documented. Per Plan 11-04 this threat is acknowledged-and-accepted; the QA walk DOCUMENTS the observed behavior empirically rather than gating on PASS/FAIL."
    - "Edge case from Plan 11-04 T-11-18 (pin-then-type-before-response) is attempted in the matrix — outcome documented (PASS / FAIL / N/A-couldn't-repro)."
    - "Final checkpoint pauses for human approval before phase-close per the workflow (autonomous: false). User types 'approved' OR provides FAIL evidence."
  artifacts:
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-QA-MATRIX.md"
      provides: "Manual QA matrix walked on iOS + Android with cell-by-cell PASS/FAIL/DEFERRED records (includes Cell 2.7 explicit T-11-18 race documentation)"
      min_lines: 60
  key_links:
    - from: "QA matrix cells"
      to: "Plans 11-01..11-05 must-have truths"
      via: "Empirical verification of the goal-backward truths in each upstream plan"
      pattern: "PASS|FAIL"
    - from: "Cell 2.7 (T-11-18 race documentation)"
      to: "Plan 11-04 threat register T-11-18 (acknowledged-and-accepted)"
      via: "Empirical observation captured during physical-device walk; not a PASS/FAIL gate"
      pattern: "T-11-18"
    - from: "RU locale cells"
      to: "Plan 11-05 i18n key additions"
      via: "On-device language toggle to RU; observe Russian strings"
      pattern: "Адрес|Поиск адреса"
    - from: "Phase 11 close"
      to: "M5 v5.0 milestone roadmap"
      via: "Manual user approval transitions Phase 11 row in ROADMAP.md from [ ] to [x]"
      pattern: "Phase 11.*\\[x\\]"
---

<objective>
Walk a focused manual physical-device QA matrix on iOS + Android to empirically verify the 6 ROADMAP success criteria. This is the gate that converts code-passing-tests into a user-experienced phase delivery. No additional code changes — this plan creates a QA-MATRIX.md, walks it, records evidence, and pauses for human approval before phase-close.

Purpose: Per memory `gsd-verifier-misses-regressions.md`, the GSD verifier missed 2 CRITICAL regressions in M2 Phase 1 while saying PASS 15/15. Plan 11-06 is the empirical gate that catches what unit tests + verifier passes can't see: animation feel, debounce timing on a real keyboard, Russian text rendering width, anti-"random pin" behavior under noisy network conditions, etc. Per M3 RETROSPECTIVE.md lesson 4 (empirical-sampling-mass-disposition), feature-surface cells can take mass disposition while golden-path cells must walk-and-confirm.

Output:
- 1 NEW file: `11-QA-MATRIX.md` (~80–150 lines) with the matrix walked and recorded.
- 1 final blocking checkpoint pausing for user approval.
- 0 code changes (this plan is empirical verification only).
- Inputs to Plan 11-XX-SUMMARY.md (the final phase summary) and the ROADMAP transition to [x].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/beckmaldinVL/development/mobileApps/JayTap/CLAUDE.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/PROJECT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/ROADMAP.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-01-PLAN.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-02-PLAN.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-03-PLAN.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-04-PLAN.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-05-PLAN.md

<prereqs>
Before walking this matrix:
1. Backend MUST be deployed to Railway with the Plan 11-01 + 11-02 commits live. Verify via: `curl -s https://jaytap-services-production.up.railway.app/api/locations/geocode -X POST -H "Content-Type: application/json" -d '{}' | grep -c "ADDRESS_REQUIRED"` returns 1 (endpoint exists; sanitizer fires; 401 may appear instead because no Bearer — that ALSO confirms the route exists).
2. RN-client build (debug or release) MUST include Plans 11-03, 11-04, 11-05 commits. Confirm via local app version OR commit SHA at app launch.
3. Test fixtures: at least 1 pre-Phase-11 listing in production Mongo with NO `location.address` (verify via Mongo Compass or Atlas UI — `db.listings.find({ 'location.address': { $exists: false } }).limit(1)`).
4. Test fixtures: a known KG address that geocodes ("100 Manas Street") + a known KZ address ("Abay 100, Almaty") + a known UZ address ("Amir Temur 5, Tashkent") + a known-bad string ("zzzzzz-not-a-real-place").
5. Both devices CHARGED and have Wi-Fi reachable to Railway.
</prereqs>

<interfaces>
<!-- Reference QA matrices from prior phases for format conventions. -->

The M3 Phase 5 REL-03 matrix at `.planning/milestones/v3.0-phases/05-hardening-manual-physical-device-qa-and-release-v3-0-0/05-06-QA-MATRIX.md` (if archived there) and the M2 Phase 6 matrix establish the table format. Cell statuses used:
- `PASS` — feature works as specified.
- `FAIL` — feature broken; blocks phase close.
- `PARTIAL` — feature works in some condition (e.g., iOS) but not another (Android); document the split.
- `DEFERRED-USER-APPROVED` — user explicitly accepts the gap for this phase (rare; documented inline).
- `N/A` — cell not applicable to this device/locale (e.g., a Russian-locale cell on a device set to English).
- `BLOCKED` — cell could not walk because of an environment issue (e.g., backend down); MUST be resolved before phase close.
- `DOCUMENTED` — cell is informational-only (e.g., known accepted race per upstream threat register); record OBSERVED behavior, not PASS/FAIL.

The matrix table format used by M3 Phase 5:
| Cell | Device | Locale | Action | Expected | Result | Evidence |
|------|--------|--------|--------|----------|--------|----------|
| 1.1  | iPhone | EN | ... | ... | PASS | one-line note |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 11-QA-MATRIX.md skeleton (≥12 cells, devices x locales x scenarios, incl. explicit Cell 2.7 for T-11-18 race documentation)</name>
  <files>
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-QA-MATRIX.md
  </files>
  <read_first>
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-04-PLAN.md
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/ROADMAP.md
  </read_first>
  <action>
    PRE-FLIGHT: Verify `pwd` is `/Users/beckmaldinVL/development/mobileApps/JayTap` AND `git branch --show-current`.

    Create `.planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` with the following structure:

    ```markdown
    # Phase 11 QA Matrix — Listing Address Geocode (Forward + Reverse)

    **Walked:** YYYY-MM-DD (filled by the executor when walking)
    **Devices:**
    - iOS: iPhone 15 Pro Max (iOS XX.X) — build SHA: <fill in app launch>
    - Android: Moto G XT2513V (Android XX) — build SHA: <fill in app launch>
    **Backend SHA:** <fill from Railway dashboard>

    ## Pre-Flight Health Checks

    | # | Check | Expected | Result | Evidence |
    |---|-------|----------|--------|----------|
    | P.1 | Railway live route ping: `curl -X POST <prod>/api/locations/geocode -d '{}'` | 400 ADDRESS_REQUIRED (or 401 if no Bearer) | TBD | one-line |
    | P.2 | App launch on iOS | No crash; lands on Home | TBD | screenshot/none |
    | P.3 | App launch on Android | No crash; lands on Home | TBD | screenshot/none |

    ## Golden-Path Cells (walk-and-confirm; mass disposition NOT allowed)

    | Cell | Device | Locale | Action | Expected | Result | Evidence |
    |------|--------|--------|--------|----------|--------|----------|
    | 1.1 | iPhone | EN | Create Listing → Step 1 = sale + apartment → Step 2 → select city=Bishkek → toggle "Show exact address" ON | Address input appears below toggle with placeholder "e.g. 100 Manas St" | TBD | one-line |
    | 1.2 | iPhone | EN | (continuing 1.1) Type "100 Manas Street" → pause 1s | Spinner appears next to input → spinner disappears → pin moves on map → input updates to canonical Nominatim displayName | TBD | one-line |
    | 1.3 | iPhone | RU | Re-walk 1.1+1.2 with app language = RU | Placeholder shows "напр. ул. Манаса 100"; loading hint shows "Поиск адреса…"; success path identical | TBD | one-line |
    | 1.4 | iPhone | EN | Step 2 with city=Bishkek selected → tap on map to drop a pin (do NOT type address) | Pin drops at tap location; address input AUTO-FILLS with reverse-geocoded displayName within ~1s | TBD | one-line |
    | 1.5 | iPhone | EN | Step 2 → drop a pin first (capturing address X via reverse) → THEN type "Erkindik 5" → wait | Spinner appears; on success: pin re-moves to Erkindik 5 AND input shows canonical Erkindik 5 displayName. The previously-reverse-filled address is OVERWRITTEN by the new forward result (this is correct — typed text always wins over reverse text). | TBD | one-line |
    | 1.6 | Android | EN | Re-walk 1.1+1.2 on Android | Same behavior; no platform-specific glitch | TBD | one-line |
    | 1.7 | Android | RU | Re-walk 1.3 on Android | Same RU strings + same success path | TBD | one-line |

    ## Anti-Regression Cells (defense-in-depth verification)

    | Cell | Device | Locale | Action | Expected | Result | Evidence |
    |------|--------|--------|--------|----------|--------|----------|
    | 2.1 | iPhone | EN | Step 2 → type gibberish "zzzzzz-not-a-place" → wait | Spinner → "Couldn't find that address — drop a pin instead" inline error label appears under the input; pin DOES NOT MOVE from its previous location; typed text remains in input. (Anti-"random pin" defense.) | TBD | one-line |
    | 2.2 | Android | EN | Re-walk 2.1 on Android | Same behavior | TBD | one-line |
    | 2.3 | iPhone | EN | Step 2 → type a valid address → wait for pin move → DROP A PIN AT A DIFFERENT LOCATION → check input | Input is NOT overwritten by reverse-geocode (the empty-text guard fires because address was non-empty post-forward). | TBD | one-line |
    | 2.4 | iPhone | EN | Step 1 = sale + HOTEL → Step 2 | "Show exact address" toggle is HIDDEN (hotel forces showExactAddress=true); address input is VISIBLE WITHOUT toggling. (11-CONTEXT.md decision 9: hospitality always shows the input.) | TBD | one-line |
    | 2.5 | iPhone | EN | Step 1 = sale + apartment → Step 2 → DO NOT toggle "Show exact address" → check | Address input is NOT visible (gated on toggle for non-hospitality). | TBD | one-line |
    | 2.6 | iPhone | EN | Step 1 = sale + apartment → Step 2 → fill city + district + drop pin → DO NOT touch address input → advance to Step 3+ → submit listing | Listing creates successfully; backend returns 200; no validation error about missing address. (Address is optional per 11-CONTEXT.md.) | TBD | one-line |
    | 2.7 | iPhone | EN | **T-11-18 documented race** — On iPhone EN, drop pin on map → within 1 second, type 1 character in the address field → wait for the reverse-geocode response. | Reverse-geocode is ALLOWED to overwrite the just-typed character (known accepted race per Plan 11-04 T-11-18 threat register entry). Document the actual observed behavior (which one won? did the input flash? was there a perceptible "fight"?). Not a fail unless the app crashes or the pin moves unexpectedly. Status should be `DOCUMENTED` rather than `PASS` / `FAIL` since the race is acknowledged-and-accepted upstream. | DOCUMENTED | "Why: documents T-11-18 acceptance with empirical evidence. If observed UX is significantly worse than the threat-register prediction, escalate to a Plan 11-07 hotfix; otherwise record + close." |

    ## Round-Trip / Display Cells

    | Cell | Device | Locale | Action | Expected | Result | Evidence |
    |------|--------|--------|--------|----------|--------|----------|
    | 3.1 | iPhone | EN | Complete a fresh listing with `location.address: '100 Manas Street, Bishkek, KG'` → submit → open PropertyDetailsScreen | Address line shows the FULL Nominatim displayName (NOT just "Bishkek, Bishkek" synthesis). Layout matches M5 Phase 1 redesign (HeaderInfoCard pill row + title + address row + red divider + map). | TBD | one-line |
    | 3.2 | iPhone | EN | Open a pre-Phase-11 listing (no `location.address`) — find one via the listings list | Address line shows district + city slug synthesis as before (fallback path). Zero visual regression from M5 Phase 1 baseline. | TBD | one-line |
    | 3.3 | iPhone | EN | Open a Phase-11 listing → tap Share button | Share sheet message includes the Nominatim displayName line (not the synthesized fallback). | TBD | one-line |
    | 3.4 | iPhone | RU | Re-walk 3.1 with RU locale + RU address ("Манас 100, Бишкек") | Address shown in Russian (Nominatim Cyrillic when Accept-Language: ru) | TBD | one-line |
    | 3.5 | Android | EN | Re-walk 3.1 on Android | Same behavior | TBD | one-line |
    | 3.6 | iPhone | EN | Open a Phase-11 listing from the HORIZONTAL HOSPITALITY STRIP (HomeScreen) → check the HospitalityCard's visible address row + tap Share | Card body's address row shows full Nominatim displayName; share message includes the displayName (per Plan 11-05 Task 3 HospitalityCard precedence flip). | TBD | one-line |
    | 3.7 | iPhone | EN | Open a Phase-11 listing from SEARCH RESULTS or FAVORITES (PropertyCard surface) → tap Share | Share message includes the Nominatim displayName (per Plan 11-05 Task 3 PropertyCard precedence flip on the addressDisplay const). Note: the Row 3 MapPin label intentionally stays district+city (geographic context). | TBD | one-line |

    ## Edit-Mode Cells

    | Cell | Device | Locale | Action | Expected | Result | Evidence |
    |------|--------|--------|--------|----------|--------|----------|
    | 4.1 | iPhone | EN | Open a Phase-11 listing as owner → tap Edit → Step 2 | Address input is pre-populated with the persisted displayName; toggle reflects showExactAddress true | TBD | one-line |
    | 4.2 | iPhone | EN | Edit listing → modify address only → submit | PUT succeeds; reopen detail → updated address shows | TBD | one-line |
    | 4.3 | iPhone | EN | Open a Phase-11 listing as moderator (edit-on-behalf) → Step 2 | Same pre-populated input; mod can edit | TBD | one-line |

    ## Geographic-Scope Cells (KG/KZ/UZ)

    | Cell | Device | Locale | Action | Expected | Result | Evidence |
    |------|--------|--------|--------|----------|--------|----------|
    | 5.1 | iPhone | EN | Step 2 → select city=Almaty (KZ) → type "Abay 100" → wait | Pin moves to an Almaty location (viewbox bias from Almaty centroid prevents Bishkek match). | TBD | one-line |
    | 5.2 | iPhone | EN | Step 2 → select city=Tashkent (UZ) → type "Amir Temur 5" → wait | Pin moves to a Tashkent location. | TBD | one-line |
    | 5.3 | iPhone | EN | Step 2 → DO NOT select a city → type "100 Manas Street" → wait | Backend route still succeeds (no citySlug → no viewbox → countrycodes=kg,kz,uz still narrows the search); pin moves to a KG/KZ/UZ location. | TBD | one-line |

    ## Timeout / Failure Cells

    | Cell | Device | Locale | Action | Expected | Result | Evidence |
    |------|--------|--------|--------|----------|--------|----------|
    | 6.1 | iPhone | EN | Step 2 → enable airplane mode → type address → wait | Spinner → "Couldn't find that address" label (axios fails / times out → null result → notFound state); pin static. Cellular off → no crash. | TBD | one-line |
    | 6.2 | iPhone | EN | Backend 5s AbortController fires (simulate by quick Wi-Fi disconnect after typing) | Same notFound label; no spinner stuck forever. | TBD | one-line |

    ## KBD-02 / Keyboard Regression

    | Cell | Device | Locale | Action | Expected | Result | Evidence |
    |------|--------|--------|--------|----------|--------|----------|
    | 7.1 | iPhone | EN | Step 2 → tap address input → keyboard appears | Keyboard does NOT cover the input; can see what is being typed (M1 KBD-02 invariant). | TBD | one-line |
    | 7.2 | Android | EN | Re-walk 7.1 on Android | Same KBD-02 behavior. | TBD | one-line |

    ## Summary

    - Total cells: XX
    - PASS: XX
    - FAIL: XX (list cells if any; FAIL count > 0 BLOCKS phase close per CARRY-FORWARD or REJECT)
    - PARTIAL: XX
    - DEFERRED-USER-APPROVED: XX (must list each with rationale)
    - DOCUMENTED: XX (informational-only cells — primarily Cell 2.7 T-11-18 race)
    - BLOCKED: XX (must be resolved before close)

    ## Disposition

    - [ ] All FAIL cells either (a) carried forward as documented M6 backlog items with user approval, OR (b) fixed in a follow-up plan within this phase.
    - [ ] All BLOCKED cells resolved.
    - [ ] Cell 2.7 (T-11-18 documented race) has empirical observation recorded — if observed UX is "much worse than predicted by threat register", escalate to Plan 11-07 hotfix before close.
    - [ ] User reviewed and approved phase close.

    ## Carry-Forward Items (if any)

    (Append M6 backlog seeds here per user direction during the close checkpoint.)
    ```

    Replace placeholders (`<fill in app launch>`, `<fill from Railway dashboard>`, `TBD`, `XX`) as cells are walked. Cell 2.7's default status is `DOCUMENTED` (not `TBD`) because the cell is informational by design — the executor's job is to fill in the Evidence column with the OBSERVED race outcome, not to flip the Result to PASS/FAIL. Do NOT commit the file with `TBD` markers in non-2.7 cells — every non-DOCUMENTED cell MUST be FULLY walked before this task's `done` is set.

    SCALE NOTE: The matrix above has 32 cells total (incl. Cell 2.7 + the two new browse-card cells 3.6 + 3.7 added during the same revision pass that introduced Cell 2.7). Per M3 RETROSPECTIVE.md lesson 4 (empirical-sampling-mass-disposition), it's acceptable to walk-and-confirm the GOLDEN-PATH cells (section 1) + ANTI-REGRESSION cells (section 2, including Cell 2.7) explicitly, and use MASS DISPOSITION for feature-surface cells (sections 3, 4, 5, 6, 7) where logical equivalence between iOS and Android holds. Apply judgment: if any cell SURFACES a defect, escalate to walk-and-confirm.

    MINIMUM TO PASS THIS TASK (per the must_haves): walk-and-confirm ≥ 12 cells across the 5+ sections, covering both devices and both locales for at least the golden-path cells AND covering Cell 2.7 (T-11-18 race documentation) explicitly.

    Save and commit the file at end. DO NOT pause for the human-verify checkpoint inside this task — Task 2 owns that checkpoint.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && test -f .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md && grep -c "PASS\|FAIL\|PARTIAL\|DEFERRED-USER-APPROVED\|N/A\|DOCUMENTED" .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-QA-MATRIX.md`.
    - Cell count ≥ 12: `grep -cE "^\| [0-9]\.[0-9] \|" .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` returns ≥ 12.
    - No remaining TBD markers in walked cells: `grep -c " TBD " .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` returns 0 (all cells dispositioned; Cell 2.7 uses `DOCUMENTED`; any explicitly-skipped cells use N/A or BLOCKED).
    - Cell 2.7 (T-11-18 race) present: `grep -c "^| 2\.7 " .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` returns ≥ 1 AND `grep -c "T-11-18" .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` returns ≥ 1.
    - Cell 2.7 expected text mentions race / reverse-geocode response window: `grep -i "reverse-geocode" .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md | wc -l` returns ≥ 2 (Cell 2.7 + surrounding cells).
    - Both devices represented: `grep -c "iPhone" .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` ≥ 5 AND `grep -c "Android\|Moto" .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` ≥ 3.
    - Both locales represented: `grep -c " EN " .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` ≥ 8 AND `grep -c " RU " .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` ≥ 2.
    - Summary section filled: `grep -c "Total cells:" .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` returns 1 with a non-XX numeric.
    - DOCUMENTED tally surfaced in Summary: `grep -c "^- DOCUMENTED:" .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` returns 1.
    - Backend SHA filled: `grep -E "Backend SHA: [a-f0-9]" .planning/phases/11-listing-address-geocode/11-QA-MATRIX.md | wc -l` returns 1.
  </acceptance_criteria>
  <done>
    Matrix file exists, ≥ 12 cells walked, both devices + both locales represented, Cell 2.7 (T-11-18 race documentation) explicitly present with `DOCUMENTED` status and empirical evidence note, Summary section filled with counts including the DOCUMENTED tally, no remaining TBD placeholders in walked cells.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human approval — phase 11 close</name>
  <what-built>
    Plans 11-01 through 11-05 implementations + Plan 11-06 Task 1 QA matrix walked on iPhone 15 Pro Max + Moto G XT2513V across EN + RU locales with ≥ 12 dispositioned cells (incl. Cell 2.7 T-11-18 race documentation).

    Deliverables to verify:
    - Backend: `Property.location.address` schema field + hardened `geocoder.js` (AbortController + viewbox + addressdetails + Accept-Language) + new `reverseGeocode` helper + 2 new POST routes (`/api/locations/geocode` + `/api/locations/reverse-geocode`) + CONTEXT.md Decision 4 sanitizer audit recorded for BOTH propertyRoutes.js AND moderationRoutes.js PUT paths.
    - Client: `geocodeService.ts` (NEW) + Step 2 address TextInput with debounced forward + reverse-on-pin + 4 new EN/RU i18n keys + `PropertyDetailsScreen.addressDisplay` precedence flip + HospitalityCard + PropertyCard precedence flips (browse-surface coverage of CONTEXT.md Decision 11 closed).
    - Tests: ~37 new test cases (4 schema + 14 geocoder + 15 locationRoutes; counts may vary slightly).
    - QA matrix: `.planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` walked, including Cell 2.7 T-11-18 race documentation with observed behavior recorded.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/11-listing-address-geocode/11-QA-MATRIX.md` and review the Summary section. Confirm:
       a. FAIL count is 0 (or each FAIL is paired with a follow-up disposition in Carry-Forward Items).
       b. BLOCKED count is 0.
       c. DOCUMENTED count ≥ 1 (Cell 2.7 T-11-18 race) — read the Evidence column for Cell 2.7 and confirm the observed behavior is acceptable per the upstream threat-register acceptance (not "much worse than predicted").
       d. DEFERRED-USER-APPROVED items (if any) have rationale you accept.
    2. Optional spot-check on a single device:
       a. Open Create Listing → Step 2.
       b. Toggle "Show exact address" → type "100 Manas Street" → confirm pin moves.
       c. Type gibberish → confirm "Couldn't find that address" appears AND pin does not move.
       d. Drop a fresh pin somewhere else → confirm address field auto-fills.
    3. Confirm Backend live + healthy at the SHA recorded in the matrix.
    4. Confirm ROADMAP.md Phase 11 row is ready to transition from `[ ]` to `[x]`.

    Resolution paths:
    - All cells PASS / DOCUMENTED-acceptable + you approve → type "approved" to close the phase.
    - One or more cells FAIL and you want them carried forward as M6 backlog → type "approved with carry-forward: <one-line items>".
    - Cell 2.7 observed UX is much worse than threat-register prediction → type "blocked: T-11-18 UX worse than accepted; need debounce hardening" and the workflow opens Plan 11-07 hotfix.
    - Real blocking defect surfaced → type "blocked: <one-line description>" and the workflow opens a follow-up plan (Plan 11-07-PLAN.md gap-closure) before phase close.
  </how-to-verify>
  <resume-signal>Type "approved" OR "approved with carry-forward: ..." OR "blocked: ..."</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA executor → manual device interaction | The executor operates the physical devices; no software trust boundary crossed during the walk. |
| QA matrix file → ROADMAP transition | The file is the authoritative record of "did this phase ship the user value." Subsequent retro / cross-milestone trends read from it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-25 | Repudiation | Mass-disposition without empirical evidence | mitigate | Per M3 RETROSPECTIVE.md lesson 4: mass disposition is allowed only when iOS↔Android logical equivalence holds AND the cell is feature-surface (not golden-path). Golden-path cells (section 1) MUST walk-and-confirm. Cell 2.7 (T-11-18 race) MUST walk-and-record-observation. The matrix template enforces this distinction structurally — sections are labeled. |
| T-11-26 | Tampering | QA matrix marked PASS without device walk | accept | Project convention trusts the executor's evidence notes (one-line per cell). Cross-check is the human-approval checkpoint (Task 2) — user reviews the matrix before approval. If user suspects fabrication, they can re-walk any cell themselves. |
| T-11-27 | DoS | Nominatim rate limit hit during QA walk | mitigate | Walking ≤32 cells across 2 devices = ≤64 geocode requests. At 1 req/sec Nominatim limit, this completes in ~1.1 minutes even sequentially. Acceptable. |
| T-11-28 | Repudiation | T-11-18 race silently skipped during QA walk (no Cell 2.7) | mitigate | Cell 2.7 is structurally present in the matrix skeleton (WARNING #2 closure from plan-checker revision pass). The Summary section's DOCUMENTED tally surfaces whether the executor walked the documented-race cells; a 0 in that tally with Cell 2.7 present signals an unwalked cell to the approver. |

ASVS L1 is not directly applicable to manual QA — verification happens at the empirical layer.
</threat_model>

<verification>
Phase-wide verification this plan closes:

1. **All 6 ROADMAP Success Criteria empirically validated** on physical devices (not just in unit tests):
   - SC #1 forward geocode happy path + failure-preserves-pin: cells 1.2, 1.3, 1.6, 2.1, 2.2.
   - SC #2 reverse geocode anti-clobber: cells 1.4, 1.5, 2.3.
   - SC #3 round-trip + pre-Phase-11 backwards compat: cells 3.1, 3.2.
   - SC #4 KG/KZ/UZ viewbox + AbortController: cells 5.1, 5.2, 5.3, 6.1, 6.2; Accept-Language: cells 1.3, 3.4.
   - SC #5 EN+RU parity: covered by Plan 11-05's sentinel + cell 1.3 / 3.4 empirical confirmation.
   - SC #6 PropertyDetailsScreen + browse-card coverage + no M5 P1 regression: cells 3.1, 3.2, 3.5, 3.6, 3.7.
2. **T-11-18 race empirically documented** — Cell 2.7 captures the acknowledged-and-accepted race from Plan 11-04 threat register with observed behavior recorded (WARNING #2 closure from plan-checker revision pass).
3. **MANDATORY paired code review** (per memory `gsd-verifier-misses-regressions.md`): even though this plan is QA-only, the orchestrator should schedule a code review pass across Plans 11-01..11-05 BEFORE this checkpoint. Verifier + Reviewer paired gates per the memory.
4. **ROADMAP update** post-approval: transition Phase 11 row from `[ ]` to `[x]` in `.planning/ROADMAP.md`; add a short milestones-section entry (this happens via `/gsd-transition` or manual edit — not part of this plan's writes).
5. **STATE.md update** post-approval: log the phase close + any carry-forward items.
6. **Memory note (recommend)**: If the matrix surfaces a novel anti-pattern, save a memory note via the `memory` skill — same convention as M3 / M4 milestone closes. If Cell 2.7's observed T-11-18 UX is noticeably worse than the threat-register prediction, that itself is a memory-worthy observation.
</verification>

<success_criteria>
This plan succeeds when:
- `11-QA-MATRIX.md` written + walked + committed with ≥ 12 dispositioned cells across both devices + both locales, INCLUDING Cell 2.7 (T-11-18 race documentation) with `DOCUMENTED` status and empirical evidence note.
- FAIL count is 0 OR all FAILs have user-approved carry-forward dispositions.
- BLOCKED count is 0.
- DOCUMENTED count surfaced in Summary (Cell 2.7 minimum).
- Human approval checkpoint reached + user types "approved" (with or without carry-forward).
- Phase 11 ready to mark `[x]` in ROADMAP.md.
- M5 v5.0 milestone has 2 GSD-tracked phases shipped (M5 Phase 1 outside-GSD + Phase 11 = first GSD-tracked).
</success_criteria>

<output>
After completion, create `.planning/phases/11-listing-address-geocode/11-06-SUMMARY.md` covering:
- Matrix file path + total cell count + PASS/FAIL/PARTIAL/DEFERRED-USER-APPROVED/DOCUMENTED/BLOCKED breakdown.
- Cell 2.7 (T-11-18 race) observed behavior + whether it matched threat-register prediction (if not, flag for memory note + potential M6 follow-up).
- Devices walked + their build SHAs.
- Backend SHA + Railway health confirmation.
- User-approval timestamp + the exact approval text typed.
- Carry-forward items (if any) seeded into the M6 backlog with rationale.
- Phase 11 close certificate: a one-paragraph summary suitable for the M5 milestone retrospective.
- Confirmation that ROADMAP.md Phase 11 row was transitioned `[ ]` → `[x]` (or note that the orchestrator's `/gsd-transition` step will do it).
- Memory notes saved (if any).
</output>
</content>
</invoke>