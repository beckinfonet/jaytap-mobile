---
phase: 11-listing-address-geocode
plan: 05
type: execute
wave: 3
depends_on:
  - 11-03
  - 11-04
files_modified:
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/HospitalityCard.tsx
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/PropertyCard.tsx
autonomous: true
requirements:
  - GEO-05
  - GEO-06
tags:
  - rn-client
  - i18n
  - display
  - read-path

must_haves:
  truths:
    - "`src/locales/en.ts` declares 4 NEW keys (per 11-CONTEXT.md decision 10): `contextualListing.step2.addressLabel` = 'Street address'; `contextualListing.step2.addressPlaceholder` = 'e.g. 100 Manas St'; `contextualListing.step2.addressNotFound` = `'Couldn\\'t find that address — drop a pin instead'`; `contextualListing.step2.addressGeocoding` = 'Looking up address…'."
    - "`src/locales/ru.ts` declares the same 4 keys in identical alphabetical order with Russian translations: `addressLabel` = 'Адрес'; `addressPlaceholder` = 'напр. ул. Манаса 100'; `addressNotFound` = 'Не удалось найти адрес — поставьте точку на карте'; `addressGeocoding` = 'Поиск адреса…'."
    - "`scripts/check-i18n-parity.sh` exits 0 after the additions (EN and RU key sets identical) — ROADMAP SC #5."
    - "`PropertyDetailsScreen.tsx` line 525 region: the `addressDisplay` derivation prefers `property.location?.address` when non-empty, falling back to the existing `[districtLabel, cityLabel].filter(Boolean).join(', ')` synthesis — exact precedence: `const addressDisplay = (property.location?.address ?? '').trim() || [districtLabel, cityLabel].filter(Boolean).join(', ');`."
    - "`HospitalityCard.tsx` line ~75 `addressDisplay` derivation flips to the SAME precedence pattern, PRESERVING this file's existing `', '` fallback separator: `const addressDisplay = (property.location?.address ?? '').trim() || [districtLabel, cityLabel].filter(Boolean).join(', ');`. Browse-strip hospitality cards now show `location.address` when present — closing CONTEXT.md Decision 11 + ROADMAP SC #6 for browse surfaces."
    - "`PropertyCard.tsx` line ~74 `addressDisplay` derivation flips to the SAME precedence pattern, PRESERVING this file's existing `' · '` (bullet) fallback separator: `const addressDisplay = (property.location?.address ?? '').trim() || [districtLabel, cityLabel].filter(Boolean).join(' · ');`. The pre-Phase-11 visual is byte-identical when address is absent (same `' · '` separator); when address is present, the share message at line ~116 and the address used in handleShare inherits the precedence flip automatically."
    - "`PropertyCard.tsx` line ~291 inline JSX `[districtLabel, cityLabel].filter(Boolean).join(' · ')` (Row 3 MapPin + district · city) is INTENTIONALLY NOT changed — this is the geographic-context label row directly under the title, semantically equivalent to `MapPreviewCard`'s district+city role (geographic context, not granular street). Same rationale as MapPreviewCard exclusion. Document this in the action block."
    - "All downstream consumers of `addressDisplay` in PropertyDetailsScreen (line 525) inherit the new precedence automatically — `handleShare` (line 542), email body (line 638), HeaderInfoCard (line 969 via formatAddress), fullScreenMap (lines 1385, 1401) — confirmed via the existing `addressDisplay` const usage pattern (line 525 is the single derivation point)."
    - "`HeaderInfoCard.tsx` and `MapPreviewCard.tsx` and `formatAddress.ts` do NOT need direct edits: `HeaderInfoCard` receives `formattedAddress={formatAddress(addressDisplay)}` (line 969 of PropertyDetailsScreen) so the precedence change at the addressDisplay derivation propagates through formatAddress to HeaderInfoCard automatically; `MapPreviewCard` receives `district + city` props directly (line 989) and is independent of the address string — it always shows district/city, which is correct behavior (the map-card's purpose is geographic context, not the granular street). NO CHANGES needed in these files for Phase 11."
    - "M5 Phase 1 details redesign layout is preserved bit-for-bit — `git diff src/screens/PropertyDetailsScreen.tsx` shows ONLY the one-line change at the addressDisplay derivation; no JSX structure, no style, no other derivation changes."
    - "Pre-Phase-11 listings (no `location.address`) continue to render IDENTICALLY to today across all three surfaces (details, hospitality-card, property-card) — when `property.location?.address` is `undefined` OR `''`, the fallback synthesis fires per-file (`, ` for details + hospitality-card; `' · '` for property-card) and the result matches the pre-change output exactly."
    - "`npx tsc --noEmit` from the RN client root reports zero NEW errors (the i18n key additions resolve the `as TranslationKeys` casts Plan 11-04 used; the addressDisplay precedence changes are pure expression edits)."
    - "After this plan lands, `as TranslationKeys` casts in `Step2Location.tsx` (added by Plan 11-04 as a temporary union-expansion workaround) resolve to REAL union members — every cast site references a key that now exists in the TranslationKeys union. Verifiable via `grep -n 'as TranslationKeys' src/components/ContextualListingFlow/Step2Location.tsx` — each cast site should reference one of the 4 new keys (addressLabel / addressPlaceholder / addressNotFound / addressGeocoding) added by this plan."
  artifacts:
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts"
      provides: "4 new contextualListing.step2.address* keys (EN)"
      contains: "addressLabel"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts"
      provides: "4 new contextualListing.step2.address* keys (RU)"
      contains: "addressLabel"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx"
      provides: "addressDisplay prefers location.address with fallback to district, city"
      contains: "location?.address"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/components/HospitalityCard.tsx"
      provides: "Browse-strip hospitality card addressDisplay prefers location.address (per CONTEXT.md Decision 11 + ROADMAP SC #6)"
      contains: "location?.address"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/components/PropertyCard.tsx"
      provides: "Browse property card addressDisplay (const at line ~74; used by handleShare) prefers location.address (per CONTEXT.md Decision 11 + ROADMAP SC #6)"
      contains: "location?.address"
  key_links:
    - from: "en.ts / ru.ts addressLabel etc."
      to: "Step2Location.tsx t('contextualListing.step2.addressLabel') calls (Plan 11-04)"
      via: "TranslationKeys union expansion — resolves Plan 11-04's `as TranslationKeys` casts to real union members"
      pattern: "contextualListing\\.step2\\.address"
    - from: "PropertyDetailsScreen addressDisplay (line 525)"
      to: "HeaderInfoCard formattedAddress + share/email/fullScreen surfaces"
      via: "Single derivation flows through 5+ consumer sites"
      pattern: "location\\?\\.address"
    - from: "HospitalityCard addressDisplay (line ~75)"
      to: "Hospitality strip browse card body + share message (line ~111)"
      via: "Single derivation drives the visible address line AND the share-message address line"
      pattern: "location\\?\\.address"
    - from: "PropertyCard addressDisplay (line ~74)"
      to: "Property-card share message (handleShare line ~116) — Row 3 MapPin label is INDEPENDENT and intentionally unchanged"
      via: "Const drives the share message text only; the visible body location row uses a separate inline expression by design"
      pattern: "location\\?\\.address"
    - from: "check-i18n-parity.sh"
      to: "RN-client jest pre-test hook (per M3 conventions)"
      via: "Parity gate exits 0; npm test sentinel chain green"
      pattern: "PASS.*parity"
---

<objective>
Three parallel surfaces, all small:
(A) Add 4 new EN+RU i18n keys for the Step 2 address input (label, placeholder, "not found" error, "looking up" hint) — locked text per 11-CONTEXT.md decision 10. Keep `scripts/check-i18n-parity.sh` green.
(B) Update the SINGLE addressDisplay derivation in `PropertyDetailsScreen.tsx` (line 525) to prefer `property.location?.address` when non-empty, falling back to the existing `[district, city]` synthesis. This change propagates automatically through `HeaderInfoCard`, share-link generation, email-body composition, and the full-screen map address line — no edits needed in those consumer sites.
(C) Apply the SAME precedence flip to the `addressDisplay` constants in BOTH browse cards (`HospitalityCard.tsx` line ~75 and `PropertyCard.tsx` line ~74), preserving each file's existing fallback separator. This closes CONTEXT.md Decision 11 ("PropertyDetailsScreen.tsx, HospitalityCard.tsx, HeaderInfoCard.tsx — prefer property.location.address when present") for the browse surfaces — without this, the listing strip + favorites + search results would continue to show district+city even on Phase-11 listings with a populated address.

Purpose: This is the READ-side of the round-trip. After this plan ships, a listing created in Step 2 with a typed address ("100 Manas Street") shows that address on the details screen, on the hospitality strip card, on the search results / favorites property card (in the share message), the share message, the email body, and the full-screen map address line. Pre-Phase-11 listings with empty address fall back to the existing district/city synthesis exactly as today — zero regression in the M5 Phase 1 details redesign layout or the browse-card visuals (per ROADMAP SC #6).

Output:
- 4 new keys × 2 locale files = 8 string additions.
- 1 one-line expression change in PropertyDetailsScreen.tsx line 525.
- 1 one-line expression change in HospitalityCard.tsx line ~75 (preserves `', '` separator).
- 1 one-line expression change in PropertyCard.tsx line ~74 (preserves `' · '` separator).
- 0 changes to HeaderInfoCard.tsx, MapPreviewCard.tsx, formatAddress.ts (analyzed and confirmed not needed).
- 0 changes to PropertyCard.tsx line ~291 inline JSX (Row 3 MapPin label is intentionally geographic-context-only, parallel to MapPreviewCard exclusion).
- 0 changes in any other file.

Depends on Plan 11-04 because Plan 11-04 used `as TranslationKeys` casts in `Step2Location.tsx` to compile against the 4 new keys before they existed; this plan adds the keys to the union and verifies the casts resolve to real union members (WARNING #1 from plan-check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/beckmaldinVL/development/mobileApps/JayTap/CLAUDE.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/PROJECT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/ROADMAP.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/codebase/CONVENTIONS.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-03-PLAN.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-04-PLAN.md

<interfaces>
<!-- Required reading: existing key namespace + addressDisplay derivation site + browse-card derivations. -->

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts lines 650–672, the existing `contextualListing.step2.*` namespace. The 4 new keys should be added in alphabetical order within this namespace, near the existing toggle/coordinate keys:
- Existing keys cover: title, cityLabel/Required/Other(.*), districtLabel/Required/districts.empty/disabled/districtOther(.*), mapLabel, coordinatesRequired, exactAddressToggle.
- The 4 new keys should be inserted in the order: `addressGeocoding`, `addressLabel`, `addressNotFound`, `addressPlaceholder` (alphabetical within the new `address*` subset). Placement: AFTER `cityLabel` block, BEFORE `districtLabel` block (alphabetical group respect), OR (simpler) at the end of the `step2.*` block right before `step3.*` starts — either works for the parity check. CHOOSE: append at the end of the contextualListing.step2.* block (after `exactAddressToggle` ~line 671) — this minimizes diff churn vs. interleaving into the middle of the block.

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts (per the i18n parity sentinel script, ru.ts mirrors en.ts key-set exactly): add the 4 new keys at the IDENTICAL position in ru.ts — same `addressGeocoding / addressLabel / addressNotFound / addressPlaceholder` order, same neighborhood (end of step2.* block).

`scripts/check-i18n-parity.sh` semantics (verified by reading the script):
- Greps `'<keyname>':` patterns from each file, sorts, diffs.
- Exit 0 iff EN and RU key sets are identical.
- Belt-and-suspenders to TypeScript: `ru.ts: Record<TranslationKeys, string>` enforces the same at compile time.

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx, the existing derivation site (lines 519–525):
```
const titleText = property.content?.title ?? '';
const descriptionText = property.content?.description ?? '';
const cityLabel = property.location?.city ?? '';
const districtLabel = property.location?.district ?? '';
// Address line synthesized from nested location (no flat property.address on M3 shape).
// Render slug-as-is — M4 owns dynamic-dictionary label lookup (Plan 02-05 D-10).
const addressDisplay = [districtLabel, cityLabel].filter(Boolean).join(', ');
```

Post-Phase-11 line 525 becomes:
```
const addressDisplay = (property.location?.address ?? '').trim() || [districtLabel, cityLabel].filter(Boolean).join(', ');
```
And update the comment above (lines 523–524) to read:
```
// Address line: prefer the geocoded Phase-11 location.address when present (typed by
// the lister in Step 2 / reverse-geocoded from pin drop), else fall back to the
// synthesized district + city slugs. Existing pre-Phase-11 listings have no
// location.address and continue to render via the fallback unchanged.
```

Downstream consumers of `addressDisplay` in PropertyDetailsScreen verified via grep (`grep -n "addressDisplay" src/screens/PropertyDetailsScreen.tsx`):
- Line 525: derivation (THIS PLAN edits).
- Line 542: `handleShare` — `${addressDisplay}` in share message — inherits change.
- Line 638: email body — `Address: ${addressDisplay}` — inherits change.
- Line 969: `formattedAddress={formatAddress(addressDisplay)}` — HeaderInfoCard prop — inherits change.
- Line 1385: full-screen map marker `description={addressDisplay}` — inherits change.
- Line 1401: full-screen map address text — inherits change.

NO direct edits needed in HeaderInfoCard.tsx, MapPreviewCard.tsx, formatAddress.ts, or any other PropertyDetailsScreen consumer — the precedence change at the single derivation point flows through all 5+ consumer surfaces.

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/HospitalityCard.tsx lines 70-75 (the browse-strip hospitality card body):
```
// Phase 2 D-20 nested-shape derivations (Phase 1 D-04..D-15).
// D-21 PRESERVED: top-level maxGuests + amenities reads stay verbatim.
const title = property.content?.title ?? '';
const cityLabel = property.location?.city ?? '';
const districtLabel = property.location?.district ?? '';
const addressDisplay = [districtLabel, cityLabel].filter(Boolean).join(', ');
```

Post-Phase-11 line 75 becomes:
```
const addressDisplay = (property.location?.address ?? '').trim() || [districtLabel, cityLabel].filter(Boolean).join(', ');
```
Existing fallback separator (`', '`) is preserved verbatim. The `addressDisplay` const flows into:
- Line ~111 `handleShare` — share message body.
- Line ~191 `formatAddress(addressDisplay)` — the visible address row under the title.

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/PropertyCard.tsx lines 69-77 (the search-results / favorites property card):
```
// Phase 2 D-20 nested-shape derivations (Phase 1 D-04..D-15)
const title = property.content?.title ?? '';
const heroPhoto = property.media?.photos?.[0];
const cityLabel = property.location?.city ?? '';
const districtLabel = property.location?.district ?? '';
const addressDisplay = [districtLabel, cityLabel].filter(Boolean).join(' · ');
// Tradeoff §K: hospitality strip uses dealType !== 'sale'; PropertyCard's M2 status
// badge maps the sale/rent split via the same rule so it renders correctly on M3 data.
const isSale = property.dealType === 'sale';
```

Post-Phase-11 line 74 becomes:
```
const addressDisplay = (property.location?.address ?? '').trim() || [districtLabel, cityLabel].filter(Boolean).join(' · ');
```
NOTE: existing separator here is `' · '` (space + bullet U+00B7 + space), NOT `', '`. PRESERVE the bullet separator in the fallback — pre-Phase-11 visuals must remain byte-identical.

The `addressDisplay` const in PropertyCard flows into ONE consumer:
- Line ~116 `handleShare` — share message body (`${title}\n${addressDisplay}\n${priceText}\n\n${shareUrl}`).

PropertyCard line ~283-294 contains an INDEPENDENT inline expression for the Row 3 MapPin + district · city visible label:
```
{(districtLabel || cityLabel) ? (
  <View style={styles.locationRow}>
    <MapPin size={14} color={colors.accent} strokeWidth={2.25} />
    <Text ... numberOfLines={1} ellipsizeMode="tail">
      {[districtLabel, cityLabel].filter(Boolean).join(' · ')}
    </Text>
  </View>
) : null}
```
This Row 3 expression is INTENTIONALLY NOT changed by this plan. Rationale (same as MapPreviewCard exclusion in PropertyDetailsScreen):
- The Row 3 MapPin label is the geographic-context label — semantically "where in the city is this listing" (district + city). It pairs visually with the MapPin icon to communicate area, not street address.
- Showing a full Nominatim displayName ("100 Manas Street, Bishkek, Kyrgyzstan") in this single-line truncated row would (a) duplicate the granular address shown elsewhere on the details screen, (b) lose the area-at-a-glance UX, (c) overflow more aggressively under `numberOfLines={1}`.
- The granular address is the responsibility of (i) the share message (`addressDisplay` const — THIS plan flips precedence), (ii) the PropertyDetailsScreen's address row (THIS plan flips precedence).
- Document this exclusion in the action block + audit comment so a future reader doesn't "fix" it.

NO direct edits needed in HeaderInfoCard.tsx, MapPreviewCard.tsx, formatAddress.ts — same rationale as PropertyDetailsScreen consumers above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 EN+RU i18n keys for Step 2 address input</name>
  <files>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts
  </files>
  <read_first>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/scripts/check-i18n-parity.sh
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
  </read_first>
  <action>
    PRE-FLIGHT: Verify `pwd` is `/Users/beckmaldinVL/development/mobileApps/JayTap` AND `git branch --show-current`.

    STEP 1 — Add 4 new keys to `src/locales/en.ts`:
    Locate the existing `contextualListing.step2.exactAddressToggle` key (currently around line 671). IMMEDIATELY AFTER that line, BEFORE the first `contextualListing.step3.*` key, insert the following 4 lines (alphabetical order WITHIN the new address* subset):

    ```
      'contextualListing.step2.addressGeocoding': 'Looking up address…',
      'contextualListing.step2.addressLabel': 'Street address',
      'contextualListing.step2.addressNotFound': "Couldn't find that address — drop a pin instead",
      'contextualListing.step2.addressPlaceholder': 'e.g. 100 Manas St',
    ```

    NOTE: The `addressNotFound` value uses DOUBLE quotes around the outer string because the inner value contains an apostrophe (`Couldn't`). This matches the existing convention in this file (look for analogous keys that contain apostrophes — `grep "n't" src/locales/en.ts` for examples) — if the file uses single quotes everywhere with escape sequences, switch to `'Couldn\\'t find that address — drop a pin instead'` instead. Confirm the existing pattern via `grep -E "'.*\\\\'.*'" src/locales/en.ts | head -3` AND `grep -E '".*'"'"'.*"' src/locales/en.ts | head -3` to determine which style the file uses; match it.

    NOTE: The em-dash characters `—` in `addressNotFound` are UTF-8 (U+2014), matching the project's punctuation conventions (verified in existing keys via `grep "—" src/locales/en.ts | head -3`). The ellipsis in `addressGeocoding` is the single-character `…` (U+2026), not three dots `...`.

    DO NOT add any other keys. DO NOT modify any existing key. DO NOT change the TranslationKeys union type — it's generated from the en.ts key set automatically via `ru.ts: Record<TranslationKeys, string>`.

    STEP 2 — Add the IDENTICAL 4 keys to `src/locales/ru.ts` AT THE SAME POSITION (locate `contextualListing.step2.exactAddressToggle` in ru.ts; same neighborhood):

    ```
      'contextualListing.step2.addressGeocoding': 'Поиск адреса…',
      'contextualListing.step2.addressLabel': 'Адрес',
      'contextualListing.step2.addressNotFound': 'Не удалось найти адрес — поставьте точку на карте',
      'contextualListing.step2.addressPlaceholder': 'напр. ул. Манаса 100',
    ```

    DO NOT translate differently from the LOCKED EN+RU text in 11-CONTEXT.md decision 10. The text strings above are user-facing and reviewed.

    STEP 3 — Run the i18n parity sentinel:
    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && bash scripts/check-i18n-parity.sh`
    Expected output: `PASS: FORM-09 key-set parity holds` (exit code 0).

    STEP 4 — Run TypeScript check to confirm `TranslationKeys` union expanded and `as TranslationKeys` casts in Step2Location.tsx (added by Plan 11-04) resolve cleanly:
    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -E "(Step2Location\.tsx|en\.ts|ru\.ts)" | wc -l`
    Expected: 0 new errors.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && bash scripts/check-i18n-parity.sh 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "contextualListing\.step2\.address" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts` returns 4.
    - Source assertion: `grep -c "contextualListing\.step2\.address" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts` returns 4.
    - Source assertion (key parity): both files have all 4 keys: `for k in addressGeocoding addressLabel addressNotFound addressPlaceholder; do grep -c "step2\\.$k" src/locales/en.ts; grep -c "step2\\.$k" src/locales/ru.ts; done | sort -u` returns just `1` (every per-key count in both files is 1).
    - Source assertion (locked text — EN): `grep -F "'Street address'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts | wc -l` returns ≥ 1; `grep -F "'Looking up address…'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts | wc -l` returns ≥ 1; `grep "Couldn" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts | grep "drop a pin" | wc -l` returns ≥ 1; `grep "100 Manas" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/en.ts | wc -l` returns ≥ 1.
    - Source assertion (locked text — RU): `grep -F "'Адрес'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts | wc -l` returns ≥ 1; `grep "Манаса 100" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts | wc -l` returns ≥ 1; `grep "Поиск адреса" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts | wc -l` returns ≥ 1; `grep "Не удалось найти" /Users/beckmaldinVL/development/mobileApps/JayTap/src/locales/ru.ts | wc -l` returns ≥ 1.
    - Parity sentinel: `bash scripts/check-i18n-parity.sh` exits 0 with `PASS:` line.
    - TSC delta: `npx tsc --noEmit 2>&1 | grep -E "(en\.ts|ru\.ts)" | wc -l` returns 0.
    - Pre-existing locale key count baseline preserved: `grep -cE "^\s*'[a-zA-Z.]+':" src/locales/en.ts` returns exactly 4 MORE than the pre-change baseline (capture via `git stash && grep -cE "^\s*'[a-zA-Z.]+':" src/locales/en.ts && git stash pop` if needed).
  </acceptance_criteria>
  <done>
    4 keys added to both en.ts + ru.ts with the LOCKED text from 11-CONTEXT.md; i18n parity sentinel green; TranslationKeys union covers the new keys (Plan 11-04's `as TranslationKeys` casts resolve cleanly).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PropertyDetailsScreen addressDisplay to prefer location.address</name>
  <files>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx
  </files>
  <read_first>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/utils/formatAddress.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/details/HeaderInfoCard.tsx
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/details/MapPreviewCard.tsx
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
  </read_first>
  <action>
    PRE-FLIGHT: Verify `pwd` AND `git branch --show-current`.

    STEP 1 — Re-confirm the addressDisplay derivation site BEFORE editing:
    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -n "addressDisplay" src/screens/PropertyDetailsScreen.tsx`
    Expected output: lines 525 (derivation), 542 (share), 638 (email), 969 (HeaderInfoCard prop), 1385 (full-screen map description), 1401 (full-screen map text). If any line numbers drifted ±10 lines, that's fine — the derivation site is uniquely identifiable as the `const addressDisplay = [districtLabel, cityLabel].filter(Boolean).join(', ');` line.

    STEP 2 — Edit `src/screens/PropertyDetailsScreen.tsx`:

    Replace lines 523–525 (the existing comment + derivation):
    ```
    // Address line synthesized from nested location (no flat property.address on M3 shape).
    // Render slug-as-is — M4 owns dynamic-dictionary label lookup (Plan 02-05 D-10).
    const addressDisplay = [districtLabel, cityLabel].filter(Boolean).join(', ');
    ```

    With:
    ```
    // Phase 11 GEO-06: address line prefers the geocoded `location.address` (typed by
    // the lister in Step 2 or reverse-geocoded from pin drop) when non-empty. Falls back
    // to the synthesized [district, city] slug pair when address is absent/empty (existing
    // pre-Phase-11 listings + listings where the lister never typed an address). M5 Phase 1
    // details redesign layout is preserved — only this one derivation flips precedence,
    // which propagates through HeaderInfoCard / share / email / fullScreenMap consumers.
    const addressDisplay = (property.location?.address ?? '').trim()
      || [districtLabel, cityLabel].filter(Boolean).join(', ');
    ```

    DO NOT touch any other line in PropertyDetailsScreen.tsx. Specifically:
    - DO NOT touch the `titleText / descriptionText / cityLabel / districtLabel / photos / tourUrl / videoUrl` derivations on lines 519–528.
    - DO NOT touch `handleShare`, the email body, the HeaderInfoCard JSX, MapPreviewCard JSX, or any specs/KeyStatsCard block (M4 + M5 Phase 1 territory).
    - DO NOT modify the `cityLabel` / `districtLabel` derivations — keep them as-is so the fallback synthesis behavior is byte-identical to pre-change.

    DO NOT edit `HeaderInfoCard.tsx`, `MapPreviewCard.tsx`, `formatAddress.ts`, or any other file. They consume `addressDisplay` (via props) and inherit the new precedence automatically.

    Rationale (also documented in the must_haves block):
    - HeaderInfoCard: line 969 passes `formattedAddress={formatAddress(addressDisplay)}`. The fix at the source propagates.
    - MapPreviewCard: line 989 passes `district + city` directly — independent of address; renders district/city always. Correct behavior (map card shows geographic context, not granular street).
    - formatAddress: pure function on a string input. Address-vs-synthesized switch happens before this function is called; formatAddress logic doesn't need awareness of the source.

    STEP 3 — TypeScript verification:
    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep "PropertyDetailsScreen\.tsx" | wc -l`
    Expected: 0 (zero new tsc errors on this file; the pre-existing App.tsx Property.tours errors are NOT on this file).

    STEP 4 — Regression smoke (manual but easy — render no-op verification):
    Confirm pre-Phase-11 listings (no `location.address`) render identically:
    - The expression `(undefined ?? '').trim()` → `''` → falsy → falls back to `[districtLabel, cityLabel].filter(Boolean).join(', ')` → same as today.
    - The expression `('' ?? '').trim()` → `''` → falsy → same fallback.
    - The expression `('100 Manas Street' ?? '').trim()` → `'100 Manas Street'` → truthy → prefers the address.
    - The expression `('  ' ?? '').trim()` → `''` → falsy → falls back (whitespace-only address is treated as empty).

    This satisfies ROADMAP SC #3 ("Existing M3 listings with no `address` continue to load and render") and SC #6 ("No regression in M5 Phase 1 layout").
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep "src/screens/PropertyDetailsScreen\.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "property\.location?\.address" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx` returns ≥ 1 (the new precedence read).
    - Source assertion: derivation site still uses district + city fallback — `grep -c "\\[districtLabel, cityLabel\\].filter(Boolean).join" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx` returns 1 (preserved as fallback).
    - Git scope check: `cd /Users/beckmaldinVL/development/mobileApps/JayTap && git diff --stat src/screens/PropertyDetailsScreen.tsx | tail -1` shows a tiny change — expect ~3–8 lines changed total (1 expression edit + comment expansion). If the diff is larger than ~15 lines on this file, halt and re-review.
    - File-boundary check: `cd /Users/beckmaldinVL/development/mobileApps/JayTap && git diff --stat src/components/details/ src/utils/formatAddress.ts` shows ZERO files changed (no edits in detail-card components or formatAddress.ts).
    - TSC delta: `npx tsc --noEmit 2>&1 | grep "src/screens/PropertyDetailsScreen\.tsx" | wc -l` returns 0.
    - Pre-existing App.tsx error count unchanged: `npx tsc --noEmit 2>&1 | grep "App\.tsx.*Property\.tours" | wc -l` returns 3 (STATE.md row 51 baseline).
  </acceptance_criteria>
  <done>
    addressDisplay derivation prefers `location.address` when present, falls back to district+city; all 5+ downstream consumers inherit the change without per-site edits; M5 Phase 1 layout preserved bit-for-bit; tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply addressDisplay precedence flip to BOTH browse cards (HospitalityCard + PropertyCard) — closes CONTEXT.md Decision 11 for browse surfaces</name>
  <files>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/HospitalityCard.tsx
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/PropertyCard.tsx
  </files>
  <read_first>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/HospitalityCard.tsx
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/PropertyCard.tsx
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/utils/formatAddress.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
  </read_first>
  <action>
    PRE-FLIGHT: Verify `pwd` is `/Users/beckmaldinVL/development/mobileApps/JayTap` AND `git branch --show-current`.

    Context: CONTEXT.md Decision 11 reads "PropertyDetailsScreen.tsx, HospitalityCard.tsx, HeaderInfoCard.tsx — prefer property.location.address when present; fall back to existing [district, city].filter(Boolean).join(', ') synthesis. No regression in the M5 Phase 1 details redesign layout." Task 2 covers PropertyDetailsScreen.tsx. HeaderInfoCard.tsx is covered transitively (receives `addressDisplay` as prop). HospitalityCard.tsx must be edited directly because it derives its own `addressDisplay`. PropertyCard.tsx is part of the same browse-surface group (ROADMAP SC #6 "downstream renderers using location text") and uses the same own-derivation pattern.

    STEP 1 — Re-confirm BOTH derivation sites BEFORE editing:
    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -n "addressDisplay" src/components/HospitalityCard.tsx`
    Expected output: line ~75 (derivation), line ~111 (handleShare), line ~191 (formatAddress prop). Line numbers may drift ±5 lines — the derivation site is uniquely identifiable as `const addressDisplay = [districtLabel, cityLabel].filter(Boolean).join(', ');`.

    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -n "addressDisplay" src/components/PropertyCard.tsx`
    Expected output: line ~74 (derivation, separator is `' · '`), line ~116 (handleShare). Note PropertyCard's derivation uses a DIFFERENT separator (`' · '`) than HospitalityCard's (`', '`). Preserve each file's existing separator in the fallback.

    STEP 2A — Edit `src/components/HospitalityCard.tsx`:
    Replace the existing line 75 (derivation):
    ```
    const addressDisplay = [districtLabel, cityLabel].filter(Boolean).join(', ');
    ```
    With (PRESERVING the existing `', '` separator in the fallback):
    ```
    // Phase 11 GEO-06 / CONTEXT.md Decision 11: prefer geocoded location.address (Step 2
    // typed or reverse-geocoded) when non-empty; fall back to district+city synthesis for
    // pre-Phase-11 listings or empty-address listings. Fallback separator preserved
    // verbatim (`, `) so pre-Phase-11 browse-strip cards render byte-identically.
    const addressDisplay = (property.location?.address ?? '').trim()
      || [districtLabel, cityLabel].filter(Boolean).join(', ');
    ```

    DO NOT touch any other line in HospitalityCard.tsx. The derivation flows into:
    - Line ~111 `handleShare` — share message body (inherits precedence flip automatically).
    - Line ~191 `formatAddress(addressDisplay)` — visible address row (inherits precedence flip automatically).

    STEP 2B — Edit `src/components/PropertyCard.tsx`:
    Replace the existing line 74 (derivation):
    ```
    const addressDisplay = [districtLabel, cityLabel].filter(Boolean).join(' · ');
    ```
    With (PRESERVING the existing `' · '` bullet separator in the fallback):
    ```
    // Phase 11 GEO-06 / CONTEXT.md Decision 11: prefer geocoded location.address (Step 2
    // typed or reverse-geocoded) when non-empty; fall back to district+city synthesis for
    // pre-Phase-11 listings or empty-address listings. Fallback separator preserved
    // verbatim (` · ` bullet) so pre-Phase-11 share messages render byte-identically.
    // INTENTIONAL EXCLUSION: the Row 3 MapPin label JSX at line ~291 keeps its own inline
    // [districtLabel, cityLabel].filter(Boolean).join(' · ') expression — that row is the
    // geographic-context label (district + city area-at-a-glance), parallel to
    // MapPreviewCard's role on PropertyDetailsScreen. Granular street address lives in the
    // share message (which uses THIS const) and on PropertyDetailsScreen, not in the
    // single-line truncated Row 3.
    const addressDisplay = (property.location?.address ?? '').trim()
      || [districtLabel, cityLabel].filter(Boolean).join(' · ');
    ```

    DO NOT touch any other line in PropertyCard.tsx. Specifically:
    - DO NOT touch the Row 3 MapPin + district·city JSX at line ~291 — it is INTENTIONALLY independent of the precedence flip (geographic-context label only; see the inline comment above).
    - DO NOT touch `heroPhoto`, `isSale`, `cityLabel`, `districtLabel` derivations (lines 70-77).
    - DO NOT touch `handleShare` (line ~109) — it inherits the new precedence via `addressDisplay`.

    STEP 3 — TypeScript verification:
    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -E "(HospitalityCard|PropertyCard)\.tsx" | wc -l`
    Expected: 0 new errors.

    STEP 4 — Regression smoke (expression evaluation — same as Task 2 logic):
    - HospitalityCard pre-Phase-11: `(undefined ?? '').trim()` → falsy → fallback `'district, city'` → unchanged.
    - HospitalityCard post-Phase-11 with address: `('100 Manas Street, Bishkek' ?? '').trim()` → truthy → shows full Nominatim displayName.
    - PropertyCard pre-Phase-11: `(undefined ?? '').trim()` → falsy → fallback `'district · city'` (bullet) → byte-identical to today.
    - PropertyCard post-Phase-11 with address: share message includes full displayName.

    Browse-surface coverage gap from CONTEXT.md Decision 11 is now closed (PropertyDetailsScreen + HospitalityCard + PropertyCard's share text all prefer `location.address` when present; HeaderInfoCard inherits transitively).
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -E "src/components/(HospitalityCard|PropertyCard)\.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (HospitalityCard precedence wired): `grep -c "property\.location?\.address" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/HospitalityCard.tsx` returns ≥ 1.
    - Source assertion (HospitalityCard fallback separator preserved): `grep -c "\\[districtLabel, cityLabel\\].filter(Boolean).join(', ')" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/HospitalityCard.tsx` returns 1.
    - Source assertion (PropertyCard precedence wired on the const): `grep -c "property\.location?\.address" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/PropertyCard.tsx` returns ≥ 1.
    - Source assertion (PropertyCard fallback bullet separator preserved): `grep -c "\\[districtLabel, cityLabel\\].filter(Boolean).join(' · ')" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/PropertyCard.tsx` returns 2 — one for the const fallback (line ~74) and one for the Row 3 inline JSX (line ~291, intentionally unchanged).
    - Source assertion (browse-surface coverage count): `grep -lE "property\\.location\\?\\.address" /Users/beckmaldinVL/development/mobileApps/JayTap/src/screens/PropertyDetailsScreen.tsx /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/HospitalityCard.tsx /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/PropertyCard.tsx | wc -l` returns 3 (all three surfaces wired — closes CONTEXT.md Decision 11 + ROADMAP SC #6).
    - File-boundary check: `cd /Users/beckmaldinVL/development/mobileApps/JayTap && git diff --stat src/components/details/ src/utils/formatAddress.ts` shows ZERO files changed.
    - Git scope check (HospitalityCard): `git diff --stat src/components/HospitalityCard.tsx | tail -1` shows ~3–10 lines changed.
    - Git scope check (PropertyCard): `git diff --stat src/components/PropertyCard.tsx | tail -1` shows ~3–12 lines changed (slightly larger due to the intentional-exclusion comment block).
    - TSC delta: `npx tsc --noEmit 2>&1 | grep -E "src/components/(HospitalityCard|PropertyCard)\.tsx" | wc -l` returns 0.
    - Pre-existing App.tsx error count unchanged: `npx tsc --noEmit 2>&1 | grep "App\.tsx.*Property\.tours" | wc -l` returns 3.
  </acceptance_criteria>
  <done>
    HospitalityCard.tsx + PropertyCard.tsx both flip `addressDisplay` to prefer `location.address` with per-file fallback separator preserved (`', '` for HospitalityCard, `' · '` for PropertyCard); Row 3 MapPin label in PropertyCard intentionally unchanged with audit comment; tsc clean; browse-surface coverage of CONTEXT.md Decision 11 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend response → PropertyDetailsScreen render | `property.location.address` is already sanitized at the schema layer (Plan 11-01: trim + maxlength 200). Renders into `<Text>` (React Native — no HTML interpretation, no XSS risk). |
| Backend response → HospitalityCard / PropertyCard render (browse surfaces) | Same trust boundary as above; the precedence flip applies the same address value (server-sanitized) to the browse-card body / share message. |
| Locale string → t() render | EN+RU strings are static, reviewed text; no user-supplied interpolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-21 | Tampering | i18n key collision with existing keys | mitigate | All 4 new keys use the established `contextualListing.step2.address*` prefix that does not collide with any existing key (verified via `grep "address" src/locales/en.ts` — current matches reference `emailAddress` only, in a different namespace). Parity sentinel + TypeScript `Record<TranslationKeys, string>` are belt-and-suspenders against accidental collision. |
| T-11-22 | Information Disclosure | addressDisplay rendered into share message + email body (PropertyDetailsScreen + HospitalityCard + PropertyCard share messages) | accept | The address string is whatever the lister chose to display publicly. Same risk surface as the existing `district, city` synthesis; this plan does not widen the disclosed information beyond what the lister opted to enter. |
| T-11-23 | Tampering | property.location.address coming from backend with no client-side validation | accept | Plan 11-01 + Plan 11-02 enforce server-side `trim` + `maxlength: 200`. RN client trusts the backend response. No HTML/script execution surface (RN `<Text>` is plain-text). |
| T-11-24 | DoS | Long addressDisplay overflowing single-line UI | mitigate | HeaderInfoCard already enforces `numberOfLines={1}` + `ellipsizeMode="tail"` on the address row (lines 71–72 of HeaderInfoCard.tsx). HospitalityCard's address row (line ~194/198) already enforces `numberOfLines={1}` + `ellipsizeMode="tail"`. PropertyCard's share message has no length cap but `address` is bounded to 200 chars by backend. 200-char addresses truncate gracefully on all three surfaces. |
| T-11-25 | Tampering | Future contributor "fixes" the PropertyCard Row 3 MapPin label to also use location.address | mitigate | The audit comment block above PropertyCard line ~74 explicitly documents the intentional exclusion of the Row 3 inline JSX. Code review on any PR touching PropertyCard.tsx Row 3 should reference Plan 11-05 Task 3 + CONTEXT.md Decision 11 + the MapPreviewCard parallel before reaching consensus. |

No new attack surface added by this plan — pure read-path widening across three surfaces.
</threat_model>

<verification>
Phase-wide verification this plan contributes to:

1. **i18n parity sentinel** — `bash scripts/check-i18n-parity.sh` exits 0 (ROADMAP SC #5).
2. **TypeScript clean** — `npx tsc --noEmit` shows zero new errors across all four edited files; Plan 11-04's `as TranslationKeys` casts now resolve cleanly without the cast (but the cast is inert, so leave them).
3. **WARNING #1 closure (`as TranslationKeys` cast verification)**: After this plan lands, confirm `as TranslationKeys` casts in `Step2Location.tsx` (added by Plan 11-04 as a temporary workaround while the keys didn't yet exist) resolve to REAL union members. Grep for the casts and verify each one targets one of the 4 new keys added by Task 1:
   ```
   cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -n 'as TranslationKeys' src/components/ContextualListingFlow/Step2Location.tsx
   ```
   Each cast site should reference `contextualListing.step2.addressLabel`, `…addressPlaceholder`, `…addressNotFound`, or `…addressGeocoding`. If any cast site references a key NOT in the post-Task-1 union, that's a Plan 11-04 typo — flag and fix in a Plan 11-07 hotfix, do NOT silently let the cast mask it. The reason this plan depends on 11-04 (per the updated `depends_on: [11-03, 11-04]`) is precisely so this cross-check is meaningful — Plan 11-04's casts must exist for this verification to be useful.
4. **Backwards compatibility** — pre-Phase-11 listings without `location.address` render IDENTICALLY to today via the per-file fallback (PropertyDetailsScreen uses `, `; HospitalityCard uses `, `; PropertyCard uses `' · '` bullet) (ROADMAP SC #3 + SC #6).
5. **Browse-surface coverage** — three derivation sites (PropertyDetailsScreen line 525 + HospitalityCard line ~75 + PropertyCard line ~74) all prefer `location.address`. Grep gate: `grep -l "property\\.location?\\.address" src/{screens/PropertyDetailsScreen,components/HospitalityCard,components/PropertyCard}.tsx | wc -l` returns 3.
6. **5+ downstream consumer surfaces inherit the change on details screen** — share message, email body, HeaderInfoCard, full-screen map (description + bottom text) — all read from the single `addressDisplay` const in PropertyDetailsScreen.
7. **Browse-card share messages inherit the change** — HospitalityCard line ~111 + PropertyCard line ~116 both read `addressDisplay` for the share-sheet body.
8. **No edits to M5 Phase 1 components** — `git diff --stat src/components/details/` returns empty.
9. **Parallel-safe with Plan 11-04** — Plans 11-04 + 11-05 modify disjoint file sets (Plan 11-04: Step2Location.tsx + geocodeService.ts; Plan 11-05: en.ts + ru.ts + PropertyDetailsScreen.tsx + HospitalityCard.tsx + PropertyCard.tsx). Wave 3 + 11-05 depends on 11-04 only for the cast-resolution cross-check (not for file conflicts).
</verification>

<success_criteria>
This plan succeeds when:
- 4 EN keys + 4 RU keys added with the LOCKED text from 11-CONTEXT.md decision 10.
- `scripts/check-i18n-parity.sh` exits 0.
- `PropertyDetailsScreen` addressDisplay prefers `location.address` (when non-empty after trim) with fallback to `[district, city]` synthesis.
- `HospitalityCard` addressDisplay (line ~75) prefers `location.address` with fallback to district+city using `', '` separator (preserved).
- `PropertyCard` addressDisplay const (line ~74) prefers `location.address` with fallback to district+city using `' · '` separator (preserved); Row 3 MapPin label JSX intentionally unchanged with audit comment.
- No edits to HeaderInfoCard.tsx, MapPreviewCard.tsx, formatAddress.ts, or any detail-card consumer.
- TSC clean; pre-existing App.tsx error count unchanged.
- Plan 11-04's `as TranslationKeys` casts in Step2Location.tsx all resolve to real union members (WARNING #1 closure).
- M5 Phase 1 details layout preserved bit-for-bit (visual + structural).
- Browse-surface coverage of CONTEXT.md Decision 11 closed (3 derivation sites wired).
- Plan 11-06 can walk the QA matrix knowing all three browse/display surfaces are live.
</success_criteria>

<output>
After completion, create `.planning/phases/11-listing-address-geocode/11-05-SUMMARY.md` covering:
- Files modified (en.ts + ru.ts + PropertyDetailsScreen.tsx + HospitalityCard.tsx + PropertyCard.tsx).
- New EN key count delta + new RU key count delta (both = +4).
- i18n parity sentinel output (PASS line).
- PropertyDetailsScreen.tsx diff size (expect ~3–8 lines).
- HospitalityCard.tsx diff size (expect ~3–10 lines).
- PropertyCard.tsx diff size (expect ~3–12 lines, including the intentional-exclusion comment block).
- Confirmation that HeaderInfoCard / MapPreviewCard / formatAddress untouched (`git diff --stat` excerpt).
- `as TranslationKeys` cast resolution result from `grep -n 'as TranslationKeys' src/components/ContextualListingFlow/Step2Location.tsx` — list each cast site + the key it now references.
- Browse-surface coverage grep result (the 3-file `wc -l` check).
- Commit SHA in the RN client repo.
- Any deviations from the LOCKED text in 11-CONTEXT.md decision 10 (none expected — text is verbatim).
- Any deviations from the per-file fallback separator preservation (none expected — separators are preserved per-file).
</output>
</content>
</invoke>