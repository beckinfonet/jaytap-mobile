---
phase: 11-listing-address-geocode
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/types/Property.ts
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/types.ts
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/adapters.ts
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/validators.ts
autonomous: true
requirements:
  - GEO-03
tags:
  - rn-client
  - types
  - adapters
  - formbag

must_haves:
  truths:
    - "`src/types/Property.ts` declares `address?: string` inside the nested `location?` block, ALONGSIDE the existing `city`, `district`, `coordinates`, `showExactAddress` paths (per 11-CONTEXT.md decision 7)."
    - "`src/components/ContextualListingFlow/types.ts` declares `address: string` (REQUIRED string, default `''`) inside `FormBag.location`, alongside the existing `city`, `district`, `coordinates`, `showExactAddress` paths (per 11-CONTEXT.md decision 6). The FormBag uses a required string (default `''`) because all FormBag scalar fields use that convention — empty-string discriminates from `undefined` in the adapter."
    - "`emptyFormBag()` in `validators.ts` initializes `location.address: ''` so create-mode forms start with a clean empty value."
    - "`propertyToFormBag(p)` in `adapters.ts` maps `p.location?.address ?? ''` to `FormBag.location.address` — round-trips existing listings that have `address: ''` (post-Phase-11 listings) and existing listings without the field (pre-Phase-11 listings) identically."
    - "`formBagToPropertyPayload(v)` in `adapters.ts` maps `v.location.address` through to `payload.location.address` verbatim (no trim, no truncation — backend Mongoose `trim: true` + `maxlength: 200` are authoritative; client passes string as-typed)."
    - "`validateStep(2, ...)` in `validators.ts` does NOT require `location.address` (the field is optional per 11-CONTEXT.md decision — Step 2's existing required set stays `{city, district, coordinates}`). NO new validation rule is added for `address` at this phase (a user can submit a listing with an empty address; the field is informational, not gating)."
    - "FIELD_ORDER_PER_STEP[2] in `validators.ts` appends `'location.address'` AFTER `'location.coordinates'` so any future address validation error (if added) scrolls into view in the correct order."
    - "`npx tsc --noEmit` from the RN client root reports zero NEW TypeScript errors attributable to these 4 file changes (pre-existing errors unchanged)."
  artifacts:
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/types/Property.ts"
      provides: "Optional address?: string under nested location"
      contains: "address?: string"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/types.ts"
      provides: "Required address: string under FormBag.location (default '' via emptyFormBag)"
      contains: "address: string"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/adapters.ts"
      provides: "address round-trip in both adapter directions"
      contains: "address"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/validators.ts"
      provides: "emptyFormBag().location.address = '' + FIELD_ORDER_PER_STEP[2] includes location.address"
      contains: "address"
  key_links:
    - from: "FormBag.location.address (types.ts)"
      to: "Step2Location.tsx TextInput in Plan 11-04"
      via: "SectionProps onChange('location', { ...values.location, address: newText })"
      pattern: "address:\\s*string"
    - from: "Property.location.address (Property.ts)"
      to: "Plan 11-05 downstream display read-path (HeaderInfoCard / MapPreviewCard / formatAddress consumers)"
      via: "property.location?.address ?? synthesize(district, city) fallback"
      pattern: "address\\?:\\s*string"
    - from: "formBagToPropertyPayload (adapters.ts)"
      to: "Backend POST/PUT /api/properties (Plan 11-01 schema)"
      via: "payload.location.address → Mongoose location.address"
      pattern: "address:\\s*v\\.location\\.address"
---

<objective>
Add the `address` field to the RN-client TypeScript type stubs that flow through the create-listing pipeline: (1) optional `address?: string` on the persisted `Property.location` interface, (2) required `address: string` (default `''`) on the in-flight `FormBag.location` shape, (3) round-trip mapping in both adapter directions, (4) defensive `emptyFormBag()` default + `FIELD_ORDER_PER_STEP[2]` entry in `validators.ts`. NO new validation rule is added — `address` is informational and optional at submit time (an empty string is acceptable). This is a purely type-level / glue plan; no UI work and no behavior change for users who don't touch the new input (Plan 11-04 owns the UI).

Purpose: Without these type-stub additions, Plan 11-04's `<TextInput value={values.location.address}>` would TS-error at every reference site; Plan 11-05's downstream display read-path would TS-error at `property.location.address`. This plan creates the silent foundation that subsequent plans build UI and display on top of.

Output:
- 1 new optional field on `Property.location` (`Property.ts`).
- 1 new required field on `FormBag.location` (`types.ts`).
- 2 adapter additions (one per direction in `adapters.ts`).
- 2 surgical edits to `validators.ts` (default + field-order entry).
- Zero behavior change for existing listings; zero new validation gates.
- Runs in PARALLEL with Plan 11-01 (no shared files — wave 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/beckmaldinVL/development/mobileApps/JayTap/CLAUDE.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/PROJECT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/ROADMAP.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/debug/listing-address-geocode.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Existing patterns the executor must replicate verbatim — extracted so no scavenger-hunt is needed. -->

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/types/Property.ts lines 37–43, the current nested `location?` block:
```
location?: {
  city?: string;
  district?: string;
  coordinates?: { lat: number; lng: number };
  showExactAddress?: boolean;
};
```
The new `address?: string` line is added INSIDE this block, AFTER `showExactAddress`, BEFORE the closing `};`. All 4 existing fields stay byte-identical.

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/types.ts lines 14–20, the current `FormBag.location` block:
```
location: {
  city: string; // city slug
  district: string; // district slug
  coordinates: { lat: number; lng: number } | null;
  showExactAddress: boolean; // forced true if propertyType ∈ {hotel, hostel} per D-07
};
```
The new `address: string` line is added INSIDE this block, AFTER `showExactAddress`, BEFORE the closing `};`. Per the FormBag convention (M1 + M2 + M3 precedent), FormBag scalars are REQUIRED strings with empty-string defaults — NOT optional. The optionality lives on the persisted Property type only.

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/adapters.ts lines 21–28, the current `propertyToFormBag` location block:
```
location: {
  city: p.location?.city ?? '',
  district: p.location?.district ?? '',
  coordinates: p.location?.coordinates
    ? { lat: p.location.coordinates.lat, lng: p.location.coordinates.lng }
    : null,
  showExactAddress: p.location?.showExactAddress ?? false,
},
```
Add one new line AFTER `showExactAddress`: `address: p.location?.address ?? '',`.

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/adapters.ts lines 85–90, the current `formBagToPropertyPayload` location block:
```
location: {
  city: v.location.city,
  district: v.location.district,
  coordinates: v.location.coordinates ?? undefined,
  showExactAddress: v.location.showExactAddress,
},
```
Add one new line AFTER `showExactAddress`: `address: v.location.address || undefined,` — empty string becomes `undefined` so we don't write `address: ''` to backend; backend Mongoose default is `''` so the absence and the empty string are equivalent on persistence (this is the same pattern used for `currency: v.basics.currency || undefined` on line 94).

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/validators.ts lines 36–43, current `emptyFormBag()`:
```
return {
  dealType: '',
  propertyType: '',
  location: { city: '', district: '', coordinates: null, showExactAddress: false },
  basics: { areaSqm: '', price: '', currency: '' },
  conditionAndAmenities: { condition: '', furnished: null, amenities: [] },
  content: { title: '', description: '', language: 'ru' },
  terms: {},
};
```
Change the `location` line to: `location: { city: '', district: '', coordinates: null, showExactAddress: false, address: '' },`.

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/validators.ts lines 14–32, current `FIELD_ORDER_PER_STEP`:
```
2: ['location.city', 'location.district', 'location.coordinates'],
```
Change to: `2: ['location.city', 'location.district', 'location.coordinates', 'location.address'],`.

DO NOT add a Step 2 validation rule for `address` in `validateStep()` lines 52–56 — per 11-CONTEXT.md `<decisions>` the address field is OPTIONAL and submit must succeed with an empty string. Adding a validation rule here would silently break the existing happy-path (a user who never types an address would now fail Step 2 validation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add address field to Property.location + FormBag.location + adapters + validators defaults</name>
  <files>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/types/Property.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/types.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/adapters.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/validators.ts
  </files>
  <read_first>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/types/Property.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/types.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/adapters.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/validators.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
  </read_first>
  <action>
    PRE-FLIGHT (RN client repo, no nvm switch needed — RN client runs on default Node v20): Verify `pwd` is `/Users/beckmaldinVL/development/mobileApps/JayTap` AND `git branch --show-current` returns the expected branch BEFORE any edit.

    STEP 1 — `src/types/Property.ts` addition:
    Inside the `location?: { ... };` block (currently lines 37–43), AFTER the `showExactAddress?: boolean;` line and BEFORE the closing `};`, add ONE new field declaration:

    `address?: string; // Phase 11 GEO-03 — Nominatim displayName persisted as free-form string. Optional; backend default ''.`

    DO NOT touch any other field in the file. Specifically: do NOT touch `basics?.bedrooms / bathroomCount` (M4 Phase 6); do NOT touch the M2/M3 audit fields; do NOT touch `PlatformVerifications`.

    STEP 2 — `src/components/ContextualListingFlow/types.ts` addition:
    Inside the `FormBag.location` block (currently lines 15–20), AFTER the `showExactAddress: boolean;` line and BEFORE the closing `};`, add ONE new field declaration:

    `address: string; // Phase 11 GEO-03 — required-but-defaults-to-'' per FormBag convention. Plan 11-04 Step2Location TextInput writes this.`

    DO NOT make this field optional (`address?: string`). Per the FormBag convention (every scalar string field — `city`, `district`, `dealType`, `propertyType`, etc. — is required with `''` default), `address: string` matches. The adapter handles the optionality boundary by converting `''` → `undefined` on the way out.

    DO NOT touch the discriminated-union `ContextualListingFlowProps` block at the bottom of the file.

    STEP 3 — `src/components/ContextualListingFlow/adapters.ts` two-direction round-trip:

    Edit A — `propertyToFormBag` block (lines 21–28):
    Add ONE new line AFTER `showExactAddress: p.location?.showExactAddress ?? false,`:
    `address: p.location?.address ?? '',`

    Edit B — `formBagToPropertyPayload` block (lines 85–90):
    Add ONE new line AFTER `showExactAddress: v.location.showExactAddress,`:
    `address: v.location.address || undefined,`

    The `|| undefined` coercion matches the existing convention on line 94 (`currency: v.basics.currency || undefined`) — empty string becomes `undefined` so the JSON-serialized payload omits the key, which the backend Mongoose schema treats as "use default ''". This avoids spuriously overwriting a populated `address` with `''` on a partial PUT.

    DO NOT touch the basics / conditionAndAmenities / content / terms / amenities blocks. Specifically: do NOT touch the M4 `bedrooms` / `bathroomCount` verbatim-passthrough lines (M4 Phase 6 + 7) or the v4.0.1 amenities intersection.

    STEP 4 — `src/components/ContextualListingFlow/validators.ts` two surgical edits:

    Edit A — `FIELD_ORDER_PER_STEP[2]` (line 16):
    Change `2: ['location.city', 'location.district', 'location.coordinates'],` to
    `2: ['location.city', 'location.district', 'location.coordinates', 'location.address'],`

    Edit B — `emptyFormBag()` location line (line 38):
    Change `location: { city: '', district: '', coordinates: null, showExactAddress: false },` to
    `location: { city: '', district: '', coordinates: null, showExactAddress: false, address: '' },`

    DO NOT add a Step 2 validation rule for `address` in `validateStep()` (line 52–56 region). The field is optional per 11-CONTEXT.md; submitting with `address: ''` MUST continue to advance from Step 2. The FIELD_ORDER_PER_STEP entry exists purely for future-proofing — if Plan 11-04 or a later phase adds an inline validation hint (e.g., maxlength client-side), the scroll-to-error logic will know where the field lives in the visual order.

    STEP 5 — TypeScript verification:
    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | tee /tmp/tsc-11-03.log`

    Compare against pre-change baseline. The expected NEW-error count is ZERO. Pre-existing errors (per STATE.md row 51, 3 `App.tsx` `Property.tours` errors) MUST remain unchanged in count and location.

    The Step 2 type additions to FormBag may surface as ONE legitimate-needs-attention error: Step2Location.tsx currently does NOT pass `address` to its `onChange('location', ...)` calls (verified in this plan's interfaces block — lines 138, 145, 153–158, 166–172, 207, 216). Because FormBag.location.address is REQUIRED (not optional), TypeScript will flag those callsites as missing the `address` property. THAT IS EXPECTED at the end of this plan; Plan 11-04 fixes them all by either (a) including `...values.location` first (which preserves the existing `address` from FormBag state via emptyFormBag's `''` default), or (b) the new TextInput onChange handler that adds `address` explicitly.

    Run a focused grep BEFORE finalizing the TS-error count assertion:
    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -E "Step2Location\.tsx" | wc -l`
    Expected: 6+ errors on Step2Location.tsx, each one a "Property 'address' is missing in type" error referencing `FormBag['location']`. THESE ARE EXPECTED — Plan 11-04 closes them.

    Confirm by grepping the Step2Location.tsx callsites to verify each `onChange('location', {...values.location, X})` already spreads `...values.location` first:
    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -nE "onChange\(['\"]location['\"]" src/components/ContextualListingFlow/Step2Location.tsx`

    If all 6 existing callsites spread `...values.location` (verified in the interfaces block — they do: lines 138, 145, 153, 167, 207, 216 all start with `{ ...values.location, ... }`), then NO Step2Location.tsx changes are needed at runtime — the spread preserves `address` from FormBag state. The TS errors are spurious because TS doesn't narrow the spread's contribution; they'll need a one-line fix in Plan 11-04 (or, alternatively, this plan can include the typecast fix; per the file boundary policy, this plan defers it to Plan 11-04 since Plan 11-04 owns Step2Location.tsx).

    EXCEPTION POLICY: if the TS error count on files OTHER than Step2Location.tsx grows by even 1, halt and surface — that would indicate a downstream consumer that needs accommodating (likely something reading `property.location` via destructuring with missing-field expectations).

    Report the exact TS diagnostic delta in the summary.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -vE "Step2Location\.tsx|App\.tsx" | grep -cE "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "address?:\s*string" /Users/beckmaldinVL/development/mobileApps/JayTap/src/types/Property.ts` returns ≥ 1.
    - Source assertion: `grep -cE "^\s*address:\s*string;" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/types.ts` returns ≥ 1 (required, not optional).
    - Source assertion: `grep -c "p\.location?\.address" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/adapters.ts` returns ≥ 1.
    - Source assertion: `grep -c "v\.location\.address" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/adapters.ts` returns ≥ 1.
    - Source assertion: `grep "address: ''" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/validators.ts | wc -l` returns ≥ 1 (emptyFormBag default).
    - Source assertion: `grep "'location.address'" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/validators.ts | wc -l` returns ≥ 1 (FIELD_ORDER_PER_STEP[2] entry).
    - Source assertion: existing Property.ts location fields preserved — `grep -cE "^\s*(city|district|coordinates|showExactAddress)\?:" /Users/beckmaldinVL/development/mobileApps/JayTap/src/types/Property.ts` returns ≥ 4.
    - Source assertion: existing FormBag.location fields preserved — `grep -cE "^\s*(city|district|coordinates|showExactAddress):" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/types.ts` returns ≥ 4.
    - Source assertion: NO new `validateStep` rule added — `grep -c "step2\.addressRequired\|address.*Required" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/validators.ts` returns 0.
    - TSC delta: `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -vE "Step2Location\.tsx|App\.tsx" | grep -cE "error TS"` returns 0 (zero NEW errors on files outside Step2Location.tsx; pre-existing App.tsx errors filtered).
    - TSC expected-error confirmation: `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -E "Step2Location\.tsx" | wc -l` returns ≥ 1 (the expected Step2Location.tsx errors that Plan 11-04 will close — confirm the executor saw these and did NOT accidentally make `FormBag.location.address` optional to silence them).
    - Source assertion: NO Step2Location.tsx changes — `cd /Users/beckmaldinVL/development/mobileApps/JayTap && git diff --stat src/components/ContextualListingFlow/Step2Location.tsx` shows zero lines changed (Plan 11-04 owns this file exclusively).
  </acceptance_criteria>
  <done>
    All 4 RN-client files carry the new `address` field per their respective conventions; `emptyFormBag()` and `FIELD_ORDER_PER_STEP` updated; no new validation rule added; tsc shows the expected Step2Location.tsx errors that Plan 11-04 will close (no spurious errors elsewhere).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → backend `/api/properties` POST/PUT — `location.address` field | Plan 11-01 owns the backend-side validation. This plan only adds the client-side type stub; no runtime behavior change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Information Disclosure | TypeScript type stub additions | accept | Type stubs are build-time documentation only — no runtime data exposure. Adding one optional `string` field cannot leak PII. |
| T-11-14 | Tampering | adapters.ts `formBagToPropertyPayload` address mapping | accept | The adapter forwards user-typed text verbatim to the payload. Backend Mongoose `trim: true` + `maxlength: 200` (Plan 11-01) are the authoritative sanitizers. Client-side trim would be redundant and risks divergence from server semantics. |
| T-11-15 | DoS | validators.ts FIELD_ORDER_PER_STEP[2] array growth | accept | Adding one string to a 3-element array has zero runtime impact. The scroll-to-error logic iterates the array linearly; O(1) growth. |

No new attack surface added by this plan — purely type-level + adapter glue.
</threat_model>

<verification>
Phase-wide verification this plan contributes to:

1. **TypeScript diagnostic delta on RN client** — net-new errors outside Step2Location.tsx + pre-existing App.tsx errors: 0. Step2Location.tsx errors are expected and tracked as the close-out for Plan 11-04.
2. **No behavior change for users who don't touch the new field** — the empty-string default in `emptyFormBag()` + the `|| undefined` coercion in `formBagToPropertyPayload` ensures pre-existing create-listing flows behave identically.
3. **Cross-repo commit hygiene** — Plan 11-03 commits land in the RN client repo only. Confirm `cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && git status --short` shows zero modified files at task end.
4. **Parallel-safe with Plan 11-01** — files_modified do not overlap with Plan 11-01's backend files; wave 1 parallelism preserved.
</verification>

<success_criteria>
This plan succeeds when:
- `Property.location.address?: string` declared.
- `FormBag.location.address: string` declared (required, not optional).
- Both adapters round-trip the field.
- `emptyFormBag()` defaults `address: ''`.
- `FIELD_ORDER_PER_STEP[2]` includes `'location.address'`.
- No new `validateStep(2, ...)` rule for `address` (field is optional at submit per 11-CONTEXT.md).
- `npx tsc --noEmit` shows zero new errors EXCEPT in Step2Location.tsx (expected; Plan 11-04 closes those).
- Plans 11-04 (UI wiring) and 11-05 (downstream display) can proceed with the types in place.
</success_criteria>

<output>
After completion, create `.planning/phases/11-listing-address-geocode/11-03-SUMMARY.md` covering:
- Files modified in the RN client repo (4 files).
- TSC diagnostic delta (before → after); explicit count of Step2Location.tsx expected errors (to be closed by Plan 11-04).
- Confirmation that NO Step2Location.tsx changes were made (file boundary respected).
- Commit SHA in the RN client repo.
- Any deviations from the LOCKED decisions 6, 7, 8 in 11-CONTEXT.md (none expected).
</output>
