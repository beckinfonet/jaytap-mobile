---
phase: 11-listing-address-geocode
plan: 04
type: execute
wave: 3
depends_on:
  - 11-02
  - 11-03
files_modified:
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx
autonomous: true
requirements:
  - GEO-01
  - GEO-02
tags:
  - rn-client
  - ui
  - geocoder
  - step2

must_haves:
  truths:
    - "NEW file `src/services/geocodeService.ts` exports `geocodeAddress({ address, citySlug, lang })` (returns Promise<{ lat, lng, displayName } | null>) and `reverseGeocode({ lat, lng, lang })` (returns Promise<{ displayName } | null>) — both call the apiClient against `/locations/geocode` + `/locations/reverse-geocode` and return null on any non-2xx (rather than throwing) so the caller's debounced-input code path can swallow failures gracefully (per 11-CONTEXT.md decision 5)."
    - "Step2Location.tsx renders a `<TextInput>` with `testID='step2-address-input'` that is visible ONLY when `showExactAddress === true` OR `isHospitality === true` (per 11-CONTEXT.md decision 9 first bullet — gating semantics match the existing toggle policy: hotel/hostel force showExactAddress=true so the input is always shown for them)."
    - "Typing into the address input debounces by 300ms after the user stops typing, then calls `geocodeAddress({ address, citySlug: values.location.city, lang })` and, on success, calls `onChange('location', { ...values.location, address: result.displayName, coordinates: { lat: result.lat, lng: result.lng } })` — moves the pin AND updates the persisted address to the canonical Nominatim displayName (per 11-CONTEXT.md decision 9 second bullet)."
    - "On forward-geocode failure (null result OR network error), the TextInput value remains the user-typed text (NOT overwritten with displayName), the pin does NOT move, and an inline error label rendered below the input shows `t('contextualListing.step2.addressNotFound')` until the user types again (per 11-CONTEXT.md decision 9 fifth bullet — anti-'random pin' defense layer 3; Claude's Discretion fourth bullet — inline label not toast)."
    - "While geocoding is in flight, an inline `<ActivityIndicator>` (small) renders next to the input with `t('contextualListing.step2.addressGeocoding')` label (per 11-CONTEXT.md decision 9 fourth bullet)."
    - "`handleMapPress` and `handleMarkerDragEnd` in Step2Location.tsx call `reverseGeocode({ lat, lng, lang })` AFTER writing coordinates to FormBag. On success AND when current `values.location.address.trim() === ''`, set `address: result.displayName` via `onChange('location', { ... })`. NEVER overwrites typed text (per 11-CONTEXT.md decision 9 third bullet)."
    - "Reverse geocode failures are silent (no error label, no toast) — pin drop's primary purpose is coordinates capture; the address fill is opportunistic."
    - "Both forward and reverse geocode calls pass `lang: language` from `useLanguage()` so backend `Accept-Language` matches the UI locale (EN or RU per ROADMAP SC #4)."
    - "Existing Step2Location.tsx callsites that already spread `...values.location` (lines 138, 145, 153, 167, 207, 216 per pre-plan-11-03 audit) continue to spread the (now-required) `address` field — no manual additions needed beyond the new TextInput and the two new geocode-trigger code paths."
    - "`npx tsc --noEmit` from the RN client root reports zero NEW errors (the Step2Location.tsx errors surfaced by Plan 11-03 are CLOSED by this plan)."
    - "Debounce mechanism uses a single `useRef<ReturnType<typeof setTimeout> | null>` cleared in a cleanup effect to avoid race conditions where a stale geocode response stomps a newer user keystroke (per Claude's Discretion first bullet — simplest mechanism)."
    - "Forward geocode is NOT triggered when `values.location.city` is empty (cannot compute viewbox bias without a city; degrades to country-wide search but the helper still works). The geocode call fires once a city is selected — DECISION: still fire even without city (the backend route degrades gracefully per Plan 11-02 must_have #5). No additional client-side gate beyond non-empty trimmed input + min length 3."
  artifacts:
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts"
      provides: "Client-side wrappers for /locations/geocode + /locations/reverse-geocode"
      contains: "export async function geocodeAddress"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts"
      provides: "Reverse geocode client wrapper"
      contains: "export async function reverseGeocode"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx"
      provides: "Address TextInput + debounced forward geocode + reverse geocode on pin events"
      contains: "step2-address-input"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx"
      provides: "Inline ActivityIndicator + addressNotFound error label"
      contains: "addressGeocoding"
  key_links:
    - from: "geocodeService.ts geocodeAddress"
      to: "Backend POST /api/locations/geocode (Plan 11-02)"
      via: "apiClient.post('/locations/geocode', { address, citySlug, lang })"
      pattern: "apiClient\\.post.*locations/geocode"
    - from: "Step2Location.tsx TextInput onChangeText"
      to: "geocodeService.geocodeAddress (debounced via setTimeout ref)"
      via: "debouncedGeocode setter + 300ms delay"
      pattern: "setTimeout.*300"
    - from: "Step2Location.tsx handleMapPress / handleMarkerDragEnd"
      to: "geocodeService.reverseGeocode"
      via: "fire-and-forget after coordinate write"
      pattern: "reverseGeocode\\("
    - from: "Step2Location.tsx error label"
      to: "i18n key contextualListing.step2.addressNotFound (Plan 11-05)"
      via: "t('contextualListing.step2.addressNotFound')"
      pattern: "contextualListing\\.step2\\.address(NotFound|Geocoding|Label|Placeholder)"
---

<objective>
Build the client-facing surface for Phase 11: (1) NEW `src/services/geocodeService.ts` that wraps the backend routes from Plan 11-02 with apiClient-based calls + null-on-failure semantics, (2) extend `src/components/ContextualListingFlow/Step2Location.tsx` with an address `<TextInput>` under the "Show exact address" toggle (or always-shown for hotel/hostel), debounced 300ms forward-geocode wired to `onChange('location', {...})`, reverse-geocode triggered from `handleMapPress` / `handleMarkerDragEnd` with empty-text guard, inline ActivityIndicator + inline error label for the addressNotFound state, and lang-sourced-from-useLanguage propagation.

Purpose: This is the user-visible delivery of the phase. After this plan ships, a user creating a listing in Step 2 can type "100 Manas Street" and watch the pin auto-place; they can also drop a pin and watch the address field fill in. All three layers of the anti-"random pin" defense (countrycodes + viewbox + on-failure-don't-move-pin) live in this plan's failure-handling code paths plus the backend stack from Plans 11-01/11-02.

Output:
- 1 NEW file `geocodeService.ts` (~80–120 LOC, mirrors `locationService.ts` style).
- ~120–180 LOC delta to Step2Location.tsx (new TextInput JSX + 2 debounced-geocode useEffects/handlers + reverse-geocode wiring inside the 2 existing pin handlers + import additions).
- Zero changes to any other file. i18n keys are added in Plan 11-05 (parallel wave 3 plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/beckmaldinVL/development/mobileApps/JayTap/CLAUDE.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/PROJECT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/ROADMAP.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/codebase/CONVENTIONS.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/debug/listing-address-geocode.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-02-PLAN.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-03-PLAN.md

<cross_repo_note>
RN client only. No backend touches. Plan 11-02 + Plan 11-03 must be committed BEFORE starting this plan:
- Plan 11-02 provides the backend HTTP contract this plan calls.
- Plan 11-03 provides the FormBag.location.address type that Step2Location reads/writes.
Verify both are merged: `cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -c "address?:" src/types/Property.ts` returns ≥ 1 (Plan 11-03 landed); confirm Plan 11-02's commit SHA exists in the backend repo log per the orchestrator-tracked dependency.
</cross_repo_note>

<interfaces>
<!-- Required reading patterns extracted for the executor. -->

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/locationService.ts (entire file, 133 lines), the canonical apiClient service pattern this repo uses:
- Named exports `export async function fetchCities(): Promise<City[]>` style (NOT `export const X = { ... }` object pattern — older M1/M2 services use the object pattern; M3 Phase 2 introduced the named-function pattern for new services).
- `apiClient.get<{ cities: City[] }>('/locations/cities')` — typed `apiClient` generic on the response payload shape.
- Error handling: most reads throw on non-2xx (axios default); writes catch 409 and return a structured `{ status: 409, ... }` shape. For Plan 11-04 GEO-01/02 the helper MUST swallow ALL non-2xx and return `null` (the consumer is a debounced text input — throwing pollutes RN logs on every miskey).

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/apiClient.ts lines 17–40, the apiClient definition:
- Already attaches Bearer token via request interceptor.
- baseURL is `https://jaytap-services-production.up.railway.app/api` — so service-layer paths are RELATIVE (`/locations/geocode`, NOT `/api/locations/geocode`).
- `timeout: 15000` (15s) — fine; the backend's own 5s AbortController fires first.

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/context/LanguageContext.tsx (use via `import { useLanguage } from '../../context/LanguageContext'`), the `language` value is `'en' | 'ru'` — exactly what the backend route expects.

From /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx, the existing structure to extend:

(a) Imports block (lines 17–42): add `import { geocodeAddress, reverseGeocode } from '../../services/geocodeService';` after the existing `locationService` import (line 34–40). React imports already pull in `useRef`, `useState`, `useEffect`, `useCallback` — only add `useRef` if not already present (verify).

(b) `isHospitality` derived (line 69): `const isHospitality = values.propertyType === 'hotel' || values.propertyType === 'hostel';` — reuse verbatim. NEW: derive `showAddressInput = values.location.showExactAddress || isHospitality` (computed at the same site).

(c) `handleMapPress` (lines 151–162) and `handleMarkerDragEnd` (lines 165–176): after the existing `onChange('location', { ...values.location, coordinates: { lat, lng } })` write, fire a reverse-geocode (best-effort; don't await). Pseudo:
```
reverseGeocode({ lat: e.nativeEvent.coordinate.latitude, lng: e.nativeEvent.coordinate.longitude, lang: language })
  .then((r) => {
    if (r && r.displayName && !values.location.address?.trim()) {
      onChange('location', { ...values.location, coordinates: { lat, lng }, address: r.displayName });
    }
  })
  .catch(() => { /* swallow per service contract — already returns null */ });
```
NOTE: this triggers a follow-up onChange which Step2Location's parent ContextualListingFlow batches into next render. The `values.location.address?.trim()` guard reads the SNAPSHOT at the time of the callback, NOT at click time — so a user who typed an address between the tap and the reverse-geocode response will NOT have their text clobbered. The closed-over `values` variable in useCallback is the click-time snapshot — to read the latest value, EITHER (i) include `values.location.address` in the useCallback deps + accept the re-render churn (current pattern in this file already does this for `values.location`), OR (ii) use a ref-tracked latest value. Pattern (i) matches the file's existing style — use it.

(d) Address `<TextInput>` JSX: render INSIDE the existing `{!isHospitality ? (toggle) : null}` block — NO, that's the wrong gate (the toggle is hidden for hospitality). The address input should render when `showAddressInput === true` (showExactAddress OR isHospitality). Placement: directly BELOW the toggle (`<View testID="exact-address-toggle-container">` on lines 446–464), OR for hospitality where the toggle is hidden, BELOW the map but in its own conditional wrapper. To match the existing file's section pattern, wrap in `{showAddressInput ? (<View style={commonStyles.section}>...</View>) : null}`.

(e) The TextInput shape (reusing modal-input styling per 11-CONTEXT.md decision 9 first bullet — look at lines 494–510 for the existing `<TextInput testID="other-input-ru">` style for visual reference; reuse the same border/padding/colors via `useTheme()` tokens):
```
<View style={commonStyles.section}>
  <Text style={[commonStyles.sectionLabel, { color: colors.text }]}>
    {t('contextualListing.step2.addressLabel')}
  </Text>
  <View style={{ position: 'relative' }}>
    <TextInput
      testID="step2-address-input"
      value={addressInput}
      onChangeText={(v) => { setAddressInput(v); scheduleGeocode(v); }}
      placeholder={t('contextualListing.step2.addressPlaceholder')}
      placeholderTextColor={colors.textSecondary}
      style={{
        borderWidth: 1,
        borderColor: isDark ? '#3A3A3C' : '#E5E5EA',
        borderRadius: 8,
        padding: 12,
        color: colors.text,
        backgroundColor: colors.surface,
      }}
      autoCorrect={false}
      autoCapitalize="words"
    />
    {geocodingState === 'loading' ? (
      <View style={{ position: 'absolute', right: 12, top: 12, flexDirection: 'row', alignItems: 'center' }}>
        <ActivityIndicator size="small" />
      </View>
    ) : null}
  </View>
  {geocodingState === 'loading' ? (
    <Text style={{ color: colors.textSecondary, marginTop: 4, fontSize: 12 }}>
      {t('contextualListing.step2.addressGeocoding')}
    </Text>
  ) : null}
  {geocodingState === 'notFound' ? (
    <Text testID="address-not-found-error" style={[commonStyles.errorText, { color: colors.error }]}>
      {t('contextualListing.step2.addressNotFound')}
    </Text>
  ) : null}
</View>
```

(f) New local state to add INSIDE the `Step2Location` function body (near the other `useState` declarations on lines 54–67):
```
const [addressInput, setAddressInput] = useState<string>(values.location.address ?? '');
const [geocodingState, setGeocodingState] = useState<'idle' | 'loading' | 'notFound'>('idle');
const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null);
```
And a sync effect to handle prop-driven address changes (e.g. when reverse-geocode updates `values.location.address` via the parent's re-render — keep the local input mirror in sync):
```
useEffect(() => {
  setAddressInput(values.location.address ?? '');
  // intentional: do NOT include addressInput in deps — this effect listens to PARENT prop changes only.
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [values.location.address]);
```

(g) `scheduleGeocode` callback:
```
const scheduleGeocode = useCallback((text: string) => {
  if (debounceRef.current) clearTimeout(debounceRef.current);
  if (!text.trim() || text.trim().length < 3) {
    setGeocodingState('idle');
    return;
  }
  setGeocodingState('loading');
  debounceRef.current = setTimeout(async () => {
    const result = await geocodeAddress({
      address: text.trim(),
      citySlug: values.location.city || undefined,
      lang: language as 'en' | 'ru',
    });
    if (result) {
      setGeocodingState('idle');
      onChange('location', {
        ...values.location,
        address: result.displayName,
        coordinates: { lat: result.lat, lng: result.lng },
      });
      setAddressInput(result.displayName); // canonicalize on success
    } else {
      setGeocodingState('notFound');
      // explicitly DO NOT touch coordinates or location.address — preserves user text + static pin (anti-"random pin")
    }
  }, 300);
}, [onChange, values.location, language]);
```

(h) Cleanup effect to clear pending timer on unmount:
```
useEffect(() => {
  return () => {
    if (debounceRef.current) clearTimeout(debounceRef.current);
  };
}, []);
```

(i) IMPORTANT — the existing `useEffect` block on lines 73–80 already covers the `isHospitality → force showExactAddress=true` policy. Do NOT touch it.

(j) The 6 existing `onChange('location', { ...values.location, X })` callsites (lines 138, 145, 153–158, 167–172, 207, 216) already spread `...values.location` — meaning `address` (now a required FormBag field per Plan 11-03) is preserved automatically. No edits needed at these callsites BEYOND adding the reverse-geocode trigger to lines 151 and 165 (handleMapPress + handleMarkerDragEnd).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create geocodeService.ts client wrapper (NEW file)</name>
  <files>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts
  </files>
  <read_first>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/locationService.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/apiClient.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-02-PLAN.md
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
  </read_first>
  <action>
    PRE-FLIGHT: Verify `pwd` is `/Users/beckmaldinVL/development/mobileApps/JayTap` AND `git branch --show-current`. Confirm Plan 11-02 committed by checking the backend repo's most recent log (orchestrator-tracked dependency); confirm Plan 11-03 committed in this repo by `grep -c "address?:" src/types/Property.ts` returning ≥ 1.

    Create `src/services/geocodeService.ts` with the following exact structure (mirroring locationService.ts conventions):

    Top-of-file JSDoc comment block (4–8 lines):
    - Phase 11 GEO-01 + GEO-02 — client wrappers for the backend `/locations/geocode` + `/locations/reverse-geocode` routes added in Plan 11-02.
    - Null-on-failure contract: BOTH functions return `null` instead of throwing — caller is debounced UI code that should swallow per-keystroke failures silently. Differs from locationService.ts where most reads throw on non-2xx.
    - Backend timeout is 5s (AbortController); apiClient timeout is 15s — backend always fires first.
    - Anti-"random pin" layer 3: caller is responsible for NOT moving the map pin on null returns (see Step2Location.tsx in this plan's Task 2).

    Imports (mirror locationService.ts):
    `import { apiClient } from './apiClient';`

    Exported types:
    ```
    export interface GeocodeResult {
      lat: number;
      lng: number;
      displayName: string;
    }

    export interface ReverseGeocodeResult {
      displayName: string;
    }

    export interface GeocodeOptions {
      address: string;
      citySlug?: string;
      lang?: 'en' | 'ru';
    }

    export interface ReverseGeocodeOptions {
      lat: number;
      lng: number;
      lang?: 'en' | 'ru';
    }
    ```

    Exported function `geocodeAddress`:
    ```
    /**
     * Forward geocode an address string to lat/lng + canonical displayName.
     * Returns null on any non-2xx (400 ADDRESS_REQUIRED, 404 NOT_FOUND, 5xx, network timeout).
     * Caller MUST treat null as "preserve user text, do NOT move pin" (anti-"random pin" defense).
     */
    export async function geocodeAddress(opts: GeocodeOptions): Promise<GeocodeResult | null> {
      try {
        const r = await apiClient.post<GeocodeResult>('/locations/geocode', {
          address: opts.address,
          citySlug: opts.citySlug,
          lang: opts.lang ?? 'en',
        });
        if (r.status !== 200 || !r.data || typeof r.data.lat !== 'number' || typeof r.data.lng !== 'number') {
          return null;
        }
        return { lat: r.data.lat, lng: r.data.lng, displayName: r.data.displayName ?? '' };
      } catch (err: any) {
        // Swallow per the null-on-failure contract. Log at debug level for ops.
        console.warn('geocodeAddress failed:', err?.response?.status ?? err?.message ?? 'unknown');
        return null;
      }
    }
    ```

    Exported function `reverseGeocode`:
    ```
    /**
     * Reverse geocode lat/lng to a canonical address displayName.
     * Returns null on any non-2xx. Caller MUST guard against overwriting user-typed
     * address text — the address fill is opportunistic (Step2Location pin-drop handler).
     */
    export async function reverseGeocode(opts: ReverseGeocodeOptions): Promise<ReverseGeocodeResult | null> {
      try {
        const r = await apiClient.post<ReverseGeocodeResult>('/locations/reverse-geocode', {
          lat: opts.lat,
          lng: opts.lng,
          lang: opts.lang ?? 'en',
        });
        if (r.status !== 200 || !r.data || typeof r.data.displayName !== 'string') {
          return null;
        }
        return { displayName: r.data.displayName };
      } catch (err: any) {
        console.warn('reverseGeocode failed:', err?.response?.status ?? err?.message ?? 'unknown');
        return null;
      }
    }
    ```

    DO NOT add a third helper. DO NOT add caching. DO NOT add retry logic. DO NOT add request-cancellation via AbortController on the client side (the backend's 5s AbortController + apiClient's 15s timeout are sufficient; client cancellation is deferred unless QA surfaces a need).

    Run `npx tsc --noEmit 2>&1 | grep "geocodeService\.ts" | wc -l` — expect 0 errors on the new file.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && test -f src/services/geocodeService.ts && npx tsc --noEmit 2>&1 | grep "src/services/geocodeService.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts`.
    - Source assertion: `grep -c "export async function geocodeAddress" /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts` returns 1.
    - Source assertion: `grep -c "export async function reverseGeocode" /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts` returns 1.
    - Source assertion: `grep -c "apiClient.post" /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts` returns ≥ 2.
    - Source assertion: `grep -E "(throw|reject\()" /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts | wc -l` returns 0 (null-on-failure contract; no throws).
    - Source assertion: `grep -c "return null" /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts` returns ≥ 2 (one per function failure path; may be more for different failure branches).
    - TSC delta: `npx tsc --noEmit 2>&1 | grep "src/services/geocodeService.ts" | wc -l` returns 0.
  </acceptance_criteria>
  <done>
    New `geocodeService.ts` file ships both functions with null-on-failure contract; zero new tsc errors on the new file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire address TextInput + debounced forward + reverse-on-pin into Step2Location.tsx</name>
  <files>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx
  </files>
  <read_first>
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx
    /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/geocodeService.ts
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/11-listing-address-geocode/11-CONTEXT.md
    /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/debug/listing-address-geocode.md
  </read_first>
  <action>
    PRE-FLIGHT: Verify `pwd` AND `git branch --show-current`. Confirm Task 1 file exists.

    Edit `src/components/ContextualListingFlow/Step2Location.tsx`:

    STEP 1 — Imports block additions:
    Add to the existing React import line (line 17): if `useRef` is not already imported, add it: `import React, { useState, useEffect, useCallback, useMemo, useRef } from 'react';`.
    Verify `TextInput` and `ActivityIndicator` are already imported from 'react-native' (they are: line 23 + line 27 per the interfaces section above).
    Add ONE new import after the locationService import block (after line 40):
    `import { geocodeAddress, reverseGeocode } from '../../services/geocodeService';`

    STEP 2 — Inside the Step2Location function body, after the existing `isHospitality` derived constant (line 69), add THREE new state hooks and a debounce ref (PLACE these near the top of the function body, before `useEffect` blocks):
    ```
    const showAddressInput = values.location.showExactAddress || isHospitality;
    const [addressInput, setAddressInput] = useState<string>(values.location.address ?? '');
    const [geocodingState, setGeocodingState] = useState<'idle' | 'loading' | 'notFound'>('idle');
    const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null);
    ```

    STEP 3 — Add the prop-sync useEffect (AFTER the existing isHospitality→showExactAddress effect that ends ~line 80):
    ```
    // Mirror parent-driven address changes (e.g. reverse-geocode fill via handleMapPress)
    // into local input state. Local-typed updates flow via onChangeText → scheduleGeocode →
    // onChange (parent). This effect closes the loop in the parent→child direction so the
    // input doesn't go stale when reverse-geocode populates an empty address field.
    useEffect(() => {
      setAddressInput(values.location.address ?? '');
      // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [values.location.address]);
    ```

    STEP 4 — Add the debounce cleanup effect (AFTER the prop-sync effect):
    ```
    useEffect(() => {
      return () => {
        if (debounceRef.current) clearTimeout(debounceRef.current);
      };
    }, []);
    ```

    STEP 5 — Add the `scheduleGeocode` callback (after `handleSelectDistrict` ~line 148, before `handleMapPress` ~line 151):
    ```
    // Phase 11 GEO-01 — debounced forward geocode (300ms after typing pauses).
    // On success: write BOTH address (canonical displayName) AND coordinates to FormBag,
    // re-center the map via the marker re-render. On failure: preserve typed text, DO NOT
    // move pin (anti-"random pin" defense; see 11-CONTEXT.md <specifics> "THREE layers").
    const scheduleGeocode = useCallback(
      (text: string) => {
        if (debounceRef.current) clearTimeout(debounceRef.current);
        const trimmed = text.trim();
        if (!trimmed || trimmed.length < 3) {
          setGeocodingState('idle');
          return;
        }
        setGeocodingState('loading');
        debounceRef.current = setTimeout(async () => {
          const result = await geocodeAddress({
            address: trimmed,
            citySlug: values.location.city || undefined,
            lang: language as 'en' | 'ru',
          });
          if (result) {
            setGeocodingState('idle');
            onChange('location', {
              ...values.location,
              address: result.displayName,
              coordinates: { lat: result.lat, lng: result.lng },
            });
            // canonicalize the input mirror — the prop-sync effect would do this too, but
            // setting it inline avoids the one-frame flash of pre-canonical text.
            setAddressInput(result.displayName);
          } else {
            // explicitly preserve typed text + static pin
            setGeocodingState('notFound');
          }
        }, 300);
      },
      [onChange, values.location, language],
    );
    ```

    STEP 6 — Extend `handleMapPress` (lines 151–162) to fire reverse-geocode after the coordinate write:
    Wrap the existing body — keep the `onChange('location', ...)` write FIRST (immediate pin update), then fire-and-forget the reverse-geocode:
    ```
    const handleMapPress = useCallback(
      (e: { nativeEvent: { coordinate: { latitude: number; longitude: number } } }) => {
        const lat = e.nativeEvent.coordinate.latitude;
        const lng = e.nativeEvent.coordinate.longitude;
        onChange('location', {
          ...values.location,
          coordinates: { lat, lng },
        });
        // Phase 11 GEO-02 — best-effort reverse geocode. Only fills address if currently empty
        // (never clobbers typed text). Silent on failure (no toast, no error label) — pin-drop
        // primary purpose is coordinates, address fill is opportunistic.
        reverseGeocode({ lat, lng, lang: language as 'en' | 'ru' }).then((r) => {
          if (r && r.displayName && !(values.location.address ?? '').trim()) {
            onChange('location', {
              ...values.location,
              coordinates: { lat, lng },
              address: r.displayName,
            });
            setAddressInput(r.displayName);
          }
        });
      },
      [onChange, values.location, language],
    );
    ```

    STEP 7 — Extend `handleMarkerDragEnd` (lines 165–176) identically — same body shape, same guard:
    ```
    const handleMarkerDragEnd = useCallback(
      (e: { nativeEvent: { coordinate: { latitude: number; longitude: number } } }) => {
        const lat = e.nativeEvent.coordinate.latitude;
        const lng = e.nativeEvent.coordinate.longitude;
        onChange('location', {
          ...values.location,
          coordinates: { lat, lng },
        });
        reverseGeocode({ lat, lng, lang: language as 'en' | 'ru' }).then((r) => {
          if (r && r.displayName && !(values.location.address ?? '').trim()) {
            onChange('location', {
              ...values.location,
              coordinates: { lat, lng },
              address: r.displayName,
            });
            setAddressInput(r.displayName);
          }
        });
      },
      [onChange, values.location, language],
    );
    ```

    STEP 8 — Add the address input JSX. INSERT this block INSIDE the return-JSX tree, placed AFTER the existing exact-address-toggle block (current lines 446–465) — render conditional on `showAddressInput`:
    ```
    {showAddressInput ? (
      <View style={commonStyles.section} testID="step2-address-input-section">
        <Text style={[commonStyles.sectionLabel, { color: colors.text }]}>
          {t('contextualListing.step2.addressLabel')}
        </Text>
        <View style={{ position: 'relative' }}>
          <TextInput
            testID="step2-address-input"
            value={addressInput}
            onChangeText={(v) => {
              setAddressInput(v);
              scheduleGeocode(v);
            }}
            placeholder={t('contextualListing.step2.addressPlaceholder')}
            placeholderTextColor={colors.textSecondary}
            style={{
              borderWidth: 1,
              borderColor: isDark ? '#3A3A3C' : '#E5E5EA',
              borderRadius: 8,
              padding: 12,
              paddingRight: 36,
              color: colors.text,
              backgroundColor: colors.surface,
            }}
            autoCorrect={false}
            autoCapitalize="words"
          />
          {geocodingState === 'loading' ? (
            <View
              style={{
                position: 'absolute',
                right: 12,
                top: 12,
              }}
              testID="step2-address-geocoding-spinner"
            >
              <ActivityIndicator size="small" />
            </View>
          ) : null}
        </View>
        {geocodingState === 'loading' ? (
          <Text style={{ color: colors.textSecondary, marginTop: 4, fontSize: 12 }}>
            {t('contextualListing.step2.addressGeocoding')}
          </Text>
        ) : null}
        {geocodingState === 'notFound' ? (
          <Text
            testID="step2-address-not-found-error"
            style={[commonStyles.errorText, { color: colors.error }]}
          >
            {t('contextualListing.step2.addressNotFound')}
          </Text>
        ) : null}
      </View>
    ) : null}
    ```

    DO NOT modify the existing exact-address-toggle block — the new section renders AFTER it, gated on the same effective predicate (showAddressInput).
    DO NOT modify the existing city / district / map blocks.
    DO NOT modify the existing Modal at the bottom of the file (Other-city/district submission).
    DO NOT add a "use my current location" button (deferred per 11-CONTEXT.md `<deferred>`).
    DO NOT add caching, debounce-cancellation on city change, or AbortController to the client-side geocode call (deferred unless QA flags a need).

    STEP 9 — TypeScript verification:
    `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | tee /tmp/tsc-11-04.log`

    Expected:
    - Zero NEW errors on Step2Location.tsx (the Plan 11-03 expected errors are now CLOSED).
    - Zero NEW errors on any other file.
    - Pre-existing App.tsx `Property.tours` errors remain (per STATE.md row 51 — unchanged count).

    STEP 10 — i18n key sentinel:
    The 4 t() calls reference keys (`contextualListing.step2.addressLabel`, `addressPlaceholder`, `addressGeocoding`, `addressNotFound`) that Plan 11-05 adds. Until Plan 11-05 lands, `npx tsc --noEmit` will surface TS errors on `t('contextualListing.step2.addressLabel' as TranslationKeys)` because the key isn't in the TranslationKeys union. To avoid blocking on Plan 11-05, cast each new key to `as TranslationKeys` at the call site:
    ```
    t('contextualListing.step2.addressLabel' as TranslationKeys)
    ```
    This matches the existing pattern in the file (see lines 312, 440 — existing `t(errors['location.city'] as TranslationKeys)` style for dynamic keys).

    Alternatively, since the orchestrator's wave structure runs Plans 11-04 and 11-05 in PARALLEL (both wave 3), the executor may instead choose to coordinate: ensure Plan 11-05 lands within the same atomic wave so the cast is unnecessary. The simpler-and-safer choice is the `as TranslationKeys` cast inline — Plan 11-05's job already includes the key additions, but tsc check at the END of this plan should not fail.

    DECISION (locked for this plan): use `as TranslationKeys` casts so this plan is independently committable. Plan 11-05 adds the actual keys and can drop the casts in a cleanup if desired (or leave them — the casts are inert once the keys exist).
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -E "(Step2Location\.tsx|geocodeService\.ts)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "step2-address-input" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx` returns ≥ 1 (TextInput testID present).
    - Source assertion: `grep -c "geocodeAddress\|reverseGeocode" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx` returns ≥ 3 (1 import + 1 forward call + 2 reverse calls = 4; allow 3+ for resiliency).
    - Source assertion: `grep -c "setTimeout(async" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx` returns ≥ 1 (debounce setup).
    - Source assertion: `grep -c "300)" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx` returns ≥ 1 (300ms debounce literal).
    - Source assertion: `grep -c "showAddressInput" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx` returns ≥ 2 (derived const + JSX gate).
    - Source assertion: `grep -c "addressLabel\|addressPlaceholder\|addressNotFound\|addressGeocoding" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx` returns ≥ 4 (all 4 new i18n keys referenced).
    - Source assertion: anti-clobber guard present — `grep -c "values\.location\.address.*trim\|address ?? '').trim()" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx` returns ≥ 2 (one per pin handler).
    - Source assertion: KBD-02 invariant preserved — `grep -c "keyboardVerticalOffset" /Users/beckmaldinVL/development/mobileApps/JayTap/src/components/ContextualListingFlow/Step2Location.tsx` returns 0 (memory m1-keyboard-kbd-02-invariants.md hard rule across `src/`; this plan must NOT introduce one).
    - Project-wide KBD-02 grep gate preserved — `cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -rc "keyboardVerticalOffset" src/ | grep -v ":0$" | wc -l` returns 0.
    - TSC delta: `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep -E "(Step2Location\.tsx|geocodeService\.ts)" | wc -l` returns 0 (all Plan 11-03 expected errors closed; no new errors).
    - Pre-existing App.tsx error count unchanged: `cd /Users/beckmaldinVL/development/mobileApps/JayTap && npx tsc --noEmit 2>&1 | grep "App\.tsx.*Property\.tours" | wc -l` returns 3 (matches STATE.md row 51 baseline).
  </acceptance_criteria>
  <done>
    Step2Location.tsx renders the address TextInput gated on showExactAddress||isHospitality, debounces forward geocode at 300ms, fires reverse geocode after pin events with anti-clobber guard, shows loading spinner + addressNotFound label per state; tsc clean; KBD-02 invariant preserved; debounce-ref cleanup on unmount.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keystroke → client geocodeService | User-supplied address text. Backend Plan 11-02 sanitizes (length-bounds, type check); client passes verbatim. |
| client geocodeService → apiClient → backend | Trusted internal call; Bearer auth via apiClient interceptor. |
| Backend response → Step2Location FormBag.location.address write | Trusted upstream (already validated by Plan 11-02 sanitization); written verbatim to FormBag. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Tampering | Stale geocode response stomping newer user keystroke | mitigate | Single `debounceRef` + `clearTimeout` on every keystroke ensures only the most recent 300ms-quiet input fires a geocode. Race where a slow response from keystroke N completes after keystroke N+1's input handler fires: NOT possible — keystroke N+1's setAddressInput overwrites local state BEFORE its own scheduleGeocode fires; if keystroke N's response then completes, it would set addressInput to its own resolved displayName — that IS a possible mid-typing UX glitch. Acceptable for this phase per 11-CONTEXT.md (debounce mechanism = Claude's Discretion); follow-up could add a request-ID guard if QA flags real instances. |
| T-11-17 | DoS | Rapid keystrokes flooding backend with geocode requests | mitigate | 300ms debounce + 3-char minimum + `clearTimeout` ensures at most ~3 req/sec per active user; Plan 11-02's `requireListingCapability` gate further restricts to listing-capable users. Backend Nominatim ToS (1 req/sec global) is the upstream constraint; current expected concurrent-typer count is single-digit. |
| T-11-18 | Tampering | Reverse-geocode response clobbering typed address | mitigate | Both `handleMapPress` and `handleMarkerDragEnd` check `!(values.location.address ?? '').trim()` BEFORE writing the reverse result. The check uses the useCallback's closed-over `values` snapshot which is stable for the duration of the async chain — if the user types between pin-drop and response, the typed value will be in a NEWER `values` snapshot (next render), but the closure captures the click-time snapshot. Edge case: a user who pins THEN types BEFORE the reverse response arrives — the closure still sees empty address from click time AND writes the reverse displayName. This would clobber. Mitigation: explicit check `!(values.location.address ?? '').trim()` reads from the closure; for the edge case to bite, the user must (a) drop a pin into an area with empty address, (b) type ≤ ~2 seconds after, (c) the network response arrives between their first keystroke and their last. Tradeoff accepted — adding a ref-tracked latest address adds complexity for an unlikely race. Surface in QA matrix (Plan 11-06) — if QA reproduces, M5+ follow-up. |
| T-11-19 | Information Disclosure | Logging full user-typed addresses | accept | Service helpers log only the HTTP status / message on failure — full address never reaches `console.warn`. Step2Location.tsx does not log addresses at all. No PII leak. |
| T-11-20 | Tampering | Replacing user-typed text with backend displayName on success | accept | Per 11-CONTEXT.md decision 9 second bullet, success path explicitly writes `address: result.displayName` (canonical Nominatim form, e.g. "Erkindik Bulvar, Bishkek, Chuy Region, Kyrgyzstan") OVER the user's typed text (e.g. "Erkindik 5"). This is intentional — the canonicalized form is what persists to backend + downstream display. User can re-edit if needed; next geocode cycle re-canonicalizes. |

ASVS L1 V5.1.3 (input validation) is satisfied client-side by the 3-char minimum + trim + null-on-failure handling; backend Plan 11-02 owns the authoritative sanitization.
</threat_model>

<verification>
Phase-wide verification this plan contributes to:

1. **TypeScript clean** — `npx tsc --noEmit` shows zero NEW errors on Step2Location.tsx or geocodeService.ts; pre-existing App.tsx errors unchanged.
2. **KBD-02 grep gate preserved** — `grep -rc "keyboardVerticalOffset" src/` returns 0 (memory m1-keyboard-kbd-02-invariants.md — hard rule).
3. **Manual smoke** (Plan 11-06 owns full QA matrix):
   - In Step 2 with city='bishkek' selected, toggle "Show exact address" → ON → see address input.
   - Type "100 Manas Street" → pause → see spinner → pin moves → input shows canonical displayName.
   - Type gibberish "asdkfjhasdkfj" → spinner → "Couldn't find that address" error label appears → pin DOES NOT MOVE.
   - Clear input + drop a fresh pin on the map → reverse-geocode fills input.
   - Drop pin THEN type → typed value preserved (anti-clobber).
   - Switch language to RU, repeat — backend Accept-Language: ru returns Russian displayName.
4. **NOT yet wired into release** — Plan 11-06 owns physical-device QA + commit to release-notes.
5. **MANDATORY paired code review** (per memory `gsd-verifier-misses-regressions.md`): this plan touches a critical user-facing form; verifier alone insufficient.
</verification>

<success_criteria>
This plan succeeds when:
- `src/services/geocodeService.ts` exports `geocodeAddress` + `reverseGeocode` with null-on-failure contract.
- Step2Location.tsx renders address TextInput gated on `showExactAddress || isHospitality`.
- Forward geocode debounces 300ms, calls backend with citySlug + lang, on success moves pin + canonicalizes input.
- Forward geocode failure preserves typed text + DOES NOT move pin + shows inline error label.
- Reverse geocode fires from both pin handlers with empty-text guard; silent failure.
- All 4 new i18n keys referenced via `t('...' as TranslationKeys)` casts (Plan 11-05 makes them real).
- TSC clean; KBD-02 invariant preserved (0 keyboardVerticalOffset in src/); debounce-ref cleanup on unmount.
- Plan 11-05 can add the i18n keys in parallel without conflict (no shared files).
- Plan 11-06 can walk the physical-device QA matrix against the wired-up UI.
</success_criteria>

<output>
After completion, create `.planning/phases/11-listing-address-geocode/11-04-SUMMARY.md` covering:
- Files modified (`geocodeService.ts` NEW + `Step2Location.tsx`).
- LOC delta on Step2Location.tsx (expect +120 to +180).
- TSC diagnostic delta (expect 0 new; the Plan 11-03 expected Step2Location.tsx errors are now closed).
- KBD-02 grep gate confirmation (returns 0).
- The handful of `as TranslationKeys` casts on the 4 new keys (so the executor sees they are intentional).
- The accepted race edge case from T-11-18 (pin-then-type-before-response clobber) — note for Plan 11-06 QA to attempt repro.
- Commit SHA in the RN client repo.
- Any deviations from the LOCKED decisions 5, 9 in 11-CONTEXT.md (none expected).
</output>
