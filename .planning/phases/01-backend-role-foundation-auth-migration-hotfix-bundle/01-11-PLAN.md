---
phase: 01-backend-role-foundation-auth-migration-hotfix-bundle
plan: 11
type: execute
wave: 8
depends_on: ["10"]
files_modified:
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/PropertyService.ts
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/FavoritesService.ts
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/ChatService.ts
  - /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/AppointmentService.ts
autonomous: true
requirements: [ROLE-05, HF-04]
threat_refs: [T-1-05]

must_haves:
  truths:
    - "All 5 RN client services use the shared `apiClient` instance — no more direct `axios` imports for backend calls"
    - "All `getHeaders()` helpers (ChatService, AppointmentService) and inline `x-firebase-uid` writes (FavoritesService) are deleted — Bearer is auto-attached by the apiClient request interceptor"
    - "Duplicated `PRODUCTION_URL = 'https://jaytap-services-production.up.railway.app/api'` constants in the 5 services are removed (apiClient owns the baseURL)"
    - "ChatService.connectSocket sends `auth: { token }` (HF-04) instead of `auth: { firebaseUid }`"
    - "ChatService.connectSocket signature drops the `firebaseUid` parameter; existing call sites (`ChatScreen.tsx` etc.) compile cleanly"
    - "AuthService.signIn / signUp still uses direct `axios` to Firebase Identity Toolkit endpoints (those are NOT backend; do not migrate to apiClient)"
  artifacts:
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/services/PropertyService.ts"
      provides: "All HTTP via apiClient (Bearer auto-attached); FormData multipart still works"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/services/FavoritesService.ts"
      provides: "All HTTP via apiClient; soft-auth GET pattern preserved (anonymous returns [])"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/services/ChatService.ts"
      provides: "HTTP via apiClient; socket auth payload switched to {token: idToken}"
      contains: "apiClient, auth: { token }"
    - path: "/Users/beckmaldinVL/development/mobileApps/JayTap/src/services/AppointmentService.ts"
      provides: "All HTTP via apiClient"
  key_links:
    - from: "5 RN client service files"
      to: "src/services/apiClient.ts (Plan 10)"
      via: "import { apiClient } from './apiClient'"
      pattern: "All backend HTTP through one configured axios instance"
    - from: "ChatService.connectSocket"
      to: "Backend index.js io.use() handshake (Plan 07)"
      via: "socket.io-client `auth: { token }`"
      pattern: "Bearer-only path; legacy auth.firebaseUid removed from client (D-04 backend keeps dual-accept until Phase 6)"
---

<objective>
Wave-8 client migration. Convert all 5 services from `axios` + per-service `getHeaders()` boilerplate to the shared `apiClient` from Plan 10. Switch `ChatService.connectSocket` to send `auth: { token }` (HF-04 client side).

Purpose: Closes the 5-service collapse symmetry. Backend (Plan 06) handles dual-accept; client (this plan) only sends Bearer. Removes the duplicated `getHeaders()` boilerplate (CONCERNS.md High-severity item).

Output: 4 files modified (PropertyService, FavoritesService, ChatService, AppointmentService). AuthService is intentionally NOT modified — its calls are to Firebase Identity Toolkit, NOT the JayTap backend, so they don't go through `apiClient`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-PATTERNS.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-RESEARCH.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/src/services/apiClient.ts

<interfaces>
<!-- Migration pattern (PATTERNS.md lines 690-754) -->

Before (FavoritesService.ts:23-27 verbatim):
```typescript
const response = await axios.get(`${API_URL}/favorites`, {
  headers: { 'x-firebase-uid': userData.localId },
});
```

After:
```typescript
import { apiClient } from './apiClient';
// ...
const response = await apiClient.get('/favorites');
// Authorization: Bearer auto-attached by request interceptor
```

<!-- ChatService HF-04 socket switch (PATTERNS.md lines 736-748) -->

Before (ChatService.ts:96-104):
```typescript
connectSocket: (firebaseUid: string, onNewMessage: ...): Socket => {
  const socket = io(WS_URL, {
    path: '/socket.io',
    auth: { firebaseUid },
    transports: ['websocket', 'polling'],
  });
  // ...
}
```

After:
```typescript
connectSocket: async (onNewMessage: ...): Promise<Socket> => {
  const token = await AsyncStorage.getItem('userToken');
  const socket = io(WS_URL, {
    path: '/socket.io',
    auth: { token },
    transports: ['websocket', 'polling'],
  });
  // ...
}
```

The signature change drops the `firebaseUid` param. Call sites (verified via grep — likely `ChatScreen.tsx`, `ConversationScreen.tsx`, possibly App.tsx) must update to drop the param.

<!-- Per-file gotchas (PATTERNS.md lines 749-754) -->
- PropertyService: uses `multipart/form-data` via FormData for image uploads. apiClient handles this (axios auto-detects FormData and sets Content-Type). Verify no manual `Content-Type: multipart/form-data` header is being set elsewhere — that breaks FormData boundary handling.
- FavoritesService: soft-auth pattern (GET returns [] without auth). The backend route still allows GET without Bearer (Plan 06 kept it per-route on writes only). After migration, GET /favorites WITHOUT Bearer continues to return [] from the backend; the client just sends Bearer when present (interceptor auto-attaches).
- ChatService: getHeaders() helper at lines 14-18 — DELETE entirely after migration.
- AppointmentService: getHeaders() helper at lines 10-14 — DELETE entirely after migration.

<!-- AuthService stays unchanged -->
AuthService.signIn / signUp / refreshIdToken call Firebase Identity Toolkit endpoints (`identitytoolkit.googleapis.com`, `securetoken.googleapis.com`) — NOT the JayTap backend. They use direct `axios.post` and continue to do so. Do NOT migrate AuthService HTTP calls to apiClient.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate PropertyService + FavoritesService to apiClient</name>
  <files>/Users/beckmaldinVL/development/mobileApps/JayTap/src/services/PropertyService.ts, /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/FavoritesService.ts</files>
  <read_first>
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/PropertyService.ts (full file — locate every `axios.` call + every URL constant + every `getHeaders` reference + the `x-firebase-uid` reads)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/FavoritesService.ts (full file — soft-auth pattern at lines 22-50)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-PATTERNS.md (lines 690-754)
  </read_first>
  <action>
    PropertyService.ts:
    1. Replace `import axios from 'axios';` with `import { apiClient } from './apiClient';`. Keep other imports (Platform, AsyncStorage if used, AuthService if used for non-header reasons).
    2. DELETE the URL constants block (PRODUCTION_URL, LOCAL_URL, API_URL near lines 8-12). The shared apiClient owns the baseURL.
    3. For each `axios.get/post/put/patch/delete` call:
       - Change to `apiClient.get/post/put/patch/delete`
       - Drop the `${API_URL}` baseURL prefix from the URL — e.g. `axios.get(\`${API_URL}/properties\`)` -> `apiClient.get('/properties')`
       - DELETE any `headers: { 'x-firebase-uid': ... }` — auto-attached by interceptor
       - For multipart FormData calls, leave the FormData arg alone — axios auto-detects and sets Content-Type with the correct boundary. Don't manually set Content-Type. (If the existing code has an explicit `'Content-Type': 'multipart/form-data'` line, DELETE it — it forces a missing-boundary header that breaks the upload.)

    FavoritesService.ts:
    1. Same import swap.
    2. DELETE URL constants.
    3. Convert all `axios.get/post/...` to `apiClient.*` and drop x-firebase-uid headers.
    4. Pay attention to the GET / soft-auth path (around lines 22-58). The current behavior reads `userData = await AuthService.getUserData()`, then either sends with x-firebase-uid OR returns [] when missing. New behavior:
       - Keep the early-return `if (!userData?.localId) return [];` if you still want client-side anonymous-shortcut. OR remove the userData lookup entirely and let the request fly — the backend GET /favorites route returns [] when no auth (per Plan 06's per-route mount on writes only). Either path is acceptable; document the choice in the SUMMARY.

    Type-check + smoke:
    ```
    cd /Users/beckmaldinVL/development/mobileApps/JayTap
    npx tsc --noEmit
    ```

    Commit:
    ```
    refactor(services): migrate Property+Favorites to apiClient [ROLE-05]
    ```
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -q "import { apiClient } from './apiClient'" src/services/PropertyService.ts && grep -q "import { apiClient } from './apiClient'" src/services/FavoritesService.ts && ! grep -q "x-firebase-uid" src/services/PropertyService.ts && ! grep -q "x-firebase-uid" src/services/FavoritesService.ts && ! grep -q "PRODUCTION_URL" src/services/PropertyService.ts && ! grep -q "PRODUCTION_URL" src/services/FavoritesService.ts && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - PropertyService imports `apiClient` and does NOT contain `import axios`
    - PropertyService does NOT contain `x-firebase-uid` references
    - PropertyService does NOT contain `PRODUCTION_URL` constant
    - FavoritesService imports `apiClient` and does NOT contain `import axios`
    - FavoritesService does NOT contain `x-firebase-uid` references
    - FavoritesService does NOT contain `PRODUCTION_URL` constant
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Property + Favorites use shared client.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ChatService (HTTP + socket auth switch) + AppointmentService</name>
  <files>/Users/beckmaldinVL/development/mobileApps/JayTap/src/services/ChatService.ts, /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/AppointmentService.ts</files>
  <read_first>
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/ChatService.ts (full file — getHeaders helper at 14-18; connectSocket at 96-104)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/AppointmentService.ts (full file — getHeaders helper at 10-14)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-PATTERNS.md (lines 723-748 — socket auth switch + service migration)
  </read_first>
  <action>
    ChatService.ts:
    1. Replace `import axios from 'axios';` with `import { apiClient } from './apiClient';`. Keep `import { io, Socket } from 'socket.io-client'` and `import AsyncStorage from '@react-native-async-storage/async-storage'`.
    2. DELETE URL constants (`API_URL`, `WS_URL` if it's a derived constant — but if WS_URL is the websocket URL it stays since apiClient is HTTP-only; check the existing file). Keep WS_URL.
    3. DELETE the `getHeaders()` helper (lines 14-18 — verbatim).
    4. Convert all `axios.*(${API_URL}/...)` -> `apiClient.*(/...)`.
    5. **HF-04 socket switch** — locate `connectSocket` (around line 96):
       ```typescript
       connectSocket: (firebaseUid: string, onNewMessage: ...) => {
         const socket = io(WS_URL, {
           path: '/socket.io',
           auth: { firebaseUid },
           transports: ['websocket', 'polling'],
         });
         // ...
       }
       ```
       Replace with:
       ```typescript
       connectSocket: async (onNewMessage: (msg: any) => void): Promise<Socket> => {
         const token = await AsyncStorage.getItem('userToken');
         const socket = io(WS_URL, {
           path: '/socket.io',
           auth: { token },
           transports: ['websocket', 'polling'],
         });
         // ... rest of existing handler body unchanged ...
       }
       ```
       Match the existing onNewMessage parameter signature exactly (copy from current file). The function becomes async.

    6. Update call sites of `connectSocket(`. Grep:
       ```
       cd /Users/beckmaldinVL/development/mobileApps/JayTap
       grep -rn "connectSocket(" src/ App.tsx
       ```
       For each call site (likely `ChatScreen.tsx`, `ConversationScreen.tsx`):
       - Drop the `firebaseUid` arg: `ChatService.connectSocket(uid, handler)` -> `ChatService.connectSocket(handler)` 
       - Wrap in `async`/`await` since the function now returns `Promise<Socket>`. If the call site is in a useEffect, wrap in an inner async IIFE:
         ```typescript
         useEffect(() => {
           let socketRef: Socket | null = null;
           (async () => { socketRef = await ChatService.connectSocket(handler); })();
           return () => { socketRef?.disconnect(); };
         }, [...]);
         ```

    AppointmentService.ts:
    1. Replace `import axios` with apiClient.
    2. DELETE URL constants.
    3. DELETE the `getHeaders()` helper (lines 10-14).
    4. Convert all `axios.*` calls to `apiClient.*` and drop x-firebase-uid headers.

    Type-check after both edits:
    ```
    cd /Users/beckmaldinVL/development/mobileApps/JayTap
    npx tsc --noEmit
    ```

    Commit:
    ```
    refactor(services): migrate Chat+Appointment to apiClient; ChatService socket auth -> token [ROLE-05,HF-04]
    ```
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap && grep -q "import { apiClient } from './apiClient'" src/services/ChatService.ts && grep -q "import { apiClient } from './apiClient'" src/services/AppointmentService.ts && grep -q "auth: { token }" src/services/ChatService.ts && ! grep -q "auth: { firebaseUid }" src/services/ChatService.ts && ! grep -q "x-firebase-uid" src/services/ChatService.ts && ! grep -q "x-firebase-uid" src/services/AppointmentService.ts && ! grep -E "const getHeaders" src/services/ChatService.ts && ! grep -E "const getHeaders" src/services/AppointmentService.ts && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ChatService imports apiClient; does NOT contain `x-firebase-uid` or `getHeaders` helper
    - ChatService `connectSocket` uses `auth: { token }` (NOT `auth: { firebaseUid }`)
    - AppointmentService imports apiClient; does NOT contain `x-firebase-uid` or `getHeaders` helper
    - All call sites of `ChatService.connectSocket` updated (no remaining `connectSocket(uid, ...)` invocations)
    - `npx tsc --noEmit` exits 0
    - Manual smoke (record in 01-VALIDATION.md): chat + appointments load post-migration on a physical device sign-in
  </acceptance_criteria>
  <done>
    All 5 services migrated. ChatService HF-04 client side complete (server side from Plan 07 is dual-accept; client now Bearer-only).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RN client outbound HTTP -> Railway backend | Now sends Bearer instead of x-firebase-uid header. Backend (Plan 05/06) verifies; legacy header dual-accept until Phase 6. |
| RN client outbound socket -> backend io.use handshake | Now sends `auth: { token }`. Backend (Plan 07) verifies; legacy auth.firebaseUid dual-accept until Phase 6. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05 | Information Disclosure | Socket cross-room subscription (client side) | mitigate | Client sends `auth: { token }`; server (Plan 07) JWKS-verifies and only auto-joins `user:<verified_sub>`. Even if a malicious build sent a fake token, backend rejects via JWKS. |
</threat_model>

<verification>
- 4 service files type-check
- Full project `npx tsc --noEmit` clean
- Manual smoke on physical device: sign-in, browse properties (PropertyService), favorite a listing (FavoritesService), open a chat thread (ChatService HTTP + socket), schedule a viewing (AppointmentService) — record results in 01-VALIDATION.md
</verification>

<success_criteria>
- 5-service consolidation complete on the client side
- HF-04 client switch complete (`auth: { token }`)
- Duplicated boilerplate removed (CONCERNS.md improvement)
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-11-SUMMARY.md` recording: file edits per service, call-site updates for connectSocket, manual smoke results (5 surfaces), TypeScript clean status.
</output>
