---
phase: 01-backend-role-foundation-auth-migration-hotfix-bundle
plan: 08
type: execute
wave: 5
depends_on: ["06", "07"]
files_modified:
  - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/scripts/migrate-roles-m2.js
autonomous: false
requirements: [ROLE-02]
threat_refs: [T-1-07, T-1-08]

must_haves:
  truths:
    - "`npm run migrate:roles-m2 -- --dry-run` prints planned updateMany counts WITHOUT writing"
    - "`npm run migrate:roles-m2` flips legacy userType values: renter -> user, owner -> user, agent -> user (admin -> admin unchanged)"
    - "M1 admin email (`beckprograms@gmail.com`) has `userType: 'admin'` in MongoDB after migration runs"
    - "Idempotent: re-running the script after a successful run is a no-op (filter excludes already-migrated records)"
    - "`npm run migrate:roles-m2 -- --verify` prints PASS when `db.user_profile.countDocuments({userType: {$nin: ['user', 'moderator', 'admin']}})` returns 0; FAIL otherwise (exit 1)"
    - "Script connects via `connectDB()` from `../config/db` (preserves the `dbName: 'bizdinkonush'` invariant — does NOT use direct mongoose.connect)"
  artifacts:
    - path: "/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/scripts/migrate-roles-m2.js"
      provides: "One-shot Mongo migration: enum flip + M1 admin upsert + verify subcommand"
      contains: "M1_ADMIN_EMAILS, --dry-run, --verify, connectDB, updateMany, $nin"
  key_links:
    - from: "Production MongoDB Atlas (user_profile collection)"
      to: "Plan 09 User.js enum cutover"
      via: "Manual `npm run migrate:roles-m2 -- --verify` returns PASS (exit 0)"
      pattern: "5-step ordering: this script's --verify=PASS GATES Plan 09's enum cutover"
---

<objective>
[BLOCKING] Wave-5 backend operations. Author the role migration script and then RUN it against production MongoDB Atlas. This task is the literal D-08 acceptance criterion — Phase 1 cannot pass without it. The migration MUST land BEFORE Plan 09 (enum cutover) AND BEFORE Plan 13 (RN client allowlist deletion) per PATTERNS.md cross-cutting risk #2 and PITFALLS Pitfall 5.

**Mongoose has no formal `db push`** — schema becomes effective at process boot. The migration script IS the schema push for this phase: data first, then Plan 09 tightens the schema to match.

Purpose: Flip every existing User document's `userType` from the legacy enum (`renter` / `owner` / `agent`) to the canonical M2 enum (`user`), and ensure the M1 admin email is set to `userType: 'admin'` in Mongo BEFORE the RN client deletes `adminAllowlist.ts`. Without this seed, the maintainer (`beckprograms@gmail.com`) loses admin access at the M2 client cut.

Output: One script file added; one production-Mongo write executed; `--verify` returns PASS (exit 0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-CONTEXT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-RESEARCH.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-PATTERNS.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/src/constants/adminAllowlist.ts

<interfaces>
D-09 RESOLUTION (RESEARCH.md Pattern 7 lines 674-684 + PATTERNS.md flag 8): Use UPDATE-ONLY for the admin upsert. The M1 admin (`beckprograms@gmail.com`) HAS already signed in (verified by `git log --author=beckmaldinVL` activity per RESEARCH.md), so the email exists in Mongo with a real `uid`. The script `findOne({email})` then `updateOne({email}, {$set: {userType: 'admin'}})` — no upsert with fake uid. If the email is missing from Mongo, log a WARNING and skip (admin must sign in once via the live app, then re-run the script).

A1 confirmation step (RESEARCH.md line 1244): pre-flight check before running migration:
```
mongosh "<MONGO_URI>" --eval "db.user_profile.findOne({email: 'beckprograms@gmail.com'})"
```
If returns null, abort migration; have the maintainer sign in via the live app first; re-attempt.

The acceptance count uses `user_profile` (the actual collection per PATTERNS.md flag 2):
```
mongosh "<MONGO_URI>" --eval "db.user_profile.countDocuments({userType: {\$nin: ['user', 'moderator', 'admin']}})"
```
NOT `db.users` (CONTEXT.md/REQUIREMENTS.md statement is wrong — collection is `user_profile`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migrate-roles-m2.js with --dry-run, --verify, and admin upsert</name>
  <files>/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/scripts/migrate-roles-m2.js</files>
  <read_first>
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/scripts/seed.js (existing analog — connectDB pattern, top-to-bottom CLI script idiom)
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/config/db.js (verify connectDB applies dbName: 'bizdinkonush')
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-RESEARCH.md (Pattern 6 lines 568-664 — full script + Pattern 7 D-09 resolution lines 674-684)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-PATTERNS.md (lines 128-188 — script section + flag 8 admin update-only)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/constants/adminAllowlist.ts (line 13 — single source of truth: ['beckprograms@gmail.com'])
  </read_first>
  <action>
    Create `/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/scripts/migrate-roles-m2.js` with the verbatim script body shown in RESEARCH.md Pattern 6 (lines 568-664). Important details that MUST be present:

    1. `dotenv.config()` at top, then `connectDB` import from `../config/db` (NOT direct `mongoose.connect`)
    2. `const M1_ADMIN_EMAILS = ['beckprograms@gmail.com'];` at top (mirrors RN client `src/constants/adminAllowlist.ts:13`)
    3. CLI flag parsing via `process.argv.slice(2)`: `--dry-run` and `--verify`
    4. Three flips array: `[{from: 'renter', to: 'user'}, {from: 'owner', to: 'user'}, {from: 'agent', to: 'user'}]` — admin → admin is no-op
    5. Per-flip: `User.countDocuments(filter)` → log → if not dry-run AND matched > 0 → `User.updateMany(filter, {$set: {userType: to}})`
    6. Admin upsert: UPDATE-ONLY per Pattern 7 Option A (Pattern 7 lines 678-680)
       - `findOne({email})` first
       - If exists and userType === 'admin': log "already admin (no-op)"
       - If exists and not admin: log promotion; if not dry-run, `updateOne({email}, {$set: {userType: 'admin'}})`
       - If NOT exists: `console.warn("WARNING: ${email} — record not in Mongo. Admin must sign in once via the live app, then re-run script.")`
    7. Verification subcommand: when `--verify`, run `User.countDocuments({userType: {$nin: ['user', 'moderator', 'admin']}})` and exit 0 if 0, exit 1 otherwise (printing PASS/FAIL)
    8. Final block: post-migration count + acceptance log line: `ACCEPTANCE MET (ROLE-02): db.user_profile.countDocuments({userType:{$nin:["user","moderator","admin"]}}) === 0`
    9. `await mongoose.disconnect()` before each `process.exit()`
    10. Top-level catch: `main().catch(err => { console.error(err); process.exit(1); })`

    DO NOT include any path that creates a new User with a fake uid (Pattern 7 Option B was rejected). DO NOT use upsert: true on the email update.

    Smoke test the script syntax:
    ```
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services
    node --check src/scripts/migrate-roles-m2.js
    ```

    DO NOT yet run against production — that is Task 2 (the BLOCKING checkpoint).

    Commit:
    ```
    feat(scripts): migrate-roles-m2 — enum flip + M1 admin upsert + verify [ROLE-02]
    ```
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && test -f src/scripts/migrate-roles-m2.js && grep -q "M1_ADMIN_EMAILS = \['beckprograms@gmail.com'\]" src/scripts/migrate-roles-m2.js && grep -q "renter" src/scripts/migrate-roles-m2.js && grep -q "owner" src/scripts/migrate-roles-m2.js && grep -q "agent" src/scripts/migrate-roles-m2.js && grep -q "isDryRun" src/scripts/migrate-roles-m2.js && grep -q "isVerify" src/scripts/migrate-roles-m2.js && grep -q "connectDB" src/scripts/migrate-roles-m2.js && grep -q '\$nin' src/scripts/migrate-roles-m2.js && ! grep -q "upsert: true" src/scripts/migrate-roles-m2.js && node --check src/scripts/migrate-roles-m2.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/scripts/migrate-roles-m2.js` exists
    - File contains `M1_ADMIN_EMAILS = ['beckprograms@gmail.com']`
    - File contains string literals `'renter'`, `'owner'`, `'agent'` (legacy enum keys)
    - File contains `isDryRun` and `isVerify` flag handling
    - File contains `connectDB` import from `../config/db`
    - File contains the `$nin` MongoDB operator with the canonical 3-role array
    - File does NOT contain `upsert: true` (Pattern 7 Option C was rejected)
    - `node --check` exits 0
    - Commit message contains `[ROLE-02]`
  </acceptance_criteria>
  <done>
    Script ready to run. Task 2 executes the production migration under maintainer supervision.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Run migration against production Mongo + verify acceptance</name>
  <files>(no file changes — production Mongo writes only)</files>
  <read_first>
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/scripts/migrate-roles-m2.js (Task 1 output)
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/.env (verify MONGO_URI points to production Atlas with the rotated password from Plan 01)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-RESEARCH.md (Pattern 7 lines 674-684 — A1 pre-flight check)
  </read_first>
  <what-built>The script from Task 1 will be run against the production MONGO_URI by the maintainer. Backend is currently deployed via Plan 06 + Plan 07 with dual-accept verifyFirebaseToken — production traffic continues working through this migration window.</what-built>
  <how-to-verify>
    Step A — Pre-flight A1 check (verify the M1 admin email exists in Mongo):
    ```
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services
    # Replace <MONGO_URI> with the connection string from .env
    mongosh "<MONGO_URI>" --eval 'db.user_profile.findOne({email: "beckprograms@gmail.com"}, {_id: 0, uid: 1, email: 1, userType: 1})'
    ```
    Expected output: a document with `uid`, `email`, `userType` (likely `userType: 'renter'` since the M1 client never set it to admin — the email allowlist branch was the only thing making the maintainer admin in M1).

    If this returns `null`, ABORT — the maintainer must sign in to the live JayTap app once, which creates the user_profile record via `POST /api/auth/users`. Then retry Step A.

    Step B — Dry-run preview:
    ```
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services
    npm run migrate:roles-m2 -- --dry-run
    ```
    Expected output (counts will vary):
    ```
    === DRY RUN ===
      renter -> user: <N> records
      owner -> user: <M> records
      agent -> user: <K> records

    Step 2: M1 admin promotion (update-only)
      beckprograms@gmail.com: promote renter -> admin

    Post-migration: 0 records still at non-canonical userType.
    (dry-run — would meet acceptance after running without --dry-run)
    ```
    Verify the counts match expectations (small dataset; total `user_profile` count should be < 50 in M2 pre-launch). If counts look surprising, escalate before live run.

    Step C — Live migration:
    ```
    npm run migrate:roles-m2
    ```
    Expected output:
    ```
    === MIGRATION ===
      renter -> user: <N> records
        updated: <N>
      owner -> user: <M> records
        updated: <M>
      agent -> user: <K> records
        updated: <K>

    Step 2: M1 admin promotion (update-only)
      beckprograms@gmail.com: promote renter -> admin

    Post-migration: 0 records still at non-canonical userType.
    ACCEPTANCE MET (ROLE-02): db.user_profile.countDocuments({userType:{$nin:["user","moderator","admin"]}}) === 0
    ```

    Step D — Verify subcommand:
    ```
    npm run migrate:roles-m2 -- --verify
    ```
    Expected output: `Records with non-canonical userType: 0` then `VERIFY: PASS — ROLE-02 acceptance met`. Exit code 0.

    Step E — Idempotency check (re-run live):
    ```
    npm run migrate:roles-m2
    ```
    Expected output:
    ```
    === MIGRATION ===
      renter -> user: 0 records
      owner -> user: 0 records
      agent -> user: 0 records

    Step 2: M1 admin promotion (update-only)
      beckprograms@gmail.com: already admin (no-op)

    Post-migration: 0 records still at non-canonical userType.
    ACCEPTANCE MET ...
    ```
    All counts at 0 + admin "already admin (no-op)" confirms idempotency.

    Step F — Spot-check the maintainer record:
    ```
    mongosh "<MONGO_URI>" --eval 'db.user_profile.findOne({email: "beckprograms@gmail.com"}, {_id: 0, uid: 1, email: 1, userType: 1})'
    ```
    Expected: `userType: 'admin'`.

    Step G — Record results in 01-VALIDATION.md "Manual-Only Verifications" Migration row:
    - dry-run counts (renter/owner/agent → user)
    - live-run counts
    - --verify PASS confirmed
    - idempotency re-run = all zeros
    - admin email userType = 'admin'
    - Run timestamp
  </how-to-verify>
  <acceptance_criteria>
    - `npm run migrate:roles-m2 -- --verify` exits 0 with "VERIFY: PASS"
    - `mongosh ... db.user_profile.countDocuments({userType: {$nin: ["user", "moderator", "admin"]}})` returns 0
    - `mongosh ... db.user_profile.findOne({email: "beckprograms@gmail.com"})` returns a document with `userType: "admin"`
    - Re-running `npm run migrate:roles-m2` returns all 0 counts (idempotency proven)
    - 01-VALIDATION.md updated with run timestamp + per-flip counts
  </acceptance_criteria>
  <resume-signal>Type "verify-pass" when --verify exits 0 AND the maintainer admin record is confirmed at userType:'admin', OR describe any failure (count anomalies, network errors, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer laptop with MONGO_URI in `.env` -> production Atlas | Trusted operator runs script against production. Pre-flight A1 check defends against missing-record edge case. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | Tampering / Denial of Service | Migration partial-failure leaving inconsistent role state | mitigate | --dry-run preview before live run; idempotent filter (`{userType: 'renter'}` only matches not-yet-migrated records); --verify subcommand gates Plan 09 enum cutover. Re-run is safe (no-op on already-migrated records). |
| T-1-08 | Denial of Service | M1 admin lockout post-migration | mitigate | D-09 admin seed embedded in same script; UPDATE-ONLY path per Pattern 7 Option A. Pre-flight A1 check (Step A) verifies the email exists in Mongo before running. If missing, abort; maintainer signs in once via live app to create the record, then retry. |
</threat_model>

<verification>
- Task 1 verify: script syntax-checks clean
- Task 2 verify: `--verify` PASSes against production; admin record updated; idempotent re-run is no-op
- 01-VALIDATION.md "Manual-Only Verifications" Migration row updated with run timestamp + counts
</verification>

<success_criteria>
- ROLE-02 acceptance criterion met empirically: `db.user_profile.countDocuments({userType: {$nin: ['user','moderator','admin']}})` = 0
- M1 admin (`beckprograms@gmail.com`) has `userType: 'admin'` in Mongo
- Plan 09 unblocked (enum cutover safe to deploy)
- Plan 13 unblocked (RN client `adminAllowlist.ts` deletion safe to ship)
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-08-SUMMARY.md` recording: dry-run output, live-run output (with counts per flip), --verify output, idempotency re-run output, admin record spot-check, run timestamp, exact `mongosh` command output for the acceptance count.
</output>
