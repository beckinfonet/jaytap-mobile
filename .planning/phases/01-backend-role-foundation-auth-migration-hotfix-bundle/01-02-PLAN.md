---
phase: 01-backend-role-foundation-auth-migration-hotfix-bundle
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/models/Property.js
  - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/routes/propertyRoutes.js
autonomous: true
requirements: [HF-01]
threat_refs: []

must_haves:
  truths:
    - "Hospitality listings save and re-fetch with rooms, maxGuests, and 12-amenity selections preserved"
    - "Mongoose Property schema accepts rooms, maxGuests, amenities at write time (no longer silently dropped)"
    - "POST /api/properties handler reads rooms/maxGuests/amenities from req.body and writes them"
    - "PUT /api/properties/:id handler updates rooms/maxGuests/amenities on edit"
  artifacts:
    - path: "/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/models/Property.js"
      provides: "Property Mongoose schema with rooms, maxGuests, amenities fields"
      contains: "rooms:, maxGuests:, amenities:"
    - path: "/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/routes/propertyRoutes.js"
      provides: "POST + PUT handlers that pass rooms/maxGuests/amenities through to the schema"
      contains: "rooms, maxGuests, amenities"
  key_links:
    - from: "src/services/PropertyService.ts (RN client; already sends these fields per M1 Phase 6)"
      to: "JayTap-services/src/routes/propertyRoutes.js (POST/PUT handlers)"
      via: "axios POST /api/properties body containing rooms, maxGuests, amenities"
      pattern: "Round trip POST → GET preserves all three fields"
---

<objective>
HF-01 standalone hotfix per D-02. Add `rooms`, `maxGuests`, and `amenities` to the `Property` Mongoose schema (currently dropped silently because Mongoose strict mode rejects undeclared fields), and add the matching field reads in `propertyRoutes.js` POST + PUT handlers. Ships independently of Phase 1's main work — Hospitality listings created today silently lose these three fields, which is a data-integrity bug.

Purpose: M1 Phase 6 shipped client-side support for `rooms`, `maxGuests`, and 12 amenities (`PropertyService.ts:83-85, 154-156`). The backend `Property` schema never declared them. Mongoose has no formal `db push`; schema is effective at process boot. Once this lands, hospitality round-trip works empirically.

Output: Two file modifications + one commit. Backend redeploy on Railway picks up the new schema automatically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/PROJECT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/REQUIREMENTS.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-CONTEXT.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-RESEARCH.md
@/Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-PATTERNS.md

<interfaces>
<!-- HF-01 patch verbatim from RESEARCH.md lines 1014-1042 + PATTERNS.md lines 478-498 -->

Property.js — additive (place near existing bedrooms/bathrooms ~line 14-15):
```javascript
rooms:      { type: Number, default: 0 },     // hospitality: number of rooms in hostel/hotel
maxGuests:  { type: Number, default: 0 },     // hospitality: max guests per stay
amenities:  { type: [String], default: [] },  // hospitality: 12-amenity multi-select
```

propertyRoutes.js POST/PUT body destructuring — ADD `rooms, maxGuests, amenities` to the existing destructured field list (~line 125-143 for POST, line 343+ for PUT).

propertyRoutes.js propertyData object construction (~line 197 POST; mirror in PUT update payload):
```javascript
rooms: parseInt(rooms) || 0,
maxGuests: parseInt(maxGuests) || 0,
amenities: typeof amenities === 'string' ? JSON.parse(amenities) : (Array.isArray(amenities) ? amenities : []),
```

The amenities triple-branch handles multipart/form-data (string-encoded JSON via FormData) AND application/json (already-parsed array) — propertyRoutes.js uses `multer.array(...)` so multipart bodies arrive with array fields as JSON strings. Mirror the existing field's parsing pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rooms/maxGuests/amenities to Property schema</name>
  <files>/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/models/Property.js</files>
  <read_first>
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/models/Property.js (existing schema; identify the bedrooms/bathrooms block as the insertion anchor)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-PATTERNS.md (lines 478-498 — additive number/array pattern + the verbatim insert)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-RESEARCH.md (lines 1014-1021 — Code Examples)
  </read_first>
  <action>
    Open `/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/models/Property.js`.

    Locate the existing `bedrooms` / `bathrooms` / `areaSqm` block (around lines 13-15 per PATTERNS.md verification). Immediately AFTER that block (or directly inside the same "physical fields" cluster — match surrounding indentation), add three lines verbatim:

    ```javascript
    rooms:      { type: Number, default: 0 },
    maxGuests:  { type: Number, default: 0 },
    amenities:  { type: [String], default: [] },
    ```

    The `[String]` array shape mirrors the existing `features: [String]` field (around line 22). The explicit `default: []` is intentional (different from `features` which has no default) — explicit is safer for new fields.

    DO NOT change `collection: 'user_profile'` (this is the User model's invariant, not Property — but if you accidentally edit User.js, halt). DO NOT change `timestamps: true`. DO NOT change any existing field's shape. ONLY add the three lines.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && grep -q "rooms:" src/models/Property.js && grep -q "maxGuests:" src/models/Property.js && grep -q "amenities:" src/models/Property.js && grep -q "type: \[String\], default: \[\]" src/models/Property.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/models/Property.js` contains literal `rooms:` field declaration with `type: Number`
    - `src/models/Property.js` contains literal `maxGuests:` with `type: Number`
    - `src/models/Property.js` contains literal `amenities:` with `type: [String], default: []`
    - `node -e "require('./src/models/Property')"` from `JayTap-services/` exits 0 (schema loads without syntax error)
  </acceptance_criteria>
  <done>
    Schema declares all three fields. Backend boot picks up the new schema automatically — no formal `db push` needed (Mongoose evaluates schema at first model use).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire rooms/maxGuests/amenities through POST + PUT property routes</name>
  <files>/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/routes/propertyRoutes.js</files>
  <read_first>
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/routes/propertyRoutes.js (locate POST `/` handler and PUT `/:id` handler — find by `router.post('/'` and `router.put('/:id'`)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-RESEARCH.md (lines 1023-1044 — full HF-01 route patch)
    - /Users/beckmaldinVL/development/mobileApps/JayTap/src/services/PropertyService.ts (lines 83-85 + 154-156 — confirm RN client sends these fields as multipart/form-data)
  </read_first>
  <action>
    Open `/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/routes/propertyRoutes.js`.

    POST `/` handler:
    1. Locate the `req.body` destructuring block (around lines 125-143). ADD `rooms, maxGuests, amenities` to the destructured field list. Match the existing comma + indentation style.
    2. Locate the `propertyData` construction object (around line 197). ADD three property assignments inside that object literal:
       ```javascript
       rooms: parseInt(rooms) || 0,
       maxGuests: parseInt(maxGuests) || 0,
       amenities: typeof amenities === 'string' ? JSON.parse(amenities) : (Array.isArray(amenities) ? amenities : []),
       ```
       The triple-branch on `amenities` handles both multipart/form-data (string-encoded array via FormData → arrives as JSON string) and application/json (already-parsed array). This mirrors how multer-parsed string fields need conversion.

    PUT `/:id` handler (around line 343+):
    1. Apply the SAME destructuring addition (`rooms, maxGuests, amenities`).
    2. Apply the SAME assignment block to the update payload object.

    Verify the file still parses by running `node -e "require('./src/routes/propertyRoutes')"` (the require may fail if mongoose connection is not initialized — that's expected; the syntax check is what matters). Use `node --check src/routes/propertyRoutes.js` for a pure syntax check instead.

    Do NOT touch the existing inline auth blocks in this file (`req.body.firebaseUid || req.headers['x-firebase-uid']`) — those are replaced in Plan 06 via the verifyFirebaseToken middleware migration. HF-01 is purely additive.

    Commit with message:
    ```
    fix(properties): persist rooms/maxGuests/amenities for hospitality listings [HF-01]
    ```
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && node --check src/routes/propertyRoutes.js && grep -c "rooms" src/routes/propertyRoutes.js | awk '{ exit ($1 < 4) }' && grep -q "maxGuests" src/routes/propertyRoutes.js && grep -q "amenities" src/routes/propertyRoutes.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/routes/propertyRoutes.js` exits 0 (no syntax errors)
    - `grep -c "rooms" src/routes/propertyRoutes.js` returns >= 4 (destructure POST + propertyData POST + destructure PUT + payload PUT)
    - `grep "maxGuests" src/routes/propertyRoutes.js` returns matches in BOTH POST and PUT handlers
    - `grep "JSON.parse(amenities)" src/routes/propertyRoutes.js` returns at least one match (the triple-branch parser)
    - Manual: After Railway redeploys with this change, posting a Hospitality listing from the live app preserves rooms/maxGuests/amenities on round-trip GET (recorded in 01-VALIDATION.md "Manual-Only Verifications").
  </acceptance_criteria>
  <done>
    Route handlers extract and persist the three fields. After backend redeploy, M1 Phase 6 client correctly round-trips Hospitality data.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RN client → POST /api/properties | Already authenticated (legacy `x-firebase-uid` until Plan 06 migration); body field validation is per-field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none Phase 1) | — | Property model additive fields | accept | HF-01 is purely additive (data integrity). The new fields are non-sensitive (room count, guest count, amenity list). No new attack surface beyond what the existing `bedrooms`/`bathrooms` already accept. |
</threat_model>

<verification>
- HF-01 round-trip empirically verified post-deploy via the live app: create Hospitality listing → save → re-fetch → all three fields present (recorded in 01-VALIDATION.md Manual-Only)
- `node --check` passes on both edited files
</verification>

<success_criteria>
- `Property.js` contains rooms / maxGuests / amenities field declarations
- `propertyRoutes.js` POST and PUT handlers both destructure and write the three fields
- Backend redeploy succeeds on Railway (auto-trigger on git push, or manual via Railway dashboard)
- Live app smoke test: create + edit a Hospitality listing; confirm fields persist
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-role-foundation-auth-migration-hotfix-bundle/01-02-SUMMARY.md` recording: file edits made, Railway redeploy timestamp, manual round-trip smoke test result (PASS/FAIL).
</output>
