---
phase: 06-hospitality-rendering
plan: 07
type: execute
wave: 4
depends_on: [06-03, 06-05, 06-06]
files_modified:
  - src/locales/en.ts
  - src/locales/ru.ts
  - src/components/CreateListingForm/HospitalitySection.tsx
  - .planning/phases/06-hospitality-rendering/06-VERIFICATION.md
  - .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md
autonomous: false
requirements: [HOSP-06]
requirements_addressed: [HOSP-06]
tags: [verification, qa, i18n-cleanup, phase-exit, checkpoint]

must_haves:
  truths:
    - "hospitality.amenitiesPhase6Placeholder key is deleted from both en.ts and ru.ts in a single commit (parity preserved)"
    - "Every runtime reference to hospitality.amenitiesPhase6Placeholder has been removed (tsc enforces — Record<TranslationKeys,string> would reject orphan consumers)"
    - "06-VERIFICATION.md exists and contains the full phase-exit regression bundle (commands + expected results + PASS/FAIL disposition per row)"
    - "06-QA-MATRIX.md exists with ~80 cells mirroring Phase 5's 54-cell precedent + RESEARCH §QA matrix breakdown"
    - "User completes physical-device QA walk on iOS + Android with admin + non-admin accounts and records PASS/FAIL per cell"
    - "All 3 CI gate scripts exit 0; npm test fully green; tsc baseline preserved; D-09 anchors + MediaSection Gated count unchanged"
  artifacts:
    - path: ".planning/phases/06-hospitality-rendering/06-VERIFICATION.md"
      provides: "Phase-exit regression bundle — command list + expected outputs + disposition rows"
      min_lines: 80
    - path: ".planning/phases/06-hospitality-rendering/06-QA-MATRIX.md"
      provides: "Manual physical-device QA matrix (~80 cells) per RESEARCH §Validation Architecture + CLAUDE.md M1 testing bar"
      min_lines: 100
  key_links:
    - from: "06-VERIFICATION.md"
      to: "06-QA-MATRIX.md"
      via: "reference from regression bundle to manual QA section"
      pattern: "06-QA-MATRIX"
    - from: "src/locales/en.ts + ru.ts"
      to: "scripts/check-i18n-parity.sh"
      via: "post-deletion parity"
      pattern: "amenitiesPhase6Placeholder"
---

<objective>
Close Phase 6 with three coordinated exit tasks:

1. **i18n cleanup** — Delete `hospitality.amenitiesPhase6Placeholder` from BOTH `en.ts` and `ru.ts` in a single commit (parity preserved). Plan 03 already removed the runtime consumer; the key is now orphaned. Plan 07 finishes the placeholder retirement.
2. **Regression bundle** — Create `06-VERIFICATION.md` recording the phase-exit automated-gates + invariants (3 CI scripts, `npm test`, tsc baseline, D-09 anchors, MediaSection `<Gated>` count, PropertyCard zero-diff, requirement-ID → plan traceability).
3. **Manual QA matrix + walk checkpoint** — Create `06-QA-MATRIX.md` (~80 cells per RESEARCH §Validation Architecture). User completes the matrix on physical iPhone (iOS 26) + Moto G (Android 16 / Fabric) with admin (`beckprograms@gmail.com`) + non-admin accounts. This is the BLOCKING phase-exit checkpoint per CLAUDE.md M1 testing bar. On all-PASS, Phase 6 is complete.

Purpose: Phase 6 cannot close without physical-device verification — simulators and unit tests do not certify touch-target sizing, Fabric render correctness, real deep-link behavior to WhatsApp/Telegram/phone, or bilingual overflow on actual viewport widths. Phase 4 and Phase 5 both closed with physical-device QA walks; Phase 6 follows the same pattern.

Output: 2 file edits (locales) + 2 new documentation files. User performs manual walk and records results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-hospitality-rendering/06-CONTEXT.md
@.planning/phases/06-hospitality-rendering/06-RESEARCH.md
@.planning/phases/06-hospitality-rendering/06-VALIDATION.md
@.planning/phases/06-hospitality-rendering/06-UI-SPEC.md
@.planning/phases/06-hospitality-rendering/06-01-PLAN.md
@.planning/phases/06-hospitality-rendering/06-02-PLAN.md
@.planning/phases/06-hospitality-rendering/06-03-PLAN.md
@.planning/phases/06-hospitality-rendering/06-04-PLAN.md
@.planning/phases/06-hospitality-rendering/06-05-PLAN.md
@.planning/phases/06-hospitality-rendering/06-06-PLAN.md
@.planning/phases/04-listing-form-taxonomy-decomposition/04-QA-MATRIX.md
@.planning/phases/04-listing-form-taxonomy-decomposition/04-VERIFICATION.md

<interfaces>
Phase 4 QA matrix precedent (.planning/phases/04-listing-form-taxonomy-decomposition/04-QA-MATRIX.md) — 18 cells across 4 matrices, PASS/FAIL per device/OS column. Phase 5 had 54 cells. RESEARCH §Validation Architecture defines Phase 6's ~80 cells across 10 cell groups.

Phase 4 VERIFICATION precedent (04-VERIFICATION.md) — 10 sections: CI gates, test baselines, manual QA link, D-09 anchor grep verifications, MediaSection Gated count, tsc baseline, overall disposition.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete hospitality.amenitiesPhase6Placeholder from both locales (single commit)</name>
  <files>src/locales/en.ts, src/locales/ru.ts</files>
  <read_first>
    - src/locales/en.ts (grep for `hospitality.amenitiesPhase6Placeholder` — confirm exactly 1 occurrence)
    - src/locales/ru.ts (same — confirm exactly 1 occurrence)
    - src/components/CreateListingForm/HospitalitySection.tsx (Plan 03 removed the runtime consumer — confirm 0 references before deleting the key)
    - src/ (grep for any remaining consumer — expected 0 hits across src/)
    - .planning/phases/06-hospitality-rendering/06-UI-SPEC.md § Keys deleted at end of Wave (after picker UX lands)
  </read_first>
  <behavior>
    - `hospitality.amenitiesPhase6Placeholder` key + value removed from BOTH locale files in one commit (parity-safe)
    - `./scripts/check-i18n-parity.sh` continues to exit 0 after the deletion
    - `Record<TranslationKeys,string>` tsc gate does not break — no orphan consumer remains
    - No other keys are deleted, renamed, or reordered
  </behavior>
  <action>
    **Step A — pre-flight consumer check.** Run:
    ```bash
    grep -rn "hospitality.amenitiesPhase6Placeholder" src/ || echo "CLEAN"
    ```
    Expected: `CLEAN` (Plan 03 removed the only consumer in `src/components/CreateListingForm/HospitalitySection.tsx`). If any src/ file still references the key, STOP — route to a follow-up mini-plan before deletion. Never delete a locale key that has a live consumer.

    **Step B — locate and delete from en.ts.** In `src/locales/en.ts`, find the line:
    ```typescript
    'hospitality.amenitiesPhase6Placeholder': 'Amenities list — coming in a future update',
    ```
    Delete exactly that line (and only that line). Do NOT touch any adjacent key.

    **Step C — locate and delete from ru.ts.** In `src/locales/ru.ts`, find the matching line (the RU translation — inspect to confirm the exact string). Delete exactly that line.

    Commit: `chore(06-07): delete hospitality.amenitiesPhase6Placeholder i18n key (replaced by D-18 picker in Plan 03)`. Both files committed together.
  </action>
  <verify>
    <automated>! grep -rn "hospitality.amenitiesPhase6Placeholder" src/ &amp;&amp; ./scripts/check-i18n-parity.sh &amp;&amp; ./scripts/check-role-grep.sh &amp;&amp; ./scripts/check-land-removed.sh &amp;&amp; npm test 2&gt;&amp;1 | tail -n 5 &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "hospitality.amenitiesPhase6Placeholder" src/` returns no matches (0 references anywhere in src/)
    - `grep -c "hospitality.amenitiesPhase6Placeholder" src/locales/en.ts` returns `0`
    - `grep -c "hospitality.amenitiesPhase6Placeholder" src/locales/ru.ts` returns `0`
    - `./scripts/check-i18n-parity.sh` exits 0 (parity preserved — both keys removed together)
    - `./scripts/check-role-grep.sh` exits 0
    - `./scripts/check-land-removed.sh` exits 0
    - All other existing keys still present: `grep -c "'amenity.wifi'" src/locales/en.ts` returns `1`, same for ru.ts; `grep -c "'hospitality.amenities'" src/locales/en.ts` returns `1` (the Phase 4 key — NOT deleted)
    - `npx tsc --noEmit` — no new errors beyond baseline 16 lines; specifically no `Record<TranslationKeys,string>` error from an orphan consumer
    - `npm test` — still green
  </acceptance_criteria>
  <done>
    Placeholder key deleted from both locale files in parity-safe single commit. Zero consumers remain. All CI gates green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 06-VERIFICATION.md regression bundle</name>
  <files>.planning/phases/06-hospitality-rendering/06-VERIFICATION.md (NEW)</files>
  <read_first>
    - .planning/phases/04-listing-form-taxonomy-decomposition/04-VERIFICATION.md (structural analog — 10 sections)
    - .planning/phases/05-listing-form-validation-edit-flow/05-VERIFICATION.md (if exists — another precedent)
    - .planning/phases/06-hospitality-rendering/06-VALIDATION.md § Phase-Gate Regression Bundle
    - .planning/phases/06-hospitality-rendering/06-RESEARCH.md § Validation Architecture § Phase-gate regression bundle
    - 06-01-PLAN.md through 06-06-PLAN.md (all 6 prior plan summaries once they exist — reference their must_haves for traceability matrix)
  </read_first>
  <behavior>
    - 06-VERIFICATION.md exists with clear section headings
    - Every CI gate, test baseline, and invariant mentioned in RESEARCH §Phase-gate regression bundle has a row with the exact command + expected output/count + PASS/FAIL disposition column
    - A traceability matrix maps each HOSP-01..06 requirement to the plan(s) that delivered it
    - A D-09 anchor verification section has the 3 grep commands with "exactly 1 hit" expected results (and notes Gap 9.2's potential line shift from Plan 02)
    - Phase 3 D-14, Phase 4 FORM-01/FORM-09, Phase 4 `<Gated>` count, PropertyCard zero-diff — each has a command + expected result
    - tsc baseline recorded (16 lines — AuthContext + ThemeContext pre-existing)
    - Total automated-gate count tallied with PASS status before user walks the manual matrix
  </behavior>
  <action>
    Create `.planning/phases/06-hospitality-rendering/06-VERIFICATION.md` with the following structure. Run each command; record its ACTUAL output alongside the expected; mark disposition PASS/FAIL. If any FAIL, STOP — escalate to orchestrator before proceeding to Task 3.

    ```markdown
    # Phase 6 — Verification Bundle

    **Created:** <today's date>
    **Phase status:** AWAITING-MANUAL-QA (all automated gates must PASS before user walks 06-QA-MATRIX.md)

    ## 1. CI Gate Scripts

    | Gate | Origin | Command | Expected | Actual | Disposition |
    |------|--------|---------|----------|--------|-------------|
    | Role grep | Phase 3 D-14 | `./scripts/check-role-grep.sh` | exit 0 | <record> | PASS/FAIL |
    | Land removed | Phase 4 FORM-01 | `./scripts/check-land-removed.sh` | exit 0 | <record> | PASS/FAIL |
    | i18n parity | Phase 4 FORM-09 | `./scripts/check-i18n-parity.sh` | exit 0 | <record> | PASS/FAIL |

    ## 2. Test Suite Baseline

    | Suite | Command | Phase 5 baseline | Phase 6 actual | Disposition |
    |-------|---------|-------------------|----------------|-------------|
    | Full | `npm test` | 50/50 across 6 suites | <record; expect 50 + 4 new amenity = 54+> | PASS/FAIL |
    | Validators only | `npm test -- --testPathPattern validators` | <baseline count> | <record> | PASS/FAIL |

    Plan 02 Task 1 added 4 new assertions + 1 flipped assertion. Expected delta: +4 net-new passing tests.

    ## 3. TypeScript Baseline

    | Metric | Command | Phase 5 baseline | Phase 6 actual | Disposition |
    |--------|---------|-------------------|----------------|-------------|
    | tsc error line count | `npx tsc --noEmit 2>&1 \| wc -l` | 16 | <record> | PASS/FAIL (=baseline) |

    Expected unchanged: AuthContext (10) + ThemeContext (2) = 16 lines pre-existing.

    ## 4. Phase 4 D-09 Preserve-on-Save Anchors

    Gap 9.2 in Plan 02 added 3 lines inside the rehydrate useEffect, which may have shifted line numbers. Anchor grep patterns MUST still return exactly 1 hit each.

    | Anchor | Grep command (adapt pattern from 04-VERIFICATION.md) | Expected | Actual | Disposition |
    |--------|-------------------------------------------------------|----------|--------|-------------|
    | A — rehydrate useEffect entry | <04-VERIFICATION.md pattern> | 1 | <record> | PASS/FAIL |
    | B — panoramicPhotosUrl payload | `grep -c "panoramicPhotosUrl: panoramicPhotosUrl.trim()" src/screens/CreateListingScreen.tsx` | 1 | <record> | PASS/FAIL |
    | C — tours ternary payload | `grep -c "tours: tours.length > 0 ? tours : undefined" src/screens/CreateListingScreen.tsx` | 1 | <record> | PASS/FAIL |

    If anchor A line number shifted, record the new line number here and confirm 04-VERIFICATION.md was updated in Plan 02.

    ## 5. Phase 4 MediaSection `<Gated>` Invariant

    | Invariant | Command | Expected | Actual | Disposition |
    |-----------|---------|----------|--------|-------------|
    | Gated count in MediaSection | `grep -c '<Gated' src/components/CreateListingForm/MediaSection.tsx` | 2 | <record> | PASS/FAIL |

    Phase 6 introduces zero new `<Gated>` wraps per D-24.

    ## 6. PropertyCard Zero-Diff (D-07 / Pitfall 7)

    | Invariant | Command | Expected | Actual | Disposition |
    |-----------|---------|----------|--------|-------------|
    | PropertyCard unchanged | `git diff src/components/PropertyCard.tsx \| wc -l` | 0 | <record> | PASS/FAIL |

    Any non-zero output indicates D-07 regression — the HospitalityCard effort leaked into PropertyCard.

    ## 7. Phase 6 Net-New File Inventory

    | File | Expected LOC range | Actual | Disposition |
    |------|-------------------|--------|-------------|
    | src/utils/hospitalityAmenities.ts | 40-80 | <record> | PASS/FAIL |
    | src/components/HospitalityCard.tsx | 220-320 | <record> | PASS/FAIL |
    | src/components/HospitalitySection.tsx | 100-180 | <record> | PASS/FAIL |

    ## 8. Requirement ID → Plan Traceability

    | REQ ID | Delivered by plan(s) | Observable outcome | Disposition |
    |--------|----------------------|---------------------|-------------|
    | HOSP-01 | 04 (section component) + 05 (screen mounts) | Strip renders on 4 list screens, hidden when empty | PASS/FAIL |
    | HOSP-02 | 05 (filter logic) | Hospitality appears under both Rent and Sell, no price | PASS/FAIL |
    | HOSP-03 | 04 (card component) | HospitalityCard renders no-price tour-first | PASS/FAIL |
    | HOSP-04 | 06 (detail branch) | PropertyDetailsScreen Hospitality: no price, tour above gallery, sticky contact bar | PASS/FAIL |
    | HOSP-05 | 01 (taxonomy) + 02 (validator + backend + rehydrate) + 03 (picker) | 12-amenity multi-select + validation + rehydrate + backend wire-up | PASS/FAIL |
    | HOSP-06 | 01 (Wave 0 i18n parity) + 07 (placeholder cleanup) | All 24 amenity labels + strip/badge keys bilingual; check-i18n-parity exits 0 | PASS/FAIL |

    ## 9. New Evidence Files

    | File | Purpose | Exists |
    |------|---------|--------|
    | src/utils/hospitalityAmenities.ts | 12-token taxonomy + AMENITY_ICONS | <yes/no> |
    | src/components/HospitalityCard.tsx | Tour-first card | <yes/no> |
    | src/components/HospitalitySection.tsx | Horizontal strip | <yes/no> |
    | .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md | ~80-cell physical-device walk | <yes/no> |

    ## 10. Overall Disposition

    **Automated gates:** <PASS / FAIL>. All 9 sections above must be PASS before user walks 06-QA-MATRIX.md.

    **Manual QA checkpoint:** BLOCKING per CLAUDE.md M1 testing bar. On all-PASS across the ~80 cells, Phase 6 closes via `/gsd-verify-work`.
    ```

    Fill in the `<record>` placeholders by running each command. The task is complete when every row has a recorded Actual value + disposition.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-hospitality-rendering/06-VERIFICATION.md &amp;&amp; grep -q "CI Gate Scripts" .planning/phases/06-hospitality-rendering/06-VERIFICATION.md &amp;&amp; grep -q "Test Suite Baseline" .planning/phases/06-hospitality-rendering/06-VERIFICATION.md &amp;&amp; grep -q "D-09 Preserve-on-Save" .planning/phases/06-hospitality-rendering/06-VERIFICATION.md &amp;&amp; grep -q "MediaSection" .planning/phases/06-hospitality-rendering/06-VERIFICATION.md &amp;&amp; grep -q "PropertyCard Zero-Diff" .planning/phases/06-hospitality-rendering/06-VERIFICATION.md &amp;&amp; grep -q "HOSP-01" .planning/phases/06-hospitality-rendering/06-VERIFICATION.md &amp;&amp; grep -q "HOSP-06" .planning/phases/06-hospitality-rendering/06-VERIFICATION.md &amp;&amp; wc -l .planning/phases/06-hospitality-rendering/06-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-hospitality-rendering/06-VERIFICATION.md` passes
    - All 10 sections present (CI gates, test baseline, tsc, D-09 anchors, MediaSection Gated, PropertyCard zero-diff, new-file inventory, traceability, evidence files, overall disposition)
    - All 6 HOSP-* requirement IDs have a traceability row
    - File LOC ≥ 80
    - Every command in the tables has been executed and its Actual output recorded (no `<record>` placeholders remain)
    - Overall disposition line at bottom is PASS before handing off to Task 3; if any row FAILED, Task 3 does not begin and the planner is re-engaged
  </acceptance_criteria>
  <done>
    06-VERIFICATION.md exists with all 10 sections populated; every automated gate + invariant has been run and recorded; overall disposition is PASS. Ready for the manual QA matrix walk.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create 06-QA-MATRIX.md scaffold</name>
  <files>.planning/phases/06-hospitality-rendering/06-QA-MATRIX.md (NEW)</files>
  <read_first>
    - .planning/phases/04-listing-form-taxonomy-decomposition/04-QA-MATRIX.md (18-cell precedent — cell format, PASS/FAIL columns, device metadata header)
    - .planning/phases/06-hospitality-rendering/06-RESEARCH.md § Validation Architecture § QA matrix table (10 cell groups totaling ~80 cells)
    - .planning/phases/06-hospitality-rendering/06-VALIDATION.md § Manual-Only Verifications
    - CLAUDE.md § Testing bar
    - User auto-memory: overlays hide the bottom tab bar by design — some cells may be N/A if they depend on seeing tabs under overlays
  </read_first>
  <behavior>
    - 06-QA-MATRIX.md has 10 matrix sections per RESEARCH breakdown, ~80 cells total
    - Device metadata header: Device/OS/Build/Account columns for BOTH iOS (iPhone 15 Pro / iOS 26) and Android (Moto G XT2513V / Android 16 Fabric) per STATE.md Phase 5 QA precedent
    - Each cell has a row with: Cell ID, Behavior under test, Expected observation, iOS PASS/FAIL column, Android PASS/FAIL column, Notes column
    - User executes on both physical devices with admin (`beckprograms@gmail.com`) + non-admin accounts where applicable
    - Sign-off section at the bottom requires user to type "approved" OR enumerate FAIL cells with device/OS details
  </behavior>
  <action>
    Create `.planning/phases/06-hospitality-rendering/06-QA-MATRIX.md`. Mirror Phase 4 + 5 structural precedent; ~80 cells across 10 matrix groups per RESEARCH §Validation Architecture.

    ```markdown
    # Phase 6 — Manual QA Matrix (Physical Devices)

    **Created:** <today>
    **Testing bar:** M1 manual physical-device QA per CLAUDE.md

    ## Test Environment

    | Device | OS | Build Variant | Account |
    |--------|----|----|---------|
    | iPhone 15 Pro | iOS 26.x | Fabric / Release | admin: beckprograms@gmail.com + non-admin: <test-account> |
    | Moto G XT2513V | Android 16 | Fabric / Release | admin + non-admin |

    Seed data required: at least 2 Hostel + 2 Hotel listings populated with varying amenity counts (0 / 1-3 / 4+ / 12), tours (0 / 1+ thumbnailUrl), and owner contact combinations (none / WA only / TG only / phone only / all three).

    ---

    ## Matrix 1 — Strip render on 4 list screens (16 cells)

    | Cell | Screen | Hospitality state | iOS | Android | Notes |
    |------|--------|--------------------|-----|---------|-------|
    | 1.1 | Home | Empty (no Hospitality listings) | ☐ | ☐ | Strip returns null — no header, no placeholder |
    | 1.2 | Home | Populated (≥1) | ☐ | ☐ | Strip renders; title "Hostels & Hotels (N)" visible |
    | 1.3 | Favorites | Empty | ☐ | ☐ | Null render |
    | 1.4 | Favorites | Populated | ☐ | ☐ | Strip at top |
    | 1.5 | RenterListings (My Listings) | Empty | ☐ | ☐ | Null render |
    | 1.6 | RenterListings (My Listings) | Populated | ☐ | ☐ | Strip with showEditButton=true; edit/delete icons on each card |
    | 1.7 | OwnerListings (landlord-profile view) | Empty | ☐ | ☐ | Null render |
    | 1.8 | OwnerListings (landlord-profile view) | Populated | ☐ | ☐ | Strip without edit/delete chrome |

    (2 states × 4 screens × 2 devices = 16)

    ## Matrix 2 — HomeScreen tri-state filter (6 cells)

    | Cell | selectedCategory | iOS | Android | Notes |
    |------|-------------------|-----|---------|-------|
    | 2.1 | Residential | ☐ | ☐ | Vertical list = residential PropertyCards; strip at top if Hospitality populated |
    | 2.2 | Commercial | ☐ | ☐ | Vertical list = commercial PropertyCards; strip at top if Hospitality populated |
    | 2.3 | Hospitality | ☐ | ☐ | Vertical list = HospitalityCards; strip NOT rendered (would be redundant) |

    (3 × 2 = 6)

    ## Matrix 3 — HomeScreen transactionType × Hospitality (4 cells — Pitfall 2)

    | Cell | transactionType | iOS | Android | Notes |
    |------|------------------|-----|---------|-------|
    | 3.1 | Rent | ☐ | ☐ | Strip count reflects Rent-only Hospitality listings (property.type === 'rent') |
    | 3.2 | Sell | ☐ | ☐ | Strip count changes when toggled; reflects Sell-only Hospitality |

    (2 × 2 = 4)

    ## Matrix 4 — HomeScreen type chip under Hospitality (4 cells)

    | Cell | Chip tapped | iOS | Android | Notes |
    |------|--------------|-----|---------|-------|
    | 4.1 | Hostel | ☐ | ☐ | Vertical list narrows to Hostel only |
    | 4.2 | Hotel | ☐ | ☐ | Vertical list narrows to Hotel only |

    (2 × 2 = 4)

    ## Matrix 5 — HospitalityCard variants (8 cells)

    | Cell | Tour | Amenities | iOS | Android | Notes |
    |------|------|-----------|-----|---------|-------|
    | 5.1 | None | 0 | ☐ | ☐ | Fallback hero; no 3D badge; no amenity preview row |
    | 5.2 | None | 1-3 | ☐ | ☐ | Fallback hero; no 3D badge; amenity chips render |
    | 5.3 | Has tour | 0 | ☐ | ☐ | Tour thumbnail hero; 3D Tour badge bottom-left; no amenity row |
    | 5.4 | Has tour | 4+ | ☐ | ☐ | Tour hero; 3D Tour badge; first 3 chips + "+N more" overflow chip |

    (4 × 2 = 8)

    ## Matrix 6 — PropertyDetailsScreen Hostel (16 cells)

    | Cell | Tour | WA | TG | Phone | iOS | Android | Notes |
    |------|------|----|----|-------|-----|---------|-------|
    | 6.1 | No tour | ✗ | ✗ | ✗ | — | ☐ | ☐ | No price block; sticky bar 3 disabled buttons (opacity 0.4) |
    | 6.2 | No tour | ✓ | ✗ | ✗ | — | ☐ | ☐ | WA button active green; TG + Phone disabled |
    | 6.3 | No tour | ✗ | ✓ | ✗ | — | ☐ | ☐ | TG active blue |
    | 6.4 | No tour | ✓ | ✓ | ✓ | — | ☐ | ☐ | All 3 active |
    | 6.5 | Has tour | ✗ | ✗ | ✗ | — | ☐ | ☐ | TourHeroCard above gallery; all 3 disabled |
    | 6.6 | Has tour | ✓ | ✗ | ✗ | — | ☐ | ☐ | Tour above gallery; WA active |
    | 6.7 | Has tour | ✗ | ✓ | ✗ | — | ☐ | ☐ | Tour above; TG active |
    | 6.8 | Has tour | ✓ | ✓ | ✓ | — | ☐ | ☐ | Tour above; all 3 active; WA/TG/phone deep-link opens correct app |

    (8 × 2 = 16)

    ## Matrix 7 — PropertyDetailsScreen Hotel (16 cells)

    Identical structure to Matrix 6 but with a Hotel listing. Type badge reads "Hotel" on card; detail-screen amenity section title reads `hospitality.amenities`.

    (16 cells)

    ## Matrix 8 — Create form amenity picker (6 cells)

    | Cell | Selection | iOS | Android | Notes |
    |------|-----------|-----|---------|-------|
    | 8.1 | 0 amenities (submit) | ☐ | ☐ | Inline error "Select at least one amenity" / "Выберите хотя бы одно удобство" renders below grid |
    | 8.2 | 1 amenity (wifi) | ☐ | ☐ | No error; submit succeeds; round-trip preserves amenity |
    | 8.3 | All 12 amenities | ☐ | ☐ | All chips visible with accent bg + white icon/text; grid wraps cleanly; RU "Камеры хранения" / "Санузел в номере" truncate, do NOT wrap two lines |

    (3 × 2 = 6)

    ## Matrix 9 — Edit-mode rehydrate for Hospitality (Gap 9.2) (2 cells)

    | Cell | Action | iOS | Android | Notes |
    |------|--------|-----|---------|-------|
    | 9.1 | Edit an existing Hospitality listing | ☐ | ☐ | rooms / maxGuests / amenities fields pre-populate from server; no blanking |

    (1 × 2 = 2)

    ## Matrix 10 — EN+RU live language toggle during active filter (2 cells)

    | Cell | Action | iOS | Android | Notes |
    |------|--------|-----|---------|-------|
    | 10.1 | Switch language while Hospitality category active with populated strip + detail open | ☐ | ☐ | All 12 amenity labels retranslate; strip title retranslates; detail amenity chips retranslate |

    (1 × 2 = 2)

    ---

    ## Totals

    16 + 6 + 4 + 4 + 8 + 16 + 16 + 6 + 2 + 2 = **80 cells**

    ## Sign-off

    **User action:** Walk all cells on both physical devices with both accounts where applicable. Mark each cell ☐ → ✅ (PASS) or ❌ (FAIL). For each FAIL, add a Notes entry describing the actual vs expected behavior.

    **On all-PASS:** Record device serial / OS build / git SHA here, then run `/gsd-verify-work` to close Phase 6.

    **On any FAIL:** Surface cell ID + device/OS + actual behavior to the planner; orchestrator routes to a targeted gap-closure plan OR accepted-risk disposition in PROJECT.md per user approval.

    | Metadata | Value |
    |----------|-------|
    | QA walker | <user> |
    | iOS device serial | <fill> |
    | iOS build | <fill> |
    | Android device serial | <fill> |
    | Android build | <fill> |
    | Git SHA at walk start | <fill> |
    | Final disposition | <APPROVED / FAILED cells listed> |
    ```

    Commit: `docs(06-07): QA matrix scaffold (~80 cells) + phase-exit verification bundle`.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md &amp;&amp; grep -c "Matrix [0-9]" .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md | awk '$1&gt;=10 {exit 0} {exit 1}' &amp;&amp; grep -q "## Totals" .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md &amp;&amp; grep -q "## Sign-off" .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md &amp;&amp; grep -q "80 cells" .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md &amp;&amp; wc -l .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md` passes
    - `grep -c "Matrix [0-9]" .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md` returns at least `10`
    - `grep -c "iOS" .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md` returns at least `12` (1 per matrix + header + env + sign-off)
    - `grep -c "Android" .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md` returns at least `12`
    - File references both `iPhone 15 Pro / iOS 26` and `Moto G XT2513V / Android 16 Fabric` in the environment section
    - Total cell count documented as 80 (`grep -q "80 cells"` succeeds)
    - Sign-off metadata table has slots for Git SHA, device serials, and final disposition
    - File LOC ≥ 100
  </acceptance_criteria>
  <done>
    06-QA-MATRIX.md exists with 10 matrices totaling 80 cells, environment metadata header, and sign-off section ready for the user's physical-device walk.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT — User walks physical-device QA matrix</name>
  <what-built>
    Phase 6 complete set of artifacts:
    - 1 new utility file (`hospitalityAmenities.ts`)
    - 2 new components (`HospitalityCard.tsx`, `HospitalitySection.tsx`)
    - Extended type (`Property.ts`), narrowed form type (`FormBag.amenities`), extended validator (amenity required + payload), wired backend (`PropertyService` rooms/maxGuests/amenities), rehydrated edit mode (Gap 9.2), 12-chip form picker, 4 list screens mounted with strip via ListHeaderComponent (HomeScreen tri-state + WR-03 closed), PropertyDetailsScreen 4-branch Hospitality view
    - 32 new EN+RU i18n keys + 1 key deleted (placeholder)
    - All automated gates PASS per 06-VERIFICATION.md
  </what-built>
  <how-to-verify>
    Per CLAUDE.md M1 testing bar: manual physical-device QA on iPhone 15 Pro (iOS 26.x) + Moto G XT2513V (Android 16 / Fabric). Budget ~2-3 hours per device.

    1. Ensure seed data matches Matrix requirements (see 06-QA-MATRIX.md Test Environment section): at least 2 Hostel + 2 Hotel listings with varying amenity counts (0, 1-3, 4+, 12), tour states (none, has thumbnailUrl), and owner-contact combinations (none, WA only, TG only, phone only, all three).

    2. Walk each cell in `.planning/phases/06-hospitality-rendering/06-QA-MATRIX.md` on BOTH devices, recording PASS (✅) or FAIL (❌) per cell per device. Add Notes column entries for any FAIL describing actual vs expected behavior with enough detail to route a fix.

    3. For cells requiring account-context (admin vs non-admin — specifically Matrix 5 owner-context chrome on RenterListings + Matrix 6/7 sticky contact bar deep-link behavior), sign in with `beckprograms@gmail.com` (admin) AND a non-admin test account.

    4. Language-toggle cells (Matrix 10): Use the in-app language toggle to switch EN↔RU while the Hospitality strip is populated + a Hostel/Hotel detail screen is open. Verify all 12 amenity labels + strip title + sticky bar accessibilityLabels retranslate.

    5. Deep-link cells (Matrix 6/7 WhatsApp/Telegram/Phone active states): Tap each active button and confirm the correct external app opens (WhatsApp → WhatsApp app, Telegram → Telegram web or app, Phone → dialer). Do NOT tap disabled buttons (they should have `disabled={true}` and `opacity: 0.4`).

    6. Edit-rehydrate cell (Matrix 9.1): From RenterListings, tap an existing Hospitality listing's edit button, scroll to the HospitalitySection, confirm `rooms` / `maxGuests` fields show the server value (not blank) and selected amenities (e.g. Wi-Fi + A/C) show as selected chips.

    7. Record metadata at the bottom of 06-QA-MATRIX.md: QA walker name, device serials, OS builds, git SHA at walk start, final disposition.

    8. On all-PASS (or FAIL cells user accepts as risk with rationale recorded in PROJECT.md Key Decisions): type "approved" in chat AND run `/gsd-verify-work` to finalize Phase 6.

    9. On any unaccepted FAIL: surface cell ID + device/OS + actual-behavior narrative. Planner creates a gap-closure plan (e.g., `06-08-PLAN.md`) per `/gsd-plan-phase 6 --gaps`.
  </how-to-verify>
  <resume-signal>
    User types "approved" (all 80 cells PASS or accepted-risk documented) or reports cell IDs + device details for any FAIL.
  </resume-signal>
</task>

</tasks>

<verification>
Pre-checkpoint (after Tasks 1-3):

```bash
./scripts/check-role-grep.sh && ./scripts/check-land-removed.sh && ./scripts/check-i18n-parity.sh
npm test 2>&1 | tail                                             # 50 baseline + 4 amenity = 54+
npx tsc --noEmit 2>&1 | wc -l                                    # ~16 (baseline unchanged)
grep -c '<Gated' src/components/CreateListingForm/MediaSection.tsx          # 2
[ -z "$(git diff src/components/PropertyCard.tsx)" ]                         # zero-diff
! grep -rn "hospitality.amenitiesPhase6Placeholder" src/                     # 0 matches
test -f .planning/phases/06-hospitality-rendering/06-VERIFICATION.md
test -f .planning/phases/06-hospitality-rendering/06-QA-MATRIX.md
```

Post-checkpoint (after Task 4 — user walks matrix):
All 80 cells across both devices recorded PASS/FAIL; sign-off metadata filled; overall disposition APPROVED or FAIL list surfaced.
</verification>

<success_criteria>
- `hospitality.amenitiesPhase6Placeholder` deleted from both locale files; zero consumers remain; parity preserved
- `06-VERIFICATION.md` exists with all 10 sections populated (CI gates, test baseline, tsc, D-09, MediaSection, PropertyCard zero-diff, new-file LOC, HOSP-01..06 traceability, evidence, overall disposition)
- `06-QA-MATRIX.md` exists with 10 matrices totaling 80 cells + environment metadata + sign-off table
- User completes physical-device walk on iOS + Android, records PASS/FAIL per cell, signs off
- On all-PASS: `/gsd-verify-work` closes Phase 6
- On FAIL: gap-closure plan routed per `/gsd-plan-phase 6 --gaps`
</success_criteria>

<output>
Create `.planning/phases/06-hospitality-rendering/06-07-SUMMARY.md` recording:
- Confirmation placeholder key deleted from both locale files
- 06-VERIFICATION.md overall disposition (PASS / partial / FAIL — itemize)
- 06-QA-MATRIX.md user walk results: cell-pass counts per device; disposition (APPROVED / itemized FAIL list)
- Final Phase 6 status: COMPLETE (all 6 HOSP-* requirements closed) OR gap-closure needed
- Handoff note for Phase 7 (Alignment Pass): Phase 6 dispositions any visual polish items that the user wants to revisit under a screenshot-driven pass (UI-SPEC §Dark-mode contrast dispositions already deferred to Phase 7)
</output>
