---
phase: 05-hardening-manual-qa-release-v3
plan: 06
type: execute
wave: 4
depends_on: [05-03, 05-05]
files_modified:
  - .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md
autonomous: false
requirements: [REL-05, REL-06]
requirements_addressed: [REL-05, REL-06]
tags: [release, paired-gates, gsd-verifier, gsd-code-reviewer, inheritance-audit, m1-d-13-descope, package-json-diff, no-firebase-sdk-rule, m2-baseline-anchor, security-paired-gate]

must_haves:
  truths:
    - "05-INHERITANCE-AUDIT.md exists in the phase directory documenting the M1 D-13 inheritance descope rationale + per-item re-open conditions for v2.0.0 → v3.0.0"
    - "RN client `package.json` diff between v2.0.0 (M2 close anchor) and v3.0.0 is captured in the artifact, with each new/changed/removed dep classified by data-collection capability + Firebase-SDK-violation"
    - "All seven inheritance items (iOS PrivacyInfo.xcprivacy + ASC App Privacy + Play Console Data Safety + store listing copy/screenshots/icon/category + Maps API key restrictions + applinks entitlement) are explicitly documented as INHERITED with re-open conditions"
    - "Frontmatter `proceed_with_inheritance: true` only if zero RED-flagged items; if ANY new dep has `network: true` AND `collects_pii: true` OR a Firebase-SDK-violation, set `proceed_with_inheritance: false` + populate `flagged_red_items`"
    - "Auto-memory `no-firebase-sdk.md` rule is asserted: `grep -E '\"(firebase|@react-native-firebase/)\"' package.json` returns 0 matches (zero firebase or @react-native-firebase packages added in M3)"
    - "Phase 5 paired-gates run: `/gsd-verify-work 5` (goal-backward verifier on REL-01..REL-06 must_haves) + `/gsd-code-review 5` (regression-forward reviewer on Phase 5 commit chain) — both gates must clear before Plan 05-07 starts (memory `gsd-verifier-misses-regressions.md`)"
    - "Phase 5 verdict aggregation recorded inline: GREEN (both gates pass) / YELLOW (minor issues only — PARTIAL or LOW; ship + log) / RED (CRITICAL or HIGH issue — block Plan 05-07 until fix-loop closes)"
  artifacts:
    - path: ".planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md"
      provides: "REL-06 — descope-by-inheritance audit; binding evidence that v2.0.0 privacy + store-listing declarations carry forward to v3.0.0 unchanged. Plan 05-07's Task 0 reads `proceed_with_inheritance: true` as a binding signal."
      contains: "INHERIT"
      contains_also: "no-firebase-sdk"
  key_links:
    - from: "05-INHERITANCE-AUDIT.md :: package_json_diff section"
      to: "Plan 05-07 :: ASC + Play Console submission inheritance"
      via: "Plan 05-07 reads this audit's `proceed_with_inheritance: true` flag and SKIPS re-authoring privacy/store-listing assets"
      pattern: "proceed_with_inheritance: true"
    - from: "Phase 5 paired-gates verdict (GREEN | YELLOW | RED)"
      to: "Plan 05-07 :: Task 0 prerequisite check"
      via: "Plan 05-07 only proceeds if paired-gates verdict is GREEN or YELLOW (with documented rationale); RED blocks"
      pattern: "phase_5_paired_gates_verdict"
---

<objective>
Wave 4 of Phase 5 — paired-gate verifier+reviewer for end-of-phase Phase 5 close + descope-by-inheritance audit for v3.0.0 submission.

Two distinct deliverables, both feeding Plan 05-07:

1. **Phase 5 paired-gate close** (per memory `gsd-verifier-misses-regressions.md`): in M2 Phase 1, the verifier said PASS 15/15 while the reviewer found 2 CRITICAL regressions in the same commit chain. Verifier + reviewer are independent gates and both must clear before phase close. Plan 05-06 invokes:
   - `/gsd-verify-work 5` — goal-backward verifier on REL-01..REL-06 must_haves (does each requirement have evidence at the code/artifact surface?)
   - `/gsd-code-review 5` — regression-forward reviewer on Phase 5 commit chain (do upstream changes break downstream surfaces? does the MEDIUM-01/02 fix introduce new bugs? do the version-bump edits regress any CI gate?)
   The two gate verdicts are aggregated into GREEN | YELLOW | RED; Plan 05-07 reads the aggregate.

2. **Inheritance audit** (M1 D-13 + M2 D-13 carry-forward): produce `05-INHERITANCE-AUDIT.md` documenting that v3.0.0 inherits v2.0.0's iOS PrivacyInfo.xcprivacy + ASC App Privacy answers + Play Console Data Safety + store-listing copy + screenshots + Maps API key restrictions + applinks entitlement, plus the spot-check evidence that no new data-collecting dependency landed in M3 (Phases 1-4 were schema/route/UI work + bug fixes; no new external SDKs in the RN client).

Why bundle the two: they're both Wave-4 (need Plan 05-03 atomic bump + Plan 05-05 walk APPROVED to land), they're both reading-and-classifying tasks (no source-file mutation), and they both feed Plan 05-07 as binding gates. The verifier+reviewer outputs inform whether the inheritance-audit dispositions can stay INHERITED (a CRITICAL regression might re-open privacy declarations).

CONTEXT.md `Claude's Discretion › INHERITANCE-AUDIT.md format`: M3 expected outcome is 7/7 INHERITED — no new data-collecting SDKs, no privacy manifest changes, no entitlement changes, no Maps key restriction changes. Re-open condition unchanged: future phase introduces new data-collecting SDK or auth provider.

The package.json diff for M3: M3 was schema (Phase 1) / route (Phase 1) / UI (Phase 2) / mod media (Phase 3) / bug fixes (Phase 4) — no new SDKs anticipated. Verify empirically with `git diff <v2.0.0-SHA> HEAD -- package.json`.

Output: `05-INHERITANCE-AUDIT.md` with the M2 06-INHERITANCE-AUDIT.md analog structure + the Phase 5 paired-gate verdict aggregation block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/RETROSPECTIVE.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-CONTEXT.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-PATTERNS.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md
@.planning/phases/04-m2-carry-forward-bug-fixes/REVIEW.md
@.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-INHERITANCE-AUDIT.md
@.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-02-PLAN.md
@.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md
@CLAUDE.md
@package.json

<interfaces>
<!-- v2.0.0 baseline reference (the M3 baseline anchor): -->
<!-- M2 v2.0.0 release commit SHA can be found in the M2 Phase 6 SUBMISSION-LOG.md frontmatter `submitted_against_sha`. -->
<!-- The M2 close commit on the JayTap RN client repo is referenced in memory `m2-shipped-2026-05-05.md`. -->

```bash
# Locate the M2 v2.0.0 release SHA — first try the M2 Phase 6 SUBMISSION-LOG:
V200_SHA=$(grep -E '^\s*submitted_against_sha:\s*[0-9a-f]{7,40}' .planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md | grep -oE '[0-9a-f]{7,40}')
# If empty, locate via git log for the chore(06-05) atomic-bump commit:
[ -z "$V200_SHA" ] && V200_SHA=$(git log --oneline | grep 'chore(06-05).*2.0.0' | head -1 | cut -d' ' -f1)
# Fallback: the M2 archive close commit per memory m2-shipped-2026-05-05.md
[ -z "$V200_SHA" ] && V200_SHA=$(git log --oneline --grep='M2 v2.0' | head -1 | cut -d' ' -f1)
```

M1 + M2 D-13 inheritance scope (from M2 06-INHERITANCE-AUDIT.md):
- iOS `PrivacyInfo.xcprivacy` — DO NOT MODIFY; live in production with v2.0.0, accepted by Apple
- iOS `JayTap.entitlements` — DO NOT MODIFY; live in production with v2.0.0
- ASC App Privacy answers — inherited from v2.0.0 (no new data types collected in M3)
- Play Console Data Safety declarations — inherited from v2.0.0
- Maps API key restrictions — inherited (no package or keystore change)
- Store listing copy / screenshots / app icon / category — inherited
- applinks entitlement — inherited

Repo rule per CLAUDE.md "Stack Decisions" + auto-memory `no-firebase-sdk.md`:
- NO `firebase` or `@react-native-firebase/*` package may be added to the RN client repo. Backend uses `jose` for JWKS verification.

Auto-memory `no-firebase-sdk.md`:
> Firebase is identity provider via Identity Toolkit REST. Do NOT propose `firebase` / `@react-native-firebase/*` packages in the RN client repo — caused issues previously.

M3 expected dependency set (sampled from package.json at planning time, 2026-05-07):
- dependencies: @react-native-async-storage/async-storage, @react-native-community/datetimepicker, @react-native/new-app-screen, axios, lucide-react-native, react, react-native, react-native-image-picker, react-native-keyboard-controller, react-native-linear-gradient, react-native-maps, react-native-reanimated, react-native-safe-area-context, react-native-svg, react-native-webview, react-native-worklets, socket.io-client
- devDependencies: @babel/* (3 packages), @react-native-community/cli + 2 platform variants, @react-native/babel-preset/eslint-config/metro-config/typescript-config, @types/jest, @types/react, @types/react-test-renderer, eslint, jest, prettier, react-test-renderer, typescript

Zero `firebase` or `@react-native-firebase/*` keys present at planning time. M3 close should preserve this.

Paired-gate invocation pattern per memory `gsd-verifier-misses-regressions.md`:
- `/gsd-verify-work 5` — verifier reads ROADMAP Phase 5 success criteria + REQUIREMENTS.md REL-01..REL-06 + plan-level must_haves; checks evidence at code/artifact surface; produces verdict (PASS / PARTIAL / FAIL) per requirement.
- `/gsd-code-review 5` — reviewer reads Phase 5 commit chain (git log $(git merge-base main HEAD)..HEAD); identifies CRITICAL / HIGH / MEDIUM / LOW issues; checks Security Invariant Verification table; produces aggregate verdict.

Reviewer output format reference: `.planning/phases/04-m2-carry-forward-bug-fixes/REVIEW.md` (Phase 4's own paired-gate close — frontmatter + Critical/High/Medium/Low sections + Security Invariant Verification table).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run paired-gates `/gsd-verify-work 5` + `/gsd-code-review 5` and aggregate verdicts</name>
  <files>(read-only — verdicts captured in chat; aggregated into Task 2's 05-INHERITANCE-AUDIT.md frontmatter)</files>
  <read_first>
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/gsd-verifier-misses-regressions.md (re-anchor paired-gates discipline — verifier alone misses regressions; reviewer is mandatory)
    - .planning/phases/04-m2-carry-forward-bug-fixes/REVIEW.md (read full file — internalize the reviewer output structure: frontmatter + Critical/High/Medium/Low sections + Security Invariant Verification table; this is the format reference for what gsd-code-review output looks like)
    - .planning/REQUIREMENTS.md lines 78-86 (REL-01..REL-06 acceptance criteria — what the verifier reads as the goal-backward gate)
    - .planning/phases/05-hardening-manual-qa-release-v3/05-PREFLIGHT.md (binding gate inputs for paired-gate context)
    - .planning/phases/05-hardening-manual-qa-release-v3/05-QA-MATRIX.md (Plan 05-05 walk_disposition input)
  </read_first>
  <action>
    Two-step paired-gate invocation + verdict aggregation. Both gate invocations are operator-driven — the user runs the slash commands; the executor reads the resulting outputs.

    **Step 1 — Invoke `/gsd-verify-work 5`:**

    The user runs `/gsd-verify-work 5` in their session. The verifier:
    - Reads ROADMAP Phase 5 success criteria (REL-01..REL-06 acceptance criteria from REQUIREMENTS.md)
    - Reads each Phase 5 plan's `must_haves` block (frontmatter)
    - Checks each must_have at the code/artifact surface (file existence, grep patterns, command exit codes)
    - Cross-references 05-PREFLIGHT.md frontmatter (verify-PASS timestamps + health_check_status + tsc count)
    - Cross-references 05-QA-MATRIX.md frontmatter (`walk_disposition: APPROVED`)
    - Produces per-requirement verdict: PASS / PARTIAL / FAIL with evidence

    Capture the verifier's output:
    - Total requirement count (should be 6 — REL-01..REL-06)
    - PASS count
    - PARTIAL count (with rationale per cell)
    - FAIL count (with cell + missing-evidence)
    - human_needed count (if any item requires operator confirmation that wasn't captured automatically)
    - Aggregate verdict: PASS (all 6 PASS) / PARTIAL (mix of PASS + PARTIAL, no FAIL) / FAIL (any FAIL)

    **Step 2 — Invoke `/gsd-code-review 5`:**

    The user runs `/gsd-code-review 5` in their session. The reviewer:
    - Reads the Phase 5 commit chain (git log of all commits from Phase 5 plans — Plan 05-01's 2 commits + Plan 05-03's 1 commit + any walked-matrix commits + Plan 05-06's audit commit)
    - Identifies regressions vs Phase 4 close baseline:
      - Did Plan 05-03's atomic bump regress any CI gate (i18n parity / atomic-deletion sentinel / npm test)?
      - Did Plan 05-01 Tasks 1+2 (MEDIUM-01 + MEDIUM-02) introduce any new bug at the touched-handler surfaces?
      - Did the schema migration land any orphaned data?
      - Did the backend deploy break any M2 surface (regression-forward check on listing read paths after the v3.0.0 nested-shape cutover)?
    - Runs the Security Invariant Verification table:
      - Anti-spoofing on actorUid (M2 HF-03)
      - Anti-spoofing on landlord-uid (Phase 4 D-07)
      - Multer absent from propertyRoutes.js (Phase 3 D-13)
      - i18n parity (M1 + M2 + M3 invariant)
      - INVALID_ID 400 on 6 moderationRoutes handlers (5 new from MEDIUM-02 + 1 existing DELETE)
      - verify-PASS pure probe (MEDIUM-01)
    - Produces issue list classified by severity:
      - CRITICAL: blocks ship
      - HIGH: blocks ship unless fix-loop closes
      - MEDIUM: ships with note + M4 backlog
      - LOW: ships with note + M4 polish
    - Aggregate verdict: GREEN (no CRITICAL or HIGH) / YELLOW (CRITICAL=0, HIGH≤1 with documented rationale) / RED (any CRITICAL OR HIGH≥2)

    Capture the reviewer's output similarly.

    **Step 3 — Aggregate verdicts:**

    Combine the two gate outputs into a single Phase 5 paired-gate verdict:

    | Verifier | Reviewer | Combined Verdict |
    |----------|----------|------------------|
    | PASS | GREEN | GREEN — proceed to Plan 05-07 |
    | PASS | YELLOW | YELLOW — proceed; document the YELLOW rationale in 05-INHERITANCE-AUDIT.md |
    | PASS | RED | RED — open fix-loop; do NOT proceed to Plan 05-07 |
    | PARTIAL | GREEN | YELLOW — proceed; document the PARTIAL items + M4 backlog |
    | PARTIAL | YELLOW | YELLOW — proceed if PARTIALs + YELLOW issues are all M4-backlog-class; document |
    | PARTIAL | RED | RED — open fix-loop |
    | FAIL | * | RED — open fix-loop on the failing requirement |
    | * | RED | RED — open fix-loop on the regression |

    **Step 4 — Record verdicts:**

    Capture both gates' raw outputs into chat for Task 2's executor reference. The aggregate verdict + each gate's per-item disposition feeds into 05-INHERITANCE-AUDIT.md's `phase_5_paired_gates:` frontmatter block (Task 2 step 5).

    If the aggregate verdict is RED, HALT — do NOT proceed to Task 2. Open a fix-loop:
    - For verifier FAIL: identify the missing evidence for the failing requirement; either supply the evidence (if it exists but wasn't captured) or fix the gap (if the requirement isn't actually satisfied).
    - For reviewer CRITICAL/HIGH: identify the regression's root cause; commit a fix; re-run the affected gate.
    - Once fix-loop closes, re-run both gates from scratch.
  </action>
  <verify>
    <automated>echo "Manual verdict capture step — operator-confirmed output from /gsd-verify-work 5 + /gsd-code-review 5 commands"</automated>
  </verify>
  <acceptance_criteria>
    - Operator-confirmed observation: `/gsd-verify-work 5` was invoked; the verifier output is captured in chat; per-requirement verdicts (REL-01..REL-06) are recorded with PASS/PARTIAL/FAIL classification + evidence
    - Operator-confirmed observation: `/gsd-code-review 5` was invoked; the reviewer output is captured in chat; per-issue classification (CRITICAL/HIGH/MEDIUM/LOW) is recorded with file/line references
    - Aggregate verdict (GREEN/YELLOW/RED) is determined per the Step 3 truth table and recorded for Task 2 to consume
    - If aggregate is RED, the fix-loop is opened (Plan 05-06 does NOT proceed to Task 2 until verdict is GREEN or YELLOW)
    - Security Invariant Verification table records: anti-spoofing actorUid (PASS/FAIL), anti-spoofing landlord-uid (PASS/FAIL), multer-stripped (PASS/FAIL), i18n parity (PASS/FAIL), INVALID_ID 6 sites (PASS/FAIL), MEDIUM-01 verify-PASS pure-probe (PASS/FAIL) — at least 6 invariants checked
    - Reviewer captures whether Plan 05-03's atomic bump broke any CI gate (i18n parity / check-create-listing-screen-removed / npm test all green post-bump)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Diff RN client package.json v2.0.0 vs v3.0.0 + author 05-INHERITANCE-AUDIT.md (with paired-gate verdict block)</name>
  <files>.planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md</files>
  <read_first>
    - .planning/RETROSPECTIVE.md (re-anchor M1 Key Lesson #2 — "Descope-by-inheritance saves enormous churn on update submissions")
    - .planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-INHERITANCE-AUDIT.md (full file — copy the SHAPE: frontmatter + 7 inheritance dispositions + per-item rationale + re-open conditions; this is the EXACT analog per 05-PATTERNS.md)
    - .planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-02-PLAN.md (read full file — internalize the package_json_diff classification cadence: added / removed / changed / added_dev / removed_dev / changed_dev with network/collects_pii/firebase_sdk_violation classification)
    - .planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md (read frontmatter — extract `submitted_against_sha` as the v2.0.0 baseline SHA)
    - CLAUDE.md (re-anchor "no Firebase SDK in this repo" repo rule + Stack Decisions section)
    - package.json (read full file — sample current dependencies + devDependencies for the v3.0.0 state)
    - .planning/phases/05-hardening-manual-qa-release-v3/05-PATTERNS.md lines 163-228 (read the M3-specific 7-item disposition table + expected outcome 7/7 INHERITED)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/no-firebase-sdk.md (re-anchor the auto-memory entry)
  </read_first>
  <action>
    Three-step diff + audit-authoring sequence. NO source-file mutation. Append the Task 1 paired-gate verdict block at the end.

    **Step 1 — Resolve the v2.0.0 baseline SHA:**

    ```bash
    # Try M2 Phase 6 SUBMISSION-LOG first (most authoritative):
    V200_SHA=$(grep -E '^\s*submitted_against_sha:\s*[0-9a-f]{7,40}' .planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md | grep -oE '[0-9a-f]{7,40}')
    # Fallback 1 — locate via git log for the M2 atomic-bump commit:
    [ -z "$V200_SHA" ] && V200_SHA=$(git log --oneline | grep -E 'chore\(06-05\).*2\.0\.0|atomic v2\.0\.0' | head -1 | cut -d' ' -f1)
    # Fallback 2 — use the most recent commit that touched package.json before the v3.0.0 bump:
    [ -z "$V200_SHA" ] && V200_SHA=$(git log --oneline -- package.json | grep -v '3.0.0' | head -1 | cut -d' ' -f1)
    echo "v2.0.0 baseline SHA: $V200_SHA"
    ```

    Record the SHA used in the artifact's frontmatter (`v200_baseline_sha`). If all three paths fail, surface to user and HALT — the audit can't run without a baseline.

    **Step 2 — Capture the package.json diff:**

    ```bash
    git show $V200_SHA:package.json | jq -r '.dependencies | keys | sort | join("\n")' > /tmp/v200-deps.txt
    git show $V200_SHA:package.json | jq -r '.devDependencies | keys | sort | join("\n")' > /tmp/v200-devdeps.txt
    jq -r '.dependencies | keys | sort | join("\n")' package.json > /tmp/v300-deps.txt
    jq -r '.devDependencies | keys | sort | join("\n")' package.json > /tmp/v300-devdeps.txt
    diff -u /tmp/v200-deps.txt /tmp/v300-deps.txt > /tmp/dep-diff.txt
    diff -u /tmp/v200-devdeps.txt /tmp/v300-devdeps.txt > /tmp/devdep-diff.txt

    # Also capture version-only changes (same key, new version):
    git show $V200_SHA:package.json | jq -r '.dependencies | to_entries | map(.key + ":" + .value) | .[]' | sort > /tmp/v200-deps-with-versions.txt
    jq -r '.dependencies | to_entries | map(.key + ":" + .value) | .[]' package.json | sort > /tmp/v300-deps-with-versions.txt
    diff -u /tmp/v200-deps-with-versions.txt /tmp/v300-deps-with-versions.txt > /tmp/dep-version-diff.txt

    # Repo-rule check: zero firebase/firebase-admin/@react-native-firebase keys
    grep -cE '"(firebase|firebase-admin|@firebase/|@react-native-firebase/)"' package.json   # MUST return 0
    ```

    Capture the diff outputs verbatim into the artifact's `package_json_diff` body section.

    Per CONTEXT.md `Claude's Discretion › INHERITANCE-AUDIT.md format`, M3 expected outcome: 7/7 INHERITED. Verify empirically:
    - If `dep-diff.txt` is empty → `byte-for-byte identical dependencies` — reuse M2's "empirical conclusion: byte-for-byte identical" prose verbatim.
    - If `dep-diff.txt` shows version-only changes (same key, different value) → classify each: most version bumps are non-data-collecting (axios bumps, react-native bumps); flag any that introduce a new SDK family.
    - If `dep-diff.txt` shows added/removed keys → classify each per the dimensions below.

    **Step 3 — Classify each diff entry on three dimensions:**

    For every dep added / removed / changed between v2.0.0 and v3.0.0, classify it on:
    - `network: true|false` — does the package make outbound network calls? (e.g., axios → true, lucide-react-native → false)
    - `collects_pii: true|false` — does the package collect PII (analytics, crash reporting, fingerprinting)?
    - `firebase_sdk_violation: true|false` — does the package match `firebase` OR `firebase-admin` OR `@firebase/*` OR `@react-native-firebase/*` (auto-memory `no-firebase-sdk.md` violation)?

    Expected classification (orchestrator hypothesis — verify at execution time): zero new deps with `network: true`, zero with `collects_pii: true`, zero `firebase_sdk_violation`. M3 added schema/route/UI work + bug fixes, but no new external SDKs in the RN client.

    If ANY entry has `firebase_sdk_violation: true`, HALT the plan and surface to the user — that's a CLAUDE.md repo-rule violation that must be removed before Phase 5 ships.

    If ANY entry has `network: true` AND `collects_pii: true`, set frontmatter `proceed_with_inheritance: false` and populate `flagged_red_items: [<package-name>]`. The user must update privacy declarations before Plan 05-07 ships.

    **Step 4 — Author `05-INHERITANCE-AUDIT.md` using the M2 06-INHERITANCE-AUDIT.md analog:**

    Use the Write tool. Frontmatter:

    ```yaml
    ---
    phase: 05-hardening-manual-qa-release-v3
    plan: 06
    type: inheritance-audit
    audited_at: <ISO 8601 timestamp>
    v200_baseline_sha: <V200_SHA from Step 1>
    v300_audit_sha: <git rev-parse HEAD output for RN client at audit time>
    commits_since_baseline: <int — git log $V200_SHA..HEAD --oneline | wc -l>
    package_json_commits_since_baseline: <int — git log $V200_SHA..HEAD --oneline -- package.json | wc -l>

    package_json_diff:
      added: []   # any new dep keys (orchestrator hypothesis: empty)
      removed: []   # any removed dep keys
      changed: []   # version-only changes (e.g., react-native 0.84.0 → 0.84.1)
      added_dev: []
      removed_dev: []
      changed_dev: []

    classification:
      firebase_sdk_violations: []   # MUST be empty per CLAUDE.md repo rule
      new_network_data_collecting: []   # MUST be empty for proceed_with_inheritance: true
      new_network_no_data_collection: []   # network but no PII (e.g., a CDN client) — soft-flag for ASC App Privacy review
      new_no_network: []   # offline-only utility packages — INHERITED dispositions safe

    inheritance_dispositions:
      ios_privacy_info_xcprivacy:
        disposition: INHERIT
        rationale: "v2.0.0's PrivacyInfo.xcprivacy is live in production accepted by Apple. M3 added zero data-collecting SDKs (verified via package.json diff above). No changes to Required Reasons API usage. Re-open if any future SDK adds NSPrivacyAccessedAPITypes."
        re_open_conditions:
          - "Future phase introduces a new data-collecting SDK"
          - "Apple updates the Required Reasons API list and our existing usages need re-declaration"
      asc_app_privacy_questionnaire:
        disposition: INHERIT
        rationale: "v2.0.0's ASC App Privacy answers describe data types collected by the existing dep set (axios for backend calls, react-native-image-picker for user-uploaded photos in M1+M2 — preserved through Phase 1 SCHEMA-02 migration). M3 changes user-side media flow (Phase 3 inverts to mod-uploaded; users no longer upload), but the existing 'photos collected' declaration covers historical user uploads + the mod-uploaded photos which are still associated with user accounts."
        re_open_conditions:
          - "Future phase introduces a new data-collecting SDK"
          - "Apple's App Privacy questionnaire schema changes"
      play_console_data_safety:
        disposition: INHERIT
        rationale: "Same logic as ASC App Privacy — no new data types collected. Play Console Data Safety from v2.0.0 carries forward."
        re_open_conditions:
          - "Future phase introduces a new data-collecting SDK"
          - "Google's Data Safety questionnaire schema changes"
      store_listing_copy:
        disposition: INHERIT
        rationale: "v2.0.0 store listing copy + screenshots + app icon + category remain accurate for v3.0.0. The new 6-step flow is the headline release-notes feature (Plan 05-04 owns the copy refresh for that surface ONLY, NOT the store listing)."
        re_open_conditions:
          - "Future phase changes app category or core value proposition"
          - "Localization expands to KK/UZ"
      app_icon_screenshots_category:
        disposition: INHERIT
        rationale: "App icon + screenshots + category unchanged. The 6-step flow doesn't warrant new screenshots for the internal-testing audience; if v3.0 graduates to production review, screenshots may need refresh."
        re_open_conditions:
          - "v3.0 graduates from internal-testing to production review"
          - "Major UI redesign in a future phase"
      maps_api_key_restrictions:
        disposition: INHERIT
        rationale: "Google Maps API key restrictions (bundle ID + package name allow-list) unchanged. M3 didn't change bundle ID or package name."
        re_open_conditions:
          - "Bundle ID or package name changes"
          - "Maps key rotated"
      applinks_entitlement:
        disposition: INHERIT
        rationale: "iOS applinks entitlement (associated domains for deep linking) unchanged in M3. The 6-step flow doesn't introduce new deep link routes."
        re_open_conditions:
          - "Future phase adds new deep link routes"
          - "Domain ownership changes"

    plan_07_disposition_signal:
      proceed_with_inheritance: <true | false based on classification + paired-gate verdict>
      flagged_red_items: []   # populate if any new dep has network=true AND collects_pii=true
      summary: "M3 expected outcome 7/7 INHERITED per CONTEXT.md `Claude's Discretion › INHERITANCE-AUDIT.md format`. Phase 5 paired-gate verdict <GREEN | YELLOW | RED> from Plan 05-06 Task 1 — see phase_5_paired_gates block below."

    phase_5_paired_gates:
      verifier_invoked_at: <ISO 8601 from Task 1>
      verifier_total_requirements: 6   # REL-01..REL-06
      verifier_pass: <int>
      verifier_partial: <int>
      verifier_fail: <int>
      verifier_aggregate: <PASS | PARTIAL | FAIL>
      reviewer_invoked_at: <ISO 8601 from Task 1>
      reviewer_critical: <int>
      reviewer_high: <int>
      reviewer_medium: <int>
      reviewer_low: <int>
      reviewer_aggregate: <GREEN | YELLOW | RED>
      paired_gate_verdict: <GREEN | YELLOW | RED>
      paired_gate_rationale: |
        Combined verdict per Plan 05-06 Task 1 Step 3 truth table:
        verifier <PASS|PARTIAL|FAIL> + reviewer <GREEN|YELLOW|RED> = <combined>.
        <Per-issue rationale if YELLOW/RED — list each PARTIAL/HIGH/CRITICAL with file:line + classification.>
    ---
    ```

    Body — follow the M2 06-INHERITANCE-AUDIT.md structure (lines 90-269):

    1. **Audit Methodology** numbered list (4-6 steps describing how the diff + classification + dispositions were derived).
    2. **RN client package.json diff** section with the actual diff content from Step 2 (`/tmp/dep-diff.txt`, `/tmp/devdep-diff.txt`, `/tmp/dep-version-diff.txt`). If diffs are empty, the empirical conclusion paragraph reads "byte-for-byte identical dependencies between v2.0.0 ($V200_SHA) and v3.0.0 (HEAD); 7/7 inheritance dispositions safe."
    3. **Repo-rule check:** `grep -E '"(firebase|@react-native-firebase/[^"]+)"' package.json` (expected 0 lines — record the actual output).
    4. **7 inheritance disposition subsections** with `### <item>` heading + Rationale paragraph + Re-open conditions bullets. Use the frontmatter `inheritance_dispositions:` map's content for each subsection — keep the prose human-readable.
    5. **Plan 07 disposition signal subsection** echoing the frontmatter `proceed_with_inheritance` boolean.
    6. **Phase 5 paired-gates verdict subsection** echoing the frontmatter `phase_5_paired_gates:` block content + per-item issue list (CRITICAL/HIGH/MEDIUM/LOW from reviewer; PARTIAL/FAIL from verifier).
    7. **Re-open audit trail prose paragraph** describing what events would invalidate the inheritance.
    8. **Cross-references list:**
       - M2 Phase 6 precedent: `06-INHERITANCE-AUDIT.md`
       - Memory `no-firebase-sdk.md`
       - Memory `gsd-verifier-misses-regressions.md` (paired-gate discipline)
       - REQUIREMENTS.md REL-05 + REL-06 acceptance criteria
       - 05-PREFLIGHT.md (backend deploy state)
       - 05-QA-MATRIX.md (walk_disposition input)

    **Step 5 — Final acceptance check:**

    Run the validators from CONTEXT.md (sentinel re-run before submission per `Claude's Discretion › Sentinel re-run before submission`):

    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/JayTap
    bash scripts/check-i18n-parity.sh   # MUST exit 0
    bash scripts/check-create-listing-screen-removed.sh   # MUST exit 0
    npm test   # MUST exit 0 — chains check:atomic-deletion + jest

    # Backend sentinels (cross-repo):
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services
    nvm use 24
    bash scripts/check-no-actoruid-spoofing.sh   # MUST exit 0
    bash scripts/check-no-landlord-uid-spoofing.sh   # MUST exit 0
    bash scripts/check-property-routes-media-stripped.sh   # MUST exit 0
    npm test   # MUST exit 0 — chains all 3 sentinels + jest
    ```

    Record the sentinel results in 05-INHERITANCE-AUDIT.md `## Sentinel Final Re-run` body section. If any sentinel exits non-zero, HALT — re-investigate before letting the audit close.

    Use the Write tool to create `05-INHERITANCE-AUDIT.md`. After write, run the assertions in `<acceptance_criteria>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md && grep -q 'proceed_with_inheritance' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md && grep -q 'paired_gate_verdict' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md && grep -E '"(firebase|firebase-admin|@firebase/|@react-native-firebase/)"' package.json | grep -c '' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` exits 0
    - `grep -E '^\s*v200_baseline_sha:\s*[0-9a-f]{7,40}' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns 1 line (a real git SHA, not `<V200_SHA>` placeholder)
    - `grep -E '^\s*v300_audit_sha:\s*[0-9a-f]{7,40}' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns 1 line
    - `grep -cE '^### (ios_privacy_info_xcprivacy|asc_app_privacy_questionnaire|play_console_data_safety|store_listing_copy|app_icon_screenshots_category|maps_api_key_restrictions|applinks_entitlement)' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns ≥ 7 (all 7 inheritance dispositions have body subsections — accept either H3 with the underscored key OR a similarly-named heading)
    - `grep -c 'INHERIT' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns ≥ 7 (each disposition documented as INHERIT)
    - `grep -E '^\s*proceed_with_inheritance:\s*(true|false)' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns 1 line; under M3 expected outcome 7/7 INHERITED + paired-gate GREEN, this MUST be `true`. Plan 05-07 reads this as a binding signal.
    - `grep -E '^\s*paired_gate_verdict:\s*(GREEN|YELLOW|RED)' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns 1 line (Phase 5 paired-gates verdict from Task 1 captured in artifact)
    - `grep -E '^\s*verifier_aggregate:\s*(PASS|PARTIAL|FAIL)' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns 1 line
    - `grep -E '^\s*reviewer_aggregate:\s*(GREEN|YELLOW|RED)' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns 1 line
    - `grep -cE '"(firebase|firebase-admin|@firebase/|@react-native-firebase/)"' package.json` returns 0 (CLAUDE.md repo rule preserved — no new Firebase SDK keys in v3.0.0)
    - `grep -c '## Sentinel Final Re-run' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns 1
    - `grep -c 'no-firebase-sdk' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns ≥ 1 (memory anchor cited)
    - `grep -c 'gsd-verifier-misses-regressions' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns ≥ 1 (paired-gate discipline memory cited)
    - `grep -c '## Cross-references' .planning/phases/05-hardening-manual-qa-release-v3/05-INHERITANCE-AUDIT.md` returns 1
    - All 5 RN client + 3 backend sentinels re-run exit 0 — recorded in `## Sentinel Final Re-run` body
    - If `paired_gate_verdict: RED` in frontmatter, `proceed_with_inheritance: false` MUST also be set (RED gate cannot ship)
    - If `flagged_red_items` is non-empty in frontmatter, `proceed_with_inheritance: false` MUST also be set (any RED item blocks)
  </acceptance_criteria>
  <done>
    `05-INHERITANCE-AUDIT.md` exists with: 7/7 inheritance dispositions (all INHERITED under M3 expected outcome), package.json diff captured + classified, repo-rule check (zero firebase keys) recorded, paired-gate verdict block with verifier + reviewer aggregates, sentinel re-run results recorded. Plan 05-07's Task 0 reads `proceed_with_inheritance: true` + `paired_gate_verdict: GREEN|YELLOW` as binding signals to start archive build.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verifier output → reviewer-independence | Per memory `gsd-verifier-misses-regressions.md`: in M2 Phase 1, verifier said PASS 15/15 while reviewer found 2 CRITICAL regressions in the same commit chain. The two gates MUST run independently — the reviewer must NOT consume the verifier's output as prior context. |
| Aggregate verdict → Plan 05-07 gate | Plan 05-07's Task 0 reads `paired_gate_verdict` as a binding signal. RED blocks; YELLOW proceeds with documented rationale; GREEN proceeds. |
| package.json diff → privacy descope | If a new dep adds `network: true` + `collects_pii: true`, the inheritance descope must NOT proceed. Apple's automated privacy scan would catch the discrepancy at submission; the `flagged_red_items` mechanism prevents the false-INHERIT signal. |
| 05-INHERITANCE-AUDIT.md → committed git history | The audit is committed to the JayTap RN client repo. No live secrets (Mongo URI, AWS keys) in this artifact; package version strings are non-sensitive. |
| Phase 4 review carry-forward → MEDIUM-01/02 fix verification | Plan 05-01 Tasks 1+2 landed MEDIUM-01 + MEDIUM-02. Reviewer must confirm the fixes don't regress the 5 handler surfaces or break verify-PASS pure-probe semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-37 | Tampering | Verifier output co-opted by reviewer (loss of independence) | mitigate | The two slash commands run in independent invocations; the reviewer reads the commit chain + code surface, not the verifier's output. The acceptance criteria capture both aggregates separately, so a future auditor can verify they were derived independently. |
| T-05-38 | Spoofing | False-INHERIT signal when a new data-collecting SDK landed | mitigate | Acceptance criterion `grep -cE '"(firebase|firebase-admin|@firebase/|@react-native-firebase/)"' package.json` returns 0. The classification step explicitly checks `network` + `collects_pii` per dep; the `flagged_red_items` array forces `proceed_with_inheritance: false` if any RED item exists. |
| T-05-39 | Repudiation | Paired-gate verdict not recorded with timestamps | mitigate | Frontmatter `verifier_invoked_at` + `reviewer_invoked_at` are ISO8601 timestamps; the per-issue rationale lists file:line for each PARTIAL/HIGH/CRITICAL. A future maintainer can cross-reference these against the SUMMARY's commit SHA. |
| T-05-40 | Tampering | Audit committed before paired-gates ran | mitigate | The plan order forces Task 1 BEFORE Task 2; Task 2's frontmatter `phase_5_paired_gates:` block requires Task 1's output. If Task 2 ran without Task 1's output, the verifier_aggregate and reviewer_aggregate fields would be empty — caught by the `grep -E '^\s*verifier_aggregate:\s*(PASS|PARTIAL|FAIL)'` acceptance criterion. |
| T-05-41 | Information Disclosure | Live secrets in audit artifact | accept | The audit records package versions + commit SHAs + timestamps + verdict labels. No env-var values, no Mongo URIs, no AWS keys. The git diff captures package.json diff (version strings only). |
| T-05-42 | DoS | Reviewer hits CRITICAL/HIGH issue mid-audit | mitigate | Plan 05-06 HALTs at Task 1 if aggregate is RED; the fix-loop opens. Plan 05-07 cannot start until paired-gate verdict is GREEN or YELLOW. The supplemental "ship YELLOW with documented rationale" path keeps the project moving without hiding regressions. |
| T-05-43 | Tampering | Sentinels regressed silently after Plan 05-03 atomic bump | mitigate | Step 5 (Sentinel Final Re-run) re-runs all 5 RN client + 3 backend sentinels at audit time. Acceptance criterion forces `## Sentinel Final Re-run` section to record the results. If any sentinel exits non-zero, the audit doesn't close. |

Block-on severity: high.
- T-05-38 (HIGH) — blocks if mitigation absent (Apple/Google reject the privacy descope at submission, surface scrutiny)
- T-05-40 (HIGH) — blocks if mitigation absent (audit ships without paired-gate evidence; Plan 05-07 reads stale signal)
- T-05-43 (HIGH) — blocks if mitigation absent (silent regression of i18n parity / multer-stripped / anti-spoofing invariants ships in v3.0.0)
</threat_model>

<verification>
- 05-INHERITANCE-AUDIT.md exists with 7 inheritance dispositions all INHERITED (M3 expected outcome)
- package.json diff captured + classified — zero `firebase_sdk_violations`
- Phase 5 paired-gate verdict block populated (verifier + reviewer aggregates + combined GREEN/YELLOW/RED)
- proceed_with_inheritance: true (under M3 expected outcome + GREEN/YELLOW verdict)
- All 5 RN client + 3 backend sentinels re-run exit 0 (recorded in body)
- No live secrets in artifact
- Memory anchors cited (no-firebase-sdk.md, gsd-verifier-misses-regressions.md)
</verification>

<success_criteria>
Plan 05-07 (Wave 5 — store submission) reads two binding signals from this plan:
1. `proceed_with_inheritance: true` from 05-INHERITANCE-AUDIT.md frontmatter — green-light to skip privacy + store-listing re-authoring (M1 D-13 inheritance descope honored).
2. `paired_gate_verdict: GREEN|YELLOW` — confirmation that Phase 5 commit chain has no CRITICAL or unresolved HIGH regressions.

If either signal is RED or false, Plan 05-07 HALTs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-manual-qa-release-v3/05-06-SUMMARY.md` with: package.json diff disposition (byte-for-byte / version-only-bumps / new-deps with classification), 7-item INHERITANCE table summary, paired-gate verdicts (verifier_aggregate + reviewer_aggregate + combined), per-issue list of any PARTIAL/HIGH/CRITICAL items + their classification (ship / fix-loop / M4-backlog), sentinel re-run results (5 RN + 3 backend), and `proceed_with_inheritance:` final boolean.
</output>
</content>
</invoke>