---
phase: 05-hardening-manual-qa-release-v3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md
autonomous: false
requirements: [REL-02]
requirements_addressed: [REL-02]
tags: [release, store-history, m1-d-02-lesson, version-baseline, query-first, testflight, play-console, m2-shipped-anchor, paired-gates-cleared]

must_haves:
  truths:
    - "05-STORE-HISTORY.md exists in the phase directory recording the highest-accepted iOS CURRENT_PROJECT_VERSION (build number) per ASC track at query time, with timestamp"
    - "05-STORE-HISTORY.md records the highest-accepted Android versionCode in Google Play Console (production + open-testing + closed-testing + internal-testing tracks), with timestamp"
    - "05-STORE-HISTORY.md derives the next-bump targets per M1 D-02 lesson: next_ios_build_number = max(local_pbxproj, store_max) + 1; next_android_version_code = max(local_build_gradle, store_max) + 1"
    - "Local source files are sampled into the artifact for an explicit local-vs-store delta — local is documented as untrusted per M1 Key Lesson #1 (memory `release-android-versioncode-trust-play-console.md`)"
    - "If the queried-store maximum exceeds the local source value, the artifact MUST flag this as 'local-stale' and Plan 05-03 MUST adopt the queried-max+1 baseline (NOT local+1)"
    - "Phase 4 paired-gates prerequisite is documented inline as `clear` — Phase 4 closed paired-gates green per .planning/STATE.md (memory `gsd-verifier-misses-regressions.md` discipline honored)"
    - "Frontmatter records `query_method:` per platform with one of the three allowed enum values (asc_api_direct | asc_web_ui_user_relay | orchestrator_hypothesis for ios; gcloud_play_publishing_api | play_console_web_ui_user_relay | orchestrator_hypothesis for android)"
  artifacts:
    - path: ".planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md"
      provides: "REL-02 — derived iOS build-number + Android versionCode baselines for Plan 05-03 atomic version bump; M1 D-02 lesson encoded as a Wave-1 query-first task"
      contains: "next_ios_build_number"
      contains_also: "next_android_version_code"
  key_links:
    - from: "05-STORE-HISTORY.md :: derived_targets.next_ios_build_number"
      to: "Plan 05-03 :: ios/JayTap.xcodeproj/project.pbxproj :: CURRENT_PROJECT_VERSION (lines 274 + 308)"
      via: "Plan 05-03 reads next_ios_build_number from this artifact and uses it as the bump target"
      pattern: "CURRENT_PROJECT_VERSION = [0-9]+"
    - from: "05-STORE-HISTORY.md :: derived_targets.next_android_version_code"
      to: "Plan 05-03 :: android/app/build.gradle :: versionCode (line 91)"
      via: "Plan 05-03 reads next_android_version_code from this artifact and uses it as the bump target"
      pattern: "versionCode [0-9]+"
---

<objective>
Wave 1 of Phase 5 — query the live store history for highest-accepted iOS build number (TestFlight) and Android versionCode (Play Console) BEFORE Plan 05-03's atomic v3.0.0 version bump runs.

This plan exists ONLY because of the M1 D-02 lesson + M2 Wave-1 confirmation recorded in memory `release-android-versioncode-trust-play-console.md`: in M1 Phase 8, the planner trusted the local `android/app/build.gradle` baseline and Play Console rejected it because the same versionCode had already been accepted from a prior submission. M2 Phase 6 codified the query-first pattern as Plan 06-01; Phase 5 reuses it verbatim.

Per memory `m2-shipped-2026-05-05.md`: M2 v2.0.0 shipped with iOS build 27 in TestFlight + Android versionCode 30 in Play Console Internal Testing. Local source files (sampled 2026-05-07) read iOS CURRENT_PROJECT_VERSION = 27 + Android versionCode = 30 (both equal to store, per 05-PATTERNS.md observation). The expected `local_vs_store_delta` is `local-equal` for BOTH platforms; the next-bump formula `max(local, store) + 1` collapses to `local + 1`, but the FORMULA is still recorded as the binding rule (preventing the next release regressing to the M1 anti-pattern).

Per CONTEXT.md `Phase 4 paired-gates prerequisite`: Phase 4 closed paired-gates clean (per memory `phase06-m3-carry-forward.md` + STATE.md updates). The artifact records `phase_4_paired_gates_prerequisite.status: clear` so Plan 05-05 (QA walks) can proceed without re-litigation.

Output: `05-STORE-HISTORY.md` with 4 sections — (1) ASC TestFlight history snapshot, (2) Play Console history snapshot, (3) derived next-bump targets per track + math table + rationale, (4) Phase 4 paired-gates prerequisite block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-CONTEXT.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-PATTERNS.md
@.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md
@.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Current local-source values (sampled 2026-05-07 — confirmed via grep at planning time): -->

`ios/JayTap.xcodeproj/project.pbxproj`:
- Line 274 (Debug build configuration): `				CURRENT_PROJECT_VERSION = 27;`
- Line 283 (Debug build configuration): `				MARKETING_VERSION = 2.0.0;`
- Line 308 (Release build configuration): `				CURRENT_PROJECT_VERSION = 27;`
- Line 316 (Release build configuration): `				MARKETING_VERSION = 2.0.0;`

`android/app/build.gradle`:
- Line 91: `        versionCode 30`
- Line 92: `        versionName "2.0.0"`

`package.json` line 3:
- `  "version": "2.0.0",`

These local values are the M1 D-02 anti-pattern reference. Plan 05-02 treats them as untrusted; the binding values for Plan 05-03 come from the live stores, sampled by this plan.

Per memory `m2-shipped-2026-05-05.md` (the M3 baseline anchor — orchestrator hypothesis):
- ASC TestFlight: highest accepted iOS build for v2.0.0 = `27` (M2 close, build 27 in Internal Testing)
- Play Console: highest accepted Android versionCode = `30` (M2 close, Internal Testing track)

Both platforms appear `local-equal` at M2 close baseline — local pbxproj=27 vs store TestFlight=27; local build.gradle=30 vs store Play Console=30. The executor MUST re-confirm both numbers against the live stores at execution time (the user's known state may have changed since 2026-05-05 if any post-M2 build was uploaded out-of-band); the orchestrator-supplied values are the working hypothesis to verify, not the answer.

<!-- M3-specific math reality (per 05-PATTERNS.md): -->
Both platforms `local-equal` → `max + 1` formula collapses to `local + 1` → next_ios_build_number = 28; next_android_version_code = 31. The FORMULA is still recorded as the binding rule.

<!-- Phase 4 paired-gates prerequisite (per memory phase06-m3-carry-forward.md): -->
Phase 4 closed paired-gates clean. `phase_4_paired_gates_prerequisite.status: clear`.

<!-- Path A / Path B / Path C precedent (06-STORE-HISTORY.md:60-65): -->
- Path A — automated via xcrun altool / gcloud (likely unavailable in this env without API key on disk)
- Path B — user-relay through web UI (https://appstoreconnect.apple.com + https://play.google.com/console)
- Path C — orchestrator_hypothesis from prior memory (m2-shipped-2026-05-05.md = iOS build 27 / Android versionCode 30)

Plan must record `query_method:` honestly per platform. Path C is acceptable when A + B both fail; in that case Plan 05-07's executor knows to retry-on-reject.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Query ASC + Play Console for live highest-accepted version baselines and author 05-STORE-HISTORY.md</name>
  <files>.planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md</files>
  <read_first>
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/release-android-versioncode-trust-play-console.md (re-anchor M1 D-02 lesson — "Trust Play Console (and TestFlight) for live-version state, not local build files")
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/m2-shipped-2026-05-05.md (re-anchor M2 baseline — iOS build 27 + Android versionCode 30; this is the orchestrator_hypothesis fallback if live query fails)
    - .planning/STATE.md (sample for any post-M2 store activity since 2026-05-05 — confirm orchestrator-supplied store-history hypothesis matches current STATE notes)
    - ios/JayTap.xcodeproj/project.pbxproj line range 270-320 (read this slice — confirm lines 274/308 are CURRENT_PROJECT_VERSION = 27 and lines 283/316 are MARKETING_VERSION = 2.0.0; record the as-sampled state in the artifact)
    - android/app/build.gradle line range 85-100 (read this slice — confirm lines 91/92 are versionCode 30 / versionName "2.0.0"; record the as-sampled state in the artifact)
    - .planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md (read full file — copy the SHAPE: frontmatter + ASC table + Play Console table + Math table + M1 D-02 lesson application prose + Re-open conditions; this is the EXACT analog per 05-PATTERNS.md)
    - .planning/phases/05-hardening-manual-qa-release-v3/05-PATTERNS.md lines 104-160 (read the M3-specific math reality: local-equal collapses to local+1)
  </read_first>
  <action>
    Two-step query + artifact authoring. NO source-file mutation. The query step is operator-supervised because Path A (xcrun altool / gcloud Play Publishing API) typically requires API keys on disk; Path B (web UI relay) is the practical fallback for a single-developer release. Path C (orchestrator_hypothesis) is acceptable when A + B both fail.

    **Step 1 — Query Apple App Store Connect (ASC) for highest-accepted iOS build number across ALL versions for bundle ID `com.movein`:**

    Path A — automated (try first if API key present):
    ```bash
    ls ~/.private_keys/AuthKey_*.p8 2>/dev/null   # confirm API key exists
    xcrun altool --list-builds -u $APPLE_ID -p '@keychain:AC_PASSWORD' --output-format json 2>/dev/null | head -200
    # OR (depending on what's installed):
    xcrun notarytool history --apple-id $APPLE_ID --keychain-profile AC_PASSWORD 2>/dev/null
    ```

    Path B — user-relay via ASC web UI (most likely path):
    Operator opens https://appstoreconnect.apple.com → JayTap → TestFlight tab → Internal Testing group → list of builds. Reports the highest build-number value seen across ALL versions (including any v3.0.0 prior archives from out-of-band uploads).

    Path C — orchestrator_hypothesis fallback:
    If A + B both fail at execution time, use the M2 baseline (iOS build 27 from memory `m2-shipped-2026-05-05.md`) and ANNOTATE the artifact with `query_method: orchestrator_hypothesis`. Plan 05-07's executor must understand that Path C means "needs verification at upload time, retry-on-reject is expected".

    **Step 2 — Query Google Play Console for highest-accepted Android versionCode for `com.movein` across ALL tracks (production + open-testing + closed-testing + internal-testing):**

    Path A — `gcloud` Play Publishing API (try first if service account on disk):
    ```bash
    gcloud auth list 2>/dev/null
    # If a Play Console service account is logged in, use the publishing API to list bundles.
    # The exact command varies by gcloud version — read-only listing of bundles is awkward through gcloud,
    # bundletool / curl-against-the-Play-Developer-API may be easier.
    ```

    Path B — user-relay via Play Console web UI:
    Operator opens https://play.google.com/console → JayTap → "App bundle explorer" or "Internal Testing" track → list of releases. Reports the highest versionCode value seen across ALL tracks.

    Path C — orchestrator_hypothesis fallback:
    Same fallback as iOS — use the M2 baseline (Android versionCode 30 from memory `m2-shipped-2026-05-05.md`) and ANNOTATE.

    **Step 3 — Author `05-STORE-HISTORY.md` using the EXACT structure from `06-STORE-HISTORY.md` (M2 Phase 6 analog).** Use the Write tool. The orchestrator hypothesis collapses to `next_ios_build_number = 28` and `next_android_version_code = 31` under `local_vs_store_delta: local-equal` for both platforms.

    Frontmatter:
    ```yaml
    ---
    phase: 05-hardening-manual-qa-release-v3
    plan: 02
    type: store-history
    queried_at: <ISO 8601 timestamp at query time>
    queried_against_sha: <git rev-parse HEAD output for RN client at query time — currently fe3b888 per CONTEXT.md>

    ios:
      query_method: <"asc_api_direct" | "asc_web_ui_user_relay" | "orchestrator_hypothesis">
      asc_track: TestFlight Internal + (if any prior 3.0.0 build) prod
      highest_accepted_marketing_version: <e.g., 2.0.0>
      highest_accepted_build_number: <integer; from M2 close hypothesis = 27 unless re-queried>
      local_source_marketing_version: 2.0.0
      local_source_build_number: 27
      local_vs_store_delta: <"local-ahead" | "local-equal" | "local-stale">

    android:
      query_method: <"gcloud_play_publishing_api" | "play_console_web_ui_user_relay" | "orchestrator_hypothesis">
      tracks_inspected: [production, open-testing, closed-testing, internal-testing]
      highest_accepted_version_name: <e.g., 2.0.0>
      highest_accepted_version_code: <integer; from M2 close hypothesis = 30 unless re-queried>
      local_source_version_name: "2.0.0"
      local_source_version_code: 30
      local_vs_store_delta: <"local-ahead" | "local-equal" | "local-stale">

    derived_targets:
      next_ios_build_number: <max(local_source_build_number, highest_accepted_build_number) + 1>
      next_android_version_code: <max(local_source_version_code, highest_accepted_version_code) + 1>
      rationale: "M1 D-02 lesson — bump from max(local, store) + 1, not local + 1, to avoid the M1 reactive-bump pattern. M3-specific reality: both platforms local-equal at M2 close baseline (local pbxproj=27 vs store=27; local build.gradle=30 vs store=30); formula collapses to local+1, but FORMULA is still binding rule for the next release."

    phase_4_paired_gates_prerequisite:
      status: clear
      detail: |
        Phase 4 (M2 carry-forward bug fixes — CARRY-01 + CARRY-02) closed paired-gates clean per
        STATE.md updates and memory `phase06-m3-carry-forward.md`. Plan 05-05 (QA walks) can proceed
        without re-litigation. Plan 05-06 (paired-gate verifier+reviewer) re-runs the gates against
        the M3 cumulative surface for end-of-phase confirmation per memory
        `gsd-verifier-misses-regressions.md`.
    ---
    ```

    Body — 5 sections per 06-STORE-HISTORY.md analog:

    ```markdown
    # Phase 5 — Store History Snapshot

    Authored by Plan 05-02 to encode the M1 D-02 lesson + M2 Wave-1 confirmation as a Wave-1
    query-live artifact. Plan 05-03 reads `derived_targets.next_ios_build_number` +
    `derived_targets.next_android_version_code` as the binding values for the v3.0.0 atomic bump.
    Local source files are NOT trusted as the bump baseline — the live stores are.

    ## ASC TestFlight history (queried at <timestamp>, method <enum>)

    | Track | Marketing Version | Highest Build Number Accepted | Notes |
    |-------|-------------------|-------------------------------|-------|
    | Internal Testing | 2.0.0 | <N> | M2 close — build 27 per memory `m2-shipped-2026-05-05.md` |
    | (any 3.0.0 prior) | 3.0.0 | <none expected; if present, FLAG> | First v3.0.0 archive should be next_ios_build_number |

    ## Play Console history (queried at <timestamp>, method <enum>)

    | Track | Version Name | Highest versionCode Accepted | Notes |
    |-------|--------------|------------------------------|-------|
    | Internal Testing | 2.0.0 | <N> | M2 close — versionCode 30 per memory `m2-shipped-2026-05-05.md` |
    | (any 3.0.0 prior) | (none expected) | (none) | First v3.0.0 archive should be next_android_version_code |

    ## Derived bump targets (Plan 05-03 binding values)

    Local source anchor lines (sampled at HEAD on <timestamp>):
    - `package.json` line 3: `"version": "2.0.0"`
    - `ios/JayTap.xcodeproj/project.pbxproj` line 274 (Debug): `CURRENT_PROJECT_VERSION = 27;`
    - `ios/JayTap.xcodeproj/project.pbxproj` line 308 (Release): `CURRENT_PROJECT_VERSION = 27;`
    - `ios/JayTap.xcodeproj/project.pbxproj` line 283 (Debug): `MARKETING_VERSION = 2.0.0;`
    - `ios/JayTap.xcodeproj/project.pbxproj` line 316 (Release): `MARKETING_VERSION = 2.0.0;`
    - `android/app/build.gradle` line 91: `versionCode 30`
    - `android/app/build.gradle` line 92: `versionName "2.0.0"`

    Plan 05-03 bump targets:
    - **iOS CURRENT_PROJECT_VERSION** (Debug + Release; lines 274 + 308): `<next_ios_build_number>` (= 28 under hypothesis)
    - **Android versionCode** (line 91): `<next_android_version_code>` (= 31 under hypothesis)
    - **iOS MARKETING_VERSION** (Debug + Release; lines 283 + 316): `3.0.0`
    - **Android versionName** (line 92): `"3.0.0"`
    - **package.json `version`** (line 3): `"3.0.0"`

    ## Math (the binding rule, NOT just the integer)

    | Platform | Local | Store | max(local, store) | + 1 | Next |
    |----------|-------|-------|-------------------|-----|------|
    | iOS build | 27 | <N> | <max> | +1 | <next_ios_build_number> |
    | Android versionCode | 30 | <N> | <max> | +1 | <next_android_version_code> |

    ## M1 D-02 lesson application

    Per memory `release-android-versioncode-trust-play-console.md`: in M1 Phase 8, the planner
    anchored the bump on the local `build.gradle` value (versionCode 25). Play Console had already
    accepted versionCode 25 from a prior submission, so the bump-to-25 plan would have been rejected.
    The user authored an out-of-band reactive commit at archive time. M2 Phase 6 codified the
    lesson as a Wave-1 query-first step (Plan 06-01 → `06-STORE-HISTORY.md`); M3 Phase 5 reuses it
    verbatim. M3-specific reality: both platforms `local-equal` at M2 close baseline (local pbxproj=27
    vs store=27; local build.gradle=30 vs store=30); the formula collapses to `local + 1`, but the
    FORMULA is still binding for the next release (preventing regression to the anti-pattern).

    ## Phase 4 paired-gates prerequisite

    `phase_4_paired_gates_prerequisite.status: clear`. Phase 4 closed paired-gates clean per STATE.md
    updates and memory `phase06-m3-carry-forward.md`. Plan 05-05 (QA matrix walks) can proceed without
    re-litigation. Plan 05-06 (paired-gate verifier+reviewer) re-runs `/gsd-verify-work 5` +
    `/gsd-code-review 5` at end-of-phase per memory `gsd-verifier-misses-regressions.md`.

    ## Re-open conditions

    - If a future ASC TestFlight upload rejects the build number Plan 05-03 sets, re-run Step 1
      to re-derive the next target (max + 1 of the rejection notice's number).
    - If Play Console rejects the versionCode, same drill against Step 2.
    - If a post-2026-05-05 out-of-band upload landed a build above the M2 baseline (27/30), the
      hypothesis-fallback values are stale — re-query the live stores.

    ## Cross-references

    - M2 Phase 6 precedent: `.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md`
    - Memory `release-android-versioncode-trust-play-console.md` (M1 D-02 lesson)
    - Memory `m2-shipped-2026-05-05.md` (M2 baseline = orchestrator_hypothesis fallback)
    - Memory `phase06-m3-carry-forward.md` (Phase 4 paired-gates closure)
    - REQUIREMENTS.md REL-02 acceptance criteria.

    ---

    *Authored: <date> (Plan 05-02, Wave 1 of Phase 5)*
    *Plan 05-03 reads derived_targets.next_ios_build_number + derived_targets.next_android_version_code*
    ```

    Use the Write tool to create the file at the exact path declared in `<files>`. After write, run the assertions in `<acceptance_criteria>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md && grep -E '^\s*next_ios_build_number:\s*[0-9]+' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md > /dev/null && grep -E '^\s*next_android_version_code:\s*[0-9]+' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md > /dev/null && grep -E 'phase_4_paired_gates_prerequisite' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md > /dev/null && grep -E 'rationale:' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md | grep -c 'M1 D-02' | grep -q '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` exits 0
    - `grep -c 'next_ios_build_number' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` returns ≥ 1 (declared in frontmatter)
    - `grep -c 'next_android_version_code' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` returns ≥ 1
    - `grep -E '^\s*next_ios_build_number:\s*[0-9]+' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` returns a line with an INTEGER value (NOT a placeholder like `<N>` or `TBD`)
    - `grep -E '^\s*next_android_version_code:\s*[0-9]+' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` returns a line with an INTEGER value
    - `grep -E '^\s*query_method:' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` returns 2 lines (one for ios, one for android), each with one of the three allowed enum values per platform
    - `grep -c 'phase_4_paired_gates_prerequisite' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` returns ≥ 1
    - `grep -A 2 'phase_4_paired_gates_prerequisite:' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md | grep -E '^\s*status:\s*clear'` returns 1 line (Phase 4 paired-gates clear — Plan 05-05 unblocked)
    - `grep -E 'rationale:' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md | grep -c 'M1 D-02'` returns ≥ 1 (rationale references the M1 lesson explicitly)
    - The derived `next_ios_build_number` value satisfies the formula `next_ios_build_number = max(local_source_build_number, highest_accepted_build_number) + 1`. **Under the orchestrator hypothesis (local iOS = 27, store iOS = 27), this evaluates to `28`.** If the executor re-queried and the live store has changed since 2026-05-05, the executor MUST recompute from the re-queried values, document the new derivation in `derived_targets.rationale`, and the integer in `derived_targets.next_ios_build_number` must equal the recomputed `max(local, store) + 1`. The formula is the binding rule, not the integer 28.
    - The derived `next_android_version_code` value satisfies the formula `next_android_version_code = max(local_source_version_code, highest_accepted_version_code) + 1`. **Under the orchestrator hypothesis (local Android = 30, store Android = 30), this evaluates to `31`.** Same recompute-and-document rule as iOS — the formula is the binding rule, not the integer 31.
    - `grep -c '## Math' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` returns 1 (Math table section exists)
    - `grep -c '## Re-open conditions' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` returns 1
    - `grep -c '## Cross-references' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` returns 1
    - `grep -c 'local-equal\|local-ahead\|local-stale' .planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` returns ≥ 2 (one delta value per platform)
    - Operator-confirmed observation: at the recorded `queried_at` timestamp, the operator confirms the integers in the frontmatter match what they observe in the ASC TestFlight Internal Testing track (or cross-reference the Path C fallback timestamp from memory `m2-shipped-2026-05-05.md`)
  </acceptance_criteria>
  <done>
    `.planning/phases/05-hardening-manual-qa-release-v3/05-STORE-HISTORY.md` exists, contains both derived targets as integers (not placeholders), declares `query_method` for each platform, encodes the M1 D-02 rationale, surfaces the Phase 4 paired-gates prerequisite as `clear`, and is ready for Plan 05-03 to read as the binding bump source.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local source → live store | Local pbxproj/build.gradle values are NOT trusted; the live stores are the binding state |
| executor → user (Path B fallback) | If ASC API / gcloud Play Publishing API is unavailable, the executor pauses and the user reads the value from the store dashboard. The relayed integer is a trust point — record `query_method: *_user_relay` so the value's provenance is auditable |
| orchestrator hypothesis → derived target | If both Path A and Path B fail at execution time, the M2 baseline (memory `m2-shipped-2026-05-05.md`) is used and ANNOTATED. Plan 05-03 executor must understand `query_method: orchestrator_hypothesis` means "needs verification at upload time, retry-on-reject is expected" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | derived_targets values | mitigate | Acceptance criteria assert INTEGER values (not placeholders); the integer must equal `max(local, store) + 1` per the formula recorded in the artifact's `rationale` field. The artifact is committed to git so any post-hoc tampering is auditable. |
| T-05-12 | Repudiation | query_method field | mitigate | The `query_method` enum is one of three explicit values per platform; the artifact records WHICH path was used. If a later release rejection is traced to a stale value, the audit trail shows whether it was a query-live result or an orchestrator-hypothesis fallback. |
| T-05-13 | Information Disclosure | committed values | accept | The build numbers + versionCodes are non-sensitive (visible to anyone who downloads the binary). No credential or PII leaves the local machine via this artifact. |
| T-05-14 | DoS | ASC API rate limit | accept | One-shot query at execution time; no loop. ASC + Play Console rate limits are several hundred requests per hour; a single read does not approach them. |
| T-05-15 | Build-identity drift | bump-from-stale-baseline (M1 D-02 anti-pattern repeat) | mitigate | This entire plan exists to prevent T-05-15. Acceptance criteria force the executor to record the source of the value (live query vs hypothesis), so Plan 05-03 cannot inherit a stale baseline silently. The math table forces the executor to write down `local | store | max | +1 | next` per platform — the formula is auditable. |
| T-05-16 | Spoofing | Phase 4 paired-gates flagged outstanding when actually closed (or vice versa) | mitigate | Acceptance criterion `grep -A 2 'phase_4_paired_gates_prerequisite:' ... | grep -E '^\s*status:\s*clear'` returns 1 line — context-anchored to the YAML block (avoids matching unrelated `status: clear` lines elsewhere). The detail prose cross-references STATE.md + memory `phase06-m3-carry-forward.md`. |

Block-on severity: high.
- T-05-11 (HIGH) — blocks if mitigation absent (placeholder integers passing acceptance check would mean a malformed artifact reaches Plan 05-03)
- T-05-15 (HIGH) — blocks if mitigation absent (stale-baseline = M1 reactive-bump repeat at archive time)
</threat_model>

<verification>
- `05-STORE-HISTORY.md` file exists in the phase directory with all required sections
- Both derived targets are concrete integers, not placeholders
- `query_method` is declared for each platform with an enum-valid value
- M1 D-02 rationale text is present
- Phase 4 paired-gates prerequisite block is present and `status: clear`
- Math table records the `max(local, store) + 1` formula explicitly
- Both platforms record `local_vs_store_delta` (likely `local-equal` per the M3 baseline)
</verification>

<success_criteria>
Plan 05-03 (Wave 2) reads `derived_targets.next_ios_build_number` and `derived_targets.next_android_version_code` from this file as the binding bump targets, eliminating the M1 D-02 anti-pattern (trust-local-source).
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-manual-qa-release-v3/05-02-SUMMARY.md` with: which `query_method` was used per platform, what integers landed (with the math `local | store | max | +1 | next` rows), `local_vs_store_delta` per platform, and confirmation that `phase_4_paired_gates_prerequisite.status: clear`.
</output>
</content>
</invoke>