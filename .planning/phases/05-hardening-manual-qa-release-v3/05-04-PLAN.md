---
phase: 05-hardening-manual-qa-release-v3
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md
autonomous: true
requirements: [REL-04]
requirements_addressed: [REL-04]
tags: [release, release-notes, en-ru-parity, play-console-500-char-limit, m1-d-10-content-order, no-bishkek-only-phrasing, no-fabricated-specifics, kg-kz-uz-scope]

must_haves:
  truths:
    - "05-RELEASE-NOTES.md exists with EN + RU bodies for ASC + Play Console paste at submission time (Plan 05-07)"
    - "EN body fits Play Console's binding 500-character-per-locale limit (`wc -m` ≤ 500)"
    - "RU body fits Play Console's binding 500-character-per-locale limit (`wc -m` ≤ 500)"
    - "Content order honors M1 D-10: What's new (6-step contextual listing flow + KG/KZ/UZ expansion enabler hint per memory `geographic-scope.md`) → Improvements (stability + polish) → Bug fixes (catch-all phrasing only — NO fabricated specifics)"
    - "RU body uses Cyrillic (NOT Kyrgyz or Uzbek — per memory `geographic-scope.md`, RU is the only non-EN locale at v3.0.0)"
    - "Geographic phrasing AVOIDS Bishkek-only assumptions — even if v3.0 ships only Bishkek + KG city seeds, language must accommodate planned KZ/UZ expansion (memory `geographic-scope.md`)"
    - "Bug fixes section uses M1 D-10 catch-all phrasing only (no specific fix names fabricated)"
    - "Frontmatter `target_version: 3.0.0` (not 2.0.0); `paste_only: true` (M1 D-11 precedent); `languages: [en, ru]`"
    - "Character counts table contains actual integers (not `<N>` placeholders) — re-runnable awk verification commands documented for Plan 05-07's paste-time re-check"
  artifacts:
    - path: ".planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md"
      provides: "REL-04 — bilingual EN+RU release notes pasted on ASC + Play Console at Plan 05-07 submission time"
      contains: "## EN"
      contains_also: "## RU"
  key_links:
    - from: "05-RELEASE-NOTES.md :: ## EN body"
      to: "ASC :: What's New in This Version (en-US locale) + Play Console :: Release notes (en-US)"
      via: "Plan 05-07 paste step at submission time"
      pattern: "## EN"
    - from: "05-RELEASE-NOTES.md :: ## RU body"
      to: "ASC :: What's New in This Version (ru-RU locale) + Play Console :: Release notes (ru-RU)"
      via: "Plan 05-07 paste step at submission time"
      pattern: "## RU"
---

<objective>
Wave 1 of Phase 5 — draft bilingual EN+RU release notes for v3.0.0 "Contextual Forms" that fit Google Play Console's binding 500-character-per-locale limit and follow M1 D-10 content order.

This is purely a documentation task; no source code, no app behavior, no submission action. The output is a paste-only artifact (per M1 D-11 precedent — `.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md` is structurally identical and was M2's Plan 06-03 deliverable).

Why this plan runs in Wave 1 (parallel with Plan 05-01 + 05-02, not after the QA matrix walk):
- Release notes copy is independent of the version bump (Plan 05-03) and the QA matrix walk (Plan 05-05).
- Drafting Wave-1 lets the user review copy before the time-sensitive QA window opens.
- M1 D-10 prescribes content order (What's new → Improvements → Bug fixes); the M3 closed-phase deliverables (Phases 1–4) are the "What's new" content — those are stable at planning time.
- "Bug fixes" copy uses catch-all phrasing per M1 D-10, so the QA matrix walk does NOT need to surface specific fixes for the release notes (per M1 D-08, regression QA loop fixes are documented internally; release-notes copy stays generic).

M3 content emphasis (per CONTEXT.md `Claude's Discretion › Release notes emphasis`): lead with new features (6-step contextual listing flow + KG/KZ/UZ city dictionary as expansion-enabler hint per memory `geographic-scope.md` — NO Bishkek-only phrasing) since v3.0 is a meaningful jump and internal-testing audience benefits from knowing what to walk.

Output: `05-RELEASE-NOTES.md` with the structural template from M2's `06-RELEASE-NOTES.md` adapted to v3.0.0 content. Both bodies under 500 chars per `wc -m`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/RETROSPECTIVE.md
@.planning/PROJECT.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-CONTEXT.md
@.planning/phases/05-hardening-manual-qa-release-v3/05-PATTERNS.md
@.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md
@.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-03-PLAN.md
@CLAUDE.md

<interfaces>
<!-- M2 v2.0.0 release notes are the structural template (NOT the body). Copy the SHAPE -->
<!-- (frontmatter + ## EN / ## RU sections + Submission instructions + Character counts -->
<!-- table + verification commands), but author NEW v3.0.0 BODY content. -->

M2 v2.0.0 release notes structure (from `.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md`):
1. Frontmatter (paste-only artifact metadata)
2. ## EN section: What's new → Improvements → "Minor refinements" closer (M1 D-10)
3. ## RU section: Что нового → Улучшения → "Мелкие исправления" closer
4. Submission instructions (ASC + Play Console paths)
5. Character counts table (`wc -m` measured per body, ≤ 500)
6. Verification commands (`awk` slice + `wc -m` re-runnable at paste time)

M1 D-10 content order (re-anchored from `06-RELEASE-NOTES.md`):
1. **What's new** — the headline feature
2. **Improvements** — supporting work (UX polish / stability)
3. **Bug fixes** — catch-all phrasing only ("Minor stability and polish refinements" / «Мелкие исправления и шлифовка интерфейса»). NO fabricated specifics.

M3 closed-phase deliverables (the "What's new" content for v3.0.0):
- Phase 1: Mongoose schema reshape (location/basics/conditionAndAmenities/content/terms/media nested) + backend route cutover (NOT user-visible directly — frame as foundation)
- Phase 2: 6-step contextual listing flow — Deal Type × Property Type × Location × Basics × Title/Description × Deal Conditions; conditional sub-fields; replaces old single-screen CreateListingScreen atomically (the headline user-visible feature)
- Phase 3: Mod media curation — admin/mod uploads photos/videos/3D-tour after metadata review; user-facing flow has zero media fields (NOT user-visible to non-mods — frame as quality safeguard)
- Phase 4: M2 carry-forward bug fixes (CARRY-01 ROLE-11 mid-action 403 popup-recovery + CARRY-02 Phase 4.5 landlord-app uid-mismatch — frame as catch-all closer line)

The headline feature for owners is **the new 6-step listing flow** (clearer step-by-step UX with conditional fields per property/deal type). KG/KZ/UZ city dictionary (Phase 2 Plan 02-01) is a quiet enabler — mention without claiming KZ/UZ is live yet.

Russian release-notes locale per memory `geographic-scope.md`: RU only (NOT Kyrgyz or Uzbek). RU body uses Cyrillic.

Geographic invariant per memory `geographic-scope.md`: NO Bishkek-only phrasing. Even if v3.0 ships KG-first, language must accommodate planned KZ/UZ expansion. Acceptable framings: "your city / в вашем городе"; "across our markets / в наших регионах". Unacceptable: "Bishkek-only / только в Бишкеке".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft EN + RU v3.0.0 release notes within 500-char Play Console limit per locale</name>
  <files>.planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md</files>
  <read_first>
    - .planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md (full file — copy the SHAPE: frontmatter, ## EN / ## RU sections, Submission instructions block, Character counts table, verification commands)
    - .planning/phases/05-hardening-manual-qa-release-v3/05-CONTEXT.md (re-anchor `Claude's Discretion › Release notes emphasis` — lead with new features + KG/KZ/UZ enabler hint; M1 D-10 content order)
    - .planning/phases/05-hardening-manual-qa-release-v3/05-PATTERNS.md lines 311-356 (read the M3-specific content seed + trim priority list + M2 Phase 6 cadence)
    - .planning/REQUIREMENTS.md lines 26-86 (re-anchor M3 closed-phase deliverables — what shipped in Phases 1-4)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/geographic-scope.md (re-anchor KG/KZ/UZ scope; avoid Bishkek-only phrasing)
    - .planning/PROJECT.md lines 1-30 (re-anchor M3 v3.0 milestone framing — "6-step contextual flow" + "moderated media curation" rather than "Roles & Moderation" framing)
  </read_first>
  <action>
    Single-file draft, single commit. NO source-file mutation.

    **Step 1 — Author the EN body.** Target ≤ 500 chars (Play Console binding limit). Suggested draft:

    ```
    What's new
    - New 6-step listing creation flow: each step shows only the fields that fit your property and deal type (apartment/house/office/commercial/hotel/hostel × sale/long-term/daily).
    - Locations menu now covers cities in Kyrgyzstan, Kazakhstan, and Uzbekistan, ready for expansion as we grow.
    - Photos and tour links are now reviewed by our team before listings go live, so what you see is more trustworthy.

    Improvements
    - Smoother session handling and recovery flows.

    Minor stability and polish refinements.
    ```

    Iterate to fit ≤ 500. Verify with `wc -m`:
    ```bash
    cat <<'EOF' | wc -m
    [paste candidate body — everything between '## EN' and the next '---', excluding markers]
    EOF
    ```

    Trimming priority if over budget (per M1 / M2 precedent + 05-PATTERNS.md lines 348-353):
    1. Drop the "Minor stability..." closer first.
    2. Drop the second Improvements bullet (or the only one).
    3. Trim the descriptive tail of the longest What's new bullet (typically the 6-step description).
    Preserve EN ↔ RU parity (same content, same order, same number of bullets).

    **Step 2 — Author the RU body.** Same content, same order, in Cyrillic. Target ≤ 500 chars. Suggested draft:

    ```
    Что нового
    - Новый процесс создания объявления из 6 шагов: на каждом шаге показаны только поля, подходящие вашему типу недвижимости и типу сделки (квартира/дом/офис/коммерция/отель/хостел × продажа/долгосрочно/посуточно).
    - Справочник городов теперь включает Кыргызстан, Казахстан и Узбекистан — мы готовы к расширению.
    - Фото и 3D-туры проверяются нашей командой до публикации — то, что вы видите, надёжнее.

    Улучшения
    - Более плавная работа с сеансами и восстановлением входа.

    Мелкие исправления и шлифовка интерфейса.
    ```

    Iterate to fit ≤ 500. Use the same RU verification command (replace `## EN` with `## RU` in the awk pattern).

    **CRITICAL — Bishkek-only phrasing check before commit:** `grep -i 'bishkek\|бишкек' .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md` should return 0 (NO matches). The phrase "Kyrgyzstan/Кыргызстан" is acceptable (country, not city); "Bishkek/Бишкек" is NOT acceptable (memory `geographic-scope.md`).

    **Step 3 — Author `05-RELEASE-NOTES.md` using the M2 structural template.**

    Use the `Write` tool. Frontmatter:

    ```yaml
    ---
    phase: 05-hardening-manual-qa-release-v3
    plan: 04
    type: release-notes
    created: <ISO 8601 timestamp>
    target_stores: [App Store Connect, Google Play Console]
    target_version: 3.0.0
    walk_disposition: pending  # flips to APPROVED after Plan 05-05 manual QA matrix
    bug_fix_loop_triggered: pending  # flips after Plan 05-05 walk completes
    walked_against_sha: pending  # flips after Plan 05-03 atomic bump
    languages: [en, ru]
    binding_length_limit: 500 chars per locale (Play Console)
    asc_length_limit: 4000 chars per locale
    paste_only: true
    m1_template: .planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-RELEASE-NOTES.md
    m2_template: .planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md
    ---

    # JayTap v3.0.0 — Release Notes

    Bilingual draft for App Store Connect (per-locale "What's New in This Version") and
    Google Play Console (per-language "Release notes" / "What's new"). Paste at submission
    time in Plan 05-07; do not commit changes back from the ASC / Play Console UI into
    this file (per M1 D-11 precedent — paste-only artifact, not preserved as in-app i18n).

    Both bodies cover the same content in the same order: **What's new → Improvements →
    Bug fixes**. The "Bug fixes" reduces to a single "minor refinements" line per M1 D-10
    catch-all phrasing — specific fixes are NOT fabricated. If Plan 05-05's QA matrix walk
    surfaces a release-blocking defect that should be called out, this file is updated
    after that walk completes (the M2 v2.0.0 walk surfaced zero defects, so the closer
    line was retained as-is; the same default applies here).

    Each language body fits Google Play Console's binding 500-character-per-locale limit;
    App Store Connect's 4000-character limit is non-binding for these notes.

    No Bishkek-only phrasing per memory `geographic-scope.md` — the app's planned scope is
    KG/KZ/UZ. The "Locations menu now covers Kyrgyzstan, Kazakhstan, and Uzbekistan" copy
    references the new Phase 2 Plan 02-01 city + Bishkek district seed (KG launch market;
    KZ + UZ city seeds positioned as expansion enabler).

    ---

    ## EN

    [Step 1 EN body, post-trim, with `- ` prefixes preserved as plain text — both stores
    render bullets as plain text, no Markdown]

    ---

    ## RU

    [Step 2 RU body, post-trim]

    ---

    ## Submission instructions

    [Copied from M2 06-RELEASE-NOTES.md "Submission instructions" section, with v2.0.0 → v3.0.0]

    **App Store Connect (per-locale "What's New in This Version"):** App Store Connect →
    JayTap → version 3.0.0 → "What's New in This Version". Default localization is English
    (US): paste the EN body above (everything between `## EN` and `---`, excluding those
    structural markers). Add the Russian (Russia) localization and paste the RU body.
    Both fit comfortably under ASC's 4000-char-per-locale limit.

    **Google Play Console (per-language "Release notes"):** Internal Testing track → Create new
    release → Release notes section → tap "Add language" or "Edit release notes per
    language". For `en-US` paste the EN body; for `ru-RU` paste the RU body. Both bodies
    are pre-trimmed to fit Play Console's 500-char-per-locale binding limit.

    Do NOT include the `## EN` / `## RU` headings or the `---` separators when pasting —
    those are file-structure markers for this draft, not part of the user-facing notes.

    ---

    ## Character counts

    Measured with `wc -m` (character count, not byte count — Play Console caps on
    characters). Both languages fit Play Console's 500-character-per-locale binding limit.

    | Language | Body characters | Play Console limit (500) | Fits? | Headroom |
    | -------- | --------------- | ------------------------ | ----- | -------- |
    | EN       | <N>             | 500                      | YES   | <500-N>  |
    | RU       | <N>             | 500                      | YES   | <500-N>  |

    Verification commands (re-runnable at paste time to confirm no drift):

    ```bash
    # EN body
    awk '/^## EN$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m

    # RU body
    awk '/^## RU$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m
    ```

    If either count climbs over 500 after edits, trim per the priority listed in the
    Action block of Plan 05-04 (Step 1).

    ---

    ## Re-open conditions

    - If Plan 05-05's QA matrix walk surfaces a release-blocking defect that requires explicit
      acknowledgment in user-facing copy, update this file (and re-verify wc -m) before Plan 05-07
      pastes.
    - If a future Phase introduces a Cyrillic-locale-only feature, the RU body may need a separate
      bullet (parity with EN may not be exact for region-specific functionality).
    - If KZ or UZ launches before this build ships (unlikely per ROADMAP), the "ready for expansion"
      phrasing needs softening.

    ## Cross-references

    - M1 release notes: `.planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-RELEASE-NOTES.md`
    - M2 release notes: `.planning/milestones/v2.0-phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md`
    - Memory `geographic-scope.md` (no Bishkek-only phrasing)
    - REQUIREMENTS.md REL-04 acceptance criteria.

    ---

    *Draft authored: <date> (Plan 05-04, Wave 1 of Phase 5)*
    *To be walked against build-identity SHA from Plan 05-03 (atomic v3.0.0 bump)*
    *QA disposition pending — flips when Plan 05-05 closes*
    *Per M1 D-11: paste-only artifact — not preserved as in-app i18n keys*
    ```

    After write, run the verification commands inline and replace `<N>` placeholders in the Character counts table with actual counts. The acceptance criteria below assert both counts are ≤ 500.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md && [ "$(awk '/^## EN$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m | tr -d ' ')" -le 500 ] && [ "$(awk '/^## RU$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m | tr -d ' ')" -le 500 ] && grep -q "## EN" .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md && grep -q "## RU" .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md && [ "$(grep -ic 'bishkek\|бишкек' .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md` exits 0
    - EN body character count via `awk '/^## EN$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' <file> | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m` is ≤ 500 (Play Console binding limit)
    - RU body character count via the equivalent `## RU` awk slice is ≤ 500
    - `grep -q '^## EN$' <file>` exits 0 (EN section exists)
    - `grep -q '^## RU$' <file>` exits 0 (RU section exists)
    - `grep -i '6-step\|6 шагов' <file>` returns ≥ 2 matches (the M3 headline feature is mentioned in BOTH bodies)
    - `grep -i 'kyrgyzstan\|кыргызстан\|kazakhstan\|казахстан\|uzbekistan\|узбекистан' <file>` returns ≥ 2 matches (KG/KZ/UZ expansion enabler hint per memory `geographic-scope.md`)
    - `grep -ic 'bishkek\|бишкек' <file>` returns 0 (NO Bishkek-only phrasing — country-level "Kyrgyzstan/Кыргызстан" is fine; city-level "Bishkek/Бишкек" is NOT)
    - `grep -E '"(firebase|@react-native-firebase)"' <file>` returns 0 (no SDK names in user-facing copy)
    - The "Bug fixes" closer is M1 D-10 catch-all phrasing only — `grep -iE 'fixed [a-z]+|исправлен[а-я]* [а-я]+' <file>` does not match a specific named bug (only generic "minor refinements" / «мелкие исправления» allowed). The grep -iE pattern checks for "fixed <word>" (specific) — the closer should NOT contain such; it should contain only the generic line.
    - The Character counts table contains actual integers (not `<N>` placeholders) — `grep -E '\| EN\s+\| [0-9]+' <file>` returns 1 line; same for RU
    - frontmatter `target_version: 3.0.0` (not `2.0.0`)
    - frontmatter `paste_only: true` (M1 D-11 precedent)
    - frontmatter `languages: [en, ru]`
    - `grep -c '## Re-open conditions' <file>` returns 1
    - `grep -c '## Cross-references' <file>` returns 1
  </acceptance_criteria>
  <done>
    `.planning/phases/05-hardening-manual-qa-release-v3/05-RELEASE-NOTES.md` exists with EN + RU bodies under 500 chars each, M1 D-10 content order (What's new → Improvements → Bug fixes catch-all), no Bishkek-only phrasing, no fabricated specific bug fixes, KG/KZ/UZ enabler hint present, and a re-runnable verification command block.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Draft → Plan 05-07 paste step | The release notes are a paste-only artifact; Plan 05-07 reads the EN + RU sections and pastes them into ASC + Play Console. |
| RU translation → user perception | The RU body is what Russian-speaking users see in their store. A bad translation would not be a security issue but would be a release-quality issue; Cyrillic + clean phrasing matter. |
| Geographic phrasing → market positioning | Bishkek-only phrasing leaking would prematurely commit to a city-only market positioning that contradicts memory `geographic-scope.md`'s KG/KZ/UZ scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Tampering | Release-notes integrity (fabricated bug-fix specifics) | mitigate | M1 D-10 catch-all phrasing is the rule. Acceptance criterion `grep -iE 'fixed [a-z]+|исправлен[а-я]* [а-я]+' <file>` returns 0 specific-named-bug matches. Future maintenance: if a release-blocking bug surfaces in Plan 05-05's QA walk and is fixed in-phase, the closer line stays generic; the specific fix is documented in `.planning/RETROSPECTIVE.md`, not in user-facing notes. |
| T-05-25 | Information Disclosure | Internal SDK / library names leaking into user-facing notes | mitigate | Acceptance criterion `grep -E '"(firebase|@react-native-firebase)"'` returns 0 — no SDK names in the user-facing copy. The "What's new" framing is feature-level (6-step flow + KG/KZ/UZ city dictionary + mod media curation), not implementation-level (Mongoose schema reshape, JWKS auth). |
| T-05-26 | Length-budget overflow | Play Console reject on > 500 chars | mitigate | Both `wc -m` checks are blocking acceptance criteria. The trimming priority is documented in the action block so the executor has a deterministic path to fit. |
| T-05-27 | Spoofing | Geographic phrasing leaking Bishkek-only assumptions | mitigate | Acceptance criterion `grep -ic 'bishkek\|бишкек' <file>` returns 0, per memory `geographic-scope.md` — the app's planned scope is KG/KZ/UZ, and the release notes should not assume Bishkek-only. The suggested draft uses "Kyrgyzstan, Kazakhstan, and Uzbekistan" / «Кыргызстан, Казахстан и Узбекистан» country-level phrasing without geographic anchors. |
| T-05-28 | Tampering | Mid-flight content shift between draft and paste-time | accept | Plan 05-07's paste step uses the documented `awk` slice commands to extract EN + RU bodies — deterministic byte-for-byte extraction. If the user manually retyped at paste time, the SUBMISSION-LOG records paste timestamps but trusts the user; this is the same flow as M2 Phase 6 Plan 06-07 and was clean. |

Block-on severity: high.
- T-05-26 (HIGH) — blocks if mitigation absent (Play Console reject means the release ships without notes or with truncated notes)
- T-05-27 (HIGH) — blocks if mitigation absent (Bishkek-only phrasing locks-in city positioning that contradicts geographic-scope.md)
</threat_model>

<verification>
- `05-RELEASE-NOTES.md` exists with EN + RU bodies
- Both bodies measure ≤ 500 chars per `wc -m`
- M1 D-10 content order honored (What's new → Improvements → Bug fixes catch-all)
- Cyrillic in RU body
- No Bishkek-only phrasing (`grep -ic 'bishkek\|бишкек'` returns 0)
- KG/KZ/UZ enabler hint present (≥ 2 matches across both bodies)
- 6-step contextual flow mentioned in both bodies
- No fabricated specific bug-fix names
- Character counts table has integers, not placeholders
</verification>

<success_criteria>
At Plan 05-07 (Wave 4 — store submission), the executor reads `## EN` and `## RU` body slices via the documented `awk` commands and pastes them into ASC + Play Console release-notes fields without further drafting work.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-manual-qa-release-v3/05-04-SUMMARY.md` with: actual EN + RU character counts (per `wc -m`), the language scope (en + ru only — no kk or uz per `geographic-scope.md`), the Bishkek-only-phrasing check result (0 matches), and the M1 D-10 disposition (catch-all closer retained / customised based on Plan 05-05's QA outcome).
</output>
</content>
</invoke>