---
phase: 06-hardening-manual-physical-device-qa-release
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md
autonomous: true
requirements: [REL-04]
requirements_addressed: [REL-04]
tags: [release, release-notes, en-ru-parity, play-console-500-char-limit, m1-d-10-content-order, no-fabricated-specifics]

must_haves:
  truths:
    - "06-RELEASE-NOTES.md exists with EN + RU bodies for ASC + Play Console paste at submission time (Plan 07)"
    - "EN body fits Play Console's binding 500-character-per-locale limit (`wc -m` ≤ 500)"
    - "RU body fits Play Console's binding 500-character-per-locale limit (`wc -m` ≤ 500)"
    - "Content order honors M1 D-10: What's new (Roles & Moderation) → Improvements (security hardening / data integrity) → Bug fixes (catch-all phrasing only — NO fabricated specifics)"
    - "RU body uses Cyrillic (NOT Kyrgyz or Uzbek — per memory `geographic-scope.md`, RU is the only non-EN locale at v2.0.0)"
    - "Geographic phrasing avoids Bishkek-only assumptions (per memory `geographic-scope.md` — KG/KZ/UZ scope)"
    - "Bug fixes section uses M1 D-10 catch-all phrasing only (no specific fix names fabricated)"
  artifacts:
    - path: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md"
      provides: "REL-04 — bilingual EN+RU release notes pasted on ASC + Play Console at Plan 07 submission time"
      contains: "## EN"
      contains_also: "## RU"
  key_links:
    - from: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md :: ## EN body"
      to: "ASC :: What's New in This Version (en-US locale) + Play Console :: Release notes (en-US)"
      via: "Plan 07 paste step at submission time"
      pattern: "## EN"
    - from: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md :: ## RU body"
      to: "ASC :: What's New in This Version (ru-RU locale) + Play Console :: Release notes (ru-RU)"
      via: "Plan 07 paste step at submission time"
      pattern: "## RU"
---

<objective>
Wave 1 of Phase 6 — draft bilingual EN+RU release notes for v2.0.0 "Roles & Moderation" that fit Google Play Console's binding 500-character-per-locale limit and follow M1 D-10 content order.

This is purely a documentation task; no source code, no app behavior, no submission action. The output is a paste-only artifact (per M1 D-11 precedent — `.planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-RELEASE-NOTES.md` is structurally identical and was Plan 08-04's deliverable in M1).

Why this plan runs in Wave 1 (parallel with Plan 01 + 02 + 04, not after the QA matrix walk):
- Release notes copy is independent of the version bump (Plan 05) and the QA matrix walk (Plan 06).
- Drafting Wave-1 lets the user review copy before the time-sensitive QA window opens.
- M1 D-10 prescribes content order (What's new → Improvements → Bug fixes); the M2 closed-phase deliverables (Phases 1–5) are the "What's new" content — those are stable at planning time.
- "Bug fixes" copy uses catch-all phrasing per M1 D-10, so the QA matrix walk does NOT need to surface specific fixes for the release notes (per M1 D-08, regression QA loop fixes are documented internally; release-notes copy stays generic).

Output: `06-RELEASE-NOTES.md` with the structural template from M1's `08-RELEASE-NOTES.md` adapted to v2.0.0 content. Both bodies under 500 chars per `wc -m`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/RETROSPECTIVE.md
@.planning/PROJECT.md
@.planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-RELEASE-NOTES.md
@.planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-CONTEXT.md

<interfaces>
<!-- M1 v1.0.4 release notes are the structural template (NOT the body). Copy the SHAPE -->
<!-- (frontmatter + ## EN / ## RU sections + Submission instructions + Character counts -->
<!-- table + verification commands), but author NEW v2.0.0 BODY content. -->

M1 v1.0.4 release notes structure (from `.planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-RELEASE-NOTES.md`):
1. Frontmatter (paste-only artifact metadata)
2. ## EN section: What's new → Improvements → "Minor refinements" closer (M1 D-10)
3. ## RU section: Что нового → Улучшения → "Мелкие исправления" closer
4. Submission instructions (ASC + Play Console paths)
5. Character counts table (`wc -m` measured per body, ≤ 500)
6. Verification commands (`awk` slice + `wc -m` re-runnable at paste time)

M1 D-10 content order (re-anchored from `08-CONTEXT.md`):
1. **What's new** — the headline feature
2. **Improvements** — supporting work (security / data integrity / UX polish)
3. **Bug fixes** — catch-all phrasing only ("Minor stability and polish refinements" / «Мелкие исправления и шлифовка интерфейса»). NO fabricated specifics.

M2 closed-phase deliverables (the "What's new" content for v2.0.0):
- Phase 1: backend role foundation + JWKS auth + 4 hotfixes (security hardening; not user-visible directly)
- Phase 2: listing lifecycle status field + 4-tab OwnerListings (Live / Pending / Rejected / Archived) + status pill on PropertyCard + RejectionBanner on PropertyDetailsScreen + Home rejection banner
- Phase 3: Moderation Queue (FIFO) + Approve / Reject / Edit-on-behalf actions
- Phase 4: Archive lifecycle (owner self-archive + mod/admin archive + restore-to-pending re-moderation)
- Phase 5: Admin Role Management UI (search users + change roles)

The headline feature for renters/buyers is **moderated listings** (a quality / safety improvement). The headline feature for owners is **the new listing-status flow** (clearer visibility into where their listings are in the moderation pipeline). The internal admin/moderation surfaces (Moderation Queue + Role Management) are NOT user-visible to most users — the release notes should frame them as "behind-the-scenes safety improvements" rather than as a feature renters/buyers can use.

Russian release-notes locale per memory `geographic-scope.md`: RU only (NOT Kyrgyz or Uzbek). RU body uses Cyrillic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft EN + RU v2.0.0 release notes within 500-char Play Console limit per locale</name>
  <files>.planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md</files>
  <read_first>
    - .planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-RELEASE-NOTES.md (full file — copy the SHAPE: frontmatter, ## EN / ## RU sections, Submission instructions block, Character counts table, verification commands)
    - .planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-CONTEXT.md lines 72-87 (re-anchor M1 D-10 + D-11)
    - .planning/REQUIREMENTS.md lines 30-82 (re-anchor M2 closed-phase deliverables — what shipped in Phases 1-5)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/geographic-scope.md (re-anchor KG/KZ/UZ scope; avoid Bishkek-only phrasing)
    - .planning/PROJECT.md lines 5-30 (re-anchor M2 v2.0 milestone framing — "moderated listings" + "owner clarity in pipeline" rather than internal-mod-tools framing)
  </read_first>
  <action>
    Single-file draft, single commit. NO source-file mutation.

    **Step 1 — Author the EN body.** Target ≤ 500 chars (Play Console binding limit). Suggested draft:

    ```
    What's new
    - Listings are now reviewed before going public, so what you see is more trustworthy.
    - Owners get a clearer status flow: Live, Pending, Rejected, Archived — with reason notes when a listing is sent back for edits.

    Improvements
    - Stronger account security and authentication.
    - More reliable session handling — you stay signed in across the app.

    Minor stability and polish refinements.
    ```

    Iterate to fit ≤ 500. Verify with `wc -m`:
    ```bash
    cat <<'EOF' | wc -m
    [paste candidate body — everything between '## EN' and the next '---', excluding markers]
    EOF
    ```

    Trimming priority if over budget (per M1 precedent):
    1. Drop the "Minor stability..." closer first.
    2. Drop the second Improvements bullet.
    3. Trim the descriptive tail of the longest What's new bullet.
    Preserve EN ↔ RU parity (same content, same order, same number of bullets).

    **Step 2 — Author the RU body.** Same content, same order, in Cyrillic. Target ≤ 500 chars. Suggested draft:

    ```
    Что нового
    - Объявления теперь проходят модерацию перед публикацией — то, что вы видите, надёжнее.
    - У владельцев появился прозрачный статус: Опубликовано, На модерации, Отклонено, Снято с публикации — с причиной возврата на доработку.

    Улучшения
    - Усилена защита аккаунта и проверка входа.
    - Сессии работают стабильнее — вы остаётесь в системе по всему приложению.

    Мелкие исправления и шлифовка интерфейса.
    ```

    Iterate to fit ≤ 500. Use the same RU verification command (replace `## EN` with `## RU` in the awk pattern).

    **Step 3 — Author `06-RELEASE-NOTES.md` using the M1 structural template.**

    Use the `Write` tool. Frontmatter:

    ```yaml
    ---
    phase: 06-hardening-manual-physical-device-qa-release
    plan: 03
    type: release-notes
    created: <ISO 8601 timestamp>
    target_stores: [App Store Connect, Google Play Console]
    target_version: 2.0.0
    walk_disposition: pending  # flips to APPROVED after Plan 06 manual QA matrix
    bug_fix_loop_triggered: pending  # flips after Plan 06 walk completes
    walked_against_sha: pending  # flips after Plan 05 atomic bump
    languages: [en, ru]
    binding_length_limit: 500 chars per locale (Play Console)
    asc_length_limit: 4000 chars per locale
    paste_only: true
    m1_template: .planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-RELEASE-NOTES.md
    ---

    # JayTap v2.0.0 — Release Notes

    Bilingual draft for App Store Connect (per-locale "What's New in This Version") and
    Google Play Console (per-language "Release notes" / "What's new"). Paste at submission
    time in Plan 06-07; do not commit changes back from the ASC / Play Console UI into
    this file (per M1 D-11 precedent — paste-only artifact, not preserved as in-app i18n).

    Both bodies cover the same content in the same order: **What's new → Improvements →
    Bug fixes**. The "Bug fixes" reduces to a single "minor refinements" line per M1 D-10
    catch-all phrasing — specific fixes are NOT fabricated. If Plan 06's QA matrix walk
    surfaces a release-blocking defect that should be called out, this file is updated
    after that walk completes (the M1 v1.0.4 walk surfaced zero defects, so the closer
    line was retained as-is; the same default applies here).

    Each language body fits Google Play Console's binding 500-character-per-locale limit;
    App Store Connect's 4000-character limit is non-binding for these notes.

    ---

    ## EN

    [Step 1 EN body, post-trim, with `- ` prefixes preserved as plain text — both stores
    render bullets as plain text, no Markdown]

    ---

    ## RU

    [Step 2 RU body, post-trim]

    ---

    ## Submission instructions

    [Copied from M1 08-RELEASE-NOTES.md "Submission instructions" section, with v1.0.4 → v2.0.0]

    **App Store Connect (per-locale "What's New in This Version"):** App Store Connect →
    JayTap → version 2.0.0 → "What's New in This Version". Default localization is English
    (US): paste the EN body above (everything between `## EN` and `---`, excluding those
    structural markers). Add the Russian (Russia) localization and paste the RU body.
    Both fit comfortably under ASC's 4000-char-per-locale limit.

    **Google Play Console (per-language "Release notes"):** Production track → Create new
    release → Release notes section → tap "Add language" or "Edit release notes per
    language". For `en-US` paste the EN body; for `ru-RU` paste the RU body. Both bodies
    are pre-trimmed to fit Play Console's 500-char-per-locale binding limit.

    Do NOT include the `## EN` / `## RU` headings or the `---` separators when pasting —
    those are file-structure markers for this draft, not part of the user-facing notes.

    ---

    ## Character counts

    Measured with `wc -m` (character count, not byte count — Play Console caps on
    characters). Both languages fit Play Console's 500-character-per-locale binding limit.

    | Language | Body characters | Play Console limit (500) | Fits? | Headroom |
    | -------- | --------------- | ------------------------ | ----- | -------- |
    | EN       | <N>             | 500                      | YES   | <500-N>  |
    | RU       | <N>             | 500                      | YES   | <500-N>  |

    Verification commands (re-runnable at paste time to confirm no drift):

    ```bash
    # EN body
    awk '/^## EN$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m

    # RU body
    awk '/^## RU$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m
    ```

    If either count climbs over 500 after edits, trim per the priority listed in Step 1
    of Plan 06-03's action block.

    ---

    *Draft authored: <date> (Plan 06-03, Wave 1 of Phase 6)*
    *To be walked against build-identity SHA from Plan 06-05 (atomic v2.0.0 bump)*
    *QA disposition pending — flips when Plan 06-06 closes*
    *Per M1 D-11: paste-only artifact — not preserved as in-app i18n keys*
    ```

    After write, run the verification commands inline and replace `<N>` placeholders in the Character counts table with actual counts. The acceptance criteria below assert both counts are ≤ 500.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md && [ "$(awk '/^## EN$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m | tr -d ' ')" -le 500 ] && [ "$(awk '/^## RU$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m | tr -d ' ')" -le 500 ] && grep -q "## EN" .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md && grep -q "## RU" .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md` exits 0
    - EN body character count via `awk '/^## EN$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' <file> | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m` is ≤ 500 (Play Console binding limit)
    - RU body character count via the equivalent `## RU` awk slice is ≤ 500
    - `grep -q '^## EN$' <file>` exits 0 (EN section exists)
    - `grep -q '^## RU$' <file>` exits 0 (RU section exists)
    - `grep -i 'roles\|moderation\|review' <file>` returns ≥ 1 match in the EN body (the M2 headline feature is mentioned)
    - `grep 'модерац' <file>` returns ≥ 1 match in the RU body (Cyrillic «модерация» / «модерации»)
    - `grep -i 'bishkek-only\|только бишкек' <file>` returns 0 lines (no Bishkek-only phrasing per memory `geographic-scope.md`)
    - `grep -E '"(firebase|@react-native-firebase)"' <file>` returns 0 lines (no SDK names in user-facing copy)
    - The "Bug fixes" closer is M1 D-10 catch-all phrasing only — `grep -E "(fixed|исправлен)" <file>` returns 0 lines that name a specific bug (only generic "minor refinements" / «мелкие исправления» allowed)
    - The Character counts table contains actual integers (not `<N>` placeholders)
    - frontmatter `target_version: 2.0.0` (not `1.0.4`)
    - frontmatter `paste_only: true` (M1 D-11 precedent)
  </acceptance_criteria>
  <done>
    `.planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md` exists with EN + RU bodies under 500 chars each, M1 D-10 content order (What's new → Improvements → Bug fixes catch-all), no fabricated specific bug fixes, no Bishkek-only phrasing, and a re-runnable verification command block.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Draft → Plan 07 paste step | The release notes are a paste-only artifact; Plan 07 reads the EN + RU sections and pastes them into ASC + Play Console. |
| RU translation → user perception | The RU body is what Russian-speaking users see in their store. A bad translation would not be a security issue but would be a release-quality issue; Cyrillic + clean phrasing matter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | Release-notes integrity (fabricated bug-fix specifics) | mitigate | M1 D-10 catch-all phrasing is the rule. Acceptance criteria assert `grep -E "(fixed\|исправлен)"` for specific bugs returns 0 lines. Future maintenance: if a release-blocking bug surfaces in Plan 06's QA walk and is fixed in-phase, the closer line stays generic; the specific fix is documented in `.planning/RETROSPECTIVE.md`, not in user-facing notes. |
| T-06-12 | Information Disclosure | Internal SDK / library names leaking into user-facing notes | mitigate | Acceptance criterion `grep -E '"(firebase\|@react-native-firebase)"'` returns 0 — no SDK names in the user-facing copy. The "What's new" framing is feature-level (moderated listings), not implementation-level (JWKS auth). |
| T-06-13 | Length-budget overflow | Play Console reject on > 500 chars | mitigate | Both `wc -m` checks are blocking acceptance criteria. The trimming priority is documented in the action block so the executor has a deterministic path to fit. |
| T-06-14 | Spoofing | Geographic phrasing leaking Bishkek-only assumptions | mitigate | Acceptance criterion `grep -i 'bishkek-only\|только бишкек'` returns 0 lines, per memory `geographic-scope.md` — the app's planned scope is KG/KZ/UZ, and the release notes should not assume Bishkek-only. The suggested draft uses generic "Listings are now reviewed" / «Объявления теперь проходят модерацию» phrasing without geographic anchors. |

Block-on severity: high.
- T-06-13 (HIGH) — blocks if mitigation absent (Play Console reject means the release ships without notes or with truncated notes)
</threat_model>

<verification>
- `06-RELEASE-NOTES.md` exists with EN + RU bodies
- Both bodies measure ≤ 500 chars per `wc -m`
- M1 D-10 content order honored (What's new → Improvements → Bug fixes catch-all)
- Cyrillic in RU body
- No Bishkek-only phrasing
- No fabricated specific bug-fix names
- Character counts table has integers, not placeholders
</verification>

<success_criteria>
At Plan 07 (Wave 4 — store submission), the executor reads `## EN` and `## RU` body slices via the documented `awk` commands and pastes them into ASC + Play Console release-notes fields without further drafting work.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-manual-physical-device-qa-release/06-03-SUMMARY.md` with: actual EN + RU character counts (per `wc -m`), the language scope (en + ru only — no kk or uz per `geographic-scope.md`), and the M1 D-10 disposition (catch-all closer retained / customised based on Plan 06's QA outcome).
</output>
