---
phase: 06-hardening-manual-physical-device-qa-release
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md
autonomous: false
requirements: [REL-05]
requirements_addressed: [REL-05]
tags: [release, backend-deploy, railway, credential-rotation, firebase-project-id, mongodb-rotation, aws-iam-rotation, firebase-admin-removal, cross-repo, nvm-use-24]

user_setup:
  - service: railway
    why: "Backend deploy target. FIREBASE_PROJECT_ID env var must be set in Railway dashboard for jose JWKS verification (verifyFirebaseToken middleware reads it)."
    env_vars:
      - name: FIREBASE_PROJECT_ID
        source: "Firebase Console → Project Settings → General → Project ID. Must match the same project ID used by the RN client (which has Identity Toolkit API key)."
    dashboard_config:
      - task: "Confirm FIREBASE_PROJECT_ID is set under JayTap-services service variables"
        location: "Railway Dashboard → JayTap-services → Variables tab"
  - service: mongodb-atlas
    why: "M2 HF-02 hotfix bundle rotated MongoDB Atlas password. Phase 6 confirms the OLD password is dead (revocation verified, not just rotated)."
    env_vars: []
    dashboard_config:
      - task: "Confirm the pre-M2 MongoDB Atlas password is revoked (deletion of the database user with the old password, or password change to a value distinct from the pre-M2 value)"
        location: "MongoDB Atlas Dashboard → JayTap project → Database Access → Users"
  - service: aws-iam
    why: "Per auto-memory `aws-iam-jaytap-prod-s3.md`, the dedicated `jaytap-prod-s3` IAM user was created 2026-04-29 to replace a shared cross-project IAM user. Phase 6 confirms the OLD shared IAM user's keys are revoked or that the user no longer has S3 write access to the JayTap bucket."
    env_vars: []
    dashboard_config:
      - task: "Confirm the OLD shared IAM user is dead — either deleted, or its access keys are revoked, or its policy no longer grants s3:PutObject on the JayTap bucket. The new dedicated `jaytap-prod-s3` IAM user is the only credential set Railway uses for JayTap S3 operations."
        location: "AWS IAM Console → Users → check the OLD shared user's access-keys tab + policy attachments"

must_haves:
  truths:
    - "Backend repo at /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services is on its main branch with the M2 hotfix bundle (HF-01..HF-04) shipped"
    - "Backend `package.json` does NOT contain `firebase-admin` (REL-05 — orchestrator additional_context confirms this is already true; spot-check verifies)"
    - "Spot-check evidence in 06-BACKEND-DEPLOY.md Section 1 confirms firebase-admin was already absent at audit time (per memory `backend-node-version.md` / orchestrator additional_context — removed 2026-04-30 in M2 Phase 1) — REL-05 satisfied by inheritance, not by a removal action this phase"
    - "Backend package.json contains zero firebase-admin / firebase / @firebase/* / @react-native-firebase/* deps — repo rule mirror per memory `no-firebase-sdk.md` confirmed for the backend repo too (jose-only for JWKS verification)"
    - "Backend `package.json` `engines.node` is `>=22.12.0` (jose@6 ESM-only — see memory `backend-node-version.md`)"
    - "Railway environment has `FIREBASE_PROJECT_ID` set (JWKS audience + issuer construction in `verifyFirebaseToken.js` reads this)"
    - "OLD pre-M2 MongoDB Atlas password is REVOKED (not just rotated — verification confirms it can no longer authenticate)"
    - "OLD shared cross-project AWS IAM user's keys are REVOKED OR its policy no longer grants s3:PutObject on the JayTap bucket (per memory `aws-iam-jaytap-prod-s3.md`)"
    - "Railway production endpoint responds 200 to a basic health-check route after redeploy"
    - "06-BACKEND-DEPLOY.md exists and records all five rotation/revocation/deploy outcomes with timestamps"
  artifacts:
    - path: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md"
      provides: "REL-05 — backend-side release readiness evidence; binding for Plan 07 submission gate"
      contains: "FIREBASE_PROJECT_ID"
      contains_also: "firebase-admin"
  key_links:
    - from: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md :: rotation evidence"
      to: "Plan 06-06 manual QA matrix (REL-03) :: HF-02 hotfix verification cell"
      via: "QA matrix verifies that the LIVE backend rejects auth requests under the OLD MongoDB password (rotation took effect) and accepts them under the NEW credentials"
      pattern: "rotated_at"
    - from: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md :: railway deploy SHA"
      to: "Plan 06-07 store submission (REL-06)"
      via: "Plan 07 only proceeds with submission if Plan 04 declares the backend live + healthy"
      pattern: "railway_deploy_sha"
---

<objective>
Wave 1 of Phase 6 — bring the JayTap-services backend to v2.0.0 release readiness on Railway: confirm `firebase-admin` removed (orchestrator says already done; spot-check), confirm `FIREBASE_PROJECT_ID` env var present in Railway, confirm OLD pre-M2 MongoDB Atlas password REVOKED (not just rotated), confirm OLD shared cross-project AWS IAM user dead, redeploy backend, and verify health-check.

This plan operates against a SEPARATE REPO at `/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services` (per memory `backend-repo-location.md`). The executor MUST `cd` into the backend repo and `nvm use 24` BEFORE running any `npm` / `node` / Railway CLI command (per memory `backend-node-version.md` — backend declares `engines.node >=22.12.0`; default shell node is v20.19.1 which won't run jose@6 ESM imports).

This plan is `autonomous: false` because credential revocation lives in dashboards (MongoDB Atlas + AWS IAM Console + Railway Dashboard) that the executor cannot drive automatically. The plan packages a checkpoint:human-action gate at the end.

Output: `06-BACKEND-DEPLOY.md` recording (a) backend `package.json` spot-check (firebase-admin absent confirmed), (b) Railway env var presence confirmed, (c) MongoDB old-password revocation confirmed (not just rotated), (d) AWS IAM old-user revocation confirmed, (e) Railway redeploy SHA + timestamp + health-check status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@CLAUDE.md

<interfaces>
<!-- Cross-repo: backend lives at /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services -->
<!-- Backend Node version: nvm use 24 (engines.node >=22.12.0; jose@6 ESM-only) -->
<!-- Memory anchors: backend-repo-location.md, backend-node-version.md, aws-iam-jaytap-prod-s3.md, no-firebase-sdk.md -->

Backend repo state at audit time (sampled 2026-05-05 by orchestrator additional_context):
- `package.json` `dependencies` keys (sample): `@aws-sdk/client-s3`, `cors`, `dotenv`, `express`, `jose`, `mongoose`, `multer`, `multer-s3`, `socket.io`. No `firebase-admin`. No `firebase`.
- `engines.node`: `>=22.12.0`
- `.env.example` keys: `MONGO_URI`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_REGION`, `AWS_BUCKET_NAME`, `FIREBASE_PROJECT_ID`, `PORT`. The example file is the contract for what Railway production env vars must define.

Backend's verifyFirebaseToken.js reads:
```javascript
const { JWKS, ISSUER, FIREBASE_PROJECT_ID } = require('../config/firebase');
// ...
const { payload } = await jwtVerify(bearerToken, JWKS, {
  issuer: ISSUER,
  audience: FIREBASE_PROJECT_ID,
  algorithms: ['RS256'],
  clockTolerance: 60,
});
```

If `FIREBASE_PROJECT_ID` is missing in Railway env, the JWKS verification fails for every request and every protected route returns 401. This is the highest-priority env-var-presence check.

Hotfixes shipped in M2 Phase 1 (the four hotfixes Plan 06 must empirically verify in the QA matrix; Plan 04 verifies the backend deploy that hosts them):
- HF-01: Hospitality `rooms`/`maxGuests`/`amenities` schema persistence
- HF-02: Backend `.env` removed from git tracking + secrets rotated (this plan's primary credential-rotation scope)
- HF-03: `POST /api/auth/users` rejects body-supplied `firebaseUid` mismatching JWKS-verified `sub`
- HF-04: Socket.io handshake JWKS-verifies and only allows `user:<verified-sub>` room join
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spot-check backend package.json + verify Railway env vars + redeploy</name>
  <files>.planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md</files>
  <read_first>
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/package.json (full file — confirm firebase-admin absent + engines.node >=22.12.0)
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/.env.example (full file — confirm FIREBASE_PROJECT_ID is in the example contract)
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/src/middleware/verifyFirebaseToken.js (lines 1-100 — confirm FIREBASE_PROJECT_ID is consumed for JWKS audience verification)
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/index.js (read full file — find the health-check / root route the post-deploy verification will hit)
    - /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services/Procfile (read full file — confirm Railway start command)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/backend-repo-location.md (re-anchor cross-repo path)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/backend-node-version.md (re-anchor `nvm use 24` requirement before npm/node/railway commands)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/aws-iam-jaytap-prod-s3.md (re-anchor AWS IAM rotation context — new dedicated user already created; this plan's job is OLD-user-dead verification)
  </read_first>
  <action>
    Multi-step plan combining automated checks (Steps 1, 2, 6) with human-action checkpoints (Steps 3, 4, 5).

    **All `npm` / `node` / `railway` CLI commands MUST run with `nvm use 24` first per memory `backend-node-version.md`.** Failure to do this means the commands run on node v20 which doesn't support jose@6 ESM imports (the test suite + health-check WILL fail in confusing ways without nvm).

    **Step 1 — Spot-check backend package.json (automated):**

    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services
    # Combined Firebase-family grep — mirrors the RN-client repo rule (memory no-firebase-sdk.md) for the backend repo too.
    # Backend uses jose for JWKS verification — no firebase-admin, no firebase, no @firebase/*, no @react-native-firebase/*.
    grep -cE '"(firebase-admin|firebase|@firebase/|@react-native-firebase/)"' package.json   # MUST return 0
    # Per-key spot checks for human-readable evidence in Section 1 of 06-BACKEND-DEPLOY.md:
    grep -c '"firebase-admin"' package.json   # MUST return 0
    grep -c '"firebase"' package.json          # MUST return 0
    grep -c '"@firebase/' package.json          # MUST return 0
    grep -c '"@react-native-firebase' package.json   # MUST return 0
    grep '"engines"' -A 3 package.json | grep 'node'   # MUST contain >=22.12.0
    ```

    Record outputs verbatim in `06-BACKEND-DEPLOY.md` Section 1.

    **Step 2 — Verify Railway env vars (`railway` CLI; cross-repo + nvm-use-24):**

    ```bash
    cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services
    nvm use 24
    railway status   # confirm logged in + correct project
    railway variables   # list all env vars currently set on the service
    ```

    Confirm `FIREBASE_PROJECT_ID` is in the output. Also confirm presence of `MONGO_URI`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_REGION`, `AWS_BUCKET_NAME`, `PORT` (the .env.example contract). DO NOT print actual values into the artifact — only key NAMES + a "present: true" / "present: false" boolean per key. Values are sensitive.

    If `FIREBASE_PROJECT_ID` is MISSING, that's a release-blocker. The executor MUST set it via:
    ```bash
    railway variables set FIREBASE_PROJECT_ID=<value-from-firebase-console>
    ```
    The value comes from Firebase Console → Project Settings → General → Project ID. The executor SHOULD pause and ask the user to provide it (it's not a public secret per se, but pasting it into a shell command from chat is fine — the user has it). After setting, re-run `railway variables` and confirm presence.

    Record results in `06-BACKEND-DEPLOY.md` Section 2: which keys are present, which are missing, what action was taken.

    **Step 3 — Verify OLD MongoDB Atlas password REVOKED (checkpoint:human-action):**

    The executor cannot directly assert that a password is revoked from outside the dashboard. Pause the plan with a checkpoint instructing the user to:
    1. Open MongoDB Atlas Dashboard → JayTap project → Database Access → Users.
    2. Confirm the database user password has been changed since the M2 HF-02 rotation event (2026-04-29 or later — see HF-02 in REQUIREMENTS.md).
    3. Confirm the OLD password no longer authenticates. Verification path: from a clean shell, attempt to connect with the OLD URI:
       ```bash
       mongo "mongodb+srv://<user>:<old-password>@<cluster>/<db>" --eval 'db.runCommand({ping: 1})'
       ```
       Expected: authentication failure (the OLD password is dead).
    4. Report back to the executor with: revoked_at timestamp, OR a screenshot of MongoDB Atlas showing the user's password-rotated date is post-2026-04-29.

    Record outcome in `06-BACKEND-DEPLOY.md` Section 3.

    **Step 4 — Verify OLD shared cross-project AWS IAM user DEAD (checkpoint:human-action):**

    Per memory `aws-iam-jaytap-prod-s3.md`, the new dedicated `jaytap-prod-s3` IAM user was created 2026-04-29 with inline policy scoped to the JayTap bucket. The OLD shared user (used to be borrowed cross-project) MUST no longer have credentials that work for JayTap S3 writes.

    Pause the plan and ask the user to:
    1. Open AWS IAM Console → Users → locate the OLD shared user.
    2. Confirm one of:
       - The OLD user is DELETED (preferred), OR
       - The OLD user's access keys are deactivated/deleted (no active key exists), OR
       - The OLD user's policy no longer includes `s3:PutObject` / `s3:DeleteObject` on the JayTap bucket.
    3. Report back: which path, with timestamp / policy-revision evidence.

    Record outcome in `06-BACKEND-DEPLOY.md` Section 4.

    **Step 5 — Redeploy backend on Railway (checkpoint:human-action OR `railway up` if executor has CLI permissions):**

    Two paths:
    - Path A — `railway up` (if executor has Railway CLI auth on disk):
      ```bash
      cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services
      nvm use 24
      git status   # MUST be clean
      git rev-parse HEAD   # capture deploy SHA
      railway up   # OR `railway redeploy` — depends on Railway CLI version
      railway logs --tail   # confirm successful boot (look for the "listening on PORT" line)
      ```
    - Path B — checkpoint:human-action (if Railway CLI is not authed): pause and ask the user to trigger a Railway redeploy from the dashboard (or via `git push`-based auto-deploy if the project is wired to auto-deploy main). The user reports back the deploy SHA + boot timestamp.

    Record results in `06-BACKEND-DEPLOY.md` Section 5: deploy SHA, deploy timestamp, boot-success boolean.

    **Step 6 — Health-check the live Railway endpoint (automated):**

    ```bash
    curl -sS -o /dev/null -w '%{http_code}' https://jaytap-services-production.up.railway.app/   # adjust path to actual root URL
    # Expected: 200 (or whatever the index route returns — check index.js)
    ```

    If the index route returns a non-200, find the health-check route in index.js (often `/` or `/health`) and use that. If no health route exists, hit `GET /api/properties` (a known existing route) and confirm it responds 200 with empty array OR 401 if the route requires auth (either is acceptable — the point is the server is up).

    Record results in `06-BACKEND-DEPLOY.md` Section 6: HTTP status, response time.

    **Step 7 — Author `06-BACKEND-DEPLOY.md` with this structure:**

    ```markdown
    ---
    phase: 06-hardening-manual-physical-device-qa-release
    plan: 04
    type: backend-deploy-evidence
    audited_at: <ISO 8601 timestamp>
    backend_repo: /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services
    backend_repo_sha_at_deploy: <git rev-parse HEAD output from Step 5>
    railway_deploy_sha: <Railway-reported deployed-image SHA — may equal repo SHA>
    railway_deploy_timestamp: <ISO 8601>
    health_check_status: <HTTP code from Step 6>
    nvm_node_version_used: 24
    ---

    # Phase 6 — Backend Deploy Evidence (REL-05)

    ## Section 1 — package.json spot-check
    ...
    ## Section 2 — Railway env vars present
    ...
    ## Section 3 — MongoDB Atlas old-password REVOKED
    ...
    ## Section 4 — AWS IAM old-user DEAD
    ...
    ## Section 5 — Railway redeploy
    ...
    ## Section 6 — Health-check
    ...
    ## Re-open conditions
    ...
    ```

    Use the `Write` tool. After write, run the assertions in `<acceptance_criteria>`.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && [ "$(grep -cE '"(firebase-admin|firebase|@firebase/|@react-native-firebase/)"' package.json)" = "0" ] && grep -q '"node": ">=22.12' package.json && test -f /Users/beckmaldinVL/development/mobileApps/JayTap/.planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && grep -cE '"(firebase-admin|firebase|@firebase/|@react-native-firebase/)"' package.json` returns 0 (combined Firebase-family check — REL-05 dead-code removal + repo-rule mirror for backend per memory `no-firebase-sdk.md`)
    - `cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && grep -c '"firebase-admin"' package.json` returns 0 (REL-05 dead-code removal already done; spot-check confirms — orchestrator additional_context: removed 2026-04-30 in M2 Phase 1)
    - `cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && grep -c '"firebase"' package.json` returns 0 (no Firebase SDK at all in the backend either; jose-only)
    - `cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && grep -cE '"@(firebase|react-native-firebase)/' package.json` returns 0 (no scoped Firebase packages either)
    - `cd /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services && grep '"node"' package.json` returns a line containing `>=22.12.0` (engines pin honored per memory `backend-node-version.md`)
    - `06-BACKEND-DEPLOY.md` Section 2 lists every env var key from `.env.example` (`MONGO_URI`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_REGION`, `AWS_BUCKET_NAME`, `FIREBASE_PROJECT_ID`, `PORT`) with a `present: true` entry. NO env-var VALUES are in the file (only key names + presence booleans).
    - `06-BACKEND-DEPLOY.md` Section 3 records the MongoDB old-password revocation evidence with a timestamp ≥ 2026-04-29 (HF-02 rotation event).
    - `06-BACKEND-DEPLOY.md` Section 4 records the AWS IAM old-user DEAD evidence per one of the three acceptable paths (deleted / keys deactivated / policy revoked) with a timestamp.
    - `06-BACKEND-DEPLOY.md` Section 5 records the Railway deploy SHA (matches `git rev-parse HEAD` at deploy time) and a deploy timestamp.
    - `06-BACKEND-DEPLOY.md` Section 6 records `health_check_status: 200` (or another acceptable status if the root route returns 401 by design — record the actual code).
    - `grep -rE '(OLD_MONGO_PASSWORD|OLD_AWS_KEY)' .planning/phases/06-hardening-manual-physical-device-qa-release/` returns 0 matches IF the user has substituted real OLD-credential substrings into the artifact (the security gate per the orchestrator security_threat_model — OLD credentials must NOT be committed). The artifact MUST refer to OLD credentials only by date and metadata, never by literal value.
    - `grep -rE '(MONGO_URI|AWS_SECRET_ACCESS_KEY)=mongodb\+srv://[^<]|AKIA[A-Z0-9]+' .planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md` returns 0 matches (no live secrets in the artifact)
    - `git log --oneline -5 .planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md` after commit shows ≥ 1 commit (the file is committed to the JayTap RN client repo, not the backend repo)
  </acceptance_criteria>
  <done>
    `.planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md` exists with all six sections populated; backend live on Railway with FIREBASE_PROJECT_ID present; OLD MongoDB password + OLD shared AWS IAM user both confirmed DEAD; firebase-admin absent from backend package.json; health-check responds 200.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human-action gate — credential-revocation evidence collection</name>
  <what-built>
    Plan 04 Task 1 automated the package.json spot-check and Railway env-var listing. Steps 3 + 4 + 5 of Task 1 are dashboard-driven and pause for the user to perform the action and report back to the executor. This checkpoint formalizes that pause.
  </what-built>
  <how-to-verify>
    1. **MongoDB Atlas (Step 3):** Open https://cloud.mongodb.com → JayTap project → Database Access → Users. Click the database user; confirm "Last password change" timestamp is ≥ 2026-04-29 (HF-02 rotation event). Optionally test that the OLD password fails: `mongo "mongodb+srv://<user>:<old-password>@<cluster>/" --eval 'db.runCommand({ping: 1})'` should return an authentication error. Report the timestamp.

    2. **AWS IAM (Step 4):** Open https://console.aws.amazon.com/iam → Users. Locate the OLD shared cross-project IAM user (the one whose keys were borrowed by JayTap pre-2026-04-29). Confirm one of: (a) the user is DELETED; OR (b) all access keys for that user are "Inactive" / "Deleted"; OR (c) the user's attached/inline policies no longer grant `s3:PutObject` or `s3:DeleteObject` on the JayTap bucket ARN. Report which path applies, with a timestamp / policy-revision date.

    3. **Railway redeploy (Step 5):** If the executor's Path A (CLI `railway up`) failed in Task 1, manually trigger a Railway redeploy from https://railway.app → JayTap-services service → Deployments tab → "Redeploy". Wait for status `SUCCESS`. Note the deploy SHA + timestamp.

    Resume by reporting the three timestamps + AWS IAM revocation path back to the executor. The executor adds them to Sections 3, 4, 5 of `06-BACKEND-DEPLOY.md`.
  </how-to-verify>
  <resume-signal>
    Reply with: `mongo_revoked_at: <ISO 8601>; aws_iam_path: <deleted|keys_deactivated|policy_revoked>; aws_iam_at: <ISO 8601>; railway_deploy_sha: <SHA>; railway_deploy_at: <ISO 8601>` — or describe issues blocking any of the three.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RN client repo → backend repo | Cross-repo: the executor `cd`s into `/Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services` for backend operations; commands using node/npm MUST run under `nvm use 24` per memory `backend-node-version.md` |
| Local shell → Railway production | Railway env-var changes (`railway variables set`) take effect on next deploy; misorder of "set var → redeploy → verify" produces stale config |
| Old credentials → revocation gate | OLD MongoDB password + OLD AWS IAM user keys must be DEAD (verified, not just rotated); a rotation that leaves the OLD credentials still working is incomplete |
| Artifact → committed git history | The artifact is committed to the JayTap RN client repo. Live secrets MUST NEVER appear in the artifact (only metadata: timestamps, presence booleans, key names) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Information Disclosure | Live secrets leaking into committed artifact | mitigate | Acceptance criteria explicitly grep for `MONGO_URI=...mongodb+srv://[^<]` and `AKIA[A-Z0-9]+` patterns in the artifact, asserting 0 matches. Section 2 records env-var KEY names and a `present: true|false` boolean only — no values. Section 3 + 4 record timestamps and revocation evidence (e.g., "Last password change date in MongoDB Atlas dashboard"), not the live values themselves. |
| T-06-16 | Spoofing | Old credentials still working post-"rotation" | mitigate | Acceptance criteria force timestamp evidence (≥ 2026-04-29 for MongoDB; revocation path + timestamp for AWS IAM). The checkpoint:human-action Step 3 prescribes an optional `mongo "...:<old-password>@..." --eval` test that confirms the OLD password authenticates as failed. AWS IAM Step 4 enumerates three acceptable revocation paths (delete user / deactivate keys / revoke policy). |
| T-06-17 | Tampering | FIREBASE_PROJECT_ID missing in Railway → silent 401 storm | mitigate | Step 2 explicitly enumerates `.env.example` keys against the live Railway variables list. If FIREBASE_PROJECT_ID is missing, the plan halts and instructs the executor to `railway variables set FIREBASE_PROJECT_ID=<value>`. Plan 06's QA matrix walks at least one auth-protected endpoint per device (sign in, fetch /api/auth/me) which would fail 401-storm if this env var were missing. |
| T-06-18 | Denial of Service | Railway deploy stuck / hangs | accept | If Railway redeploy fails, the executor surfaces the error to the user; this is a checkpoint flow, not an autonomous loop. |
| T-06-19 | Repudiation | No audit trail of revocation | mitigate | The artifact is committed to git. Each section's timestamps + revocation paths are recorded; a future auditor can cross-reference the dashboard UI history (MongoDB Atlas + AWS IAM Console + Railway Activity log) against the recorded timestamps. |
| T-06-20 | Build-identity drift | Backend running stale code | mitigate | Section 5 records `railway_deploy_sha` and `backend_repo_sha_at_deploy`. Plan 06's QA matrix transitively verifies the live backend behaves per M2 specification (HF-02/03/04 are part of the QA matrix's hotfix-bundle row). |

Block-on severity: high.
- T-06-15 (HIGH) — blocks if mitigation absent (live secret in committed git history)
- T-06-16 (HIGH) — blocks if mitigation absent (false sense of security if rotation incomplete)
- T-06-17 (HIGH) — blocks if mitigation absent (FIREBASE_PROJECT_ID missing means every protected route 401s — the entire app stops working post-deploy)
</threat_model>

<verification>
- Backend `package.json` confirms firebase-admin removed + engines.node `>=22.12.0`
- Railway env vars listed with FIREBASE_PROJECT_ID `present: true`
- MongoDB old-password revocation evidence recorded (timestamp ≥ 2026-04-29)
- AWS IAM old-user revocation evidence recorded (path + timestamp)
- Railway redeploy SHA + timestamp recorded; health-check responds 200 (or designed-401 for auth-required routes)
- Artifact contains NO live secrets (regex assertions return 0 matches)
- File committed to git
</verification>

<success_criteria>
Plan 07 (Wave 4 — store submission) reads from `06-BACKEND-DEPLOY.md` that the backend is live + healthy + with rotated credentials. Plan 06 (Wave 3 — manual QA matrix) reads that the live backend is at the recorded SHA so the QA matrix walks against the correct deployed state. The four hotfix-bundle fixes (HF-01..HF-04) are empirically observable on the live backend via Plan 06's QA matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-manual-physical-device-qa-release/06-04-SUMMARY.md` with: backend deploy SHA, all six section dispositions (PASS/FAIL with one-liner evidence), and the credential-revocation timestamps for the audit trail.
</output>
