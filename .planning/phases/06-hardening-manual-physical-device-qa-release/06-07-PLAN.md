---
phase: 06-hardening-manual-physical-device-qa-release
plan: 07
type: execute
wave: 4
depends_on: [06-02, 06-03, 06-04, 06-06]
files_modified:
  - .planning/phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md
autonomous: false
requirements: [REL-06]
requirements_addressed: [REL-06]
tags: [release, archive, app-store-connect, testflight, play-console, ios-archive, android-aab, store-submission, paste-release-notes, m1-d-12-testflight-internal-sufficient, descope-by-inheritance]

user_setup:
  - service: app-store-connect
    why: "v2.0.0 iOS archive uploads here. TestFlight Internal Testing sufficient for phase-exit per M1 D-12 (no External Testing required)."
    env_vars: []
    dashboard_config:
      - task: "Confirm App Store Connect → JayTap → Versions → 2.0.0 record exists (or create); paste EN + RU release notes from 06-RELEASE-NOTES.md"
        location: "https://appstoreconnect.apple.com → JayTap → App Store / Versions tab"
  - service: google-play-console
    why: "v2.0.0 Android .aab uploads here under existing keystore. Production track release-notes paste under 500-char-per-locale limit."
    env_vars: []
    dashboard_config:
      - task: "Confirm Play Console → JayTap → Production track → Create new release; upload .aab; paste EN + RU release notes (≤ 500 chars per locale per Play Console binding limit)"
        location: "https://play.google.com/console → JayTap → Production"
  - service: xcode
    why: "iOS archive builds via Xcode 26.4+. Same toolchain that shipped v1.0.4 per PROJECT.md row 129."
    env_vars: []
    dashboard_config:
      - task: "Open ios/JayTap.xcworkspace; select 'Any iOS Device' destination; Product → Archive; Distribute App → App Store Connect → Upload"
        location: "Xcode → Organizer → Archives tab"

must_haves:
  truths:
    - "iOS archive built under Xcode 26.4+ (xcodebuild -version | grep '^Xcode 26' exits 0 BEFORE archive starts)"
    - "iOS archive successfully uploaded to App Store Connect (visible in TestFlight Internal Testing within ~10 min)"
    - "Android .aab built under existing keystore (`gradlew bundleRelease` succeeds; .aab file exists at android/app/build/outputs/bundle/release/app-release.aab)"
    - "Android .aab successfully uploaded to Google Play Console (visible in Production track or Internal Testing track per user choice)"
    - "EN + RU release notes from 06-RELEASE-NOTES.md pasted in BOTH stores' release-notes fields, under their respective binding length limits"
    - "06-SUBMISSION-LOG.md records: ASC build number, ASC processing status, Play Console versionCode, Play Console processing status, paste timestamps for both locales × both stores"
    - "Privacy + store-listing inheritance honored per 06-INHERITANCE-AUDIT.md `proceed_with_inheritance: true` — no privacy questionnaire re-authoring, no store-listing copy refresh, no screenshot updates"
    - "06-QA-MATRIX.md walk_disposition is APPROVED before archive starts (binding gate from Plan 06)"
  artifacts:
    - path: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md"
      provides: "REL-06 — submission audit trail; binding evidence that v2.0.0 reached TestFlight + Play Console processing/internal-testing"
      contains: "ASC build"
      contains_also: "Play Console versionCode"
  key_links:
    - from: "Plan 06-05 commit SHA (v2.0.0 atomic bump)"
      to: "ASC TestFlight + Play Console binaries"
      via: "iOS archive + Android .aab built from this exact SHA; SHA recorded in 06-SUBMISSION-LOG.md"
      pattern: "submitted_against_sha:"
    - from: "06-RELEASE-NOTES.md ## EN + ## RU bodies"
      to: "ASC + Play Console release-notes fields × 2 locales"
      via: "User pastes via the awk slice commands documented in 06-RELEASE-NOTES.md"
      pattern: "paste_at_<store>_<locale>:"
---

<objective>
Wave 4 of Phase 6 — final phase-exit plan. Build the iOS archive + Android .aab from Plan 05's atomic v2.0.0 commit; upload to App Store Connect (TestFlight Internal Testing track sufficient per M1 D-12) and Google Play Console (Production OR Internal Testing per user choice); paste EN + RU release notes from `06-RELEASE-NOTES.md` into both stores under the binding length limits.

This plan reads four upstream artifacts as binding gates:
1. `06-INHERITANCE-AUDIT.md` `proceed_with_inheritance: true` — green-light to skip privacy + store-listing re-authoring (M1 D-13 inheritance descope)
2. `06-RELEASE-NOTES.md` — EN + RU bodies ready to paste
3. `06-BACKEND-DEPLOY.md` — backend live + healthy at the recorded deploy SHA
4. `06-QA-MATRIX.md` `walk_disposition: APPROVED` — the v2.0.0 binary works end-to-end

If any of those four gates is not satisfied, this plan HALTS and surfaces to the user. No archive build happens until all four upstream artifacts are in their accept state.

This plan is `autonomous: false` — Xcode Archive + ASC web UI upload + Play Console web UI upload + release-notes paste are dashboard / GUI actions that the executor cannot fully drive via CLI. The plan has two checkpoint:human-action tasks (one for iOS, one for Android), and final-state recording is the executor's job.

Output: `06-SUBMISSION-LOG.md` recording build numbers, upload timestamps, paste timestamps; v2.0.0 visible in TestFlight Internal Testing + Play Console release track; phase ready to close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/06-hardening-manual-physical-device-qa-release/06-INHERITANCE-AUDIT.md
@.planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md
@.planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md
@.planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md
@.planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md
@.planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-05-PLAN.md

<interfaces>
<!-- Build identity at submission time (read from upstream artifacts at execution time): -->
<!-- - iOS: MARKETING_VERSION 2.0.0 + CURRENT_PROJECT_VERSION <next_ios_build_number from 06-STORE-HISTORY.md> -->
<!-- - Android: versionName "2.0.0" + versionCode <next_android_version_code from 06-STORE-HISTORY.md> -->
<!-- - Repo SHA: Plan 05's atomic-bump commit SHA -->
<!-- - Backend SHA: 06-BACKEND-DEPLOY.md railway_deploy_sha -->

iOS archive flow (Xcode 26.4+ per PROJECT.md row 129 — Xcode 26.4 / build 17E192 confirmed):
1. Open `ios/JayTap.xcworkspace`
2. Select destination "Any iOS Device (arm64)"
3. Product → Archive
4. Wait for archive to complete (Organizer opens automatically)
5. Distribute App → App Store Connect → Upload (with automatic signing)
6. Wait for upload + processing in ASC (~5-10 min)
7. ASC → JayTap → TestFlight tab → Internal Testing → confirm build appears with the expected build number

Android archive flow (existing keystore — REUSE; no new keystore generation):
1. `cd android && ./gradlew bundleRelease`
2. Output: `android/app/build/outputs/bundle/release/app-release.aab`
3. Play Console → JayTap → Production (or Internal Testing) → Create new release → Upload .aab
4. Paste release notes (per locale) → Save → Submit for review (or roll out to internal testers)

Release notes paste (read EN + RU bodies via documented awk slices):
```bash
# EN body
awk '/^## EN$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}'

# RU body
awk '/^## RU$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}'
```

These produce the exact text to paste (without `## EN` / `## RU` headings or `---` separators).

Per M1 D-12 (RETROSPECTIVE.md + 08-CONTEXT.md): TestFlight Internal Testing pass is sufficient for phase-exit. NOT required: External TestFlight, App Store production review submission, Play Console production rollout. The user MAY pursue any of these post-phase-exit; this plan is satisfied at "v2.0.0 visible in TestFlight Internal + Play Console release track."

Per M1 D-13 (06-INHERITANCE-AUDIT.md `proceed_with_inheritance: true` — Plan 02's binding signal):
- DO NOT re-author iOS PrivacyInfo.xcprivacy
- DO NOT re-submit ASC App Privacy questionnaire
- DO NOT re-submit Play Console Data Safety form
- DO NOT update store listing copy / screenshots / app icon / category
- DO NOT update Maps API key restrictions
- DO NOT update applinks entitlement

If the audit's `proceed_with_inheritance` is `false`, this plan HALTS and surfaces the flagged_red_items list to the user.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: PREREQUISITE GATE — verify all four upstream artifacts in their accept state</name>
  <files>(read-only — no file modification in this task)</files>
  <read_first>
    - .planning/phases/06-hardening-manual-physical-device-qa-release/06-INHERITANCE-AUDIT.md (read `proceed_with_inheritance` boolean — MUST be `true`)
    - .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md (read frontmatter — confirm both bodies measured ≤ 500 chars per `wc -m`)
    - .planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md (read `railway_deploy_sha` + `health_check_status` — backend live)
    - .planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md (read `walk_disposition` — MUST be `APPROVED`)
    - .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md (read `derived_targets` — confirm Plan 05's bumped values match)
    - package.json + ios/JayTap.xcodeproj/project.pbxproj + android/app/build.gradle (sample current state — confirm Plan 05's atomic bump landed; values match 06-STORE-HISTORY.md derived targets)
  </read_first>
  <action>
    Five-step gate check:

    1. **06-INHERITANCE-AUDIT.md gate:**
       ```bash
       grep -E '^\s*proceed_with_inheritance:\s*true\s*$' .planning/phases/06-hardening-manual-physical-device-qa-release/06-INHERITANCE-AUDIT.md
       ```
       MUST exit 0. If `false`, surface the artifact's `flagged_red_items` list to the user and HALT.

    2. **06-RELEASE-NOTES.md gate:**
       ```bash
       EN_LEN=$(awk '/^## EN$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m | tr -d ' ')
       RU_LEN=$(awk '/^## RU$/{flag=1; next} /^---$/{if(flag){flag=0}} flag' .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md | sed '1{/^$/d;}' | sed '${/^$/d;}' | wc -m | tr -d ' ')
       [ "$EN_LEN" -le 500 ] && [ "$RU_LEN" -le 500 ]
       ```
       MUST be true. If either body exceeds 500, HALT and instruct user to re-trim the release notes file before proceeding.

    3. **06-BACKEND-DEPLOY.md gate:**
       ```bash
       grep -E 'health_check_status:\s*200' .planning/phases/06-hardening-manual-physical-device-qa-release/06-BACKEND-DEPLOY.md
       ```
       MUST exit 0 (or accept 401 if root route requires auth — case-by-case). If backend is not healthy, HALT.

    4. **06-QA-MATRIX.md gate:**
       ```bash
       grep -E '^\s*walk_disposition:\s*APPROVED\s*$' .planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md
       ```
       MUST exit 0. If `walk_disposition: pending` or `BLOCKED-BY-FAIL`, HALT.

    5. **Build identity match gate:**
       ```bash
       NEXT_IOS=$(grep -E '^\s*next_ios_build_number:\s*[0-9]+' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md | grep -oE '[0-9]+')
       NEXT_AND=$(grep -E '^\s*next_android_version_code:\s*[0-9]+' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md | grep -oE '[0-9]+')
       grep -c '"version": "2.0.0"' package.json   # MUST equal 1
       grep -c "MARKETING_VERSION = 2.0.0" ios/JayTap.xcodeproj/project.pbxproj   # MUST equal 2
       grep -c "CURRENT_PROJECT_VERSION = $NEXT_IOS" ios/JayTap.xcodeproj/project.pbxproj   # MUST equal 2
       grep -c 'versionName "2.0.0"' android/app/build.gradle   # MUST equal 1
       grep -c "versionCode $NEXT_AND" android/app/build.gradle   # MUST equal 1
       ```
       All MUST hold. If not, Plan 05 didn't land cleanly — HALT and re-run Plan 05.

    If all five gates pass, proceed to Task 1. If ANY gate fails, surface the failure to the user and HALT.
  </action>
  <verify>
    <automated>grep -E '^\s*proceed_with_inheritance:\s*true' .planning/phases/06-hardening-manual-physical-device-qa-release/06-INHERITANCE-AUDIT.md && grep -E '^\s*walk_disposition:\s*APPROVED' .planning/phases/06-hardening-manual-physical-device-qa-release/06-QA-MATRIX.md && [ "$(grep -c '"version": "2.0.0"' package.json)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `proceed_with_inheritance: true` in 06-INHERITANCE-AUDIT.md
    - EN + RU bodies in 06-RELEASE-NOTES.md both ≤ 500 chars per `wc -m`
    - 06-BACKEND-DEPLOY.md `health_check_status: 200` (or designed-401)
    - 06-QA-MATRIX.md `walk_disposition: APPROVED`
    - All five build-identity grep counts match 06-STORE-HISTORY.md derived targets
  </acceptance_criteria>
  <done>
    All four upstream artifacts confirmed in their accept state; build identity confirmed; cleared to proceed to Task 1 (iOS archive).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Build iOS archive in Xcode 26.4+ and upload to App Store Connect (TestFlight Internal)</name>
  <what-built>
    Task 0 confirmed all upstream gates. The v2.0.0 atomic bump from Plan 05 is in place. This task pauses for the user to drive Xcode + ASC web UI to produce + upload the iOS archive.
  </what-built>
  <how-to-verify>
    1. **Confirm Xcode toolchain version:**
       ```bash
       xcodebuild -version | grep '^Xcode 26'
       ```
       MUST exit 0 (Xcode 26.4+ per PROJECT.md row 129). If not, install/select Xcode 26.4+.

    2. **Open the workspace + clean build folder:**
       - Open `/Users/beckmaldinVL/development/mobileApps/JayTap/ios/JayTap.xcworkspace` in Xcode
       - Product → Clean Build Folder (Cmd+Shift+K) — removes any cached v1.0.4 artifacts
       - Confirm scheme "JayTap" is selected and destination is "Any iOS Device (arm64)" (NOT a simulator)

    3. **Archive:**
       - Product → Archive
       - Wait for build to complete (~3-7 min)
       - Organizer opens automatically when archive succeeds

    4. **Verify archive identity:**
       - In Organizer, click the new archive (top of list); verify:
         - "Version" reads `2.0.0`
         - "Build" reads the integer `<next_ios_build_number>` from 06-STORE-HISTORY.md
       - If either is wrong, Plan 05 didn't land — abort and re-run Plan 05.

    5. **Upload to App Store Connect:**
       - Click "Distribute App" → "App Store Connect" → "Upload"
       - Use automatic signing (the existing distribution certificate + provisioning profile from v1.0.4 ship — REUSE per M1 D-13 inheritance)
       - Wait for upload (~3-5 min) + processing on ASC side (~5-15 min)

    6. **Confirm visibility in TestFlight Internal Testing:**
       - Open https://appstoreconnect.apple.com → JayTap → TestFlight tab
       - Internal Testing group → confirm the new build appears with version `2.0.0` + build `<next_ios_build_number>` + status "Ready to Test" (or "Processing" if still in flight)
       - Per M1 D-12: TestFlight Internal is sufficient for phase-exit. External TestFlight + production review are post-phase actions.

    7. **Paste EN + RU release notes:**
       - Run the awk slice command for EN body, copy the output, paste into ASC → JayTap → 2.0.0 record → "What's New in This Version" (default English (US) localization)
       - Add Russian (Russia) localization if not present; paste RU body
       - Save

    8. **Report back to executor:** ASC build number visible in TestFlight; ASC processing status; release-notes pasted in both locales; timestamps for each step.
  </how-to-verify>
  <resume-signal>
    Reply with: `ios_archive_uploaded: <build-number>; asc_status: <Ready to Test | Processing>; en_pasted_at: <ISO 8601>; ru_pasted_at: <ISO 8601>` — or describe blocking issues (Xcode error, ASC reject, signing failure).
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Build Android .aab via gradlew + upload to Google Play Console + paste EN + RU release notes</name>
  <what-built>
    Task 1 landed iOS in TestFlight Internal. This task does the parallel Android workflow: build .aab, upload to Play Console, paste release notes.
  </what-built>
  <how-to-verify>
    1. **Build .aab from the v2.0.0 commit:**
       ```bash
       cd /Users/beckmaldinVL/development/mobileApps/JayTap/android
       ./gradlew clean
       ./gradlew bundleRelease
       ```
       Output: `android/app/build/outputs/bundle/release/app-release.aab`
       Confirm file exists: `ls -lh android/app/build/outputs/bundle/release/app-release.aab`

    2. **Verify .aab identity** (optional, if `bundletool` is installed):
       ```bash
       bundletool dump manifest --bundle=android/app/build/outputs/bundle/release/app-release.aab | grep -E 'versionCode|versionName'
       ```
       MUST show `versionCode <next_android_version_code>` and `versionName "2.0.0"`.

    3. **Upload to Play Console:**
       - Open https://play.google.com/console → JayTap → "Production" track (or "Internal Testing" if user prefers a softer rollout)
       - "Create new release" (or "Promote release" if continuing a release-in-progress)
       - Upload `app-release.aab`
       - Play Console verifies signing (existing keystore from v1.0.4 — REUSE per M1 D-13) + versionCode strictly greater than last accepted
       - If Play Console rejects with "versionCode <N> already accepted", run Plan 06-01 again with a refreshed `next_android_version_code` (max accepted on rejection notice + 1) and re-run Plan 06-05's Android edits, then retry this step.

    4. **Paste EN + RU release notes:**
       - In the release page → "Release notes" section → "Edit release notes per language"
       - For `en-US`: paste the EN body (from `awk '/^## EN$/...'` output)
       - For `ru-RU`: paste the RU body
       - Confirm both bodies fit Play Console's 500-char-per-locale limit (Play Console will reject with error if exceeded; the awk-slice + wc -m gate in Plan 06-03 already verified).

    5. **Submit for review (Production) OR roll out to Internal Testers (Internal Testing track):**
       - Production: "Save" → "Review release" → "Start rollout to Production" → choose 100% or staged
       - Internal Testing: "Save" → "Save and review" → "Start rollout to Internal Testing"
       - Per M1 D-12: Either path is sufficient for phase-exit. The user picks.

    6. **Confirm visibility:**
       - Production track: Release dashboard shows "In review" or "Rolling out" with versionCode `<next_android_version_code>`
       - Internal Testing track: Release dashboard shows "Available to testers"

    7. **Report back to executor:** Play Console versionCode visible; Play Console status; release-notes pasted in both locales; track choice (Production vs Internal Testing); timestamps.
  </how-to-verify>
  <resume-signal>
    Reply with: `android_aab_uploaded: <versionCode>; play_console_status: <In review | Rolling out | Available to testers>; track_chosen: <Production | Internal Testing>; en_pasted_at: <ISO 8601>; ru_pasted_at: <ISO 8601>` — or describe blocking issues (gradlew build failure, Play Console reject, signing mismatch).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author 06-SUBMISSION-LOG.md with full submission audit trail</name>
  <files>.planning/phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md</files>
  <read_first>
    - (the user's resume-signal outputs from Tasks 1 + 2 are in chat; the executor compiles them into the log)
    - .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md (re-anchor derived targets for the audit)
    - .planning/phases/06-hardening-manual-physical-device-qa-release/06-INHERITANCE-AUDIT.md (re-anchor inheritance dispositions)
    - .planning/phases/06-hardening-manual-physical-device-qa-release/06-RELEASE-NOTES.md (re-anchor character counts)
  </read_first>
  <action>
    Author `06-SUBMISSION-LOG.md` with the user's reported submission outcomes from Tasks 1 + 2:

    ```markdown
    ---
    phase: 06-hardening-manual-physical-device-qa-release
    plan: 07
    type: submission-log
    submitted_at: <ISO 8601 timestamp at log creation>
    submitted_against_sha: <Plan 05 commit SHA>
    backend_against_sha: <Plan 04 06-BACKEND-DEPLOY.md railway_deploy_sha>
    target_version: 2.0.0
    ios:
      build_number: <integer; from Task 1 resume signal>
      asc_status: <Task 1 resume signal>
      en_pasted_at: <Task 1 timestamp>
      ru_pasted_at: <Task 1 timestamp>
      track: TestFlight Internal Testing
    android:
      version_code: <integer; from Task 2 resume signal>
      play_console_status: <Task 2 resume signal>
      en_pasted_at: <Task 2 timestamp>
      ru_pasted_at: <Task 2 timestamp>
      track: <Production | Internal Testing>
    inheritance_descope:
      ios_privacy_info_xcprivacy: INHERITED
      asc_app_privacy: INHERITED
      play_data_safety: INHERITED
      store_listing_copy: INHERITED
      screenshots_icon_category: INHERITED
      maps_api_key: INHERITED
      applinks: INHERITED
    rationale: M1 D-13 inheritance descope per 06-INHERITANCE-AUDIT.md `proceed_with_inheritance: true`
    ---

    # Phase 6 — v2.0.0 Submission Log

    ## iOS submission

    | Field | Value |
    |-------|-------|
    | Build number | <N> |
    | TestFlight status | <Ready to Test \| Processing \| ...> |
    | EN release notes pasted | <ISO 8601> |
    | RU release notes pasted | <ISO 8601> |
    | TestFlight URL | https://appstoreconnect.apple.com/apps/.../TestFlight/iOS |

    ## Android submission

    | Field | Value |
    |-------|-------|
    | versionCode | <N> |
    | Play Console status | <In review \| Rolling out \| Available to testers> |
    | Track | <Production \| Internal Testing> |
    | EN release notes pasted | <ISO 8601> |
    | RU release notes pasted | <ISO 8601> |

    ## Build identity

    | Surface | Value |
    |---------|-------|
    | Repo SHA | <Plan 05 commit SHA> |
    | iOS MARKETING_VERSION | 2.0.0 |
    | iOS CURRENT_PROJECT_VERSION | <N> |
    | Android versionName | "2.0.0" |
    | Android versionCode | <N> |
    | Backend deploy SHA | <Plan 04 SHA> |

    ## Inheritance descope (M1 D-13 / 06-INHERITANCE-AUDIT.md)

    All seven items inherited from v1.0.4 production state per the audit:
    - iOS PrivacyInfo.xcprivacy
    - ASC App Privacy questionnaire
    - Play Console Data Safety
    - Store listing copy + screenshots + app icon + category
    - Maps API key restrictions
    - applinks entitlement

    Re-open conditions logged in 06-INHERITANCE-AUDIT.md.

    ## Phase-exit disposition

    Per M1 D-12: TestFlight Internal Testing visibility + Play Console release-track upload is sufficient for phase-exit. The user MAY pursue ASC production review submission or Play Console production rollout post-phase-exit; this is the binding milestone for Phase 6 close.

    ## Re-open conditions

    - If ASC processing fails or rejects the build → re-run Plan 06-01 (refresh build-number target) + Plan 06-05 (re-bump iOS only) + Task 1 of this plan.
    - If Play Console rejects versionCode → re-run Plan 06-01 (refresh versionCode target) + Plan 06-05 (re-bump Android only) + Task 2 of this plan.
    - If Apple flags new privacy data type at review → re-open REL-03/REL-06 per M1 D-13 re-open conditions in 06-INHERITANCE-AUDIT.md.
    ```

    Use the `Write` tool. After write, run the assertions in `<acceptance_criteria>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md && grep -c 'TestFlight Internal' .planning/phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md && grep -c 'Play Console' .planning/phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md && grep -c 'inheritance_descope' .planning/phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-hardening-manual-physical-device-qa-release/06-SUBMISSION-LOG.md` exits 0
    - `grep -c 'TestFlight Internal' <file>` returns >= 1 (iOS track recorded)
    - `grep -c 'Play Console' <file>` returns >= 1 (Android track recorded)
    - `grep -c 'inheritance_descope' <file>` returns >= 1 (M1 D-13 inheritance recorded)
    - frontmatter `submitted_against_sha:` is populated with Plan 05 commit SHA
    - frontmatter `backend_against_sha:` is populated with Plan 04 deploy SHA
    - frontmatter `ios.build_number:` is an integer matching 06-STORE-HISTORY.md derived target
    - frontmatter `android.version_code:` is an integer matching 06-STORE-HISTORY.md derived target
    - All seven inheritance items show `INHERITED` (per 06-INHERITANCE-AUDIT.md `proceed_with_inheritance: true`)
    - `grep -E '(MONGO_URI|AWS_SECRET_ACCESS_KEY)=' <file>` returns 0 lines (no live secrets in the audit log)
  </acceptance_criteria>
  <done>
    `06-SUBMISSION-LOG.md` exists with full submission audit trail. iOS visible in TestFlight Internal Testing; Android visible in Play Console release track. Phase 6 ready to close via the metadata commit (separate operation: flip ROADMAP/REQUIREMENTS checkboxes, advance STATE.md per M1 Key Lesson #4).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 05 binary -> archived binary | The iOS archive + Android .aab are built from Plan 05's commit SHA. Building from a stale checkout (different SHA) would submit unverified code; the build-identity match gate in Task 0 prevents this. |
| Local archive -> ASC / Play Console | Once uploaded, the binary is out of the executor's control. Apple's Identity Services + Play Console signing verify the upload's signature against the existing certificates/keystores. |
| 06-INHERITANCE-AUDIT.md -> privacy descope | Plan 07 reads the audit's binding boolean and skips re-authoring. If the audit was tampered with (false-flagged inheritance when a new data-collecting dep landed), Apple's automated privacy scan would catch the discrepancy; the inheritance disposition is committed to git for audit. |
| Release notes -> store paste | The user copies EN + RU bodies via documented awk commands; pasting into ASC + Play Console is a manual step. The Plan 06-03 wc -m gate already verified ≤ 500 chars per locale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-34 | Build-identity drift | Archive built from non-Plan-05 SHA | mitigate | Task 0 Step 5 grep-asserts the local source files match 06-STORE-HISTORY.md derived targets. Task 1 Step 4 cross-references the Xcode Organizer's "Version" + "Build" against the expected values; if mismatched, abort. |
| T-06-35 | Spoofing | Wrong distribution certificate / wrong keystore signs the binary | accept | Apple's automatic-signing flow uses the team's existing distribution cert (REUSE per M1 D-13). Play Console verifies the .aab signature against the registered upload key; if wrong, the upload is rejected and the user must resolve before retry. Apple/Google's signing-verification is the binding control. |
| T-06-36 | Privacy/store-listing tampering | Re-authored privacy answers when audit said INHERIT | mitigate | Task 0 Step 1 reads `proceed_with_inheritance: true` as a binding signal. Tasks 1 + 2 explicitly DO NOT touch privacy questionnaire / Data Safety / store listing assets — the user is instructed only to paste release notes. The 06-INHERITANCE-AUDIT.md re_open_conditions document what events would invalidate INHERIT in the future. |
| T-06-37 | Information Disclosure | Live credentials / signing secrets leak into committed audit log | mitigate | Acceptance criterion `grep -E '(MONGO_URI|AWS_SECRET_ACCESS_KEY)=' <file>` returns 0 lines. The submission log records build numbers, status strings, timestamps, and track choices — no credential material. |
| T-06-38 | DoS | ASC / Play Console processing hang or reject | accept | Both stores publish status pages; processing typically completes within 5-15 min. If ASC processes for >2 hours without a status flip, the user investigates via App Store Connect support or via the binary's Activity tab. The plan's resume-signal pattern lets the user surface blocking failures. |
| T-06-39 | Repudiation | No record of who submitted | mitigate | The submission log records timestamps + the human walker (per Task 1+2 resume signals). The committed git history shows when the log landed. |
| T-06-40 | Tampering | Release-notes drift between draft and paste | mitigate | The user uses the documented `awk` slice commands to extract EN + RU bodies; this is a deterministic byte-for-byte extraction. If the user manually retyped or trimmed the body at paste time, the log records the paste timestamps but trusts the user; this is the same flow as M1 Phase 8 Plan 08-05 and was clean. |

Block-on severity: high.
- T-06-34 (HIGH) — blocks if mitigation absent (submitting unverified code)
- T-06-36 (HIGH) — blocks if mitigation absent (privacy declaration drift -> store rejection)
</threat_model>

<verification>
- All four upstream gates (Task 0) confirmed in accept state before archive starts
- Xcode 26.4+ confirmed via `xcodebuild -version`
- iOS archive uploaded; visible in TestFlight Internal Testing
- Android .aab uploaded; visible in Play Console release track
- EN + RU release notes pasted into both stores' release-notes fields
- 06-SUBMISSION-LOG.md committed with full audit trail
- All seven inheritance items recorded as INHERITED
- No live secrets in the log
</verification>

<success_criteria>
v2.0.0 visible in App Store Connect TestFlight Internal Testing AND in Google Play Console release track (Production OR Internal Testing per user choice). Bilingual EN+RU release notes pasted on both stores under their respective length limits. M1 D-13 privacy + store-listing inheritance honored — no questionnaire re-authoring. Phase 6 closes; M2 v2.0 milestone ships.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-manual-physical-device-qa-release/06-07-SUMMARY.md` with: iOS build number + ASC status, Android versionCode + Play Console status + track, EN/RU paste timestamps for both stores, inheritance descope confirmation (all 7 items INHERITED), upstream artifact gate-pass evidence (4 artifacts in accept state), and the close-out commit SHA.

Phase 6 close-out metadata commit (per M1 Key Lesson #4 — bookkeeping owed at phase close, not deferred to milestone close): the SAME commit that lands 06-07-SUMMARY.md should:
- Flip `[ ] Phase 6` to `[x] Phase 6` in `.planning/ROADMAP.md` (line 32 + the table at line 173)
- Flip `[ ]` to `[x]` for REL-01..REL-06 in `.planning/REQUIREMENTS.md` (lines 85-90)
- Update `.planning/STATE.md` "Last updated" timestamp + Phase 6 entry to status `closed`
- Update `.planning/PROJECT.md` "Active" -> "Validated" for REL-01..REL-06 with `v2.0.0 (Phase 6)` annotations matching the M1 v1.0.4 pattern (lines 85-88)
</output>
