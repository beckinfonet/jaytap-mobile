---
phase: 06-hardening-manual-physical-device-qa-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md
autonomous: true
requirements: [REL-02]
requirements_addressed: [REL-02]
tags: [release, store-history, m1-d-02-lesson, version-baseline, query-first, testflight, play-console]

must_haves:
  truths:
    - "06-STORE-HISTORY.md exists in the phase directory and records the highest-accepted iOS CURRENT_PROJECT_VERSION (build number) per ASC track at query time, with a timestamp"
    - "06-STORE-HISTORY.md records the highest-accepted Android versionCode in Google Play Console (production track + closed/internal tracks), with a timestamp"
    - "06-STORE-HISTORY.md derives the next-bump targets per M1 D-02 lesson: iOS CURRENT_PROJECT_VERSION = max-accepted + 1; Android versionCode = max-accepted + 1"
    - "Local source files (ios/JayTap.xcodeproj/project.pbxproj line 274/308 + android/app/build.gradle line 91) are sampled into the artifact for an explicit local-vs-store delta — local is documented as untrusted per M1 Key Lesson #1"
    - "If the queried-store maximum exceeds the local source value, the artifact MUST flag this as 'local-stale' and Plan 05 MUST adopt the queried-max+1 baseline (NOT local+1)"
    - "Phase 3+4 paired-gates prerequisite is documented inline as a hard prerequisite that the user MUST satisfy BEFORE Plan 06 (manual QA matrix) walks"
  artifacts:
    - path: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md"
      provides: "REL-02 — derived iOS build-number + Android versionCode baselines for Plan 05 atomic version bump; M1 D-02 lesson encoded as a Wave-1 query-first task"
      contains: "next_ios_build_number"
      contains_also: "next_android_version_code"
  key_links:
    - from: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md :: next_ios_build_number"
      to: "Plan 06-05 :: ios/JayTap.xcodeproj/project.pbxproj :: CURRENT_PROJECT_VERSION"
      via: "Plan 05 reads next_ios_build_number from this artifact and uses it as the bump target"
      pattern: "CURRENT_PROJECT_VERSION = [0-9]+"
    - from: ".planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md :: next_android_version_code"
      to: "Plan 06-05 :: android/app/build.gradle :: versionCode"
      via: "Plan 05 reads next_android_version_code from this artifact and uses it as the bump target"
      pattern: "versionCode [0-9]+"
---

<objective>
Wave 1 of Phase 6 — query the live store history for highest-accepted iOS build number (TestFlight) and Android versionCode (Play Console) BEFORE Plan 05's atomic v2.0.0 version bump runs.

This plan exists ONLY because of the M1 D-02 lesson recorded in `.planning/RETROSPECTIVE.md` "What Was Inefficient" + auto-memory `release-android-versioncode-trust-play-console.md`: in M1 Phase 8, the planner trusted the local `android/app/build.gradle` baseline (versionCode 25) and Play Console rejected it because versionCode 25 had already been accepted from a prior submission. The user authored an out-of-band reactive bump (versionCode 25 → 28; iOS CURRENT_PROJECT_VERSION 21 → 22) at archive time. M2 Phase 6 must NOT repeat that error.

Per the orchestrator's additional_context: last-accepted iOS in TestFlight = build 22; last-accepted Android in Play Console = versionCode 28. Local source files currently read iOS CURRENT_PROJECT_VERSION = 26 (ahead of TestFlight's 22) and Android versionCode = 29 (ahead of Play Console's 28). Local is **ahead** of store on both tracks today, so the next bump targets read from a max(local, store) + 1 calculation per track, and the artifact must record the rationale.

Purpose: produce a single Wave-1 artifact (`06-STORE-HISTORY.md`) that Plan 05 reads as the binding source for build-number baselines, NOT the local source files. Encodes M1 Key Lesson #1 as a planning artifact.

Output: `06-STORE-HISTORY.md` with three sections — (1) ASC TestFlight history snapshot, (2) Play Console history snapshot, (3) derived next-bump targets per track + rationale + Phase 3+4 paired-gates prerequisite block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RETROSPECTIVE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- Current local-source values (sampled 2026-05-05). Plan 01 does NOT modify these — -->
<!-- it ONLY queries the live stores, snapshots the answer, and derives the next bump -->
<!-- target. Plan 05 (Wave 2) does the actual edit. -->

`ios/JayTap.xcodeproj/project.pbxproj` (sampled at HEAD on 2026-05-05):
- Line 274 (Debug build configuration): `				CURRENT_PROJECT_VERSION = 26;`
- Line 308 (Release build configuration): `				CURRENT_PROJECT_VERSION = 26;`
- Line 283 (Debug build configuration): `				MARKETING_VERSION = 1.0.4;`
- Line 316 (Release build configuration): `				MARKETING_VERSION = 1.0.4;`

`android/app/build.gradle` (sampled at HEAD on 2026-05-05):
- Line 91: `        versionCode 29`
- Line 92: `        versionName "1.0.29"`

`package.json` line 3 (sampled at HEAD on 2026-05-05):
- `  "version": "1.0.4",`

These local values are the M1 D-02 anti-pattern. Plan 01 treats them as untrusted; the binding values for Plan 05 come from the live stores, sampled by this plan.

Per orchestrator additional_context (the user's directly-known store state):
- ASC TestFlight: highest accepted iOS build for v1.0.4 = `22` (from M1 close, archive-time bump)
- Play Console: highest accepted Android versionCode = `28` (from M1 close, archive-time bump per memory `release-android-versioncode-trust-play-console.md`)

The executor MUST re-confirm both numbers against the live stores at execution time (the user's known state may have changed since 2026-04-30); the orchestrator-supplied values are the working hypothesis to verify, not the answer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query ASC + Play Console for live highest-accepted version baselines and derive next-bump targets</name>
  <files>.planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md</files>
  <read_first>
    - .planning/RETROSPECTIVE.md (re-anchor M1 D-02 lesson + Key Lesson #1 — "Trust Play Console (and TestFlight) for live-version state, not local build files")
    - .planning/STATE.md (sample the current "Phase pipeline" entry for Phase 6 — confirm orchestrator-supplied store-history hypothesis matches current STATE notes)
    - ios/JayTap.xcodeproj/project.pbxproj line range 270-320 (read this slice — confirm lines 274/308 are CURRENT_PROJECT_VERSION = 26 and lines 283/316 are MARKETING_VERSION = 1.0.4 — record the as-sampled state in the artifact)
    - android/app/build.gradle line range 80-100 (read this slice — confirm lines 91/92 are versionCode 29 / versionName "1.0.29" — record the as-sampled state in the artifact)
    - .planning/milestones/v1.0.4-phases/08-release-and-store-submission/08-01-PLAN.md (read for the M1 verify-only Wave-0 pattern this plan generalizes — Wave-0 was VERIFY-ONLY in M1; M2 Wave-1 is QUERY-LIVE because the M1 lesson taught us that)
    - $HOME/.claude/projects/-Users-beckmaldinVL-development-mobileApps-JayTap/memory/release-android-versioncode-trust-play-console.md (re-anchor the auto-memory entry that codifies "trust Play Console, not local build.gradle")
  </read_first>
  <action>
    Two-step query + one-step artifact authoring. NO source-file mutation.

    **Step 1 — Query Apple App Store Connect (ASC) for highest-accepted iOS build number for bundle ID `com.movein` under MARKETING_VERSION 1.0.4 (the live shipped version) AND under MARKETING_VERSION 2.0.0 (the v2.0.0 target, in case any prior 2.0.0 archive was uploaded out-of-band):**

    The user has the ASC web UI logged-in session and `xcrun altool`/`xcrun notarytool` available. Two equivalent paths the executor may pick:

    Path A — ASC API via `xcrun` (preferred if API key is on disk; check `~/.private_keys/AuthKey_*.p8`):
    ```bash
    # The exact xcrun command shape depends on what's installed; if AuthKey exists:
    xcrun altool --list-builds -u $APPLE_ID -p '@keychain:AC_PASSWORD' --output-format json 2>/dev/null | head -200
    # OR:
    xcrun notarytool history --apple-id $APPLE_ID --keychain-profile AC_PASSWORD 2>/dev/null
    ```
    Path B — checkpoint-into-user (fallback if no API key on disk): pause and ask the user to read the ASC web UI at https://appstoreconnect.apple.com/apps → JayTap → TestFlight tab → "Internal Testing" group → list of builds; report the highest build-number value seen across all versions.

    The executor SHOULD attempt Path A first; if it fails (no API key, expired credentials, etc.), record the failure in the artifact and pivot to Path B with a `checkpoint:decision` task NOT prepared in this plan — instead, use the orchestrator-supplied hypothesis (TestFlight highest accepted = build 22) and ANNOTATE the artifact with `query_method: orchestrator_hypothesis` so Plan 05's executor knows the value is unverified-against-live and may need to retry the upload if ASC rejects it.

    **Step 2 — Query Google Play Console for highest-accepted Android versionCode for `com.movein` across ALL tracks (production + open-testing + closed-testing + internal-testing):**

    The Google Play Console has a CLI/API path via `gcloud` or via `bundletool` only in limited shape; the user-friendly path is the web UI.

    Path A — `gcloud` (preferred if `gcloud` is configured for Play Console publishing API):
    ```bash
    gcloud auth list 2>/dev/null
    # If a Play Console service account is logged in:
    # gcloud beta android publishing edits create --package com.movein --output-format json
    # (Read-only listing of bundles is awkward through gcloud — bundletool/curl-against-the-Play-Developer-API path may be easier)
    ```
    Path B — checkpoint-into-user (fallback): pause and ask the user to read the Play Console web UI at https://play.google.com/console → JayTap → "App bundle explorer" or "Production" track → list of releases; report the highest versionCode value seen across all tracks.

    Same fallback semantics as Step 1: if Path A fails, use the orchestrator-supplied hypothesis (Play Console highest accepted versionCode = 28) and ANNOTATE the artifact with `query_method: orchestrator_hypothesis`.

    **Step 3 — Author `06-STORE-HISTORY.md` with this exact structure (use the Write tool, not heredoc):**

    ```markdown
    ---
    phase: 06-hardening-manual-physical-device-qa-release
    plan: 01
    type: store-history
    queried_at: <ISO 8601 timestamp at query time>
    queried_against_sha: <git rev-parse HEAD output>

    ios:
      query_method: <"asc_api_direct" | "asc_web_ui_user_relay" | "orchestrator_hypothesis">
      asc_track: TestFlight Internal + (if any prior 2.0.0 build) prod
      highest_accepted_marketing_version: <e.g., 1.0.4>
      highest_accepted_build_number: <integer; from M1 close hypothesis = 22 unless re-queried>
      local_source_marketing_version: 1.0.4
      local_source_build_number: 26
      local_vs_store_delta: <"local-ahead" | "local-equal" | "local-stale">

    android:
      query_method: <"gcloud_play_publishing_api" | "play_console_web_ui_user_relay" | "orchestrator_hypothesis">
      tracks_inspected: [production, open-testing, closed-testing, internal-testing]
      highest_accepted_version_name: <e.g., 1.0.29>
      highest_accepted_version_code: <integer; from M1 close hypothesis = 28 unless re-queried>
      local_source_version_name: "1.0.29"
      local_source_version_code: 29
      local_vs_store_delta: <"local-ahead" | "local-equal" | "local-stale">

    derived_targets:
      next_ios_build_number: <max(local_source_build_number, highest_accepted_build_number) + 1>
      next_android_version_code: <max(local_source_version_code, highest_accepted_version_code) + 1>
      rationale: "M1 D-02 lesson — bump from max(local, store) + 1, not local + 1, to avoid the M1 reactive-bump pattern"

    phase_3_4_paired_gates_prerequisite:
      status: <"clear" | "outstanding">
      detail: |
        Phases 3 + 4 are marked complete in ROADMAP but the orchestrator's additional_context flagged that
        /gsd-verify-work + /gsd-code-review have NOT been run for either phase. Per memory
        `gsd-verifier-misses-regressions.md` paired-gates discipline, this is a HARD prerequisite the user
        MUST satisfy BEFORE Plan 06-06 (manual QA matrix walk) starts. Re-run `/gsd-verify-work 3` +
        `/gsd-code-review 3` and `/gsd-verify-work 4` + `/gsd-code-review 4` and confirm both pass clean
        before resuming Phase 6. If either gate surfaces a regression, FIX FIRST, then resume.
    ---

    # Phase 6 — Store History Snapshot

    Authored by Plan 06-01 to encode the M1 D-02 lesson (RETROSPECTIVE.md Key Lesson #1) as a
    Wave-1 query-live artifact. Plan 05 reads `derived_targets.next_ios_build_number` +
    `derived_targets.next_android_version_code` as the binding values for the v2.0.0 atomic bump.
    Local source files are NOT trusted as the bump baseline — the live stores are.

    ## ASC TestFlight history (queried at <timestamp>)

    | Track | Marketing Version | Highest Build Number Accepted | Notes |
    |-------|-------------------|-------------------------------|-------|
    | Internal Testing | 1.0.4 | <N> | M1 close — RETROSPECTIVE Key Lesson #1; reactive bump 21 → 22 |
    | (any 2.0.0 prior) | 2.0.0 | <none expected; if present, FLAG> | First v2.0.0 archive should be next_ios_build_number |

    ## Play Console history (queried at <timestamp>)

    | Track | Version Name | Highest versionCode Accepted | Notes |
    |-------|--------------|------------------------------|-------|
    | Production | 1.0.29 | <N> | M1 close — reactive bump 25 → 28; later release pushed to 29 |
    | (any 2.0.x prior) | (none expected) | (none) | First v2.0.0 archive should be next_android_version_code |

    ## Derived bump targets

    - **iOS CURRENT_PROJECT_VERSION** (Plan 05 target): `<next_ios_build_number>` — both Debug and Release configs.
    - **Android versionCode** (Plan 05 target): `<next_android_version_code>`.
    - **iOS MARKETING_VERSION** (Plan 05 target): `2.0.0` — both Debug and Release configs.
    - **Android versionName** (Plan 05 target): `"2.0.0"`.
    - **package.json `version`** (Plan 05 target): `"2.0.0"`.

    ## M1 D-02 lesson application

    Per RETROSPECTIVE.md "What Was Inefficient" entry on M1 D-02: in M1 Phase 8, the planner anchored
    the bump on the local `build.gradle` value (versionCode 25). Play Console had already accepted
    versionCode 25 from a prior submission, so the bump-to-25 plan would have been rejected. The user
    authored out-of-band commit `63f3b72` to bump versionCode 25 → 28. M2 Phase 6 codifies that lesson
    as a Wave-1 step: query live, derive max(local, store) + 1, record rationale.

    ## Phase 3 + 4 paired-gates prerequisite (from orchestrator additional_context)

    Phases 3 and 4 are marked complete in ROADMAP.md (rows 168 + 171), but the orchestrator's planning
    context flagged that `/gsd-verify-work` + `/gsd-code-review` have NOT been run for either phase.
    Per auto-memory `gsd-verifier-misses-regressions.md`, paired gates are mandatory before declaring
    a phase complete. Phase 6 cannot ship a release that depends on Phases 3+4 surfaces (Moderation
    Queue actions; Archive flows) without those gates clearing.

    **Resolution:** This is documented as a HARD prerequisite the user runs OUTSIDE Plan 06's
    `/gsd-execute-phase 6` invocation, BEFORE Plan 06-06 (manual QA matrix walk) starts. Plan 06-06's
    `must_haves` includes a check for this prerequisite. If the user wants paired-gates baked into
    Phase 6 directly, the orchestrator should split this into a Phase 5.5 or insert plans 0a/0b before
    Wave 1; that decision is escalated to the user in the SUMMARY of this plan rather than executed
    autonomously here.

    ## Re-open conditions

    - If a future ASC TestFlight upload rejects the build number Plan 05 sets, re-run this plan's
      Step 1 to re-derive the next target (probably max + 1 of the rejection notice's number).
    - If Play Console rejects the versionCode, same drill against Step 2.
    ```

    Use the `Write` tool (NOT heredoc / `cat << EOF`) to create the file at the exact path declared in `<files>`. After write, run the assertions in `<acceptance_criteria>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md && grep -c 'next_ios_build_number' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md && grep -c 'next_android_version_code' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md && grep -c 'phase_3_4_paired_gates_prerequisite' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md` exits 0 (file exists)
    - `grep -c 'next_ios_build_number' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md` returns ≥ 1 (declared in frontmatter)
    - `grep -c 'next_android_version_code' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md` returns ≥ 1
    - `grep -E '^\s*next_ios_build_number:\s*[0-9]+' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md` returns a line with an INTEGER value (NOT a placeholder like `<N>` or `TBD`)
    - `grep -E '^\s*next_android_version_code:\s*[0-9]+' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md` returns a line with an INTEGER value
    - `grep -E '^\s*query_method:' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md` returns 2 lines (one for ios, one for android), each with one of the three allowed enum values (`asc_api_direct` | `asc_web_ui_user_relay` | `orchestrator_hypothesis` for ios; equivalent for android)
    - `grep -c 'phase_3_4_paired_gates_prerequisite' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md` returns ≥ 1
    - `grep -E 'rationale:' .planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md | grep -c 'M1 D-02'` returns ≥ 1 (rationale references the M1 lesson explicitly)
    - The derived `next_ios_build_number` value satisfies the formula `next_ios_build_number = max(local_source_build_number, highest_accepted_build_number) + 1`. **Under the orchestrator hypothesis (local iOS = 26, store iOS = 22), this evaluates to `27`.** If the executor re-queried and the live store has changed since 2026-04-30, the executor MUST recompute from the re-queried values, document the new derivation in the artifact's `derived_targets.rationale` field, and the integer in `derived_targets.next_ios_build_number` must equal the recomputed `max(local, store) + 1`. A correct re-queried bump must NOT trip a strict checker — the formula is the binding rule, not the integer 27.
    - The derived `next_android_version_code` value satisfies the formula `next_android_version_code = max(local_source_version_code, highest_accepted_version_code) + 1`. **Under the orchestrator hypothesis (local Android = 29, store Android = 28), this evaluates to `30`.** Same recompute-and-document rule as iOS if the live store has changed since 2026-04-30 — the formula is the binding rule, not the integer 30.
  </acceptance_criteria>
  <done>
    `.planning/phases/06-hardening-manual-physical-device-qa-release/06-STORE-HISTORY.md` exists, contains both derived targets as integers (not placeholders), declares `query_method` for each platform, encodes the M1 D-02 rationale, and surfaces the Phase 3+4 paired-gates prerequisite as a `must_haves` input for Plan 06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local source → live store | Local pbxproj/build.gradle values are NOT trusted; the live stores are the binding state |
| executor → user (Path B fallback) | If ASC API / gcloud Play Publishing API is unavailable, the executor pauses and the user reads the value from the store dashboard. The relayed integer is a trust point — record `query_method: *_user_relay` so the value's provenance is auditable |
| orchestrator hypothesis → derived target | If both Path A and Path B fail at execution time, the orchestrator-supplied hypothesis is used and ANNOTATED. Plan 05 executor must understand `query_method: orchestrator_hypothesis` means "needs verification at upload time, retry-on-reject is expected" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | derived_targets values | mitigate | Acceptance criteria assert INTEGER values (not placeholders); the integer must equal `max(local, store) + 1` per the formula recorded in the artifact's `rationale` field. The artifact is committed to git so any post-hoc tampering is auditable. |
| T-06-02 | Repudiation | query_method field | mitigate | The `query_method` enum is one of three explicit values; the artifact records WHICH path was used. If a later release rejection is traced to a stale value, the audit trail shows whether it was a query-live result or an orchestrator-hypothesis fallback. |
| T-06-03 | Information Disclosure | committed values | accept | The build numbers + versionCodes are non-sensitive (visible to anyone who downloads the binary). No credential or PII leaves the local machine via this artifact. |
| T-06-04 | DoS | ASC API rate limit | accept | One-shot query at execution time; no loop. ASC + Play Console rate limits are several hundred requests per hour; a single read does not approach them. |
| T-06-05 | Build-identity drift | bump-from-stale-baseline | mitigate | This entire plan exists to prevent T-06-05. The acceptance criteria force the executor to record the source of the value (live query vs hypothesis), so Plan 05 cannot inherit a stale baseline silently. |

Block-on severity: high.
- T-06-01 (HIGH) — blocks if mitigation absent (placeholders pass acceptance check would mean a malformed artifact reaches Plan 05)
- T-06-05 (HIGH) — blocks if mitigation absent (stale-baseline = M1 reactive-bump repeat)
</threat_model>

<verification>
- `06-STORE-HISTORY.md` file exists in the phase directory with all three required sections
- Both derived targets are concrete integers, not placeholders
- `query_method` is declared for each platform with an enum-valid value
- M1 D-02 rationale text is present
- Phase 3+4 paired-gates prerequisite block is present
- `git status` shows the new file as `untracked`; the close-out metadata commit will add it
</verification>

<success_criteria>
Plan 05 (Wave 2) can read `derived_targets.next_ios_build_number` and `derived_targets.next_android_version_code` from this file as the binding bump targets, eliminating the M1 D-02 anti-pattern (trust-local-source).
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-manual-physical-device-qa-release/06-01-SUMMARY.md` with a concise rollup: which `query_method` was used per platform, what integers landed, and whether the `phase_3_4_paired_gates_prerequisite.status` is `clear` or `outstanding` at SUMMARY time.
</output>
