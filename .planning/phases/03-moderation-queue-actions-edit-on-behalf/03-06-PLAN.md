---
phase: 03
plan: 06
type: execute
wave: 4
depends_on: [03-02, 03-04]
files_modified:
  - src/screens/PropertyDetailsScreen.tsx
  - src/screens/CreateListingScreen.tsx
  - src/components/PropertyDetailsHost.tsx
  - App.tsx
autonomous: false
requirements: [MOD-10, MOD-11, MOD-13, MOD-14, MOD-15, MOD-18]
tags: [phase-03, client, moderation, app-tsx-wireup, property-details-footer, edit-on-behalf, loc-mitigation]

must_haves:
  truths:
    - "App.tsx has isModerationQueueOpen state + setter + entry in OVERLAY_FLAGS array + back-handler branch + ModerationQueueScreen mount block"
    - "App.tsx wires onReviewModerationQueue + pendingModerationCount to ProfileScreen, onOpenPropertyDetails + onEditOnBehalf to ModerationQueueScreen"
    - "PropertyDetailsScreen renders a moderation action footer (Approve Listing / Reject Listing / Edit on behalf) when can('approveListings') AND status === 'pending'; 409 handling per PATTERNS §C; RejectListingModal mounted as sibling"
    - "CreateListingScreen accepts moderatorContext?: {editingOwnerUid, reason?, ownerEmail?} prop; when set, renders mod-context banner above form, suppresses RejectionBanner, save dispatches editAsModerator instead of updateProperty"
    - "src/components/PropertyDetailsHost.tsx extracts the App.tsx 77-LOC selectedProperty mount block (PATTERNS §App.tsx Pattern 4 / RESEARCH §9 D-15 carry-forward); App.tsx call site collapses to ~10 LOC"
    - "App.tsx final LOC ≤ 1130 (per RESEARCH net calculation: 1132 - 67 extracted + 45 added = 1110); soft cap drift is signal-not-block but extraction is recommended"
    - "client tsc baseline preserved at 2; i18n parity gate exits 0; manual smoke checkpoint walks the full flow on iPhone 15 Pro Max"
  artifacts:
    - path: "src/components/PropertyDetailsHost.tsx"
      provides: "Extracted 77-LOC selectedProperty PropertyDetailsScreen overlay block (App.tsx LOC mitigation)"
      min_lines: 80
    - path: "src/screens/PropertyDetailsScreen.tsx"
      provides: "Moderation action footer mounted when role>=mod AND status==='pending'; 409-handler + RejectListingModal sibling mount + Approve confirm Alert"
      contains: "moderation.action.approve"
    - path: "src/screens/CreateListingScreen.tsx"
      provides: "moderatorContext prop + mod-context banner + suppress RejectionBanner + save dispatches editAsModerator"
      contains: "moderatorContext"
    - path: "App.tsx"
      provides: "isModerationQueueOpen state, OVERLAY_FLAGS extension, ModerationQueueScreen mount + callback chain, PropertyDetailsHost extraction"
      contains: "isModerationQueueOpen"
  key_links:
    - from: "App.tsx"
      to: "src/screens/ModerationQueueScreen.tsx"
      via: "<ModerationQueueScreen onBack={...} onOpenPropertyDetails={...} onEditOnBehalf={...} />"
      pattern: "<ModerationQueueScreen"
    - from: "App.tsx"
      to: "src/screens/ProfileScreen.tsx"
      via: "onReviewModerationQueue={() => setIsModerationQueueOpen(true)} pendingModerationCount={...}"
      pattern: "onReviewModerationQueue"
    - from: "src/screens/PropertyDetailsScreen.tsx"
      to: "src/components/RejectListingModal.tsx"
      via: "<RejectListingModal visible={...} onClose={...} onSubmit={handleRejectSubmit} submitting={...} />"
      pattern: "<RejectListingModal"
    - from: "src/screens/CreateListingScreen.tsx"
      to: "src/services/PropertyService.ts"
      via: "PropertyService.editAsModerator(propertyId, payload, images, reason)"
      pattern: "editAsModerator"
---

<objective>
Land the FINAL plan of Phase 3: wire the standalone screens from Plan 04 into App.tsx's overlay state machine, mount the moderation action footer on PropertyDetailsScreen, layer the `moderatorContext` prop on CreateListingScreen so a moderator's edit-on-behalf save dispatches `editAsModerator` (not `updateProperty`), and apply the App.tsx LOC pressure mitigation by extracting the 77-LOC `selectedProperty` mount block into a new `<PropertyDetailsHost>` component (per RESEARCH §9 + PATTERNS §App.tsx Pattern 4).

This plan ALSO includes the manual physical-device smoke checkpoint that walks the full Phase 3 happy-path on iPhone 15 Pro Max — the moderator opens Profile, sees the badge, taps the queue, the queue lists pending listings, tap-row navigates to PropertyDetailsScreen with the action footer visible, Approve fires the confirm dialog and clears the row from queue, Reject opens the modal with chips and submits, Edit-on-behalf opens CreateListingScreen with the mod-context banner above the form, save dispatches editAsModerator and the listing flips to 'live' / disappears from queue.

Output: 1 NEW component (PropertyDetailsHost) + 3 modified files (App.tsx, PropertyDetailsScreen, CreateListingScreen). App.tsx soft-cap drift mitigated. Manual smoke checkpoint walks PASS on iPhone 15 Pro Max.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-CONTEXT.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-RESEARCH.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md
@.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-UI-SPEC.md

<interfaces>
From Plan 02 — locale keys in en.ts + ru.ts (all 19 moderation.* keys present)
From Plan 02 — useRole.ts: `can('viewModerationQueue')`, `can('approveListings')`, `can('editAnyListing')` all return admin|moderator
From Plan 02 — PropertyService.editAsModerator(propertyId, propertyData, images, reason) (multipart; defensively strips ownerUid)
From Plan 04 — `<RejectListingModal>` reusable component (props: visible, onClose, onSubmit, submitting)
From Plan 04 — `<ModerationQueueScreen>` (props: onBack, onOpenPropertyDetails, onEditOnBehalf)
From Plan 04 — ProfileScreen accepts onReviewModerationQueue + pendingModerationCount props

From `App.tsx` (1132 LOC current — read these specific sections before editing):
- Line 50: existing `const [isLandlordApplicationQueueOpen, setIsLandlordApplicationQueueOpen] = useState(false);` — Phase 3 adds the analog `isModerationQueueOpen` state below it
- Lines 104-111: OVERLAY_FLAGS array — Phase 3 adds `!!user && isModerationQueueOpen` to this array
- Lines 286-300: hardware back-handler useEffect — Phase 3 adds an `if (isModerationQueueOpen) {...}` branch
- Lines 360-362: deps array of the back-handler — Phase 3 adds `isModerationQueueOpen` to deps
- Lines 837-913: the `selectedProperty && (<View ...><PropertyDetailsScreen .../></View>)` block — 77 LOC; Phase 3 EXTRACTS this into PropertyDetailsHost.tsx
- Lines 939-971: `isCreateListingOpen` mount block — Phase 3 adds `moderatorContext` prop wiring here
- Lines 993-1008: `isLandlordApplicationQueueOpen` mount block — Phase 3 adds the analog `isModerationQueueOpen` mount block beside it

From `PropertyDetailsScreen.tsx` (1925 LOC — too large; read specifically): the bottom safe-area inset region where the action footer mounts; the `useRole()` import + `can()` usage; the existing payload refresh function (used after approve/reject for the screen to re-evaluate the action footer)

From `CreateListingScreen.tsx` (910 LOC):
- Interface at lines 46-54: existing props `onBack`, `onSuccess`, `propertyToEdit?`, `verificationOnly?`, `onNavigateToAccountSettings?` — Phase 3 adds 1 new optional prop `moderatorContext?`
- The save dispatcher in handleSubmit: existing branches `propertyToEdit ? updateProperty : createProperty` — Phase 3 adds a 3rd branch `moderatorContext ? editAsModerator : ...`
- The RejectionBanner mount site (find via grep): Phase 3 wraps with `{!moderatorContext && ...}` guard
</interfaces>

<app_tsx_loc_strategy>
Per RESEARCH.md §"9. App.tsx LOC pressure (D-15 carry-forward)" + PATTERNS.md §"MODIFIED — App.tsx" + RESEARCH.md Pattern 4:

**Recommended path: Option (a) — Extract PropertyDetailsHost.**

Net LOC math:
- App.tsx today: 1132 LOC
- Extract App.tsx:837-913 (the `selectedProperty && (...)` block): -77 LOC
- New `<PropertyDetailsHost>` call site at extraction point: +10 LOC
- Phase 3 additions to App.tsx:
  - `isModerationQueueOpen` state + setter: +1 LOC
  - `moderatorContext` state + setter (for CreateListingScreen edit-on-behalf): +1 LOC
  - OVERLAY_FLAGS entry: +1 LOC
  - Back-handler branch: +6 LOC
  - Deps array entry: +1 LOC
  - ModerationQueueScreen mount block (mirror isLandlordApplicationQueueOpen): +20 LOC
  - moderatorContext wiring in CreateListingScreen mount: +3 LOC
  - onReviewModerationQueue + onEditOnBehalf callbacks (a few LOC each): +5 LOC

Total Phase 3 adds: ~38 LOC; total Phase 3 net (with extraction): 1132 - 77 + 10 + 38 = **1103 LOC**.

This is 3 LOC over the 1100 soft cap (signal-not-block per Phase 2 PATTERN D — drift documented in SUMMARY is acceptable). If the planner finds the actual delta lower (e.g., the back-handler branch lands in 4 LOC not 6), it could land at exactly 1100 or below.

**Acceptable alternatives (planner discretion):**
- Option (b): Skip extraction, accept ~1175 LOC drift, document in SUMMARY. PITFALLS Pitfall 8 says soft cap is signal-not-block.
- Option (c): Hybrid — extract a smaller block and inline. Modest improvement.

This plan defaults to Option (a) (extract). If extraction creates unexpected callback-prop-passing churn that materially expands PropertyDetailsHost beyond 100 LOC, planner falls back to Option (b) and documents in SUMMARY. The acceptance criterion below allows either ≤1130 (extracted) OR ≤1180 (inline-only with documented drift) — but NOT both unbounded.
</app_tsx_loc_strategy>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 03-06-01: Extract PropertyDetailsHost component (App.tsx LOC mitigation)</name>
  <read_first>
    - `App.tsx` lines 837-913 (the FULL 77-LOC `selectedProperty && (<View ...><PropertyDetailsScreen .../></View>)` block — read every callback wired through, every prop, every state read)
    - `App.tsx` lines 1-105 (imports, top-level state — confirm what App.tsx exposes that the extracted component will need as props: user, homeViewMode, favoriteStatuses, favoriteLoading, callbacks)
    - `src/screens/PropertyDetailsScreen.tsx` interface (props shape)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-RESEARCH.md` §"9. App.tsx LOC pressure" extraction sketch (lines 1306-1350)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md` §"MODIFIED — App.tsx" subsection on extraction
    - `.planning/research/PITFALLS.md` Pitfall 8 (App.tsx LOC cap)
  </read_first>
  <files>src/components/PropertyDetailsHost.tsx, App.tsx</files>
  <action>
**Step A: Create `src/components/PropertyDetailsHost.tsx`** — pure pass-through wrapper that takes the previously-inlined props of the App.tsx:837-913 block and renders the same JSX.

Structure:

```
import React from 'react';
import { View, StyleSheet } from 'react-native';
import PropertyDetailsScreen from '../screens/PropertyDetailsScreen';
import type { Property } from '../types/Property';
import type { AuthUser } from '../types/Auth';

interface PropertyDetailsHostProps {
  selectedProperty: Property;
  user: AuthUser | null;
  homeViewMode: 'list' | 'map';
  favoriteStatuses: Record<string, boolean>;
  favoriteLoading: Record<string, boolean>;
  onClose: () => void;
  onOpenTours: (p: Property) => void;
  onOpenPhotos: (url: string) => void;
  onMessage: (p: Property) => void;
  onScheduleViewing: (p: Property) => void;
  onLandlordPress: (uid: string, name: string) => void;
  onAdminVerifyDocuments: (p: Property) => void;
  onEditListing: (p: Property) => void;
  onFavorite: (p: Property) => Promise<void>;
  onAuthRequired: (message: string) => void;
}

const PropertyDetailsHost: React.FC<PropertyDetailsHostProps> = (props) => {
  return (
    <View style={styles.fullScreenOverlayWrap} pointerEvents="auto">
      <PropertyDetailsScreen
        property={props.selectedProperty}
        user={props.user}
        // ... copy every prop wired in App.tsx:837-913 verbatim ...
      />
    </View>
  );
};

const styles = StyleSheet.create({
  fullScreenOverlayWrap: { position: 'absolute', top: 0, left: 0, right: 0, bottom: 0 },
});

export default PropertyDetailsHost;
```

Read the App.tsx:837-913 block in full and copy EVERY prop and EVERY callback verbatim into the host's PropertyDetailsScreen invocation. The exact prop set varies — read before authoring.

If PropertyDetailsScreen takes additional props (e.g., `themeStyles`, `t`) that come from hooks rather than App.tsx state, the host can also use those hooks directly (`useTheme()`, `useLanguage()`) inside its body — that's cleaner than threading them through props.

If the App.tsx mount block uses an inline `style` like `[fullScreenOverlayWrap, ...]` from a top-level const, replicate the same StyleSheet here. The visual result MUST be identical to the pre-extraction state — this is a refactor, NOT a behavior change.

**Step B: Update App.tsx call site** — Replace lines 837-913 with the new PropertyDetailsHost call.

Before (~77 LOC at App.tsx:837-913):
```
{selectedProperty && (
  <View style={[fullScreenOverlayWrap, { pointerEvents: 'auto' }]}>
    <PropertyDetailsScreen
      property={selectedProperty}
      user={user}
      // ... 70+ LOC of props + inline callbacks ...
    />
  </View>
)}
```

After (~10 LOC):
```
{selectedProperty && (
  <PropertyDetailsHost
    selectedProperty={selectedProperty}
    user={user}
    homeViewMode={homeViewMode}
    favoriteStatuses={favoriteStatuses}
    favoriteLoading={favoriteLoading}
    onClose={() => setSelectedProperty(null)}
    onOpenTours={handleOpenTours}
    onOpenPhotos={(url) => setActivePhotosUrl(url)}
    onMessage={(p) => { /* keep existing inline body */ }}
    onScheduleViewing={(p) => { /* keep existing inline body */ }}
    onLandlordPress={(uid, name) => { setSelectedProperty(null); setOwnerListingsUid(uid); setOwnerListingsName(name); }}
    onAdminVerifyDocuments={(p) => { skipRenterListingsReopenRef.current = true; setPropertyToEdit(p); setIsAdminVerificationMode(true); setIsCreateListingOpen(true); setSelectedProperty(null); }}
    onEditListing={(p) => { setIsAdminVerificationMode(false); setPropertyToEdit(p); setSelectedProperty(null); setIsCreateListingOpen(true); }}
    onFavorite={handleFavorite}
    onAuthRequired={(msg) => { setAuthPromptMessage(msg); setShowAuthPrompt(true); }}
  />
)}
```

The inline `onMessage` / `onScheduleViewing` callbacks may be heavy (depending on Phase 2 wiring). If they exceed ~5 LOC each, EITHER move their bodies into named functions at the top of App.tsx (zero behavior change, slight rearrangement) OR copy them verbatim as inline arrows into the host call.

After this task, App.tsx LOC count should be approximately `1132 - 77 + 10 = 1065` BEFORE Plan 06's other tasks add their lines. Run `wc -l App.tsx` after the extraction and record the count for the SUMMARY.

Do NOT change PropertyDetailsScreen.tsx itself in this task (Task 03 owns the action footer mount).
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10 ; wc -l App.tsx src/components/PropertyDetailsHost.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/PropertyDetailsHost.tsx` exists
    - `wc -l src/components/PropertyDetailsHost.tsx` returns at least 80
    - `grep -F "import PropertyDetailsScreen" src/components/PropertyDetailsHost.tsx` returns 1 hit
    - `grep -F "interface PropertyDetailsHostProps" src/components/PropertyDetailsHost.tsx` returns 1 hit
    - `grep -F "<PropertyDetailsScreen" src/components/PropertyDetailsHost.tsx` returns 1 hit
    - `grep -F "<PropertyDetailsHost" App.tsx` returns 1 hit (the new call site)
    - `grep -c "<PropertyDetailsScreen" App.tsx` returns 0 (the inline mount is GONE — moved into PropertyDetailsHost)
    - `wc -l App.tsx` returns a number BETWEEN 1050 and 1080 (after the extraction, before this plan's other tasks add their lines — proves the extraction landed cleanly)
    - `npx tsc --noEmit` reports the SAME 2 baseline errors only (no new errors from the extraction)
  </acceptance_criteria>
  <done>PropertyDetailsHost.tsx exists as a pure-passthrough wrapper; App.tsx no longer inlines the 77-LOC selectedProperty block; behavior is unchanged. tsc baseline preserved at 2. App.tsx LOC drops to ~1065 before other Plan 06 tasks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-06-02: Mount moderation action footer on PropertyDetailsScreen with 409 handling + RejectListingModal sibling</name>
  <read_first>
    - `src/screens/PropertyDetailsScreen.tsx` (find the bottom-of-screen region — likely a footer above safe-area-inset; identify the existing `useRole` import + `can()` usage; identify the existing payload-refresh function used after Phase 2's owner-edit auto-flip)
    - `src/components/RejectListingModal.tsx` (Plan 04 component — props shape)
    - `src/services/PropertyService.ts` (Plan 02 added approveListing, rejectListing, getModerationQueueCount methods)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-UI-SPEC.md` §"Interaction Contracts > Moderation action footer on <PropertyDetailsScreen>"
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md` §MODIFIED—`src/screens/PropertyDetailsScreen.tsx` AND §C "409 race-condition UX"
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-RESEARCH.md` Pattern §C 409 UX
  </read_first>
  <files>src/screens/PropertyDetailsScreen.tsx</files>
  <action>
Add a moderation action footer (3-button row: Approve Listing / Reject Listing / Edit on behalf) that mounts above the safe-area bottom inset when `can('approveListings')` AND `property.status === 'pending'`. Plus mount `<RejectListingModal>` as a sibling, controlled by a local visibility state.

**Step A: Add state and handlers** — In the component body (alongside existing state):

```
const [isRejectModalOpen, setIsRejectModalOpen] = useState(false);
const [submittingAction, setSubmittingAction] = useState(false);

const showModFooter = can('approveListings') && property?.status === 'pending';

const handleRaceConflict = async () => {
  setIsRejectModalOpen(false);
  Alert.alert('', t('moderation.race.toast'));
  // Refetch the property — status may now be 'live' or 'rejected' which auto-hides the footer
  if (onRefreshProperty) await onRefreshProperty();
};

const handleApprove = () => {
  Alert.alert(
    t('moderation.approve.confirmTitle'),
    t('moderation.approve.confirmMessage'),
    [
      { text: t('common.cancel'), style: 'cancel' },
      {
        text: t('moderation.action.approve'),
        onPress: async () => {
          setSubmittingAction(true);
          try {
            await PropertyService.approveListing(property.id);
            if (onRefreshProperty) await onRefreshProperty();
          } catch (err: any) {
            if (err?.response?.status === 409) {
              await handleRaceConflict();
              return;
            }
            Alert.alert(t('common.error'), err?.response?.data?.message || err?.message || '');
          } finally {
            setSubmittingAction(false);
          }
        },
      },
    ]
  );
};

const handleRejectSubmit = async (params: { reasonCode: any; reasonNote?: string }) => {
  setSubmittingAction(true);
  try {
    await PropertyService.rejectListing(property.id, params.reasonCode, params.reasonNote);
    setIsRejectModalOpen(false);
    if (onRefreshProperty) await onRefreshProperty();
  } catch (err: any) {
    if (err?.response?.status === 409) {
      await handleRaceConflict();
      return;
    }
    Alert.alert(t('common.error'), err?.response?.data?.message || err?.message || '');
  } finally {
    setSubmittingAction(false);
  }
};

const handleEditOnBehalf = () => {
  if (onEditOnBehalfPressed) onEditOnBehalfPressed(property);
};
```

**Step B: Add the new optional callback prop** — `onEditOnBehalfPressed?: (property: Property) => void` — App.tsx Plan 06 Task 04 wires this to set moderatorContext + open CreateListingScreen.

Also add `onRefreshProperty?: () => Promise<void>` if PropertyDetailsScreen does not already receive one. If a refresh callback already exists with a different name, reuse it.

**Step C: Footer JSX** — Mount near the bottom of the screen, above the safe-area inset, conditional on `showModFooter`:

```
{showModFooter && (
  <View style={[styles.modActionFooter, { borderTopColor: colors.border, backgroundColor: colors.surface }]}>
    <TouchableOpacity
      style={[styles.actionBtn, { backgroundColor: colors.success }]}
      onPress={handleApprove}
      disabled={submittingAction}
      activeOpacity={0.7}
    >
      {submittingAction
        ? <ActivityIndicator color="#FFF" size="small" />
        : (<><Check size={18} color="#FFF" /><Text style={styles.actionBtnText}>{t('moderation.action.approve')}</Text></>)}
    </TouchableOpacity>
    <TouchableOpacity
      style={[styles.actionBtn, { backgroundColor: colors.error }]}
      onPress={() => setIsRejectModalOpen(true)}
      disabled={submittingAction}
      activeOpacity={0.7}
    >
      <X size={18} color="#FFF" />
      <Text style={styles.actionBtnText}>{t('moderation.action.reject')}</Text>
    </TouchableOpacity>
    <TouchableOpacity
      style={[styles.actionBtn, { backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border }]}
      onPress={handleEditOnBehalf}
      disabled={submittingAction}
      activeOpacity={0.7}
    >
      <Edit3 size={18} color={colors.text} />
      <Text style={[styles.actionBtnText, { color: colors.text }]}>{t('moderation.action.editOnBehalf')}</Text>
    </TouchableOpacity>
  </View>
)}

<RejectListingModal
  visible={isRejectModalOpen}
  onClose={() => setIsRejectModalOpen(false)}
  onSubmit={handleRejectSubmit}
  submitting={submittingAction}
/>
```

**Step D: Add the styles** — Add to the existing StyleSheet:

```
modActionFooter: {
  flexDirection: 'row', gap: 8, padding: 16,
  borderTopWidth: 1,
},
actionBtn: {
  flex: 1, flexDirection: 'row', alignItems: 'center', justifyContent: 'center',
  paddingVertical: 12, borderRadius: 10, gap: 6,
},
actionBtnText: { color: '#FFF', fontSize: 14, fontWeight: '600', lineHeight: 20 },
```

(Same actionBtn style as ModerationQueueScreen — UI-SPEC §"Spacing Scale > Action footer geometry on PropertyDetailsScreen" specifies the same shape.)

**Step E: Add imports** — Add `Check, X, Edit3` to the lucide-react-native import; add `RejectListingModal` import; add `PropertyService` import if not present; add `useState` if not present; verify `useRole` / `useLanguage` imports.

Key invariants:
- The footer renders ONLY when role>=moderator AND status==='pending' (belt-and-suspenders with backend Plan 03's role gate; PATTERNS §A).
- The footer auto-hides after a successful approve/reject because `onRefreshProperty` updates the screen's property state and `status === 'pending'` becomes false.
- 409 handling: close any open modal, then fire `Alert.alert('', t('moderation.race.toast'))`, then refetch (PATTERNS §C).
- The Approve confirm uses native `Alert.alert` (D-05); the Reject opens the modal extracted in Plan 04.
- Edit-on-behalf is dispatched via the `onEditOnBehalfPressed` callback prop — App.tsx Task 04 wires it to set moderatorContext + open CreateListingScreen (the actual nav happens at the App.tsx layer, NOT inside PropertyDetailsScreen).
- `submittingAction` flag disables all 3 buttons during any in-flight call to prevent double-submit.

Do NOT change unrelated parts of PropertyDetailsScreen.tsx. The screen is 1925 LOC; this task adds ~80-100 LOC.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "showModFooter" src/screens/PropertyDetailsScreen.tsx` returns at least 2 hits (definition + JSX guard)
    - `grep -F "can('approveListings') && property?.status === 'pending'" src/screens/PropertyDetailsScreen.tsx` returns 1 hit
    - `grep -F "PropertyService.approveListing(" src/screens/PropertyDetailsScreen.tsx` returns 1 hit
    - `grep -F "PropertyService.rejectListing(" src/screens/PropertyDetailsScreen.tsx` returns 1 hit
    - `grep -F "<RejectListingModal" src/screens/PropertyDetailsScreen.tsx` returns 1 hit
    - `grep -F "t('moderation.action.approve')" src/screens/PropertyDetailsScreen.tsx` returns at least 1 hit
    - `grep -F "t('moderation.action.reject')" src/screens/PropertyDetailsScreen.tsx` returns at least 1 hit
    - `grep -F "t('moderation.action.editOnBehalf')" src/screens/PropertyDetailsScreen.tsx` returns at least 1 hit
    - `grep -F "t('moderation.approve.confirmTitle')" src/screens/PropertyDetailsScreen.tsx` returns 1 hit
    - `grep -F "t('moderation.race.toast')" src/screens/PropertyDetailsScreen.tsx` returns 1 hit
    - `grep -F "err?.response?.status === 409" src/screens/PropertyDetailsScreen.tsx` returns at least 2 hits (approve + reject 409 paths)
    - `grep -F "onEditOnBehalfPressed" src/screens/PropertyDetailsScreen.tsx` returns at least 2 hits (prop interface + handler)
    - `grep -F "modActionFooter" src/screens/PropertyDetailsScreen.tsx` returns at least 2 hits (style + JSX)
    - `npx tsc --noEmit` reports the same 2 baseline errors only
  </acceptance_criteria>
  <done>PropertyDetailsScreen renders the 3-button moderation footer when role>=mod AND status==='pending'; Approve fires the Alert.alert confirm + races correctly; Reject opens the new RejectListingModal sibling; Edit on behalf dispatches via onEditOnBehalfPressed callback. Footer auto-hides after successful action via onRefreshProperty. tsc baseline preserved at 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-06-03: Add moderatorContext prop + mod-context banner + save dispatcher branch to CreateListingScreen</name>
  <read_first>
    - `src/screens/CreateListingScreen.tsx` (FULL file — 910 LOC; specifically: interface at lines 46-54; the propertyToEdit rehydrate useEffect; the handleSubmit dispatcher; any RejectionBanner mount site inside the form)
    - `src/components/RejectionBanner.tsx` (Phase 2 component shape — for the suppression guard)
    - `src/services/PropertyService.ts` (Plan 02's editAsModerator method signature)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-UI-SPEC.md` §"Interaction Contracts > Mod-context banner on <CreateListingScreen>" + §"Spacing Scale > Mod-context banner geometry"
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md` §MODIFIED—`src/screens/CreateListingScreen.tsx`
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-RESEARCH.md` §"7. D-12 status-after-save for edit-on-behalf"
  </read_first>
  <files>src/screens/CreateListingScreen.tsx</files>
  <action>
Three additive changes:

**Change 1: Props interface extension** — Add 1 new optional prop:

```
interface CreateListingScreenProps {
  // ... existing props (onBack, onSuccess, propertyToEdit?, verificationOnly?, onNavigateToAccountSettings?) ...
  // Phase 3 addition:
  moderatorContext?: { editingOwnerUid: string; reason?: string; ownerEmail?: string };
}
```

**Change 2: Mod-context banner JSX** — Mount at the TOP of the form's ScrollView (above all sections) when `moderatorContext` is set. Visual: same shell shape as Phase 2 RejectionBanner with warning palette per UI-SPEC §"Mod-context banner geometry":

```
{moderatorContext && (
  <View style={[styles.modContextBanner, { backgroundColor: colors.surface, borderColor: colors.border }]}>
    <View style={[styles.modContextStripe, { backgroundColor: colors.warning }]} />
    <View style={styles.modContextBody}>
      <Text style={[styles.modContextTitle, { color: colors.text }]}>
        {t('moderation.editOnBehalf.banner').replace('{ownerEmail}', moderatorContext.ownerEmail || moderatorContext.editingOwnerUid)}
      </Text>
      {moderatorContext.reason && (
        <Text style={[styles.modContextSubtitle, { color: colors.textSecondary }]} numberOfLines={3}>
          {moderatorContext.reason}
        </Text>
      )}
    </View>
  </View>
)}
```

Add styles:

```
modContextBanner: {
  flexDirection: 'row', borderRadius: 14, borderWidth: 1, marginBottom: 16, overflow: 'hidden',
},
modContextStripe: { width: 4 },
modContextBody: { flex: 1, padding: 12, paddingHorizontal: 16 },
modContextTitle: { fontSize: 14, fontWeight: '600', lineHeight: 20 },
modContextSubtitle: { fontSize: 13, fontWeight: '400', lineHeight: 18, marginTop: 4 },
```

**Change 3: Suppress RejectionBanner when moderatorContext is set** — Find the RejectionBanner mount inside CreateListingScreen (if it exists from Phase 2) and wrap with the suppression guard:

```
{!moderatorContext && propertyToEdit?.status === 'rejected' && (
  <RejectionBanner ... />
)}
```

If the existing CreateListingScreen does not currently mount a RejectionBanner inside the form (the mount may live ONLY on PropertyDetailsScreen per Phase 2 D-13), then this step is a no-op. Document in the SUMMARY.

**Change 4: Save dispatcher branch** — Find the existing handleSubmit function. Locate the existing `propertyToEdit ? updateProperty : createProperty` decision. Add a 3rd branch for moderatorContext:

```
if (moderatorContext) {
  // Edit-on-behalf — dispatches editAsModerator (NOT updateProperty per MOD-14)
  result = await PropertyService.editAsModerator(
    propertyToEdit!.id,
    payload,
    selectedImages,
    moderatorContext.reason,
  );
} else if (propertyToEdit) {
  // Existing owner-edit branch
  result = await PropertyService.updateProperty(propertyToEdit.id, payload, selectedImages);
} else {
  // Existing create branch
  result = await PropertyService.createProperty(payload, selectedImages);
}
```

After save success, fire `Alert.alert('', t('moderation.editOnBehalf.success'))` (the "Listing updated and approved." toast) when in moderatorContext mode, then call `onSuccess()`.

**Change 5: Imports** — Add `useTheme` (if not present), verify `t()` is in scope. No new lucide icons needed.

Key invariants:
- The mod-context banner mounts ONLY when `moderatorContext` is truthy. Owner self-edit remains unchanged.
- The banner's color stripe is `colors.warning` (NOT error — moderator context is informational/cautionary, not destructive per UI-SPEC §"Color > warning row").
- The save dispatcher's 3-branch order (moderatorContext > propertyToEdit > create) — moderatorContext takes precedence even when propertyToEdit is set (the mod IS editing an existing property).
- `editAsModerator` strips ownerUid + status defensively (Plan 02), and the backend Plan 05 strips them again (belt-and-suspenders).
- The "Listing updated and approved." toast in moderatorContext mode signals the D-12 status-after-save semantics to the moderator (status flipped to 'live').

Do NOT touch unrelated form fields, validation logic, or the existing rehydrate useEffect for propertyToEdit. Additive only.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "moderatorContext?: { editingOwnerUid: string;" src/screens/CreateListingScreen.tsx` returns 1 hit
    - `grep -F "PropertyService.editAsModerator(" src/screens/CreateListingScreen.tsx` returns 1 hit
    - `grep -F "if (moderatorContext)" src/screens/CreateListingScreen.tsx` returns at least 1 hit (dispatcher branch)
    - `grep -F "t('moderation.editOnBehalf.banner')" src/screens/CreateListingScreen.tsx` returns 1 hit
    - `grep -F "{ownerEmail}" src/screens/CreateListingScreen.tsx` returns at least 1 hit (placeholder replacement)
    - `grep -F ".replace('{ownerEmail}'," src/screens/CreateListingScreen.tsx` returns 1 hit
    - `grep -F "modContextBanner" src/screens/CreateListingScreen.tsx` returns at least 2 hits (style + JSX)
    - `grep -F "colors.warning" src/screens/CreateListingScreen.tsx` returns at least 1 hit (banner stripe)
    - `grep -F "t('moderation.editOnBehalf.success')" src/screens/CreateListingScreen.tsx` returns 1 hit (success toast)
    - `npx tsc --noEmit` reports the same 2 baseline errors only
  </acceptance_criteria>
  <done>CreateListingScreen accepts moderatorContext prop; renders mod-context banner above form when set; suppresses RejectionBanner when in mod-context (or no-op if Phase 2 didn't mount one inside the form); save dispatcher's 3-branch correctly routes to editAsModerator with editing-owner-uid + reason. tsc baseline preserved at 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-06-04: App.tsx wireup — state + OVERLAY_FLAGS + back-handler + ModerationQueueScreen mount + callback chain</name>
  <read_first>
    - `App.tsx` lines 1-130 (imports + top-level state — Plan 02-08's wire-up established many state slots; identify exactly where Phase 3 additions slot in)
    - `App.tsx` lines 100-115 (OVERLAY_FLAGS array — verify the post-extraction-from-Task-01 line numbers)
    - `App.tsx` lines 280-370 (back-handler useEffect + deps array)
    - `App.tsx` lines 935-1010 (the create-listing + landlord-application-queue mount blocks — Phase 3 mounts ModerationQueueScreen alongside)
    - `src/screens/ProfileScreen.tsx` (Plan 04 added onReviewModerationQueue + pendingModerationCount props)
    - `src/screens/ModerationQueueScreen.tsx` (Plan 04 — props onBack + onOpenPropertyDetails + onEditOnBehalf)
    - `src/screens/CreateListingScreen.tsx` (Task 03 added moderatorContext prop)
    - `src/screens/PropertyDetailsScreen.tsx` (Task 02 added onEditOnBehalfPressed prop)
    - `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-PATTERNS.md` §"MODIFIED — App.tsx" full subsection
  </read_first>
  <files>App.tsx</files>
  <action>
Apply 7 additive changes to App.tsx, mirroring the Phase 4.5 isLandlordApplicationQueueOpen wiring pattern.

**Change 1: New state (placement: alongside `isLandlordApplicationQueueOpen` at ~line 50):**

```
const [isModerationQueueOpen, setIsModerationQueueOpen] = useState(false);
const [moderatorContext, setModeratorContext] = useState<{ editingOwnerUid: string; reason?: string; ownerEmail?: string } | null>(null);
const [pendingModerationCount, setPendingModerationCount] = useState<number>(0);
```

**Change 2: Pending-count fetch effect** — Add a useEffect that fetches when the user is mod/admin AND on AppState 'active'. Mirror the Plan 04 ProfileScreen self-fetch but at App.tsx level so the count is available everywhere:

```
const userType = user?.backendProfile?.userType;
const canViewModerationQueue = userType === 'admin' || userType === 'moderator';

useEffect(() => {
  if (!canViewModerationQueue) {
    setPendingModerationCount(0);
    return;
  }
  let cancelled = false;
  (async () => {
    try {
      const count = await PropertyService.getModerationQueueCount();
      if (!cancelled) setPendingModerationCount(count);
    } catch { /* non-fatal */ }
  })();
  return () => { cancelled = true; };
}, [canViewModerationQueue, isModerationQueueOpen]);    // refetch when queue closes
```

(If App.tsx already has a similar pattern for landlord-application count, mirror that. The "refetch when queue closes" dep handles the auto-refetch after a moderator's own approve/reject.)

**Change 3: OVERLAY_FLAGS extension** — Add `!!user && isModerationQueueOpen` to the array (line ~104-110). The array already contains entries like `isRenterListingsOpen`, `!!user && isCreateListingOpen`, etc. Phase 3 adds one entry per CONTEXT.md "Anti-Patterns to Avoid" rule (every overlay flag MUST land here).

**Change 4: Back-handler branch** — In the existing back-handler useEffect (~lines 286-300), add an `if (isModerationQueueOpen) {...}` branch BEFORE the broader fall-through:

```
if (isModerationQueueOpen) {
  setIsModerationQueueOpen(false);
  return true;
}
```

And in the deps array (~line 360-362) add `isModerationQueueOpen`.

**Change 5: ModerationQueueScreen mount block** (placement: alongside the isLandlordApplicationQueueOpen block at ~lines 993-1008):

```
{!!user && isModerationQueueOpen && (
  <View style={[fullScreenOverlayWrap, { pointerEvents: 'auto' }]}>
    <ModerationQueueScreen
      onBack={() => setIsModerationQueueOpen(false)}
      onOpenPropertyDetails={(p) => {
        setSelectedProperty(p);
        // Keep the queue overlay open underneath; closing the detail screen returns to it.
      }}
      onEditOnBehalf={(p) => {
        setIsModerationQueueOpen(false);
        setPropertyToEdit(p);
        setModeratorContext({
          editingOwnerUid: p.ownerUid,
          ownerEmail: p.ownerEmail,    // if present in Property type; else use uid
        });
        setIsCreateListingOpen(true);
      }}
    />
  </View>
)}
```

**Change 6: Wire ModerationQueue callbacks to ProfileScreen** — In the existing ProfileScreen JSX, add the 2 new props:

```
<ProfileScreen
  // ... existing props ...
  onReviewModerationQueue={canViewModerationQueue ? () => setIsModerationQueueOpen(true) : undefined}
  pendingModerationCount={pendingModerationCount}
/>
```

**Change 7: Wire moderatorContext to CreateListingScreen mount block** (~line 939-971):

Find the existing `<CreateListingScreen ... />` invocation. Add 2 props:

```
<CreateListingScreen
  // ... existing props ...
  moderatorContext={moderatorContext}
  onSuccess={() => {
    // Existing onSuccess body — keep as-is
    // Plus: clear moderatorContext on success
    setModeratorContext(null);
    setIsCreateListingOpen(false);
  }}
  onBack={() => {
    setModeratorContext(null);    // also clear on cancel
    setIsCreateListingOpen(false);
    // ... existing onBack body ...
  }}
/>
```

The exact onSuccess and onBack body depends on what existed pre-edit; preserve all existing behavior, just ADD the moderatorContext clear at the top.

**Change 8: Wire onEditOnBehalfPressed to PropertyDetailsHost** — In the PropertyDetailsHost call site (created in Task 01), add the new prop:

```
<PropertyDetailsHost
  // ... existing props ...
  onEditOnBehalfPressed={(p) => {
    setSelectedProperty(null);
    setPropertyToEdit(p);
    setModeratorContext({
      editingOwnerUid: p.ownerUid,
      ownerEmail: p.ownerEmail,
    });
    setIsCreateListingOpen(true);
  }}
/>
```

(This requires PropertyDetailsHost.tsx to also forward the `onEditOnBehalfPressed` prop down to PropertyDetailsScreen — update PropertyDetailsHost.tsx interface AND its <PropertyDetailsScreen onEditOnBehalfPressed={...}> call.)

**Imports:** Add to App.tsx:
```
import ModerationQueueScreen from './src/screens/ModerationQueueScreen';
import PropertyService from './src/services/PropertyService';
```

**Final LOC check:** After all 8 changes, run `wc -l App.tsx`. With Task 01's extraction (-77 LOC) and Task 04's additions (+~38 LOC), the expected delta is ~-39 LOC from the 1132 baseline → ~1093 LOC. If the file lands above 1130, document the drift in SUMMARY (signal-not-block per Phase 2 PATTERN D); if it lands above 1180, surface a checkpoint warning to user.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/JayTap &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -15 ; wc -l App.tsx ; bash scripts/check-i18n-parity.sh; echo "PARITY=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "isModerationQueueOpen, setIsModerationQueueOpen" App.tsx` returns 1 hit (state declaration)
    - `grep -F "moderatorContext, setModeratorContext" App.tsx` returns 1 hit
    - `grep -F "pendingModerationCount, setPendingModerationCount" App.tsx` returns 1 hit
    - `grep -F "PropertyService.getModerationQueueCount()" App.tsx` returns at least 1 hit
    - `grep -F "!!user && isModerationQueueOpen" App.tsx` returns at least 1 hit (OVERLAY_FLAGS entry)
    - `grep -F "if (isModerationQueueOpen)" App.tsx` returns at least 1 hit (back-handler branch)
    - `grep -F "<ModerationQueueScreen" App.tsx` returns 1 hit (mount block)
    - `grep -F "onReviewModerationQueue={" App.tsx` returns 1 hit (ProfileScreen wiring)
    - `grep -F "pendingModerationCount={pendingModerationCount}" App.tsx` returns 1 hit
    - `grep -F "moderatorContext={moderatorContext}" App.tsx` returns 1 hit (CreateListingScreen wiring)
    - `grep -F "onEditOnBehalfPressed={" App.tsx` returns at least 1 hit (PropertyDetailsHost wiring)
    - `grep -F "import ModerationQueueScreen" App.tsx` returns 1 hit
    - `wc -l App.tsx` returns at most 1180 (acceptable drift; ≤1130 with extraction is preferred)
    - `npx tsc --noEmit` reports the same 2 baseline errors only
    - `bash scripts/check-i18n-parity.sh` exits 0 (PARITY=0)
  </acceptance_criteria>
  <done>App.tsx has full Phase 3 wiring: state + OVERLAY_FLAGS entry + back-handler branch + ModerationQueueScreen mount + ProfileScreen entry-point wiring + CreateListingScreen moderatorContext wiring + PropertyDetailsHost onEditOnBehalfPressed wiring. The full callback chain is in place. tsc baseline preserved at 2; i18n parity gate exits 0. App.tsx LOC delta documented in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 03-06-05: Manual physical-device smoke (iPhone 15 Pro Max)</name>
  <what-built>
Phase 3 end-to-end happy-path: moderator opens app, sees pending-count badge on Profile, taps the moderation entry-point, queue lists pending listings (FIFO), inline Approve/Reject/Edit-on-behalf buttons work + tap-row navigates to PropertyDetailsScreen which shows the action footer. Approve fires native confirm dialog and clears the row. Reject opens the modal with 4 chips + free-text note + Reject Listing submit. Edit-on-behalf opens CreateListingScreen with the warning-stripe mod-context banner above the form; save dispatches editAsModerator and the listing flips to 'live' / disappears from queue. RejectionBanner on the owner's PropertyDetailsScreen shows the localized rejection reason in the OWNER's locale (not the moderator's locale).
  </what-built>
  <how-to-verify>
**Pre-conditions:**
- Backend deployed to Railway (Plans 01, 03, 05 backend changes live).
- iPhone 15 Pro Max app build with all Phase 3 client changes (Plans 02, 04, 06).
- Two test accounts: an admin (`beckprograms@gmail.com` per auto-memory) and a regular renter account.
- A few pending listings in the queue (create them as the renter — Phase 2 status='pending' default makes new submissions land here automatically).

**Walk this matrix on iPhone 15 Pro Max (and ideally Moto G XT2513V too — but iPhone is the gating device):**

1. **Profile entry-point + badge:**
   - Sign in as admin → Profile screen → confirm "Moderation Queue" / «Очередь модерации» row exists with `(N)` badge in red where N === pending-count
   - Sign in as a regular renter → Profile screen → confirm the row is HIDDEN
2. **Queue screen:**
   - As admin, tap the moderation row → ModerationQueueScreen opens as overlay (BottomNav hidden — auto-memory `nav-overlay-hides-bottom-nav.md`)
   - Confirm listings render in FIFO order (oldest pending first)
   - Confirm StatusPill on each PropertyCard reads «На модерации» (RU) / "Pending review" (EN)
   - Pull-to-refresh — confirm spinner appears and list reloads
   - Background the app for >60s, then foreground → confirm queue auto-refreshes (AppState refetch)
3. **Approve flow (single-tap + confirm):**
   - Tap the green "Approve Listing" inline button on a row → native Alert.alert appears with title "Approve this listing?" / «Одобрить это объявление?» + body "This listing will become live and visible to all users." + Cancel + Approve
   - Tap Approve → row disappears from queue + badge count decrements + listing now appears in the renter app's main listings (status='live')
4. **Reject flow (modal):**
   - Tap the red "Reject Listing" inline button → modal opens with 4 chips horizontally: Incomplete information / Prohibited content / Out of service area / Other; first chip pre-selected (incomplete-info)
   - Confirm the multiline note input has placeholder "Add a note (optional)"
   - Type a 50-char note; tap a different chip → highlight changes
   - Tap "Reject Listing" submit → modal closes; row disappears from queue
   - Sign in as the listing's OWNER → confirm RejectionBanner shows on PropertyDetailsScreen with the localized reason ("Prohibited content" / «Запрещённое содержимое» — depending on owner's app locale, NOT moderator's locale)
   - Confirm the banner reads either "JayTap moderator" / «Модератор JayTap» or no actor name AT ALL — never the moderator's email or uid
5. **Edit-on-behalf flow:**
   - As admin, in the queue, tap "Edit on behalf" on a row → CreateListingScreen opens
   - Confirm: warning-stripe banner at the TOP of the form reading "Editing on behalf of {ownerEmail}" / «Редактирование от имени {ownerEmail}»
   - Confirm: any RejectionBanner that would normally appear inside the form is SUPPRESSED
   - Make a small edit (e.g., change the title) and tap Save → toast shows "Listing updated and approved." / «Объявление обновлено и одобрено.» → form closes → listing now appears in the live listings (status='live'), NOT in the moderation queue
6. **PropertyDetailsScreen action footer:**
   - From the queue, tap a row → PropertyDetailsScreen opens with the listing details
   - Confirm: bottom-pinned 3-button footer is visible with green Approve Listing / red Reject Listing / neutral Edit on behalf (when status=='pending')
   - Tap any button → same flows as #3-5 above
7. **409 race-condition (manual two-device test if a second device is available; otherwise skip and rely on Plan 05 supertest):**
   - On Device A and Device B (both signed in as different mods), open the same pending listing's PropertyDetailsScreen
   - On Device A, tap Approve → confirm
   - On Device B (within ~5 seconds), tap Approve → expected: native Alert.alert toast "This listing was already reviewed by another moderator." / «Это объявление уже рассмотрено другим модератором.»
   - Device B's screen status pill auto-updates from "Pending review" to "Live" (or footer auto-hides)
8. **EN+RU locale parity walk:**
   - Switch app language to RU (in Account Settings)
   - Walk steps 1-6 again — confirm every Phase 3 string is in Russian (no raw key strings like `moderation.queue.title` showing)
9. **Dark/light parity:**
   - Switch to dark mode on iOS → walk steps 1-6 again, confirm visual contrast is acceptable (chip selected-text color, accent badge text, button labels all readable)

**Pass criteria:**
- All 9 matrix steps complete without crashes, layout regressions, or raw-key-string surfaces
- The 409 toast appears on the loser device with the verbatim copy
- The owner-locale rejection translation works in both EN and RU
- The mod-context banner on CreateListingScreen has the warning stripe (not error red)
- App.tsx final LOC delta is documented (whichever number landed)

**Expected duration:** ~20-30 minutes for the full matrix.
  </how-to-verify>
  <resume-signal>Type "approved" if all matrix steps pass. Otherwise describe the failure (which step, what went wrong, screenshot path if applicable) and the executor will produce a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.tsx state -> ModerationQueueScreen / PropertyDetailsScreen / CreateListingScreen | Callback chain; no auth boundary at this layer |
| Approve/Reject buttons -> Backend | Action footer renders only if can('approveListings') AND status==='pending'; backend Plan 03/05 enforce role gate |
| Edit-on-behalf -> editAsModerator endpoint | moderatorContext prop carries editingOwnerUid; PropertyService strips ownerUid client-side; backend strips again |
| Owner's RejectionBanner -> reasonCode i18n | Owner's app locale drives translation; no backend Accept-Language plumbing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Information Disclosure | Moderator's identity surfaces in mod-context banner on owner's device | mitigate | The mod-context banner mounts ONLY inside CreateListingScreen when `moderatorContext` prop is set. The prop is set ONLY by App.tsx Task 04's wiring (queue → edit-on-behalf or detail-screen → edit-on-behalf), both of which require `can('approveListings')`. Owner self-edit never triggers the banner. The banner exposes the OWNER's email TO THE MODERATOR (the moderator needs context); it does NOT expose the MODERATOR's email to the owner. MOD-13 invariant maintained. |
| T-03-06-02 | Tampering | moderatorContext prop tampered to pass non-mod's payload | mitigate | App.tsx Task 04 wiring sets moderatorContext only inside `setModeratorContext(...)` calls that fire AFTER the moderator role gate. PropertyService.editAsModerator (Plan 02) calls canFromUser('editAnyListing') which throws PermissionDeniedError for non-mod. Backend Plan 05 PUT /listings/:id requires moderator role. Three-tier defense. |
| T-03-06-03 | Elevation of Privilege | Plain user sees the moderation action footer on PropertyDetailsScreen | mitigate | The footer's render condition is `can('approveListings') && status === 'pending'`. canFromUser returns false for role==='user'/guest. Backend Plan 03 returns 403 if a malicious client somehow triggers the action. |
| T-03-06-04 | Race / 409 | Owner is editing a rejected listing while a mod approves it | mitigate | The atomic findOneAndUpdate on the backend (Plan 05) handles this — if the owner's PUT lands first and flips status to 'pending' (Phase 2 D-22 auto-flip), the mod's edit-on-behalf still works (filter is $in: ['pending','rejected']). If the mod's approve lands first and flips to 'live', the owner's PUT returns 409 ALREADY_MODERATED. UI surfaces the verbatim toast on either side. |
| T-03-06-05 | DoS | App.tsx LOC blowup re-introduces M1 nav bugs | mitigate | This plan applies the PropertyDetailsHost extraction per Pattern 4. Net LOC drop projected at ~-39 from baseline. Acceptance criterion caps at 1180; preferred ≤1130. |
| T-03-06-06 | Tampering / Auth | onEditOnBehalfPressed callback in App.tsx is unconditionally wired | accept | The callback fires only after the user clicks the button; the button only renders when can('approveListings'). The callback itself doesn't need a re-check. PropertyService.editAsModerator's canFromUser throws if the role is somehow stale. |
</threat_model>

<verification>
After all 5 tasks complete, run from `/Users/beckmaldinVL/development/mobileApps/JayTap`:

```
npx tsc --noEmit 2>&1 | tail -10
bash scripts/check-i18n-parity.sh; echo "PARITY=$?"
wc -l App.tsx src/components/PropertyDetailsHost.tsx
```

Expected:
- tsc baseline 2 errors only
- PARITY=0
- App.tsx LOC ≤ 1180 (preferred ≤1130)
- PropertyDetailsHost.tsx ≥ 80 LOC

Backend smoke test (run from /Users/beckmaldinVL/development/mobileApps/backend-services/JayTap-services):

```
nvm use 24
npm test 2>&1 | tail -10
```

Expected: 75+ tests passing (Plan 05's harness still green; Plan 06 didn't change backend code).

Manual smoke checkpoint (Task 05) walks the 9-step matrix on iPhone 15 Pro Max — must approve before this plan declares Phase 3 complete.
</verification>

<success_criteria>
- 1 NEW component (PropertyDetailsHost.tsx) extracted from App.tsx:837-913
- App.tsx wires Phase 3: isModerationQueueOpen state, OVERLAY_FLAGS entry, back-handler branch, ModerationQueueScreen mount, ProfileScreen onReviewModerationQueue + pendingModerationCount, CreateListingScreen moderatorContext, PropertyDetailsHost onEditOnBehalfPressed
- PropertyDetailsScreen renders the 3-button moderation action footer when role≥mod AND status==='pending'; 409 handling per PATTERNS §C; RejectListingModal mounted as sibling
- CreateListingScreen accepts moderatorContext prop + mounts mod-context banner with warning stripe + suppresses RejectionBanner + dispatches editAsModerator on save
- App.tsx LOC ≤ 1180 (preferred ≤ 1130); the soft-cap drift is documented in SUMMARY
- tsc baseline preserved at 2 errors
- i18n parity gate exits 0
- Manual smoke checkpoint (Task 05) walks PASS on iPhone 15 Pro Max with all 9 matrix steps green
</success_criteria>

<output>
After completion, create `.planning/phases/03-moderation-queue-actions-edit-on-behalf/03-06-SUMMARY.md` documenting:
- Final App.tsx LOC delta (with vs without extraction)
- PropertyDetailsHost.tsx LOC + the props it forwards
- The full Phase 3 callback chain (Profile → App.tsx → ModerationQueueScreen → App.tsx → PropertyDetailsHost → PropertyDetailsScreen → App.tsx → CreateListingScreen)
- The 9 manual-smoke matrix steps walked PASS (or any FAIL with screenshot path + repro)
- Confirmation that all 19 moderation.* locale keys are consumed end-to-end
- Confirmation that the 3-tier defense for moderator-actions (client `<Gated>` + service `canFromUser` + backend `requireMinRole`) is observable in the rendered UI
- Any deviations and rationale
- Phase 3 phase-exit checklist (delegate to Plan 06's SUMMARY OR create a sibling 03-PHASE-EXIT.md per orchestrator preference)
</output>
